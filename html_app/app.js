const FIXTURE_CATALOGUE = {
  Troffer: {
    "2X2": {
      T8: [
        {
          desc: 'Troffer 2X2, (1) 17W 24" T8 - Prismatic',
          watts: 20,
        },
        {
          desc: 'Troffer 2X2, (2) 17W 24" T8 - Prismatic',
          watts: 33,
        },
        {
          desc: 'Troffer 2X2, (3) 17W 24" T8 - Prismatic',
          watts: 47,
        },
        {
          desc: 'Troffer 2X2, (4) 17W 24" T8 - Prismatic',
          watts: 61,
        },
        {
          desc: 'Troffer 2X2, (1) 17W 24" T8 - Parabolic',
          watts: 20,
        },
        {
          desc: 'Troffer 2X2, (2) 17W 24" T8 - Parabolic',
          watts: 33,
        },
        {
          desc: 'Troffer 2X2, (3) 17W 24" T8 - Parabolic',
          watts: 47,
        },
        {
          desc: 'Troffer 2X2, (4) 17W 24" T8 - Parabolic',
          watts: 61,
        },
        {
          desc: 'Troffer 2X2, (1) 17W 24" T8 - Volumetric',
          watts: 20,
        },
        {
          desc: 'Troffer 2X2, (2) 17W 24" T8 - Volumetric',
          watts: 33,
        },
        {
          desc: 'Troffer 2X2, (3) 17W 24" T8 - Volumetric',
          watts: 47,
        },
        {
          desc: 'Troffer 2X2, (4) 17W 24" T8 - Volumetric',
          watts: 61,
        },
      ],
      "U-Lamp/Biax": [
        {
          desc: "Troffer 2X2, (1) 32W U6 T8 - Prismatic",
          watts: 31,
        },
        {
          desc: "Troffer 2X2, (2) 32W U6 T8 - Prismatic",
          watts: 59,
        },
        {
          desc: "Troffer 2X2, (1) 32W U6 T8 - Parabolic",
          watts: 31,
        },
        {
          desc: "Troffer 2X2, (2) 32W U6 T8 - Parabolic",
          watts: 59,
        },
        {
          desc: 'Troffer 2X2, (1) 31W 1-5/8" U-Tubes - Prismatic',
          watts: 31,
        },
        {
          desc: 'Troffer 2X2, (2) 31W 1-5/8" U-Tubes - Prismatic',
          watts: 59,
        },
        {
          desc: 'Troffer 2X2, (3) 31W 1-5/8" U-Tubes - Prismatic',
          watts: 89,
        },
        {
          desc: 'Troffer 2X2, (1) 31W 1-5/8" U-Tubes - Parabolic',
          watts: 31,
        },
        {
          desc: 'Troffer 2X2, (2) 31W 1-5/8" U-Tubes - Parabolic',
          watts: 59,
        },
        {
          desc: 'Troffer 2X2, (3) 31W 1-5/8" U-Tubes - Parabolic',
          watts: 89,
        },
        {
          desc: "Troffer 2X2, (1) 40W 2G11 Biax Lamps - Prismatic",
          watts: 46,
        },
        {
          desc: "Troffer 2X2, (2) 40W 2G11 Biax Lamps - Prismatic",
          watts: 85,
        },
        {
          desc: "Troffer 2X2, (3) 40W 2G11 Biax Lamps - Prismatic",
          watts: 133,
        },
        {
          desc: "Troffer 2X2, (1) 40W 2G11 Biax Lamps - Parabolic",
          watts: 46,
        },
        {
          desc: "Troffer 2X2, (2) 40W 2G11 Biax Lamps - Parabolic",
          watts: 85,
        },
        {
          desc: "Troffer 2X2, (3) 40W 2G11 Biax Lamps - Parabolic",
          watts: 133,
        },
        {
          desc: "Troffer 2X2, (1) 40W 2G11 Biax Lamps - Volumetric",
          watts: 46,
        },
        {
          desc: "Troffer 2X2, (2) 40W 2G11 Biax Lamps - Volumetric",
          watts: 85,
        },
        {
          desc: "Troffer 2X2, (3) 40W 2G11 Biax Lamps - Volumetric",
          watts: 133,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Troffer 2X2, (1) 9W 24" LED T8 (UL-A) - Prismatic',
          watts: 9,
        },
        {
          desc: 'Troffer 2X2, (2) 9W 24" LED T8 (UL-A) - Prismatic',
          watts: 18,
        },
        {
          desc: 'Troffer 2X2, (3) 9W 24" LED T8 (UL-A) - Prismatic',
          watts: 27,
        },
        {
          desc: 'Troffer 2X2, (4) 9W 24" LED T8 (UL-A) - Prismatic',
          watts: 36,
        },
        {
          desc: 'Troffer 2X2, (1) 9W 24" LED T8 (UL-A) - Parabolic',
          watts: 9,
        },
        {
          desc: 'Troffer 2X2, (2) 9W 24" LED T8 (UL-A) - Parabolic',
          watts: 18,
        },
        {
          desc: 'Troffer 2X2, (3) 9W 24" LED T8 (UL-A) - Parabolic',
          watts: 27,
        },
        {
          desc: 'Troffer 2X2, (4) 9W 24" LED T8 (UL-A) - Parabolic',
          watts: 36,
        },
        {
          desc: 'Troffer 2X2, (1) 9W 24" LED T8 (UL-A) - Volumetric',
          watts: 9,
        },
        {
          desc: 'Troffer 2X2, (2) 9W 24" LED T8 (UL-A) - Volumetric',
          watts: 18,
        },
        {
          desc: 'Troffer 2X2, (3) 9W 24" LED T8 (UL-A) - Volumetric',
          watts: 27,
        },
        {
          desc: 'Troffer 2X2, (4) 9W 24" LED T8 (UL-A) - Volumetric',
          watts: 36,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Troffer 2X2, (1) 9W 24" LED T8 (UL-B) - Prismatic',
          watts: 9,
        },
        {
          desc: 'Troffer 2X2, (2) 9W 24" LED T8 (UL-B) - Prismatic',
          watts: 18,
        },
        {
          desc: 'Troffer 2X2, (3) 9W 24" LED T8 (UL-B) - Prismatic',
          watts: 27,
        },
        {
          desc: 'Troffer 2X2, (4) 9W 24" LED T8 (UL-B) - Prismatic',
          watts: 36,
        },
        {
          desc: 'Troffer 2X2, (1) 9W 24" LED T8 (UL-B) - Parabolic',
          watts: 9,
        },
        {
          desc: 'Troffer 2X2, (2) 9W 24" LED T8 (UL-B) - Parabolic',
          watts: 18,
        },
        {
          desc: 'Troffer 2X2, (3) 9W 24" LED T8 (UL-B) - Parabolic',
          watts: 27,
        },
        {
          desc: 'Troffer 2X2, (4) 9W 24" LED T8 (UL-B) - Parabolic',
          watts: 36,
        },
        {
          desc: 'Troffer 2X2, (1) 9W 24" LED T8 (UL-B) - Volumetric',
          watts: 9,
        },
        {
          desc: 'Troffer 2X2, (2) 9W 24" LED T8 (UL-B) - Volumetric',
          watts: 18,
        },
        {
          desc: 'Troffer 2X2, (3) 9W 24" LED T8 (UL-B) - Volumetric',
          watts: 27,
        },
        {
          desc: 'Troffer 2X2, (4) 9W 24" LED T8 (UL-B) - Volumetric',
          watts: 36,
        },
      ],
      T12: [
        {
          desc: 'Troffer 2X2, (1) 20W 24" T12 - Prismatic',
          watts: 28,
        },
        {
          desc: 'Troffer 2X2, (2) 20W 24" T12 - Prismatic',
          watts: 56,
        },
        {
          desc: 'Troffer 2X2, (3) 20W 24" T12 - Prismatic',
          watts: 84,
        },
        {
          desc: 'Troffer 2X2, (4) 20W 24" T12 - Prismatic',
          watts: 112,
        },
        {
          desc: 'Troffer 2X2, (1) 20W 24" T12 - Parabolic',
          watts: 28,
        },
        {
          desc: 'Troffer 2X2, (2) 20W 24" T12 - Parabolic',
          watts: 56,
        },
        {
          desc: 'Troffer 2X2, (3) 20W 24" T12 - Parabolic',
          watts: 84,
        },
        {
          desc: 'Troffer 2X2, (4) 20W 24" T12 - Parabolic',
          watts: 112,
        },
        {
          desc: 'Troffer 2X2, (1) 20W 24" T12 - Volumetric',
          watts: 28,
        },
        {
          desc: 'Troffer 2X2, (2) 20W 24" T12 - Volumetric',
          watts: 56,
        },
        {
          desc: 'Troffer 2X2, (3) 20W 24" T12 - Volumetric',
          watts: 84,
        },
        {
          desc: 'Troffer 2X2, (4) 20W 24" T12 - Volumetric',
          watts: 112,
        },
      ],
      LED: [
        {
          desc: "LED 2X2 20W Troffer",
          watts: 20,
        },
        {
          desc: "LED 2X2 30W Troffer",
          watts: 30,
        },
        {
          desc: "LED 2X2 40W Troffer",
          watts: 40,
        },
        {
          desc: "LED 2X2 50W Troffer",
          watts: 50,
        },
        {
          desc: "LED 2X2 20W Troffer - Integral Sensor",
          watts: 20,
        },
        {
          desc: "LED 2X2 30W Troffer - Integral Sensor",
          watts: 30,
        },
        {
          desc: "LED 2X2 40W Troffer - Integral Sensor",
          watts: 40,
        },
        {
          desc: "LED 2X2 50W Troffer - Integral Sensor",
          watts: 50,
        },
      ],
    },
    "2X4": {
      T8: [
        {
          desc: 'Troffer 2X4, (2) 32W 48" T8 - Prismatic',
          watts: 56,
        },
        {
          desc: 'Troffer 2X4, (3) 32W 48" T8 - Prismatic',
          watts: 82,
        },
        {
          desc: 'Troffer 2X4, (4) 32W 48" T8 - Prismatic',
          watts: 108,
        },
        {
          desc: 'Troffer 2X4, (2) 32W 48" T8 - Parabolic',
          watts: 56,
        },
        {
          desc: 'Troffer 2X4, (3) 32W 48" T8 - Parabolic',
          watts: 82,
        },
        {
          desc: 'Troffer 2X4, (4) 32W 48" T8 - Parabolic',
          watts: 108,
        },
        {
          desc: 'Troffer 2X4, (2) 32W 48" T8 - Volumetric',
          watts: 56,
        },
        {
          desc: 'Troffer 2X4, (3) 32W 48" T8 - Volumetric',
          watts: 82,
        },
        {
          desc: 'Troffer 2X4, (4) 32W 48" T8 - Volumetric',
          watts: 108,
        },
      ],
      T5HE: [
        {
          desc: 'Troffer 2X4, (2) 28W 48" T5HE - Prismatic',
          watts: 63,
        },
        {
          desc: 'Troffer 2X4, (3) 28W 48" T5HE - Prismatic',
          watts: 95,
        },
        {
          desc: 'Troffer 2X4, (4) 28W 48" T5HE - Prismatic',
          watts: 126,
        },
        {
          desc: 'Troffer 2X4, (2) 28W 48" T5HE - Parabolic',
          watts: 63,
        },
        {
          desc: 'Troffer 2X4, (3) 28W 48" T5HE - Parabolic',
          watts: 95,
        },
        {
          desc: 'Troffer 2X4, (4) 28W 48" T5HE - Parabolic',
          watts: 126,
        },
        {
          desc: 'Troffer 2X4, (2) 28W 48" T5HE - Volumetric',
          watts: 63,
        },
        {
          desc: 'Troffer 2X4, (3) 28W 48" T5HE - Volumetric',
          watts: 95,
        },
        {
          desc: 'Troffer 2X4, (4) 28W 48" T5HE - Volumetric',
          watts: 126,
        },
      ],
      T5HO: [
        {
          desc: 'Troffer 2X4, (2) 54W 48" T5HO - Prismatic',
          watts: 117,
        },
        {
          desc: 'Troffer 2X4, (3) 54W 48" T5HO - Prismatic',
          watts: 177,
        },
        {
          desc: 'Troffer 2X4, (4) 54W 48" T5HO - Prismatic',
          watts: 234,
        },
        {
          desc: 'Troffer 2X4, (2) 54W 48" T5HO - Parabolic',
          watts: 117,
        },
        {
          desc: 'Troffer 2X4, (3) 54W 48" T5HO - Parabolic',
          watts: 177,
        },
        {
          desc: 'Troffer 2X4, (4) 54W 48" T5HO - Parabolic',
          watts: 234,
        },
        {
          desc: 'Troffer 2X4, (2) 54W 48" T5HO - Volumetric',
          watts: 117,
        },
        {
          desc: 'Troffer 2X4, (3) 54W 48" T5HO - Volumetric',
          watts: 177,
        },
        {
          desc: 'Troffer 2X4, (4) 54W 48" T5HO - Volumetric',
          watts: 234,
        },
      ],
      "U-Lamp/Biax": [
        {
          desc: "Troffer 2X4, (2) Biax CFT40 - Volumetric",
          watts: 85,
        },
        {
          desc: "Troffer 2X4, (4) Biax CFT40 - Volumetric",
          watts: 144,
        },
      ],
      LED: [
        {
          desc: "LED 2X4 30W Troffer",
          watts: 30,
        },
        {
          desc: "LED 2X4 40W Troffer",
          watts: 40,
        },
        {
          desc: "LED 2X4 50W Troffer",
          watts: 50,
        },
        {
          desc: "LED 2X4 60W Troffer",
          watts: 60,
        },
        {
          desc: "LED 2X4 30W Troffer - Integral Sensor",
          watts: 30,
        },
        {
          desc: "LED 2X4 40W Troffer - Integral Sensor",
          watts: 40,
        },
        {
          desc: "LED 2X4 50W Troffer - Integral Sensor",
          watts: 50,
        },
        {
          desc: "LED 2X4 60W Troffer - Integral Sensor",
          watts: 60,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Troffer 2X4, (2) 13W 48" LED T8 (UL-A) - Prismatic',
          watts: 30,
        },
        {
          desc: 'Troffer 2X4, (3) 13W 48" LED T8 (UL-A) - Prismatic',
          watts: 45,
        },
        {
          desc: 'Troffer 2X4, (4) 13W 48" LED T8 (UL-A) - Prismatic',
          watts: 60,
        },
        {
          desc: 'Troffer 2X4, (2) 13W 48" LED T8 (UL-A) - Parabolic',
          watts: 30,
        },
        {
          desc: 'Troffer 2X4, (3) 13W 48" LED T8 (UL-A) - Parabolic',
          watts: 45,
        },
        {
          desc: 'Troffer 2X4, (4) 13W 48" LED T8 (UL-A) - Parabolic',
          watts: 60,
        },
        {
          desc: 'Troffer 2X4, (2) 13W 48" LED T8 (UL-A) - Volumetric',
          watts: 30,
        },
        {
          desc: 'Troffer 2X4, (3) 13W 48" LED T8 (UL-A) - Volumetric',
          watts: 45,
        },
        {
          desc: 'Troffer 2X4, (4) 13W 48" LED T8 (UL-A) - Volumetric',
          watts: 60,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Troffer 2X4, (2) 12W 48" LED T8 (UL-B) - Prismatic',
          watts: 24,
        },
        {
          desc: 'Troffer 2X4, (3) 12W 48" LED T8 (UL-B) - Prismatic',
          watts: 36,
        },
        {
          desc: 'Troffer 2X4, (4) 12W 48" LED T8 (UL-B) - Prismatic',
          watts: 48,
        },
        {
          desc: 'Troffer 2X4, (2) 12W 48" LED T8 (UL-B) - Parabolic',
          watts: 24,
        },
        {
          desc: 'Troffer 2X4, (3) 12W 48" LED T8 (UL-B) - Parabolic',
          watts: 36,
        },
        {
          desc: 'Troffer 2X4, (4) 12W 48" LED T8 (UL-B) - Parabolic',
          watts: 48,
        },
        {
          desc: 'Troffer 2X4, (2) 12W 48" LED T8 (UL-B) - Volumetric',
          watts: 24,
        },
        {
          desc: 'Troffer 2X4, (3) 12W 48" LED T8 (UL-B) - Volumetric',
          watts: 36,
        },
        {
          desc: 'Troffer 2X4, (4) 12W 48" LED T8 (UL-B) - Volumetric',
          watts: 48,
        },
      ],
      "LED T5 (B)": [
        {
          desc: 'Troffer 2X4, (2) 13W 48" LED T5HE (UL-B) - Prismatic',
          watts: 26,
        },
        {
          desc: 'Troffer 2X4, (3) 13W 48" LED T5HE (UL-B) - Prismatic',
          watts: 39,
        },
        {
          desc: 'Troffer 2X4, (4) 13W 48" LED T5HE (UL-B) - Prismatic',
          watts: 52,
        },
        {
          desc: 'Troffer 2X4, (2) 13W 48" LED T5HE (UL-B) - Parabolic',
          watts: 26,
        },
        {
          desc: 'Troffer 2X4, (3) 13W 48" LED T5HE (UL-B) - Parabolic',
          watts: 39,
        },
        {
          desc: 'Troffer 2X4, (4) 13W 48" LED T5HE (UL-B) - Parabolic',
          watts: 52,
        },
        {
          desc: 'Troffer 2X4, (2) 13W 48" LED T5HE (UL-B) - Volumetric',
          watts: 26,
        },
        {
          desc: 'Troffer 2X4, (3) 13W 48" LED T5HE (UL-B) - Volumetric',
          watts: 39,
        },
        {
          desc: 'Troffer 2X4, (4) 13W 48" LED T5HE (UL-B) - Volumetric',
          watts: 52,
        },
      ],
      "LED T5HO (B)": [
        {
          desc: 'Troffer 2X4, (2) 25W 48" LED T5HO (UL-B) - Prismatic',
          watts: 50,
        },
        {
          desc: 'Troffer 2X4, (3) 25W 48" LED T5HO (UL-B) - Prismatic',
          watts: 75,
        },
        {
          desc: 'Troffer 2X4, (4) 25W 48" LED T5HO (UL-B) - Prismatic',
          watts: 100,
        },
        {
          desc: 'Troffer 2X4, (2) 25W 48" LED T5HO (UL-B) - Parabolic',
          watts: 50,
        },
        {
          desc: 'Troffer 2X4, (3) 25W 48" LED T5HO (UL-B) - Parabolic',
          watts: 75,
        },
        {
          desc: 'Troffer 2X4, (4) 25W 48" LED T5HO (UL-B) - Parabolic',
          watts: 100,
        },
        {
          desc: 'Troffer 2X4, (2) 25W 48" LED T5HO (UL-B) - Volumetric',
          watts: 50,
        },
        {
          desc: 'Troffer 2X4, (3) 25W 48" LED T5HO (UL-B) - Volumetric',
          watts: 75,
        },
        {
          desc: 'Troffer 2X4, (4) 25W 48" LED T5HO (UL-B) - Volumetric',
          watts: 100,
        },
      ],
      T12: [
        {
          desc: 'Troffer 2X4, (2) 34W 48" T12 - Prismatic',
          watts: 72,
        },
        {
          desc: 'Troffer 2X4, (3) 34W 48" T12 - Prismatic',
          watts: 115,
        },
        {
          desc: 'Troffer 2X4, (4) 34W 48" T12 - Prismatic',
          watts: 144,
        },
        {
          desc: 'Troffer 2X4, (2) 34W 48" T12 - Parabolic',
          watts: 72,
        },
        {
          desc: 'Troffer 2X4, (3) 34W 48" T12 - Parabolic',
          watts: 115,
        },
        {
          desc: 'Troffer 2X4, (4) 34W 48" T12 - Parabolic',
          watts: 144,
        },
        {
          desc: 'Troffer 2X4, (2) 34W 48" T12 - Volumetric',
          watts: 72,
        },
        {
          desc: 'Troffer 2X4, (3) 34W 48" T12 - Volumetric',
          watts: 115,
        },
        {
          desc: 'Troffer 2X4, (4) 34W 48" T12 - Volumetric',
          watts: 144,
        },
      ],
    },
    "1X4": {
      T8: [
        {
          desc: 'Troffer 1X4, (1) 32W 48" T8 - Prismatic',
          watts: 28,
        },
        {
          desc: 'Troffer 1X4, (2) 32W 48" T8 - Prismatic',
          watts: 56,
        },
        {
          desc: 'Troffer 1X4, (3) 32W 48" T8 - Prismatic',
          watts: 82,
        },
        {
          desc: 'Troffer 1X4, (1) 32W 48" T8 - Parabolic',
          watts: 28,
        },
        {
          desc: 'Troffer 1X4, (2) 32W 48" T8 - Parabolic',
          watts: 56,
        },
        {
          desc: 'Troffer 1X4, (3) 32W 48" T8 - Parabolic',
          watts: 82,
        },
        {
          desc: 'Troffer 1X4, (1) 32W 48" T8 - Volumetric',
          watts: 28,
        },
        {
          desc: 'Troffer 1X4, (2) 32W 48" T8 - Volumetric',
          watts: 56,
        },
        {
          desc: 'Troffer 1X4, (3) 32W 48" T8 - Volumetric',
          watts: 82,
        },
      ],
      T5HE: [
        {
          desc: 'Troffer 1X4, (1) 28W 48" T5HE - Prismatic',
          watts: 32,
        },
        {
          desc: 'Troffer 1X4, (2) 28W 48" T5HE - Prismatic',
          watts: 63,
        },
        {
          desc: 'Troffer 1X4, (3) 28W 48" T5HE - Prismatic',
          watts: 95,
        },
        {
          desc: 'Troffer 1X4, (1) 28W 48" T5HE - Parabolic',
          watts: 32,
        },
        {
          desc: 'Troffer 1X4, (2) 28W 48" T5HE - Parabolic',
          watts: 63,
        },
        {
          desc: 'Troffer 1X4, (3) 28W 48" T5HE - Parabolic',
          watts: 95,
        },
        {
          desc: 'Troffer 1X4, (1) 28W 48" T5HE - Volumetric',
          watts: 32,
        },
        {
          desc: 'Troffer 1X4, (2) 28W 48" T5HE - Volumetric',
          watts: 63,
        },
        {
          desc: 'Troffer 1X4, (3) 28W 48" T5HE - Volumetric',
          watts: 95,
        },
      ],
      "U-Lamp/Biax": [
        {
          desc: "Troffer 1X4, (2) 40W 2G11 Biax Lamps - Volumetric",
          watts: 85,
        },
        {
          desc: "Troffer 1X4, (4) 40W 2G11 Biax Lamps - Volumetric",
          watts: 144,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Troffer 1X4, (1) 13W 48" LED T8 (UL-A) - Prismatic',
          watts: 15,
        },
        {
          desc: 'Troffer 1X4, (2) 13W 48" LED T8 (UL-A) - Prismatic',
          watts: 30,
        },
        {
          desc: 'Troffer 1X4, (3) 13W 48" LED T8 (UL-A) - Prismatic',
          watts: 45,
        },
        {
          desc: 'Troffer 1X4, (1) 13W 48" LED T8 (UL-A) - Parabolic',
          watts: 15,
        },
        {
          desc: 'Troffer 1X4, (2) 13W 48" LED T8 (UL-A) - Parabolic',
          watts: 30,
        },
        {
          desc: 'Troffer 1X4, (3) 13W 48" LED T8 (UL-A) - Parabolic',
          watts: 45,
        },
        {
          desc: 'Troffer 1X4, (1) 13W 48" LED T8 (UL-A) - Volumetric',
          watts: 15,
        },
        {
          desc: 'Troffer 1X4, (2) 13W 48" LED T8 (UL-A) - Volumetric',
          watts: 30,
        },
        {
          desc: 'Troffer 1X4, (3) 13W 48" LED T8 (UL-A) - Volumetric',
          watts: 45,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Troffer 1X4, (1) 12W 48" LED T8 (UL-B) - Prismatic',
          watts: 12,
        },
        {
          desc: 'Troffer 1X4, (2) 12W 48" LED T8 (UL-B) - Prismatic',
          watts: 24,
        },
        {
          desc: 'Troffer 1X4, (3) 12W 48" LED T8 (UL-B) - Prismatic',
          watts: 36,
        },
        {
          desc: 'Troffer 1X4, (1) 12W 48" LED T8 (UL-B) - Parabolic',
          watts: 12,
        },
        {
          desc: 'Troffer 1X4, (2) 12W 48" LED T8 (UL-B) - Parabolic',
          watts: 24,
        },
        {
          desc: 'Troffer 1X4, (3) 12W 48" LED T8 (UL-B) - Parabolic',
          watts: 36,
        },
        {
          desc: 'Troffer 1X4, (1) 12W 48" LED T8 (UL-B) - Volumetric',
          watts: 12,
        },
        {
          desc: 'Troffer 1X4, (2) 12W 48" LED T8 (UL-B) - Volumetric',
          watts: 24,
        },
        {
          desc: 'Troffer 1X4, (3) 12W 48" LED T8 (UL-B) - Volumetric',
          watts: 36,
        },
      ],
      LED: [
        {
          desc: "LED 1X4 20W Troffer",
          watts: 20,
        },
        {
          desc: "LED 1X4 30W Troffer",
          watts: 30,
        },
        {
          desc: "LED 1X4 40W Troffer",
          watts: 40,
        },
        {
          desc: "LED 1X4 50W Troffer",
          watts: 50,
        },
        {
          desc: "LED 1X4 20W Troffer - Integral Sensor",
          watts: 20,
        },
        {
          desc: "LED 1X4 30W Troffer - Integral Sensor",
          watts: 30,
        },
        {
          desc: "LED 1X4 40W Troffer - Integral Sensor",
          watts: 40,
        },
        {
          desc: "LED 1X4 50W Troffer - Integral Sensor",
          watts: 50,
        },
      ],
    },
  },
  Wrap: {
    "2\u0027": {
      T8: [
        {
          desc: 'Linear 2\u0027 Wrap, (1) 17W 24" T8',
          watts: 20,
        },
        {
          desc: 'Linear 2\u0027 Wrap, (2) 17W 24" T8',
          watts: 33,
        },
        {
          desc: 'Linear 2\u0027 Wrap, (3) 17W 24" T8',
          watts: 47,
        },
        {
          desc: 'Linear 2\u0027 Wrap, (4) 17W 24" T8',
          watts: 61,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Linear 2\u0027 Wrap, (1) 9W 24" LED T8 (UL-A)',
          watts: 10,
        },
        {
          desc: 'Linear 2\u0027 Wrap, (2) 9W 24" LED T8 (UL-A)',
          watts: 20,
        },
        {
          desc: 'Linear 2\u0027 Wrap, (3) 9W 24" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Linear 2\u0027 Wrap, (4) 9W 24" LED T8 (UL-A)',
          watts: 40,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Linear 2\u0027 Wrap, (1) 9W 24" LED T8 (UL-B)',
          watts: 9,
        },
        {
          desc: 'Linear 2\u0027 Wrap, (2) 9W 24" LED T8 (UL-B)',
          watts: 18,
        },
        {
          desc: 'Linear 2\u0027 Wrap, (3) 9W 24" LED T8 (UL-B)',
          watts: 27,
        },
        {
          desc: 'Linear 2\u0027 Wrap, (4) 9W 24" LED T8 (UL-B)',
          watts: 36,
        },
      ],
      T12: [
        {
          desc: 'Linear 2\u0027 Wrap, (1) 20W 24" T12',
          watts: 28,
        },
        {
          desc: 'Linear 2\u0027 Wrap, (2) 20W 24" T12',
          watts: 56,
        },
        {
          desc: 'Linear 2\u0027 Wrap, (3) 20W 24" T12',
          watts: 84,
        },
        {
          desc: 'Linear 2\u0027 Wrap, (4) 20W 24" T12',
          watts: 112,
        },
      ],
    },
    "4\u0027": {
      T8: [
        {
          desc: 'Linear 4\u0027 Wrap, (1) 32W 48" T8',
          watts: 28,
        },
        {
          desc: 'Linear 4\u0027 Wrap, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'Linear 4\u0027 Wrap, (3) 32W 48" T8',
          watts: 82,
        },
        {
          desc: 'Linear 4\u0027 Wrap, (4) 32W 48" T8',
          watts: 108,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Linear 4\u0027 Wrap, (1) 13W 48" LED T8 (UL-A)',
          watts: 15,
        },
        {
          desc: 'Linear 4\u0027 Wrap, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Linear 4\u0027 Wrap, (3) 13W 48" LED T8 (UL-A)',
          watts: 45,
        },
        {
          desc: 'Linear 4\u0027 Wrap, (4) 13W 48" LED T8 (UL-A)',
          watts: 60,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Linear 4\u0027 Wrap, (1) 12W 48" LED T8 (UL-A)',
          watts: 12,
        },
        {
          desc: 'Linear 4\u0027 Wrap, (2) 12W 48" LED T8 (UL-A)',
          watts: 24,
        },
        {
          desc: 'Linear 4\u0027 Wrap, (3) 12W 48" LED T8 (UL-A)',
          watts: 36,
        },
        {
          desc: 'Linear 4\u0027 Wrap, (4) 12W 48" LED T8 (UL-A)',
          watts: 48,
        },
      ],
      T12: [
        {
          desc: 'Linear 4\u0027 Wrap, (1) 34W 48" T12',
          watts: 43,
        },
        {
          desc: 'Linear 4\u0027 Wrap, (2) 34W 48" T12',
          watts: 72,
        },
        {
          desc: 'Linear 4\u0027 Wrap, (3) 34W 48" T12',
          watts: 115,
        },
        {
          desc: 'Linear 4\u0027 Wrap, (4) 34W 48" T12',
          watts: 144,
        },
      ],
      LED: [
        {
          desc: "LED 4\u0027 Wrap - 20W",
          watts: 20,
        },
        {
          desc: "LED 4\u0027 Wrap - 30W",
          watts: 30,
        },
        {
          desc: "LED 4\u0027 Wrap - 40W",
          watts: 40,
        },
        {
          desc: "LED 4\u0027 D/I Wrap - 20W",
          watts: 20,
        },
        {
          desc: "LED 4\u0027 D/I Wrap - 30W",
          watts: 30,
        },
        {
          desc: "LED 4\u0027 D/I Wrap - 40W",
          watts: 40,
        },
      ],
    },
    "8\u0027": {
      T8: [
        {
          desc: 'Tandem 8\u0027 Wrap, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (3) 32W 48" T8',
          watts: 82,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (4) 32W 48" T8',
          watts: 108,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (6) 32W 48" T8',
          watts: 164,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (8) 32W 48" T8',
          watts: 216,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Tandem 8\u0027 Wrap, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (3) 13W 48" LED T8 (UL-A)',
          watts: 45,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (4) 13W 48" LED T8 (UL-A)',
          watts: 60,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (6) 13W 48" LED T8 (UL-A)',
          watts: 90,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (8) 13W 48" LED T8 (UL-A)',
          watts: 120,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Tandem 8\u0027 Wrap, (2) 12W 48" LED T8 (UL-B)',
          watts: 24,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (3) 12W 48" LED T8 (UL-B)',
          watts: 36,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (4) 12W 48" LED T8 (UL-B)',
          watts: 48,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (6) 12W 48" LED T8 (UL-B)',
          watts: 72,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (8) 12W 48" LED T8 (UL-B)',
          watts: 96,
        },
      ],
      T5HE: [
        {
          desc: 'Tandem 8\u0027 Wrap, (2) 28W 48" T5HE',
          watts: 63,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (3) 28W 48" T5HE',
          watts: 95,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (4) 28W 48" T5HE',
          watts: 126,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (6) 28W 48" T5HE',
          watts: 190,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (8) 28W 48" T5HE',
          watts: 252,
        },
      ],
      T5HO: [
        {
          desc: 'Tandem 8\u0027 Wrap, (2) 54W 48" T5HO',
          watts: 117,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (3) 54W 48" T5HO',
          watts: 177,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (4) 54W 48" T5HO',
          watts: 234,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (6) 54W 48" T5HO',
          watts: 354,
        },
        {
          desc: 'Tandem 8\u0027 Wrap, (8) 54W 48" T5HO',
          watts: 468,
        },
      ],
      LED: [
        {
          desc: "LED 8\u0027 Wrap - 40W",
          watts: 40,
        },
        {
          desc: "LED 8\u0027 Wrap - 50W",
          watts: 50,
        },
        {
          desc: "LED 8\u0027 Wrap - 60W",
          watts: 60,
        },
        {
          desc: "LED 8\u0027 D/I Wrap - 40W",
          watts: 40,
        },
        {
          desc: "LED 8\u0027 D/I Wrap - 50W",
          watts: 50,
        },
        {
          desc: "LED 8\u0027 D/I Wrap - 60W",
          watts: 60,
        },
      ],
    },
  },
  Strip: {
    "2\u0027": {
      T8: [
        {
          desc: 'Linear 2\u0027 Strip, (1) 17W 24" T8',
          watts: 20,
        },
        {
          desc: 'Linear 2\u0027 Strip, (2) 17W 24" T8',
          watts: 33,
        },
        {
          desc: 'Linear 2\u0027 Strip, (3) 17W 24" T8',
          watts: 47,
        },
        {
          desc: 'Linear 2\u0027 Strip, (4) 17W 24" T8',
          watts: 61,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Linear 2\u0027 Strip, (1) 9W 24" LED T8 (UL-A)',
          watts: 10,
        },
        {
          desc: 'Linear 2\u0027 Strip, (2) 9W 24" LED T8 (UL-A)',
          watts: 20,
        },
        {
          desc: 'Linear 2\u0027 Strip, (3) 9W 24" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Linear 2\u0027 Strip, (4) 9W 24" LED T8 (UL-A)',
          watts: 40,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Linear 2\u0027 Strip, (1) 9W 24" LED T8 (UL-B)',
          watts: 9,
        },
        {
          desc: 'Linear 2\u0027 Strip, (2) 9W 24" LED T8 (UL-B)',
          watts: 18,
        },
        {
          desc: 'Linear 2\u0027 Strip, (3) 9W 24" LED T8 (UL-B)',
          watts: 27,
        },
        {
          desc: 'Linear 2\u0027 Strip, (4) 9W 24" LED T8 (UL-B)',
          watts: 36,
        },
      ],
      T12: [
        {
          desc: 'Linear 2\u0027 Strip, (1) 20W 24" T12',
          watts: 28,
        },
        {
          desc: 'Linear 2\u0027 Strip, (2) 20W 24" T12',
          watts: 56,
        },
        {
          desc: 'Linear 2\u0027 Strip, (3) 20W 24" T12',
          watts: 84,
        },
        {
          desc: 'Linear 2\u0027 Strip, (4) 20W 24" T12',
          watts: 112,
        },
      ],
      LED: [
        {
          desc: "LED 2\u0027 Strip - 15W",
          watts: 15,
        },
        {
          desc: "LED 2\u0027 Strip - 20W",
          watts: 20,
        },
      ],
    },
    "3\u0027": {
      T8: [
        {
          desc: 'Linear 3\u0027 Strip, (1) 25W 36" T8',
          watts: 26,
        },
        {
          desc: 'Linear 3\u0027 Strip, (2) 25W 36" T8',
          watts: 46,
        },
        {
          desc: 'Linear 3\u0027 Strip, (3) 25W 36" T8',
          watts: 67,
        },
        {
          desc: 'Linear 3\u0027 Strip, (4) 25W 36" T8',
          watts: 87,
        },
      ],
      T12: [
        {
          desc: 'Linear 3\u0027 Strip, (1) 30W 36" T12',
          watts: 38,
        },
        {
          desc: 'Linear 3\u0027 Strip, (2) 30W 36" T12',
          watts: 66,
        },
        {
          desc: 'Linear 3\u0027 Strip, (3) 30W 36" T12',
          watts: 104,
        },
        {
          desc: 'Linear 3\u0027 Strip, (4) 30W 36" T12',
          watts: 132,
        },
      ],
    },
    "4\u0027": {
      T8: [
        {
          desc: 'Linear 4\u0027 Strip, (1) 32W 48" T8',
          watts: 28,
        },
        {
          desc: 'Linear 4\u0027 Strip, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'Linear 4\u0027 Strip, (3) 32W 48" T8',
          watts: 82,
        },
        {
          desc: 'Linear 4\u0027 Strip, (4) 32W 48" T8',
          watts: 108,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Linear 4\u0027 Strip, (1) 13W 48" LED T8 (UL-A)',
          watts: 15,
        },
        {
          desc: 'Linear 4\u0027 Strip, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Linear 4\u0027 Strip, (3) 13W 48" LED T8 (UL-A)',
          watts: 45,
        },
        {
          desc: 'Linear 4\u0027 Strip, (4) 13W 48" LED T8 (UL-A)',
          watts: 60,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Linear 4\u0027 Strip, (1) 12W 48" LED T8 (UL-A)',
          watts: 12,
        },
        {
          desc: 'Linear 4\u0027 Strip, (2) 12W 48" LED T8 (UL-A)',
          watts: 24,
        },
        {
          desc: 'Linear 4\u0027 Strip, (3) 12W 48" LED T8 (UL-A)',
          watts: 36,
        },
        {
          desc: 'Linear 4\u0027 Strip, (4) 12W 48" LED T8 (UL-A)',
          watts: 48,
        },
      ],
      T12: [
        {
          desc: 'Linear 4\u0027 Strip, (1) 34W 48" T12',
          watts: 43,
        },
        {
          desc: 'Linear 4\u0027 Strip, (2) 34W 48" T12',
          watts: 72,
        },
        {
          desc: 'Linear 4\u0027 Strip, (3) 34W 48" T12',
          watts: 115,
        },
        {
          desc: 'Linear 4\u0027 Strip, (4) 34W 48" T12',
          watts: 144,
        },
      ],
      LED: [
        {
          desc: "LED 4\u0027 Strip - 30W",
          watts: 30,
        },
        {
          desc: "LED 4\u0027 Strip - 40W",
          watts: 40,
        },
        {
          desc: "LED 4\u0027 Strip - 30W - Integral Sensor",
          watts: 30,
        },
        {
          desc: "LED 4\u0027 Strip - 40W - Integral Sensor",
          watts: 40,
        },
      ],
    },
    "8\u0027": {
      T8: [
        {
          desc: 'Tandem 8\u0027 Strip, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (3) 32W 48" T8',
          watts: 82,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (4) 32W 48" T8',
          watts: 108,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (6) 32W 48" T8',
          watts: 164,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (8) 32W 48" T8',
          watts: 216,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Tandem 8\u0027 Strip, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (3) 13W 48" LED T8 (UL-A)',
          watts: 45,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (4) 13W 48" LED T8 (UL-A)',
          watts: 60,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (6) 13W 48" LED T8 (UL-A)',
          watts: 90,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (8) 13W 48" LED T8 (UL-A)',
          watts: 120,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Tandem 8\u0027 Strip, (2) 12W 48" LED T8 (UL-B)',
          watts: 24,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (3) 12W 48" LED T8 (UL-B)',
          watts: 36,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (4) 12W 48" LED T8 (UL-B)',
          watts: 48,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (6) 12W 48" LED T8 (UL-B)',
          watts: 72,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (8) 12W 48" LED T8 (UL-B)',
          watts: 96,
        },
      ],
      T5HE: [
        {
          desc: 'Tandem 8\u0027 Strip, (2) 28W 48" T5HE',
          watts: 63,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (3) 28W 48" T5HE',
          watts: 95,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (4) 28W 48" T5HE',
          watts: 126,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (6) 28W 48" T5HE',
          watts: 190,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (8) 28W 48" T5HE',
          watts: 252,
        },
      ],
      T5HO: [
        {
          desc: 'Tandem 8\u0027 Strip, (2) 54W 48" T5HO',
          watts: 117,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (3) 54W 48" T5HO',
          watts: 177,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (4) 54W 48" T5HO',
          watts: 234,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (6) 54W 48" T5HO',
          watts: 354,
        },
        {
          desc: 'Tandem 8\u0027 Strip, (8) 54W 48" T5HO',
          watts: 468,
        },
      ],
      LED: [
        {
          desc: "LED 8\u0027 Strip - 40W",
          watts: 40,
        },
        {
          desc: "LED 8\u0027 Strip - 50W",
          watts: 50,
        },
        {
          desc: "LED 8\u0027 Strip - 60W",
          watts: 60,
        },
        {
          desc: "LED 8\u0027 Strip - 75W",
          watts: 75,
        },
        {
          desc: "LED 8\u0027 Strip - 40W - Integral Sensor",
          watts: 40,
        },
        {
          desc: "LED 8\u0027 Strip - 50W - Integral Sensor",
          watts: 50,
        },
        {
          desc: "LED 8\u0027 Strip - 60W - Integral Sensor",
          watts: 60,
        },
        {
          desc: "LED 8\u0027 Strip - 75W - Integral Sensor",
          watts: 75,
        },
      ],
    },
  },
  Industrial: {
    "4\u0027": {
      T8: [
        {
          desc: 'Industrial 4\u0027 Strip, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'Industrial 4\u0027 Strip, (3) 32W 48" T8',
          watts: 82,
        },
        {
          desc: 'Industrial 4\u0027 Strip, (4) 32W 48" T8',
          watts: 108,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Industrial 4\u0027 Strip, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Industrial 4\u0027 Strip, (3) 13W 48" LED T8 (UL-A)',
          watts: 45,
        },
        {
          desc: 'Industrial 4\u0027 Strip, (4) 13W 48" LED T8 (UL-A)',
          watts: 60,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Industrial 4\u0027 Strip, (2) 12W 48" LED T8 (UL-B)',
          watts: 24,
        },
        {
          desc: 'Industrial 4\u0027 Strip, (3) 12W 48" LED T8 (UL-B)',
          watts: 36,
        },
        {
          desc: 'Industrial 4\u0027 Strip, (4) 12W 48" LED T8 (UL-B)',
          watts: 48,
        },
      ],
      T12: [
        {
          desc: 'Industrial 4\u0027 Strip, (2) 34W 48" T12',
          watts: 72,
        },
        {
          desc: 'Industrial 4\u0027 Strip, (3) 34W 48" T12',
          watts: 115,
        },
        {
          desc: 'Industrial 4\u0027 Strip, (4) 34W 48" T12',
          watts: 144,
        },
      ],
      LED: [
        {
          desc: "LED Industrial 4\u0027 Strip - 30W",
          watts: 30,
        },
        {
          desc: "LED Industrial 4\u0027 Strip - 40W",
          watts: 40,
        },
        {
          desc: "LED Industrial 4\u0027 Strip - 30W - Integral Sensor",
          watts: 30,
        },
        {
          desc: "LED Industrial 4\u0027 Strip - 40W - Integral Sensor",
          watts: 40,
        },
      ],
    },
    "8\u0027": {
      T8: [
        {
          desc: 'Industrial 8\u0027 Strip, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'Industrial 8\u0027 Strip, (4) 32W 48" T8',
          watts: 108,
        },
        {
          desc: 'Industrial 8\u0027 Strip, (6) 32W 48" T8',
          watts: 164,
        },
        {
          desc: 'Industrial 8\u0027 Strip, (8) 32W 48" T8',
          watts: 216,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Industrial 8\u0027 Strip, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Industrial 8\u0027 Strip, (4) 13W 48" LED T8 (UL-A)',
          watts: 60,
        },
        {
          desc: 'Industrial 8\u0027 Strip, (6) 13W 48" LED T8 (UL-A)',
          watts: 90,
        },
        {
          desc: 'Industrial 8\u0027 Strip, (8) 13W 48" LED T8 (UL-A)',
          watts: 120,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Industrial 8\u0027 Strip, (2) 12W 48" LED T8 (UL-B)',
          watts: 24,
        },
        {
          desc: 'Industrial 8\u0027 Strip, (4) 12W 48" LED T8 (UL-B)',
          watts: 48,
        },
        {
          desc: 'Industrial 8\u0027 Strip, (6) 12W 48" LED T8 (UL-B)',
          watts: 72,
        },
        {
          desc: 'Industrial 8\u0027 Strip, (8) 12W 48" LED T8 (UL-B)',
          watts: 96,
        },
      ],
      LED: [
        {
          desc: "LED Industrial 8\u0027 Strip - 40W",
          watts: 40,
        },
        {
          desc: "LED Industrial 8\u0027 Strip - 50W",
          watts: 50,
        },
        {
          desc: "LED Industrial 8\u0027 Strip - 60W",
          watts: 60,
        },
        {
          desc: "LED Industrial 8\u0027 Strip - 75W",
          watts: 75,
        },
        {
          desc: "LED Industrial 8\u0027 Strip - 40W - Integral Sensor",
          watts: 40,
        },
        {
          desc: "LED Industrial 8\u0027 Strip - 50W - Integral Sensor",
          watts: 50,
        },
        {
          desc: "LED Industrial 8\u0027 Strip - 60W - Integral Sensor",
          watts: 60,
        },
        {
          desc: "LED Industrial 8\u0027 Strip - 75W - Integral Sensor",
          watts: 75,
        },
      ],
    },
  },
  Downlight: {
    '4"': {
      Incan: [
        {
          desc: 'Rec. 4" Downlight, (1) 25W Incan. A19',
          watts: 25,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 40W Incan. A19',
          watts: 40,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 60W Incan. A19',
          watts: 60,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 75W Incan. A19',
          watts: 75,
        },
      ],
      Halogen: [
        {
          desc: 'Rec. 4" Downlight, (1) 45W BR20 Halogen Lamp',
          watts: 45,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 60W BR30 Halogen Lamp',
          watts: 60,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 60W BR40 Halogen Lamp',
          watts: 60,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 75W BR40 Halogen Lamp',
          watts: 75,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 50W PAR20 Halogen Lamp',
          watts: 50,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 60W PAR30 Halogen Lamp',
          watts: 60,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 75W PAR30 Halogen Lamp',
          watts: 75,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 75W PAR38 Halogen Lamp',
          watts: 75,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 90W PAR38 Halogen Lamp',
          watts: 90,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 120W PAR38 Halogen Lamp',
          watts: 120,
        },
      ],
      "CFL SP": [
        {
          desc: 'Rec. 4" Downlight, (1) 9W CFL Spiral',
          watts: 9,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 13W CFL Spiral',
          watts: 13,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 18W CFL Spiral',
          watts: 18,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 23W CFL Spiral',
          watts: 23,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 28W CFL Spiral',
          watts: 28,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 36W CFL Spiral',
          watts: 36,
        },
      ],
      "CFL PIN": [
        {
          desc: 'Rec. 4" Downlight, (1) 9W 2P CFL Lamps',
          watts: 14,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 13W 2P CFL Lamps',
          watts: 17,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 15W 2P CFL Lamps',
          watts: 20,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 18W 2P CFL Lamps',
          watts: 26,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 23W 4P CFL Lamps',
          watts: 27,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 26W 4P CFL Lamps',
          watts: 33,
        },
      ],
      "LED SI": [
        {
          desc: 'Rec. 4" Downlight, (1) LED 8W A19',
          watts: 8,
        },
        {
          desc: 'Rec. 4" Downlight, (1) LED 12W A19',
          watts: 12,
        },
        {
          desc: 'Rec. 4" Downlight, (1) LED 15W A19',
          watts: 15,
        },
        {
          desc: 'Rec. 4" Downlight, (1) LED 12W BR30 Lamp',
          watts: 12,
        },
        {
          desc: 'Rec. 4" Downlight, (1) LED 12W PAR30 Lamp',
          watts: 12,
        },
        {
          desc: 'Rec. 4" Downlight, (1) LED 15W BR40 Lamp',
          watts: 15,
        },
        {
          desc: 'Rec. 4" Downlight, (1) LED 15W PAR38 Lamp',
          watts: 15,
        },
      ],
      "LED PIN": [
        {
          desc: 'Rec. 4" Downlight, (1) 6W LED PL (UL-B)',
          watts: 8,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 9W LED PL (UL-B)',
          watts: 12,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 12W LED PL (UL-B)',
          watts: 15,
        },
        {
          desc: 'Rec. 4" Downlight, (1) 15W LED PL (UL-B)',
          watts: 18,
        },
      ],
      LED: [
        {
          desc: 'LED 4" Downlight Kit (10W)',
          watts: 10,
        },
        {
          desc: 'LED 4" Downlight Kit (15W)',
          watts: 15,
        },
        {
          desc: 'LED 4" Downlight Kit (20W)',
          watts: 20,
        },
      ],
    },
    '6"': {
      Incan: [
        {
          desc: 'Rec. 6" Downlight, (1) 25W Incan. A19',
          watts: 25,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 40W Incan. A19',
          watts: 40,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 60W Incan. A19',
          watts: 60,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 75W Incan. A19',
          watts: 75,
        },
      ],
      Halogen: [
        {
          desc: 'Rec. 6" Downlight, (1) 45W BR20 Halogen Lamp',
          watts: 45,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 60W BR30 Halogen Lamp',
          watts: 60,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 60W BR40 Halogen Lamp',
          watts: 60,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 75W BR40 Halogen Lamp',
          watts: 75,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 50W PAR20 Halogen Lamp',
          watts: 50,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 60W PAR30 Halogen Lamp',
          watts: 60,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 75W PAR30 Halogen Lamp',
          watts: 75,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 75W PAR38 Halogen Lamp',
          watts: 75,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 90W PAR38 Halogen Lamp',
          watts: 90,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 120W PAR38 Halogen Lamp',
          watts: 120,
        },
      ],
      "CFL SP": [
        {
          desc: 'Rec. 6" Downlight, (1) 9W CFL Spiral',
          watts: 9,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 13W CFL Spiral',
          watts: 13,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 18W CFL Spiral',
          watts: 18,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 23W CFL Spiral',
          watts: 23,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 28W CFL Spiral',
          watts: 28,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 36W CFL Spiral',
          watts: 36,
        },
      ],
      "CFL PIN": [
        {
          desc: 'Rec. 6" Downlight, (1) 9W 2P CFL Lamps',
          watts: 14,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 13W 2P CFL Lamps',
          watts: 17,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 15W 2P CFL Lamps',
          watts: 20,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 18W 2P CFL Lamps',
          watts: 26,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 23W 4P CFL Lamps',
          watts: 27,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 26W 4P CFL Lamps',
          watts: 33,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 32W 4P CFL Lamps',
          watts: 34,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 42W 4P CFL Lamps',
          watts: 48,
        },
        {
          desc: 'Rec. 6" Downlight, (2) 9W 2P CFL Lamps',
          watts: 23,
        },
        {
          desc: 'Rec. 6" Downlight, (2) 13W 2P CFL Lamps',
          watts: 31,
        },
        {
          desc: 'Rec. 6" Downlight, (2) 15W 4P CFL Lamps',
          watts: 35,
        },
        {
          desc: 'Rec. 6" Downlight, (2) 18W 4P CFL Lamps',
          watts: 45,
        },
        {
          desc: 'Rec. 6" Downlight, (2) 23W 4P CFL Lamps',
          watts: 50,
        },
        {
          desc: 'Rec. 6" Downlight, (2) 26W 4P CFL Lamps',
          watts: 54,
        },
        {
          desc: 'Rec. 6" Downlight, (2) 32W 4P CFL Lamps',
          watts: 62,
        },
        {
          desc: 'Rec. 6" Downlight, (2) 42W 4P CFL Lamps',
          watts: 100,
        },
      ],
      "LED SI": [
        {
          desc: 'Rec. 6" Downlight, (1) LED 8W A19',
          watts: 8,
        },
        {
          desc: 'Rec. 6" Downlight, (1) LED 12W A19',
          watts: 12,
        },
        {
          desc: 'Rec. 6" Downlight, (1) LED 15W A19',
          watts: 15,
        },
        {
          desc: 'Rec. 6" Downlight, (1) LED 12W BR30 Lamp',
          watts: 12,
        },
        {
          desc: 'Rec. 6" Downlight, (1) LED 12W PAR30 Lamp',
          watts: 12,
        },
        {
          desc: 'Rec. 6" Downlight, (1) LED 15W BR40 Lamp',
          watts: 15,
        },
        {
          desc: 'Rec. 6" Downlight, (1) LED 15W PAR38 Lamp',
          watts: 15,
        },
      ],
      "LED PIN": [
        {
          desc: 'Rec. 6" Downlight, (1) 6W LED PL (UL-B)',
          watts: 8,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 9W LED PL (UL-B)',
          watts: 12,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 12W LED PL (UL-B)',
          watts: 15,
        },
        {
          desc: 'Rec. 6" Downlight, (1) 15W LED PL (UL-B)',
          watts: 18,
        },
        {
          desc: 'Rec. 6" Downlight, (2) 6W LED PL (UL-B)',
          watts: 16,
        },
        {
          desc: 'Rec. 6" Downlight, (2) 9W LED PL (UL-B)',
          watts: 24,
        },
        {
          desc: 'Rec. 6" Downlight, (2) 12W LED PL (UL-B)',
          watts: 30,
        },
        {
          desc: 'Rec. 6" Downlight, (2) 15W LED PL (UL-B)',
          watts: 36,
        },
      ],
      LED: [
        {
          desc: 'LED 6" Downlight Kit (10W)',
          watts: 10,
        },
        {
          desc: 'LED 6" Downlight Kit (15W)',
          watts: 15,
        },
        {
          desc: 'LED 6" Downlight Kit (20W)',
          watts: 20,
        },
        {
          desc: 'LED 6" Downlight Kit (25W)',
          watts: 25,
        },
      ],
    },
    '8"': {
      Incan: [
        {
          desc: 'Rec. 8" Downlight, (1) 40W Incan. A19',
          watts: 40,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 60W Incan. A19',
          watts: 60,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 75W Incan. A19',
          watts: 75,
        },
      ],
      Halogen: [
        {
          desc: 'Rec. 8" Downlight, (1) 45W BR20 Halogen Lamp',
          watts: 45,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 60W BR30 Halogen Lamp',
          watts: 60,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 60W BR40 Halogen Lamp',
          watts: 60,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 75W BR40 Halogen Lamp',
          watts: 75,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 50W PAR20 Halogen Lamp',
          watts: 50,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 60W PAR30 Halogen Lamp',
          watts: 60,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 75W PAR30 Halogen Lamp',
          watts: 75,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 75W PAR38 Halogen Lamp',
          watts: 75,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 90W PAR38 Halogen Lamp',
          watts: 90,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 120W PAR38 Halogen Lamp',
          watts: 120,
        },
      ],
      "CFL SP": [
        {
          desc: 'Rec. 8" Downlight, (1) 13W CFL Spiral',
          watts: 13,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 18W CFL Spiral',
          watts: 18,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 23W CFL Spiral',
          watts: 23,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 28W CFL Spiral',
          watts: 28,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 36W CFL Spiral',
          watts: 36,
        },
      ],
      "CFL PIN": [
        {
          desc: 'Rec. 8" Downlight, (1) 9W 2P CFL Lamps',
          watts: 14,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 13W 2P CFL Lamps',
          watts: 17,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 15W 2P CFL Lamps',
          watts: 20,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 18W 4P CFL Lamps',
          watts: 26,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 23W 4P CFL Lamps',
          watts: 27,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 26W 4P CFL Lamps',
          watts: 33,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 32W 4P CFL Lamps',
          watts: 34,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 42W 4P CFL Lamps',
          watts: 48,
        },
        {
          desc: 'Rec. 8" Downlight, (2) 9W 2P CFL Lamps',
          watts: 23,
        },
        {
          desc: 'Rec. 8" Downlight, (2) 13W 2P CFL Lamps',
          watts: 31,
        },
        {
          desc: 'Rec. 8" Downlight, (2) 15W 2P CFL Lamps',
          watts: 35,
        },
        {
          desc: 'Rec. 8" Downlight, (2) 18W 2P CFL Lamps',
          watts: 45,
        },
        {
          desc: 'Rec. 8" Downlight, (2) 23W 4P CFL Lamps',
          watts: 50,
        },
        {
          desc: 'Rec. 8" Downlight, (2) 26W 4P CFL Lamps',
          watts: 54,
        },
        {
          desc: 'Rec. 8" Downlight, (2) 32W 4P CFL Lamps',
          watts: 62,
        },
        {
          desc: 'Rec. 8" Downlight, (2) 42W 4P CFL Lamps',
          watts: 100,
        },
      ],
      "LED SI": [
        {
          desc: 'Rec. 8" Downlight, (1) LED 12W A19',
          watts: 12,
        },
        {
          desc: 'Rec. 8" Downlight, (1) LED 15W A19',
          watts: 15,
        },
        {
          desc: 'Rec. 8" Downlight, (1) LED 12W BR30 Lamp',
          watts: 12,
        },
        {
          desc: 'Rec. 8" Downlight, (1) LED 12W PAR30 Lamp',
          watts: 12,
        },
        {
          desc: 'Rec. 8" Downlight, (1) LED 15W BR40 Lamp',
          watts: 15,
        },
        {
          desc: 'Rec. 8" Downlight, (1) LED 15W PAR38 Lamp',
          watts: 15,
        },
      ],
      "LED PIN": [
        {
          desc: 'Rec. 8" Downlight, (1) 12W LED PL (UL-B)',
          watts: 15,
        },
        {
          desc: 'Rec. 8" Downlight, (1) 15W LED PL (UL-B)',
          watts: 18,
        },
        {
          desc: 'Rec. 8" Downlight, (2) 6W LED PL (UL-B)',
          watts: 16,
        },
        {
          desc: 'Rec. 8" Downlight, (2) 9W LED PL (UL-B)',
          watts: 24,
        },
        {
          desc: 'Rec. 8" Downlight, (2) 12W LED PL (UL-B)',
          watts: 30,
        },
        {
          desc: 'Rec. 8" Downlight, (2) 15W LED PL (UL-B)',
          watts: 36,
        },
      ],
      LED: [
        {
          desc: 'LED 8" Downlight Kit (15W)',
          watts: 15,
        },
        {
          desc: 'LED 8" Downlight Kit (20W)',
          watts: 20,
        },
        {
          desc: 'LED 8" Downlight Kit (25W)',
          watts: 25,
        },
        {
          desc: 'LED 8" Downlight Kit (30W)',
          watts: 30,
        },
      ],
    },
    '10"': {
      "CFL PIN": [
        {
          desc: 'Rec. 10" Downlight, (2) 18W 2P CFL Lamps',
          watts: 45,
        },
        {
          desc: 'Rec. 10" Downlight, (2) 23W 4P CFL Lamps',
          watts: 50,
        },
        {
          desc: 'Rec. 10" Downlight, (2) 26W 4P CFL Lamps',
          watts: 54,
        },
        {
          desc: 'Rec. 10" Downlight, (2) 32W 4P CFL Lamps',
          watts: 62,
        },
        {
          desc: 'Rec. 10" Downlight, (2) 42W 4P CFL Lamps',
          watts: 100,
        },
        {
          desc: 'Rec. 10" Downlight, (3) 18W 2P CFL Lamps',
          watts: 71,
        },
        {
          desc: 'Rec. 10" Downlight, (3) 23W 4P CFL Lamps',
          watts: 75,
        },
        {
          desc: 'Rec. 10" Downlight, (3) 26W 4P CFL Lamps',
          watts: 99,
        },
        {
          desc: 'Rec. 10" Downlight, (3) 32W 4P CFL Lamps',
          watts: 114,
        },
        {
          desc: 'Rec. 10" Downlight, (3) 42W 4P CFL Lamps',
          watts: 141,
        },
      ],
      "LED PIN": [
        {
          desc: 'Rec. 10" Downlight, (2) 12W LED PL (UL-B)',
          watts: 30,
        },
        {
          desc: 'Rec. 10" Downlight, (2) 15W LED PL (UL-B)',
          watts: 36,
        },
        {
          desc: 'Rec. 10" Downlight, (2) 18W LED PL (UL-B)',
          watts: 42,
        },
        {
          desc: 'Rec. 10" Downlight, (3) 12W LED PL (UL-B)',
          watts: 45,
        },
        {
          desc: 'Rec. 10" Downlight, (3) 15W LED PL (UL-B)',
          watts: 54,
        },
        {
          desc: 'Rec. 10" Downlight, (3) 18W LED PL (UL-B)',
          watts: 63,
        },
      ],
      LED: [
        {
          desc: 'LED 10" Downlight Kit (20W)',
          watts: 20,
        },
        {
          desc: 'LED 10" Downlight Kit (25W)',
          watts: 25,
        },
        {
          desc: 'LED 10" Downlight Kit (30W)',
          watts: 30,
        },
        {
          desc: 'LED 10" Downlight Kit (40W)',
          watts: 40,
        },
      ],
    },
    '12"': {
      "CFL PIN": [
        {
          desc: 'Rec. 12" Downlight, (2) 23W 4P CFL Lamps',
          watts: 50,
        },
        {
          desc: 'Rec. 12" Downlight, (2) 26W 4P CFL Lamps',
          watts: 54,
        },
        {
          desc: 'Rec. 12" Downlight, (2) 32W 4P CFL Lamps',
          watts: 62,
        },
        {
          desc: 'Rec. 12" Downlight, (2) 42W 4P CFL Lamps',
          watts: 100,
        },
        {
          desc: 'Rec. 12" Downlight, (3) 23W 4P CFL Lamps',
          watts: 75,
        },
        {
          desc: 'Rec. 12" Downlight, (3) 26W 4P CFL Lamps',
          watts: 99,
        },
        {
          desc: 'Rec. 12" Downlight, (3) 32W 4P CFL Lamps',
          watts: 114,
        },
        {
          desc: 'Rec. 12" Downlight, (3) 42W 4P CFL Lamps',
          watts: 141,
        },
      ],
      "LED PIN": [
        {
          desc: 'Rec. 12" Downlight, (2) 15W LED PL (UL-B)',
          watts: 36,
        },
        {
          desc: 'Rec. 12" Downlight, (2) 18W LED PL (UL-B)',
          watts: 42,
        },
        {
          desc: 'Rec. 12" Downlight, (3) 15W LED PL (UL-B)',
          watts: 54,
        },
        {
          desc: 'Rec. 12" Downlight, (3) 18W LED PL (UL-B)',
          watts: 63,
        },
      ],
      LED: [
        {
          desc: 'LED 12" Downlight Kit (30W)',
          watts: 30,
        },
        {
          desc: 'LED 12" Downlight Kit (40W)',
          watts: 40,
        },
        {
          desc: 'LED 12" Downlight Kit (50W)',
          watts: 50,
        },
        {
          desc: 'LED 12" Downlight Kit (60W)',
          watts: 60,
        },
      ],
      HID: [
        {
          desc: 'Recessed 12" Downlight, (1) 100W HID Lamp',
          watts: 115,
        },
        {
          desc: 'Recessed 12" Downlight, (1) 150W HID Lamp',
          watts: 165,
        },
      ],
    },
  },
  "Recessed Canopy": {
    Square: {
      Incan: [
        {
          desc: "Rec. SQ Canopy, (1) 100W A19",
          watts: 100,
        },
        {
          desc: "Rec. SQ Canopy, (1) 75W A19",
          watts: 75,
        },
        {
          desc: "Rec. SQ Canopy, (1) 60W A19",
          watts: 60,
        },
        {
          desc: "Rec. SQ Canopy, (1) 40W A19",
          watts: 40,
        },
        {
          desc: "Rec. SQ Canopy, (2) 100W A19",
          watts: 200,
        },
        {
          desc: "Rec. SQ Canopy, (2) 75W A19",
          watts: 150,
        },
        {
          desc: "Rec. SQ Canopy, (2) 60W A19",
          watts: 120,
        },
        {
          desc: "Rec. SQ Canopy, (2) 40W A19",
          watts: 80,
        },
      ],
      "CFL SP": [
        {
          desc: "Rec. SQ Canopy, (1) 32W CFL Spiral",
          watts: 32,
        },
        {
          desc: "Rec. SQ Canopy, (1) 26W CFL Spiral",
          watts: 26,
        },
        {
          desc: "Rec. SQ Canopy, (1) 18W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Rec. SQ Canopy, (1) 13W CFL Spiral",
          watts: 13,
        },
        {
          desc: "Rec. SQ Canopy, (2) 32W CFL Spiral",
          watts: 64,
        },
        {
          desc: "Rec. SQ Canopy, (2) 26W CFL Spiral",
          watts: 52,
        },
        {
          desc: "Rec. SQ Canopy, (2) 18W CFL Spiral",
          watts: 36,
        },
        {
          desc: "Rec. SQ Canopy, (2) 13W CFL Spiral",
          watts: 26,
        },
      ],
      "LED SI": [
        {
          desc: "Rec. SQ Canopy, (1) 15W LED A19",
          watts: 15,
        },
        {
          desc: "Rec. SQ Canopy, (1) 12W LED A19",
          watts: 12,
        },
        {
          desc: "Rec. SQ Canopy, (1) 8W LED A19",
          watts: 8,
        },
        {
          desc: "Rec. SQ Canopy, (2) 15W LED A19",
          watts: 30,
        },
        {
          desc: "Rec. SQ Canopy, (2) 12W LED A19",
          watts: 24,
        },
        {
          desc: "Rec. SQ Canopy, (2) 8W LED A19",
          watts: 16,
        },
      ],
      "CFL PIN": [
        {
          desc: "Rec. SQ Canopy, (1) 13W 2P CFL Lamp",
          watts: 17,
        },
        {
          desc: "Rec. SQ Canopy, (1) 15W 4P CFL Lamp",
          watts: 20,
        },
        {
          desc: "Rec. SQ Canopy, (1) 18W 4P CFL Lamp",
          watts: 26,
        },
        {
          desc: "Rec. SQ Canopy, (1) 23W 4P CFL Lamp",
          watts: 27,
        },
        {
          desc: "Rec. SQ Canopy, (1) 26W 4P CFL Lamp",
          watts: 33,
        },
        {
          desc: "Rec. SQ Canopy, (1) 32W 4P CFL Lamp",
          watts: 34,
        },
        {
          desc: "Rec. SQ Canopy, (2) 13W 2P CFL Lamp",
          watts: 31,
        },
        {
          desc: "Rec. SQ Canopy, (2) 15W 4P CFL Lamp",
          watts: 35,
        },
        {
          desc: "Rec. SQ Canopy, (2) 18W 4P CFL Lamp",
          watts: 45,
        },
        {
          desc: "Rec. SQ Canopy, (2) 23W 4P CFL Lamp",
          watts: 50,
        },
        {
          desc: "Rec. SQ Canopy, (2) 26W 4P CFL Lamp",
          watts: 54,
        },
        {
          desc: "Rec. SQ Canopy, (2) 32W 4P CFL Lamp",
          watts: 62,
        },
      ],
    },
    "Square (Drop)": {
      Incan: [
        {
          desc: "Rec. SQ Canopy, (1) 100W A19 - Drop Lens",
          watts: 100,
        },
        {
          desc: "Rec. SQ Canopy, (1) 75W A19 - Drop Lens",
          watts: 75,
        },
        {
          desc: "Rec. SQ Canopy, (1) 60W A19 - Drop Lens",
          watts: 60,
        },
        {
          desc: "Rec. SQ Canopy, (1) 40W A19 - Drop Lens",
          watts: 40,
        },
        {
          desc: "Rec. SQ Canopy, (2) 100W A19 - Drop Lens",
          watts: 200,
        },
        {
          desc: "Rec. SQ Canopy, (2) 75W A19 - Drop Lens",
          watts: 150,
        },
        {
          desc: "Rec. SQ Canopy, (2) 60W A19 - Drop Lens",
          watts: 120,
        },
        {
          desc: "Rec. SQ Canopy, (2) 40W A19 - Drop Lens",
          watts: 80,
        },
      ],
      "CFL SP": [
        {
          desc: "Rec. SQ Canopy, (1) 32W CFL Spiral - Drop Lens",
          watts: 32,
        },
        {
          desc: "Rec. SQ Canopy, (1) 26W CFL Spiral - Drop Lens",
          watts: 26,
        },
        {
          desc: "Rec. SQ Canopy, (1) 18W CFL Spiral - Drop Lens",
          watts: 18,
        },
        {
          desc: "Rec. SQ Canopy, (1) 13W CFL Spiral - Drop Lens",
          watts: 13,
        },
        {
          desc: "Rec. SQ Canopy, (2) 32W CFL Spiral - Drop Lens",
          watts: 64,
        },
        {
          desc: "Rec. SQ Canopy, (2) 26W CFL Spiral - Drop Lens",
          watts: 52,
        },
        {
          desc: "Rec. SQ Canopy, (2) 18W CFL Spiral - Drop Lens",
          watts: 36,
        },
        {
          desc: "Rec. SQ Canopy, (2) 13W CFL Spiral - Drop Lens",
          watts: 26,
        },
      ],
      "LED SI": [
        {
          desc: "Rec. SQ Canopy, (1) 15W LED A19 - Drop Lens",
          watts: 15,
        },
        {
          desc: "Rec. SQ Canopy, (1) 12W LED A19 - Drop Lens",
          watts: 12,
        },
        {
          desc: "Rec. SQ Canopy, (1) 8W LED A19 - Drop Lens",
          watts: 8,
        },
        {
          desc: "Rec. SQ Canopy, (2) 15W LED A19 - Drop Lens",
          watts: 30,
        },
        {
          desc: "Rec. SQ Canopy, (2) 12W LED A19 - Drop Lens",
          watts: 24,
        },
        {
          desc: "Rec. SQ Canopy, (2) 8W LED A19 - Drop Lens",
          watts: 16,
        },
      ],
      "CFL PIN": [
        {
          desc: "Rec. SQ Canopy, (1) 13W 2P CFL Lamp - Drop Lens",
          watts: 17,
        },
        {
          desc: "Rec. SQ Canopy, (1) 15W 4P CFL Lamp - Drop Lens",
          watts: 20,
        },
        {
          desc: "Rec. SQ Canopy, (1) 18W 4P CFL Lamp - Drop Lens",
          watts: 26,
        },
        {
          desc: "Rec. SQ Canopy, (1) 23W 4P CFL Lamp - Drop Lens",
          watts: 27,
        },
        {
          desc: "Rec. SQ Canopy, (1) 26W 4P CFL Lamp - Drop Lens",
          watts: 33,
        },
        {
          desc: "Rec. SQ Canopy, (1) 32W 4P CFL Lamp - Drop Lens",
          watts: 34,
        },
        {
          desc: "Rec. SQ Canopy, (2) 13W 2P CFL Lamp - Drop Lens",
          watts: 31,
        },
        {
          desc: "Rec. SQ Canopy, (2) 15W 4P CFL Lamp - Drop Lens",
          watts: 35,
        },
        {
          desc: "Rec. SQ Canopy, (2) 18W 4P CFL Lamp - Drop Lens",
          watts: 45,
        },
        {
          desc: "Rec. SQ Canopy, (2) 23W 4P CFL Lamp - Drop Lens",
          watts: 50,
        },
        {
          desc: "Rec. SQ Canopy, (2) 26W 4P CFL Lamp - Drop Lens",
          watts: 54,
        },
        {
          desc: "Rec. SQ Canopy, (2) 32W 4P CFL Lamp - Drop Lens",
          watts: 62,
        },
      ],
    },
  },
  "Flush-Mount": {
    Round: {
      Incan: [
        {
          desc: "Round Flush-Mount, (1) 100W A19",
          watts: 100,
        },
        {
          desc: "Round Flush-Mount, (1) 75W A19",
          watts: 75,
        },
        {
          desc: "Round Flush-Mount, (1) 60W A19",
          watts: 60,
        },
        {
          desc: "Round Flush-Mount, (1) 40W A19",
          watts: 40,
        },
        {
          desc: "Round Flush-Mount, (2) 100W A19",
          watts: 200,
        },
        {
          desc: "Round Flush-Mount, (2) 75W A19",
          watts: 150,
        },
        {
          desc: "Round Flush-Mount, (2) 60W A19",
          watts: 120,
        },
        {
          desc: "Round Flush-Mount, (2) 40W A19",
          watts: 80,
        },
      ],
      "CFL SP": [
        {
          desc: "Round Flush-Mount, (1) 32W CFL Spiral",
          watts: 32,
        },
        {
          desc: "Round Flush-Mount, (1) 26W CFL Spiral",
          watts: 26,
        },
        {
          desc: "Round Flush-Mount, (1) 18W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Round Flush-Mount, (1) 13W CFL Spiral",
          watts: 13,
        },
        {
          desc: "Round Flush-Mount, (2) 32W CFL Spiral",
          watts: 64,
        },
        {
          desc: "Round Flush-Mount, (2) 26W CFL Spiral",
          watts: 52,
        },
        {
          desc: "Round Flush-Mount, (2) 18W CFL Spiral",
          watts: 36,
        },
        {
          desc: "Round Flush-Mount, (2) 13W CFL Spiral",
          watts: 26,
        },
      ],
      "LED SI": [
        {
          desc: "Round Flush-Mount, (1) 15W LED A19",
          watts: 15,
        },
        {
          desc: "Round Flush-Mount, (1) 12W LED A19",
          watts: 12,
        },
        {
          desc: "Round Flush-Mount, (1) 8W LED A19",
          watts: 8,
        },
        {
          desc: "Round Flush-Mount, (2) 15W LED A19",
          watts: 30,
        },
        {
          desc: "Round Flush-Mount, (2) 12W LED A19",
          watts: 24,
        },
        {
          desc: "Round Flush-Mount, (2) 8W LED A19",
          watts: 16,
        },
      ],
      "CFL PIN": [
        {
          desc: "Round Flush-Mount, (1) 13W 2P CFL Lamp",
          watts: 17,
        },
        {
          desc: "Round Flush-Mount, (1) 15W 4P CFL Lamp",
          watts: 20,
        },
        {
          desc: "Round Flush-Mount, (1) 18W 4P CFL Lamp",
          watts: 26,
        },
        {
          desc: "Round Flush-Mount, (1) 23W 4P CFL Lamp",
          watts: 27,
        },
        {
          desc: "Round Flush-Mount, (1) 26W 4P CFL Lamp",
          watts: 33,
        },
        {
          desc: "Round Flush-Mount, (1) 32W 4P CFL Lamp",
          watts: 34,
        },
        {
          desc: "Round Flush-Mount, (2) 13W 2P CFL Lamp",
          watts: 31,
        },
        {
          desc: "Round Flush-Mount, (2) 15W 4P CFL Lamp",
          watts: 35,
        },
        {
          desc: "Round Flush-Mount, (2) 18W 4P CFL Lamp",
          watts: 45,
        },
        {
          desc: "Round Flush-Mount, (2) 23W 4P CFL Lamp",
          watts: 50,
        },
        {
          desc: "Round Flush-Mount, (2) 26W 4P CFL Lamp",
          watts: 54,
        },
        {
          desc: "Round Flush-Mount, (2) 32W 4P CFL Lamp",
          watts: 62,
        },
      ],
    },
  },
  "Sconce/Bulkhead": {
    Sconce: {
      Incan: [
        {
          desc: "Wall-Mount Sconce, (1) 60W A19",
          watts: 60,
        },
        {
          desc: "Wall-Mount Sconce, (1) 40W A19",
          watts: 40,
        },
        {
          desc: "Wall-Mount Sconce, (2) 60W A19",
          watts: 120,
        },
        {
          desc: "Wall-Mount Sconce, (2) 40W A19",
          watts: 80,
        },
      ],
      "CFL SP": [
        {
          desc: "Wall-Mount Sconce, (1) 18W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Wall-Mount Sconce, (1) 13W CFL Spiral",
          watts: 13,
        },
        {
          desc: "Wall-Mount Sconce, (2) 18W CFL Spiral",
          watts: 36,
        },
        {
          desc: "Wall-Mount Sconce, (2) 13W CFL Spiral",
          watts: 26,
        },
      ],
      "CFL PIN": [
        {
          desc: "Wall-Mount Sconce, (1) 7W 2P CFL Twin",
          watts: 10,
        },
        {
          desc: "Wall-Mount Sconce, (1) 9W 2P CFL Twin",
          watts: 11,
        },
        {
          desc: "Wall-Mount Sconce, (1) 13W 2P CFL Twin",
          watts: 17,
        },
        {
          desc: "Wall-Mount Sconce, (2) 7W 2P CFL Twin",
          watts: 21,
        },
        {
          desc: "Wall-Mount Sconce, (2) 9W 2P CFL Twin",
          watts: 23,
        },
        {
          desc: "Wall-Mount Sconce, (2) 13W 2P CFL Twin",
          watts: 31,
        },
      ],
    },
    Bulkhead: {
      Incan: [
        {
          desc: "Bulkhead, (1) 60W A19",
          watts: 60,
        },
        {
          desc: "Bulkhead, (1) 40W A19",
          watts: 40,
        },
        {
          desc: "Bulkhead, (2) 60W A19",
          watts: 120,
        },
        {
          desc: "Bulkhead, (2) 40W A19",
          watts: 80,
        },
      ],
      "CFL SP": [
        {
          desc: "Bulkhead, (1) 18W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Bulkhead, (1) 13W CFL Spiral",
          watts: 13,
        },
        {
          desc: "Bulkhead, (2) 18W CFL Spiral",
          watts: 36,
        },
        {
          desc: "Bulkhead, (2) 13W CFL Spiral",
          watts: 26,
        },
      ],
      "CFL PIN": [
        {
          desc: "Bulkhead, (1) 7W 2P CFL Twin",
          watts: 10,
        },
        {
          desc: "Bulkhead, (1) 9W 2P CFL Twin",
          watts: 11,
        },
        {
          desc: "Bulkhead, (1) 13W 2P CFL Twin",
          watts: 17,
        },
        {
          desc: "Bulkhead, (2) 7W 2P CFL Twin",
          watts: 21,
        },
        {
          desc: "Bulkhead, (2) 9W 2P CFL Twin",
          watts: 23,
        },
        {
          desc: "Bulkhead, (2) 13W 2P CFL Twin",
          watts: 31,
        },
      ],
    },
  },
  Vanity: {
    Linear: {
      T8: [
        {
          desc: 'Vanity 2\u0027 Wrap, (1) 17W 24" T8',
          watts: 15,
        },
        {
          desc: 'Vanity 2\u0027 Wrap, (2) 17W 24" T8',
          watts: 33,
        },
        {
          desc: 'Vanity 3\u0027 Wrap, (1) 25W 36" T8',
          watts: 26,
        },
        {
          desc: 'Vanity 3\u0027 Wrap, (2) 25W 36" T8',
          watts: 46,
        },
        {
          desc: 'Vanity 4\u0027 Wrap, (1) 32W 48" T8',
          watts: 28,
        },
        {
          desc: 'Vanity 4\u0027 Wrap, (2) 32W 48" T8',
          watts: 56,
        },
      ],
    },
    Lamp: {
      Incan: [
        {
          desc: "Vanity Fixture, (1) 100W A19",
          watts: 100,
        },
        {
          desc: "Vanity Fixture, (1) 60W A19",
          watts: 60,
        },
        {
          desc: "Vanity Fixture, (1) 40W A19",
          watts: 40,
        },
        {
          desc: "Vanity Fixture, (1) 25W A19",
          watts: 25,
        },
        {
          desc: "Vanity Fixture, (1) 60W G40 Globe",
          watts: 60,
        },
        {
          desc: "Vanity Fixture, (1) 40W G40 Globe",
          watts: 40,
        },
        {
          desc: "Vanity Fixture, (1) 25W G25 Globe",
          watts: 25,
        },
        {
          desc: "Vanity Fixture, (2) 100W A19",
          watts: 200,
        },
        {
          desc: "Vanity Fixture, (2) 60W A19",
          watts: 120,
        },
        {
          desc: "Vanity Fixture, (2) 40W A19",
          watts: 80,
        },
        {
          desc: "Vanity Fixture, (2) 25W A19",
          watts: 50,
        },
        {
          desc: "Vanity Fixture, (2) 60W G40 Globe",
          watts: 120,
        },
        {
          desc: "Vanity Fixture, (2) 40W G40 Globe",
          watts: 80,
        },
        {
          desc: "Vanity Fixture, (2) 25W G25 Globe",
          watts: 50,
        },
        {
          desc: "Vanity Fixture, (3) 100W A19",
          watts: 300,
        },
        {
          desc: "Vanity Fixture, (3) 60W A19",
          watts: 180,
        },
        {
          desc: "Vanity Fixture, (3) 40W A19",
          watts: 120,
        },
        {
          desc: "Vanity Fixture, (3) 25W A19",
          watts: 75,
        },
        {
          desc: "Vanity Fixture, (3) 60W G40 Globe",
          watts: 180,
        },
        {
          desc: "Vanity Fixture, (3) 40W G40 Globe",
          watts: 120,
        },
        {
          desc: "Vanity Fixture, (3) 25W G25 Globe",
          watts: 75,
        },
      ],
      "CFL SP": [
        {
          desc: "Vanity Fixture, (1) 26W CFL Spiral",
          watts: 26,
        },
        {
          desc: "Vanity Fixture, (1) 18W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Vanity Fixture, (1) 13W CFL Spiral",
          watts: 13,
        },
        {
          desc: "Vanity Fixture, (1) 9W CFL Spiral",
          watts: 9,
        },
        {
          desc: "Vanity Fixture, (2) 26W CFL Spiral",
          watts: 52,
        },
        {
          desc: "Vanity Fixture, (2) 18W CFL Spiral",
          watts: 36,
        },
        {
          desc: "Vanity Fixture, (2) 13W CFL Spiral",
          watts: 26,
        },
        {
          desc: "Vanity Fixture, (2) 9W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Vanity Fixture, (3) 26W CFL Spiral",
          watts: 78,
        },
        {
          desc: "Vanity Fixture, (3) 18W CFL Spiral",
          watts: 54,
        },
        {
          desc: "Vanity Fixture, (3) 13W CFL Spiral",
          watts: 39,
        },
        {
          desc: "Vanity Fixture, (3) 9W CFL Spiral",
          watts: 27,
        },
      ],
      "LED SI": [
        {
          desc: "Vanity Fixture, (1) 12W LED A19",
          watts: 12,
        },
        {
          desc: "Vanity Fixture, (1) 8W LED A19",
          watts: 8,
        },
        {
          desc: "Vanity Fixture, (1) 6W LED A19",
          watts: 6,
        },
        {
          desc: "Vanity Fixture, (2) 12W LED A19",
          watts: 24,
        },
        {
          desc: "Vanity Fixture, (2) 8W LED A19",
          watts: 16,
        },
        {
          desc: "Vanity Fixture, (2) 6W LED A19",
          watts: 12,
        },
        {
          desc: "Vanity Fixture, (3) 12W LED A19",
          watts: 36,
        },
        {
          desc: "Vanity Fixture, (3) 8W LED A19",
          watts: 24,
        },
        {
          desc: "Vanity Fixture, (3) 6W LED A19",
          watts: 18,
        },
      ],
    },
  },
  Pendant: {
    Shade: {
      Incan: [
        {
          desc: "Pendant Shade, (1) 100W A19",
          watts: 100,
        },
        {
          desc: "Pendant Shade, (1) 75W A19",
          watts: 75,
        },
        {
          desc: "Pendant Shade, (1) 60W A19",
          watts: 60,
        },
        {
          desc: "Pendant Shade, (1) 40W A19",
          watts: 40,
        },
      ],
      "CFL SP": [
        {
          desc: "Pendant Shade, (1) 32W CFL Spiral",
          watts: 32,
        },
        {
          desc: "Pendant Shade, (1) 26W CFL Spiral",
          watts: 26,
        },
        {
          desc: "Pendant Shade, (1) 18W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Pendant Shade, (1) 13W CFL Spiral",
          watts: 13,
        },
      ],
      "LED SI": [
        {
          desc: "Pendant Shade, (1) 15W LED A19",
          watts: 15,
        },
        {
          desc: "Pendant Shade, (1) 12W LED A19",
          watts: 12,
        },
        {
          desc: "Pendant Shade, (1) 8W LED A19",
          watts: 8,
        },
        {
          desc: "Pendant Shade, (1) LED 12W PAR30 Lamp",
          watts: 12,
        },
        {
          desc: "Pendant Shade, (1) LED 15W PAR38 Lamp",
          watts: 15,
        },
        {
          desc: "Pendant Shade, (1) LED 12W BR30 Lamp",
          watts: 12,
        },
        {
          desc: "Pendant Shade, (1) LED 15W BR40 Lamp",
          watts: 15,
        },
      ],
      Halogen: [
        {
          desc: "Pendant Shade, (1) 65W PAR30 Lamp",
          watts: 65,
        },
        {
          desc: "Pendant Shade, (1) 75W PAR38 Lamp",
          watts: 75,
        },
        {
          desc: "Pendant Shade, (1) 65W BR30 Lamp",
          watts: 65,
        },
        {
          desc: "Pendant Shade, (1) 65W BR40 Lamp",
          watts: 65,
        },
      ],
    },
    "XP Box": {
      "CFL PIN": [
        {
          desc: "Pendant Box Fixture, (1) 13W 2P CFL",
          watts: 17,
        },
        {
          desc: "Pendant Box Fixture, (1) 18W 4P CFL",
          watts: 26,
        },
        {
          desc: "Pendant Box Fixture, (2) 13W 2P CFL",
          watts: 31,
        },
        {
          desc: "Pendant Box Fixture, (2) 18W 4P CFL",
          watts: 45,
        },
      ],
    },
    "Large Round": {
      Incan: [
        {
          desc: "Round Pendant, (1) 100W A19",
          watts: 100,
        },
        {
          desc: "Round Pendant, (1) 75W A19",
          watts: 75,
        },
        {
          desc: "Round Pendant, (1) 60W A19",
          watts: 60,
        },
        {
          desc: "Round Pendant, (2) 100W A19",
          watts: 200,
        },
        {
          desc: "Round Pendant, (2) 60W A19",
          watts: 120,
        },
        {
          desc: "Round Pendant, (2) 25W A19",
          watts: 50,
        },
        {
          desc: "Round Pendant, (2) 60W G40 Globe",
          watts: 120,
        },
        {
          desc: "Round Pendant, (2) 40W G40 Globe",
          watts: 80,
        },
        {
          desc: "Round Pendant, (2) 25W G25 Globe",
          watts: 50,
        },
        {
          desc: "Round Pendant, (3) 100W A19",
          watts: 300,
        },
        {
          desc: "Round Pendant, (3) 60W A19",
          watts: 180,
        },
        {
          desc: "Round Pendant, (3) 40W A19",
          watts: 120,
        },
        {
          desc: "Round Pendant, (3) 25W A19",
          watts: 75,
        },
        {
          desc: "Round Pendant, (3) 60W G40 Globe",
          watts: 180,
        },
        {
          desc: "Round Pendant, (3) 40W G40 Globe",
          watts: 120,
        },
        {
          desc: "Round Pendant, (3) 25W G25 Globe",
          watts: 75,
        },
      ],
      "CFL SP": [
        {
          desc: "Round Pendant, (1) 32W CFL Spiral",
          watts: 32,
        },
        {
          desc: "Round Pendant, (1) 26W CFL Spiral",
          watts: 26,
        },
        {
          desc: "Round Pendant, (1) 18W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Round Pendant, (1) 13W CFL Spiral",
          watts: 13,
        },
        {
          desc: "Round Pendant, (2) 26W CFL Spiral",
          watts: 52,
        },
        {
          desc: "Round Pendant, (2) 18W CFL Spiral",
          watts: 36,
        },
        {
          desc: "Round Pendant, (2) 13W CFL Spiral",
          watts: 26,
        },
        {
          desc: "Round Pendant, (2) 9W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Round Pendant, (3) 26W CFL Spiral",
          watts: 78,
        },
        {
          desc: "Round Pendant, (3) 18W CFL Spiral",
          watts: 54,
        },
        {
          desc: "Round Pendant, (3) 13W CFL Spiral",
          watts: 39,
        },
        {
          desc: "Round Pendant, (3) 9W CFL Spiral",
          watts: 27,
        },
      ],
      "LED SI": [
        {
          desc: "Round Pendant, (1) 15W LED A19",
          watts: 15,
        },
        {
          desc: "Round Pendant, (1) 12W LED A19",
          watts: 12,
        },
        {
          desc: "Round Pendant, (1) 8W LED A19",
          watts: 8,
        },
        {
          desc: "Round Pendant, (2) 12W LED A19",
          watts: 24,
        },
        {
          desc: "Round Pendant, (2) 8W LED A19",
          watts: 16,
        },
        {
          desc: "Round Pendant, (3) 12W LED A19",
          watts: 36,
        },
        {
          desc: "Round Pendant, (3) 8W LED A19",
          watts: 24,
        },
      ],
      "U-Lamp/Biax": [
        {
          desc: "Round Pendant, (2) 40W Biax 2G11 Lamps",
          watts: 85,
        },
        {
          desc: "Round Pendant, (3) 40W Biax 2G11 Lamps",
          watts: 133,
        },
      ],
      "CFL PIN": [
        {
          desc: "Round Pendant, (2) 18W 2P CFL Lamps",
          watts: 45,
        },
        {
          desc: "Round Pendant, (2) 23W 4P CFL Lamps",
          watts: 50,
        },
        {
          desc: "Round Pendant, (2) 26W 4P CFL Lamps",
          watts: 54,
        },
        {
          desc: "Round Pendant, (2) 32W 4P CFL Lamps",
          watts: 62,
        },
        {
          desc: "Round Pendant, (2) 42W 4P CFL Lamps",
          watts: 100,
        },
        {
          desc: "Round Pendant, (3) 18W 2P CFL Lamps",
          watts: 71,
        },
        {
          desc: "Round Pendant, (3) 23W 4P CFL Lamps",
          watts: 75,
        },
        {
          desc: "Round Pendant, (3) 26W 4P CFL Lamps",
          watts: 99,
        },
        {
          desc: "Round Pendant, (3) 32W 4P CFL Lamps",
          watts: 114,
        },
        {
          desc: "Round Pendant, (3) 42W 4P CFL Lamps",
          watts: 141,
        },
      ],
    },
  },
  Socket: {
    Keyless: {
      Incan: [
        {
          desc: "Keyless Socket, (1) 100W A19",
          watts: 100,
        },
        {
          desc: "Keyless Socket, (1) 75W A19",
          watts: 75,
        },
        {
          desc: "Keyless Socket, (1) 60W A19",
          watts: 60,
        },
        {
          desc: "Keyless Socket, (1) 40W A19",
          watts: 40,
        },
        {
          desc: "Keyless Socket, (1) 200W PS25 (E26)",
          watts: 200,
        },
        {
          desc: "Keyless Socket, (1) 300W PS25 (E26)",
          watts: 300,
        },
      ],
      "CFL SP": [
        {
          desc: "Keyless Socket, (1) 32W CFL Spiral",
          watts: 32,
        },
        {
          desc: "Keyless Socket, (1) 26W CFL Spiral",
          watts: 26,
        },
        {
          desc: "Keyless Socket, (1) 18W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Keyless Socket, (1) 13W CFL Spiral",
          watts: 13,
        },
        {
          desc: "Keyless Socket, (1) 85W CFL Spiral (E39)",
          watts: 85,
        },
        {
          desc: "Keyless Socket, (1) 105W CFL Spiral (E39)",
          watts: 105,
        },
      ],
      "LED SI": [
        {
          desc: "Keyless Socket, (1) 15W LED A19",
          watts: 15,
        },
        {
          desc: "Keyless Socket, (1) 12W LED A19",
          watts: 12,
        },
        {
          desc: "Keyless Socket, (1) 8W LED A19",
          watts: 8,
        },
      ],
      Halogen: [
        {
          desc: "Keyless Socket, (1) 65W PAR30 Lamp",
          watts: 65,
        },
        {
          desc: "Keyless Socket, (1) 75W PAR38 Lamp",
          watts: 75,
        },
        {
          desc: "Keyless Socket, (1) 65W BR30 Lamp",
          watts: 65,
        },
        {
          desc: "Keyless Socket, (1) 65W BR40 Lamp",
          watts: 65,
        },
      ],
    },
  },
  Track: {
    Track: {
      Incan: [
        {
          desc: "Track Fixture, (1) 100W A19",
          watts: 100,
        },
        {
          desc: "Track Fixture, (1) 75W A19",
          watts: 75,
        },
        {
          desc: "Track Fixture, (1) 60W A19",
          watts: 60,
        },
        {
          desc: "Track Fixture, (1) 40W A19",
          watts: 40,
        },
      ],
      "CFL SP": [
        {
          desc: "Track Fixture, (1) 32W CFL Spiral",
          watts: 32,
        },
        {
          desc: "Track Fixture, (1) 26W CFL Spiral",
          watts: 26,
        },
        {
          desc: "Track Fixture, (1) 18W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Track Fixture, (1) 13W CFL Spiral",
          watts: 13,
        },
      ],
      "LED SI": [
        {
          desc: "Track Fixture, (1) 15W LED A19",
          watts: 15,
        },
        {
          desc: "Track Fixture, (1) 12W LED A19",
          watts: 12,
        },
        {
          desc: "Track Fixture, (1) 8W LED A19",
          watts: 8,
        },
      ],
      Halogen: [
        {
          desc: "Track Fixture, (1) 65W PAR30 Lamp",
          watts: 65,
        },
        {
          desc: "Track Fixture, (1) 75W PAR38 Lamp",
          watts: 75,
        },
        {
          desc: "Track Fixture, (1) 65W BR30 Lamp",
          watts: 65,
        },
        {
          desc: "Track Fixture, (1) 65W BR40 Lamp",
          watts: 65,
        },
        {
          desc: "Track Fixture, (1) 50W PAR20 Lamp",
          watts: 50,
        },
        {
          desc: "Track Fixture, (1) 50W MR16 Lamp (Bi-Pin)",
          watts: 50,
        },
        {
          desc: "Track Fixture, (1) 50W MR16 Lamp (GU10)",
          watts: 50,
        },
        {
          desc: "Track Fixture, (1) 35W MR16 Lamp (Bi-Pin)",
          watts: 35,
        },
        {
          desc: "Track Fixture, (1) 35W MR16 Lamp (GU10)",
          watts: 35,
        },
      ],
      CMH: [
        {
          desc: "Track Fixture, (1) Ceramic Metal Halide PAR30",
          watts: 75,
        },
      ],
      LED: [
        {
          desc: "Integrated LED Track Fixture - 10W",
          watts: 10,
        },
        {
          desc: "Integrated LED Track Fixture - 15W",
          watts: 15,
        },
        {
          desc: "Integrated LED Track Fixture - 20W",
          watts: 20,
        },
      ],
    },
  },
  "Vapor Jar": {
    "Wall-Mount": {
      Incan: [
        {
          desc: "Wall-Mount Jar, (1) 100W A19",
          watts: 100,
        },
        {
          desc: "Wall-Mount Jar, (1) 75W A19",
          watts: 75,
        },
        {
          desc: "Wall-Mount Jar, (1) 60W A19",
          watts: 60,
        },
        {
          desc: "Wall-Mount Jar, (1) 40W A19",
          watts: 40,
        },
      ],
      "CFL SP": [
        {
          desc: "Wall-Mount Jar, (1) 32W CFL Spiral",
          watts: 32,
        },
        {
          desc: "Wall-Mount Jar, (1) 26W CFL Spiral",
          watts: 26,
        },
        {
          desc: "Wall-Mount Jar, (1) 18W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Wall-Mount Jar, (1) 13W CFL Spiral",
          watts: 13,
        },
      ],
      "LED SI": [
        {
          desc: "Wall-Mount Jar, (1) 15W LED A19",
          watts: 15,
        },
        {
          desc: "Wall-Mount Jar, (1) 12W LED A19",
          watts: 12,
        },
        {
          desc: "Wall-Mount Jar, (1) 8W LED A19",
          watts: 8,
        },
      ],
      "CFL PIN": [
        {
          desc: "Wall-Mount Jar, (1) 13W 2P CFL Lamp",
          watts: 17,
        },
        {
          desc: "Wall-Mount Jar, (1) 15W 4P CFL Lamp",
          watts: 20,
        },
        {
          desc: "Wall-Mount Jar, (1) 18W 4P CFL Lamp",
          watts: 26,
        },
        {
          desc: "Wall-Mount Jar, (1) 23W 4P CFL Lamp",
          watts: 27,
        },
        {
          desc: "Wall-Mount Jar, (1) 26W 4P CFL Lamp",
          watts: 33,
        },
        {
          desc: "Wall-Mount Jar, (1) 32W 4P CFL Lamp",
          watts: 34,
        },
      ],
    },
    "Ceiling-Mount": {
      Incan: [
        {
          desc: "Ceiling-Mount Jar, (1) 100W A19",
          watts: 100,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 75W A19",
          watts: 75,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 60W A19",
          watts: 60,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 40W A19",
          watts: 40,
        },
      ],
      "CFL SP": [
        {
          desc: "Ceiling-Mount Jar, (1) 32W CFL Spiral",
          watts: 32,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 26W CFL Spiral",
          watts: 26,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 18W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 13W CFL Spiral",
          watts: 13,
        },
      ],
      "LED SI": [
        {
          desc: "Ceiling-Mount Jar, (1) 15W LED A19",
          watts: 15,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 12W LED A19",
          watts: 12,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 8W LED A19",
          watts: 8,
        },
      ],
      "CFL PIN": [
        {
          desc: "Ceiling-Mount Jar, (1) 13W 2P CFL Lamp",
          watts: 17,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 15W 4P CFL Lamp",
          watts: 20,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 18W 4P CFL Lamp",
          watts: 26,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 23W 4P CFL Lamp",
          watts: 27,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 26W 4P CFL Lamp",
          watts: 33,
        },
        {
          desc: "Ceiling-Mount Jar, (1) 32W 4P CFL Lamp",
          watts: 34,
        },
      ],
    },
    "XP Jar": {
      Incan: [
        {
          desc: "Explosion-Proof Jar, (1) 100W A19",
          watts: 100,
        },
        {
          desc: "Explosion-Proof Jar, (1) 75W A19",
          watts: 75,
        },
        {
          desc: "Explosion-Proof Jar, (1) 60W A19",
          watts: 60,
        },
        {
          desc: "Explosion-Proof Jar, (1) 40W A19",
          watts: 40,
        },
      ],
      "CFL SP": [
        {
          desc: "Explosion-Proof Jar, (1) 32W CFL Spiral",
          watts: 32,
        },
        {
          desc: "Explosion-Proof Jar, (1) 26W CFL Spiral",
          watts: 26,
        },
        {
          desc: "Explosion-Proof Jar, (1) 18W CFL Spiral",
          watts: 18,
        },
        {
          desc: "Explosion-Proof Jar, (1) 13W CFL Spiral",
          watts: 13,
        },
      ],
      "LED SI": [
        {
          desc: "Explosion-Proof Jar, (1) 15W LED A19",
          watts: 15,
        },
        {
          desc: "Explosion-Proof Jar, (1) 12W LED A19",
          watts: 12,
        },
        {
          desc: "Explosion-Proof Jar, (1) 8W LED A19",
          watts: 8,
        },
      ],
      "CFL PIN": [
        {
          desc: "Explosion-Proof Jar, (1) 13W 2P CFL Lamp",
          watts: 17,
        },
        {
          desc: "Explosion-Proof Jar, (1) 15W 4P CFL Lamp",
          watts: 20,
        },
        {
          desc: "Explosion-Proof Jar, (1) 18W 4P CFL Lamp",
          watts: 26,
        },
        {
          desc: "Explosion-Proof Jar, (1) 23W 4P CFL Lamp",
          watts: 27,
        },
        {
          desc: "Explosion-Proof Jar, (1) 26W 4P CFL Lamp",
          watts: 33,
        },
        {
          desc: "Explosion-Proof Jar, (1) 32W 4P CFL Lamp",
          watts: 34,
        },
      ],
    },
  },
  "High Bay": {
    Round: {
      HID: [
        {
          desc: "Round High Bay, (1) 1000W MH Lamp (EX39)",
          watts: 1040,
        },
        {
          desc: "Round High Bay, (1) 400W MH Lamp (EX39)",
          watts: 425,
        },
        {
          desc: "Round High Bay, (1) 250W MH Lamp",
          watts: 265,
        },
        {
          desc: "Round High Bay, (1) 150W MH Lamp",
          watts: 160,
        },
        {
          desc: "Round High Bay, (1) 100W MH Lamp",
          watts: 110,
        },
      ],
      LED: [
        {
          desc: "LED Round High Bay 75W",
          watts: 75,
        },
        {
          desc: "LED Round High Bay 100W",
          watts: 100,
        },
        {
          desc: "LED Round High Bay 125W",
          watts: 125,
        },
        {
          desc: "LED Round High Bay 150W",
          watts: 150,
        },
        {
          desc: "LED Round High Bay 200W",
          watts: 200,
        },
        {
          desc: "LED Round High Bay 250W",
          watts: 250,
        },
        {
          desc: "LED Round High Bay 300W",
          watts: 300,
        },
        {
          desc: "LED Round High Bay 400W",
          watts: 400,
        },
        {
          desc: "LED Round High Bay 500W",
          watts: 500,
        },
      ],
      "LED SI": [
        {
          desc: "Round High Bay, (1) 50W LED HID Lamp (E26)",
          watts: 50,
        },
        {
          desc: "Round High Bay, (1) 75W LED HID Lamp (E26)",
          watts: 75,
        },
        {
          desc: "Round High Bay, (1) 100W LED HID Lamp (EX39)",
          watts: 100,
        },
        {
          desc: "Round High Bay, (1) 120W LED HID Lamp (EX39)",
          watts: 120,
        },
        {
          desc: "Round High Bay, (1) 150W LED HID Lamp (EX39)",
          watts: 150,
        },
      ],
    },
    Linear: {
      LED: [
        {
          desc: "Linear 1\u0027 X 2\u0027 LED High Bay 60W",
          watts: 60,
        },
        {
          desc: "Linear 1\u0027 X 2\u0027 LED High Bay 75W",
          watts: 75,
        },
        {
          desc: "Linear 1\u0027 X 2\u0027 LED High Bay 90W",
          watts: 90,
        },
        {
          desc: "Linear 1\u0027 X 2\u0027 LED High Bay 100W",
          watts: 100,
        },
        {
          desc: "Linear 1\u0027 X 2\u0027 LED High Bay 120W",
          watts: 120,
        },
        {
          desc: "Linear 1\u0027 X 2\u0027 LED High Bay 140W",
          watts: 140,
        },
        {
          desc: "Linear 1\u0027 X 2\u0027 LED High Bay 150W",
          watts: 150,
        },
        {
          desc: "Linear 1\u0027 X 2\u0027 LED High Bay 160W",
          watts: 160,
        },
        {
          desc: "Linear 1\u0027 X 2\u0027 LED High Bay 175W",
          watts: 175,
        },
        {
          desc: "Linear 1\u0027 X 3\u0027 LED High Bay 200W",
          watts: 200,
        },
        {
          desc: "Linear 1\u0027 X 3\u0027 LED High Bay 250W",
          watts: 250,
        },
        {
          desc: "Linear 1\u0027 X 3\u0027 LED High Bay 300W",
          watts: 300,
        },
        {
          desc: "Linear 1\u0027 X 4\u0027 LED High Bay 400W",
          watts: 400,
        },
      ],
      T8: [
        {
          desc: 'Linear High Bay, (3) 32W 48" T8 - NLO',
          watts: 82,
        },
        {
          desc: 'Linear High Bay, (4) 32W 48" T8 - NLO',
          watts: 108,
        },
        {
          desc: 'Linear High Bay, (6) 32W 48" T8 - NLO',
          watts: 164,
        },
        {
          desc: 'Linear High Bay, (8) 32W 48" T8 - NLO',
          watts: 216,
        },
        {
          desc: 'Linear High Bay, (3) 32W 48" T8 - HLO',
          watts: 82,
        },
        {
          desc: 'Linear High Bay, (4) 32W 48" T8 - HLO',
          watts: 156,
        },
        {
          desc: 'Linear High Bay, (6) 32W 48" T8 - HLO',
          watts: 224,
        },
        {
          desc: 'Linear High Bay, (8) 32W 48" T8 - HLO',
          watts: 292,
        },
      ],
      "LED T8": [
        {
          desc: 'Linear High Bay, (3) 15W 48" LED T8 (UL-B) - NLO',
          watts: 45,
        },
        {
          desc: 'Linear High Bay, (4) 15W 48" LED T8 (UL-B) - NLO',
          watts: 60,
        },
        {
          desc: 'Linear High Bay, (6) 15W 48" LED T8 (UL-B) - NLO',
          watts: 90,
        },
        {
          desc: 'Linear High Bay, (8) 15W 48" LED T8 (UL-B) - NLO',
          watts: 120,
        },
      ],
      T5HO: [
        {
          desc: 'Linear High Bay, (2) 54W 48" T5HO',
          watts: 117,
        },
        {
          desc: 'Linear High Bay, (3) 54W 48" T5HO',
          watts: 177,
        },
        {
          desc: 'Linear High Bay, (4) 54W 48" T5HO',
          watts: 234,
        },
        {
          desc: 'Linear High Bay, (6) 54W 48" T5HO',
          watts: 354,
        },
        {
          desc: 'Linear High Bay, (8) 54W 48" T5HO',
          watts: 468,
        },
      ],
      "LED T5HO (B)": [
        {
          desc: 'Linear High Bay, (2) 25W 48" LED T5HO (UL-B)',
          watts: 50,
        },
        {
          desc: 'Linear High Bay, (3) 25W 48" LED T5HO (UL-B)',
          watts: 75,
        },
        {
          desc: 'Linear High Bay, (4) 25W 48" LED T5HO (UL-B)',
          watts: 100,
        },
        {
          desc: 'Linear High Bay, (6) 25W 48" LED T5HO (UL-B)',
          watts: 150,
        },
        {
          desc: 'Linear High Bay, (8) 25W 48" LED T5HO (UL-B)',
          watts: 200,
        },
      ],
    },
  },
  "Direct/Indirect": {
    Linear: {
      T8: [
        {
          desc: 'D/I 4\u0027 Pendant, (1) 32W 48" T8',
          watts: 28,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (3) 32W 48" T8',
          watts: 82,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (4) 32W 48" T8',
          watts: 108,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (4) 32W 48" T8',
          watts: 108,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (6) 32W 48" T8',
          watts: 164,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (8) 32W 48" T8',
          watts: 216,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'D/I 4\u0027 Pendant, (1) 13W 48" LED T8 (UL-A)',
          watts: 15,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (3) 13W 48" LED T8 (UL-A)',
          watts: 45,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (4) 13W 48" LED T8 (UL-A)',
          watts: 60,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (4) 13W 48" LED T8 (UL-A)',
          watts: 60,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (6) 13W 48" LED T8 (UL-A)',
          watts: 90,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (8) 13W 48" LED T8 (UL-A)',
          watts: 120,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'D/I 4\u0027 Pendant, (1) 12W 48" LED T8 (UL-B)',
          watts: 12,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (2) 12W 48" LED T8 (UL-B)',
          watts: 24,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (3) 12W 48" LED T8 (UL-B)',
          watts: 36,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (4) 12W 48" LED T8 (UL-B)',
          watts: 48,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (2) 12W 48" LED T8 (UL-B)',
          watts: 24,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (4) 12W 48" LED T8 (UL-B)',
          watts: 48,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (6) 12W 48" LED T8 (UL-B)',
          watts: 72,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (8) 12W 48" LED T8 (UL-B)',
          watts: 96,
        },
      ],
      T5HE: [
        {
          desc: 'D/I 4\u0027 Pendant, (1) 28W 48" T5HE',
          watts: 32,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (2) 28W 48" T5HE',
          watts: 63,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (3) 28W 48" T5HE',
          watts: 95,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (4) 28W 48" T5HE',
          watts: 126,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (2) 28W 48" T5HE',
          watts: 63,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (4) 28W 48" T5HE',
          watts: 126,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (6) 28W 48" T5HE',
          watts: 190,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (8) 28W 48" T5HE',
          watts: 252,
        },
      ],
      T5HO: [
        {
          desc: 'D/I 4\u0027 Pendant, (1) 54W 48" T5HO',
          watts: 59,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (2) 54W 48" T5HO',
          watts: 117,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (3) 54W 48" T5HO',
          watts: 177,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (4) 54W 48" T5HO',
          watts: 234,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (2) 54W 48" T5HO',
          watts: 117,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (4) 54W 48" T5HO',
          watts: 234,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (6) 54W 48" T5HO',
          watts: 354,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (8) 54W 48" T5HO',
          watts: 468,
        },
      ],
      "LED T5 (B)": [
        {
          desc: 'D/I 4\u0027 Pendant, (1) 13W 48" LED T5HE (UL-B)',
          watts: 13,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (2) 13W 48" LED T5HE (UL-B)',
          watts: 26,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (3) 13W 48" LED T5HE (UL-B)',
          watts: 39,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (4) 13W 48" LED T5HE (UL-B)',
          watts: 52,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (2) 13W 48" LED T5HE (UL-B)',
          watts: 26,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (4) 13W 48" LED T5HE (UL-B)',
          watts: 52,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (6) 13W 48" LED T5HE (UL-B)',
          watts: 78,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (8) 13W 48" LED T5HE (UL-B)',
          watts: 108,
        },
      ],
      "LED T5HO (B)": [
        {
          desc: 'D/I 4\u0027 Pendant, (1) 25W 48" LED T5HO (UL-B)',
          watts: 25,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (2) 25W 48" LED T5HO (UL-B)',
          watts: 50,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (3) 25W 48" LED T5HO (UL-B)',
          watts: 75,
        },
        {
          desc: 'D/I 4\u0027 Pendant, (4) 25W 48" LED T5HO (UL-B)',
          watts: 100,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (2) 25W 48" LED T5HO (UL-B)',
          watts: 50,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (4) 25W 48" LED T5HO (UL-B)',
          watts: 100,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (6) 25W 48" LED T5HO (UL-B)',
          watts: 150,
        },
        {
          desc: 'D/I 8\u0027 Pendant, (8) 25W 48" LED T5HO (UL-B)',
          watts: 200,
        },
      ],
    },
  },
  "Tamper Proof": {
    Linear: {
      T8: [
        {
          desc: 'Linear 2\u0027 Tamper Proof, (1) 17W 24" T8',
          watts: 20,
        },
        {
          desc: 'Linear 2\u0027 Tamper Proof, (2) 17W 24" T8',
          watts: 33,
        },
        {
          desc: 'Linear 2\u0027 Tamper Proof, (3) 17W 24" T8',
          watts: 47,
        },
        {
          desc: 'Linear 4\u0027 Tamper Proof, (1) 32W 48" T8',
          watts: 28,
        },
        {
          desc: 'Linear 4\u0027 Tamper Proof, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'Linear 4\u0027 Tamper Proof, (3) 32W 48" T8',
          watts: 82,
        },
        {
          desc: 'Linear 4\u0027 Tamper Proof, (4) 32W 48" T8',
          watts: 108,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Linear 2\u0027 Tamper Proof, (1) 9W 24" LED T8 (UL-A)',
          watts: 10,
        },
        {
          desc: 'Linear 2\u0027 Tamper Proof, (2) 9W 24" LED T8 (UL-A)',
          watts: 20,
        },
        {
          desc: 'Linear 2\u0027 Tamper Proof, (3) 9W 24" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Linear 4\u0027 Tamper Proof, (1) 13W 48" LED T8 (UL-A)',
          watts: 15,
        },
        {
          desc: 'Linear 4\u0027 Tamper Proof, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Linear 4\u0027 Tamper Proof, (3) 13W 48" LED T8 (UL-A)',
          watts: 45,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Linear 2\u0027 Tamper Proof, (1) 9W 24" LED T8 (UL-B)',
          watts: 9,
        },
        {
          desc: 'Linear 2\u0027 Tamper Proof, (2) 9W 24" LED T8 (UL-B)',
          watts: 18,
        },
        {
          desc: 'Linear 2\u0027 Tamper Proof, (3) 9W 24" LED T8 (UL-B)',
          watts: 27,
        },
        {
          desc: 'Linear 4\u0027 Tamper Proof, (1) 12W 48" LED T8 (UL-B)',
          watts: 12,
        },
        {
          desc: 'Linear 4\u0027 Tamper Proof, (2) 12W 48" LED T8 (UL-B)',
          watts: 24,
        },
        {
          desc: 'Linear 4\u0027 Tamper Proof, (3) 12W 48" LED T8 (UL-B)',
          watts: 36,
        },
      ],
    },
  },
  "Vapor Tight": {
    Linear: {
      T8: [
        {
          desc: 'Linear 2\u0027 VT, (1) 17W 24" T8',
          watts: 20,
        },
        {
          desc: 'Linear 2\u0027 VT, (2) 17W 24" T8',
          watts: 33,
        },
        {
          desc: 'Linear 2\u0027 VT, (3) 17W 24" T8',
          watts: 47,
        },
        {
          desc: 'Linear 2\u0027 VT, (4) 17W 24" T8',
          watts: 61,
        },
        {
          desc: 'Linear 4\u0027 VT, (1) 32W 48" T8',
          watts: 28,
        },
        {
          desc: 'Linear 4\u0027 VT, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'Linear 4\u0027 VT, (3) 32W 48" T8',
          watts: 82,
        },
        {
          desc: 'Linear 4\u0027 VT, (4) 32W 48" T8',
          watts: 108,
        },
        {
          desc: 'Linear 8\u0027 VT, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'Linear 8\u0027 VT, (4) 32W 48" T8',
          watts: 108,
        },
        {
          desc: 'Linear 8\u0027 VT, (6) 32W 48" T8',
          watts: 164,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Linear 2\u0027 VT, (1) 9W 24" LED T8 (UL-A)',
          watts: 10,
        },
        {
          desc: 'Linear 2\u0027 VT, (2) 9W 24" LED T8 (UL-A)',
          watts: 20,
        },
        {
          desc: 'Linear 2\u0027 VT, (3) 9W 24" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Linear 2\u0027 VT, (4) 9W 24" LED T8 (UL-A)',
          watts: 40,
        },
        {
          desc: 'Linear 4\u0027 VT, (1) 13W 48" LED T8 (UL-A)',
          watts: 15,
        },
        {
          desc: 'Linear 4\u0027 VT, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Linear 4\u0027 VT, (3) 13W 48" LED T8 (UL-A)',
          watts: 45,
        },
        {
          desc: 'Linear 4\u0027 VT, (4) 13W 48" LED T8 (UL-A)',
          watts: 60,
        },
        {
          desc: 'Linear 8\u0027 VT, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
        {
          desc: 'Linear 8\u0027 VT, (4) 13W 48" LED T8 (UL-A)',
          watts: 60,
        },
        {
          desc: 'Linear 8\u0027 VT, (6) 13W 48" LED T8 (UL-A)',
          watts: 90,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Linear 2\u0027 VT, (1) 9W 24" LED T8 (UL-B)',
          watts: 9,
        },
        {
          desc: 'Linear 2\u0027 VT, (2) 9W 24" LED T8 (UL-B)',
          watts: 18,
        },
        {
          desc: 'Linear 2\u0027 VT, (3) 9W 24" LED T8 (UL-B)',
          watts: 27,
        },
        {
          desc: 'Linear 2\u0027 VT, (4) 9W 24" LED T8 (UL-B)',
          watts: 36,
        },
        {
          desc: 'Linear 4\u0027 VT, (1) 12W 48" LED T8 (UL-B)',
          watts: 12,
        },
        {
          desc: 'Linear 4\u0027 VT, (2) 12W 48" LED T8 (UL-B)',
          watts: 24,
        },
        {
          desc: 'Linear 4\u0027 VT, (3) 12W 48" LED T8 (UL-B)',
          watts: 36,
        },
        {
          desc: 'Linear 4\u0027 VT, (4) 12W 48" LED T8 (UL-B)',
          watts: 48,
        },
        {
          desc: 'Linear 8\u0027 VT, (2) 12W 48" LED T8 (UL-B)',
          watts: 24,
        },
        {
          desc: 'Linear 8\u0027 VT, (4) 12W 48" LED T8 (UL-B)',
          watts: 48,
        },
        {
          desc: 'Linear 8\u0027 VT, (6) 12W 48" LED T8 (UL-B)',
          watts: 72,
        },
      ],
      T12: [
        {
          desc: 'Linear 2\u0027 VT, (1) 20W 24" T12',
          watts: 28,
        },
        {
          desc: 'Linear 2\u0027 VT, (2) 20W 24" T12',
          watts: 56,
        },
        {
          desc: 'Linear 2\u0027 VT, (3) 20W 24" T12',
          watts: 84,
        },
        {
          desc: 'Linear 2\u0027 VT, (4) 20W 24" T12',
          watts: 112,
        },
        {
          desc: 'Linear 4\u0027 VT, (1) 34W 48" T12',
          watts: 43,
        },
        {
          desc: 'Linear 4\u0027 VT, (2) 34W 48" T12',
          watts: 72,
        },
        {
          desc: 'Linear 4\u0027 VT, (3) 34W 48" T12',
          watts: 115,
        },
        {
          desc: 'Linear 4\u0027 VT, (4) 34W 48" T12',
          watts: 144,
        },
      ],
      LED: [
        {
          desc: "LED 4\u0027 Vapor Tight - 20W",
          watts: 20,
        },
        {
          desc: "LED 4\u0027 Vapor Tight - 30W",
          watts: 30,
        },
        {
          desc: "LED 4\u0027 Vapor Tight - 40W",
          watts: 40,
        },
        {
          desc: "LED 8\u0027 Vapor TIght - 40W",
          watts: 40,
        },
        {
          desc: "LED 8\u0027 Vapor Tight - 50W",
          watts: 50,
        },
        {
          desc: "LED 8\u0027 Vapor Tight - 60W",
          watts: 60,
        },
      ],
      T5HE: [
        {
          desc: 'Linear 8\u0027 VT, (2) 28W 48" T5HE',
          watts: 63,
        },
        {
          desc: 'Linear 8\u0027 VT, (4) 28W 48" T5HE',
          watts: 126,
        },
        {
          desc: 'Linear 8\u0027 VT, (6) 28W 48" T5HE',
          watts: 190,
        },
      ],
      T5HO: [
        {
          desc: 'Linear 8\u0027 VT, (2) 54W 48" T5HO',
          watts: 117,
        },
        {
          desc: 'Linear 8\u0027 VT, (4) 54W 48" T5HO',
          watts: 234,
        },
        {
          desc: 'Linear 8\u0027 VT, (6) 54W 48" T5HO',
          watts: 354,
        },
      ],
    },
  },
  Slot: {
    Recessed: {
      T8: [
        {
          desc: 'Recessed 4\u0027 Slot, (1) 32W 48" T8',
          watts: 28,
        },
        {
          desc: 'Recessed 4\u0027 Slot, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'Recessed 8\u0027 Slot, (2) 32W 48" T8',
          watts: 56,
        },
        {
          desc: 'Recessed 8\u0027 Slot, (4) 32W 48" T8',
          watts: 108,
        },
      ],
      T5HE: [
        {
          desc: "Recessed 4\u0027 Slot, (1) 28W T5HE",
          watts: 32,
        },
        {
          desc: "Recessed 4\u0027 Slot, (2) 28W T5HE",
          watts: 63,
        },
        {
          desc: "Recessed 8\u0027 Slot, (2) 28W T5HE",
          watts: 63,
        },
        {
          desc: "Recessed 8\u0027 Slot, (4) 28W T5HE",
          watts: 126,
        },
      ],
      T5HO: [
        {
          desc: "Recessed 4\u0027 Slot, (1) 54W T5HO",
          watts: 59,
        },
        {
          desc: "Recessed 4\u0027 Slot, (2) 54W T5HO",
          watts: 117,
        },
        {
          desc: "Recessed 8\u0027 Slot, (2) 54W T5HO",
          watts: 117,
        },
        {
          desc: "Recessed 8\u0027 Slot, (4) 54W T5HO",
          watts: 234,
        },
      ],
      LED: [
        {
          desc: "LED 4\u0027 20W Recessed Slot",
          watts: 20,
        },
        {
          desc: "LED 4\u0027 30W Recessed Slot",
          watts: 30,
        },
        {
          desc: "LED 8\u0027 40W Recessed Slot",
          watts: 40,
        },
        {
          desc: "LED 8\u0027 60W Recessed Slot",
          watts: 60,
        },
      ],
    },
  },
  Cove: {
    Valance: {
      T8: [
        {
          desc: 'Restroom 2\u0027 Valance, (1) 17W 24" T8',
          watts: 20,
        },
        {
          desc: 'Restroom 3\u0027 Valance, (1) 25W 36" T8',
          watts: 26,
        },
        {
          desc: 'Restroom 4\u0027 Valance, (1) 32W 48" T8',
          watts: 28,
        },
        {
          desc: 'Restroom 2\u0027 Valance, (2) 17W 24" T8',
          watts: 33,
        },
        {
          desc: 'Restroom 3\u0027 Valance, (2) 25W 36" T8',
          watts: 46,
        },
        {
          desc: 'Restroom 4\u0027 Valance, (2) 32W 48" T8',
          watts: 56,
        },
      ],
      T5HE: [
        {
          desc: 'Restroom 2\u0027 Valance, (1) 14W 24" T5HE',
          watts: 18,
        },
        {
          desc: 'Restroom 3\u0027 Valance, (1) 21W 36" T5HE',
          watts: 27,
        },
        {
          desc: 'Restroom 4\u0027 Valance, (1) 28W 48" T5HE',
          watts: 32,
        },
        {
          desc: 'Restroom 2\u0027 Valance, (2) 14W 24" T5HE',
          watts: 35,
        },
        {
          desc: 'Restroom 3\u0027 Valance, (2) 21W 36" T5HE',
          watts: 52,
        },
        {
          desc: 'Restroom 4\u0027 Valance, (2) 28W 48" T5HE',
          watts: 63,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Restroom 2\u0027 Valance, (1) 7W 24" LED T8 (UL-B)',
          watts: 7,
        },
        {
          desc: 'Restroom 3\u0027 Valance, (1) 12W 36" LED T8 (UL-B)',
          watts: 12,
        },
        {
          desc: 'Restroom 4\u0027 Valance, (1) 12W 48" LED T8 (UL-B)',
          watts: 12,
        },
        {
          desc: 'Restroom 2\u0027 Valance, (2) 7W 24" LED T8 (UL-B)',
          watts: 14,
        },
        {
          desc: 'Restroom 3\u0027 Valance, (2) 12W 36" LED T8 (UL-B)',
          watts: 24,
        },
        {
          desc: 'Restroom 4\u0027 Valance, (2) 12W 48" LED T8 (UL-B)',
          watts: 24,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Restroom 2\u0027 Valance, (1) 9W 24" LED T8 (UL-A)',
          watts: 10,
        },
        {
          desc: 'Restroom 3\u0027 Valance, (1) 12W 36" LED T8 (UL-A)',
          watts: 13,
        },
        {
          desc: 'Restroom 4\u0027 Valance, (1) 13W 48" LED T8 (UL-A)',
          watts: 15,
        },
        {
          desc: 'Restroom 2\u0027 Valance, (2) 9W 24" LED T8 (UL-A)',
          watts: 20,
        },
        {
          desc: 'Restroom 3\u0027 Valance, (2) 12W 36" LED T8 (UL-A)',
          watts: 26,
        },
        {
          desc: 'Restroom 4\u0027 Valance, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
      ],
    },
    Lobby: {
      T8: [
        {
          desc: 'Lobby Cove 2\u0027 Valance, (1) 17W 24" T8',
          watts: 20,
        },
        {
          desc: 'Lobby Cove 3\u0027 Valance, (1) 25W 36" T8',
          watts: 26,
        },
        {
          desc: 'Lobby Cove 4\u0027 Valance, (1) 32W 48" T8',
          watts: 28,
        },
        {
          desc: 'Lobby Cove 2\u0027 Valance, (2) 17W 24" T8',
          watts: 33,
        },
        {
          desc: 'Lobby Cove 3\u0027 Valance, (2) 25W 36" T8',
          watts: 46,
        },
        {
          desc: 'Lobby Cove 4\u0027 Valance, (2) 32W 48" T8',
          watts: 56,
        },
      ],
      T5HE: [
        {
          desc: 'Lobby Cove 2\u0027 Valance, (1) 14W 24" T5HE',
          watts: 18,
        },
        {
          desc: 'Lobby Cove 3\u0027 Valance, (1) 21W 36" T5HE',
          watts: 27,
        },
        {
          desc: 'Lobby Cove 4\u0027 Valance, (1) 28W 48" T5HE',
          watts: 32,
        },
        {
          desc: 'Lobby Cove 2\u0027 Valance, (2) 14W 24" T5HE',
          watts: 35,
        },
        {
          desc: 'Lobby Cove 3\u0027 Valance, (2) 21W 36" T5HE',
          watts: 52,
        },
        {
          desc: 'Lobby Cove 4\u0027 Valance, (2) 28W 48" T5HE',
          watts: 63,
        },
      ],
      "LED T8 (B)": [
        {
          desc: 'Lobby Cove 2\u0027 Valance, (1) 7W 24" LED T8 (UL-B)',
          watts: 7,
        },
        {
          desc: 'Lobby Cove 3\u0027 Valance, (1) 12W 36" LED T8 (UL-B)',
          watts: 12,
        },
        {
          desc: 'Lobby Cove 4\u0027 Valance, (1) 12W 48" LED T8 (UL-B)',
          watts: 12,
        },
        {
          desc: 'Lobby Cove 2\u0027 Valance, (2) 7W 24" LED T8 (UL-B)',
          watts: 14,
        },
        {
          desc: 'Lobby Cove 3\u0027 Valance, (2) 12W 36" LED T8 (UL-B)',
          watts: 24,
        },
        {
          desc: 'Lobby Cove 4\u0027 Valance, (2) 12W 48" LED T8 (UL-B)',
          watts: 24,
        },
      ],
      "LED T8 (A)": [
        {
          desc: 'Lobby Cove 2\u0027 Valance, (1) 9W 24" LED T8 (UL-A)',
          watts: 10,
        },
        {
          desc: 'Lobby Cove 3\u0027 Valance, (1) 12W 36" LED T8 (UL-A)',
          watts: 13,
        },
        {
          desc: 'Lobby Cove 4\u0027 Valance, (1) 13W 48" LED T8 (UL-A)',
          watts: 15,
        },
        {
          desc: 'Lobby Cove 2\u0027 Valance, (2) 9W 24" LED T8 (UL-A)',
          watts: 20,
        },
        {
          desc: 'Lobby Cove 3\u0027 Valance, (2) 12W 36" LED T8 (UL-A)',
          watts: 26,
        },
        {
          desc: 'Lobby Cove 4\u0027 Valance, (2) 13W 48" LED T8 (UL-A)',
          watts: 30,
        },
      ],
    },
  },
  Exterior: {
    Garage: {
      Canopy: {
        "CFL PIN": [
          {
            desc: "Garage Canopy, (1) 13W CFL 2P Lamp - Horiz. Throw",
            watts: 17,
          },
          {
            desc: "Garage Canopy, (2) 13W CFL 2P Lamps - Horiz. Throw",
            watts: 31,
          },
          {
            desc: "Garage Canopy, (1) 26W CFL 4P Lamp - Horiz. Throw",
            watts: 33,
          },
          {
            desc: "Garage Canopy, (2) 26W CFL 4P Lamps - Horiz. Throw",
            watts: 54,
          },
          {
            desc: "Garage Canopy, (1) 32W CFL 4P Lamp - Horiz. Throw",
            watts: 34,
          },
          {
            desc: "Garage Canopy, (2) 32W CFL 4P Lamps - Horiz. Throw",
            watts: 34,
          },
          {
            desc: "Garage Canopy, (1) 42W CFL 4P Lamp - Horiz. Throw",
            watts: 48,
          },
          {
            desc: "Garage Canopy, (2) 42W CFL 4P Lamps - Horiz. Throw",
            watts: 100,
          },
        ],
        "LED HID": [
          {
            desc: "Garage Canopy, (1) 15W LED HID Lamp - Horiz. Throw",
            watts: 15,
          },
          {
            desc: "Garage Canopy, (1) 25W LED HID Lamp - Horiz. Throw",
            watts: 25,
          },
          {
            desc: "Garage Canopy, (1) 40W LED HID Lamp - Horiz. Throw",
            watts: 40,
          },
          {
            desc: "Garage Canopy, (1) 50W LED HID Lamp - Horiz. Throw",
            watts: 50,
          },
          {
            desc: "Garage Canopy, (1) 75W LED HID Lamp - Horiz. Throw",
            watts: 75,
          },
          {
            desc: "Garage Canopy, (1) 100W LED HID Lamp - Horiz. Throw",
            watts: 100,
          },
        ],
        "CFL SP": [
          {
            desc: "Garage Canopy, (1) 18W CFL Spiral - Horiz. Throw",
            watts: 18,
          },
          {
            desc: "Garage Canopy, (1) 26W CFL Spiral - Horiz. Throw",
            watts: 26,
          },
          {
            desc: "Garage Canopy, (1) 32W CFL Spiral - Horiz. Throw",
            watts: 32,
          },
        ],
        "Incan.": [
          {
            desc: "Garage Canopy, (1) 40W A19 Lamp - Horiz. Throw",
            watts: 40,
          },
          {
            desc: "Garage Canopy, (1) 60W A19 Lamp - Horiz. Throw",
            watts: 60,
          },
          {
            desc: "Garage Canopy, (1) 100W A19 Lamp - Horiz. Throw",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Garage Canopy, (1) 50W MH Lamp - Horiz. Throw",
            watts: 72,
          },
          {
            desc: "Garage Canopy, (1) 70W MH Lamp - Horiz. Throw",
            watts: 95,
          },
          {
            desc: "Garage Canopy, (1) 100W MH Lamp - Horiz. Throw",
            watts: 128,
          },
          {
            desc: "Garage Canopy, (1) 150W MH Lamp - Horiz. Throw",
            watts: 190,
          },
          {
            desc: "Garage Canopy, (1) 175W MH Lamp - Horiz. Throw",
            watts: 215,
          },
          {
            desc: "Garage Canopy, (1) 200W MH Lamp - Horiz. Throw",
            watts: 232,
          },
          {
            desc: "Garage Canopy, (1) 250W MH Lamp - Horiz. Throw",
            watts: 295,
          },
        ],
        HPS: [
          {
            desc: "Garage Canopy, (1) 50W HPS Lamp - Horiz. Throw",
            watts: 66,
          },
          {
            desc: "Garage Canopy, (1) 70W HPS Lamp - Horiz. Throw",
            watts: 95,
          },
          {
            desc: "Garage Canopy, (1) 100W HPS Lamp - Horiz. Throw",
            watts: 138,
          },
          {
            desc: "Garage Canopy, (1) 150W HPS Lamp - Horiz. Throw",
            watts: 188,
          },
          {
            desc: "Garage Canopy, (1) 200W HPS Lamp - Horiz. Throw",
            watts: 250,
          },
          {
            desc: "Garage Canopy, (1) 250W HPS Lamp - Horiz. Throw",
            watts: 295,
          },
        ],
        LED: [
          {
            desc: "LED 100W Garage Canopy - Horiz. Throw",
            watts: 100,
          },
          {
            desc: "LED 125W Garage Canopy - Horiz. Throw",
            watts: 125,
          },
          {
            desc: "LED 150W Garage Canopy - Horiz. Throw",
            watts: 150,
          },
          {
            desc: "LED 30W Garage Canopy - Horiz. Throw",
            watts: 30,
          },
          {
            desc: "LED 50W Garage Canopy - Horiz. Throw",
            watts: 50,
          },
          {
            desc: "LED 75W Garage Canopy - Horiz. Throw",
            watts: 75,
          },
        ],
      },
    },
    Sconce: {
      Cylinder: {
        LED: [
          {
            desc: "LED 20W Cylinder Sconce - Downlight",
            watts: 20,
          },
          {
            desc: "LED 20W Cylinder Sconce - Up/Down",
            watts: 20,
          },
          {
            desc: "LED 30W Cylinder Sconce - Downlight",
            watts: 30,
          },
          {
            desc: "LED 30W Cylinder Sconce - Up/Down",
            watts: 30,
          },
          {
            desc: "LED 40W Cylinder Sconce - Downlight",
            watts: 40,
          },
          {
            desc: "LED 40W Cylinder Sconce - Up/Down",
            watts: 40,
          },
        ],
      },
      Downlight: {
        "LED SI": [
          {
            desc: "Wall Sconce, (1) 8W LED A19 - Up/Down",
            watts: 8,
          },
          {
            desc: "Wall Sconce, (1) 10W LED A19 - Up/Down",
            watts: 10,
          },
          {
            desc: "Wall Sconce, (1) 15W LED A19 - Up/Down",
            watts: 15,
          },
        ],
        "CFL SP": [
          {
            desc: "Wall Sconce, (1) 13W CFL Spiral - Downlight",
            watts: 13,
          },
          {
            desc: "Wall Sconce, (1) 23W CFL Spiral - Downlight",
            watts: 23,
          },
          {
            desc: "Wall Sconce, (1) 32W CFL Spiral - Downlight",
            watts: 34,
          },
          {
            desc: "Wall Sconce, (1) 42W CFL Spiral - Downlight",
            watts: 42,
          },
        ],
        "Metal Halide": [
          {
            desc: "Wall Sconce, (1) 50W MH Lamp - Downlight",
            watts: 72,
          },
          {
            desc: "Wall Sconce, (1) 70W MH Lamp - Downlight",
            watts: 95,
          },
          {
            desc: "Wall Sconce, (1) 100W MH Lamp - Downlight",
            watts: 128,
          },
          {
            desc: "Wall Sconce, (1) 150W MH Lamp - Downlight",
            watts: 190,
          },
          {
            desc: "Wall Sconce, (1) 175W MH Lamp - Downlight",
            watts: 215,
          },
        ],
        HPS: [
          {
            desc: "Wall Sconce, (1) 50W HPS Lamp - Downlight",
            watts: 66,
          },
          {
            desc: "Wall Sconce, (1) 70W HPS Lamp - Downlight",
            watts: 95,
          },
          {
            desc: "Wall Sconce, (1) 100W HPS Lamp - Downlight",
            watts: 138,
          },
          {
            desc: "Wall Sconce, (1) 150W HPS Lamp - Downlight",
            watts: 188,
          },
        ],
        "Incan.": [
          {
            desc: "Wall Sconce, (1) 60W A19 Lamp - Downlight",
            watts: 60,
          },
          {
            desc: "Wall Sconce, (1) 75W A19 Lamp - Downlight",
            watts: 75,
          },
          {
            desc: "Wall Sconce, (1) 100W A19 Lamp - Downlight",
            watts: 100,
          },
        ],
        LED: [
          {
            desc: "LED 20W Sconce - Downlight",
            watts: 20,
          },
          {
            desc: "LED 30W Sconce - Downlight",
            watts: 30,
          },
          {
            desc: "LED 40W Sconce - Downlight",
            watts: 40,
          },
          {
            desc: "LED 50W Sconce - Downlight",
            watts: 50,
          },
        ],
      },
      "Up/Down": {
        LED: [
          {
            desc: "LED 20W Sconce - Up/Down",
            watts: 20,
          },
          {
            desc: "LED 30W Sconce - Up/Down",
            watts: 30,
          },
          {
            desc: "LED 40W Sconce - Up/Down",
            watts: 40,
          },
          {
            desc: "LED 50W Sconce - Up/Down",
            watts: 50,
          },
        ],
        "LED SI": [
          {
            desc: "Wall Sconce, (2) 8W LED A19 - Up/Down",
            watts: 16,
          },
          {
            desc: "Wall Sconce, (2) 10W LED A19 - Up/Down",
            watts: 20,
          },
          {
            desc: "Wall Sconce, (2) 15W LED A19 - Up/Down",
            watts: 30,
          },
        ],
        "CFL SP": [
          {
            desc: "Wall Sconce, (2) 13W CFL Spiral - Up/Down",
            watts: 26,
          },
          {
            desc: "Wall Sconce, (2) 23W CFL Spiral - Up/Down",
            watts: 46,
          },
          {
            desc: "Wall Sconce, (2) 32W CFL Spiral - Up/Down",
            watts: 64,
          },
          {
            desc: "Wall Sconce, (2) 42W CFL Spiral - Up/Down",
            watts: 84,
          },
        ],
        "Metal Halide": [
          {
            desc: "Wall Sconce, (2) 50W MH Lamps - Up/Down",
            watts: 72,
          },
          {
            desc: "Wall Sconce, (2) 70W MH Lamps - Up/Down",
            watts: 95,
          },
          {
            desc: "Wall Sconce, (2) 100W MH Lamps - Up/Down",
            watts: 128,
          },
          {
            desc: "Wall Sconce, (2) 150W MH Lamps - Up/Down",
            watts: 190,
          },
          {
            desc: "Wall Sconce, (2) 175W MH Lamps - Up/Down",
            watts: 215,
          },
        ],
        HPS: [
          {
            desc: "Wall Sconce, (2) 50W HPS Lamps - Up/Down",
            watts: 66,
          },
          {
            desc: "Wall Sconce, (2) 70W HPS Lamps - Up/Down",
            watts: 95,
          },
          {
            desc: "Wall Sconce, (2) 100W HPS Lamps - Up/Down",
            watts: 138,
          },
          {
            desc: "Wall Sconce, (2) 150W HPS Lamps - Up/Down",
            watts: 188,
          },
        ],
        "Incan.": [
          {
            desc: "Wall Sconce, (2) 60W A19 Lamps - Up/Down",
            watts: 60,
          },
          {
            desc: "Wall Sconce, (2) 75W A19 Lamps - Up/Down",
            watts: 75,
          },
          {
            desc: "Wall Sconce, (2) 100W A19 Lamps - Up/Down",
            watts: 100,
          },
        ],
      },
    },
    Flood: {
      Directional: {
        LED: [
          {
            desc: "LED 100W Flood - Directional",
            watts: 100,
          },
          {
            desc: "LED 125W Flood - Directional",
            watts: 125,
          },
          {
            desc: "LED 150W Flood - Directional",
            watts: 150,
          },
          {
            desc: "LED 200W Flood - Directional",
            watts: 200,
          },
          {
            desc: "LED 300W Flood - Directional",
            watts: 300,
          },
          {
            desc: "LED 30W Flood - Directional",
            watts: 30,
          },
          {
            desc: "LED 50W Flood - Directional",
            watts: 50,
          },
          {
            desc: "LED 75W Flood - Directional",
            watts: 75,
          },
        ],
        "LED HID": [
          {
            desc: "Flood, (1) 15W LED HID Lamp - Directional",
            watts: 15,
          },
          {
            desc: "Flood, (1) 25W LED HID Lamp - Directional",
            watts: 25,
          },
          {
            desc: "Flood, (1) 40W LED HID Lamp - Directional",
            watts: 40,
          },
          {
            desc: "Flood, (1) 50W LED HID Lamp - Directional",
            watts: 50,
          },
          {
            desc: "Flood, (1) 75W LED HID Lamp - Directional",
            watts: 75,
          },
          {
            desc: "Flood, (1) 100W LED HID Lamp - Directional",
            watts: 100,
          },
        ],
        "CFL PIN": [
          {
            desc: "Flood, (1) 26W CFL 2P Lamp - Directional",
            watts: 33,
          },
          {
            desc: "Flood, (1) 32W CFL 2P Lamp - Directional",
            watts: 34,
          },
          {
            desc: "Flood, (1) 42W CFL 2P Lamp - Directional",
            watts: 48,
          },
        ],
        "Metal Halide": [
          {
            desc: "Flood, (1) 50W MH Lamp - Directional",
            watts: 72,
          },
          {
            desc: "Flood, (1) 70W MH Lamp - Directional",
            watts: 95,
          },
          {
            desc: "Flood, (1) 100W MH Lamp - Directional",
            watts: 128,
          },
          {
            desc: "Flood, (1) 150W MH Lamp - Directional",
            watts: 190,
          },
          {
            desc: "Flood, (1) 175W MH Lamp - Directional",
            watts: 215,
          },
          {
            desc: "Flood, (1) 200W MH Lamp - Directional",
            watts: 232,
          },
          {
            desc: "Flood, (1) 250W MH Lamp - Directional",
            watts: 295,
          },
          {
            desc: "Flood, (1) 400W MH Lamp - Directional",
            watts: 458,
          },
          {
            desc: "Flood, (1) 1000W MH Lamp - Directional",
            watts: 1060,
          },
        ],
        HPS: [
          {
            desc: "Flood, (1) 50W HPS Lamp - Directional",
            watts: 66,
          },
          {
            desc: "Flood, (1) 70W HPS Lamp - Directional",
            watts: 95,
          },
          {
            desc: "Flood, (1) 100W HPS Lamp - Directional",
            watts: 138,
          },
          {
            desc: "Flood, (1) 150W HPS Lamp - Directional",
            watts: 188,
          },
          {
            desc: "Flood, (1) 200W HPS Lamp - Directional",
            watts: 250,
          },
          {
            desc: "Flood, (1) 250W HPS Lamp - Directional",
            watts: 295,
          },
          {
            desc: "Flood, (1) 400W HPS Lamp - Directional",
            watts: 465,
          },
          {
            desc: "Flood, (1) 1000W HPS Lamp - Directional",
            watts: 1100,
          },
        ],
      },
      General: {
        LED: [
          {
            desc: "LED 100W Flood - General",
            watts: 100,
          },
          {
            desc: "LED 125W Flood - General",
            watts: 125,
          },
          {
            desc: "LED 150W Flood - General",
            watts: 150,
          },
          {
            desc: "LED 200W Flood - General",
            watts: 200,
          },
          {
            desc: "LED 300W Flood - General",
            watts: 300,
          },
          {
            desc: "LED 30W Flood - General",
            watts: 30,
          },
          {
            desc: "LED 50W Flood - General",
            watts: 50,
          },
          {
            desc: "LED 75W Flood - General",
            watts: 75,
          },
        ],
        "LED HID": [
          {
            desc: "Flood, (1) 15W LED HID Lamp - General",
            watts: 15,
          },
          {
            desc: "Flood, (1) 25W LED HID Lamp - General",
            watts: 25,
          },
          {
            desc: "Flood, (1) 40W LED HID Lamp - General",
            watts: 40,
          },
          {
            desc: "Flood, (1) 50W LED HID Lamp - General",
            watts: 50,
          },
          {
            desc: "Flood, (1) 75W LED HID Lamp - General",
            watts: 75,
          },
          {
            desc: "Flood, (1) 100W LED HID Lamp - General",
            watts: 100,
          },
        ],
        "CFL PIN": [
          {
            desc: "Flood, (1) 26W CFL 2P Lamp - General",
            watts: 33,
          },
          {
            desc: "Flood, (1) 32W CFL 2P Lamp - General",
            watts: 34,
          },
          {
            desc: "Flood, (1) 42W CFL 2P Lamp - General",
            watts: 48,
          },
        ],
        "Metal Halide": [
          {
            desc: "Flood, (1) 50W MH Lamp - General",
            watts: 72,
          },
          {
            desc: "Flood, (1) 70W MH Lamp - General",
            watts: 95,
          },
          {
            desc: "Flood, (1) 100W MH Lamp - General",
            watts: 128,
          },
          {
            desc: "Flood, (1) 150W MH Lamp - General",
            watts: 190,
          },
          {
            desc: "Flood, (1) 175W MH Lamp - General",
            watts: 215,
          },
          {
            desc: "Flood, (1) 200W MH Lamp - General",
            watts: 232,
          },
          {
            desc: "Flood, (1) 250W MH Lamp - General",
            watts: 295,
          },
          {
            desc: "Flood, (1) 400W MH Lamp - General",
            watts: 458,
          },
          {
            desc: "Flood, (1) 1000W MH Lamp - General",
            watts: 1060,
          },
        ],
        HPS: [
          {
            desc: "Flood, (1) 50W HPS Lamp - General",
            watts: 66,
          },
          {
            desc: "Flood, (1) 70W HPS Lamp - General",
            watts: 95,
          },
          {
            desc: "Flood, (1) 100W HPS Lamp - General",
            watts: 138,
          },
          {
            desc: "Flood, (1) 150W HPS Lamp - General",
            watts: 188,
          },
          {
            desc: "Flood, (1) 200W HPS Lamp - General",
            watts: 250,
          },
          {
            desc: "Flood, (1) 250W HPS Lamp - General",
            watts: 295,
          },
          {
            desc: "Flood, (1) 400W HPS Lamp - General",
            watts: 465,
          },
          {
            desc: "Flood, (1) 1000W HPS Lamp - General",
            watts: 1100,
          },
        ],
      },
    },
    "Wall Pack": {
      "Full Cutoff": {
        LED: [
          {
            desc: "LED 100W Wall Pack - Full Cutoff",
            watts: 100,
          },
          {
            desc: "LED 125W Wall Pack - Full Cutoff",
            watts: 125,
          },
          {
            desc: "LED 150W Wall Pack - Full Cutoff",
            watts: 150,
          },
          {
            desc: "LED 25W Wall Pack - Full Cutoff",
            watts: 25,
          },
          {
            desc: "LED 30W Wall Pack - Full Cutoff",
            watts: 30,
          },
          {
            desc: "LED 40W Wall Pack - Full Cutoff",
            watts: 40,
          },
          {
            desc: "LED 50W Wall Pack - Full Cutoff",
            watts: 50,
          },
          {
            desc: "LED 75W Wall Pack - Full Cutoff",
            watts: 75,
          },
        ],
        "CFL PIN": [
          {
            desc: "Wall Pack, (1) 13W CFL 2P Lamp - Full Cutoff",
            watts: 17,
          },
          {
            desc: "Wall Pack, (2) 13W CFL 2P Lamp - Full Cutoff",
            watts: 31,
          },
          {
            desc: "Wall Pack, (1) 18W CFL 2P Lamp - Full Cutoff",
            watts: 26,
          },
          {
            desc: "Wall Pack, (2) 18W CFL 2P Lamp - Full Cutoff",
            watts: 45,
          },
          {
            desc: "Wall Pack, (1) 26W CFL 2P Lamp - Full Cutoff",
            watts: 33,
          },
          {
            desc: "Wall Pack, (2) 26W CFL 2P Lamp - Full Cutoff",
            watts: 54,
          },
          {
            desc: "Wall Pack, (1) 32W CFL 2P Lamp - Full Cutoff",
            watts: 34,
          },
          {
            desc: "Wall Pack, (2) 32W CFL 2P Lamp - Full Cutoff",
            watts: 62,
          },
          {
            desc: "Wall Pack, (1) 42W CFL 2P Lamp - Full Cutoff",
            watts: 48,
          },
          {
            desc: "Wall Pack, (2) 42W CFL 2P Lamp - Full Cutoff",
            watts: 100,
          },
        ],
        "LED HID": [
          {
            desc: "Wall Pack, (1) 20W LED HID Lamp - Full Cutoff",
            watts: 20,
          },
          {
            desc: "Wall Pack, (1) 30W LED HID Lamp - Full Cutoff",
            watts: 30,
          },
          {
            desc: "Wall Pack, (1) 40W LED HID Lamp - Full Cutoff",
            watts: 40,
          },
          {
            desc: "Wall Pack, (1) 50W LED HID Lamp - Full Cutoff",
            watts: 50,
          },
          {
            desc: "Wall Pack, (1) 75W LED HID Lamp - Full Cutoff",
            watts: 75,
          },
          {
            desc: "Wall Pack, (1) 100W LED HID Lamp - Full Cutoff",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Wall Pack, (1) 50W MH Lamp - Full Cutoff",
            watts: 72,
          },
          {
            desc: "Wall Pack, (1) 70W MH Lamp - Full Cutoff",
            watts: 95,
          },
          {
            desc: "Wall Pack, (1) 100W MH Lamp - Full Cutoff",
            watts: 128,
          },
          {
            desc: "Wall Pack, (1) 150W MH Lamp - Full Cutoff",
            watts: 190,
          },
          {
            desc: "Wall Pack, (1) 175W MH Lamp - Full Cutoff",
            watts: 215,
          },
          {
            desc: "Wall Pack, (1) 200W MH Lamp - Full Cutoff",
            watts: 232,
          },
          {
            desc: "Wall Pack, (1) 250W MH Lamp - Full Cutoff",
            watts: 295,
          },
          {
            desc: "Wall Pack, (1) 400W MH Lamp - Full Cutoff",
            watts: 458,
          },
          {
            desc: "Wall Pack, (1) 1000W MH Lamp - Full Cutoff",
            watts: 1060,
          },
        ],
        HPS: [
          {
            desc: "Wall Pack, (1) 50W HPS Lamp - Full Cutoff",
            watts: 66,
          },
          {
            desc: "Wall Pack, (1) 70W HPS Lamp - Full Cutoff",
            watts: 95,
          },
          {
            desc: "Wall Pack, (1) 100W HPS Lamp - Full Cutoff",
            watts: 138,
          },
          {
            desc: "Wall Pack, (1) 150W HPS Lamp - Full Cutoff",
            watts: 188,
          },
          {
            desc: "Wall Pack, (1) 200W HPS Lamp - Full Cutoff",
            watts: 250,
          },
          {
            desc: "Wall Pack, (1) 250W HPS Lamp - Full Cutoff",
            watts: 295,
          },
          {
            desc: "Wall Pack, (1) 400W HPS Lamp - Full Cutoff",
            watts: 465,
          },
          {
            desc: "Wall Pack, (1) 1000W HPS Lamp - Full Cutoff",
            watts: 1100,
          },
        ],
      },
      "Half-Moon": {
        LED: [
          {
            desc: "LED 100W Wall Pack - Half Moon",
            watts: 100,
          },
          {
            desc: "LED 125W Wall Pack - Half Moon",
            watts: 125,
          },
          {
            desc: "LED 150W Wall Pack - Half Moon",
            watts: 150,
          },
          {
            desc: "LED 25W Wall Pack - Half Moon",
            watts: 25,
          },
          {
            desc: "LED 30W Wall Pack - Half Moon",
            watts: 30,
          },
          {
            desc: "LED 40W Wall Pack - Half Moon",
            watts: 40,
          },
          {
            desc: "LED 50W Wall Pack - Half Moon",
            watts: 50,
          },
          {
            desc: "LED 75W Wall Pack - Half Moon",
            watts: 75,
          },
        ],
        "CFL PIN": [
          {
            desc: "Wall Pack, (1) 13W CFL 2P Lamp - Half Moon",
            watts: 17,
          },
          {
            desc: "Wall Pack, (2) 13W CFL 2P Lamp - Half Moon",
            watts: 31,
          },
          {
            desc: "Wall Pack, (1) 18W CFL 2P Lamp - Half Moon",
            watts: 26,
          },
          {
            desc: "Wall Pack, (2) 18W CFL 2P Lamp - Half Moon",
            watts: 45,
          },
          {
            desc: "Wall Pack, (1) 26W CFL 2P Lamp - Half Moon",
            watts: 33,
          },
          {
            desc: "Wall Pack, (2) 26W CFL 2P Lamp - Half Moon",
            watts: 54,
          },
          {
            desc: "Wall Pack, (1) 32W CFL 2P Lamp - Half Moon",
            watts: 34,
          },
          {
            desc: "Wall Pack, (2) 32W CFL 2P Lamp - Half Moon",
            watts: 62,
          },
          {
            desc: "Wall Pack, (1) 42W CFL 2P Lamp - Half Moon",
            watts: 48,
          },
          {
            desc: "Wall Pack, (2) 42W CFL 2P Lamp - Half Moon",
            watts: 100,
          },
        ],
        "LED HID": [
          {
            desc: "Wall Pack, (1) 20W LED HID Lamp - Half Moon",
            watts: 20,
          },
          {
            desc: "Wall Pack, (1) 30W LED HID Lamp - Half Moon",
            watts: 30,
          },
          {
            desc: "Wall Pack, (1) 40W LED HID Lamp - Half Moon",
            watts: 40,
          },
          {
            desc: "Wall Pack, (1) 50W LED HID Lamp - Half Moon",
            watts: 50,
          },
          {
            desc: "Wall Pack, (1) 75W LED HID Lamp - Half Moon",
            watts: 75,
          },
          {
            desc: "Wall Pack, (1) 100W LED HID Lamp - Half Moon",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Wall Pack, (1) 50W MH Lamp - Half Moon",
            watts: 72,
          },
          {
            desc: "Wall Pack, (1) 70W MH Lamp - Half Moon",
            watts: 95,
          },
          {
            desc: "Wall Pack, (1) 100W MH Lamp - Half Moon",
            watts: 128,
          },
          {
            desc: "Wall Pack, (1) 150W MH Lamp - Half Moon",
            watts: 190,
          },
          {
            desc: "Wall Pack, (1) 175W MH Lamp - Half Moon",
            watts: 215,
          },
          {
            desc: "Wall Pack, (1) 200W MH Lamp - Half Moon",
            watts: 232,
          },
          {
            desc: "Wall Pack, (1) 250W MH Lamp - Half Moon",
            watts: 295,
          },
          {
            desc: "Wall Pack, (1) 400W MH Lamp - Half Moon",
            watts: 458,
          },
          {
            desc: "Wall Pack, (1) 1000W MH Lamp - Half Moon",
            watts: 1060,
          },
        ],
        HPS: [
          {
            desc: "Wall Pack, (1) 50W HPS Lamp - Half Moon",
            watts: 66,
          },
          {
            desc: "Wall Pack, (1) 70W HPS Lamp - Half Moon",
            watts: 95,
          },
          {
            desc: "Wall Pack, (1) 100W HPS Lamp - Half Moon",
            watts: 138,
          },
          {
            desc: "Wall Pack, (1) 150W HPS Lamp - Half Moon",
            watts: 188,
          },
          {
            desc: "Wall Pack, (1) 200W HPS Lamp - Half Moon",
            watts: 250,
          },
          {
            desc: "Wall Pack, (1) 250W HPS Lamp - Half Moon",
            watts: 295,
          },
          {
            desc: "Wall Pack, (1) 400W HPS Lamp - Half Moon",
            watts: 465,
          },
          {
            desc: "Wall Pack, (1) 1000W HPS Lamp - Half Moon",
            watts: 1100,
          },
        ],
      },
      "Semi Cutoff": {
        LED: [
          {
            desc: "LED 100W Wall Pack - Semi Cutoff",
            watts: 100,
          },
          {
            desc: "LED 125W Wall Pack - Semi Cutoff",
            watts: 125,
          },
          {
            desc: "LED 150W Wall Pack - Semi Cutoff",
            watts: 150,
          },
          {
            desc: "LED 25W Wall Pack - Semi Cutoff",
            watts: 25,
          },
          {
            desc: "LED 30W Wall Pack - Semi Cutoff",
            watts: 30,
          },
          {
            desc: "LED 40W Wall Pack - Semi Cutoff",
            watts: 40,
          },
          {
            desc: "LED 50W Wall Pack - Semi Cutoff",
            watts: 50,
          },
          {
            desc: "LED 75W Wall Pack - Semi Cutoff",
            watts: 75,
          },
        ],
        "CFL PIN": [
          {
            desc: "Wall Pack, (1) 13W CFL 2P Lamp - Semi Cutoff",
            watts: 17,
          },
          {
            desc: "Wall Pack, (2) 13W CFL 2P Lamp - Semi Cutoff",
            watts: 31,
          },
          {
            desc: "Wall Pack, (1) 18W CFL 2P Lamp - Semi Cutoff",
            watts: 26,
          },
          {
            desc: "Wall Pack, (2) 18W CFL 2P Lamp - Semi Cutoff",
            watts: 45,
          },
          {
            desc: "Wall Pack, (1) 26W CFL 2P Lamp - Semi Cutoff",
            watts: 33,
          },
          {
            desc: "Wall Pack, (2) 26W CFL 2P Lamp - Semi Cutoff",
            watts: 54,
          },
          {
            desc: "Wall Pack, (1) 32W CFL 2P Lamp - Semi Cutoff",
            watts: 34,
          },
          {
            desc: "Wall Pack, (2) 32W CFL 2P Lamp - Semi Cutoff",
            watts: 62,
          },
          {
            desc: "Wall Pack, (1) 42W CFL 2P Lamp - Semi Cutoff",
            watts: 48,
          },
          {
            desc: "Wall Pack, (2) 42W CFL 2P Lamp - Semi Cutoff",
            watts: 100,
          },
        ],
        "LED HID": [
          {
            desc: "Wall Pack, (1) 20W LED HID Lamp - Semi Cutoff",
            watts: 20,
          },
          {
            desc: "Wall Pack, (1) 30W LED HID Lamp - Semi Cutoff",
            watts: 30,
          },
          {
            desc: "Wall Pack, (1) 40W LED HID Lamp - Semi Cutoff",
            watts: 40,
          },
          {
            desc: "Wall Pack, (1) 50W LED HID Lamp - Semi Cutoff",
            watts: 50,
          },
          {
            desc: "Wall Pack, (1) 75W LED HID Lamp - Semi Cutoff",
            watts: 75,
          },
          {
            desc: "Wall Pack, (1) 100W LED HID Lamp - Semi Cutoff",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Wall Pack, (1) 50W MH Lamp - Semi Cutoff",
            watts: 72,
          },
          {
            desc: "Wall Pack, (1) 70W MH Lamp - Semi Cutoff",
            watts: 95,
          },
          {
            desc: "Wall Pack, (1) 100W MH Lamp - Semi Cutoff",
            watts: 128,
          },
          {
            desc: "Wall Pack, (1) 150W MH Lamp - Semi Cutoff",
            watts: 190,
          },
          {
            desc: "Wall Pack, (1) 175W MH Lamp - Semi Cutoff",
            watts: 215,
          },
          {
            desc: "Wall Pack, (1) 200W MH Lamp - Semi Cutoff",
            watts: 232,
          },
          {
            desc: "Wall Pack, (1) 250W MH Lamp - Semi Cutoff",
            watts: 295,
          },
          {
            desc: "Wall Pack, (1) 400W MH Lamp - Semi Cutoff",
            watts: 458,
          },
          {
            desc: "Wall Pack, (1) 1000W MH Lamp - Semi Cutoff",
            watts: 1060,
          },
        ],
        HPS: [
          {
            desc: "Wall Pack, (1) 50W HPS Lamp - Semi Cutoff",
            watts: 66,
          },
          {
            desc: "Wall Pack, (1) 70W HPS Lamp - Semi Cutoff",
            watts: 95,
          },
          {
            desc: "Wall Pack, (1) 100W HPS Lamp - Semi Cutoff",
            watts: 138,
          },
          {
            desc: "Wall Pack, (1) 150W HPS Lamp - Semi Cutoff",
            watts: 188,
          },
          {
            desc: "Wall Pack, (1) 200W HPS Lamp - Semi Cutoff",
            watts: 250,
          },
          {
            desc: "Wall Pack, (1) 250W HPS Lamp - Semi Cutoff",
            watts: 295,
          },
          {
            desc: "Wall Pack, (1) 400W HPS Lamp - Semi Cutoff",
            watts: 465,
          },
          {
            desc: "Wall Pack, (1) 1000W HPS Lamp - Semi Cutoff",
            watts: 1100,
          },
        ],
      },
      Standard: {
        LED: [
          {
            desc: "LED 100W Wall Pack",
            watts: 100,
          },
          {
            desc: "LED 125W Wall Pack",
            watts: 125,
          },
          {
            desc: "LED 150W Wall Pack",
            watts: 150,
          },
          {
            desc: "LED 25W Wall Pack",
            watts: 25,
          },
          {
            desc: "LED 30W Wall Pack",
            watts: 30,
          },
          {
            desc: "LED 40W Wall Pack",
            watts: 40,
          },
          {
            desc: "LED 50W Wall Pack",
            watts: 50,
          },
          {
            desc: "LED 75W Wall Pack",
            watts: 75,
          },
        ],
        "CFL PIN": [
          {
            desc: "Wall Pack, (1) 13W CFL 2P Lamp",
            watts: 17,
          },
          {
            desc: "Wall Pack, (2) 13W CFL 2P Lamp",
            watts: 31,
          },
          {
            desc: "Wall Pack, (1) 18W CFL 2P Lamp",
            watts: 26,
          },
          {
            desc: "Wall Pack, (2) 18W CFL 2P Lamp",
            watts: 45,
          },
          {
            desc: "Wall Pack, (1) 26W CFL 2P Lamp",
            watts: 33,
          },
          {
            desc: "Wall Pack, (2) 26W CFL 2P Lamp",
            watts: 54,
          },
          {
            desc: "Wall Pack, (1) 32W CFL 2P Lamp",
            watts: 34,
          },
          {
            desc: "Wall Pack, (2) 32W CFL 2P Lamp",
            watts: 62,
          },
          {
            desc: "Wall Pack, (1) 42W CFL 2P Lamp",
            watts: 48,
          },
          {
            desc: "Wall Pack, (2) 42W CFL 2P Lamp",
            watts: 100,
          },
        ],
        "LED HID": [
          {
            desc: "Wall Pack, (1) 20W LED HID lamp",
            watts: 20,
          },
          {
            desc: "Wall Pack, (1) 30W LED HID Lamp",
            watts: 30,
          },
          {
            desc: "Wall Pack, (1) 40W LED HID Lamp",
            watts: 40,
          },
          {
            desc: "Wall Pack, (1) 50W LED HID Lamp",
            watts: 50,
          },
          {
            desc: "Wall Pack, (1) 75W LED HID Lamp",
            watts: 75,
          },
          {
            desc: "Wall Pack, (1) 100W LED HID Lamp",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Wall Pack, (1) 50W MH Lamp",
            watts: 72,
          },
          {
            desc: "Wall Pack, (1) 70W MH Lamp",
            watts: 95,
          },
          {
            desc: "Wall Pack, (1) 100W MH Lamp",
            watts: 128,
          },
          {
            desc: "Wall Pack, (1) 150W MH Lamp",
            watts: 190,
          },
          {
            desc: "Wall Pack, (1) 175W MH Lamp",
            watts: 215,
          },
          {
            desc: "Wall Pack, (1) 200W MH Lamp",
            watts: 232,
          },
          {
            desc: "Wall Pack, (1) 250W MH Lamp",
            watts: 295,
          },
          {
            desc: "Wall Pack, (1) 400W MH Lamp",
            watts: 458,
          },
          {
            desc: "Wall Pack, (1) 1000W MH Lamp",
            watts: 1060,
          },
        ],
        HPS: [
          {
            desc: "Wall Pack, (1) 50W HPS Lamp",
            watts: 66,
          },
          {
            desc: "Wall Pack, (1) 70W HPS Lamp",
            watts: 95,
          },
          {
            desc: "Wall Pack, (1) 100W HPS Lamp",
            watts: 138,
          },
          {
            desc: "Wall Pack, (1) 150W HPS Lamp",
            watts: 188,
          },
          {
            desc: "Wall Pack, (1) 200W HPS Lamp",
            watts: 250,
          },
          {
            desc: "Wall Pack, (1) 250W HPS Lamp",
            watts: 295,
          },
          {
            desc: "Wall Pack, (1) 400W HPS Lamp",
            watts: 465,
          },
          {
            desc: "Wall Pack, (1) 1000W HPS Lamp",
            watts: 1100,
          },
        ],
      },
      Trapezoidal: {
        LED: [
          {
            desc: "LED 100W Wall Pack - Trapezoidal",
            watts: 100,
          },
          {
            desc: "LED 125W Wall Pack - Trapezoidal",
            watts: 125,
          },
          {
            desc: "LED 150W Wall Pack - Trapezoidal",
            watts: 150,
          },
          {
            desc: "LED 25W Wall Pack - Trapezoidal",
            watts: 25,
          },
          {
            desc: "LED 30W Wall Pack - Trapezoidal",
            watts: 30,
          },
          {
            desc: "LED 40W Wall Pack - Trapezoidal",
            watts: 40,
          },
          {
            desc: "LED 50W Wall Pack - Trapezoidal",
            watts: 50,
          },
          {
            desc: "LED 75W Wall Pack - Trapezoidal",
            watts: 75,
          },
        ],
        "CFL PIN": [
          {
            desc: "Wall Pack, (1) 13W CFL 2P Lamp - Trapezoidal",
            watts: 17,
          },
          {
            desc: "Wall Pack, (2) 13W CFL 2P Lamp - Trapezoidal",
            watts: 31,
          },
          {
            desc: "Wall Pack, (1) 18W CFL 2P Lamp - Trapezoidal",
            watts: 26,
          },
          {
            desc: "Wall Pack, (2) 18W CFL 2P Lamp - Trapezoidal",
            watts: 45,
          },
          {
            desc: "Wall Pack, (1) 26W CFL 2P Lamp - Trapezoidal",
            watts: 33,
          },
          {
            desc: "Wall Pack, (2) 26W CFL 2P Lamp - Trapezoidal",
            watts: 54,
          },
          {
            desc: "Wall Pack, (1) 32W CFL 2P Lamp - Trapezoidal",
            watts: 34,
          },
          {
            desc: "Wall Pack, (2) 32W CFL 2P Lamp - Trapezoidal",
            watts: 62,
          },
          {
            desc: "Wall Pack, (1) 42W CFL 2P Lamp - Trapezoidal",
            watts: 48,
          },
          {
            desc: "Wall Pack, (2) 42W CFL 2P Lamp - Trapezoidal",
            watts: 100,
          },
        ],
        "LED HID": [
          {
            desc: "Wall Pack, (1) 20W LED HID Lamp - Trapezoidal",
            watts: 20,
          },
          {
            desc: "Wall Pack, (1) 30W LED HID Lamp - Trapezoidal",
            watts: 30,
          },
          {
            desc: "Wall Pack, (1) 40W LED HID Lamp - Trapezoidal",
            watts: 40,
          },
          {
            desc: "Wall Pack, (1) 50W LED HID Lamp - Trapezoidal",
            watts: 50,
          },
          {
            desc: "Wall Pack, (1) 75W LED HID Lamp - Trapezoidal",
            watts: 75,
          },
          {
            desc: "Wall Pack, (1) 100W LED HID Lamp - Trapezoidal",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Wall Pack, (1) 50W MH Lamp - Trapezoidal",
            watts: 72,
          },
          {
            desc: "Wall Pack, (1) 70W MH Lamp - Trapezoidal",
            watts: 95,
          },
          {
            desc: "Wall Pack, (1) 100W MH Lamp - Trapezoidal",
            watts: 128,
          },
          {
            desc: "Wall Pack, (1) 150W MH Lamp - Trapezoidal",
            watts: 190,
          },
          {
            desc: "Wall Pack, (1) 175W MH Lamp - Trapezoidal",
            watts: 215,
          },
          {
            desc: "Wall Pack, (1) 200W MH Lamp - Trapezoidal",
            watts: 232,
          },
          {
            desc: "Wall Pack, (1) 250W MH Lamp - Trapezoidal",
            watts: 295,
          },
          {
            desc: "Wall Pack, (1) 400W MH Lamp - Trapezoidal",
            watts: 458,
          },
          {
            desc: "Wall Pack, (1) 1000W MH Lamp - Trapezoidal",
            watts: 1060,
          },
        ],
        HPS: [
          {
            desc: "Wall Pack, (1) 50W HPS Lamp - Trapezoidal",
            watts: 66,
          },
          {
            desc: "Wall Pack, (1) 70W HPS Lamp - Trapezoidal",
            watts: 95,
          },
          {
            desc: "Wall Pack, (1) 100W HPS Lamp - Trapezoidal",
            watts: 138,
          },
          {
            desc: "Wall Pack, (1) 150W HPS Lamp - Trapezoidal",
            watts: 188,
          },
          {
            desc: "Wall Pack, (1) 200W HPS Lamp - Trapezoidal",
            watts: 250,
          },
          {
            desc: "Wall Pack, (1) 250W HPS Lamp - Trapezoidal",
            watts: 295,
          },
          {
            desc: "Wall Pack, (1) 400W HPS Lamp - Trapezoidal",
            watts: 465,
          },
          {
            desc: "Wall Pack, (1) 1000W HPS Lamp - Trapezoidal",
            watts: 1100,
          },
        ],
      },
    },
    "Area Light": {
      General: {
        LED: [
          {
            desc: "LED 100W Area Light",
            watts: 100,
          },
          {
            desc: "LED 125W Area Light",
            watts: 125,
          },
          {
            desc: "LED 150W Area Light",
            watts: 150,
          },
          {
            desc: "LED 200W Area Light",
            watts: 200,
          },
          {
            desc: "LED 300W Area Light",
            watts: 300,
          },
          {
            desc: "LED 30W Area Light",
            watts: 30,
          },
          {
            desc: "LED 50W Area Light",
            watts: 50,
          },
          {
            desc: "LED 75W Area Light",
            watts: 75,
          },
        ],
        "LED HID": [
          {
            desc: "Area Light, (1) 15W LED HID Lamp",
            watts: 15,
          },
          {
            desc: "Area Light, (1) 25W LED HID Lamp",
            watts: 25,
          },
          {
            desc: "Area Light, (1) 40W LED HID Lamp",
            watts: 40,
          },
          {
            desc: "Area Light, (1) 50W LED HID Lamp",
            watts: 50,
          },
          {
            desc: "Area Light, (1) 75W LED HID Lamp",
            watts: 75,
          },
          {
            desc: "Area Light, (1) 100W LED HID Lamp",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Area Light, (1) 50W MH Lamp",
            watts: 72,
          },
          {
            desc: "Area Light, (1) 70W MH Lamp",
            watts: 95,
          },
          {
            desc: "Area Light, (1) 100W MH Lamp",
            watts: 128,
          },
          {
            desc: "Area Light, (1) 150W MH Lamp",
            watts: 190,
          },
          {
            desc: "Area Light, (1) 175W MH Lamp",
            watts: 215,
          },
          {
            desc: "Area Light, (1) 200W MH Lamp",
            watts: 232,
          },
          {
            desc: "Area Light, (1) 250W MH Lamp",
            watts: 295,
          },
          {
            desc: "Area Light, (1) 400W MH Lamp",
            watts: 458,
          },
          {
            desc: "Area Light, (1) 1000W MH Lamp",
            watts: 1060,
          },
        ],
        HPS: [
          {
            desc: "Area Light, (1) 50W HPS Lamp",
            watts: 66,
          },
          {
            desc: "Area Light, (1) 70W HPS Lamp",
            watts: 95,
          },
          {
            desc: "Area Light, (1) 100W HPS Lamp",
            watts: 138,
          },
          {
            desc: "Area Light, (1) 150W HPS Lamp",
            watts: 188,
          },
          {
            desc: "Area Light, (1) 200W HPS Lamp",
            watts: 250,
          },
          {
            desc: "Area Light, (1) 250W HPS Lamp",
            watts: 295,
          },
          {
            desc: "Area Light, (1) 400W HPS Lamp",
            watts: 465,
          },
          {
            desc: "Area Light, (1) 1000W HPS Lamp",
            watts: 1100,
          },
        ],
      },
    },
    "Post-Top": {
      General: {
        LED: [
          {
            desc: "LED 100W Post-Top",
            watts: 100,
          },
          {
            desc: "LED 125W Post-Top",
            watts: 125,
          },
          {
            desc: "LED 150W Post-Top",
            watts: 150,
          },
          {
            desc: "LED 200W Post-Top",
            watts: 200,
          },
          {
            desc: "LED 300W Post-Top",
            watts: 300,
          },
          {
            desc: "LED 30W Post-Top",
            watts: 30,
          },
          {
            desc: "LED 50W Post-Top",
            watts: 50,
          },
          {
            desc: "LED 75W Post-Top",
            watts: 75,
          },
        ],
        "LED HID": [
          {
            desc: "Post-Top, (1) 15W LED HID Lamp",
            watts: 15,
          },
          {
            desc: "Post-Top, (1) 25W LED HID Lamp",
            watts: 25,
          },
          {
            desc: "Post-Top, (1) 40W LED HID Lamp",
            watts: 40,
          },
          {
            desc: "Post-Top, (1) 50W LED HID Lamp",
            watts: 50,
          },
          {
            desc: "Post-Top, (1) 75W LED HID Lamp",
            watts: 75,
          },
          {
            desc: "Post-Top, (1) 100W LED HID Lamp",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Post-Top, (1) 50W MH Lamp",
            watts: 72,
          },
          {
            desc: "Post-Top, (1) 70W MH Lamp",
            watts: 95,
          },
          {
            desc: "Post-Top, (1) 100W MH Lamp",
            watts: 128,
          },
          {
            desc: "Post-Top, (1) 150W MH Lamp",
            watts: 190,
          },
          {
            desc: "Post-Top, (1) 175W MH Lamp",
            watts: 215,
          },
          {
            desc: "Post-Top, (1) 200W MH Lamp",
            watts: 232,
          },
          {
            desc: "Post-Top, (1) 250W MH Lamp",
            watts: 295,
          },
          {
            desc: "Post-Top, (1) 400W MH Lamp",
            watts: 458,
          },
          {
            desc: "Post-Top, (1) 1000W MH Lamp",
            watts: 1060,
          },
        ],
        HPS: [
          {
            desc: "Post-Top, (1) 50W HPS Lamp",
            watts: 66,
          },
          {
            desc: "Post-Top, (1) 70W HPS Lamp",
            watts: 95,
          },
          {
            desc: "Post-Top, (1) 100W HPS Lamp",
            watts: 138,
          },
          {
            desc: "Post-Top, (1) 150W HPS Lamp",
            watts: 188,
          },
          {
            desc: "Post-Top, (1) 200W HPS Lamp",
            watts: 250,
          },
          {
            desc: "Post-Top, (1) 250W HPS Lamp",
            watts: 295,
          },
          {
            desc: "Post-Top, (1) 400W HPS Lamp",
            watts: 465,
          },
          {
            desc: "Post-Top, (1) 1000W HPS Lamp",
            watts: 1100,
          },
        ],
      },
    },
    Canopy: {
      Recessed: {
        LED: [
          {
            desc: "LED 100W Recessed Canopy - Square",
            watts: 100,
          },
          {
            desc: "LED 125W Recessed Canopy - Square",
            watts: 125,
          },
          {
            desc: "LED 150W Recessed Canopy - Square",
            watts: 150,
          },
          {
            desc: "LED 30W Recessed Canopy - Square",
            watts: 30,
          },
          {
            desc: "LED 50W Recessed Canopy - Square",
            watts: 50,
          },
          {
            desc: "LED 75W Recessed Canopy - Square",
            watts: 75,
          },
        ],
        "CFL PIN": [
          {
            desc: "Recessed Canopy, (1) 13W CFL 2P Lamp - Square",
            watts: 17,
          },
          {
            desc: "Recessed Canopy, (2) 13W CFL 2P Lamps - Square",
            watts: 31,
          },
          {
            desc: "Recessed Canopy, (1) 26W CFL 4P Lamp - Square",
            watts: 33,
          },
          {
            desc: "Recessed Canopy, (2) 26W CFL 4P Lamps - Square",
            watts: 54,
          },
          {
            desc: "Recessed Canopy, (1) 32W CFL 4P Lamp - Square",
            watts: 34,
          },
          {
            desc: "Recessed Canopy, (2) 32W CFL 4P Lamps - Square",
            watts: 62,
          },
          {
            desc: "Recessed Canopy, (1) 42W CFL 4P Lamp - Square",
            watts: 48,
          },
          {
            desc: "Recessed Canopy, (2) 42W CFL 4P Lamps - Square",
            watts: 100,
          },
        ],
        "LED HID": [
          {
            desc: "Recessed Canopy, (1) 15W LED HID Lamp - Square",
            watts: 15,
          },
          {
            desc: "Recessed Canopy, (1) 25W LED HID Lamp - Square",
            watts: 25,
          },
          {
            desc: "Recessed Canopy, (1) 40W LED HID Lamp - Square",
            watts: 40,
          },
          {
            desc: "Recessed Canopy, (1) 50W LED HID Lamp - Square",
            watts: 50,
          },
          {
            desc: "Recessed Canopy, (1) 75W LED HID Lamp - Square",
            watts: 75,
          },
          {
            desc: "Recessed Canopy, (1) 100W LED HID Lamp - Square",
            watts: 100,
          },
        ],
        "CFL SP": [
          {
            desc: "Recessed Canopy, (1) 18W CFL Spiral - Square",
            watts: 18,
          },
          {
            desc: "Recessed Canopy, (1) 26W CFL Spiral - Square",
            watts: 26,
          },
          {
            desc: "Recessed Canopy, (1) 32W CFL Spiral - Square",
            watts: 32,
          },
        ],
        "Incan.": [
          {
            desc: "Recessed Canopy, (1) 40W A19 Lamp - Square",
            watts: 40,
          },
          {
            desc: "Recessed Canopy, (1) 60W A19 Lamp - Square",
            watts: 60,
          },
          {
            desc: "Recessed Canopy, (1) 100W A19 Lamp - Square",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Recessed Canopy, (1) 50W MH Lamp - Square",
            watts: 72,
          },
          {
            desc: "Recessed Canopy, (1) 70W MH Lamp - Square",
            watts: 95,
          },
          {
            desc: "Recessed Canopy, (1) 100W MH Lamp - Square",
            watts: 128,
          },
          {
            desc: "Recessed Canopy, (1) 150W MH Lamp - Square",
            watts: 190,
          },
          {
            desc: "Recessed Canopy, (1) 175W MH Lamp - Square",
            watts: 215,
          },
          {
            desc: "Recessed Canopy, (1) 200W MH Lamp - Square",
            watts: 232,
          },
          {
            desc: "Recessed Canopy, (1) 250W MH Lamp - Square",
            watts: 295,
          },
        ],
        HPS: [
          {
            desc: "Recessed Canopy, (1) 50W HPS Lamp - Square",
            watts: 66,
          },
          {
            desc: "Recessed Canopy, (1) 70W HPS Lamp - Square",
            watts: 95,
          },
          {
            desc: "Recessed Canopy, (1) 100W HPS Lamp - Square",
            watts: 138,
          },
          {
            desc: "Recessed Canopy, (1) 150W HPS Lamp - Square",
            watts: 188,
          },
          {
            desc: "Recessed Canopy, (1) 200W HPS Lamp - Square",
            watts: 250,
          },
          {
            desc: "Recessed Canopy, (1) 250W HPS Lamp - Square",
            watts: 295,
          },
        ],
      },
      Round: {
        LED: [
          {
            desc: "LED 100W Canopy - Round",
            watts: 100,
          },
          {
            desc: "LED 125W Canopy - Round",
            watts: 125,
          },
          {
            desc: "LED 150W Canopy - Round",
            watts: 150,
          },
          {
            desc: "LED 30W Canopy - Round",
            watts: 30,
          },
          {
            desc: "LED 50W Canopy - Round",
            watts: 50,
          },
          {
            desc: "LED 75W Canopy - Round",
            watts: 75,
          },
        ],
        "CFL PIN": [
          {
            desc: "Canopy, (1) 13W CFL 2P Lamp - Round",
            watts: 17,
          },
          {
            desc: "Canopy, (2) 13W CFL 2P Lamps - Round",
            watts: 31,
          },
          {
            desc: "Canopy, (1) 26W CFL 4P Lamp - Round",
            watts: 33,
          },
          {
            desc: "Canopy, (2) 26W CFL 4P Lamps - Round",
            watts: 54,
          },
          {
            desc: "Canopy, (1) 32W CFL 4P Lamp - Round",
            watts: 34,
          },
          {
            desc: "Canopy, (2) 32W CFL 4P Lamps - Round",
            watts: 62,
          },
          {
            desc: "Canopy, (1) 42W CFL 4P Lamp - Round",
            watts: 48,
          },
          {
            desc: "Canopy, (2) 42W CFL 4P Lamps - Round",
            watts: 100,
          },
        ],
        "LED HID": [
          {
            desc: "Canopy, (1) 15W LED HID Lamp - Round",
            watts: 15,
          },
          {
            desc: "Canopy, (1) 25W LED HID Lamp - Round",
            watts: 25,
          },
          {
            desc: "Canopy, (1) 40W LED HID Lamp - Round",
            watts: 40,
          },
          {
            desc: "Canopy, (1) 50W LED HID Lamp - Round",
            watts: 50,
          },
          {
            desc: "Canopy, (1) 75W LED HID Lamp - Round",
            watts: 75,
          },
          {
            desc: "Canopy, (1) 100W LED HID Lamp - Round",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Canopy, (1) 50W MH Lamp - Round",
            watts: 72,
          },
          {
            desc: "Canopy, (1) 70W MH Lamp - Round",
            watts: 95,
          },
          {
            desc: "Canopy, (1) 100W MH Lamp - Round",
            watts: 128,
          },
          {
            desc: "Canopy, (1) 150W MH Lamp - Round",
            watts: 190,
          },
          {
            desc: "Canopy, (1) 175W MH Lamp - Round",
            watts: 215,
          },
          {
            desc: "Canopy, (1) 200W MH Lamp - Round",
            watts: 232,
          },
          {
            desc: "Canopy, (1) 250W MH Lamp - Round",
            watts: 295,
          },
        ],
        HPS: [
          {
            desc: "Canopy, (1) 50W HPS Lamp - Round",
            watts: 66,
          },
          {
            desc: "Canopy, (1) 70W HPS Lamp - Round",
            watts: 95,
          },
          {
            desc: "Canopy, (1) 100W HPS Lamp - Round",
            watts: 138,
          },
          {
            desc: "Canopy, (1) 150W HPS Lamp - Round",
            watts: 188,
          },
          {
            desc: "Canopy, (1) 200W HPS Lamp - Round",
            watts: 250,
          },
          {
            desc: "Canopy, (1) 250W HPS Lamp - Round",
            watts: 295,
          },
        ],
      },
      "Semi-Recessed": {
        LED: [
          {
            desc: "LED 100W Canopy - Semi-Recessed",
            watts: 100,
          },
          {
            desc: "LED 125W Canopy - Semi-Recessed",
            watts: 125,
          },
          {
            desc: "LED 150W Canopy - Semi-Recessed",
            watts: 150,
          },
          {
            desc: "LED 30W Canopy - Semi-Recessed",
            watts: 30,
          },
          {
            desc: "LED 50W Canopy - Semi-Recessed",
            watts: 50,
          },
          {
            desc: "LED 75W Canopy - Semi-Recessed",
            watts: 75,
          },
        ],
        "CFL PIN": [
          {
            desc: "Canopy, (1) 13W CFL 2P Lamp - Semi-Recessed",
            watts: 17,
          },
          {
            desc: "Canopy, (2) 13W CFL 2P Lamps - Semi-Recessed",
            watts: 31,
          },
          {
            desc: "Canopy, (1) 26W CFL 4P Lamp - Semi-Recessed",
            watts: 33,
          },
          {
            desc: "Canopy, (2) 26W CFL 4P Lamps - Semi-Recessed",
            watts: 54,
          },
          {
            desc: "Canopy, (1) 32W CFL 4P Lamp - Semi-Recessed",
            watts: 34,
          },
          {
            desc: "Canopy, (2) 32W CFL 4P Lamps - Semi-Recessed",
            watts: 62,
          },
          {
            desc: "Canopy, (1) 42W CFL 4P Lamp - Semi-Recessed",
            watts: 48,
          },
          {
            desc: "Canopy, (2) 42W CFL 4P Lamps - Semi-Recessed",
            watts: 100,
          },
        ],
        "LED HID": [
          {
            desc: "Canopy, (1) 15W LED HID Lamp - Semi-Recessed",
            watts: 15,
          },
          {
            desc: "Canopy, (1) 25W LED HID Lamp - Semi-Recessed",
            watts: 25,
          },
          {
            desc: "Canopy, (1) 40W LED HID Lamp - Semi-Recessed",
            watts: 40,
          },
          {
            desc: "Canopy, (1) 50W LED HID Lamp - Semi-Recessed",
            watts: 50,
          },
          {
            desc: "Canopy, (1) 75W LED HID Lamp - Semi-Recessed",
            watts: 75,
          },
          {
            desc: "Canopy, (1) 100W LED HID Lamp - Semi-Recessed",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Canopy, (1) 50W MH Lamp - Semi-Recessed",
            watts: 72,
          },
          {
            desc: "Canopy, (1) 70W MH Lamp - Semi-Recessed",
            watts: 95,
          },
          {
            desc: "Canopy, (1) 100W MH Lamp - Semi-Recessed",
            watts: 128,
          },
          {
            desc: "Canopy, (1) 150W MH Lamp - Semi-Recessed",
            watts: 190,
          },
          {
            desc: "Canopy, (1) 175W MH Lamp - Semi-Recessed",
            watts: 215,
          },
          {
            desc: "Canopy, (1) 200W MH Lamp - Semi-Recessed",
            watts: 232,
          },
          {
            desc: "Canopy, (1) 250W MH Lamp - Semi-Recessed",
            watts: 295,
          },
        ],
        HPS: [
          {
            desc: "Canopy, (1) 50W HPS Lamp - Semi-Recessed",
            watts: 66,
          },
          {
            desc: "Canopy, (1) 70W HPS Lamp - Semi-Recessed",
            watts: 95,
          },
          {
            desc: "Canopy, (1) 100W HPS Lamp - Semi-Recessed",
            watts: 138,
          },
          {
            desc: "Canopy, (1) 150W HPS Lamp - Semi-Recessed",
            watts: 188,
          },
          {
            desc: "Canopy, (1) 200W HPS Lamp - Semi-Recessed",
            watts: 250,
          },
          {
            desc: "Canopy, (1) 250W HPS Lamp - Semi-Recessed",
            watts: 295,
          },
        ],
      },
      Square: {
        LED: [
          {
            desc: "LED 100W Canopy - Square",
            watts: 100,
          },
          {
            desc: "LED 125W Canopy - Square",
            watts: 125,
          },
          {
            desc: "LED 150W Canopy - Square",
            watts: 150,
          },
          {
            desc: "LED 30W Canopy - Square",
            watts: 30,
          },
          {
            desc: "LED 50W Canopy - Square",
            watts: 50,
          },
          {
            desc: "LED 75W Canopy - Square",
            watts: 75,
          },
        ],
        "CFL PIN": [
          {
            desc: "Canopy, (1) 13W CFL 2P Lamp - Square",
            watts: 17,
          },
          {
            desc: "Canopy, (2) 13W CFL 2P Lamps - Square",
            watts: 31,
          },
          {
            desc: "Canopy, (1) 26W CFL 4P Lamp - Square",
            watts: 33,
          },
          {
            desc: "Canopy, (2) 26W CFL 4P Lamps - Square",
            watts: 54,
          },
          {
            desc: "Canopy, (1) 32W CFL 4P Lamp - Square",
            watts: 34,
          },
          {
            desc: "Canopy, (2) 32W CFL 4P Lamps - Square",
            watts: 62,
          },
          {
            desc: "Canopy, (1) 42W CFL 4P Lamp - Square",
            watts: 48,
          },
          {
            desc: "Canopy, (2) 42W CFL 4P Lamps - Square",
            watts: 100,
          },
        ],
        "LED HID": [
          {
            desc: "Canopy, (1) 15W LED HID Lamp - Square",
            watts: 15,
          },
          {
            desc: "Canopy, (1) 25W LED HID Lamp - Square",
            watts: 25,
          },
          {
            desc: "Canopy, (1) 40W LED HID Lamp - Square",
            watts: 40,
          },
          {
            desc: "Canopy, (1) 50W LED HID Lamp - Square",
            watts: 50,
          },
          {
            desc: "Canopy, (1) 75W LED HID Lamp - Square",
            watts: 75,
          },
          {
            desc: "Canopy, (1) 100W LED HID Lamp - Square",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Canopy, (1) 50W MH Lamp - Square",
            watts: 72,
          },
          {
            desc: "Canopy, (1) 70W MH Lamp - Square",
            watts: 95,
          },
          {
            desc: "Canopy, (1) 100W MH Lamp - Square",
            watts: 128,
          },
          {
            desc: "Canopy, (1) 150W MH Lamp - Square",
            watts: 190,
          },
          {
            desc: "Canopy, (1) 175W MH Lamp - Square",
            watts: 215,
          },
          {
            desc: "Canopy, (1) 200W MH Lamp - Square",
            watts: 232,
          },
          {
            desc: "Canopy, (1) 250W MH Lamp - Square",
            watts: 295,
          },
        ],
        HPS: [
          {
            desc: "Canopy, (1) 50W HPS Lamp - Square",
            watts: 66,
          },
          {
            desc: "Canopy, (1) 70W HPS Lamp - Square",
            watts: 95,
          },
          {
            desc: "Canopy, (1) 100W HPS Lamp - Square",
            watts: 138,
          },
          {
            desc: "Canopy, (1) 150W HPS Lamp - Square",
            watts: 188,
          },
          {
            desc: "Canopy, (1) 200W HPS Lamp - Square",
            watts: 250,
          },
          {
            desc: "Canopy, (1) 250W HPS Lamp - Square",
            watts: 295,
          },
        ],
      },
      "Vandal-Proof": {
        LED: [
          {
            desc: "LED 100W Canopy - Vandal-Resist",
            watts: 100,
          },
          {
            desc: "LED 125W Canopy - Vandal-Resist",
            watts: 125,
          },
          {
            desc: "LED 150W Canopy - Vandal-Resist",
            watts: 150,
          },
          {
            desc: "LED 30W Canopy - Vandal-Resist",
            watts: 30,
          },
          {
            desc: "LED 50W Canopy - Vandal-Resist",
            watts: 50,
          },
          {
            desc: "LED 75W Canopy - Vandal-Resist",
            watts: 75,
          },
        ],
        "CFL PIN": [
          {
            desc: "Canopy, (1) 13W CFL 2P Lamp - Vandal-Resist",
            watts: 17,
          },
          {
            desc: "Canopy, (2) 13W CFL 2P Lamps - Vandal-Resist",
            watts: 31,
          },
          {
            desc: "Canopy, (1) 26W CFL 4P Lamp - Vandal-Resist",
            watts: 33,
          },
          {
            desc: "Canopy, (2) 26W CFL 4P Lamps - Vandal-Resist",
            watts: 54,
          },
          {
            desc: "Canopy, (1) 32W CFL 4P Lamp - Vandal-Resist",
            watts: 34,
          },
          {
            desc: "Canopy, (2) 32W CFL 4P Lamps - Vandal-Resist",
            watts: 62,
          },
          {
            desc: "Canopy, (1) 42W CFL 4P Lamp - Vandal-Resist",
            watts: 48,
          },
          {
            desc: "Canopy, (2) 42W CFL 4P Lamps - Vandal-Resist",
            watts: 100,
          },
        ],
        "LED HID": [
          {
            desc: "Canopy, (1) 15W LED HID Lamp - Vandal-Resist",
            watts: 15,
          },
          {
            desc: "Canopy, (1) 25W LED HID Lamp - Vandal-Resist",
            watts: 25,
          },
          {
            desc: "Canopy, (1) 40W LED HID Lamp - Vandal-Resist",
            watts: 40,
          },
          {
            desc: "Canopy, (1) 50W LED HID Lamp - Vandal-Resist",
            watts: 50,
          },
          {
            desc: "Canopy, (1) 75W LED HID Lamp - Vandal-Resist",
            watts: 75,
          },
          {
            desc: "Canopy, (1) 100W LED HID Lamp - Vandal-Resist",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Canopy, (1) 50W MH Lamp - Vandal-Resist",
            watts: 72,
          },
          {
            desc: "Canopy, (1) 70W MH Lamp - Vandal-Resist",
            watts: 95,
          },
          {
            desc: "Canopy, (1) 100W MH Lamp - Vandal-Resist",
            watts: 128,
          },
          {
            desc: "Canopy, (1) 150W MH Lamp - Vandal-Resist",
            watts: 190,
          },
          {
            desc: "Canopy, (1) 175W MH Lamp - Vandal-Resist",
            watts: 215,
          },
          {
            desc: "Canopy, (1) 200W MH Lamp - Vandal-Resist",
            watts: 232,
          },
          {
            desc: "Canopy, (1) 250W MH Lamp - Vandal-Resist",
            watts: 295,
          },
        ],
        HPS: [
          {
            desc: "Canopy, (1) 50W HPS Lamp - Vandal-Resist",
            watts: 66,
          },
          {
            desc: "Canopy, (1) 70W HPS Lamp - Vandal-Resist",
            watts: 95,
          },
          {
            desc: "Canopy, (1) 100W HPS Lamp - Vandal-Resist",
            watts: 138,
          },
          {
            desc: "Canopy, (1) 150W HPS Lamp - Vandal-Resist",
            watts: 188,
          },
          {
            desc: "Canopy, (1) 200W HPS Lamp - Vandal-Resist",
            watts: 250,
          },
          {
            desc: "Canopy, (1) 250W HPS Lamp - Vandal-Resist",
            watts: 295,
          },
        ],
      },
    },
    Architectural: {
      "Re-Lamp Only": {
        LED: [
          {
            desc: "LED 20W Sconce - Wall Mount",
            watts: 20,
          },
          {
            desc: "LED 30W Sconce - Wall Mount",
            watts: 30,
          },
          {
            desc: "LED 40W Sconce - Wall Mount",
            watts: 40,
          },
          {
            desc: "LED 50W Sconce - Wall Mount",
            watts: 50,
          },
        ],
        "LED SI": [
          {
            desc: "Architectural, (1) 8W LED A19 - Wall Mount",
            watts: 8,
          },
          {
            desc: "Architectural, (2) 8W LED A19 - Wall Mount",
            watts: 16,
          },
          {
            desc: "Architectural, (1) 10W LED A19 - Wall Mount",
            watts: 10,
          },
          {
            desc: "Architectural, (2) 10W LED A19 - Wall Mount",
            watts: 20,
          },
          {
            desc: "Architectural, (1) 15W LED A19 - Wall Mount",
            watts: 15,
          },
          {
            desc: "Architectural, (2) 15W LED A19 - Wall Mount",
            watts: 30,
          },
        ],
        "CFL SP": [
          {
            desc: "Architectural, (2) 13W CFL Spiral - Wall Mount",
            watts: 26,
          },
          {
            desc: "Architectural, (2) 23W CFL Spiral - Wall Mount",
            watts: 46,
          },
          {
            desc: "Architectural, (2) 32W CFL Spiral - Wall Mount",
            watts: 64,
          },
          {
            desc: "Architectural, (2) 42W CFL Spiral - Wall Mount",
            watts: 84,
          },
        ],
        "Metal Halide": [
          {
            desc: "Architectural, (2) 50W MH Lamps - Wall Mount",
            watts: 72,
          },
          {
            desc: "Architectural, (2) 70W MH Lamps - Wall Mount",
            watts: 95,
          },
          {
            desc: "Architectural, (2) 100W MH Lamps - Wall Mount",
            watts: 128,
          },
          {
            desc: "Architectural, (2) 150W MH Lamps - Wall Mount",
            watts: 190,
          },
          {
            desc: "Architectural, (2) 175W MH Lamps - Wall Mount",
            watts: 215,
          },
        ],
        HPS: [
          {
            desc: "Architectural, (2) 50W HPS Lamps - Wall Mount",
            watts: 66,
          },
          {
            desc: "Architectural, (2) 70W HPS Lamps - Wall Mount",
            watts: 95,
          },
          {
            desc: "Architectural, (2) 100W HPS Lamps - Wall Mount",
            watts: 138,
          },
          {
            desc: "Architectural, (2) 150W HPS Lamps - Wall Mount",
            watts: 188,
          },
        ],
        "Incan.": [
          {
            desc: "Architectural, (2) 60W A19 Lamps - Wall Mount",
            watts: 60,
          },
          {
            desc: "Architectural, (2) 75W A19 Lamps - Wall Mount",
            watts: 75,
          },
          {
            desc: "Architectural, (2) 100W A19 Lamps - Wall Mount",
            watts: 100,
          },
        ],
      },
    },
    "Mini Wall Pack": {
      Standard: {
        "LED HID": [
          {
            desc: "Mini Wall Pack, (1) 10W LED HID lamp",
            watts: 10,
          },
          {
            desc: "Mini Wall Pack, (1) 20W LED HID lamp",
            watts: 20,
          },
          {
            desc: "Mini Wall Pack, (1) 30W LED HID Lamp",
            watts: 30,
          },
          {
            desc: "Mini Wall Pack, (1) 40W LED HID Lamp",
            watts: 40,
          },
        ],
        "CFL PIN": [
          {
            desc: "Mini Wall Pack, (1) 13W CFL 2P Lamp",
            watts: 17,
          },
          {
            desc: "Mini Wall Pack, (2) 13W CFL 2P Lamps",
            watts: 31,
          },
          {
            desc: "Mini Wall Pack, (1) 18W CFL 2P Lamp",
            watts: 26,
          },
          {
            desc: "Mini Wall Pack, (2) 18W CFL 2P Lamps",
            watts: 45,
          },
          {
            desc: "Mini Wall Pack, (1) 26W CFL 2P Lamp",
            watts: 33,
          },
          {
            desc: "Mini Wall Pack, (2) 26W CFL 2P Lamps",
            watts: 54,
          },
          {
            desc: "Mini Wall Pack, (1) 32W CFL 2P Lamp",
            watts: 34,
          },
          {
            desc: "Mini Wall Pack, (2) 32W CFL 2P Lamps",
            watts: 62,
          },
          {
            desc: "Mini Wall Pack, (1) 42W CFL 2P Lamp",
            watts: 48,
          },
          {
            desc: "Mini Wall Pack, (2) 42W CFL 2P Lamps",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Mini Wall Pack, (1) 50W MH Lamp",
            watts: 72,
          },
          {
            desc: "Mini Wall Pack, (1) 70W MH Lamp",
            watts: 95,
          },
          {
            desc: "Mini Wall Pack, (1) 100W MH Lamp",
            watts: 128,
          },
        ],
        HPS: [
          {
            desc: "Mini Wall Pack, (1) 50W HPS Lamp",
            watts: 66,
          },
          {
            desc: "Mini Wall Pack, (1) 70W HPS Lamp",
            watts: 95,
          },
          {
            desc: "Mini Wall Pack, (1) 100W HPS Lamp",
            watts: 138,
          },
        ],
      },
    },
    "Tall Pack": {
      Standard: {
        "LED HID": [
          {
            desc: "Tall Pack, (1) 10W LED HID lamp",
            watts: 10,
          },
          {
            desc: "Tall Pack, (1) 20W LED HID lamp",
            watts: 20,
          },
          {
            desc: "Tall Pack, (1) 30W LED HID Lamp",
            watts: 30,
          },
          {
            desc: "Tall Pack, (1) 40W LED HID Lamp",
            watts: 40,
          },
        ],
        "CFL PIN": [
          {
            desc: "Tall Pack, (1) 13W CFL 2P Lamp",
            watts: 17,
          },
          {
            desc: "Tall Pack, (2) 13W CFL 2P Lamps",
            watts: 31,
          },
          {
            desc: "Tall Pack, (1) 18W CFL 2P Lamp",
            watts: 26,
          },
          {
            desc: "Tall Pack, (2) 18W CFL 2P Lamps",
            watts: 45,
          },
          {
            desc: "Tall Pack, (1) 26W CFL 2P Lamp",
            watts: 33,
          },
          {
            desc: "Tall Pack, (2) 26W CFL 2P Lamps",
            watts: 54,
          },
          {
            desc: "Tall Pack, (1) 32W CFL 2P Lamp",
            watts: 34,
          },
          {
            desc: "Tall Pack, (2) 32W CFL 2P Lamps",
            watts: 62,
          },
          {
            desc: "Tall Pack, (1) 42W CFL 2P Lamp",
            watts: 48,
          },
          {
            desc: "Tall Pack, (2) 42W CFL 2P Lamps",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Tall Pack, (1) 50W MH Lamp",
            watts: 72,
          },
          {
            desc: "Tall Pack, (1) 70W MH Lamp",
            watts: 95,
          },
          {
            desc: "Tall Pack, (1) 100W MH Lamp",
            watts: 128,
          },
        ],
        HPS: [
          {
            desc: "Tall Pack, (1) 50W HPS Lamp",
            watts: 66,
          },
          {
            desc: "Tall Pack, (1) 70W HPS Lamp",
            watts: 95,
          },
          {
            desc: "Tall Pack, (1) 100W HPS Lamp",
            watts: 138,
          },
        ],
      },
    },
    "Barn Light": {
      "Tenon-Mount": {
        LED: [
          {
            desc: "LED 100W Barn Light - Tenon-Mount",
            watts: 100,
          },
          {
            desc: "LED 125W Barn Light - Tenon-Mount",
            watts: 125,
          },
          {
            desc: "LED 150W Barn Light - Tenon-Mount",
            watts: 150,
          },
          {
            desc: "LED 200W Barn Light - Tenon-Mount",
            watts: 200,
          },
          {
            desc: "LED 300W Barn Light - Tenon-Mount",
            watts: 300,
          },
          {
            desc: "LED 30W Barn Light - Tenon-Mount",
            watts: 30,
          },
          {
            desc: "LED 50W Barn Light - Tenon-Mount",
            watts: 50,
          },
          {
            desc: "LED 75W Barn Light - Tenon-Mount",
            watts: 75,
          },
        ],
        "LED HID": [
          {
            desc: "Barn Light, (1) 15W LED HID Lamp - Tenon-Mount",
            watts: 15,
          },
          {
            desc: "Barn Light, (1) 25W LED HID Lamp - Tenon-Mount",
            watts: 25,
          },
          {
            desc: "Barn Light, (1) 40W LED HID Lamp - Tenon-Mount",
            watts: 40,
          },
          {
            desc: "Barn Light, (1) 50W LED HID Lamp - Tenon-Mount",
            watts: 50,
          },
          {
            desc: "Barn Light, (1) 75W LED HID Lamp - Tenon-Mount",
            watts: 75,
          },
          {
            desc: "Barn Light, (1) 100W LED HID Lamp - Tenon-Mount",
            watts: 100,
          },
        ],
        "Metal Halide": [
          {
            desc: "Barn Light, (1) 50W MH Lamp - Tenon-Mount",
            watts: 72,
          },
          {
            desc: "Barn Light, (1) 70W MH Lamp - Tenon-Mount",
            watts: 95,
          },
          {
            desc: "Barn Light, (1) 100W MH Lamp - Tenon-Mount",
            watts: 128,
          },
          {
            desc: "Barn Light, (1) 150W MH Lamp - Tenon-Mount",
            watts: 190,
          },
          {
            desc: "Barn Light, (1) 175W MH Lamp - Tenon-Mount",
            watts: 215,
          },
          {
            desc: "Barn Light, (1) 200W MH Lamp - Tenon-Mount",
            watts: 232,
          },
          {
            desc: "Barn Light, (1) 250W MH Lamp - Tenon-Mount",
            watts: 295,
          },
          {
            desc: "Barn Light, (1) 400W MH Lamp - Tenon-Mount",
            watts: 458,
          },
          {
            desc: "Barn Light, (1) 1000W MH Lamp - Tenon-Mount",
            watts: 1060,
          },
        ],
        HPS: [
          {
            desc: "Barn Light, (1) 50W HPS Lamp - Tenon-Mount",
            watts: 66,
          },
          {
            desc: "Barn Light, (1) 70W HPS Lamp - Tenon-Mount",
            watts: 95,
          },
          {
            desc: "Barn Light, (1) 100W HPS Lamp - Tenon-Mount",
            watts: 138,
          },
          {
            desc: "Barn Light, (1) 150W HPS Lamp - Tenon-Mount",
            watts: 188,
          },
          {
            desc: "Barn Light, (1) 200W HPS Lamp - Tenon-Mount",
            watts: 250,
          },
          {
            desc: "Barn Light, (1) 250W HPS Lamp - Tenon-Mount",
            watts: 295,
          },
          {
            desc: "Barn Light, (1) 400W HPS Lamp - Tenon-Mount",
            watts: 465,
          },
          {
            desc: "Barn Light, (1) 1000W HPS Lamp - Tenon-Mount",
            watts: 1100,
          },
        ],
      },
    },
    Bulkhead: {
      Wall: {
        "LED SI": [
          {
            desc: "Bulkhead, (1) 10W LED A19 Lamp",
            watts: 10,
          },
          {
            desc: "Bulkhead, (1) 15W LED A19 Lamp",
            watts: 15,
          },
        ],
        "CFL PIN": [
          {
            desc: "Bulkhead, (1) 13W CFL 2P Lamp",
            watts: 17,
          },
          {
            desc: "Bulkhead, (2) 13W CFL 2P Lamps",
            watts: 31,
          },
          {
            desc: "Bulkhead, (1) 26W CFL 4P Lamp",
            watts: 33,
          },
          {
            desc: "Bulkhead, (2) 26W CFL 4P Lamps",
            watts: 54,
          },
          {
            desc: "Bulkhead, (1) 32W CFL 4P Lamp",
            watts: 34,
          },
          {
            desc: "Bulkhead, (2) 32W CFL 4P Lamps",
            watts: 62,
          },
          {
            desc: "Bulkhead, (1) 42W CFL 4P Lamp",
            watts: 48,
          },
          {
            desc: "Bulkhead, (2) 42W CFL 4P Lamps",
            watts: 100,
          },
        ],
        "CFL SP": [
          {
            desc: "Bulkhead, (1) 18W CFL Spiral",
            watts: 18,
          },
          {
            desc: "Bulkhead, (1) 26W CFL Spiral",
            watts: 26,
          },
          {
            desc: "Bulkhead, (1) 32W CFL Spiral",
            watts: 32,
          },
        ],
        "Metal Halide": [
          {
            desc: "Bulkhead, (1) 50W MH Lamp",
            watts: 72,
          },
          {
            desc: "Bulkhead, (1) 70W MH Lamp",
            watts: 95,
          },
          {
            desc: "Bulkhead, (1) 100W MH Lamp",
            watts: 128,
          },
        ],
        HPS: [
          {
            desc: "Bulkhead, (1) 50W HPS Lamp",
            watts: 66,
          },
          {
            desc: "Bulkhead, (1) 70W HPS Lamp",
            watts: 95,
          },
          {
            desc: "Bulkhead, (1) 100W HPS Lamp",
            watts: 138,
          },
        ],
      },
    },
  },
};
// Runtime registry: code -> { desc, watts, type }. Populated dynamically by addFixture(), fbDoSelect(), saveCustomFixture(), etc.
const FIXTURES = {};

// ── STATE ─────────────────────────────────────────────────────────────────
const state = {
  room: "",
  roomConfirmed: false,
  height: "",
  roomtype: "",
  switchType: [], // [{name, qty}]
  switchNotes: [], // ['A/B Switching', '3-Way']
  zoneLabel: "", // e.g. 'Main', 'Task', 'Emergency'
  existingCtrls: [], // [{name, qty}]
  swRec: [], // [{name, qty}] — switch recommendation
  fixRec: [], // [{name, qty}] — fixture replacement recommendation
  ctrls: [], // [{name, qty}] — control recommendation
  fixtures: [],
  activeFixture: "",
  dimensions: null,
  ceilingType: "",
  fixtureFlagIntSensor: false,
  fixtureFlagEMBB: false,
  fixtureFlagAirReturn: false,
  fixtureFlagWireGuard: false,
  fixtureFlag480V: false,
  fixtureFlagEMGen: false,
  fixtureMountType: null,
  // Exterior-only attribute flags + optic. Set by the exterior section of the
  // Attribute modal when the active fixture is an exterior type. Captured into
  // each fixture entry on Add and persisted in drafts/clipboard/log.
  fixtureFlagPhotocell: false,
  fixtureFlagTwistlockPC: false,
  fixtureFlagWetLocation: false,
  fixtureFlagDarkSky: false,
  fixtureOptic: null, // 'Type II' | 'Type III' | 'Type IV' | 'Type V' | null
};
let qty = 1;

// ── Dismiss keyboard on background tap ───────────────────

// ── Qty Numpad ────────────────────────────────────────────
let qtyKbStr = "1";

function openQtyKeyboard() {
  qtyKbStr = ""; // empty so first digit replaces — current qty shown in display until then
  document.getElementById("qtyKbText").textContent = String(qty || 1);
  const display = document.getElementById("qtyDisplay");
  const panel = document.getElementById("qtyKbPanel");
  if (display) {
    const rect = display.getBoundingClientRect();
    const panelW = 220;
    let left = rect.left - panelW / 2 + rect.width / 2;
    left = Math.max(8, Math.min(left, window.innerWidth - panelW - 8));
    panel.style.left = left + "px";
    panel.style.top = rect.bottom + 8 + "px";
  }
  document.getElementById("qtyKbOverlay").style.display = "block";
  panel.style.display = "block";
}

function closeQtyKeyboard() {
  document.getElementById("qtyKbOverlay").style.display = "none";
  document.getElementById("qtyKbPanel").style.display = "none";
  const val = Math.max(1, parseInt(qtyKbStr || qty) || 1);
  qty = val;
  document.getElementById("qtyVal").value = val;
  document.getElementById("qtyDisplay").textContent = val;
  if (state.activeFixture) renderReviewArea();
}

function qtyKbPress(n) {
  if (qtyKbStr === "" || qtyKbStr === "0") {
    qtyKbStr = String(n);
  } else {
    qtyKbStr += String(n);
  }
  if (parseInt(qtyKbStr) > 999) qtyKbStr = "999";
  document.getElementById("qtyKbText").textContent = qtyKbStr;
}

function qtyKbBackspace() {
  qtyKbStr = qtyKbStr.slice(0, -1);
  document.getElementById("qtyKbText").textContent =
    qtyKbStr || String(qty || 1);
}

// ── Room Custom Keyboard ──────────────────────────────────
const ROOM_KB_ROWS = [
  ["Q", "W", "E", "R", "T", "Y", "U", "I", "O", "P"],
  ["A", "S", "D", "F", "G", "H", "J", "K", "L", "-"],
  ["Z", "X", "C", "V", "B", "N", "M", "/", ".", "SP"],
];
const ROOM_KB_NUMPAD = [
  ["1", "2", "3"],
  ["4", "5", "6"],
  ["7", "8", "9"],
  [".", "0", "BS"],
];

function buildRoomKb() {
  const letters = document.getElementById("roomKbKeys");
  if (letters && !letters.children.length)
    ROOM_KB_ROWS.forEach((row) => {
      row.forEach((k) => {
        const btn = document.createElement("button");
        btn.className = "kb-key";
        btn.textContent = k === "SP" ? "\u2423" : k;
        btn.addEventListener("click", () => kbPress(k === "SP" ? " " : k));
        letters.appendChild(btn);
      });
    });
  const numpad = document.getElementById("roomKbNumpad");
  if (numpad && !numpad.children.length)
    ROOM_KB_NUMPAD.forEach((row) =>
      row.forEach((k) => {
        const btn = document.createElement("button");
        if (k === "BS") {
          btn.className = "kb-key bs-num";
          btn.textContent = "\u232b";
          btn.addEventListener("click", kbBackspace);
        } else {
          btn.className = "kb-key num";
          btn.textContent = k;
          btn.addEventListener("click", () => kbPress(k));
        }
        numpad.appendChild(btn);
      }),
    );
}

let kbTarget = "room"; // 'room' or 'notes'

function openRoomKeyboard() {
  kbTarget = "room";
  document.getElementById("kbDoneBtn").textContent = "Enter";
  document.getElementById("kbCloseBtn").style.display = "none";
  buildRoomKb();
  document.getElementById("roomKbOverlay").style.display = "block";
  document.getElementById("roomKbPanel").style.display = "block";
  document.getElementById("kbRoomText").textContent = "";
  if (document.activeElement) document.activeElement.blur();
}

function openNotesKeyboard() {
  kbTarget = "notes";
  document.getElementById("kbDoneBtn").textContent = "Enter ›";
  document.getElementById("kbCloseBtn").style.display = "inline-block";
  buildRoomKb();
  document.getElementById("roomKbOverlay").style.display = "block";
  document.getElementById("roomKbPanel").style.display = "block";
  document.getElementById("kbRoomText").textContent = "";
  if (document.activeElement) document.activeElement.blur();
}

function closeRoomKeyboard() {
  const val = document.getElementById("kbRoomText").textContent.trim();
  if (kbTarget === "notes") {
    if (val) {
      // Append to existing notes with comma separator
      const ta = document.getElementById("notesInput");
      const existing = ta.value.trim();
      ta.value = existing ? existing + ", " + val : val;
      syncNotesDisplay();
      updateSummary();
      scheduleSave();
      toast("✔ Note added");
    }
    // Clear keyboard but keep it OPEN for next note
    document.getElementById("kbRoomText").textContent = "";
    return; // don't close
  }
  // Room mode — close and save (empty val clears the room name)
  document.getElementById("roomKbOverlay").style.display = "none";
  document.getElementById("roomKbPanel").style.display = "none";
  document.getElementById("roomInput").value = val;
  state.room = val;
  updateRoomDisplay();
  updateSummary();
  scheduleSave();
}

function closeKbPanel() {
  document.getElementById("roomKbOverlay").style.display = "none";
  document.getElementById("roomKbPanel").style.display = "none";
  document.getElementById("kbRoomText").textContent = "";
}

function kbPress(char) {
  const s = document.getElementById("kbRoomText");
  s.textContent += char;
}

function kbBackspace() {
  const s = document.getElementById("kbRoomText");
  if (s.textContent.length > 0) {
    s.textContent = s.textContent.slice(0, -1);
  }
}

function deleteLastNote() {
  const ta = document.getElementById("notesInput");
  const val = ta.value.trim();
  if (!val) return;
  // Remove last comma-separated segment
  const lastComma = val.lastIndexOf(",");
  ta.value = lastComma >= 0 ? val.slice(0, lastComma).trim() : "";
  syncNotesDisplay();
  updateSummary();
  scheduleSave();
}

function clearAllNotes() {
  const ta = document.getElementById("notesInput");
  ta.value = "";
  syncNotesDisplay();
  updateSummary();
  scheduleSave();
}

function syncNotesDisplay() {
  const val = document.getElementById("notesInput").value;
  const txt = document.getElementById("notesDisplayText");
  if (txt) {
    txt.textContent = val || "Tap to add notes…";
    txt.style.color = val ? "#1a2a3a" : "#8a9ab0";
  }
}

function updateRoomDisplay() {
  const val = document.getElementById("roomInput").value;
  const txt = document.getElementById("roomDisplayText");
  const display = document.getElementById("roomDisplay");
  if (txt) txt.textContent = val || "Room Name";
  if (display) display.classList.toggle("filled", !!val);
  if (typeof renderAuditMinimap === "function") renderAuditMinimap();
  // Copy/Paste button label depends on current room vs the clipboard's source
  if (typeof updateCopyConditionsBtn === "function") updateCopyConditionsBtn();
  if (typeof syncRoomPhotoBtn === "function") syncRoomPhotoBtn();
}

document.addEventListener(
  "touchend",
  (e) => {
    const tag = e.target.tagName;
    if (tag !== "INPUT" && tag !== "TEXTAREA" && tag !== "SELECT") {
      if (
        document.activeElement &&
        (document.activeElement.tagName === "INPUT" ||
          document.activeElement.tagName === "TEXTAREA")
      ) {
        document.activeElement.blur();
      }
    }
  },
  { passive: true },
);

// ── Apple Pencil — block from text inputs entirely ───────
// Pencil triggers AutoFill/compact keyboard in iOS PWA — prevent it.
// IMPORTANT: skip the floor plan overlay entirely so pencil markup is not
// disturbed by document-level blur() calls (those caused a 1s delay between
// strokes on iPadOS — iOS dispatches focus/blur cleanup on each pen down).
document.addEventListener(
  "pointerdown",
  (e) => {
    if (e.pointerType !== "pen") return;
    // Floor plan handles its own pen events; do nothing here.
    if (e.target && e.target.closest && e.target.closest("#floorPlanOverlay"))
      return;
    const tag = e.target.tagName;
    // If pencil hits an input or textarea, block it and dismiss keyboard instead
    if (tag === "INPUT" || tag === "TEXTAREA") {
      e.preventDefault();
      e.stopImmediatePropagation();
      document.activeElement?.blur();
      toast("Use finger to type — Pencil disabled for text fields");
      return;
    }
    // Pencil on non-input — dismiss keyboard if open
    if (
      document.activeElement &&
      (document.activeElement.tagName === "INPUT" ||
        document.activeElement.tagName === "TEXTAREA")
    ) {
      document.activeElement.blur();
    }
  },
  { passive: false },
);

// ── Reinforce inputmode on every focus (Pencil + touch) ──
document.addEventListener("focusin", (e) => {
  const el = e.target;
  if (el.tagName === "INPUT" || el.tagName === "TEXTAREA") {
    // Force inputmode attribute in case iOS dropped it
    if (
      !el.getAttribute("inputmode") ||
      el.getAttribute("inputmode") === "none"
    ) {
      el.setAttribute("inputmode", "text");
    }
    // Scroll into view
    setTimeout(
      () => el.scrollIntoView({ behavior: "smooth", block: "center" }),
      350,
    );
  }
});

let logEntries = [];
// Per-room in-progress drafts, keyed by room name. Lets the user navigate
// between rooms (via minimap, floor-plan callout, floor switch) without losing
// uncommitted fixtures/switches/controls/notes. Snapshotted before any room
// switch, restored when navigating back, cleared on commit (logEntry).
let roomDrafts = {};

// ── FIXTURE FREQUENCY TRACKING ────────────────────────────────────────────
const fixtureFreq = {};
const recentFixtures = []; // last 4 used codes (with suffix), most recent last

function rebuildFreqGrid() {
  // Frequency grid is disabled — selections live in the pink recent buttons only
  const grid = document.getElementById("fixtureBtns");
  grid.innerHTML =
    '<span style="color:#8a9ab0;font-size:11px;font-style:italic;padding:4px 0">Tap Select Fixture to browse catalogue</span>';
}

function openRecentPopover() {
  const list = document.getElementById("recentPopoverList");
  if (!list) return;

  // Build usage counts from logEntries
  const qtyCounts = {};
  logEntries.forEach((e) => {
    if (!e.fixtureId) return;
    qtyCounts[e.fixtureId] = (qtyCounts[e.fixtureId] || 0) + (e.qty || 1);
  });

  // Combine recents with any fixtures that appear in log (last 10 unique, most used first)
  const allCodes = [
    ...new Set([...Object.keys(qtyCounts), ...recentFixtures]),
  ].slice(0, 20);

  // Sort: highest qty in log first, then recency
  const sorted = allCodes
    .map((code) => ({
      code,
      logQty: qtyCounts[code] || 0,
      recencyIdx: recentFixtures.lastIndexOf(code),
      fx: FIXTURES[code],
    }))
    .sort((a, b) => {
      if (b.logQty !== a.logQty) return b.logQty - a.logQty;
      return b.recencyIdx - a.recencyIdx;
    })
    .slice(0, 10);

  if (!sorted.length) {
    list.innerHTML =
      '<div style="text-align:center;padding:20px;color:#94a3b8;font-size:13px">No fixtures used yet</div>';
  } else {
    list.innerHTML = "";
    sorted.forEach(({ code, logQty, fx }) => {
      const desc = fx?.desc || code;
      const shortDesc = desc.length > 50 ? desc.substring(0, 50) + "…" : desc;
      const row = document.createElement("div");
      row.style.cssText =
        "display:flex;align-items:center;gap:10px;background:#fdf0f8;border:1.5px solid #f0b8dc;border-radius:10px;padding:9px 12px;cursor:pointer;transition:background .12s";
      row.innerHTML = `
        <div style="flex:1;min-width:0">
          <div style="font-size:13px;font-weight:700;color:#1a2a3a;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${shortDesc}</div>
          <div style="font-size:10px;color:#94a3b8;margin-top:2px">${code}</div>
        </div>
        ${
          logQty
            ? `<div style="flex-shrink:0;background:#c060a0;color:#fff;font-size:11px;font-weight:800;border-radius:20px;padding:3px 10px;white-space:nowrap">×${logQty} in log</div>`
            : ""
        }
        <div style="font-size:18px;color:#c060a0;flex-shrink:0">›</div>`;
      row.addEventListener(
        "mouseenter",
        () => (row.style.background = "#f9d8f0"),
      );
      row.addEventListener(
        "mouseleave",
        () => (row.style.background = "#fdf0f8"),
      );
      row.addEventListener("click", () => {
        fbDoSelect(fx?.desc || code, fx?.watts || null);
        closeRecentPopover();
      });
      list.appendChild(row);
    });
  }

  document.getElementById("recentModal").style.display = "block";
}

function closeRecentPopover() {
  document.getElementById("recentModal").style.display = "none";
}

// Keep renderRecentBtns as no-op (was inline buttons, now a popover)
function renderRecentBtns() {}

// ── ROOM DIMENSIONS POPOVER ──────────────────────────────────────────────
let dimLen = "",
  dimWid = "",
  dimActive = "len";

function openDimensionsPopover() {
  dimLen = state.dimensions?.len || "";
  dimWid = state.dimensions?.wid || "";
  dimActive = "len";
  dimRender();
  document.getElementById("dimensionsModal").style.display = "block";
}

function closeDimensionsPopover() {
  document.getElementById("dimensionsModal").style.display = "none";
}

function dimSetActive(field) {
  dimActive = field;
  dimRender();
}

function dimSwitchField() {
  dimActive = dimActive === "len" ? "wid" : "len";
  dimRender();
}

function dimKey(k) {
  let val = dimActive === "len" ? dimLen : dimWid;
  if (k === "⌫") {
    val = val.slice(0, -1);
  } else if (k === "." && val.includes(".")) {
    return;
  } else {
    val += k;
  }
  if (dimActive === "len") dimLen = val;
  else dimWid = val;
  dimRender();
  // Auto-advance to width after entering length if switching
  if (dimActive === "len" && val.length >= 1) {
    /* keep in len until user taps X */
  }
}

function dimRender() {
  const lenEl = document.getElementById("dimLenDisplay");
  const widEl = document.getElementById("dimWidDisplay");
  if (lenEl) {
    lenEl.textContent = dimLen || "—";
    lenEl.style.borderColor = dimActive === "len" ? "#2a7a50" : "#c8d4e3";
    lenEl.style.background = dimActive === "len" ? "#e8f4f0" : "#f4f7fb";
  }
  if (widEl) {
    widEl.textContent = dimWid || "—";
    widEl.style.borderColor = dimActive === "wid" ? "#2a7a50" : "#c8d4e3";
    widEl.style.background = dimActive === "wid" ? "#e8f4f0" : "#f4f7fb";
  }
}

function dimConfirm() {
  if (!dimLen && !dimWid) {
    closeDimensionsPopover();
    return;
  }
  state.dimensions = { len: dimLen, wid: dimWid };
  const label =
    dimLen && dimWid
      ? `${dimLen}' × ${dimWid}'`
      : dimLen
      ? `${dimLen}'`
      : `${dimWid}'`;
  document.getElementById("dimensionsBtnLabel").textContent = label;
  document.getElementById("dimensionsBtn").classList.add("filled");
  closeDimensionsPopover();
  updateSummary();
}

// ── CEILING TYPE POPOVER ──────────────────────────────────────────────────
let defaultCeilingType = "";

function openCeilingTypePopover() {
  document.querySelectorAll(".ct-opt").forEach((b) => {
    b.classList.toggle("selected", b.textContent === state.ceilingType);
    // Mark default with star
    b.textContent = b.textContent.replace(" ★", "");
    if (defaultCeilingType && b.textContent.trim() === defaultCeilingType)
      b.textContent += " ★";
  });
  const indicator = document.getElementById("ctDefaultIndicator");
  const defLabel = document.getElementById("ctDefaultLabel");
  if (defaultCeilingType) {
    defLabel.textContent = defaultCeilingType;
    indicator.style.display = "inline";
  } else {
    indicator.style.display = "none";
  }
  document.getElementById("ceilingTypeModal").style.display = "block";
}

function closeCeilingTypePopover() {
  document.getElementById("ceilingTypeModal").style.display = "none";
}

function selectCeilingType(val) {
  state.ceilingType = val;
  updateCeilingTypeBtn(val);
  updateSummary();
  // Keep popover open so user can optionally set as default in one tap
  document.querySelectorAll(".ct-opt").forEach((b) => {
    b.classList.toggle(
      "selected",
      b.textContent.replace(" ★", "").trim() === val,
    );
  });
}

function setDefaultCeilingType() {
  if (!state.ceilingType) {
    toast("Select a ceiling type first");
    return;
  }
  defaultCeilingType = state.ceilingType;
  document.getElementById("ctDefaultLabel").textContent = defaultCeilingType;
  document.getElementById("ctDefaultIndicator").style.display = "inline";
  document.querySelectorAll(".ct-opt").forEach((b) => {
    const raw = b.textContent.replace(" ★", "").trim();
    b.textContent = raw === defaultCeilingType ? raw + " ★" : raw;
    b.classList.toggle("selected", raw === state.ceilingType);
  });
  toast(`✔ "${defaultCeilingType}" set as default ceiling type`);
}

function updateCeilingTypeBtn(val) {
  const isDefault = val === defaultCeilingType;
  document.getElementById("ceilingTypeBtnLabel").textContent =
    val + (isDefault ? " ★" : "");
  document.getElementById("ceilingTypeBtn").classList.add("filled");
}

// ── CUSTOM BUTTON HELPERS ─────────────────────────────────────────────────
function toggleCustomInput(id) {
  const el = document.getElementById(id);
  if (!el) return;
  const showing = el.style.display === "flex";
  el.style.display = showing ? "none" : "flex";
  if (!showing) {
    const input = el.querySelector("input");
    if (input) {
      input.value = "";
      // iOS requires focus to be called from a user-gesture context
      // Remove readonly briefly to trick Safari into showing keyboard
      input.blur();
      setTimeout(() => {
        input.focus();
      }, 150);
    }
  }
}

// Sensor switch buttons act as both switch AND existing control — same name in both
function sensorSwitchSelected(btn, switchVal) {
  // --- SWITCH side: increment if exists, else add at qty 1 ---
  const existingSw = state.switchType.find((s) => s.name === switchVal);
  if (existingSw) {
    existingSw.qty++;
  } else {
    state.switchType.push({ name: switchVal, qty: 1 });
    btn.classList.add("active");
  }
  renderSwitchQtyPanel();

  // --- EXISTING CONTROLS: same name, keep qty in sync ---
  const noneBtn = document.getElementById("ecNoneBtn");
  if (noneBtn) noneBtn.classList.remove("active");
  state.existingCtrls = state.existingCtrls.filter(
    (x) => (x.name || x) !== "None",
  );

  const swEntry = state.switchType.find((s) => s.name === switchVal);
  const ecExisting = state.existingCtrls.find(
    (x) => (x.name || x) === switchVal,
  );
  if (ecExisting) {
    ecExisting.qty = swEntry ? swEntry.qty : (ecExisting.qty || 1) + 1;
  } else {
    state.existingCtrls.push({ name: switchVal, qty: 1 });
  }
  renderExistingCtrlQtyPanel();
  updateSummary();
}

// ── SWITCH BUILDER MODAL ──────────────────────────────────────────────────
const SW_SUBS = {
  dimmer: {
    title: "Dimmer",
    kind: "qty",
    options: [
      { label: "TRIAC", resolved: "Dimmer (TRIAC)" },
      { label: "0-10V", resolved: "Dimmer (0-10V)" },
      { label: "Slide Dimmer (TRIAC)", resolved: "Slide Dimmer (TRIAC)" },
      { label: "Slide Dimmer (0-10V)", resolved: "Slide Dimmer (0-10V)" },
    ],
  },
  sensor: {
    title: "Sensor Switch",
    kind: "sensor",
    options: [
      { label: "Occ", resolved: "Sensor Switch (Occ)" },
      { label: "Occ-DT", resolved: "Sensor Switch (Occ-DT)" },
      { label: "Vac", resolved: "Sensor Switch (Vac)" },
      { label: "Vac-DT", resolved: "Sensor Switch (Vac-DT)" },
      {
        label: "Sensor + 0-10V DIM",
        resolved: "Sensor Switch (Sensor + 0-10V DIM)",
      },
      { label: "Occ + 0-10V DIM", resolved: "Sensor Switch (Occ + 0-10V DIM)" },
      { label: "Vac + 0-10V DIM", resolved: "Sensor Switch (Vac + 0-10V DIM)" },
    ],
  },
  lv: {
    title: "Low Voltage",
    kind: "qty",
    options: [
      { label: "Momentary", resolved: "Momentary LV Switch" },
      { label: "Dimmer", resolved: "Dimmer (Low-Voltage)" },
      { label: "Sensor + 0-10V DIM", resolved: "LV Sensor/Dimmer" },
    ],
  },
  more: {
    title: "More Switch Types",
    kind: "qty",
    options: [
      { label: "1P/Socket", resolved: "1P/Socket" },
      { label: "1P/SocketX2", resolved: "1P/SocketX2" },
      { label: "1P + Fan", resolved: "1P + Fan" },
      { label: "Panel", resolved: "Panel" },
      { label: "Lutron Wireless Switch", resolved: "Lutron Wireless Switch" },
    ],
  },
};

function openSwitchBuilder() {
  document.getElementById("switchBuilderModal").style.display = "block";
  swBuilderShowMain();
}

function closeSwitchBuilder() {
  document.getElementById("switchBuilderModal").style.display = "none";
  document.getElementById("swBuilderCustom").style.display = "none";
}

function swBuilderShowMain() {
  document.getElementById("swBuilderMain").style.display = "grid";
  document.getElementById("swBuilderSub").style.display = "none";
  document.getElementById("swBackBtn").style.display = "none";
  document.getElementById("swBuilderTitle").textContent = "Select Switch";
}

function swBuilderShowSub(category) {
  const sub = SW_SUBS[category];
  if (!sub) return;
  const wrap = document.getElementById("swBuilderSub");
  wrap.innerHTML = sub.options
    .map((o) => {
      const safeCat = category.replace(/'/g, "\\'");
      const safeRes = o.resolved.replace(/'/g, "\\'");
      return `<button class="sw-pill sw-plate" onclick="swBuilderPickFromSub('${safeCat}','${safeRes}',this)">${o.label}</button>`;
    })
    .join("");
  document.getElementById("swBuilderMain").style.display = "none";
  wrap.style.display = "grid";
  document.getElementById("swBackBtn").style.display = "inline-block";
  document.getElementById("swBuilderTitle").textContent = sub.title;
}

function swBuilderPickPlate(name) {
  addPlateSw(name);
  toast(`✔ ${name} added`);
}

function swBuilderPickQty(name) {
  if (name === "None") {
    state.switchType = state.switchType.filter((s) => s.name !== "None");
    state.switchType.push({ name: "None", qty: 1 });
  } else {
    const ex = state.switchType.find((s) => s.name === name);
    if (ex) ex.qty = (ex.qty || 1) + 1;
    else state.switchType.push({ name, qty: 1 });
  }
  renderSwitchQtyPanel();
  updateSummary();
  toast(`✔ ${name} added`);
}

function swBuilderPickFromSub(category, resolvedName, btn) {
  const sub = SW_SUBS[category];
  if (!sub) return;
  if (sub.kind === "sensor") sensorSwitchSelected(btn, resolvedName);
  else swBuilderPickQty(resolvedName);
}

function swBuilderToggleCustom() {
  const wrap = document.getElementById("swBuilderCustom");
  const open = wrap.style.display !== "none";
  wrap.style.display = open ? "none" : "block";
  if (!open) {
    const inp = document.getElementById("swBuilderCustomInput");
    inp.value = "";
    setTimeout(() => inp.focus(), 50);
  }
}

function swBuilderSaveCustom() {
  const input = document.getElementById("swBuilderCustomInput");
  const val = input?.value.trim();
  if (!val) return;
  const ex = state.switchType.find((s) => s.name === val);
  if (ex) ex.qty = (ex.qty || 1) + 1;
  else state.switchType.push({ name: val, qty: 1 });
  renderSwitchQtyPanel();
  updateSummary();
  input.value = "";
  document.getElementById("swBuilderCustom").style.display = "none";
  toast(`✔ "${val}" added`);
}

function toggleSection(id) {
  const body = document.getElementById(id);
  if (!body) return;
  const open = body.style.display !== "none";
  body.style.display = open ? "none" : "block";
  const chevron = document.getElementById(id + "Chevron");
  if (chevron) chevron.textContent = open ? "▸" : "▾";
}

function updateSectionSummaries() {
  // Collapsible section header summaries removed in the redesign
  // (switch + controls live in popovers; selections show in the unified review area).
  // Function kept as a no-op so existing callers stay wired.
}

function toggleSwitchNote(note) {
  if (!state.switchNotes) state.switchNotes = [];
  const idx = state.switchNotes.indexOf(note);
  if (idx === -1) state.switchNotes.push(note);
  else state.switchNotes.splice(idx, 1);
  syncSwitchNoteButtons();
  updateSummary();
}

function syncSwitchNoteButtons() {
  const notes = state.switchNotes || [];
  const ab = document.getElementById("swNoteAB");
  const w3 = document.getElementById("swNote3Way");
  if (ab) {
    const on = notes.includes("A/B Switching");
    ab.style.background = on ? "#6a3aaa" : "#f7f0ff";
    ab.style.color = on ? "#fff" : "#6a3aaa";
    ab.style.borderColor = on ? "#6a3aaa" : "#d4b8f0";
  }
  if (w3) {
    const on = notes.includes("3-Way");
    w3.style.background = on ? "#6a3aaa" : "#f7f0ff";
    w3.style.color = on ? "#fff" : "#6a3aaa";
    w3.style.borderColor = on ? "#6a3aaa" : "#d4b8f0";
  }
}

// ── EXISTING CONTROLS BUILDER ─────────────────────────────────────────────
// Drill-down picker: 7 top-level types → sub-options → resolved label.
// Resolved labels are the canonical names we store in state.existingCtrls.
const CTRL_SUBS = {
  ceiling: {
    title: "Ceiling Sensor",
    options: [
      { label: "Low Voltage", resolved: "Ceiling Sensor (Low-Voltage)" },
      { label: "Line Voltage", resolved: "Ceiling Sensor (120V/277V)" },
      { label: "Wireless", resolved: "Ceiling Sensor (Wireless)" },
    ],
  },
  wall: {
    title: "Wall Sensor",
    options: [
      { label: "Low Voltage", resolved: "Wall Sensor (Low-Voltage)" },
      { label: "Line Voltage", resolved: "Wall Sensor (120V/277V)" },
      { label: "Wireless", resolved: "Wall Sensor (Wireless)" },
    ],
  },
  wide: {
    title: "Corner / Wide View",
    options: [
      { label: "Low Voltage", resolved: "Wide-View Sensor (Low-Voltage)" },
      { label: "Line Voltage", resolved: "Wide-View Sensor (120V/277V)" },
      { label: "Wireless", resolved: "Wide-View Sensor (Wireless)" },
    ],
  },
  side: {
    title: "Side-Mount",
    options: [
      { label: "Low Voltage", resolved: "Side-Mount Sensor (Low-Voltage)" },
      { label: "Line Voltage", resolved: "Side-Mount Sensor (120V/277V)" },
    ],
  },
  occphoto: {
    title: "Occ / Photocell",
    options: [
      { label: "Low Voltage", resolved: "Ceiling Occ/Photocell (Low-Voltage)" },
      { label: "Line Voltage", resolved: "Ceiling Occ/Photocell (120V/277V)" },
      { label: "Wireless", resolved: "Ceiling Occ/Photocell (Wireless)" },
    ],
  },
  powerpack: {
    title: "Power Pack",
    options: [
      { label: "Low Voltage", resolved: "Power Pack (Low-Voltage)" },
      { label: "Wireless", resolved: "Power Pack (Wireless)" },
    ],
  },
  time: {
    title: "Time-Based Control",
    options: [
      { label: "Digital", resolved: "Digital Time Clock" },
      { label: "Astronomical", resolved: "Astro. Time Clock" },
      { label: "Manual Timer", resolved: "Manual Timer" },
    ],
  },
};

// ── CATALOGUE ─────────────────────────────────────────────────────────────
// Session-built fixture quick-picks. Persists across refresh within a building
// (localStorage), capped at 10. More reliable than the auto-trimmed "Recently Used"
// list because items only leave when the user explicitly swipes-to-remove.
// Scoped to the current building name — switching buildings clears the catalogue.
const CATALOGUE_KEY = "auditTool_catalogue_v1";
const CATALOGUE_MAX = 10;
let catalogueItems = []; // [{desc, watts, isExt, isNew, trail}]
let catalogueBuilding = ""; // building name this catalogue belongs to

function loadCatalogue() {
  try {
    const raw = localStorage.getItem(CATALOGUE_KEY);
    if (raw) {
      const parsed = JSON.parse(raw) || {};
      catalogueItems = Array.isArray(parsed.items) ? parsed.items : [];
      catalogueBuilding = parsed.building || "";
    }
  } catch (e) {
    catalogueItems = [];
    catalogueBuilding = "";
  }
}
function saveCatalogue() {
  try {
    localStorage.setItem(
      CATALOGUE_KEY,
      JSON.stringify({
        building: catalogueBuilding,
        items: catalogueItems,
      }),
    );
  } catch (e) {}
}

// Catalogue is scoped to a building. If the current building doesn't match the
// stored one, wipe — user said it's not meant to carry across projects.
function ensureCatalogueScope() {
  const currentBldg =
    typeof bldgData !== "undefined" && bldgData.name ? bldgData.name : "";
  if (catalogueBuilding !== currentBldg) {
    catalogueItems = [];
    catalogueBuilding = currentBldg;
    saveCatalogue();
  }
}

function openCatalogue() {
  ensureCatalogueScope();
  openCatalogueView();
}

function openCatalogueView() {
  renderCatalogueItems();
  document.getElementById("catalogueModal").style.display = "block";
}

function closeCatalogueModal() {
  document.getElementById("catalogueModal").style.display = "none";
}

// item: { code?, desc, watts, isExt, isNew, trail?, attrs?, mountType? }
function addToCatalogue(item) {
  // Dedupe on the full identity (desc + attrs + mount) so two configurations of
  // the same fixture can coexist.
  const sig = catalogueSignature(item);
  if (catalogueItems.some((c) => catalogueSignature(c) === sig)) {
    toast("Already in catalogue");
    return false;
  }
  if (catalogueItems.length >= CATALOGUE_MAX) {
    toast(`Catalogue full (${CATALOGUE_MAX} max)`);
    return false;
  }
  catalogueItems.push(item);
  saveCatalogue();
  return true;
}

function catalogueSignature(item) {
  const a = item.attrs || {};
  const parts = [
    item.desc,
    a.intSensor ? 1 : 0,
    a.embb ? 1 : 0,
    a.airReturn ? 1 : 0,
    a.wireGuard ? 1 : 0,
    a.volt480 ? 1 : 0,
    a.emGen ? 1 : 0,
    a.photocell ? 1 : 0,
    a.twistlockPC ? 1 : 0,
    a.wetLocation ? 1 : 0,
    a.darkSky ? 1 : 0,
    a.optic || "",
    item.mountType || "",
  ];
  return parts.join("|");
}

function removeFromCatalogue(idx) {
  if (idx < 0 || idx >= catalogueItems.length) return;
  catalogueItems.splice(idx, 1);
  saveCatalogue();
  if (!catalogueItems.length) {
    closeCatalogueModal();
    toast("Catalogue cleared");
  } else {
    renderCatalogueItems();
  }
}

// Tap a catalogue pill → load fixture + attrs + mount into the pending entry pills.
function selectFromCatalogue(idx) {
  const item = catalogueItems[idx];
  if (!item) return;
  const rawDesc = item.desc.startsWith("[NEW] ")
    ? item.desc.slice(6)
    : item.desc;
  const code =
    item.code ||
    "FB:" +
      (item.isNew ? "NEW" : "") +
      rawDesc
        .substring(0, 30)
        .replace(/[^a-zA-Z0-9]/g, "")
        .toUpperCase();
  // Re-register in FIXTURES — required after a refresh wipes runtime FB: codes.
  FIXTURES[code] = {
    desc: item.desc,
    watts: item.isNew ? 0 : item.watts || null,
    type: item.isExt ? "Exterior" : "Interior",
    _newAddition: !!item.isNew,
  };
  state.activeFixture = code;
  // Restore attribute flags (default false when absent — wizard-only entries skip these)
  const a = item.attrs || {};
  state.fixtureFlagIntSensor = !!a.intSensor;
  state.fixtureFlagEMBB = !!a.embb;
  state.fixtureFlagAirReturn = !!a.airReturn;
  state.fixtureFlagWireGuard = !!a.wireGuard;
  state.fixtureFlag480V = !!a.volt480;
  state.fixtureFlagEMGen = !!a.emGen;
  state.fixtureFlagPhotocell = !!a.photocell;
  state.fixtureFlagTwistlockPC = !!a.twistlockPC;
  state.fixtureFlagWetLocation = !!a.wetLocation;
  state.fixtureFlagDarkSky = !!a.darkSky;
  state.fixtureOptic = a.optic || null;
  state.fixtureMountType = item.mountType || null;
  closeCatalogueModal();
  syncFixturePills();
  if (typeof syncAttributeToggles === "function") syncAttributeToggles();
  toast("Loaded from catalogue — set qty and tap Add ›");
}

// Snapshot the in-progress (Pending) configuration — fixture, attribute flags,
// optic, and mount — into the catalogue. Triggered by 📌 on the Pending row,
// before Add is tapped.
function addCurrentConfigToCatalogue() {
  if (!state.activeFixture) {
    toast("Configure a fixture first");
    return;
  }
  const base = FIXTURES[state.activeFixture];
  if (!base) {
    toast("Fixture details missing");
    return;
  }
  const attrs = {
    intSensor: !!state.fixtureFlagIntSensor,
    embb: !!state.fixtureFlagEMBB,
    airReturn: !!state.fixtureFlagAirReturn,
    wireGuard: !!state.fixtureFlagWireGuard,
    volt480: !!state.fixtureFlag480V,
    emGen: !!state.fixtureFlagEMGen,
    photocell: !!state.fixtureFlagPhotocell,
    twistlockPC: !!state.fixtureFlagTwistlockPC,
    wetLocation: !!state.fixtureFlagWetLocation,
    darkSky: !!state.fixtureFlagDarkSky,
    optic: state.fixtureOptic || null,
  };
  ensureCatalogueScope();
  const added = addToCatalogue({
    code: state.activeFixture,
    desc: base.desc || state.activeFixture,
    watts: base.watts || null,
    isExt: base.type === "Exterior",
    isNew: !!base._newAddition,
    trail: "",
    attrs,
    mountType: state.fixtureMountType || null,
  });
  if (added) toast("Saved to catalogue");
}

// Snapshot a committed-to-room fixture (already in state.fixtures) into the catalogue.
function addPendingFixtureToCatalogue(i) {
  const f = state.fixtures && state.fixtures[i];
  if (!f) return;
  const attrs = {
    intSensor: !!f.intSensor,
    embb: !!f.embb,
    airReturn: !!f.airReturn,
    wireGuard: !!f.wireGuard,
    volt480: !!f.volt480,
    emGen: !!f.emGen,
    photocell: !!f.photocell,
    twistlockPC: !!f.twistlockPC,
    wetLocation: !!f.wetLocation,
    darkSky: !!f.darkSky,
    optic: f.optic || null,
  };
  ensureCatalogueScope();
  const added = addToCatalogue({
    code: f.code,
    desc: f.desc || f.code || "",
    watts: f.watts || null,
    isExt: f.type === "Exterior",
    isNew: !!f.isNewAddition,
    trail: "",
    attrs,
    mountType: f.mountType || null,
  });
  if (added) toast("Saved to catalogue");
}

function renderCatalogueItems() {
  const list = document.getElementById("catalogueList");
  const count = document.getElementById("catalogueCount");
  list.innerHTML = "";
  count.textContent = `${catalogueItems.length} / ${CATALOGUE_MAX}`;
  if (!catalogueItems.length) {
    list.innerHTML = `<div style="padding:24px 16px;text-align:center;color:#8a6a30">
      <div style="font-size:36px;margin-bottom:8px">📌</div>
      <div style="font-size:13px;font-weight:700;margin-bottom:6px">No catalogue items yet</div>
      <div style="font-size:11px;color:#a08850;line-height:1.5">Configure a fixture, then tap the <b>📌</b> on the Pending row to save the full setup (fixture, attributes, mount) here for quick re-use this audit.</div>
    </div>`;
    return;
  }
  const esc = (s) =>
    String(s).replace(
      /[&<>"']/g,
      (c) =>
        ({
          "&": "&amp;",
          "<": "&lt;",
          ">": "&gt;",
          '"': "&quot;",
          "'": "&#39;",
        }[c]),
    );
  catalogueItems.forEach((item, idx) => {
    const wrap = document.createElement("div");
    wrap.className = "cat-item-wrap";
    const del = document.createElement("button");
    del.className = "cat-item-delete";
    del.textContent = "Delete";
    del.addEventListener("click", (e) => {
      e.stopPropagation();
      removeFromCatalogue(idx);
    });
    const row = document.createElement("div");
    row.className = "cat-item-row";
    const a = item.attrs || {};
    const flagPills = [
      a.intSensor && "⚡Int",
      a.embb && "🔋EMBB",
      a.airReturn && "💨Air",
      a.wireGuard && "🛡Guard",
      a.volt480 && "⚡480V",
      a.emGen && "🏭EM",
      a.photocell && "🌗PC",
      a.twistlockPC && "🔌Twist",
      a.wetLocation && "💧Wet",
      a.darkSky && "🌌Dark",
      a.optic && `🎯${a.optic}`,
    ]
      .filter(Boolean)
      .join(" · ");
    const mountStr = item.mountType
      ? (flagPills ? " · " : "") + esc(item.mountType)
      : "";
    const subStr = [item.trail, flagPills + mountStr]
      .filter(Boolean)
      .join(" · ");
    const sub = subStr ? `<div class="cat-trail">${subStr}</div>` : "";
    const wattsBadge = item.watts
      ? `<span class="cat-watts">${item.watts}W</span>`
      : "";
    row.innerHTML = `<div style="flex:1;min-width:0">
        <div class="cat-desc">${esc(item.desc)}</div>
        ${sub}
      </div>${wattsBadge}<span class="cat-chev">›</span>`;
    attachCatSwipe(row, idx);
    wrap.appendChild(del);
    wrap.appendChild(row);
    list.appendChild(wrap);
  });
}

// Swipe-left-to-reveal-delete (iOS Mail pattern). Each row owns its own drag state
// via the closure below; mouse-drag fallback is wired once globally further down.
let _catActiveSwipe = null; // {onMove, onEnd} of the currently-dragging row
function attachCatSwipe(row, idx) {
  let startX = 0,
    startY = 0,
    currentX = 0,
    dragging = false,
    axis = null,
    opened = false;
  const REVEAL = 88,
    OPEN_THRESHOLD = 44;

  const setX = (x, animate) => {
    if (animate) row.classList.remove("dragging");
    else row.classList.add("dragging");
    row.style.transform = `translateX(${x}px)`;
  };
  const closeRow = () => {
    opened = false;
    setX(0, true);
  };
  const openRow = () => {
    opened = true;
    setX(-REVEAL, true);
  };

  const closeOthers = () => {
    document.querySelectorAll("#catalogueList .cat-item-row").forEach((r) => {
      if (r !== row) {
        r.classList.remove("dragging");
        r.style.transform = "translateX(0)";
      }
    });
  };

  const onStart = (clientX, clientY) => {
    startX = clientX;
    startY = clientY;
    currentX = opened ? -REVEAL : 0;
    dragging = true;
    axis = null;
    _catActiveSwipe = { onMove, onEnd };
  };
  const onMove = (clientX, clientY, ev) => {
    if (!dragging) return;
    const dx = clientX - startX,
      dy = clientY - startY;
    if (!axis) {
      if (Math.abs(dx) < 6 && Math.abs(dy) < 6) return;
      axis = Math.abs(dx) > Math.abs(dy) ? "x" : "y";
      if (axis === "y") {
        dragging = false;
        _catActiveSwipe = null;
        return;
      }
    }
    if (ev && ev.cancelable) ev.preventDefault();
    let next = currentX + dx;
    if (next > 0) next = 0;
    if (next < -REVEAL) next = -REVEAL;
    setX(next, false);
  };
  const onEnd = (clientX) => {
    _catActiveSwipe = null;
    if (!dragging) return;
    dragging = false;
    if (axis !== "x") return;
    const dx = clientX - startX;
    const finalX = Math.max(-REVEAL, Math.min(0, currentX + dx));
    if (finalX < -OPEN_THRESHOLD) openRow();
    else closeRow();
  };

  row.addEventListener(
    "touchstart",
    (e) => {
      closeOthers();
      onStart(e.touches[0].clientX, e.touches[0].clientY);
    },
    { passive: true },
  );
  row.addEventListener(
    "touchmove",
    (e) => onMove(e.touches[0].clientX, e.touches[0].clientY, e),
    { passive: false },
  );
  row.addEventListener("touchend", (e) =>
    onEnd((e.changedTouches[0] || {}).clientX || startX),
  );
  row.addEventListener("touchcancel", () => {
    dragging = false;
    _catActiveSwipe = null;
    closeRow();
  });

  row.addEventListener("mousedown", (e) => {
    closeOthers();
    onStart(e.clientX, e.clientY);
  });

  row.addEventListener("click", (e) => {
    if (opened) {
      closeRow();
      e.stopPropagation();
      return;
    }
    if (axis === "x") {
      e.stopPropagation();
      return;
    }
    selectFromCatalogue(idx);
  });
}

// Mouse-drag fallback (desktop testing). Bound once — picks up whichever row is active.
window.addEventListener("mousemove", (e) => {
  if (_catActiveSwipe) _catActiveSwipe.onMove(e.clientX, e.clientY, null);
});
window.addEventListener("mouseup", (e) => {
  if (_catActiveSwipe) _catActiveSwipe.onEnd(e.clientX);
});

function openExistingCtrlsModal() {
  document.getElementById("existingCtrlsModal").style.display = "block";
  ecBuilderShowMain();
}

function closeExistingCtrlsModal() {
  document.getElementById("existingCtrlsModal").style.display = "none";
  document.getElementById("ecBuilderCustom").style.display = "none";
}

function ecBuilderShowMain() {
  document.getElementById("ecBuilderMain").style.display = "grid";
  document.getElementById("ecBuilderSub").style.display = "none";
  document.getElementById("ecBackBtn").style.display = "none";
  document.getElementById("ecBuilderTitle").textContent = "Select Controls";
}

function ecBuilderShowSub(category) {
  const sub = CTRL_SUBS[category];
  if (!sub) return;
  const wrap = document.getElementById("ecBuilderSub");
  wrap.innerHTML = sub.options
    .map((o) => {
      const safeRes = o.resolved.replace(/'/g, "\\'");
      return `<button class="ec-pill" onclick="ecBuilderPickQty('${safeRes}')">${o.label}</button>`;
    })
    .join("");
  document.getElementById("ecBuilderMain").style.display = "none";
  wrap.style.display = "grid";
  document.getElementById("ecBackBtn").style.display = "inline-block";
  document.getElementById("ecBuilderTitle").textContent = sub.title;
}

function ecBuilderPickQty(name) {
  if (!name) return;
  if (name === "None") {
    state.existingCtrls = [{ name: "None", qty: 1 }];
  } else {
    state.existingCtrls = state.existingCtrls.filter(
      (x) => (x.name || x) !== "None",
    );
    const ex = state.existingCtrls.find((x) => (x.name || x) === name);
    if (ex) ex.qty = (ex.qty || 1) + 1;
    else state.existingCtrls.push({ name, qty: 1 });
  }
  renderExistingCtrlQtyPanel();
  updateSummary();
  toast(`✔ ${name} added`);
}

function ecBuilderToggleCustom() {
  const wrap = document.getElementById("ecBuilderCustom");
  const open = wrap.style.display !== "none";
  wrap.style.display = open ? "none" : "block";
  if (!open) {
    const inp = document.getElementById("ecCustomInput");
    if (inp) {
      inp.value = "";
      setTimeout(() => inp.focus(), 50);
    }
  }
}

// No-op kept so existing callers (clearFixtures, init, addZone) don't break —
// the new builder doesn't carry transient picker state, so there's nothing
// to reset between rooms.
function ecInitType() {}

// ── SWITCH RECOMMENDATION ─────────────────────────────────────────────────
function renderSwRecQtyPanel() {
  const panel = document.getElementById("swRecQtyPanel");
  if (typeof updateSpecifyCounts === "function") updateSpecifyCounts();
  if (!panel) return;
  if (!state.swRec || !state.swRec.length) {
    panel.innerHTML = "";
    return;
  }
  panel.innerHTML = state.swRec
    .map(
      (s, i) => `
    <div style="display:flex;align-items:center;gap:6px;background:#fdecea;border:1.5px solid #f0b8b0;border-radius:8px;padding:6px 8px">
      <span style="font-size:13px;font-weight:700;color:#b03030;flex:1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;min-width:0">${s.name}</span>
      <div style="display:flex;align-items:center;gap:4px;flex-shrink:0">
        <button onclick="changeSwRecQty(${i},-1)" style="width:44px;height:44px;border-radius:6px;border:1px solid #f0b8b0;background:#fff;color:#b03030;font-size:18px;font-weight:700;cursor:pointer;display:flex;align-items:center;justify-content:center;line-height:1;padding:0">−</button>
        <span style="font-size:15px;font-weight:700;color:#b03030;min-width:24px;text-align:center">${s.qty}</span>
        <button onclick="changeSwRecQty(${i},1)" style="width:44px;height:44px;border-radius:6px;border:1px solid #f0b8b0;background:#fff;color:#b03030;font-size:18px;font-weight:700;cursor:pointer;display:flex;align-items:center;justify-content:center;line-height:1;padding:0">+</button>
      </div>
      <button onclick="removeSwRec(${i})" style="width:48px;height:48px;border-radius:6px;border:none;background:transparent;color:#c0a0a0;font-size:26px;cursor:pointer;display:flex;align-items:center;justify-content:center;flex-shrink:0;line-height:1;padding:0">×</button>
    </div>`,
    )
    .join("");
}

function changeSwRecQty(i, delta) {
  if (!state.swRec?.[i]) return;
  state.swRec[i].qty = Math.max(1, state.swRec[i].qty + delta);
  renderSwRecQtyPanel();
  renderCtrlQtyPanel();
  updateSummary();
}

function removeSwRec(i) {
  const name = state.swRec?.[i]?.name;
  state.swRec.splice(i, 1);
  if (name)
    document.querySelectorAll("#swRecBtns .btn[data-v]").forEach((b) => {
      if (b.dataset.v === name && !state.swRec.find((s) => s.name === name))
        b.classList.remove("active");
    });
  renderSwRecQtyPanel();
  renderCtrlQtyPanel();
  updateSummary();
}

function saveSwRec() {
  const input = document.getElementById("swRecCustomInput");
  const val = input?.value.trim();
  if (!val) return;
  if (!state.swRec) state.swRec = [];
  const ex = state.swRec.find((s) => s.name === val);
  if (ex) ex.qty++;
  else state.swRec.push({ name: val, qty: 1 });
  renderSwRecQtyPanel();
  updateSummary();
  toggleCustomInput("swRecCustom");
}

// ── FIXTURE RECOMMENDATION ────────────────────────────────────────────────
function renderFixRecQtyPanel() {
  const panel = document.getElementById("fixRecQtyPanel");
  if (typeof updateSpecifyCounts === "function") updateSpecifyCounts();
  if (!panel) return;
  if (!state.fixRec || !state.fixRec.length) {
    panel.innerHTML = "";
    return;
  }
  panel.innerHTML = state.fixRec
    .map(
      (s, i) => `
    <div style="display:flex;align-items:center;gap:6px;background:#e6f7f6;border:1.5px solid #a0d8bc;border-radius:8px;padding:6px 8px">
      <span style="font-size:13px;font-weight:700;color:#0a6b65;flex:1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;min-width:0">${s.name}</span>
      <div style="display:flex;align-items:center;gap:4px;flex-shrink:0">
        <button onclick="changeFixRecQty(${i},-1)" style="width:44px;height:44px;border-radius:6px;border:1px solid #a0d8bc;background:#fff;color:#0a6b65;font-size:18px;font-weight:700;cursor:pointer;display:flex;align-items:center;justify-content:center;line-height:1;padding:0">−</button>
        <span style="font-size:15px;font-weight:700;color:#0a6b65;min-width:24px;text-align:center">${s.qty}</span>
        <button onclick="changeFixRecQty(${i},1)" style="width:44px;height:44px;border-radius:6px;border:1px solid #a0d8bc;background:#fff;color:#0a6b65;font-size:18px;font-weight:700;cursor:pointer;display:flex;align-items:center;justify-content:center;line-height:1;padding:0">+</button>
      </div>
      <button onclick="removeFixRec(${i})" style="width:48px;height:48px;border-radius:6px;border:none;background:transparent;color:#a0c0b8;font-size:26px;cursor:pointer;display:flex;align-items:center;justify-content:center;flex-shrink:0;line-height:1;padding:0">×</button>
    </div>`,
    )
    .join("");
}

function changeFixRecQty(i, delta) {
  if (!state.fixRec?.[i]) return;
  state.fixRec[i].qty = Math.max(1, state.fixRec[i].qty + delta);
  renderFixRecQtyPanel();
  updateSummary();
}

function removeFixRec(i) {
  const name = state.fixRec?.[i]?.name;
  state.fixRec.splice(i, 1);
  if (name)
    document.querySelectorAll("#fixRecBtns .btn[data-v]").forEach((b) => {
      if (b.dataset.v === name && !state.fixRec.find((s) => s.name === name))
        b.classList.remove("active");
    });
  renderFixRecQtyPanel();
  updateSummary();
}

function saveFixRec() {
  const input = document.getElementById("fixRecCustomInput");
  const val = input?.value.trim();
  if (!val) return;
  if (!state.fixRec) state.fixRec = [];
  const ex = state.fixRec.find((s) => s.name === val);
  if (ex) ex.qty++;
  else state.fixRec.push({ name: val, qty: 1 });
  renderFixRecQtyPanel();
  updateSummary();
  toggleCustomInput("fixRecCustom");
}

function saveCustomExistingCtrl() {
  const input = document.getElementById("ecCustomInput");
  const val = input?.value.trim();
  if (!val) return;
  state.existingCtrls = state.existingCtrls.filter(
    (x) => (x.name || x) !== "None",
  );
  const existing = state.existingCtrls.find((x) => (x.name || x) === val);
  if (existing) {
    existing.qty = (existing.qty || 1) + 1;
  } else {
    state.existingCtrls.push({ name: val, qty: 1 });
  }
  renderExistingCtrlQtyPanel();
  updateSummary();
  if (input) input.value = "";
  document.getElementById("ecBuilderCustom").style.display = "none";
  toast(`✔ "${val}" added`);
}

function saveCustomCtrl() {
  const input = document.getElementById("ctrlCustomInput");
  const val = input?.value.trim();
  if (!val) return;
  state.ctrls = state.ctrls.filter((x) => x.name !== "None");
  const existing = state.ctrls.find((x) => x.name === val);
  if (existing) {
    existing.qty++;
  } else {
    state.ctrls.push({ name: val, qty: 1 });
  }
  renderCtrlQtyPanel();
  updateSummary();
  toggleCustomInput("ctrlCustom");
  toast(`✔ "${val}" added`);
}

// ── ROOM TEMPLATES ────────────────────────────────────────────────────────
let roomTemplates = [];

function openSaveTemplate() {
  if (state.fixtures.length === 0 && !state.height && !state.roomtype) {
    toast("Enter at least a fixture, height, or room type first");
    return;
  }
  const modal = document.getElementById("templateModal");
  modal.style.display = "flex";
  const input = document.getElementById("templateNameInput");
  input.value = "";
  // blur/refocus cycle kills iOS autofill toolbar
  input.blur();
  setTimeout(() => {
    input.focus();
    // Second focus after delay ensures full keyboard not compact autofill bar
    setTimeout(() => {
      if (document.activeElement !== input) input.focus();
    }, 200);
  }, 100);
}

function closeTemplateModal() {
  document.getElementById("templateModal").style.display = "none";
}

function saveRoomTemplate() {
  const input = document.getElementById("templateNameInput");
  const name = input.value.trim();
  if (!name) {
    toast("Enter a template name");
    input.focus();
    return;
  }
  if (roomTemplates.find((t) => t.name === name)) {
    toast("Template name already exists");
    return;
  }

  const snapshot = {
    height: state.height,
    roomtype: state.roomtype,
    ceilingType: state.ceilingType,
    dimensions: state.dimensions ? { ...state.dimensions } : null,
    fixtures: state.fixtures.map((f) => ({ ...f })),
    switchType: state.switchType.map((s) => ({ ...s })),
    existingCtrls: state.existingCtrls.map((c) => ({ ...c })),
    ctrls: state.ctrls.map((c) => ({ ...c })),
    swRec: (state.swRec || []).map((s) => ({ ...s })),
    fixRec: (state.fixRec || []).map((s) => ({ ...s })),
  };

  roomTemplates.push({ name, snapshot });
  saveToStorage();
  closeTemplateModal();
  renderTemplatePills();
  toast(`✔ Template "${name}" saved`);
}

function renderTemplatePills() {
  const wrap = document.getElementById("templatePillsWrap");
  const pills = document.getElementById("templatePills");
  if (!wrap || !pills) return;
  if (!roomTemplates.length) {
    wrap.style.display = "none";
    return;
  }
  wrap.style.display = "block";
  pills.innerHTML = roomTemplates
    .map(
      (t, i) => `
    <div style="display:inline-flex;align-items:center;gap:0;background:#f4f0fb;border:1.5px solid #c8b8e8;border-radius:20px;overflow:hidden">
      <button onclick="applyRoomTemplate(${i})" style="padding:7px 13px;background:transparent;border:none;color:#6a2aaa;font-size:12px;font-weight:600;cursor:pointer;font-family:inherit;white-space:nowrap">${t.name}</button>
      <button onclick="deleteRoomTemplate(${i})" style="padding:7px 8px 7px 0;background:transparent;border:none;color:#b090d0;font-size:14px;cursor:pointer;line-height:1">×</button>
    </div>`,
    )
    .join("");
}

function applyRoomTemplate(i) {
  const t = roomTemplates[i];
  if (!t) return;
  const s = t.snapshot;

  // Preserve the room context before clearFixtures wipes it. This matters when
  // the user navigated in via a minimap pin/polygon — clearFixtures resets
  // state.room + the input, which makes the `if (!state.room)` guard below
  // think we're applying a template to a blank slate.
  const preservedRoom = state.room;

  clearFixtures();

  if (preservedRoom) {
    state.room = preservedRoom;
    document.getElementById("roomInput").value = preservedRoom;
    updateRoomDisplay();
  }

  if (s.height) {
    state.height = s.height;
    updateHeightBtn(s.height);
  }
  if (s.roomtype) {
    state.roomtype = s.roomtype;
    const rtOpts = {
      "Class rooms": "Class",
      Office: "Office",
      Restroom: "Restroom",
      Closet: "Closet",
      Storage: "Storage",
      Hallway: "Hall",
      Stairwell: "Stair",
      "Slop Sink": "Slop Sink",
      Gym: "Gym",
      Mechanical: "Mech",
      Cafeteria: "Cafe",
      Library: "Library",
    };
    const lbl = document.getElementById("roomTypeBtnLabel");
    if (lbl) {
      lbl.textContent = rtOpts[s.roomtype] || s.roomtype;
      lbl.style.color = "#1a2a3a";
    }
    const btn = document.getElementById("roomTypeBtn");
    if (btn) {
      btn.style.borderColor = "#7a3abd";
      btn.style.background = "#f3eeff";
    }
  }
  if (s.ceilingType) {
    state.ceilingType = s.ceilingType;
    updateCeilingTypeBtn(s.ceilingType);
  }
  if (s.dimensions) {
    state.dimensions = { ...s.dimensions };
    const label = `${s.dimensions.len || "?"}' × ${s.dimensions.wid || "?"}'`;
    const dl = document.getElementById("dimensionsBtnLabel");
    if (dl) {
      dl.textContent = label;
      dl.style.color = "#1a3a5c";
    }
    const db = document.getElementById("dimensionsBtn");
    if (db) {
      db.style.borderColor = "#2a7a50";
      db.style.background = "#e8f4f0";
    }
  }

  s.fixtures.forEach((f) => {
    if (!FIXTURES[f.code])
      FIXTURES[f.code] = {
        desc: f.desc || "",
        watts: f.watts,
        type: f.type || "Interior",
      };
    // Re-lookup correct description from MATCH_DATA (same as addFixture)
    const baseCode = f.code.replace(/-(hall|dim|stair|H)$/, "");
    const matchEntry = MATCH_DATA.find((m) => m.id === baseCode);
    const correctDesc = baseCode.startsWith("FB:")
      ? FIXTURES[baseCode]?.desc || f.desc || ""
      : matchEntry?.existing || FIXTURES[baseCode]?.desc || f.desc || "";
    state.fixtures.push({ ...f, desc: correctDesc });
    fixtureFreq[baseCode] = (fixtureFreq[baseCode] || 0) + f.qty;
    if (!recentFixtures.includes(f.code)) {
      recentFixtures.push(f.code);
      if (recentFixtures.length > 10) recentFixtures.shift();
    }
  });

  state.switchType = s.switchType.map((x) => ({ ...x }));
  state.existingCtrls = s.existingCtrls.map((x) => ({ ...x }));
  state.ctrls = s.ctrls.map((x) => ({ ...x }));
  state.swRec = (s.swRec || []).map((x) => ({ ...x }));
  state.fixRec = (s.fixRec || []).map((x) => ({ ...x }));

  document.querySelectorAll('.btn[data-g="switch"]').forEach((b) => {
    if (b.dataset.v)
      b.classList.toggle(
        "active",
        state.switchType.some((sw) => sw.name === b.dataset.v),
      );
  });
  document.querySelectorAll("#existingCtrlBtns .btn").forEach((b) => {
    if (b.dataset.v)
      b.classList.toggle(
        "active",
        state.existingCtrls.some((ec) => (ec.name || ec) === b.dataset.v),
      );
  });
  document.querySelectorAll("#ctrlBtns .btn").forEach((b) => {
    if (b.dataset.v)
      b.classList.toggle(
        "active",
        state.ctrls.some((c) => c.name === b.dataset.v),
      );
  });

  rebuildFreqGrid();
  renderRecentBtns();
  renderChips();
  renderSwitchQtyPanel();
  renderExistingCtrlQtyPanel();
  renderCtrlQtyPanel();
  if (typeof renderSwRecQtyPanel === "function") renderSwRecQtyPanel();
  if (typeof renderFixRecQtyPanel === "function") renderFixRecQtyPanel();
  document.querySelectorAll('#swRecBtns .btn[data-g="swrec"]').forEach((b) => {
    if (b.dataset.v)
      b.classList.toggle(
        "active",
        state.swRec.some((s) => s.name === b.dataset.v),
      );
  });
  document
    .querySelectorAll('#fixRecBtns .btn[data-g="fixrec"]')
    .forEach((b) => {
      if (b.dataset.v)
        b.classList.toggle(
          "active",
          state.fixRec.some((s) => s.name === b.dataset.v),
        );
    });
  syncSwitchNoteButtons();

  const grid = document.getElementById("fixtureBtns");
  if (grid && s.fixtures.length) {
    grid.innerHTML =
      '<span style="color:#8a9ab0;font-size:11px;font-style:italic;padding:4px 0">Template loaded — enter room name and log</span>';
    grid.style.alignItems = "";
  }

  // Only clear room name if one isn't already set (e.g. came from a pin)
  if (!state.room) {
    document.getElementById("roomInput").value = "";
    updateRoomDisplay();
    updateSummary();
    toast(`📋 "${t.name}" loaded — enter room name`);
    setTimeout(() => {
      const ri = document.getElementById("roomInput");
      ri.focus();
      ri.style.borderColor = "#7a3abd";
      setTimeout(() => (ri.style.borderColor = ""), 2500);
    }, 300);
  } else {
    updateSummary();
    toast(`📋 "${t.name}" loaded for "${state.room}"`);
  }
}

function deleteRoomTemplate(i) {
  const name = roomTemplates[i]?.name;
  roomTemplates.splice(i, 1);
  saveToStorage();
  renderTemplatePills();
  if (name) toast(`🗑 "${name}" deleted`);
}

// ── CEILING HEIGHT POPOVER ────────────────────────────────────────────────
let defaultHeight = "";

function openHeightPopover() {
  document.querySelectorAll(".ht-opt").forEach((b) => {
    b.classList.toggle("selected", b.dataset.v === String(state.height));
    // Mark the default with a star indicator
    if (defaultHeight && b.dataset.v === String(defaultHeight)) {
      b.innerHTML = b.dataset.v + "' ★";
    } else {
      b.innerHTML = b.dataset.v + "'";
    }
  });
  // Show default indicator in header
  const indicator = document.getElementById("htDefaultIndicator");
  const defLabel = document.getElementById("htDefaultLabel");
  if (defaultHeight) {
    defLabel.textContent = defaultHeight + "'";
    indicator.style.display = "inline";
  } else {
    indicator.style.display = "none";
  }
  // Clear any stale custom-height entry from a previous open
  const ch = document.getElementById("customHeightInput");
  if (ch) ch.value = "";
  document.getElementById("heightModal").style.display = "block";
}

// Write-in mount height for values not in the preset grid. Applies the typed
// value then closes; if the field is empty, just closes (a preset was already
// picked inline via selectHeight).
function applyCustomHeight() {
  const inp = document.getElementById("customHeightInput");
  const raw = ((inp && inp.value) || "").trim();
  if (raw) {
    const num = parseFloat(raw);
    if (isNaN(num) || num <= 0) {
      toast("Enter a valid height in feet");
      return;
    }
    selectHeight(String(num));
  }
  closeHeightPopover();
}

function closeHeightPopover() {
  document.getElementById("heightModal").style.display = "none";
}

function selectHeight(val) {
  state.height = val;
  updateHeightBtn(val);
  // Stay in popover — update button states inline
  document.querySelectorAll(".ht-opt").forEach((b) => {
    const isThis = b.dataset.v === String(val);
    const isDef = defaultHeight && b.dataset.v === String(defaultHeight);
    b.classList.toggle("selected", isThis);
    b.innerHTML = b.dataset.v + "'" + (isDef ? " ★" : "");
  });
  updateSummary();
}

function setDefaultHeight() {
  // Toggle: if currently selected height is already the default, clear it
  if (!state.height) {
    toast("Select a height first, then set as default");
    return;
  }
  const defBtn = document.getElementById("htSetDefaultBtn");
  if (String(defaultHeight) === String(state.height)) {
    // Clear the default
    defaultHeight = "";
    document.getElementById("htDefaultIndicator").style.display = "none";
    document.querySelectorAll(".ht-opt").forEach((b) => {
      b.innerHTML = b.dataset.v + "'";
    });
    updateHeightBtn(state.height);
    if (defBtn) defBtn.textContent = "Set Default";
    toast("Default height cleared");
  } else {
    // Set new default
    defaultHeight = state.height;
    const defLabel = document.getElementById("htDefaultLabel");
    if (defLabel) defLabel.textContent = defaultHeight + "'";
    document.getElementById("htDefaultIndicator").style.display = "inline";
    document.querySelectorAll(".ht-opt").forEach((b) => {
      b.innerHTML =
        b.dataset.v === String(defaultHeight)
          ? b.dataset.v + "' ★"
          : b.dataset.v + "'";
    });
    updateHeightBtn(state.height);
    if (defBtn) defBtn.textContent = "Clear Default";
    toast(`✔ ${defaultHeight}' set as default height`);
  }
}

function updateHeightBtn(val) {
  const isDefault = val === String(defaultHeight);
  document.getElementById("heightBtnLabel").textContent =
    val + "'" + (isDefault ? " ★" : "");
  document.getElementById("heightBtn").classList.add("filled");
}

function renderSwitchQtyPanel() {
  renderReviewArea();
}

// ── FIXTURE MOUNT TYPE ────────────────────────────────────────────────────
// Active fixture's type ('Interior' | 'Exterior') decides which mount/attribute
// options the user sees. Falls back to Interior when no fixture is selected.
function _isActiveFixtureExterior() {
  const code = state.activeFixture;
  return !!(code && FIXTURES[code] && FIXTURES[code].type === "Exterior");
}

function openMountPopover() {
  const isExt = _isActiveFixtureExterior();
  const intDiv = document.getElementById("mountInteriorOptions");
  const extDiv = document.getElementById("mountExteriorOptions");
  if (intDiv) intDiv.style.display = isExt ? "none" : "grid";
  if (extDiv) extDiv.style.display = isExt ? "grid" : "none";
  document.querySelectorAll(".mount-opt").forEach((b) => {
    b.classList.toggle(
      "active",
      b.textContent.trim() === (state.fixtureMountType || ""),
    );
  });
  document.getElementById("mountModal").style.display = "block";
}

function closeMountPopover() {
  document.getElementById("mountModal").style.display = "none";
}

function selectMount(type) {
  state.fixtureMountType = type;
  syncFixtureFlagBtns();
  closeMountPopover();
}

function selectOptic(name) {
  state.fixtureOptic = name;
  document.querySelectorAll(".ext-optic-btn").forEach((b) => {
    b.classList.toggle("active", b.textContent.trim() === (name || ""));
  });
  syncFixturePills();
}

function toggleFixtureFlag(flag) {
  if (flag === "intSensor") {
    state.fixtureFlagIntSensor = !state.fixtureFlagIntSensor;
  } else if (flag === "embb") {
    state.fixtureFlagEMBB = !state.fixtureFlagEMBB;
  } else if (flag === "airReturn") {
    state.fixtureFlagAirReturn = !state.fixtureFlagAirReturn;
  } else if (flag === "wireGuard") {
    state.fixtureFlagWireGuard = !state.fixtureFlagWireGuard;
  } else if (flag === "volt480") {
    state.fixtureFlag480V = !state.fixtureFlag480V;
  } else if (flag === "emGen") {
    state.fixtureFlagEMGen = !state.fixtureFlagEMGen;
  } else if (flag === "photocell") {
    state.fixtureFlagPhotocell = !state.fixtureFlagPhotocell;
  } else if (flag === "twistlockPC") {
    state.fixtureFlagTwistlockPC = !state.fixtureFlagTwistlockPC;
  } else if (flag === "wetLocation") {
    state.fixtureFlagWetLocation = !state.fixtureFlagWetLocation;
  } else if (flag === "darkSky") {
    state.fixtureFlagDarkSky = !state.fixtureFlagDarkSky;
  }
  syncFixtureFlagBtns();
}

// ── WIRING DETAILS POPOVER (room-level: 3-Way / A/B Switching / No Neutral) ─
function openWiringDetails() {
  syncWiringToggles();
  document.getElementById("wiringDetailsModal").style.display = "block";
}
function closeWiringDetails() {
  document.getElementById("wiringDetailsModal").style.display = "none";
}
function syncWiringToggles() {
  const notes = state.switchNotes || [];
  document
    .querySelectorAll("#wiringDetailsModal .wiring-toggle")
    .forEach((b) => {
      b.classList.toggle("on", notes.includes(b.dataset.note));
    });
}

// ── FIXTURE ATTRIBUTE POPOVER ─────────────────────────────────────────────
function openAttributeModal() {
  const isExt = _isActiveFixtureExterior();
  const intSec = document.getElementById("attrInteriorSection");
  const extSec = document.getElementById("attrExteriorSection");
  if (intSec) intSec.style.display = isExt ? "none" : "flex";
  if (extSec) extSec.style.display = isExt ? "flex" : "none";
  syncAttributeToggles();
  document.getElementById("attributeModal").style.display = "block";
}
function closeAttributeModal() {
  document.getElementById("attributeModal").style.display = "none";
  syncFixturePills();
}
function syncAttributeToggles() {
  // Interior + shared flag IDs
  const map = [
    ["attrIntSensor", state.fixtureFlagIntSensor],
    ["attrEMBB", state.fixtureFlagEMBB],
    ["attrAirReturn", state.fixtureFlagAirReturn],
    ["attrWireGuard", state.fixtureFlagWireGuard],
    ["attr480V", state.fixtureFlag480V],
    ["attrEMGen", state.fixtureFlagEMGen],
    // Exterior section duplicates the same flags under different IDs so each
    // section can be styled independently — keep both in sync.
    ["attrIntSensorExt", state.fixtureFlagIntSensor],
    ["attrEMBBExt", state.fixtureFlagEMBB],
    ["attrWireGuardExt", state.fixtureFlagWireGuard],
    ["attr480VExt", state.fixtureFlag480V],
    // Exterior-only flags
    ["attrPhotocell", state.fixtureFlagPhotocell],
    ["attrTwistlockPC", state.fixtureFlagTwistlockPC],
    ["attrWetLocation", state.fixtureFlagWetLocation],
    ["attrDarkSky", state.fixtureFlagDarkSky],
  ];
  map.forEach(([id, on]) => {
    const el = document.getElementById(id);
    if (el) el.classList.toggle("on", !!on);
  });
  // Sync optic single-select pills
  document.querySelectorAll(".ext-optic-btn").forEach((b) => {
    b.classList.toggle(
      "active",
      b.textContent.trim() === (state.fixtureOptic || ""),
    );
  });
}

// ── FIXTURE ENTRY PILLS ───────────────────────────────────────────────────
// Pills stay as static labels (Fixture / Attribute / Mounting) — the full in-progress
// fixture description renders in the "Building" group at the top of the review area.
function syncFixturePills() {
  const fixBtn = document.getElementById("fixturePill");
  if (fixBtn) fixBtn.classList.toggle("filled", !!state.activeFixture);
  const attBtn = document.getElementById("attributePill");
  if (attBtn) {
    const anyFlag =
      state.fixtureFlagIntSensor ||
      state.fixtureFlagEMBB ||
      state.fixtureFlagAirReturn ||
      state.fixtureFlagWireGuard ||
      state.fixtureFlag480V ||
      state.fixtureFlagEMGen ||
      state.fixtureFlagPhotocell ||
      state.fixtureFlagTwistlockPC ||
      state.fixtureFlagWetLocation ||
      state.fixtureFlagDarkSky ||
      !!state.fixtureOptic;
    attBtn.classList.toggle("filled", !!anyFlag);
  }
  const mtBtn = document.getElementById("mountPill");
  if (mtBtn) mtBtn.classList.toggle("filled", !!state.fixtureMountType);
  if (typeof renderReviewArea === "function") renderReviewArea();
}

function cancelPendingFixture() {
  state.activeFixture = "";
  state.fixtureFlagIntSensor = false;
  state.fixtureFlagEMBB = false;
  state.fixtureFlagAirReturn = false;
  state.fixtureFlagWireGuard = false;
  state.fixtureFlag480V = false;
  state.fixtureFlagEMGen = false;
  state.fixtureFlagPhotocell = false;
  state.fixtureFlagTwistlockPC = false;
  state.fixtureFlagWetLocation = false;
  state.fixtureFlagDarkSky = false;
  state.fixtureOptic = null;
  state.fixtureMountType = null;
  syncFixturePills();
  syncAttributeToggles();
}

// Backwards-compatible alias — many existing call sites use this name.
function syncFixtureFlagBtns() {
  syncFixturePills();
  syncAttributeToggles();
}

function changeSwitchQty(i, delta) {
  if (!state.switchType[i]) return;
  const sw = state.switchType[i];
  sw.qty = Math.max(1, sw.qty + delta);
  // Keep matching existing control in sync if sensor switch
  if (SENSOR_SWITCH_VALS.has(sw.name)) {
    const ec = state.existingCtrls.find((x) => (x.name || x) === sw.name);
    if (ec) ec.qty = sw.qty;
    renderExistingCtrlQtyPanel();
  }
  renderSwitchQtyPanel();
  updateSummary();
}

const SENSOR_SWITCH_VALS = new Set([
  "Sensor Switch (Occ)",
  "Sensor Switch (Vac)",
  "Sensor Switch (Occ-DT)",
  "Sensor Switch (Vac-DT)",
  "Sensor Switch (Sensor + 0-10V DIM)",
  "Sensor Switch (Occ + 0-10V DIM)",
  "Sensor Switch (Vac + 0-10V DIM)",
]);
const PLATE_SW = new Set([
  "Toggle",
  "Wireless Switch",
  "Rocker",
  "Keyed Switch",
]); // gang-tracked plate switches

function addPlateSw(name) {
  // Each click adds a NEW plate entry at 1G (multiple plates of same type allowed)
  state.switchType.push({ name, gangs: 1 });
  renderSwitchQtyPanel();
  updateSummary();
}

function changeSwitchGangs(i, delta) {
  const sw = state.switchType[i];
  if (!sw) return;
  sw.gangs = Math.max(1, Math.min(6, (sw.gangs || 1) + delta));
  renderSwitchQtyPanel();
  updateSummary();
}

function removeSwitchType(i) {
  const name = state.switchType[i]?.name;
  const qty = state.switchType[i]?.qty || 1;
  state.switchType.splice(i, 1);
  if (name && !state.switchType.find((s) => s.name === name))
    document.querySelectorAll('.btn[data-g="switch"]').forEach((b) => {
      if (!b.dataset.v) return;
      if (b.dataset.v === name) b.classList.remove("active");
    });
  // If it was a sensor switch, remove the matching existing control entry too
  if (name && SENSOR_SWITCH_VALS.has(name)) {
    state.existingCtrls = state.existingCtrls.filter(
      (x) => (x.name || x) !== name,
    );
    document.querySelectorAll("#existingCtrlBtns .btn").forEach((b) => {
      if (b.dataset.v === name) b.classList.remove("active");
    });
    renderExistingCtrlQtyPanel();
  }
  renderSwitchQtyPanel();
  updateSummary();
}

function clearSwitchType() {
  state.switchType = [];
  state.switchNotes = [];
  state.zoneLabel = "";
  const zi = document.getElementById("zoneLabelInput");
  if (zi) {
    zi.value = "";
    zi.placeholder = "Main (default)";
  }
  document
    .querySelectorAll('.btn[data-g="switch"]')
    .forEach((b) => b.classList.remove("active"));
  renderSwitchQtyPanel();
  updateSummary();
}
function openRoomTypePopover() {
  document.querySelectorAll(".rt-opt").forEach((b) => {
    b.classList.toggle("selected", b.dataset.v === state.roomtype);
  });
  document.getElementById("roomTypeModal").style.display = "block";
}

function closeRoomTypePopover() {
  document.getElementById("roomTypeModal").style.display = "none";
}

function selectRoomType(val, label) {
  state.roomtype = val;
  document.getElementById("roomTypeBtnLabel").textContent = label;
  document.getElementById("roomTypeBtn").classList.add("filled");
  closeRoomTypePopover();
  updateSummary();
}

function init() {
  ecInitType(); // set HW as default active type for existing controls
  // Set default suffix to "none"
  const noneSfx = document.querySelector('.sfx-btn[data-sfx=""]');
  if (noneSfx) noneSfx.classList.add("active");
  // group buttons (height, roomtype) — single select, toggle off if clicked again
  document
    .querySelectorAll('.btn[data-g="height"],.btn[data-g="roomtype"]')
    .forEach((b) => {
      b.addEventListener("click", () => {
        const g = b.dataset.g;
        const already = b.classList.contains("active");
        document
          .querySelectorAll(`.btn[data-g="${g}"]`)
          .forEach((x) => x.classList.remove("active"));
        if (already) {
          if (g === "height") state.height = "";
          if (g === "roomtype") state.roomtype = "";
        } else {
          b.classList.add("active");
          if (g === "height") state.height = b.dataset.v;
          if (g === "roomtype") state.roomtype = b.dataset.v;
        }
        updateSummary();
      });
    });

  // Switch picker is now driven by the Switch Builder modal (openSwitchBuilder).
  // No inline .btn[data-g="switch"] buttons remain — wiring removed.

  // switch recommendation buttons — multi-select with qty
  if (!state.swRec) state.swRec = [];
  document.querySelectorAll('#swRecBtns .btn[data-g="swrec"]').forEach((b) => {
    if (!b.dataset.v) return;
    b.addEventListener("click", () => {
      const v = b.dataset.v;
      const ex = state.swRec.find((s) => s.name === v);
      if (ex) {
        ex.qty++;
      } else {
        state.swRec.push({ name: v, qty: 1 });
        b.classList.add("active");
      }
      renderSwRecQtyPanel();
      renderCtrlQtyPanel();
      updateSummary();
    });
  });

  // fixture recommendation buttons — multi-select with qty
  if (!state.fixRec) state.fixRec = [];
  document
    .querySelectorAll('#fixRecBtns .btn[data-g="fixrec"]')
    .forEach((b) => {
      if (!b.dataset.v) return;
      b.addEventListener("click", () => {
        const v = b.dataset.v;
        const ex = state.fixRec.find((s) => s.name === v);
        if (ex) {
          ex.qty++;
        } else {
          state.fixRec.push({ name: v, qty: 1 });
          b.classList.add("active");
        }
        renderFixRecQtyPanel();
        updateSummary();
      });
    });

  // ctrl buttons — tap adds with default qty, tap again increments, None is exclusive
  document.querySelectorAll("#ctrlBtns .btn").forEach((b) => {
    b.addEventListener("click", () => {
      const v = b.dataset.v;
      if (!v) return; // skip + Custom button
      if (v === "None") {
        const already = b.classList.contains("active");
        state.ctrls = [];
        document
          .querySelectorAll("#ctrlBtns .btn")
          .forEach((x) => x.classList.remove("active"));
        if (!already) {
          state.ctrls = [{ name: "None", qty: 1 }];
          b.classList.add("active");
        }
      } else {
        const noneBtn = document.getElementById("ctrlNoneBtn");
        if (noneBtn) noneBtn.classList.remove("active");
        state.ctrls = state.ctrls.filter((x) => x.name !== "None");
        const existing = state.ctrls.find((x) => x.name === v);
        if (existing) {
          // Already selected — increment qty
          existing.qty++;
        } else {
          // New selection — fixture-based (standalone/networked) default to total fixture qty
          const addedQty = state.fixtures.reduce(
            (sum, f) => sum + (f.qty || 1),
            0,
          );
          const pendingQty = state.activeFixture
            ? parseInt(document.getElementById("qtyVal")?.value) || 1
            : 0;
          const fixtureTotal = addedQty || pendingQty;
          const FIXTURE_INTEGRATED = new Set([
            "Fixture-Based (Standalone)",
            "Fixture-Based (Networked)",
            "Factory-Installed (Standalone)",
            "Factory-Installed (Networked)",
            "Integrated",
            "LCBS/MVS",
            "MVS",
          ]);
          const defaultQty = FIXTURE_INTEGRATED.has(v) ? fixtureTotal || 1 : 1; // all others (incl. Fixture-Mounted Sensor, sensors, Power Pack) start at 1
          state.ctrls.push({ name: v, qty: defaultQty });
          b.classList.add("active");
        }
      }
      renderCtrlQtyPanel();
      updateSummary();
    });
  });

  // existing ctrl buttons — tap adds with qty 1, tap again increments, None is exclusive
  // Skip sensor buttons that have their own onclick="ecSelectSensor()" handler
  const EC_SENSOR_VALS = new Set([
    "Ceiling Sensor",
    "Corner Sensor",
    "Wall Sensor",
    "Photosensor",
  ]);
  document.querySelectorAll("#existingCtrlBtns .btn").forEach((b) => {
    b.addEventListener("click", () => {
      const v = b.dataset.v;
      if (!v) return; // skip + Custom button
      if (EC_SENSOR_VALS.has(v)) return; // handled by ecSelectSensor()
      if (v === "None") {
        const already = b.classList.contains("active");
        state.existingCtrls = [];
        document
          .querySelectorAll("#existingCtrlBtns .btn")
          .forEach((x) => x.classList.remove("active"));
        if (!already) {
          state.existingCtrls = [{ name: "None", qty: 1 }];
          b.classList.add("active");
        }
      } else {
        const noneBtn = document.getElementById("ecNoneBtn");
        if (noneBtn) noneBtn.classList.remove("active");
        state.existingCtrls = state.existingCtrls.filter(
          (x) => (x.name || x) !== "None",
        );
        const existing = state.existingCtrls.find((x) => (x.name || x) === v);
        if (existing) {
          existing.qty = (existing.qty || 1) + 1;
        } else {
          state.existingCtrls.push({ name: v, qty: 1 });
          b.classList.add("active");
        }
      }
      renderExistingCtrlQtyPanel();
      updateSummary();
    });
  });
}

function openCustomForm(e) {
  if (e) e.stopPropagation();
  document.getElementById("customForm").style.display = "block";
  document.getElementById("customCode").value = "";
  document.getElementById("customWatts").value = "";
  document.getElementById("customDesc").value = "";
  setTimeout(() => document.getElementById("customCode").focus(), 50);
}

function closeCustomForm() {
  document.getElementById("customForm").style.display = "none";
}

function saveCustomFixture() {
  const code = document.getElementById("customCode").value.trim().toUpperCase();
  if (!code) {
    document.getElementById("customCode").focus();
    return;
  }
  const watts = parseInt(document.getElementById("customWatts").value) || null;
  const desc = document.getElementById("customDesc").value.trim();

  // Register in fixture lookup
  FIXTURES[code] = { desc, watts, type: "Interior" };
  state.activeFixture = code;

  // Show as pending button + inline suffix buttons
  const grid = document.getElementById("fixtureBtns");
  grid.innerHTML = "";
  grid.style.alignItems = "center";
  const btn = document.createElement("button");
  btn.id = "pendingFixtureBtn";
  btn.className = "btn btn-fix active";
  btn.dataset.v = code;
  btn.innerHTML = `<span style="font-size:13px;font-weight:700">${code}</span>${
    desc
      ? `<span style="display:block;font-size:9px;font-weight:400;color:#5a8ab0;margin-top:2px">${desc}</span>`
      : ""
  }`;
  btn.addEventListener("click", () => {
    state.activeFixture = "";
    rebuildFreqGrid();
  });
  grid.appendChild(btn);

  const div2 = document.createElement("div");
  div2.style.cssText =
    "width:1.5px;background:#c8d4e3;align-self:stretch;margin:0 2px;flex-shrink:0";
  grid.appendChild(div2);

  [
    { sfx: "", label: "none" },
    { sfx: "-hall", label: "-hall" },
    { sfx: "-dim", label: "-dim" },
    { sfx: "-stair", label: "-stair" },
    { sfx: "-H", label: "-H" },
  ].forEach(({ sfx, label }) => {
    const sb = document.createElement("button");
    sb.className = "sfx-btn" + (sfx === "" ? " active" : "");
    sb.dataset.sfx = sfx;
    sb.textContent = label;
    sb.onclick = () => setSuffix(sb);
    grid.appendChild(sb);
  });

  closeCustomForm();
  toast(`✔ "${code}" saved — set qty and tap Add`);
}

function setSuffix(btn) {
  document
    .querySelectorAll(".sfx-btn")
    .forEach((b) => b.classList.remove("active"));
  btn.classList.add("active");
}
// ── ADD ZONE — log current zone, clear and start next ─────────────────────
// ── NOTES MODAL ───────────────────────────────────────────────────────────
function openNotesModal() {
  document.getElementById("notesModal").style.display = "block";
  syncNotesDisplay();
}
function closeNotesModal() {
  document.getElementById("notesModal").style.display = "none";
}

// ── COMMIT ZONE PROMPT ────────────────────────────────────────────────────
function openZonePrompt() {
  if (!state.room || !state.room.trim()) {
    toast("Enter a room name first");
    const ri = document.getElementById("roomInput");
    if (ri) {
      ri.focus();
      ri.style.borderColor = "#dc2626";
      setTimeout(() => (ri.style.borderColor = ""), 2000);
    }
    return;
  }
  if (
    state.fixtures.length === 0 &&
    state.switchType.length === 0 &&
    state.existingCtrls.length === 0
  ) {
    toast("Add fixtures, switches, or controls before committing a zone");
    return;
  }
  const inp = document.getElementById("zonePromptInput");
  if (inp) inp.value = "";
  document.getElementById("zonePromptModal").style.display = "block";
  setTimeout(() => inp?.focus(), 60);
}
function closeZonePrompt() {
  document.getElementById("zonePromptModal").style.display = "none";
}
function commitZoneFromPrompt() {
  const inp = document.getElementById("zonePromptInput");
  const val = (inp?.value || "").trim();
  if (!val) {
    toast("Enter a zone name");
    inp?.focus();
    return;
  }
  state.zoneLabel = val;
  closeZonePrompt();
  addZone();
}

// ── COMMIT-TO-DATA-TABLE FLOURISH ─────────────────────────────────────────
// Fires when a room is committed: the review area briefly shrinks/fades and
// an orange medallion sweeps from its position up to the Data Table pill,
// which pulses on arrival. Pure flair — no functional purpose.
function flashCommitToDataTable() {
  const review = document.getElementById("reviewArea");
  // Match the Data Table button by its onclick handler (the top action btn that opens the log)
  const target = Array.from(document.querySelectorAll(".top-action-btn")).find(
    (b) => /showLog/.test(b.getAttribute("onclick") || ""),
  );
  if (!review || !target) return;

  const sRect = review.getBoundingClientRect();
  const tRect = target.getBoundingClientRect();
  if (!sRect.width || !tRect.width) return;

  // Briefly shrink/fade the review area so the ghost feels like it's "leaving"
  review.style.transition = "transform .28s ease-in, opacity .28s ease-in";
  review.style.transformOrigin = "top center";
  review.style.transform = "scale(.9)";
  review.style.opacity = ".4";
  setTimeout(() => {
    review.style.transition = "";
    review.style.transform = "";
    review.style.opacity = "";
  }, 380);

  // Ghost medallion — set styles individually for maximum iOS Safari compatibility
  const startX = sRect.left + sRect.width / 2;
  const startY = sRect.top + 36;
  const SIZE = 58;
  const ghost = document.createElement("div");
  ghost.textContent = "✓";
  const s = ghost.style;
  s.position = "fixed";
  s.left = startX - SIZE / 2 + "px";
  s.top = startY - SIZE / 2 + "px";
  s.width = SIZE + "px";
  s.height = SIZE + "px";
  s.borderRadius = "50%";
  s.background = "#D96C3A"; // brand-orange (literal so no var resolution risk)
  s.color = "#fff";
  s.display = "flex";
  s.alignItems = "center";
  s.justifyContent = "center";
  s.fontSize = "26px";
  s.fontWeight = "800";
  s.fontFamily = '-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif';
  s.boxShadow =
    "0 8px 28px rgba(217,108,58,.6), 0 0 0 4px rgba(217,108,58,.18)";
  s.zIndex = "99999";
  s.pointerEvents = "none";
  s.transition =
    "transform .95s cubic-bezier(.45,-0.05,.25,1), opacity .95s, box-shadow .95s";
  s.transform = "scale(1)";
  s.opacity = "1";
  document.body.appendChild(ghost);
  // Force a layout pass so the transition runs from the initial state
  void ghost.offsetWidth;

  const dx = tRect.left + tRect.width / 2 - startX;
  const dy = tRect.top + tRect.height / 2 - startY;
  requestAnimationFrame(() => {
    s.transform = `translate(${dx}px, ${dy}px) scale(.32) rotate(20deg)`;
    s.opacity = ".7";
    s.boxShadow = "0 2px 6px rgba(217,108,58,.3)";
  });

  // Pulse the Data Table pill when the ghost arrives
  setTimeout(() => {
    const origT = target.style.transform;
    const origBG = target.style.background;
    target.style.transition =
      "transform .4s cubic-bezier(.34,1.56,.64,1), background .4s";
    target.style.transform = "scale(1.22)";
    target.style.background = "#D96C3A";
    setTimeout(() => {
      target.style.transform = origT;
      target.style.background = origBG;
    }, 420);
  }, 720);

  setTimeout(() => ghost.remove(), 1050);
}

// ── CLEAR ROOM (with confirmation) ────────────────────────────────────────
function clearRoomConfirm() {
  const hasContent =
    state.room ||
    state.activeFixture ||
    state.fixtures.length ||
    state.switchType.length ||
    state.existingCtrls.length ||
    state.swRec.length ||
    (state.fixRec || []).length ||
    state.ctrls.length ||
    (document.getElementById("notesInput")?.value || "").trim();
  if (
    hasContent &&
    !confirm("Clear all current room data? This cannot be undone.")
  )
    return;
  clearFixtures();
  const ta = document.getElementById("notesInput");
  if (ta) {
    ta.value = "";
    if (typeof syncNotesDisplay === "function") syncNotesDisplay();
  }
  toast("↺ Cleared");
}

// ── SKIP ROOM ─────────────────────────────────────────────────────────────
// Reserves a placeholder line in the log for a complex room that will be
// filled in later from hand-written notes. Saves whatever has been entered.
function skipRoom() {
  if (!state.room || !state.room.trim()) {
    toast("Enter a room name first");
    const ri = document.getElementById("roomInput");
    if (ri) {
      ri.focus();
      ri.style.borderColor = "#dc2626";
      setTimeout(() => (ri.style.borderColor = ""), 2000);
    }
    return;
  }
  // Tag the notes so the auditor can find these later
  const ta = document.getElementById("notesInput");
  const existing = (ta?.value || "").trim();
  const skipTag = "[SKIPPED — fill in later]";
  if (ta && !existing.includes(skipTag)) {
    ta.value = existing ? `${skipTag} | ${existing}` : skipTag;
    syncNotesDisplay();
  }
  logEntry();
  toast("⏭ Room reserved — fill in later");
}

function addZone() {
  if (!state.room) {
    toast("Enter a room name first");
    const ri = document.getElementById("roomInput");
    if (ri) {
      ri.focus();
      ri.style.borderColor = "#dc2626";
      setTimeout(() => (ri.style.borderColor = ""), 2000);
    }
    return;
  }
  if (
    state.fixtures.length === 0 &&
    state.switchType.length === 0 &&
    state.existingCtrls.length === 0
  ) {
    toast("Add fixtures or switch info before adding another zone");
    return;
  }

  // Log the current zone directly
  _addingZone = true;
  logEntry();
  _addingZone = false;

  // Clear fixture/switch/controls but keep room name, height, roomtype, dimensions
  state.fixtures = [];
  state.switchType = [];
  state.switchNotes = [];
  state.existingCtrls = [];
  state.ctrls = [];
  state.swRec = [];
  state.fixRec = [];
  state.activeFixture = "";
  state.zoneLabel = "";
  const zi = document.getElementById("zoneLabelInput");
  if (zi) {
    zi.value = "";
    zi.placeholder = "Zone 2, Task, Emergency…";
  }
  renderChips();
  renderSwitchQtyPanel();
  renderExistingCtrlQtyPanel();
  renderCtrlQtyPanel();
  syncSwitchNoteButtons();
  ecInitType();
  updateSummary();
  toast("✔ Zone logged — enter next zone details");
}

function confirmRoom() {
  state.room = document.getElementById("roomInput").value.trim();
  updateSummary();
}

// ── QTY ───────────────────────────────────────────────────────────────────
function changeQty(d) {
  const el = document.getElementById("qtyVal");
  qty = Math.max(1, (parseInt(el.value) || 1) + d);
  el.value = qty;
  const disp = document.getElementById("qtyDisplay");
  if (disp) disp.textContent = qty;
  if (state.activeFixture) renderReviewArea();
}

// ── ADD FIXTURE ───────────────────────────────────────────────────────────
function addFixture() {
  if (!state.activeFixture) {
    toast("Select a fixture via Match IDs first");
    return;
  }
  const suffix = document.querySelector(".sfx-btn.active")?.dataset.sfx || "";
  const code = state.activeFixture + suffix;
  const base = FIXTURES[state.activeFixture] || {
    desc: "",
    watts: null,
    type: "Interior",
  };
  const matchEntry = MATCH_DATA.find((m) => m.id === state.activeFixture);
  const existingDesc = state.activeFixture.startsWith("FB:")
    ? base.desc || ""
    : matchEntry?.existing || base.desc || "";
  qty = Math.max(1, parseInt(document.getElementById("qtyVal").value) || 1);
  // Capture flag state (interior + exterior — irrelevant flags stay false)
  const intSensor = !!state.fixtureFlagIntSensor;
  const embb = !!state.fixtureFlagEMBB;
  const airReturn = !!state.fixtureFlagAirReturn;
  const wireGuard = !!state.fixtureFlagWireGuard;
  const volt480 = !!state.fixtureFlag480V;
  const emGen = !!state.fixtureFlagEMGen;
  const photocell = !!state.fixtureFlagPhotocell;
  const twistlockPC = !!state.fixtureFlagTwistlockPC;
  const wetLocation = !!state.fixtureFlagWetLocation;
  const darkSky = !!state.fixtureFlagDarkSky;
  const optic = state.fixtureOptic || null;
  const mountType = state.fixtureMountType || null;
  const isNewAddition = !!base._newAddition;
  state.fixtures.push({
    code,
    qty,
    desc: existingDesc,
    watts: base.watts,
    type: base.type,
    intSensor,
    embb,
    airReturn,
    wireGuard,
    volt480,
    emGen,
    photocell,
    twistlockPC,
    wetLocation,
    darkSky,
    optic,
    mountType,
    isNewAddition,
  });
  state.fixtureFlagIntSensor = false;
  state.fixtureFlagEMBB = false;
  state.fixtureFlagAirReturn = false;
  state.fixtureFlagWireGuard = false;
  state.fixtureFlag480V = false;
  state.fixtureFlagEMGen = false;
  state.fixtureFlagPhotocell = false;
  state.fixtureFlagTwistlockPC = false;
  state.fixtureFlagWetLocation = false;
  state.fixtureFlagDarkSky = false;
  state.fixtureOptic = null;
  state.fixtureMountType = null;
  // Track in recent
  const existing = recentFixtures.indexOf(code);
  if (existing !== -1) recentFixtures.splice(existing, 1);
  recentFixtures.push(code);
  if (recentFixtures.length > 10) recentFixtures.shift();
  renderRecentBtns();
  fixtureFreq[state.activeFixture] =
    (fixtureFreq[state.activeFixture] || 0) + qty;
  state.activeFixture = "";
  rebuildFreqGrid();
  renderChips();
  syncFixturePills();
  updateSummary();
  qty = 1;
  document.getElementById("qtyVal").value = 1;
  const qd = document.getElementById("qtyDisplay");
  if (qd) qd.textContent = 1;
}

function removeFixture(i) {
  state.fixtures.splice(i, 1);
  renderChips();
  updateSummary();
}

// ── UNIFIED REVIEW AREA ──────────────────────────────────────────────────
function renderReviewArea() {
  const wrap = document.getElementById("reviewArea");
  if (!wrap) return;

  const fixtures = state.fixtures || [];
  const switches = state.switchType || [];
  const ctrls = state.existingCtrls || [];
  const pending = state.activeFixture;

  if (!pending && !fixtures.length && !switches.length && !ctrls.length) {
    wrap.innerHTML =
      '<div class="review-empty">No items added yet — pick a fixture, switch, or control to begin.</div>';
    return;
  }

  const escapeHtml = (s) =>
    String(s).replace(
      /[&<>"']/g,
      (c) =>
        ({
          "&": "&amp;",
          "<": "&lt;",
          ">": "&gt;",
          '"': "&quot;",
          "'": "&#39;",
        }[c]),
    );

  let html = "";

  if (pending) {
    const fix = FIXTURES[pending];
    const desc = fix?.desc || pending;
    const flagStr = [
      state.fixtureFlagIntSensor ? "⚡ Integral" : "",
      state.fixtureFlagEMBB ? "🔋 EMBB" : "",
      state.fixtureFlagAirReturn ? "💨 Air Return" : "",
      state.fixtureFlagWireGuard ? "🛡 Wire Guard" : "",
      state.fixtureFlag480V ? "⚡ 480V" : "",
      state.fixtureFlagEMGen ? "🏭 EM Gen" : "",
      state.fixtureFlagPhotocell ? "🌗 Photocell" : "",
      state.fixtureFlagTwistlockPC ? "🔌 Twistlock PC" : "",
      state.fixtureFlagWetLocation ? "💧 Wet Loc" : "",
      state.fixtureFlagDarkSky ? "🌌 Dark Sky" : "",
      state.fixtureOptic ? `🎯 ${state.fixtureOptic}` : "",
    ]
      .filter(Boolean)
      .join(" · ");
    const mountStr = state.fixtureMountType
      ? ` · ${escapeHtml(state.fixtureMountType)}`
      : "";
    const currentQty =
      parseInt(document.getElementById("qtyVal")?.value) || qty || 1;
    const metaParts = [`qty ${currentQty}`, flagStr].filter(Boolean);
    const pPhotoKey = state.room ? `photo_${state.room}_fixture` : "";
    const pPhotoCt = pPhotoKey ? getPhotos(pPhotoKey).length : 0;
    const pPhotoBadge =
      pPhotoCt > 0
        ? `<button class="review-cam-badge" onclick="event.preventDefault();event.stopPropagation();openPhotoLightbox('${pPhotoKey}',0)">${pPhotoCt}</button>`
        : "";
    // Always render enabled — the handler toasts about missing room name on
    // click. Previously the disabled state could persist after a room name
    // was typed because renderReviewArea doesn't fire on room input changes.
    const pPhotoBtn = `<div class="review-cam${
      pPhotoCt ? " has-photos" : ""
    }" title="Add fixture photo" onclick="this.querySelector('input[type=file]').click()">📷${pPhotoBadge}<input type="file" accept="image/*,video/*" multiple style="display:none" onchange="handlePendingFixturePhoto(this)"></div>`;
    html += `<div class="review-group pending">
      <div class="review-group-label">Pending</div>
      <div class="review-group-rows">
        <div class="review-row">
          <button class="review-cat" onclick="addCurrentConfigToCatalogue()" title="Save to Catalogue">📌</button>
          ${pPhotoBtn}
          <div class="review-row-desc">
            <span style="font-weight:700">${escapeHtml(desc)}${mountStr}</span>
            <span class="meta">${metaParts.join(
              "  ·  ",
            )}  ·  tap Add › to commit</span>
          </div>
          <button class="review-del" onclick="cancelPendingFixture()" title="Cancel">×</button>
        </div>
      </div>
    </div>`;
  }

  if (fixtures.length) {
    const rows = fixtures
      .map((f, i) => {
        const flags = [
          f.intSensor ? "⚡ Integral" : "",
          f.embb ? "🔋 EMBB" : "",
          f.airReturn ? "💨 Air Return" : "",
          f.wireGuard ? "🛡 Wire Guard" : "",
          f.volt480 ? "⚡ 480V" : "",
          f.emGen ? "🏭 EM Gen" : "",
          f.photocell ? "🌗 Photocell" : "",
          f.twistlockPC ? "🔌 Twistlock PC" : "",
          f.wetLocation ? "💧 Wet Loc" : "",
          f.darkSky ? "🌌 Dark Sky" : "",
          f.optic ? `🎯 ${escapeHtml(f.optic)}` : "",
        ]
          .filter(Boolean)
          .join(" · ");
        const mountStr = f.mountType ? ` · ${escapeHtml(f.mountType)}` : "";
        const metaParts = [f.code ? escapeHtml(f.code) : "", flags].filter(
          Boolean,
        );
        const photoKey = state.room ? `photo_${state.room}_fixture` : "";
        const photoCt = photoKey ? getPhotos(photoKey).length : 0;
        const photoBadge =
          photoCt > 0
            ? `<button class="review-cam-badge" onclick="event.preventDefault();event.stopPropagation();openPhotoLightbox('${photoKey}',0)">${photoCt}</button>`
            : "";
        const photoBtn = photoKey
          ? `<label class="review-cam${
              photoCt ? " has-photos" : ""
            }" title="Add fixture photo">📷${photoBadge}
             <input type="file" accept="image/*,video/*" multiple style="position:absolute;opacity:0;width:1px;height:1px;pointer-events:none" onchange="handleFixtureRowPhoto(this,${i})">
           </label>`
          : `<button class="review-cam" disabled title="Enter room name first" style="opacity:.4;cursor:not-allowed">📷</button>`;
        return `
        <div class="review-row">
          <button class="review-cat" onclick="addPendingFixtureToCatalogue(${i})" title="Save to Catalogue">📌</button>
          ${photoBtn}
          <div class="review-row-desc" onclick="fixtureChipEdit(${i})" title="Tap to edit" style="cursor:pointer">
            <span style="font-weight:700">${escapeHtml(
              f.desc || f.code || "",
            )}${mountStr}</span>
            ${
              metaParts.length
                ? `<span class="meta">${metaParts.join("  ")}</span>`
                : ""
            }
          </div>
          <div class="review-qty">
            <button onclick="changeFixtureQty(${i},-1)">−</button>
            <span class="val">${f.qty}</span>
            <button onclick="changeFixtureQty(${i},1)">+</button>
          </div>
          <button class="review-del" onclick="removeFixture(${i})" title="Remove">×</button>
        </div>`;
      })
      .join("");
    html += `<div class="review-group fixture"><div class="review-group-label">Fixture</div><div class="review-group-rows">${rows}</div></div>`;
  }

  // Per-row photo button — type is 'fixture', 'switch', or 'controls' (matches
  // the existing photo_{room}_{type} bucket scheme).
  // Uses a div + child input + currentTarget.querySelector('input').click()
  // instead of a <label> wrapping the input — label-input association fails
  // when the input is display:none in some Chrome/Edge versions, and adding
  // pointer-events:none to keep it invisible-but-laid-out breaks Firefox/Safari.
  // The explicit programmatic click pattern works everywhere.
  // Always render the button enabled — the photo handler toasts "Enter room
  // name first" when the user clicks without one. Previously we rendered a
  // disabled button when state.room was empty, but the review area doesn't
  // re-render on room-name input, so the disabled state could stick around
  // after the user named the room.
  const photoBtnHtml = (type, handlerName) => {
    const key = state.room ? `photo_${state.room}_${type}` : "";
    const ct = key ? getPhotos(key).length : 0;
    const badge =
      ct > 0
        ? `<button class="review-cam-badge" onclick="event.stopPropagation();openPhotoLightbox('${key}',0)">${ct}</button>`
        : "";
    return `<div class="review-cam${
      ct ? " has-photos" : ""
    }" title="Add photo" onclick="this.querySelector('input[type=file]').click()">📷${badge}<input type="file" accept="image/*,video/*" multiple style="display:none" onchange="${handlerName}(this)"></div>`;
  };

  if (switches.length) {
    const rows = switches
      .map((s, i) => {
        const isPlate = PLATE_SW.has(s.name);
        const countVal = isPlate ? s.gangs || 1 : s.qty || 1;
        const minusFn = isPlate
          ? `changeSwitchGangs(${i},-1)`
          : `changeSwitchQty(${i},-1)`;
        const plusFn = isPlate
          ? `changeSwitchGangs(${i},1)`
          : `changeSwitchQty(${i},1)`;
        const subLabel = isPlate ? "gangs" : "qty";
        const labelTxt =
          escapeHtml(s.name) + (isPlate ? ` (${countVal}G)` : "");
        return `
        <div class="review-row">
          ${photoBtnHtml("switch", "handleSwitchRowPhoto")}
          <div class="review-row-desc">
            <span style="font-weight:700">${labelTxt}</span>
            <span class="meta">${subLabel}</span>
          </div>
          <div class="review-qty">
            <button onclick="${minusFn}">−</button>
            <span class="val">${countVal}</span>
            <button onclick="${plusFn}">+</button>
          </div>
          <button class="review-del" onclick="removeSwitchType(${i})" title="Remove">×</button>
        </div>`;
      })
      .join("");
    html += `<div class="review-group switch"><div class="review-group-label">Switch</div><div class="review-group-rows">${rows}</div></div>`;
  }

  if (ctrls.length) {
    const rows = ctrls
      .map((c, i) => {
        const name = escapeHtml(c.name || c);
        const qty = c.qty || 1;
        return `
        <div class="review-row">
          ${photoBtnHtml("controls", "handleControlsRowPhoto")}
          <div class="review-row-desc"><span style="font-weight:700">${name}</span></div>
          <div class="review-qty">
            <button onclick="changeExistingCtrlQty(${i},-1)">−</button>
            <span class="val">${qty}</span>
            <button onclick="changeExistingCtrlQty(${i},1)">+</button>
          </div>
          <button class="review-del" onclick="removeExistingCtrl(${i})" title="Remove">×</button>
        </div>`;
      })
      .join("");
    html += `<div class="review-group ctrl"><div class="review-group-label">Controls</div><div class="review-group-rows">${rows}</div></div>`;
  }

  wrap.innerHTML = html;
}

function renderChips() {
  renderReviewArea();
}

function fixtureChipEdit(i) {
  const f = state.fixtures[i];
  if (!f) return;
  // Load fixture back into pending state for editing
  state.activeFixture = f.code;
  state.fixtureFlagIntSensor = !!f.intSensor;
  state.fixtureFlagEMBB = !!f.embb;
  state.fixtureFlagAirReturn = !!f.airReturn;
  state.fixtureFlagWireGuard = !!f.wireGuard;
  state.fixtureFlag480V = !!f.volt480;
  state.fixtureFlagEMGen = !!f.emGen;
  state.fixtureFlagPhotocell = !!f.photocell;
  state.fixtureFlagTwistlockPC = !!f.twistlockPC;
  state.fixtureFlagWetLocation = !!f.wetLocation;
  state.fixtureFlagDarkSky = !!f.darkSky;
  state.fixtureOptic = f.optic || null;
  state.fixtureMountType = f.mountType || null;
  // Set qty to match
  qty = f.qty;
  const qv = document.getElementById("qtyVal");
  if (qv) qv.value = f.qty;
  const qd = document.getElementById("qtyDisplay");
  if (qd) qd.textContent = f.qty;
  syncFixtureFlagBtns();
  // Show the fixture in the fixtureBtns area as pending
  fbDoSelect(f.desc || f.code, FIXTURES[f.code]?.watts || null);
  // fbDoSelect doesn't know the original fixture's type — preserve it so the
  // Attribute/Mount modals route to the correct (interior vs exterior) options.
  if (f.type && FIXTURES[state.activeFixture])
    FIXTURES[state.activeFixture].type = f.type;
  // Remove from chips list so it doesn't double-add
  state.fixtures.splice(i, 1);
  renderChips();
  updateSummary();
  toast("Fixture loaded for editing — adjust and tap Add ›");
}

function changeFixtureQty(i, delta) {
  if (!state.fixtures[i]) return;
  state.fixtures[i].qty = Math.max(1, state.fixtures[i].qty + delta);
  renderChips();
  updateSummary();
}

function renderCtrlQtyPanel() {
  const panel = document.getElementById("ctrlQtyPanel");
  if (typeof updateSpecifyCounts === "function") updateSpecifyCounts();
  if (!panel) return;
  const SS_VALS = new Set([
    "SS (Occ)",
    "SS (Vac)",
    "SS (Occ-DT)",
    "SS (Vac-DT)",
  ]);
  // SS items from swRec also display here (read-only mirror, same qty)
  const ssFromSwRec = (state.swRec || []).filter((s) => SS_VALS.has(s.name));

  if (state.ctrls.length === 0 && ssFromSwRec.length === 0) {
    panel.innerHTML = "";
    return;
  }

  const ssRows = ssFromSwRec
    .map(
      (s) => `
    <div style="display:flex;align-items:center;gap:6px;background:#e8f8f0;border:1.5px solid #a0d8bc;border-radius:8px;padding:6px 8px;opacity:.85">
      <span style="font-size:11px;color:#5a9a78;flex-shrink:0">↳</span>
      <span style="font-size:13px;font-weight:700;color:#1a7a48;flex:1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;min-width:0">${s.name} <span style="font-size:10px;font-weight:500;color:#5a9a78">(from Sw Rec)</span></span>
      <span style="font-size:15px;font-weight:700;color:#1a7a48;min-width:24px;text-align:center">${s.qty}</span>
    </div>`,
    )
    .join("");

  const ctrlRows = state.ctrls
    .map(
      (c, i) => `
    <div style="display:flex;align-items:center;gap:6px;background:#e8f8f0;border:1.5px solid #a0d8bc;border-radius:8px;padding:6px 8px">
      <span style="font-size:13px;font-weight:700;color:#1a7a48;flex:1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;min-width:0">${c.name}</span>
      <div style="display:flex;align-items:center;gap:4px;flex-shrink:0">
        <button onclick="changeCtrlQty(${i},-1)" style="width:44px;height:44px;border-radius:6px;border:1px solid #a0d8bc;background:#fff;color:#1a7a48;font-size:18px;font-weight:700;cursor:pointer;display:flex;align-items:center;justify-content:center;line-height:1;padding:0">−</button>
        <span style="font-size:15px;font-weight:700;color:#1a7a48;min-width:24px;text-align:center">${c.qty}</span>
        <button onclick="changeCtrlQty(${i},1)" style="width:44px;height:44px;border-radius:6px;border:1px solid #a0d8bc;background:#fff;color:#1a7a48;font-size:18px;font-weight:700;cursor:pointer;display:flex;align-items:center;justify-content:center;line-height:1;padding:0">+</button>
      </div>
      <button onclick="removeCtrl(${i})" style="width:48px;height:48px;border-radius:6px;border:none;background:transparent;color:#a0b8a0;font-size:26px;cursor:pointer;display:flex;align-items:center;justify-content:center;flex-shrink:0;line-height:1;padding:0">×</button>
    </div>`,
    )
    .join("");

  panel.innerHTML = ssRows + ctrlRows;
}

function changeCtrlQty(i, delta) {
  if (!state.ctrls[i]) return;
  state.ctrls[i].qty = Math.max(1, state.ctrls[i].qty + delta);
  renderCtrlQtyPanel();
  updateSummary();
}

// Keep renderCtrlChips as alias for compatibility
function renderCtrlChips() {
  renderCtrlQtyPanel();
}

function removeCtrl(i) {
  const name = state.ctrls[i]?.name;
  state.ctrls.splice(i, 1);
  if (name) {
    document.querySelectorAll("#ctrlBtns .btn").forEach((b) => {
      if (b.dataset.v === name && !state.ctrls.find((c) => c.name === name)) {
        b.classList.remove("active");
      }
    });
  }
  renderCtrlQtyPanel();
  updateSummary();
}

function renderExistingCtrlQtyPanel() {
  renderReviewArea();
}

function changeExistingCtrlQty(i, delta) {
  if (!state.existingCtrls[i]) return;
  state.existingCtrls[i].qty = Math.max(
    1,
    (state.existingCtrls[i].qty || 1) + delta,
  );
  renderExistingCtrlQtyPanel();
  updateSummary();
}

// Keep alias for compatibility
function renderExistingCtrlChips() {
  renderExistingCtrlQtyPanel();
}

function removeExistingCtrl(i) {
  const item = state.existingCtrls[i];
  const name = item?.name || item;
  state.existingCtrls.splice(i, 1);
  if (name && !state.existingCtrls.find((x) => (x.name || x) === name)) {
    document.querySelectorAll("#existingCtrlBtns .btn").forEach((b) => {
      if (b.dataset.v === name) b.classList.remove("active");
    });
  }
  renderExistingCtrlQtyPanel();
  updateSummary();
}

// ── SUMMARY ───────────────────────────────────────────────────────────────
function updateSummary() {
  // Entry preview bar removed — selections now show in the unified review area.
  // Keep the function name as a no-op-with-side-effect so existing callers stay wired
  // and the collapsible section header summaries stay in sync.
  updateSectionSummaries();
}

// ── LOG ENTRY ─────────────────────────────────────────────────────────────
function logEntry() {
  if (!state.room.trim()) {
    toast("Enter a room name first");
    const ri = document.getElementById("roomInput");
    ri.style.borderColor = "#dc2626";
    ri.focus();
    setTimeout(() => {
      ri.style.borderColor = "";
    }, 2000);
    return;
  }
  if (state.fixtures.length === 0) {
    // Allow logging room without fixtures — creates a placeholder entry
    toast("Room placeholder logged — add fixtures when on site");
  }

  const facility = (bldgData && bldgData.name) || "";
  const floor = (activeFloor && activeFloor.name) || "";
  const notes = document.getElementById("notesInput").value;
  const roomName = state.room.trim();
  const zoneLabel =
    state.zoneLabel && state.zoneLabel.trim() ? state.zoneLabel.trim() : "";
  // Zone suffix: "Room 101 · Main" — only when explicitly set
  const spaceName = zoneLabel ? `${roomName} · ${zoneLabel}` : roomName;

  // Check if this room already exists in the log
  const existingIndices = [];
  logEntries.forEach((e, i) => {
    if (e.space === spaceName) existingIndices.push(i);
  });
  const isUpdate = existingIndices.length > 0;

  // Name check: fire if copied from log (one-time) OR if new room matches last entry
  if (logNameCheckPending) {
    // Copied room — check once if name wasn't changed
    const sourceRoom =
      logEntries.find(
        (e) => existingIndices.length === 0 && e.space !== roomName,
      ) || null;
    if (isUpdate) {
      // Name wasn't changed — warn once then allow
      logNameCheckPending = false;
      const ri = document.getElementById("roomInput");
      ri.style.borderColor = "#d97706";
      ri.style.background = "#fef3c7";
      setTimeout(() => {
        ri.style.borderColor = "";
        ri.style.background = "";
      }, 3000);
      toast("⚠ Check Room Name — same as source. Log again to confirm.");
      return;
    }
    // Name was changed — clear flag and proceed
    logNameCheckPending = false;
  } else if (!isUpdate) {
    // Standard duplicate guard for fresh entries only
    const lastEntry = logEntries[logEntries.length - 1];
    if (lastEntry && lastEntry.space === roomName) {
      const ri = document.getElementById("roomInput");
      ri.style.borderColor = "#d97706";
      ri.style.background = "#fef3c7";
      setTimeout(() => {
        ri.style.borderColor = "";
        ri.style.background = "";
      }, 3000);
      toast("⚠ Check Room Name");
      return;
    }
  }

  // Build new entries for this room
  const hasCustomSwitch = state.switchType.some(
    (s) =>
      ![
        "Toggle",
        "Wireless Switch",
        "Rocker",
        "Pico",
        "Sensor Switch",
        "1P/Socket",
        "1P/SocketX2",
        "Keyed",
        "Panel",
        "None",
      ].includes(s.name),
  );
  const hasCustomExisting = state.existingCtrls.some(
    (x) =>
      ![
        "W Occ",
        "W Vac",
        "W Occ DT",
        "Corner Occ",
        "Remote Occ",
        "H/Way Occ",
        "Fixt. Integrated",
        "Ceiling Occ",
        "None",
      ].includes(x.name || x),
  );
  const hasCustomCtrl = state.ctrls.some(
    (x) =>
      ![
        "W-Occ",
        "W-Occ DT",
        "W-Vac",
        "W-Vac DT",
        "Integrated",
        "LCBS/MVS",
        "MVS",
        "ZDIM/LCB",
        "ZDIM/LCBS",
        "PIR12W",
        "PowPack",
        "None",
      ].includes(x.name),
  );

  const baseHrs = getHoursForRoomType(state.roomtype) || bldgData.hours || 2000;

  const newEntries =
    state.fixtures.length > 0
      ? state.fixtures.map((f, idx) => ({
          line: 0,
          facility,
          floor,
          space: spaceName,
          roomType: state.roomtype,
          height: state.height,
          fixtureId: f.code,
          desc: f.desc || "",
          qty: f.qty,
          intSensor: !!f.intSensor,
          embb: !!f.embb,
          airReturn: !!f.airReturn,
          wireGuard: !!f.wireGuard,
          volt480: !!f.volt480,
          emGen: !!f.emGen,
          photocell: !!f.photocell,
          twistlockPC: !!f.twistlockPC,
          wetLocation: !!f.wetLocation,
          darkSky: !!f.darkSky,
          optic: f.optic || null,
          mountType: f.mountType || null,
          fixtureType: f.type || "Interior", // Interior | Exterior — preserves catalog source for downstream
          isNewAddition: !!f.isNewAddition,
          numSwitches:
            idx === 0 && state.switchType.length
              ? state.switchType
                  .map((s) =>
                    PLATE_SW.has(s.name)
                      ? `${s.name} (${s.gangs || 1}G)`
                      : s.qty > 1
                      ? `${s.name} X${s.qty}`
                      : s.name,
                  )
                  .join(", ")
              : "",
          switchQty: state.switchType.length ? state.switchType[0].qty : 1,
          dimensions: state.dimensions
            ? `${state.dimensions.len || ""}' × ${state.dimensions.wid || ""}'`
            : "",
          ceilingType: state.ceilingType || "",
          existingCtrls:
            idx === 0
              ? state.existingCtrls
                  .map((x) =>
                    (x.qty || 1) > 1 ? `${x.name || x} X${x.qty}` : x.name || x,
                  )
                  .join(", ")
              : "",
          hasCustomSwitch,
          hasCustomExisting,
          hasCustomCtrl,
          ctrlRecs: idx === 0 ? [...state.ctrls] : [],
          swRec: idx === 0 ? [...(state.swRec || [])] : [],
          fixRec: idx === 0 ? [...(state.fixRec || [])] : [],
          switchNotes: idx === 0 ? [...(state.switchNotes || [])] : [],
          notes:
            idx === 0
              ? state.switchNotes?.length
                ? [notes, state.switchNotes.join(", ")]
                    .filter(Boolean)
                    .join(" | ")
                : notes
              : "",
          baselineHours: baseHrs,
        }))
      : [
          {
            // Placeholder — no fixtures yet
            line: 0,
            facility,
            floor,
            space: spaceName,
            roomType: state.roomtype,
            height: state.height,
            fixtureId: "",
            desc: "— placeholder —",
            qty: 0,
            intSensor: false,
            embb: false,
            airReturn: false,
            wireGuard: false,
            volt480: false,
            emGen: false,
            mountType: null,
            numSwitches: state.switchType.length
              ? state.switchType
                  .map((s) =>
                    PLATE_SW.has(s.name)
                      ? `${s.name} (${s.gangs || 1}G)`
                      : s.qty > 1
                      ? `${s.name} X${s.qty}`
                      : s.name,
                  )
                  .join(", ")
              : "",
            switchQty: 1,
            dimensions: state.dimensions
              ? `${state.dimensions.len || ""}' × ${
                  state.dimensions.wid || ""
                }'`
              : "",
            ceilingType: state.ceilingType || "",
            existingCtrls: state.existingCtrls
              .map((x) =>
                (x.qty || 1) > 1 ? `${x.name || x} X${x.qty}` : x.name || x,
              )
              .join(", "),
            hasCustomSwitch,
            hasCustomExisting,
            hasCustomCtrl,
            ctrlRecs: [...state.ctrls],
            swRec: [...(state.swRec || [])],
            fixRec: [...(state.fixRec || [])],
            switchNotes: [...(state.switchNotes || [])],
            notes: state.switchNotes?.length
              ? [notes, state.switchNotes.join(", ")]
                  .filter(Boolean)
                  .join(" | ")
              : notes,
            baselineHours: baseHrs,
          },
        ];

  if (isUpdate) {
    // Replace existing entries for this room in-place, preserving position
    // Find the insertion point (position of first existing entry)
    const firstIdx = existingIndices[0];
    // Remove all old entries for this room
    logEntries = logEntries.filter((e) => e.space !== roomName);
    // Insert new entries at the same position
    logEntries.splice(firstIdx, 0, ...newEntries);
    // Renumber all lines
    logEntries.forEach((e, i) => {
      e.line = i + 1;
    });
    toast(`✔ Updated ${newEntries.length} line(s) for "${roomName}"`);
  } else {
    // New room — append to end with correct line numbers
    newEntries.forEach((e) => {
      e.line = logEntries.length + 1;
      logEntries.push(e);
    });
    toast(`✔ Logged ${newEntries.length} line(s) for "${roomName}"`);
  }

  // Committed — drop any in-progress draft for this room
  clearRoomDraft(roomName);

  updateLogBadge();
  scheduleSave();
  document
    .querySelectorAll(".chip-pulse")
    .forEach((el) => el.classList.remove("chip-pulse"));
  if (typeof flashCommitToDataTable === "function") flashCommitToDataTable();
  clearFixtures();
  document.getElementById("notesInput").value = "";
  if (typeof syncNotesDisplay === "function") syncNotesDisplay();
  renderPins(); // update pin colors after log
}

// ── CLEAR ─────────────────────────────────────────────────────────────────
function clearFixtures() {
  document
    .querySelectorAll(".chip-pulse")
    .forEach((el) => el.classList.remove("chip-pulse"));
  state.fixtures = [];
  state.activeFixture = "";
  state.ctrls = [];
  state.existingCtrls = [];
  state.swRec = [];
  state.fixRec = [];
  state.switchType = [];
  state.switchNotes = [];
  state.zoneLabel = "";
  const zi = document.getElementById("zoneLabelInput");
  if (zi) {
    zi.value = "";
    zi.placeholder = "Main (default)";
  }
  state.fixtureFlagIntSensor = false;
  state.fixtureFlagEMBB = false;
  state.fixtureFlagAirReturn = false;
  state.fixtureFlagWireGuard = false;
  state.fixtureFlag480V = false;
  state.fixtureFlagEMGen = false;
  state.fixtureFlagPhotocell = false;
  state.fixtureFlagTwistlockPC = false;
  state.fixtureFlagWetLocation = false;
  state.fixtureFlagDarkSky = false;
  state.fixtureOptic = null;
  state.fixtureMountType = null;
  syncFixtureFlagBtns();
  ecInitType();
  qty = 1;
  document.getElementById("qtyVal").value = 1;
  document
    .querySelectorAll(".sfx-btn")
    .forEach((b) => b.classList.remove("active"));
  const noneSfx = document.querySelector('.sfx-btn[data-sfx=""]');
  if (noneSfx) noneSfx.classList.add("active");
  document
    .querySelectorAll("#fixtureBtns .btn")
    .forEach((b) => b.classList.remove("active"));
  document.getElementById("fixtureBtns").style.alignItems = "";
  document
    .querySelectorAll("#ctrlBtns .btn")
    .forEach((b) => b.classList.remove("active"));
  document
    .querySelectorAll("#existingCtrlBtns .btn")
    .forEach((b) => b.classList.remove("active"));
  document
    .querySelectorAll('.btn[data-g="height"]')
    .forEach((b) => b.classList.remove("active"));
  document
    .querySelectorAll('.btn[data-g="switch"]')
    .forEach((b) => b.classList.remove("active"));
  renderSwitchQtyPanel();
  // Clear swRec button active states and panel
  document
    .querySelectorAll('#swRecBtns .btn[data-g="swrec"]')
    .forEach((b) => b.classList.remove("active"));
  renderSwRecQtyPanel();
  // Clear fixRec button active states and panel
  document
    .querySelectorAll('#fixRecBtns .btn[data-g="fixrec"]')
    .forEach((b) => b.classList.remove("active"));
  renderFixRecQtyPanel();
  renderCtrlQtyPanel();
  if (!_addingZone) {
    // Mount Height — keep default if set, otherwise reset to empty pill
    if (defaultHeight) {
      state.height = defaultHeight;
      updateHeightBtn(defaultHeight);
    } else {
      state.height = "";
      const htLbl = document.getElementById("heightBtnLabel");
      const htBtn = document.getElementById("heightBtn");
      if (htLbl) htLbl.textContent = "Mount Height";
      if (htBtn) htBtn.classList.remove("filled");
    }
    // Room Type
    state.roomtype = "";
    const rtLbl = document.getElementById("roomTypeBtnLabel");
    const rtBtn = document.getElementById("roomTypeBtn");
    if (rtLbl) rtLbl.textContent = "Room Type";
    if (rtBtn) rtBtn.classList.remove("filled");
    // Dimensions
    state.dimensions = null;
    const dimLbl = document.getElementById("dimensionsBtnLabel");
    const dimBtn = document.getElementById("dimensionsBtn");
    if (dimLbl) dimLbl.textContent = "Dimensions";
    if (dimBtn) dimBtn.classList.remove("filled");
    // Ceiling Type — keep default if set, otherwise reset to empty pill
    if (defaultCeilingType) {
      state.ceilingType = defaultCeilingType;
      updateCeilingTypeBtn(defaultCeilingType);
    } else {
      state.ceilingType = "";
      const ctLbl = document.getElementById("ceilingTypeBtnLabel");
      const ctBtn = document.getElementById("ceilingTypeBtn");
      if (ctLbl) ctLbl.textContent = "Ceiling Type";
      if (ctBtn) ctBtn.classList.remove("filled");
    }
    // Room Name
    state.room = "";
    document.getElementById("roomInput").value = "";
    updateRoomDisplay();
  }

  // Reset qty display
  const qd = document.getElementById("qtyDisplay");
  if (qd) qd.textContent = 1;

  renderChips();
  renderCtrlQtyPanel();
  renderExistingCtrlQtyPanel();
  updateSummary();
}

// ── LOG VIEW ──────────────────────────────────────────────────────────────
let _addingZone = false; // set true during addZone to preserve room details in logEntry
let logViewMode = "visual"; // 'visual' or 'table'

function setLogView(mode) {
  logViewMode = mode;
  document
    .getElementById("viewVisualBtn")
    .classList.toggle("active", mode === "visual");
  document
    .getElementById("viewTableBtn")
    .classList.toggle("active", mode === "table");
  document.getElementById("logVisualWrap").style.display =
    mode === "visual" ? "block" : "none";
  document.getElementById("logTableWrap").style.display =
    mode === "table" ? "block" : "none";
  if (mode === "table") renderLogTable();
  if (mode === "visual") renderRoomCards();
}

// ═════════════════════════════════════════════════════════════════════════
// SPOT CHECK — audit completeness review before leaving site / exporting
// ═════════════════════════════════════════════════════════════════════════
// Scans the current building's pins, polygons, and log entries to surface
// gaps the auditor should fix before exporting. Each finding is tappable to
// jump straight to the relevant context.
function runSpotCheck() {
  const findings = {
    emptyRooms: [], // pin named but no logEntries for that space
    missingSwitches: [], // logEntries exist but no switch info on first entry
    missingControls: [], // logEntries exist but no existingCtrls on first entry
    unnamedPins: [], // pin dropped but no roomName
    unlinkedPolygons: [], // polygon drawn but no pinId
  };

  // Walk every plan in the current building (across all floors)
  Object.values(floorPlans || {}).forEach((plan) => {
    if (!plan) return;
    // Find floor name for nicer labels
    const fl = plan.floorId
      ? (floors || []).find((f) => f.id === plan.floorId)
      : null;
    const floorLabel = fl?.name || "—";
    // Pins
    (plan.pins || []).forEach((pin) => {
      if (!pin.roomName) {
        findings.unnamedPins.push({
          planName: plan.name,
          planId: _planIdOf(plan),
          floorLabel,
          floorId: plan.floorId,
          pinId: pin.id,
          pinNum: pin.pinNum,
        });
        return;
      }
      const entries = logEntries.filter((e) => e.space === pin.roomName);
      if (!entries.length) {
        findings.emptyRooms.push({
          planName: plan.name,
          floorLabel,
          floorId: plan.floorId,
          pinId: pin.id,
          pinNum: pin.pinNum,
          roomName: pin.roomName,
        });
      } else {
        // First entry per room carries the switch/control info. Both fields
        // are comma-joined strings ("Toggle X2, Wireless Switch") — check for
        // non-empty, not a numeric count.
        const first = entries[0];
        const hasSwitches = !!(
          first.numSwitches && String(first.numSwitches).trim()
        );
        const hasCtrls = !!(
          first.existingCtrls && String(first.existingCtrls).trim()
        );
        if (!hasSwitches) {
          findings.missingSwitches.push({
            floorLabel,
            floorId: plan.floorId,
            pinId: pin.id,
            pinNum: pin.pinNum,
            roomName: pin.roomName,
          });
        }
        if (!hasCtrls) {
          findings.missingControls.push({
            floorLabel,
            floorId: plan.floorId,
            pinId: pin.id,
            pinNum: pin.pinNum,
            roomName: pin.roomName,
          });
        }
      }
    });
    // Polygons without a pin link
    (plan.markup?.polygons || []).forEach((poly) => {
      if (!poly.pinId) {
        findings.unlinkedPolygons.push({
          planName: plan.name,
          planId: _planIdOf(plan),
          floorLabel,
          floorId: plan.floorId,
          polyId: poly.id,
        });
      }
    });
  });

  return findings;
}

function _planIdOf(plan) {
  // Reverse-lookup plan id (floorPlans is keyed by id but Object.values loses keys)
  return Object.keys(floorPlans).find((k) => floorPlans[k] === plan) || "";
}

function openSpotCheck() {
  const findings = runSpotCheck();
  const total =
    findings.emptyRooms.length +
    findings.missingSwitches.length +
    findings.missingControls.length +
    findings.unnamedPins.length +
    findings.unlinkedPolygons.length;
  const sum = document.getElementById("spotCheckSummary");
  if (sum)
    sum.textContent =
      total === 0
        ? "All clear — audit looks complete"
        : `${total} item${total === 1 ? "" : "s"} need attention`;
  renderSpotCheckBody(findings);
  document.getElementById("spotCheckModal").style.display = "block";
}

function closeSpotCheck() {
  document.getElementById("spotCheckModal").style.display = "none";
}

function renderSpotCheckBody(findings) {
  const body = document.getElementById("spotCheckBody");
  if (!body) return;
  const esc = (s) =>
    String(s).replace(
      /[&<>"']/g,
      (c) =>
        ({
          "&": "&amp;",
          "<": "&lt;",
          ">": "&gt;",
          '"': "&quot;",
          "'": "&#39;",
        }[c]),
    );

  const total =
    findings.emptyRooms.length +
    findings.missingSwitches.length +
    findings.missingControls.length +
    findings.unnamedPins.length +
    findings.unlinkedPolygons.length;
  if (total === 0) {
    body.innerHTML = `<div style="text-align:center;padding:32px 16px">
      <div style="font-size:48px;margin-bottom:10px">✅</div>
      <div style="font-size:15px;font-weight:700;color:#1a6a35;margin-bottom:6px">Looks complete</div>
      <div style="font-size:12px;color:#5a7a9a;line-height:1.5">Every pinned room has fixtures, switches, and existing-control data logged. Every pin has a name, every outline is linked. Safe to export.</div>
    </div>`;
    return;
  }

  const section = (icon, title, items, renderItem, color) => {
    if (!items.length) return "";
    return `<div style="margin-bottom:14px">
      <div style="font-size:11px;font-weight:700;color:${color};text-transform:uppercase;letter-spacing:.08em;margin-bottom:6px">${icon} ${title} (${
      items.length
    })</div>
      <div style="display:flex;flex-direction:column;gap:5px">${items
        .map(renderItem)
        .join("")}</div>
    </div>`;
  };

  let html = "";
  html += section(
    "❗",
    "Empty rooms",
    findings.emptyRooms,
    (x) =>
      `<div style="display:flex;align-items:center;gap:8px;padding:8px 10px;background:#fff;border:1px solid #f0c0b8;border-radius:8px">
      <div style="flex:1;min-width:0">
        <div style="font-size:13px;font-weight:700;color:#1a2a3a">Pin ${
          x.pinNum
        } · ${esc(x.roomName)}</div>
        <div style="font-size:10px;color:#7a8fa8;margin-top:1px">${esc(
          x.floorLabel,
        )} · no fixtures logged</div>
      </div>
      <button onclick="spotCheckJumpToRoom('${x.pinId}','${
        x.floorId || ""
      }')" style="background:#2563a8;color:#fff;border:none;border-radius:12px;padding:5px 11px;font-size:11px;font-weight:700;cursor:pointer;font-family:inherit">Jump</button>
    </div>`,
    "#b03030",
  );

  html += section(
    "⚠",
    "Missing switches",
    findings.missingSwitches,
    (x) =>
      `<div style="display:flex;align-items:center;gap:8px;padding:8px 10px;background:#fff;border:1px solid #f0d8a0;border-radius:8px">
      <div style="flex:1;min-width:0">
        <div style="font-size:13px;font-weight:700;color:#1a2a3a">${esc(
          x.roomName,
        )}</div>
        <div style="font-size:10px;color:#7a8fa8;margin-top:1px">${esc(
          x.floorLabel,
        )} · fixtures logged but no switch info</div>
      </div>
      <button onclick="spotCheckJumpToRoom('${x.pinId}','${
        x.floorId || ""
      }')" style="background:#2563a8;color:#fff;border:none;border-radius:12px;padding:5px 11px;font-size:11px;font-weight:700;cursor:pointer;font-family:inherit">Jump</button>
    </div>`,
    "#a06010",
  );

  html += section(
    "⚠",
    "Missing controls",
    findings.missingControls,
    (x) =>
      `<div style="display:flex;align-items:center;gap:8px;padding:8px 10px;background:#fff;border:1px solid #f0d8a0;border-radius:8px">
      <div style="flex:1;min-width:0">
        <div style="font-size:13px;font-weight:700;color:#1a2a3a">${esc(
          x.roomName,
        )}</div>
        <div style="font-size:10px;color:#7a8fa8;margin-top:1px">${esc(
          x.floorLabel,
        )} · no existing-controls entry</div>
      </div>
      <button onclick="spotCheckJumpToRoom('${x.pinId}','${
        x.floorId || ""
      }')" style="background:#2563a8;color:#fff;border:none;border-radius:12px;padding:5px 11px;font-size:11px;font-weight:700;cursor:pointer;font-family:inherit">Jump</button>
    </div>`,
    "#a06010",
  );

  html += section(
    "📍",
    "Unnamed pins",
    findings.unnamedPins,
    (x) =>
      `<div style="display:flex;align-items:center;gap:8px;padding:8px 10px;background:#fff;border:1px solid #c8d4e3;border-radius:8px">
      <div style="flex:1;min-width:0">
        <div style="font-size:13px;font-weight:700;color:#1a2a3a">Pin ${
          x.pinNum
        }</div>
        <div style="font-size:10px;color:#7a8fa8;margin-top:1px">${esc(
          x.floorLabel,
        )} · pin dropped but never named</div>
      </div>
      <button onclick="spotCheckJumpToPlan('${x.planId}','${
        x.floorId || ""
      }')" style="background:#2563a8;color:#fff;border:none;border-radius:12px;padding:5px 11px;font-size:11px;font-weight:700;cursor:pointer;font-family:inherit">Open plan</button>
    </div>`,
    "#3a6090",
  );

  html += section(
    "⬡",
    "Unlinked outlines",
    findings.unlinkedPolygons,
    (x) =>
      `<div style="display:flex;align-items:center;gap:8px;padding:8px 10px;background:#fff;border:1px solid #c8d4e3;border-radius:8px">
      <div style="flex:1;min-width:0">
        <div style="font-size:13px;font-weight:700;color:#1a2a3a">Outline</div>
        <div style="font-size:10px;color:#7a8fa8;margin-top:1px">${esc(
          x.floorLabel,
        )} · drawn but not linked to a pin</div>
      </div>
      <button onclick="spotCheckJumpToPlan('${x.planId}','${
        x.floorId || ""
      }')" style="background:#2563a8;color:#fff;border:none;border-radius:12px;padding:5px 11px;font-size:11px;font-weight:700;cursor:pointer;font-family:inherit">Open plan</button>
    </div>`,
    "#3a6090",
  );

  body.innerHTML = html;
}

// Jump to a specific room — switches floors if needed, then loads the room.
function spotCheckJumpToRoom(pinId, floorId) {
  closeSpotCheck();
  if (floorId && (!activeFloor || activeFloor.id !== floorId)) {
    const fl = (floors || []).find((f) => f.id === floorId);
    if (fl && typeof floorPillClick === "function") floorPillClick(fl.id);
  }
  // Find the pin across all plans (it lives on one specific plan)
  let foundPin = null;
  for (const planId in floorPlans) {
    const p = floorPlans[planId];
    const pin = (p?.pins || []).find((x) => x.id === pinId);
    if (pin) {
      foundPin = pin;
      break;
    }
  }
  if (foundPin && typeof loadRoomFromPin === "function")
    loadRoomFromPin(foundPin);
}

// Jump to a plan in the floor plan tab — switches floors first, then opens plan.
function spotCheckJumpToPlan(planId, floorId) {
  closeSpotCheck();
  if (floorId && (!activeFloor || activeFloor.id !== floorId)) {
    const fl = (floors || []).find((f) => f.id === floorId);
    if (fl && typeof floorPillClick === "function") floorPillClick(fl.id);
  }
  if (planId && floorPlans[planId]) activePlanId = planId;
  if (typeof openFloorPlan === "function") openFloorPlan();
}

function showLog() {
  document.getElementById("mainApp").style.display = "none";
  document.getElementById("logPanel").classList.add("visible");
  try {
    if (screen.orientation && screen.orientation.lock) {
      screen.orientation.lock("landscape").catch(() => {});
    }
  } catch (e) {}
  renderRoomCards();
  renderLogStats();
}

function renderLogStats() {
  const groups = groupLogByRoom();
  const totalRooms = groups.length;
  const totalFixtures = logEntries
    .filter((e) => e.fixtureId && !e.fixtureId.startsWith("ctrl"))
    .reduce((s, e) => s + (e.qty || 1), 0);
  const switchEntries = logEntries.filter(
    (e) => e.numSwitches && e.numSwitches.trim() && e.numSwitches !== "—",
  );
  const ctrlEntries = logEntries.filter((e) => e.ctrlRecs && e.ctrlRecs.length);
  document.getElementById("statRooms").textContent = totalRooms;
  document.getElementById("statFixtures").textContent = totalFixtures;
  document.getElementById("statSwitches").textContent = switchEntries.length;
  document.getElementById("statCtrls").textContent = ctrlEntries.length;
  document.getElementById("logSummaryBar").style.display =
    totalRooms > 0 ? "flex" : "none";
}

function groupLogByRoom() {
  const groups = [];
  logEntries.forEach((e, i) => {
    const key = `${e.facility}|${e.floor}|${e.space}`;
    const last = groups[groups.length - 1];
    if (last && last.key === key) last.entries.push({ e, i });
    else groups.push({ key, roomName: e.space, entries: [{ e, i }] });
  });
  return groups;
}

// Groups zones of the same base room into one card entry
// Zone entries have space = "Room 101 · Main" — base room is everything before " · "
function groupLogByBaseRoom() {
  const allGroups = groupLogByRoom(); // existing per-space groups
  const baseRooms = [];

  allGroups.forEach((group, gi) => {
    const space = group.roomName || "";
    const dotIdx = space.indexOf(" · ");
    const baseName = dotIdx > -1 ? space.substring(0, dotIdx) : space;
    const zoneLabel = dotIdx > -1 ? space.substring(dotIdx + 3) : null;

    // Compose a base key without the zone suffix
    const firstE = group.entries[0].e;
    const baseKey = `${firstE.facility}|${firstE.floor}|${baseName}`;

    const last = baseRooms[baseRooms.length - 1];
    if (last && last.baseKey === baseKey) {
      last.zones.push({
        zoneLabel,
        spaceName: space,
        entries: group.entries,
        gi,
      });
    } else {
      baseRooms.push({
        baseKey,
        baseName,
        facility: firstE.facility,
        floor: firstE.floor,
        zones: [{ zoneLabel, spaceName: space, entries: group.entries, gi }],
      });
    }
  });
  return baseRooms;
}

// ── ROOM METRICS ─────────────────────────────────────────────────────────
// ASHRAE 90.1-2019 Space-by-Space LPD values (W/sq ft)
const ASHRAE_LPD = {
  "Class rooms": 0.71,
  Office: 0.61,
  Restroom: 0.63,
  Hallway: 0.41,
  Stairwell: 0.49,
  Storage: 0.38,
  Closet: 0.38,
  Gym: 0.72,
  Cafeteria: 0.65,
  Library: 0.71,
  Mechanical: 0.42,
  "Slop Sink": 0.42,
  Exterior: 0.2,
};

function buildRoomMetrics(group, swRec) {
  const first = group.entries[0]?.e;
  if (!first) return "";

  const pills = [];

  // ── LPD ──────────────────────────────────────────────────────────────────
  // LPD is calculated on post-install conditions by the AI Code Agent
  // Show total existing watts as informational only if available
  const totalW = group.entries.reduce((sum, { e }) => {
    const watts = FIXTURES[e.fixtureId]?.watts || 0;
    return sum + watts * (e.qty || 1);
  }, 0);
  const dimMatch = (first.dimensions || "").match(
    /([\d.]+)'\s*[×x]\s*([\d.]+)'/,
  );
  const sqft = dimMatch
    ? Math.round(parseFloat(dimMatch[1]) * parseFloat(dimMatch[2]))
    : 0;

  if (totalW > 0) {
    pills.push(`<div class="metric-pill">
      <div class="metric-label">Existing Watts</div>
      <div class="metric-value neutral">${totalW}W${
      sqft ? " · " + sqft + " ft²" : ""
    }</div>
    </div>`);
  }

  pills.push(`<div class="metric-pill">
    <div class="metric-label">LPD (post-install)</div>
    <div class="metric-value neutral">— pending —</div>
  </div>`);

  pills.push(`<div class="metric-pill">
    <div class="metric-label">LPD Code</div>
    <div class="metric-value neutral">${
      ASHRAE_LPD[first.roomType] != null
        ? ASHRAE_LPD[first.roomType].toFixed(2) + " W/ft²"
        : "—"
    }</div>
  </div>`);

  // ── CONTROL LEVEL (placeholder — full rules coming soon) ──────────────────
  const ctrlLevel = "— TBD —";
  pills.push(`<div class="metric-pill">
    <div class="metric-label">Control Level</div>
    <div class="metric-value neutral">${ctrlLevel}</div>
  </div>`);

  if (!pills.length) return "";
  return pills.join("");
}

// ── ROOM PHOTOS ───────────────────────────────────────────────────────────
// Stored as base64 in localStorage keyed by "photo_{roomName}_{type}"
let roomPhotos = {};

function loadRoomPhotos() {
  try {
    const raw = localStorage.getItem("auditTool_photos_v1");
    if (raw) roomPhotos = JSON.parse(raw);
  } catch (e) {
    roomPhotos = {};
  }
}

function saveRoomPhotos() {
  try {
    localStorage.setItem("auditTool_photos_v1", JSON.stringify(roomPhotos));
  } catch (e) {
    toast("⚠ Photo storage full — consider clearing old photos");
  }
}

// Returns array of photos for a key (migrates legacy single string)
function getPhotos(key) {
  const val = roomPhotos[key];
  if (!val) return [];
  if (Array.isArray(val)) return val;
  return [val]; // migrate legacy single string
}

function setPhotos(key, arr) {
  if (arr.length === 0) delete roomPhotos[key];
  else roomPhotos[key] = arr;
  saveRoomPhotos();
}

// Gallery lightbox state
let _lbKey = null,
  _lbIdx = 0;

function openPhotoLightbox(photoKey, idx) {
  _lbKey = photoKey;
  _lbIdx = idx || 0;
  renderLightbox();
  document.getElementById("photoLightbox").classList.add("open");
}

async function renderLightbox() {
  const photos = getPhotos(_lbKey);
  if (!photos.length) {
    closePhotoLightbox();
    return;
  }
  _lbIdx = Math.max(0, Math.min(_lbIdx, photos.length - 1));
  const rawVal = photos[_lbIdx];
  let src = rawVal;
  if (window.photoStore && window.auditStorage) {
    src = await window.photoStore.resolvePhotoSrc(rawVal, window.auditStorage);
  }
  document.getElementById("photoLightboxImg").src = src;
  const counter = document.getElementById("photoLbCounter");
  if (counter)
    counter.textContent =
      photos.length > 1 ? `${_lbIdx + 1} / ${photos.length}` : "";
  document.getElementById("photoLbPrev").style.display =
    photos.length > 1 ? "" : "none";
  document.getElementById("photoLbNext").style.display =
    photos.length > 1 ? "" : "none";
}

function photoLbPrev() {
  _lbIdx = (_lbIdx - 1 + getPhotos(_lbKey).length) % getPhotos(_lbKey).length;
  renderLightbox();
}
function photoLbNext() {
  _lbIdx = (_lbIdx + 1) % getPhotos(_lbKey).length;
  renderLightbox();
}

function photoLbDelete() {
  if (!_lbKey) return;
  const photos = getPhotos(_lbKey);
  if (!photos.length) return;
  photos.splice(_lbIdx, 1);
  setPhotos(_lbKey, photos);
  if (!photos.length) {
    closePhotoLightbox();
    renderRoomCards();
    if (typeof syncRoomPhotoBtn === "function") syncRoomPhotoBtn();
    if (typeof renderReviewArea === "function") renderReviewArea();
    return;
  }
  _lbIdx = Math.min(_lbIdx, photos.length - 1);
  renderLightbox();
  renderRoomCards();
  if (typeof syncRoomPhotoBtn === "function") syncRoomPhotoBtn();
  if (typeof renderReviewArea === "function") renderReviewArea();
  toast("Photo deleted");
}

function closePhotoLightbox() {
  document.getElementById("photoLightbox").classList.remove("open");
  _lbKey = null;
}

// ── PHOTO COMPRESSION ─────────────────────────────────────────────────────
// Resize to MAX_DIM-px max edge, JPEG quality 0.75. Typical iPad photo
// (~8MB) → ~300–500KB. Keeps localStorage usable until the Supabase
// upload path is wired in (then full photos go to remote, not localStorage).

// Compress file to a Blob (used by the IndexedDB blob-storage path).
async function compressToBlob(file, maxDim = 1600, quality = 0.75) {
  const dataUrl = await new Promise((res, rej) => {
    const r = new FileReader();
    r.onload = (e) => res(e.target.result);
    r.onerror = rej;
    r.readAsDataURL(file);
  });
  const img = await new Promise((res, rej) => {
    const i = new Image();
    i.onload = () => res(i);
    i.onerror = rej;
    i.src = dataUrl;
  });
  const w = img.naturalWidth || img.width;
  const h = img.naturalHeight || img.height;
  if (!w || !h) return file; // fallback — return raw file as Blob
  const scale = Math.min(1, maxDim / Math.max(w, h));
  const tw = Math.max(1, Math.round(w * scale));
  const th = Math.max(1, Math.round(h * scale));
  const canvas = document.createElement("canvas");
  canvas.width = tw;
  canvas.height = th;
  canvas.getContext("2d").drawImage(img, 0, 0, tw, th);
  return new Promise((res) => canvas.toBlob(res, "image/jpeg", quality));
}

async function compressImageDataUrl(file, maxDim = 1600, quality = 0.75) {
  const dataUrl = await new Promise((res, rej) => {
    const r = new FileReader();
    r.onload = (e) => res(e.target.result);
    r.onerror = rej;
    r.readAsDataURL(file);
  });
  const img = await new Promise((res, rej) => {
    const i = new Image();
    i.onload = () => res(i);
    i.onerror = rej;
    i.src = dataUrl;
  });
  const w = img.naturalWidth || img.width;
  const h = img.naturalHeight || img.height;
  if (!w || !h) return dataUrl; // fallback — if we can't measure, return raw
  const scale = Math.min(1, maxDim / Math.max(w, h));
  const tw = Math.max(1, Math.round(w * scale));
  const th = Math.max(1, Math.round(h * scale));
  const canvas = document.createElement("canvas");
  canvas.width = tw;
  canvas.height = th;
  canvas.getContext("2d").drawImage(img, 0, 0, tw, th);
  return canvas.toDataURL("image/jpeg", quality);
}

// Room-level photo capture from the data-entry header. Uses a "room" photo type
// (distinct from the existing fixture/switch/controls buckets) so the spec
// engine can pull overview photos separately from fixture close-ups.
async function handleRoomLevelPhoto(input) {
  if (!state.room) {
    toast("Enter the room name first");
    input.value = "";
    return;
  }
  const files = input.files;
  if (!files || !files.length) return;
  // Video capture is offered in the file picker but local-storage path isn't
  // wired up yet — defer that to the cloud-upload flow. For now warn and skip
  // video files; still process any images picked alongside.
  if (Array.from(files).some((f) => f.type && f.type.startsWith("video/"))) {
    toast("Video capture coming soon — only photos saved for now");
  }
  const key = `photo_${state.room}_room`;
  let savedAny = false;
  for (const file of Array.from(files)) {
    if (!file.type || !file.type.startsWith("image/")) continue;
    try {
      let stored;
      if (window.photoStore && window.auditStorage) {
        stored = await window.photoStore.capturePhotoBlob(
          file, key, { photoType: "room" }, window.auditStorage, compressToBlob
        );
      }
      if (!stored) stored = await compressImageDataUrl(file);
      const photos = getPhotos(key);
      photos.push(stored);
      setPhotos(key, photos);
      savedAny = true;
    } catch (_) {
      toast("Could not load one of the photos");
    }
  }
  input.value = "";
  syncRoomPhotoBtn();
  if (savedAny) toast("📷 Room photo saved");
}

function syncRoomPhotoBtn() {
  const btn = document.getElementById("roomPhotoBtn");
  const badge = document.getElementById("roomPhotoCount");
  if (!btn || !badge) return;
  const photos = state.room ? getPhotos(`photo_${state.room}_room`) : [];
  btn.classList.toggle("has-photos", photos.length > 0);
  if (photos.length > 0) {
    badge.style.display = "";
    badge.textContent = photos.length;
  } else {
    badge.style.display = "none";
  }
}

function openRoomPhotoLightbox() {
  if (!state.room) return;
  const key = `photo_${state.room}_room`;
  if (!getPhotos(key).length) return;
  openPhotoLightbox(key, 0);
}

// Photo capture from a per-fixture review row. For now all per-row photos go
// into the room-scoped fixture bucket (photo_{room}_fixture) — same data model
// the log-view room cards already use. Per-fixture-instance scoping is a
// future enhancement when the spec engine needs to correlate photos to a
// specific fixture line.
// Same as the committed-row handler but for the PENDING row (no fixture index
// since the fixture isn't in state.fixtures yet — still a draft).
async function handlePendingFixturePhoto(input) {
  if (!state.room) {
    toast("Enter the room name first");
    input.value = "";
    return;
  }
  const files = input.files;
  if (!files || !files.length) return;
  if (Array.from(files).some((f) => f.type && f.type.startsWith("video/"))) {
    toast("Video capture coming soon — only photos saved for now");
  }
  const key = `photo_${state.room}_fixture`;
  let savedAny = false;
  for (const file of Array.from(files)) {
    if (!file.type || !file.type.startsWith("image/")) continue;
    try {
      let stored;
      if (window.photoStore && window.auditStorage) {
        stored = await window.photoStore.capturePhotoBlob(
          file, key, { photoType: "fixture" }, window.auditStorage, compressToBlob
        );
      }
      if (!stored) stored = await compressImageDataUrl(file);
      const photos = getPhotos(key);
      photos.push(stored);
      setPhotos(key, photos);
      savedAny = true;
    } catch (_) {
      toast("Could not load one of the photos");
    }
  }
  input.value = "";
  renderReviewArea();
  if (savedAny) toast("📷 Fixture photo saved");
}

async function handleFixtureRowPhoto(input, i) {
  await _captureRowPhoto(input, "fixture", "📷 Fixture photo saved");
}

async function handleSwitchRowPhoto(input) {
  await _captureRowPhoto(input, "switch", "📷 Switch photo saved");
}

async function handleControlsRowPhoto(input) {
  await _captureRowPhoto(input, "controls", "📷 Controls photo saved");
}

async function _captureRowPhoto(input, type, doneToast) {
  if (!state.room) {
    toast("Enter the room name first");
    input.value = "";
    return;
  }
  const files = input.files;
  if (!files || !files.length) return;
  if (Array.from(files).some((f) => f.type && f.type.startsWith("video/"))) {
    toast("Video capture coming soon — only photos saved for now");
  }
  const key = `photo_${state.room}_${type}`;
  let savedAny = false;
  for (const file of Array.from(files)) {
    if (!file.type || !file.type.startsWith("image/")) continue;
    try {
      let stored;
      if (window.photoStore && window.auditStorage) {
        stored = await window.photoStore.capturePhotoBlob(
          file, key, { photoType: type }, window.auditStorage, compressToBlob
        );
      }
      if (!stored) stored = await compressImageDataUrl(file);
      const photos = getPhotos(key);
      photos.push(stored);
      setPhotos(key, photos);
      savedAny = true;
    } catch (_) {
      toast("Could not load one of the photos");
    }
  }
  input.value = "";
  renderReviewArea();
  if (savedAny) toast(doneToast);
}

function handleRoomPhoto(input, roomName, type, gi) {
  const files = input.files;
  if (!files || !files.length) return;
  if (Array.from(files).some((f) => f.type && f.type.startsWith("video/"))) {
    toast("Video capture coming soon — only photos saved for now");
  }
  const imageFiles = Array.from(files).filter(
    (f) => f.type && f.type.startsWith("image/"),
  );
  if (!imageFiles.length) {
    input.value = "";
    return;
  }
  const key = `photo_${roomName}_${type}`;
  let done = 0;
  imageFiles.forEach(async (file) => {
    try {
      let stored;
      if (window.photoStore && window.auditStorage) {
        stored = await window.photoStore.capturePhotoBlob(
          file, key, { photoType: type }, window.auditStorage, compressToBlob
        );
      }
      if (!stored) stored = await compressImageDataUrl(file);
      const photos = getPhotos(key);
      photos.push(stored);
      setPhotos(key, photos);
    } catch (_) {
      toast("Could not load one of the photos");
    } finally {
      done++;
      if (done === imageFiles.length) {
        toast(
          `✔ ${
            imageFiles.length > 1 ? imageFiles.length + " photos" : "Photo"
          } saved`,
        );
        renderRoomCards();
      }
    }
  });
}

// Legacy callers (saveToStorage, applyClipboard, etc.) still invoke this name.
// The visual room mode is now the squircle + minimap "Room Review" — this
// shim routes through to it. Old card-rendering code below is dead but kept
// out of the way (the dead block was replaced wholesale).
function renderRoomCards() {
  if (typeof renderRoomReview === "function") renderRoomReview();
}

// ═════════════════════════════════════════════════════════════════════════
// ROOM REVIEW (minimap + squircles + detail panel)
// ═════════════════════════════════════════════════════════════════════════
let _reviewActiveFloorId = null; // null = active audit floor (default), 'all' = every floor, else floor.id
let _reviewSelectedRoom = null; // currently-selected room name (for highlight + detail)

function renderRoomReview() {
  const empty = document.getElementById("logEmpty");
  const grid = document.getElementById("reviewSquircles");
  if (!grid) return;

  // Default filter to the active audit floor on first open
  if (_reviewActiveFloorId === null)
    _reviewActiveFloorId = activeFloor?.id || "all";

  // Empty-data state — hide grid + minimap UI
  if (
    logEntries.length === 0 &&
    _reviewRoomsForFloor(_reviewActiveFloorId).length === 0
  ) {
    grid.innerHTML = "";
    if (empty) empty.style.display = "block";
  } else if (empty) {
    empty.style.display = "none";
  }

  _renderReviewFloorBar();
  _renderReviewMinimap();
  _renderReviewSquircles();
}

// All rooms (named pins + logged spaces) belonging to a given floor filter.
// Returns array of room descriptors with everything the squircle needs.
function _reviewRoomsForFloor(floorFilter) {
  // Build a map keyed by roomName so a room shows once even if it has both
  // a pin AND log entries.
  const byName = new Map();

  // 1) Walk floor plans for pinned rooms (including rooms with no log entries)
  Object.entries(floorPlans || {}).forEach(([planId, plan]) => {
    if (!plan) return;
    const fl = plan.floorId
      ? (floors || []).find((f) => f.id === plan.floorId)
      : null;
    if (floorFilter !== "all" && fl?.id !== floorFilter) return;
    (plan.pins || []).forEach((pin) => {
      if (!pin.roomName) return;
      if (!byName.has(pin.roomName)) {
        byName.set(pin.roomName, {
          roomName: pin.roomName,
          floorName: fl?.name || "",
          floorId: fl?.id || null,
          planId,
          pin,
        });
      }
    });
  });

  // 2) Walk log entries — pick up any logged rooms that aren't pinned (orphans)
  logEntries.forEach((e) => {
    if (!e.space) return;
    // Floor filter — when filtering, also accept log entries whose floor name
    // matches the filter's floor.name (legacy entries may not have a pin)
    if (floorFilter !== "all") {
      const fl = (floors || []).find((f) => f.id === floorFilter);
      if (e.floor && fl && e.floor !== fl.name) {
        if (!byName.has(e.space)) return; // skip if not already pinned on this floor
      }
    }
    if (!byName.has(e.space)) {
      byName.set(e.space, {
        roomName: e.space,
        floorName: e.floor || "",
        floorId: null,
        planId: null,
        pin: null,
      });
    }
  });

  // Enrich each entry with completeness + meta + first-entry summary
  const rows = [];
  byName.forEach((rec) => {
    const entries = logEntries.filter((e) => e.space === rec.roomName);
    const first = entries[0];
    const state = _roomCompletenessState(rec.pin, entries);
    const photoCount = ["room", "fixture", "switch", "controls"].reduce(
      (n, t) =>
        n +
        (typeof getPhotos === "function"
          ? getPhotos(`photo_${rec.roomName}_${t}`).length
          : 0),
      0,
    );
    rows.push({
      ...rec,
      entries,
      first,
      state,
      photoCount,
      fixtureCount: entries.length,
      roomType: first?.roomType || rec.pin?.roomType || "",
      height: first?.height || rec.pin?.height || "",
      dimensions: first?.dimensions || "",
      ceilingType: first?.ceilingType || rec.pin?.ceilingType || "",
      pinNum: rec.pin?.pinNum,
    });
  });

  // Sort: by pin number when available, else by name
  rows.sort((a, b) => {
    if (a.pinNum != null && b.pinNum != null) return a.pinNum - b.pinNum;
    if (a.pinNum != null) return -1;
    if (b.pinNum != null) return 1;
    return a.roomName.localeCompare(b.roomName);
  });
  return rows;
}

function _roomCompletenessState(pin, entries) {
  if (!entries.length) return pin ? "empty" : "empty";
  const first = entries[0];
  const hasSwitches = !!(first.numSwitches && String(first.numSwitches).trim());
  const hasCtrls = !!(
    first.existingCtrls && String(first.existingCtrls).trim()
  );
  return hasSwitches && hasCtrls ? "complete" : "partial";
}

function _renderReviewFloorBar() {
  const bar = document.getElementById("reviewFloorBar");
  if (!bar) return;
  if (!floors || floors.length <= 1) {
    bar.style.display = "none";
    return;
  }
  bar.style.display = "flex";
  const esc = (s) =>
    String(s).replace(
      /[&<>"']/g,
      (c) =>
        ({
          "&": "&amp;",
          "<": "&lt;",
          ">": "&gt;",
          '"': "&quot;",
          "'": "&#39;",
        }[c]),
    );
  bar.innerHTML =
    `<span style="font-size:10px;font-weight:700;color:#5a7a9a;text-transform:uppercase;letter-spacing:.08em">Floor:</span>` +
    floors
      .map(
        (f) =>
          `<button class="rr-floor-pill${
            _reviewActiveFloorId === f.id ? " active" : ""
          }" onclick="reviewSetFloor('${f.id}')">${esc(f.name)}</button>`,
      )
      .join("") +
    `<button class="rr-floor-pill${
      _reviewActiveFloorId === "all" ? " active" : ""
    }" onclick="reviewSetFloor('all')">All</button>`;
}

function reviewSetFloor(floorId) {
  _reviewActiveFloorId = floorId;
  _reviewSelectedRoom = null;
  renderRoomReview();
}

function _renderReviewMinimap() {
  const section = document.getElementById("reviewMinimapSection");
  const svg = document.getElementById("reviewMinimapSvg");
  const img = document.getElementById("reviewMinimapBg");
  const emptyEl = document.getElementById("reviewMinimapEmpty");
  if (!section || !svg || !img) return;

  // Find the plan for the active filter (or first plan if 'all')
  let plan = null;
  if (_reviewActiveFloorId && _reviewActiveFloorId !== "all") {
    for (const id in floorPlans) {
      if (floorPlans[id]?.floorId === _reviewActiveFloorId) {
        plan = floorPlans[id];
        break;
      }
    }
  } else if (activeFloor) {
    for (const id in floorPlans) {
      if (floorPlans[id]?.floorId === activeFloor.id) {
        plan = floorPlans[id];
        break;
      }
    }
  }
  if (!plan || !plan.imageData) {
    img.removeAttribute("src");
    svg.innerHTML = "";
    if (emptyEl) emptyEl.style.display = "flex";
    return;
  }
  if (emptyEl) emptyEl.style.display = "none";
  if (img.getAttribute("src") !== plan.imageData)
    img.setAttribute("src", plan.imageData);

  // Need natural dimensions
  if (!plan._mmDims) {
    const tmp = new Image();
    tmp.onload = () => {
      plan._mmDims = { w: tmp.naturalWidth, h: tmp.naturalHeight };
      _renderReviewMinimap();
    };
    tmp.src = plan.imageData;
    return;
  }
  const W = plan._mmDims.w,
    H = plan._mmDims.h;
  const c = plan.crop;
  const cx = c ? c.xPct * W : 0;
  const cy = c ? c.yPct * H : 0;
  const cw = c ? c.wPct * W : W;
  const ch = c ? c.hPct * H : H;

  svg.setAttribute("viewBox", `${cx} ${cy} ${cw} ${ch}`);

  // Match the floated container's height to the plan's aspect ratio so the
  // image fills edge-to-edge (no letterbox gap above/below). Clamp height to
  // a sensible range so a very long or very tall plan doesn't dominate.
  const preRect = section.getBoundingClientRect();
  if (preRect.width > 0 && cw > 0 && ch > 0) {
    const targetH = Math.max(240, Math.min(560, preRect.width * (ch / cw)));
    section.style.height = targetH + "px";
  }

  // Reposition the image to match the SVG viewBox / preserveAspectRatio
  const rect = section.getBoundingClientRect();
  const CW = rect.width || 1,
    CH = rect.height || 1;
  const fit = Math.min(CW / cw, CH / ch);
  img.style.position = "absolute";
  img.style.width = W * fit + "px";
  img.style.height = H * fit + "px";
  img.style.left = (CW - cw * fit) / 2 - cx * fit + "px";
  img.style.top = (CH - ch * fit) / 2 - cy * fit + "px";
  img.style.objectFit = "fill";

  const STROKE = 1.6 / fit;
  const PIN_R = 8.5 / fit;
  const FONT_SZ = 11 / fit;
  const NS = "http://www.w3.org/2000/svg";
  svg.innerHTML = "";

  const inCrop = (xPct, yPct) => {
    if (!c) return true;
    return (
      xPct >= c.xPct &&
      xPct <= c.xPct + c.wPct &&
      yPct >= c.yPct &&
      yPct <= c.yPct + c.hPct
    );
  };

  // Completeness palette for fills (matches main floor plan tab)
  const POLY_COLORS = {
    unlinked: { fill: "rgba(148,163,184,0.20)", stroke: "#64748b" },
    empty: { fill: "rgba(220, 38, 38,0.24)", stroke: "#991b1b" },
    partial: { fill: "rgba(245,158, 11,0.26)", stroke: "#b45309" },
    complete: { fill: "rgba( 22,163, 74,0.22)", stroke: "#0e7a35" },
  };

  // Polygons first
  (plan.markup?.polygons || []).forEach((p) => {
    if (!p.points || p.points.length < 3) return;
    if (c && !p.points.some((pt) => inCrop(pt.xPct, pt.yPct))) return;
    const linkedPin = (plan.pins || []).find((pin) => pin.id === p.pinId);
    const stateClass =
      typeof _polyColorClass === "function" ? _polyColorClass(p) : "unlinked";
    const palette = POLY_COLORS[stateClass] || POLY_COLORS.unlinked;
    const isSelected =
      linkedPin &&
      linkedPin.roomName &&
      linkedPin.roomName === _reviewSelectedRoom;
    const pts = p.points.map((pt) => `${pt.xPct * W},${pt.yPct * H}`).join(" ");
    const el = document.createElementNS(NS, "polygon");
    el.setAttribute("points", pts);
    el.setAttribute("fill", isSelected ? "rgba(37,99,168,0.32)" : palette.fill);
    el.setAttribute("stroke", isSelected ? "#1a4a80" : palette.stroke);
    el.setAttribute("stroke-width", isSelected ? STROKE * 2 : STROKE);
    el.style.cursor = linkedPin?.roomName ? "pointer" : "default";
    if (linkedPin?.roomName) {
      el.addEventListener("click", () =>
        reviewSelectRoom(linkedPin.roomName, /*scroll*/ true),
      );
    }
    svg.appendChild(el);
  });

  // Pins on top
  (plan.pins || []).forEach((pin) => {
    if (!pin.roomName) return;
    if (!inCrop(pin.xPct, pin.yPct)) return;
    const isSelected = pin.roomName === _reviewSelectedRoom;
    const hasLogged = logEntries.some((e) => e.space === pin.roomName);
    const color = hasLogged ? "#ff6eb4" : "#00e64d";

    const hit = document.createElementNS(NS, "circle");
    hit.setAttribute("cx", pin.xPct * W);
    hit.setAttribute("cy", pin.yPct * H);
    hit.setAttribute("r", PIN_R * 2.5);
    hit.setAttribute("fill", "transparent");
    hit.style.cursor = "pointer";
    hit.addEventListener("click", () =>
      reviewSelectRoom(pin.roomName, /*scroll*/ true),
    );
    svg.appendChild(hit);

    const dot = document.createElementNS(NS, "circle");
    dot.setAttribute("cx", pin.xPct * W);
    dot.setAttribute("cy", pin.yPct * H);
    dot.setAttribute("r", isSelected ? PIN_R * 1.25 : PIN_R);
    dot.setAttribute("fill", color);
    dot.setAttribute("stroke", isSelected ? "#1a4a80" : "#fff");
    dot.setAttribute("stroke-width", PIN_R * (isSelected ? 0.35 : 0.18));
    dot.style.pointerEvents = "none";
    svg.appendChild(dot);

    const lbl = document.createElementNS(NS, "text");
    lbl.setAttribute("x", pin.xPct * W);
    lbl.setAttribute("y", pin.yPct * H);
    lbl.setAttribute("text-anchor", "middle");
    lbl.setAttribute("dominant-baseline", "central");
    lbl.setAttribute("fill", "#003010");
    lbl.setAttribute("font-size", FONT_SZ);
    lbl.setAttribute("font-weight", "800");
    lbl.setAttribute(
      "font-family",
      "-apple-system,BlinkMacSystemFont,sans-serif",
    );
    lbl.style.pointerEvents = "none";
    lbl.textContent = pin.pinNum;
    svg.appendChild(lbl);
  });
}

function _renderReviewSquircles() {
  const grid = document.getElementById("reviewSquircles");
  if (!grid) return;
  const rows = _reviewRoomsForFloor(_reviewActiveFloorId);
  if (!rows.length) {
    grid.innerHTML = "";
    return;
  }
  const esc = (s) =>
    String(s).replace(
      /[&<>"']/g,
      (c) =>
        ({
          "&": "&amp;",
          "<": "&lt;",
          ">": "&gt;",
          '"': "&quot;",
          "'": "&#39;",
        }[c]),
    );
  grid.innerHTML = rows
    .map((r) => {
      const sel = r.roomName === _reviewSelectedRoom ? " selected" : "";
      const stateLbl =
        r.state === "complete"
          ? "Complete"
          : r.state === "partial"
          ? "Partial"
          : "Empty";
      // Floor name dropped — it's already the active filter pill above.
      const meta = [
        r.roomType ? `<span>🏠 ${esc(r.roomType)}</span>` : "",
        r.dimensions ? `<span>📐 ${esc(r.dimensions)}</span>` : "",
        r.height ? `<span>↕ ${esc(String(r.height))}'</span>` : "",
      ]
        .filter(Boolean)
        .join("");
      const pinTag = r.pinNum != null ? `Pin ${r.pinNum}` : "— no pin —";
      const fxLine = r.fixtureCount
        ? `<span>${r.fixtureCount} fixture${
            r.fixtureCount === 1 ? "" : "s"
          }</span>`
        : '<span style="color:#b03030">no fixtures</span>';
      const photoLine = r.photoCount ? `<span>📷 ${r.photoCount}</span>` : "";
      // Room names can contain apostrophes (e.g. "Mary's office"), so we avoid
      // inline onclick string-interpolation entirely — a delegated click handler
      // below reads data-room from the closest `.rr-sq` ancestor.
      return `<div class="rr-sq state-${r.state}${sel}" data-room="${esc(
        r.roomName,
      )}">
      <span class="rr-sq-pin">${pinTag}</span>
      <div class="rr-sq-name"><span style="color:#7a8fa8;font-weight:600;font-size:11px;text-transform:uppercase;letter-spacing:.06em;display:block;margin-bottom:1px">Area</span>${esc(
        r.roomName,
      )}</div>
      <div class="rr-sq-meta">${meta}</div>
      <div class="rr-sq-footer">
        <span class="rr-sq-state-tag">${stateLbl}</span>
        <span style="display:inline-flex;gap:8px">${fxLine}${photoLine}</span>
      </div>
    </div>`;
    })
    .join("");
  // Delegated click — bind once (idempotent via data attribute)
  if (!grid.dataset.delegated) {
    grid.dataset.delegated = "1";
    grid.addEventListener("click", (e) => {
      const sq = e.target && e.target.closest && e.target.closest(".rr-sq");
      if (!sq) return;
      const name = sq.dataset.room;
      if (!name) return;
      reviewSelectRoom(name, false);
      openRoomDetail(name);
    });
  }
}

// Bi-directional selection — used by minimap clicks and squircle clicks.
function reviewSelectRoom(roomName, scrollIntoView) {
  _reviewSelectedRoom = roomName;
  _renderReviewMinimap();
  _renderReviewSquircles();
  if (scrollIntoView) {
    const el = document.querySelector(
      `.rr-sq[data-room="${roomName.replace(/"/g, '\\"')}"]`,
    );
    if (el && el.scrollIntoView)
      el.scrollIntoView({ behavior: "smooth", block: "center" });
  }
}

// ── Detail panel ─────────────────────────────────────────────────────────
function openRoomDetail(roomName) {
  if (!roomName) return;
  const modal = document.getElementById("reviewDetailModal");
  const header = document.getElementById("reviewDetailHeader");
  const body = document.getElementById("reviewDetailBody");
  if (!modal || !header || !body) return;
  const rows = _reviewRoomsForFloor("all");
  const rec = rows.find((r) => r.roomName === roomName);
  if (!rec) return;

  const esc = (s) =>
    String(s).replace(
      /[&<>"']/g,
      (c) =>
        ({
          "&": "&amp;",
          "<": "&lt;",
          ">": "&gt;",
          '"': "&quot;",
          "'": "&#39;",
        }[c]),
    );
  const stateColor =
    rec.state === "complete"
      ? "#0e7a35"
      : rec.state === "partial"
      ? "#b45309"
      : "#991b1b";
  header.innerHTML = `
    <div style="display:flex;align-items:center;gap:10px;flex:1;min-width:0">
      <span style="background:${stateColor};color:#fff;font-size:11px;font-weight:800;border-radius:10px;padding:3px 9px">${
    rec.pinNum != null ? "Pin " + rec.pinNum : "No Pin"
  }</span>
      <div style="flex:1;min-width:0">
        <div style="color:#fff;font-size:15px;font-weight:700;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">${esc(
          rec.roomName,
        )}</div>
        <div style="color:rgba(255,255,255,.75);font-size:10px;margin-top:1px">${esc(
          rec.floorName || "",
        )} ${rec.roomType ? "· " + esc(rec.roomType) : ""} ${
    rec.dimensions ? "· " + esc(rec.dimensions) : ""
  }</div>
      </div>
    </div>
    <button onclick="closeRoomDetail()" style="background:transparent;color:rgba(255,255,255,.85);font-size:20px;border:none;cursor:pointer;line-height:1;padding:0 4px">✕</button>
  `;

  // Body: photos gallery + conditions list + light levels
  body.innerHTML = _renderRoomDetailBody(rec);
  // Wire detail-action buttons via delegation so apostrophes/quotes in
  // roomName never break the string interpolation.
  body.querySelectorAll("[data-detail-action]").forEach((btn) => {
    btn.addEventListener(
      "click",
      () => {
        const act = btn.dataset.detailAction;
        if (act === "edit") reviewDetailEdit(rec.roomName);
        else if (act === "copy") reviewDetailCopy(rec.roomName);
      },
      { once: true },
    );
  });
  modal.style.display = "block";
}

function closeRoomDetail() {
  const modal = document.getElementById("reviewDetailModal");
  if (modal) modal.style.display = "none";
}

function _renderRoomDetailBody(rec) {
  const esc = (s) =>
    String(s).replace(
      /[&<>"']/g,
      (c) =>
        ({
          "&": "&amp;",
          "<": "&lt;",
          ">": "&gt;",
          '"': "&quot;",
          "'": "&#39;",
        }[c]),
    );

  // Photos — larger thumbnails grouped by type
  const photoTypes = ["room", "fixture", "switch", "controls"];
  const photoLabels = {
    room: "Room",
    fixture: "Fixture",
    switch: "Switch",
    controls: "Controls",
  };
  let photosHtml = "";
  const anyPhotos = photoTypes.some(
    (t) =>
      typeof getPhotos === "function" &&
      getPhotos(`photo_${rec.roomName}_${t}`).length,
  );
  if (anyPhotos) {
    photosHtml = `<div style="margin-bottom:16px"><div style="font-size:11px;font-weight:700;color:#5a7a9a;text-transform:uppercase;letter-spacing:.08em;margin-bottom:8px">Photos</div><div style="display:flex;flex-wrap:wrap;gap:10px">`;
    photoTypes.forEach((t) => {
      const key = `photo_${rec.roomName}_${t}`;
      const photos = typeof getPhotos === "function" ? getPhotos(key) : [];
      if (!photos.length) return;
      photos.forEach((src, idx) => {
        photosHtml += `<div style="position:relative;width:120px;height:120px;border-radius:10px;overflow:hidden;border:1px solid #d8dfe8;cursor:pointer;background:#000" onclick="openPhotoLightbox('${key}',${idx})">
          <img src="${src}" alt="${photoLabels[t]}" style="width:100%;height:100%;object-fit:cover">
          <span style="position:absolute;left:4px;bottom:4px;background:rgba(0,0,0,.6);color:#fff;font-size:9px;font-weight:700;padding:1px 6px;border-radius:8px;text-transform:uppercase;letter-spacing:.06em">${photoLabels[t]}</span>
        </div>`;
      });
    });
    photosHtml += `</div></div>`;
  } else {
    photosHtml = `<div style="margin-bottom:16px;padding:10px 12px;background:#f7f9fc;border-radius:8px;border:1px dashed #c8d4e3;color:#7a8fa8;font-size:12px;font-style:italic">No photos captured for this room.</div>`;
  }

  // Existing conditions — fixtures + switches + controls from log entries
  let condHtml = "";
  if (rec.entries.length) {
    const fxRows = rec.entries
      .map((e) => {
        const desc = e.desc || e.fixtureId || "";
        const fixId = e.fixtureId || "";
        const mount = e.mountType ? ` · ${esc(e.mountType)}` : "";
        return `<div style="display:flex;align-items:center;gap:8px;padding:6px 10px;background:#fff;border:1px solid #e4eaf2;border-radius:6px"><span style="background:#e8f0fb;color:#1a4a80;font-size:10px;font-weight:800;padding:2px 7px;border-radius:8px;flex-shrink:0">×${
          e.qty || 1
        }</span><span style="font-size:12px;font-weight:600;color:#1a2a3a;flex:1;min-width:0">${esc(
          desc,
        )}${mount}</span><span style="font-size:10px;color:#7a8fa8">${esc(
          fixId,
        )}</span></div>`;
      })
      .join("");
    const sw = rec.first?.numSwitches || "";
    const ctrls = rec.first?.existingCtrls || "";
    condHtml = `<div style="margin-bottom:16px">
      <div style="font-size:11px;font-weight:700;color:#5a7a9a;text-transform:uppercase;letter-spacing:.08em;margin-bottom:8px">Existing Conditions</div>
      <div style="display:flex;flex-direction:column;gap:6px;margin-bottom:10px">${fxRows}</div>
      ${
        sw
          ? `<div style="font-size:12px;color:#3a5a7a;margin-bottom:5px"><b>Switches:</b> ${esc(
              String(sw),
            )}</div>`
          : `<div style="font-size:12px;color:#b03030;margin-bottom:5px"><b>Switches:</b> none recorded</div>`
      }
      ${
        ctrls
          ? `<div style="font-size:12px;color:#3a5a7a"><b>Existing Controls:</b> ${esc(
              String(ctrls),
            )}</div>`
          : `<div style="font-size:12px;color:#b03030"><b>Existing Controls:</b> none recorded</div>`
      }
    </div>`;
  } else {
    condHtml = `<div style="margin-bottom:16px;padding:10px 12px;background:#fef2f2;border-radius:8px;border:1px solid #fecaca;color:#991b1b;font-size:12px;font-weight:600">No fixtures logged — visit this room to capture data.</div>`;
  }

  // Light level summary
  let llHtml = "";
  const ll = _lightLevelsForRoom(rec.roomName);
  if (ll.length) {
    const byHeight = { Floor: [], "30 AFF": [], Surface: [] };
    ll.forEach((r) => {
      if (byHeight[r.measuredAt]) byHeight[r.measuredAt].push(r.fc);
    });
    const avg = (arr) =>
      arr.length
        ? Math.round((arr.reduce((a, b) => a + b, 0) / arr.length) * 10) / 10
        : null;
    llHtml = `<div style="margin-bottom:8px">
      <div style="font-size:11px;font-weight:700;color:#5a7a9a;text-transform:uppercase;letter-spacing:.08em;margin-bottom:8px">Pre-Install Footcandles</div>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        ${
          Object.entries(byHeight)
            .map(([h, arr]) => {
              if (!arr.length) return "";
              const a = avg(arr);
              return `<div style="flex:1;min-width:90px;background:#fff8e0;border:1px solid #c4a020;border-radius:8px;padding:8px 10px">
            <div style="font-size:9px;font-weight:700;color:#7a5a10;text-transform:uppercase;letter-spacing:.08em">${esc(
              h,
            )}</div>
            <div style="font-size:18px;font-weight:800;color:#1a2a3a">${a}<span style="font-size:11px;color:#7a8fa8;font-weight:600"> FC</span></div>
            <div style="font-size:9px;color:#8a7a30">${arr.length} sample${
                arr.length === 1 ? "" : "s"
              }</div>
          </div>`;
            })
            .filter(Boolean)
            .join("") ||
          '<div style="color:#7a8fa8;font-size:11px;font-style:italic">No FC measurements</div>'
        }
      </div>
    </div>`;
  }

  // Action buttons — wired via delegation in openRoomDetail to avoid any
  // inline-string-escaping pain with room names that contain apostrophes/quotes.
  const actionsHtml = `<div style="display:flex;gap:6px;padding-top:14px;border-top:1px solid #e4eaf2;margin-top:10px;flex-wrap:wrap">
    <button data-detail-action="edit" style="flex:1;padding:9px;background:#2563a8;color:#fff;border:none;border-radius:8px;font-size:12px;font-weight:700;cursor:pointer;font-family:inherit">✏️ Modify Room</button>
    ${
      rec.entries.length
        ? `<button data-detail-action="copy" style="flex:1;padding:9px;background:#7a3abd;color:#fff;border:none;border-radius:8px;font-size:12px;font-weight:700;cursor:pointer;font-family:inherit">📋 Copy Conditions</button>`
        : ""
    }
  </div>`;

  return photosHtml + condHtml + llHtml + actionsHtml;
}

function _lightLevelsForRoom(roomName) {
  const out = [];
  Object.values(floorPlans || {}).forEach((plan) => {
    (plan?.markup?.lightLevels || []).forEach((rec) => {
      if (rec.roomName === roomName) out.push(rec);
    });
  });
  return out;
}

function reviewDetailEdit(roomName) {
  // Find first log entry index for this room and reuse the existing modify path
  const idx = logEntries.findIndex((e) => e.space === roomName);
  if (idx < 0) {
    // No log entries yet — load via pin if one exists
    for (const planId in floorPlans) {
      const p = floorPlans[planId];
      const pin = (p?.pins || []).find((x) => x.roomName === roomName);
      if (pin) {
        closeRoomDetail();
        if (typeof loadRoomFromPin === "function") loadRoomFromPin(pin);
        if (typeof showForm === "function") showForm();
        return;
      }
    }
    toast("No data to modify");
    return;
  }
  closeRoomDetail();
  roomMenuIndex = idx;
  if (typeof roomMenuModify === "function") roomMenuModify();
}

function reviewDetailCopy(roomName) {
  const idx = logEntries.findIndex((e) => e.space === roomName);
  if (idx < 0) {
    toast("Nothing to copy yet");
    return;
  }
  closeRoomDetail();
  roomMenuIndex = idx;
  if (typeof roomMenuCopy === "function") roomMenuCopy();
}

// Track collapsed state per card index
const rcardCollapsed = {};

function rcardToggle(bri) {
  rcardCollapsed[bri] = !rcardCollapsed[bri];
  const card = document.getElementById("rcard-" + bri);
  if (!card) return;
  card.classList.toggle("collapsed", !!rcardCollapsed[bri]);
}

function renderZoneBody(entries, gi) {
  const ZDIM_SET = new Set(["ZDIM/LCB", "ZDIM/LCBS"]);
  const SS_VALS = new Set([
    "SS (Occ)",
    "SS (Vac)",
    "SS (Occ-DT)",
    "SS (Vac-DT)",
  ]);
  const SS_DISPLAY = {
    "SS (Occ)": "Wall Switch Occ Sensor",
    "SS (Vac)": "Wall Switch Vac Sensor",
    "SS (Occ-DT)": "Wall Switch Occ (DT)",
    "SS (Vac-DT)": "Wall Switch Vac (DT)",
  };

  // Fixture rows
  const fixtureRows = entries
    .map(({ e }) => {
      const displayDesc =
        e.desc || FIXTURES[e.fixtureId]?.desc || e.fixtureId || "";
      const shortDesc =
        displayDesc.length > 100
          ? displayDesc.substring(0, 100) + "…"
          : displayDesc;
      const isIS = !!e.intSensor,
        isEM = !!e.embb,
        isAR = !!e.airReturn,
        isWG = !!e.wireGuard;
      const descColor = isEM ? "#c04040" : isIS ? "#1a7a48" : "#1a2a3a";
      const qtyBg = isEM ? "#fdecea" : isIS ? "#dcfce7" : "#e8f0fb";
      const qtyColor = isEM ? "#c04040" : isIS ? "#1a7a48" : "#2563a8";
      const flagTags =
        (isIS
          ? '<span style="font-size:10px;font-weight:700;background:#dcfce7;color:#1a7a48;padding:1px 6px;border-radius:4px;margin-left:5px">⚡ Integral</span>'
          : "") +
        (isEM
          ? '<span style="font-size:10px;font-weight:700;background:#fdecea;color:#c04040;padding:1px 6px;border-radius:4px;margin-left:5px">🔋 EMBB</span>'
          : "") +
        (isAR
          ? '<span style="font-size:10px;font-weight:700;background:#e0f0ff;color:#1a5a8a;padding:1px 6px;border-radius:4px;margin-left:5px">💨 Air Return</span>'
          : "");
      const mountTag = e.mountType ? " - " + e.mountType : "";
      return (
        '<div class="fixture-row">' +
        '<span class="fixture-qty" style="background:' +
        qtyBg +
        ";color:" +
        qtyColor +
        '">&times;' +
        e.qty +
        "</span>" +
        '<span class="fixture-desc" style="color:' +
        descColor +
        ";font-weight:" +
        (isEM || isIS ? "600" : "400") +
        '">' +
        shortDesc +
        flagTags +
        mountTag +
        "</span>" +
        "</div>"
      );
    })
    .join("");

  const allFixRec = entries.flatMap(({ e }) => e.fixRec || []);
  const fixRecTags = allFixRec
    .map((c) => {
      const name = typeof c === "object" ? c.name : c;
      const qty = typeof c === "object" && c.qty > 1 ? " ×" + c.qty : "";
      return (
        '<span style="display:inline-block;background:#e6f7f6;color:#0a6b65;border:1px solid #a0d8bc;border-radius:5px;padding:2px 8px;font-size:11px;font-weight:700;margin:2px 3px 2px 0">' +
        name +
        qty +
        "</span>"
      );
    })
    .join("");
  const replacementRows = fixRecTags
    ? '<div style="padding:4px 0">' + fixRecTags + "</div>"
    : entries
        .map(
          ({ e }) =>
            '<div class="fixture-row" style="opacity:.5">' +
            '<span class="fixture-qty" style="background:#f1f5f9;color:#94a3b8">&times;' +
            e.qty +
            "</span>" +
            '<span class="fixture-desc" style="color:#94a3b8;font-style:italic">&mdash; pending &mdash;</span>' +
            "</div>",
        )
        .join("");

  // Switch
  const allCtrlRecs = entries.flatMap(({ e }) => e.ctrlRecs || []);
  const zdimRecs = allCtrlRecs.filter((c) =>
    ZDIM_SET.has(typeof c === "object" ? c.name : c),
  );
  const ctrlRecRecs = allCtrlRecs.filter(
    (c) => !ZDIM_SET.has(typeof c === "object" ? c.name : c),
  );
  const allSwRec = entries.flatMap(({ e }) => e.swRec || []);
  const ssSwRec = allSwRec.filter((s) =>
    SS_VALS.has(typeof s === "object" ? s.name : s),
  );

  const switchEntry = entries.find(
    ({ e }) => e.numSwitches && e.numSwitches.trim(),
  );
  const existingSwitches = switchEntry
    ? switchEntry.e.numSwitches
        .split(",")
        .map((s) => s.trim())
        .filter(Boolean)
    : [];
  const swNotesCard =
    switchEntry && switchEntry.e.switchNotes && switchEntry.e.switchNotes.length
      ? switchEntry.e.switchNotes
          .map(
            (n) =>
              '<span style="display:inline-block;background:#f0e8ff;color:#6a3aaa;border:1px solid #c8a8f0;border-radius:5px;padding:1px 7px;font-size:10px;font-weight:700;margin:2px 2px 0 0">' +
              n +
              "</span>",
          )
          .join("")
      : "";
  const swRecTags = [
    ...allSwRec.map((c) => {
      const name = typeof c === "object" ? c.name : c;
      const qty = typeof c === "object" && c.qty > 1 ? " ×" + c.qty : "";
      const display = SS_DISPLAY[name] || name;
      const icon = /wireless/i.test(name) ? "📶 " : "";
      return (
        '<span class="tag-proposed-sw">' + icon + display + qty + "</span>"
      );
    }),
    ...zdimRecs.map((c) => {
      const name = typeof c === "object" ? c.name : c;
      const qty = typeof c === "object" && c.qty > 1 ? " ×" + c.qty : "";
      return '<span class="tag-proposed-sw">' + name + qty + "</span>";
    }),
  ].join("");

  const switchHtml =
    '<div class="room-col-label">Existing Switch</div>' +
    (existingSwitches.length
      ? existingSwitches
          .map((s) => {
            const icon = /wireless|sensor\s*switch/i.test(s)
              ? '<span style="font-size:11px;margin-left:3px">📶</span>'
              : "";
            return '<span class="tag-existing">' + s + icon + "</span>";
          })
          .join("")
      : '<span class="tag-empty">&mdash;</span>') +
    (swNotesCard
      ? '<div style="margin-top:3px">' + swNotesCard + "</div>"
      : "") +
    '<div style="font-size:16px;color:#94a3b8;margin:4px 0;line-height:1">↓</div>' +
    '<div class="room-col-label">Replacement</div>' +
    (swRecTags || '<span class="tag-empty">&mdash; pending &mdash;</span>');

  // Controls
  const ecEntry = entries.find(
    ({ e }) => e.existingCtrls && e.existingCtrls.trim(),
  );
  const existingCtrls = ecEntry
    ? ecEntry.e.existingCtrls
        .split(",")
        .map((s) => s.trim())
        .filter(Boolean)
    : [];
  const combinedCtrlRecs = [...ctrlRecRecs, ...ssSwRec];
  const ctrlHtml =
    '<div class="room-col-label">Existing Controls</div>' +
    (existingCtrls.length
      ? existingCtrls
          .map((s) => {
            const icon = /^wireless\s/i.test(s)
              ? '<span style="font-size:11px;margin-left:3px">📶</span>'
              : /^low\s*voltage\s/i.test(s)
              ? '<span style="font-size:11px;margin-left:3px">⚡</span>'
              : /^hardwired\s/i.test(s)
              ? '<span style="font-size:11px;margin-left:3px">🔌</span>'
              : "";
            return '<span class="tag-existing">' + s + icon + "</span>";
          })
          .join("")
      : '<span class="tag-empty">&mdash;</span>') +
    '<div style="font-size:16px;color:#94a3b8;margin:4px 0;line-height:1">↓</div>' +
    '<div class="room-col-label">Control Rec</div>' +
    (combinedCtrlRecs.length
      ? combinedCtrlRecs
          .map((c) => {
            const name = typeof c === "object" ? c.name : c;
            const display = SS_DISPLAY[name] || name;
            const qty = typeof c === "object" && c.qty > 1 ? " ×" + c.qty : "";
            return (
              '<span class="tag-proposed-ctrl">' + display + qty + "</span>"
            );
          })
          .join("")
      : '<span class="tag-empty">&mdash; pending &mdash;</span>');

  return (
    '<div class="room-col fixtures"><div class="room-col-label">Fixtures</div>' +
    fixtureRows +
    "</div>" +
    '<div class="room-col-divider"></div>' +
    '<div class="room-col replacement"><div class="room-col-label">Replacement</div>' +
    replacementRows +
    "</div>" +
    '<div class="room-col-divider"></div>' +
    '<div class="room-col switches">' +
    switchHtml +
    "</div>" +
    '<div class="room-col-divider"></div>' +
    '<div class="room-col controls">' +
    ctrlHtml +
    "</div>"
  );
}

function renderPhotoSection(spaceName, gi) {
  return (
    '<div style="display:flex;gap:5px;align-items:center;padding:8px 14px;border-top:1px solid #e4eaf2">' +
    ["fixture", "controls", "switch"]
      .map((type) => {
        const photoKey = "photo_" + spaceName + "_" + type;
        const photos = getPhotos(photoKey);
        const label =
          type === "fixture" ? "Fix" : type === "controls" ? "Ctrl" : "Sw";
        const safeSpace = spaceName.replace(/'/g, "\\'");
        const hasPhotos = photos.length > 0;
        return (
          '<div class="photo-thumb' +
          (hasPhotos ? " has-photo" : "") +
          '" id="pp-' +
          gi +
          "-" +
          type +
          '" style="width:40px;height:40px;position:relative">' +
          (hasPhotos
            ? '<img src="' +
              photos[0] +
              '" alt="' +
              type +
              '" onclick="event.stopPropagation();openPhotoLightbox(\'' +
              photoKey +
              '\',0)" style="position:absolute;inset:0;width:100%;height:100%;object-fit:cover;border-radius:6px;cursor:pointer">' +
              (photos.length > 1
                ? '<span style="position:absolute;top:1px;right:1px;background:#2563a8;color:#fff;font-size:9px;font-weight:800;border-radius:8px;padding:0 4px;line-height:15px;pointer-events:none">' +
                  photos.length +
                  "</span>"
                : "") +
              '<label style="position:absolute;bottom:1px;right:1px;background:rgba(0,0,0,.5);color:#fff;font-size:9px;border-radius:4px;padding:1px 4px;cursor:pointer;line-height:1.4">+' +
              '<input type="file" accept="image/*,video/*" multiple style="position:absolute;opacity:0;width:1px;height:1px;pointer-events:none" onchange="handleRoomPhoto(this,\'' +
              safeSpace +
              "','" +
              type +
              "'," +
              gi +
              ')">' +
              "</label>"
            : '<span class="photo-thumb-icon" style="font-size:16px">📷</span>' +
              '<span class="photo-thumb-lbl">' +
              label +
              "</span>" +
              '<input type="file" accept="image/*,video/*" multiple style="position:absolute;inset:0;opacity:0;cursor:pointer;width:100%;height:100%" onchange="handleRoomPhoto(this,\'' +
              safeSpace +
              "','" +
              type +
              "'," +
              gi +
              ')">') +
          "</div>"
        );
      })
      .join("") +
    "</div>"
  );
}

function renderMetricsSection(allEntries, first) {
  const fakeGroup = { entries: allEntries, roomName: first.space };
  const allSwRec = allEntries.flatMap(({ e }) => e.swRec || []);
  const metricsHtml = buildRoomMetrics(fakeGroup, allSwRec);
  return (
    '<div class="room-card-metrics"><div style="flex:1;display:flex;flex-wrap:wrap">' +
    metricsHtml +
    "</div></div>"
  );
}

function rcardCollapseAll() {
  const cards = document.querySelectorAll(".room-card");
  const anyExpanded = [...cards].some(
    (c) => !c.classList.contains("collapsed"),
  );
  cards.forEach((card, i) => {
    const gi = parseInt(card.dataset.gi);
    rcardCollapsed[gi] = anyExpanded;
    card.classList.toggle("collapsed", anyExpanded);
  });
  const btn = document.getElementById("collapseAllBtn");
  if (btn) btn.textContent = anyExpanded ? "⊞ Expand All" : "⊟ Collapse All";
}

function rcardModify(i) {
  roomMenuIndex = i;
  roomMenuModify();
}

function rcardCopy(i) {
  roomMenuIndex = i;
  roomMenuCopy();
}

function rcardDelete(spaceNameOrGi) {
  // Support both spaceName string (room view) and gi integer (data table)
  let key, roomName;
  if (typeof spaceNameOrGi === "string") {
    const entry = logEntries.find((e) => e.space === spaceNameOrGi);
    if (!entry) return;
    key = entry.facility + "|" + entry.floor + "|" + entry.space;
    roomName = spaceNameOrGi;
  } else {
    const groups = groupLogByRoom();
    const group = groups[spaceNameOrGi];
    if (!group) return;
    key = group.key;
    roomName = group.roomName;
  }
  if (!confirm('Delete all entries for "' + roomName + '"?')) return;
  logEntries = logEntries.filter(
    (e) => e.facility + "|" + e.floor + "|" + e.space !== key,
  );
  logEntries.forEach((e, i) => (e.line = i + 1));
  updateLogBadge();
  scheduleSave();
  renderRoomCards();
  renderLogStats();
}

function showForm() {
  document.getElementById("mainApp").style.display = "flex";
  document.getElementById("logPanel").classList.remove("visible");
  // Restore portrait orientation
  try {
    if (screen.orientation && screen.orientation.unlock) {
      screen.orientation.unlock();
    }
  } catch (e) {}
}

// Map ctrl rec button value → Fixture ID shown in log/export
function ctrlFixtureId(ctrl) {
  if (["PIR12WX2", "PIR12WX3", "PIR12WX4"].includes(ctrl)) return "PIR12W";
  if (["PowPack", "PowPack X2", "PowPack X3", "PowPack X4"].includes(ctrl))
    return "LCBLUECONTROL/W";
  return ctrl;
}

// Parse quantity from ctrl rec code
// Switch-to-quantity map for ZDIM/LCB
const ZDIM_SWITCH_QTY = {
  Toggle: 1,
  "Wireless Switch": 1,
  Rocker: 1,
  Pico: 1,
  "1P/Socket": 1,
  "1P/SocketX2": 2,
  Keyed: 1,
  Panel: 1,
};

function ctrlQty(ctrl, switchType) {
  // Parse trailing number from code (e.g. PIR12WX3 → 3, PowPack X2 → 2)
  const m = ctrl.match(/[Xx](\d+)\s*$/) || ctrl.match(/-(\d+)\s*$/);
  return m ? parseInt(m[1]) : 1;
}

// ── TABLE TAB SWITCHING ───────────────────────────────────────────────────
let currentTableTab = "full";
let _fullHeaderHTML = null; // cached on first render

function setTableTab(tab) {
  currentTableTab = tab;
  document.getElementById("tblTabFull").style.borderBottomColor =
    tab === "full" ? "#2563a8" : "transparent";
  document.getElementById("tblTabField").style.borderBottomColor =
    tab === "field" ? "#2563a8" : "transparent";
  document.getElementById("tblTabFull").style.color =
    tab === "full" ? "#2563a8" : "#8a9ab0";
  document.getElementById("tblTabField").style.color =
    tab === "field" ? "#2563a8" : "#8a9ab0";

  const hdr = document.getElementById("xlHeaderRow");
  if (hdr) {
    if (tab === "field") {
      // Cache the full header on first switch away from it
      if (!_fullHeaderHTML) _fullHeaderHTML = hdr.innerHTML;
      const C = "#1a3a6b",
        P = "#3b1060";
      hdr.innerHTML = `
        <th style="background:${C};color:#fff">#</th>
        <th style="background:${C};color:#fff">Facility</th>
        <th style="background:${C};color:#fff">Floor</th>
        <th style="background:${C};color:#fff;min-width:120px">Room Name</th>
        <th style="background:${P};color:#fff">Int/Ctrl</th>
        <th style="background:${P};color:#fff;min-width:180px">Fixture ID</th>
        <th style="background:${P};color:#fff;text-align:center">Qty</th>
        <th style="background:${P};color:#fff;min-width:180px">Replacement</th>
        <th style="background:${P};color:#fff;text-align:center">New Qty</th>
        <th style="background:#1a3a6b;color:#fff;min-width:180px">Notes</th>`;
    } else if (_fullHeaderHTML) {
      hdr.innerHTML = _fullHeaderHTML;
    }
  }
  renderLogTable();
}

// ── COLLAPSIBLE COLUMN GROUPS ─────────────────────────────────────────────
const colGroupOpen = { a: false, b: false, c: false, d: false, e: false };
function toggleColGroup(grp) {
  colGroupOpen[grp] = !colGroupOpen[grp];
  const open = colGroupOpen[grp];
  document.querySelectorAll(`.col-grp-${grp}`).forEach((th) => {
    th.style.display = open ? "none" : "";
  });
  document.querySelectorAll(`[data-grp="${grp}"]`).forEach((th) => {
    th.style.display = open ? "" : "none";
  });
  document.querySelectorAll(`td[data-grp="${grp}"]`).forEach((td) => {
    td.style.display = open ? "" : "none";
  });
  document.querySelectorAll(`td.col-grp-hdr-${grp}`).forEach((td) => {
    td.style.display = open ? "none" : "";
  });
  const label = document.getElementById(`grp-${grp}-label`);
  if (label) {
    const names = {
      a: "Room Info",
      b: "Watts",
      c: "kW",
      d: "Controls Energy",
      e: "Codes & Notes",
    };
    label.textContent = (open ? "\u25b8 " : "\u25be ") + names[grp];
  }
}

function renderLogTable() {
  const empty = document.getElementById("logEmpty");
  const table = document.getElementById("xlTable");
  const tbody = document.getElementById("xlBody");
  if (logEntries.length === 0) {
    empty.style.display = "block";
    table.style.display = "none";
    return;
  }
  empty.style.display = "none";
  table.style.display = "";

  const cv = (v) => (v !== null && v !== undefined && v !== "" ? v : "");
  const isField = currentTableTab === "field";

  const grpCell = (grp, content, style = "") => {
    const open = colGroupOpen[grp];
    return `<td data-grp="${grp}" style="display:${
      open ? "" : "none"
    };${style}">${content}</td>`;
  };
  const grpHdrCell = (grp, colspan, style = "") => {
    const open = colGroupOpen[grp];
    return `<td class="col-grp-hdr-${grp}" colspan="${colspan}" style="display:${
      open ? "none" : ""
    };background:#e8edf4;${style}"></td>`;
  };

  const groups = [];
  logEntries.forEach((e, i) => {
    const key = `${e.facility}|${e.floor}|${e.space}`;
    const last = groups[groups.length - 1];
    if (last && last.key === key) last.entries.push({ e, i });
    else groups.push({ key, entries: [{ e, i }] });
  });

  const pinLookup = {};
  Object.values(floorPlans).forEach((plan) => {
    (plan.pins || []).forEach((pin) => {
      if (pin.roomName && !(pin.roomName in pinLookup))
        pinLookup[pin.roomName] = pin.pinNum;
    });
  });

  const SS_DISPLAY = {
    "SS (Occ)": "Wall Switch Occupancy Sensor",
    "SS (Vac)": "Wall Switch Vacancy Sensor",
    "SS (Occ-DT)": "Wall Switch Occupancy Sensor (Dual-Tech)",
    "SS (Vac-DT)": "Wall Switch Vacancy Sensor (Dual-Tech)",
  };
  const SS_VALS_TBL = new Set([
    "SS (Occ)",
    "SS (Vac)",
    "SS (Occ-DT)",
    "SS (Vac-DT)",
  ]);

  tbody.innerHTML = groups
    .map((group) => {
      const roomName = group.entries[0]?.e?.space || "";
      const pinNum = pinLookup[roomName];
      const fe = group.entries[0].e;
      const fi = group.entries[0].i;
      const ht0 = fe.height ? fe.height + "'" : "";

      const pinCell =
        pinNum !== undefined
          ? `<td style="text-align:center;font-weight:700;color:#2563a8">${pinNum}</td>`
          : `<td style="text-align:center"><span class="set-pin-link" data-room="${roomName.replace(
              /"/g,
              "&quot;",
            )}" style="cursor:pointer;color:#e07020;font-size:10px;font-weight:700;text-decoration:underline">Pin</span></td>`;

      // ── FIXTURE ROWS ────────────────────────────────────────────────────
      const fixRecAll = group.entries.flatMap(({ e }) => e.fixRec || []);
      const fixRecDesc = fixRecAll
        .map((s) => {
          const name = typeof s === "object" ? s.name : s;
          const qty = typeof s === "object" && s.qty > 1 ? " ×" + s.qty : "";
          return name + qty;
        })
        .join(", ");
      const fixRecQty = fixRecAll.reduce(
        (sum, s) => sum + (typeof s === "object" ? s.qty : 1),
        0,
      );
      const fixtureRows = group.entries
        .map(({ e, i }, rowIdx) => {
          const ht = e.height ? e.height + "'" : "";
          const fixtureDesc =
            e.desc || FIXTURES[e.fixtureId]?.desc || e.fixtureId || "";
          // Use shared abbreviation helper so the data-table view and the XLSX export
          // stay in sync. Mount included here (export pulls it into a separate column).
          const flags = composeAttrSuffix(e, true);
          const showRec = rowIdx === 0 && fixRecDesc;
          const recCell = showRec
            ? `<td style="color:#0a6b65;font-weight:600">${fixRecDesc}</td>`
            : `<td style="color:#94a3b8;font-style:italic">— pending —</td>`;
          const recQtyCell = showRec
            ? `<td style="text-align:center;color:#0a6b65;font-weight:600">${
                fixRecQty || ""
              }</td>`
            : `<td style="text-align:center"></td>`;
          const flagNote = flags ? ` - ${flags}` : "";
          const sqft = (() => {
            const m = (e.dimensions || "").match(
              /([\d.]+)'\s*[×x]\s*([\d.]+)'/,
            );
            return m
              ? Math.round(parseFloat(m[1]) * parseFloat(m[2])).toLocaleString()
              : "";
          })();

          if (isField)
            return `<tr>
        <td>${e.line}</td>
        <td>${cv(e.facility)}</td>
        <td>${cv(e.floor)}</td>
        <td><span data-idx="${i}" class="room-link" style="cursor:pointer;color:#2563a8;text-decoration:underline;font-weight:600">${cv(
              e.space,
            )}</span></td>
        <td style="font-weight:600;color:#1a6a35">Fixture</td>
        <td style="font-weight:600;color:#1a2a3a;white-space:normal">${fixtureDesc}${flagNote}</td>
        <td style="text-align:center">${cv(e.qty)}</td>
        ${recCell}
        ${recQtyCell}
      </tr>`;

          const fixtureW = FIXTURES[e.fixtureId]?.watts || e.watts || 0;
          const exW = fixtureW ? fixtureW : "";
          const exKw =
            fixtureW && e.qty
              ? ((fixtureW * (e.qty || 1)) / 1000).toFixed(3)
              : "";
          const bhrs =
            e.baselineHours || fe.baselineHours || bldgData.hours || 0;

          return `<tr>
        <td>${e.line}</td>
        <td>${cv(e.facility)}</td>
        <td>${cv(e.floor)}</td>
        <td><span data-idx="${i}" class="room-link" style="cursor:pointer;color:#2563a8;text-decoration:underline;font-weight:600">${cv(
            e.space,
          )}</span></td>
        ${rowIdx === 0 ? pinCell : "<td></td>"}
        <td style="text-align:center">${cv(
          fe.baselineHours || bldgData.hours || "",
        )}</td>
        <td style="text-align:center">${ht}</td>
        ${grpHdrCell("a", 4)}
        ${grpCell("a", cv(e.roomType))}${grpCell(
            "a",
            cv(e.dimensions),
          )}${grpCell("a", sqft)}${grpCell("a", cv(e.ceilingType))}
        <td style="color:#1a6a35;font-weight:600">Fixture</td>
        <td style="font-weight:600;color:#1a2a3a;white-space:normal;max-width:200px">${fixtureDesc}${flagNote}</td>
        <td style="color:#6a3aaa;font-weight:600">${e.mountType || ""}</td>
        <td style="text-align:center">${cv(e.qty)}</td>
        ${recCell}
        ${recQtyCell}
        <td style="font-size:11px;color:#5a7a9a;white-space:normal;max-width:200px">${cv(
          e.notes,
        )}</td>
        <td><button class="del-row" onclick="confirmDeleteEntry(${i},this)">✕</button></td>
      </tr>`;
        })
        .join("");

      // ── SWITCH ROW (descriptive only) ──────────────────────────────────
      const existingSwitchStr =
        fe.numSwitches && fe.numSwitches.trim() ? fe.numSwitches : "";
      const swNotes =
        fe.switchNotes && fe.switchNotes.length ? fe.switchNotes : [];
      const swNotesSuffix = swNotes.length
        ? ` <span style="color:#6a3aaa;font-size:10px;font-weight:700">[${swNotes.join(
            ", ",
          )}]</span>`
        : "";
      const swNotesPlain = swNotes.length ? ` [${swNotes.join(", ")}]` : "";
      const isNoneSwitch =
        !existingSwitchStr || existingSwitchStr.trim().toLowerCase() === "none";
      let existingSwitchQty = 0;
      if (!isNoneSwitch)
        existingSwitchStr.split(",").forEach((s) => {
          const m = s.trim().match(/X(\d+)$/i);
          existingSwitchQty += m ? parseInt(m[1]) : 1;
        });
      const swRecAll = group.entries.flatMap(({ e }) => e.swRec || []);
      const swRecDesc = swRecAll
        .map((s) => {
          const name = typeof s === "object" ? s.name : s;
          const qty = typeof s === "object" && s.qty > 1 ? " ×" + s.qty : "";
          return (SS_DISPLAY[name] || name) + qty;
        })
        .join(", ");
      const swRecQty = swRecAll.reduce(
        (sum, s) => sum + (typeof s === "object" ? s.qty : 1),
        0,
      );
      const emptyGrps = `${grpHdrCell("b", 4)}${grpCell("b", "")}${grpCell(
        "b",
        "",
      )}${grpCell("b", "")}${grpCell("b", "")}${grpHdrCell("c", 4)}${grpCell(
        "c",
        "",
      )}${grpCell("c", "")}${grpCell("c", "")}${grpCell("c", "")}`;

      const switchRow = isField
        ? `<tr class="ctrl-row" style="background:#fff8f0">
          <td style="color:#c07020;font-size:10px">↳</td>
          <td>${cv(fe.facility)}</td>
          <td>${cv(fe.floor)}</td>
          <td><span data-idx="${fi}" class="room-link" style="cursor:pointer;color:#2563a8;text-decoration:underline;font-weight:600">${cv(
            fe.space,
          )}</span></td>
          <td style="color:#c07020;font-weight:600">Switch</td>
          <td style="font-weight:600;color:#c07020">${
            isNoneSwitch ? "None" : existingSwitchStr
          }${swNotesSuffix}</td>
          <td style="text-align:center;color:#c07020">${
            isNoneSwitch ? 0 : existingSwitchQty || ""
          }</td>
          <td style="color:${swRecDesc ? "#1a4a80" : "#94a3b8"};font-style:${
            swRecDesc ? "normal" : "italic"
          }">${swRecDesc || "— pending —"}</td>
          <td style="text-align:center;color:#1a4a80">${swRecQty || ""}</td>
          <td style="font-size:11px;color:#6a3aaa;white-space:normal">${
            swNotesPlain || ""
          }</td><td></td>
    </tr>`
        : `<tr class="ctrl-row" style="background:#fff8f0">
      <td style="color:#c07020;font-size:10px">↳</td>
      <td>${cv(fe.facility)}</td><td>${cv(fe.floor)}</td>
      <td><span data-idx="${fi}" class="room-link" style="cursor:pointer;color:#2563a8;text-decoration:underline;font-weight:600">${cv(
            fe.space,
          )}</span></td>
      <td></td><td></td><td>${ht0}</td>
      ${grpHdrCell("a", 4)}${grpCell("a", "")}${grpCell("a", "")}${grpCell(
            "a",
            "",
          )}${grpCell("a", "")}
      <td style="color:#c07020;font-weight:600">Switch</td>
      <td style="font-weight:600;color:#c07020">${
        isNoneSwitch ? "None" : existingSwitchStr
      }${swNotesSuffix}</td><td></td>
      <td style="text-align:center;color:#c07020">${
        isNoneSwitch ? 0 : existingSwitchQty || ""
      }</td>
      <td style="color:${swRecDesc ? "#1a4a80" : "#94a3b8"};font-style:${
            swRecDesc ? "normal" : "italic"
          }">${swRecDesc || "— pending —"}</td>
      <td style="text-align:center;color:#1a4a80">${swRecQty || ""}</td>
      <td style="font-size:11px;color:#6a3aaa;white-space:normal">${
        swNotesPlain || ""
      }</td>
      <td></td>
    </tr>`;

      // ── CTRL ROWS (descriptive only) ───────────────────────────────────
      const existingCtrlsRaw = fe.existingCtrls
        ? fe.existingCtrls
            .split(",")
            .map((s) => s.trim())
            .filter(Boolean)
        : [];
      const allCtrlRecs = group.entries.flatMap(({ e }) => e.ctrlRecs || []);
      const ssFromSwRec = swRecAll.filter((s) =>
        SS_VALS_TBL.has(typeof s === "object" ? s.name : s),
      );
      const allCtrlRecsWithSS = [...allCtrlRecs, ...ssFromSwRec];
      const emptyGrpsAll = "";

      const buildCtrlRow = (exCtrlRaw, rec) => {
        const exCtrlMatch = exCtrlRaw.match(/^(.*?)\s+X(\d+)$/i);
        const exCtrlName = exCtrlMatch ? exCtrlMatch[1].trim() : exCtrlRaw;
        const exCtrlQty =
          exCtrlRaw.toLowerCase() === "none"
            ? 0
            : exCtrlMatch
            ? parseInt(exCtrlMatch[2])
            : exCtrlRaw
            ? 1
            : "";
        const isNoneCtrl = exCtrlRaw.toLowerCase() === "none";
        const recRaw = rec ? (typeof rec === "object" ? rec.name : rec) : "";
        const isSS = SS_VALS_TBL.has(recRaw);
        const recName = isSS ? "See Switch Row" : SS_DISPLAY[recRaw] || recRaw;
        const recQty = isSS
          ? 0
          : rec && typeof rec === "object" && rec.qty > 1
          ? rec.qty
          : rec
          ? 1
          : "";
        const isExistingSensor = SENSOR_SWITCH_VALS.has(exCtrlName);
        const exDisplayName = isExistingSensor
          ? "See Switch Row"
          : exCtrlName || "";
        const exDisplayQty = isExistingSensor
          ? 0
          : isSS
          ? ""
          : isNoneCtrl
          ? 0
          : exCtrlQty;
        return `<tr class="ctrl-row"><td style="color:#9ab0c8;font-size:10px">↳</td>
        <td>${cv(fe.facility)}</td><td>${cv(fe.floor)}</td>
        <td><span data-idx="${fi}" class="room-link" style="cursor:pointer;color:#2563a8;text-decoration:underline;font-weight:600">${cv(
          fe.space,
        )}</span></td>
        <td></td><td></td><td>${ht0}</td>
        ${grpHdrCell("a", 4)}${grpCell("a", "")}${grpCell("a", "")}${grpCell(
          "a",
          "",
        )}${grpCell("a", "")}
        <td style="color:#2563a8;font-weight:600">Controls</td>
        <td style="font-weight:600;color:${
          isExistingSensor ? "#94a3b8" : "#2563a8"
        };font-style:${
          isExistingSensor ? "italic" : "normal"
        }">${exDisplayName}</td><td></td>
        <td style="text-align:center;color:${
          isExistingSensor ? "#94a3b8" : "#2563a8"
        }">${exDisplayQty}</td>
        <td style="color:${
          recRaw && !isSS ? "#1a4a80" : "#94a3b8"
        };font-style:${recRaw && !isSS ? "normal" : "italic"}">${
          recName || "— pending —"
        }</td>
        <td style="text-align:center;color:${isSS ? "#94a3b8" : "#1a4a80"}">${
          recQty !== "" ? recQty : ""
        }</td>
        <td></td><td></td>
      </tr>`;
      };

      let ctrlRows = "";
      if (isField) {
        // Field view: show ctrl rows with same 9-col structure
        if (existingCtrlsRaw.length === 0 && allCtrlRecsWithSS.length === 0) {
          ctrlRows = `<tr class="ctrl-row"><td style="color:#9ab0c8;font-size:10px">↳</td>
          <td>${cv(fe.facility)}</td><td>${cv(fe.floor)}</td>
          <td><span data-idx="${fi}" class="room-link" style="cursor:pointer;color:#2563a8;text-decoration:underline;font-weight:600">${cv(
            fe.space,
          )}</span></td>
          <td style="color:#2563a8;font-weight:600">Controls</td>
          <td style="color:#94a3b8;font-style:italic">None</td>
          <td style="text-align:center;color:#94a3b8">0</td>
          <td style="color:#94a3b8;font-style:italic">— pending —</td>
          <td></td></tr>`;
        } else {
          const maxRows = Math.max(
            existingCtrlsRaw.length,
            allCtrlRecsWithSS.length,
            1,
          );
          ctrlRows = Array.from({ length: maxRows }, (_, idx) => {
            const exCtrlRaw = existingCtrlsRaw[idx] || "";
            const exCtrlMatch = exCtrlRaw.match(/^(.*?)\s+X(\d+)$/i);
            const exCtrlName = exCtrlMatch ? exCtrlMatch[1].trim() : exCtrlRaw;
            const exCtrlQty =
              exCtrlRaw.toLowerCase() === "none"
                ? 0
                : exCtrlMatch
                ? parseInt(exCtrlMatch[2])
                : exCtrlRaw
                ? 1
                : "";
            const rec = allCtrlRecsWithSS[idx];
            const recRaw = rec
              ? typeof rec === "object"
                ? rec.name
                : rec
              : "";
            const isSS = SS_VALS_TBL.has(recRaw);
            const recName = isSS
              ? "See Switch Row"
              : SS_DISPLAY[recRaw] || recRaw;
            const recQty = isSS
              ? 0
              : rec && typeof rec === "object" && rec.qty > 1
              ? rec.qty
              : rec
              ? 1
              : "";
            const isExistingSensor = SENSOR_SWITCH_VALS.has(exCtrlName);
            const exDisplayName = isExistingSensor
              ? "See Switch Row"
              : exCtrlName || "";
            const exDisplayQty = isExistingSensor ? 0 : exCtrlQty;
            return `<tr class="ctrl-row"><td style="color:#9ab0c8;font-size:10px">↳</td>
            <td>${cv(fe.facility)}</td><td>${cv(fe.floor)}</td>
            <td><span data-idx="${fi}" class="room-link" style="cursor:pointer;color:#2563a8;text-decoration:underline;font-weight:600">${cv(
              fe.space,
            )}</span></td>
            <td style="color:#2563a8;font-weight:600">Controls</td>
            <td style="font-weight:600;color:${
              isExistingSensor ? "#94a3b8" : "#2563a8"
            };font-style:${
              isExistingSensor ? "italic" : "normal"
            }">${exDisplayName}</td>
            <td style="text-align:center;color:${
              isExistingSensor ? "#94a3b8" : "#2563a8"
            }">${exDisplayQty}</td>
            <td style="color:${
              recRaw && !isSS ? "#1a4a80" : "#94a3b8"
            };font-style:${recRaw && !isSS ? "normal" : "italic"}">${
              recName || "— pending —"
            }</td>
            <td style="text-align:center;color:#1a4a80">${
              recQty !== "" ? recQty : ""
            }</td></tr>`;
          }).join("");
        }
      } else if (
        existingCtrlsRaw.length === 0 &&
        allCtrlRecsWithSS.length === 0
      ) {
        ctrlRows = buildCtrlRow("None", null);
      } else {
        const maxRows = Math.max(
          existingCtrlsRaw.length,
          allCtrlRecsWithSS.length,
          1,
        );
        ctrlRows = Array.from({ length: maxRows }, (_, idx) =>
          buildCtrlRow(existingCtrlsRaw[idx] || "", allCtrlRecsWithSS[idx]),
        ).join("");
      }

      // ── ROOM SUMMARY ROW — removed, handled in spec engine ──────────────────
      const summaryRow = "";

      return fixtureRows + switchRow + ctrlRows;
    })
    .join("");

  tbody.querySelectorAll(".room-link").forEach((el) => {
    el.addEventListener("click", function (ev) {
      showRoomMenu(ev, parseInt(this.dataset.idx));
    });
  });
  tbody.querySelectorAll(".set-pin-link").forEach((el) => {
    el.addEventListener("click", function () {
      setLogRoomPin(this.dataset.room);
    });
  });
}

// ── ROOM CONTEXT MENU ────────────────────────────────────────────────────
let roomMenuIndex = null;
let logNameCheckPending = false; // set by Copy to new room

function showRoomMenu(event, i) {
  event.stopPropagation();
  roomMenuIndex = i;
  const e = logEntries[i];
  const roomName = e ? e.space : "";
  const menu = document.getElementById("roomMenu");
  const overlay = document.getElementById("roomMenuOverlay");
  document.getElementById("roomMenuLabel").textContent = roomName;
  const x = Math.min(event.clientX, window.innerWidth - 180);
  const y = Math.min(event.clientY + 8, window.innerHeight - 120);
  menu.style.left = x + "px";
  menu.style.top = y + "px";
  menu.style.display = "block";
  overlay.style.display = "block";
}

function closeRoomMenu() {
  document.getElementById("roomMenu").style.display = "none";
  document.getElementById("roomMenuOverlay").style.display = "none";
  roomMenuIndex = null;
}

// Close room menu on Escape or any click outside
document.addEventListener("keydown", (e) => {
  if (e.key === "Escape") closeRoomMenu();
});
document.addEventListener("click", () => {
  if (document.getElementById("roomMenu")?.style.display !== "none")
    closeRoomMenu();
});

function roomMenuCopy() {
  const i = roomMenuIndex;
  closeRoomMenu();
  if (i !== null) {
    importRoomConditions(i);
    logNameCheckPending = true; // one-time name check on next log
  }
}

function roomMenuModify() {
  const i = roomMenuIndex;
  closeRoomMenu();
  if (i === null) return;
  const e = logEntries[i];
  if (!e) return;
  const roomName = e.space;

  // Load all conditions INCLUDING the room name
  importRoomConditions(i);

  // Override: put the room name back (copy clears it, modify keeps it)
  document.getElementById("roomInput").value = roomName;
  updateRoomDisplay();
  state.room = roomName;
  updateSummary();

  // Remove all existing log entries for this room so re-logging replaces them
  logEntries = logEntries.filter((entry) => entry.space !== roomName);
  logEntries.forEach((entry, idx) => (entry.line = idx + 1));
  updateLogBadge();

  toast(`✏️ "${roomName}" loaded for editing — modify and re-log`);
}

function confirmDeleteEntry(i, btn, ci) {
  if (btn.dataset.confirm === "1") {
    deleteEntry(i, ci);
  } else {
    btn.dataset.confirm = "1";
    btn.textContent = "Delete?";
    btn.style.background = "#c43030";
    btn.style.color = "#fff";
    btn.style.borderRadius = "5px";
    btn.style.padding = "2px 6px";
    setTimeout(() => {
      if (btn.dataset.confirm === "1") {
        btn.dataset.confirm = "";
        btn.textContent = "✕";
        btn.style.background = "";
        btn.style.color = "#c04040";
        btn.style.padding = "2px 4px";
      }
    }, 3000);
  }
}

function importRoomConditions(i, overrideName) {
  const e = logEntries[i];
  if (!e) return;

  showForm();
  clearFixtures();

  // Restore height
  if (e.height) {
    state.height = e.height;
    updateHeightBtn(e.height);
  }

  // Restore room type
  if (e.roomType) {
    state.roomtype = e.roomType;
    // Update the popover trigger button label
    const rtOpts = {
      "Class rooms": "Class",
      Office: "Office",
      Restroom: "Restroom",
      Closet: "Closet",
      Storage: "Storage",
      Hallway: "Hall",
      Stairwell: "Stair",
      "Slop Sink": "Slop Sink",
      Gym: "Gym",
      Mechanical: "Mech",
      Cafeteria: "Cafe",
      Library: "Library",
    };
    const label = rtOpts[e.roomType] || e.roomType;
    const lbl = document.getElementById("roomTypeBtnLabel");
    if (lbl) {
      lbl.textContent = label;
      lbl.style.color = "#1a2a3a";
    }
    const btn = document.getElementById("roomTypeBtn");
    if (btn) {
      btn.style.borderColor = "#7a3abd";
      btn.style.background = "#f3eeff";
    }
  }

  // Restore switch type
  if (e.numSwitches) {
    // Parse "Toggle X3" → {name:"Toggle", qty:3} or "Rocker" → {name:"Rocker", qty:1}
    state.switchType = e.numSwitches
      .split(", ")
      .filter(Boolean)
      .map((s) => {
        const m = s.match(/^(.*?)\s+X(\d+)$/i);
        return m
          ? { name: m[1].trim(), qty: parseInt(m[2]) }
          : { name: s.trim(), qty: 1 };
      });
    document.querySelectorAll('.btn[data-g="switch"]').forEach((b) => {
      if (!b.dataset.v) return;
      b.classList.toggle(
        "active",
        state.switchType.some((s) => s.name === b.dataset.v),
      );
    });
    renderSwitchQtyPanel();
  }

  // Restore existing controls — parse "W Occ X2" → {name:"W Occ", qty:2}
  const exCtrls = e.existingCtrls
    ? e.existingCtrls
        .split(", ")
        .filter(Boolean)
        .map((s) => {
          const m = s.match(/^(.*?)\s+X(\d+)$/i);
          return m
            ? { name: m[1].trim(), qty: parseInt(m[2]) }
            : { name: s.trim(), qty: 1 };
        })
    : [];
  state.existingCtrls = exCtrls;
  document.querySelectorAll("#existingCtrlBtns .btn").forEach((b) => {
    b.classList.toggle(
      "active",
      exCtrls.some((x) => x.name === b.dataset.v),
    );
  });
  renderExistingCtrlQtyPanel();

  // Restore control recommendations
  state.ctrls = (e.ctrlRecs || []).map((c) =>
    typeof c === "object" ? { ...c } : { name: c, qty: 1 },
  );
  document.querySelectorAll("#ctrlBtns .btn").forEach((b) => {
    b.classList.toggle(
      "active",
      state.ctrls.some((c) => c.name === b.dataset.v),
    );
  });
  renderCtrlQtyPanel();

  // Restore switch recommendation
  state.swRec = (e.swRec || []).map((s) =>
    typeof s === "object" ? { ...s } : { name: s, qty: 1 },
  );
  document.querySelectorAll('#swRecBtns .btn[data-g="swrec"]').forEach((b) => {
    b.classList.toggle(
      "active",
      state.swRec.some((s) => s.name === b.dataset.v),
    );
  });
  renderSwRecQtyPanel();

  // Restore fixture recommendation
  state.fixRec = (e.fixRec || []).map((s) =>
    typeof s === "object" ? { ...s } : { name: s, qty: 1 },
  );
  document
    .querySelectorAll('#fixRecBtns .btn[data-g="fixrec"]')
    .forEach((b) => {
      b.classList.toggle(
        "active",
        state.fixRec.some((s) => s.name === b.dataset.v),
      );
    });
  renderFixRecQtyPanel();

  // Restore ALL fixture rows for this room (main rows only, not ctrl sub-rows)
  const roomEntries = logEntries.filter(
    (r) => r.space === e.space && r.fixtureId && r.qty,
  );
  roomEntries.forEach((r) => {
    // Ensure fixture is registered
    if (!FIXTURES[r.fixtureId]) {
      // Re-register the fixture in the lookup, preserving its catalog source
      // (interior vs exterior) so the Attribute/Mount modals route correctly
      // when the fixture is loaded for editing.
      const t = r.fixtureType || "Interior";
      if (r.fixtureId.startsWith("FB:")) {
        FIXTURES[r.fixtureId] = {
          desc: r.fixtureId.replace(/^FB:/, "") || "",
          watts: null,
          type: t,
        };
      } else {
        const match = MATCH_DATA.find((m) => m.id === r.fixtureId);
        if (match)
          FIXTURES[r.fixtureId] = {
            desc: match.existing || match.desc || "",
            watts: null,
            type: t,
          };
        else FIXTURES[r.fixtureId] = { desc: "", watts: null, type: t };
      }
    }
    const base = FIXTURES[r.fixtureId];
    const fxDesc = r.fixtureId.startsWith("FB:")
      ? base.desc || r.fixtureId.replace(/^FB:/, "")
      : base.desc;
    if (r.isNewAddition) base._newAddition = true;
    state.fixtures.push({
      code: r.fixtureId,
      qty: r.qty,
      desc: fxDesc,
      watts: base.watts,
      type: r.fixtureType || base.type,
      intSensor: !!r.intSensor,
      embb: !!r.embb,
      airReturn: !!r.airReturn,
      wireGuard: !!r.wireGuard,
      volt480: !!r.volt480,
      emGen: !!r.emGen,
      photocell: !!r.photocell,
      twistlockPC: !!r.twistlockPC,
      wetLocation: !!r.wetLocation,
      darkSky: !!r.darkSky,
      optic: r.optic || null,
      mountType: r.mountType || null,
      isNewAddition: !!r.isNewAddition,
    });
    // Track in freq grid and recent
    const baseCode = r.fixtureId.replace(/-(hall|dim|stair|H)$/, "");
    fixtureFreq[baseCode] = (fixtureFreq[baseCode] || 0) + r.qty;
    const existing = recentFixtures.indexOf(r.fixtureId);
    if (existing !== -1) recentFixtures.splice(existing, 1);
    recentFixtures.push(r.fixtureId);
    if (recentFixtures.length > 10) recentFixtures.shift();
  });
  rebuildFreqGrid();
  renderRecentBtns();
  renderChips();

  // Restore flag states from last fixture entry that has flags set
  const flagEntry = roomEntries
    .slice()
    .reverse()
    .find(
      (r) =>
        r.intSensor ||
        r.embb ||
        r.airReturn ||
        r.wireGuard ||
        r.volt480 ||
        r.emGen ||
        r.photocell ||
        r.twistlockPC ||
        r.wetLocation ||
        r.darkSky ||
        r.optic ||
        r.mountType,
    );
  if (flagEntry) {
    state.fixtureFlagIntSensor = !!flagEntry.intSensor;
    state.fixtureFlagEMBB = !!flagEntry.embb;
    state.fixtureFlagAirReturn = !!flagEntry.airReturn;
    state.fixtureFlagWireGuard = !!flagEntry.wireGuard;
    state.fixtureFlag480V = !!flagEntry.volt480;
    state.fixtureFlagEMGen = !!flagEntry.emGen;
    state.fixtureFlagPhotocell = !!flagEntry.photocell;
    state.fixtureFlagTwistlockPC = !!flagEntry.twistlockPC;
    state.fixtureFlagWetLocation = !!flagEntry.wetLocation;
    state.fixtureFlagDarkSky = !!flagEntry.darkSky;
    state.fixtureOptic = flagEntry.optic || null;
    state.fixtureMountType = flagEntry.mountType || null;
    syncFixtureFlagBtns();
  }

  // Reset fixtureBtns to neutral state (fixtures already shown in Added To Room)
  state.activeFixture = "";
  const grid = document.getElementById("fixtureBtns");
  if (grid) {
    grid.innerHTML =
      '<span style="color:#8a9ab0;font-size:11px;font-style:italic;padding:4px 0">Fixtures loaded — tap Select Fixture to add more</span>';
    grid.style.alignItems = "";
  }

  // Set qty display to last fixture's qty for convenience
  if (roomEntries.length > 0) {
    qty = roomEntries[roomEntries.length - 1].qty;
    document.getElementById("qtyVal").value = qty;
  }

  // Pre-fill room name — use override if provided (copy room flow), else source name
  const finalRoomName = overrideName || e.space;
  document.getElementById("roomInput").value = finalRoomName;
  state.room = finalRoomName;
  updateRoomDisplay();

  updateSummary();
  if (overrideName) {
    toast(`📋 "${overrideName}" ready — review and tap Log Entry`);
  } else {
    toast(`✔ "${e.space}" loaded — update room name and log`);
    setTimeout(() => {
      const ri = document.getElementById("roomInput");
      ri.focus();
      ri.select();
    }, 300);
  }
}

function deleteEntry(i, ci) {
  if (ci !== undefined) {
    // Delete only that ctrl rec row
    if (logEntries[i] && logEntries[i].ctrlRecs) {
      logEntries[i].ctrlRecs.splice(ci, 1);
    }
  } else {
    // Delete the fixture row itself
    logEntries.splice(i, 1);
    logEntries.forEach((e, idx) => (e.line = idx + 1));
  }
  updateLogBadge();
  scheduleSave();
  renderLogTable();
}

function clearLog() {
  if (!confirm("Clear all logged entries?")) return;
  logEntries = [];
  updateLogBadge();
  clearSavedSession();
  renderLogTable();
}

function updateLogBadge() {
  const badge = document.getElementById("logBadge");
  if (badge) badge.textContent = logEntries.length;
  // Pin colors in the minimap depend on logged-vs-unlogged status — refresh when
  // the log changes so the current room's pin turns pink after a commit.
  if (typeof renderAuditMinimap === "function") renderAuditMinimap();
}

// ── EXPORT XLSX ───────────────────────────────────────────────────────────
// ── ATTRIBUTE ABBREVIATIONS ──────────────────────────────────────────────────
// Compact labels used in the data-table view and the XLSX export so descriptions
// stay short. Keep these consistent with the long forms shown in the modals.
const ATTR_ABBREV = {
  intSensor: "Int",
  embb: "EMBB",
  airReturn: "AR",
  wireGuard: "WG",
  volt480: "480V",
  emGen: "EMGen",
  photocell: "PC",
  twistlockPC: "TLPC",
  wetLocation: "WL",
  darkSky: "DS",
};
const OPTIC_ABBREV = {
  "Type II": "T2",
  "Type III": "T3",
  "Type IV": "T4",
  "Type V": "T5",
};
const MOUNT_ABBREV = {
  Trunnion: "Trun",
  Bracket: "Brkt",
  Slipfitter: "Slip",
  "Wall-Mount": "Wall",
  "Pole-Mount (Round)": "R-Pole",
  "Pole-Mount (Square)": "SQ-Pole",
};

// Returns abbreviated suffix string ("PC - T3 - R-Pole") for the given log entry.
// If `includeMount` is false, the mount type is omitted (used by the XLSX export
// since mounting has its own column there).
function composeAttrSuffix(e, includeMount) {
  const parts = [];
  Object.keys(ATTR_ABBREV).forEach((k) => {
    if (e[k]) parts.push(ATTR_ABBREV[k]);
  });
  if (e.optic) parts.push(OPTIC_ABBREV[e.optic] || e.optic);
  if (includeMount && e.mountType)
    parts.push(MOUNT_ABBREV[e.mountType] || e.mountType);
  return parts.join(" - ");
}

function exportXLSX() {
  if (logEntries.length === 0) {
    toast("No entries to export");
    return;
  }

  const headers = [
    "Line #",
    "Facility",
    "Floor / Area",
    "Room Name",
    "Room Type",
    "Dimensions",
    "Ceiling Type",
    "Pin #",
    "Int / Ctrl",
    "Height",
    "Fixture ID",
    "Qty",
    "Replacement Description",
    "New Qty",
    "Notes",
    "Mounting",
    // Provenance / project tree — appended at the end so the existing column
    // indices (0-15 + hidden 16=CtrlRec) stay valid for the cellStyle map and
    // downstream parsers that key off position. Engine should prefer name-based
    // lookup; these IDs are the long-term stable identifiers.
    "Project ID",
    "Building ID",
    "Project Name",
    "Schema Version",
    // Pre-install footcandle readings — averaged per room at each height.
    // Post-install readings will live in a separate column set once captured.
    "Pre FC Floor",
    "Pre FC 30 AFF",
    "Pre FC Surface",
    "Pre FC Sample Count",
  ];
  // Schema version — bumped whenever the export shape changes meaningfully.
  // v1 = 20 cols with attribute-suffixed Fixture ID
  // v2 = 24 cols, adds Pre-Install FC measurements per room
  const EXPORT_SCHEMA_VERSION = 2;

  // Build a per-room aggregate of pre-install footcandle readings. Each room
  // may have multiple readings at different heights; we average within each
  // bucket and surface a sample count so the engine knows how many points
  // contributed.
  const _exportFcByRoom = {}; // roomName → { floor: [..], aff30: [..], surface: [..] }
  Object.values(floorPlans || {}).forEach((plan) => {
    (plan?.markup?.lightLevels || []).forEach((rec) => {
      if (!rec.roomName) return;
      const bucket = (_exportFcByRoom[rec.roomName] ||= {
        floor: [],
        aff30: [],
        surface: [],
      });
      const key =
        rec.measuredAt === "30 AFF"
          ? "aff30"
          : rec.measuredAt === "Surface"
          ? "surface"
          : "floor";
      if (typeof rec.fc === "number" && rec.fc >= 0) bucket[key].push(rec.fc);
    });
  });
  const _avg = (arr) =>
    arr.length
      ? Math.round((arr.reduce((a, b) => a + b, 0) / arr.length) * 10) / 10
      : "";
  const _fcCellsForRoom = (roomName) => {
    const b = _exportFcByRoom[roomName];
    if (!b) return ["", "", "", ""];
    return [
      _avg(b.floor),
      _avg(b.aff30),
      _avg(b.surface),
      b.floor.length + b.aff30.length + b.surface.length,
    ];
  };
  const _exportProject =
    typeof _currentProject === "function" ? _currentProject() : null;
  const _exportBuilding =
    typeof _currentBuilding === "function" ? _currentBuilding() : null;
  const _exportProjectId = _exportProject?.id || "";
  const _exportBuildingId = _exportBuilding?.id || "";
  const _exportProjectName = _exportProject?.name || "";

  // Compose attribute flags + optic onto the fixture description.
  // For export, mount is excluded — it goes in its own Mounting column.
  function composeFixtureLabel(e) {
    const base = e.desc || e.fixtureId;
    const suffix = composeAttrSuffix(e, false);
    return suffix ? `${base} - ${suffix}` : base;
  }

  // Group consecutive entries by room (same as table display)
  const exportGroups = [];
  logEntries.forEach((e, i) => {
    const key = `${e.facility}|${e.floor}|${e.space}`;
    const last = exportGroups[exportGroups.length - 1];
    if (last && last.key === key) last.entries.push({ e, i });
    else exportGroups.push({ key, entries: [{ e, i }] });
  });

  const rows = [];
  const cellStyles = {}; // track cells needing green fill: 'R,C' -> true

  // Build pin lookup for export
  const exportPinLookup = {};
  Object.values(floorPlans).forEach((plan) => {
    (plan.pins || []).forEach((pin) => {
      if (pin.roomName && !(pin.roomName in exportPinLookup))
        exportPinLookup[pin.roomName] = pin.pinNum;
    });
  });

  exportGroups.forEach((group) => {
    const roomName = group.entries[0]?.e?.space || "";
    const pinNum =
      exportPinLookup[roomName] !== undefined ? exportPinLookup[roomName] : "";
    // All fixture rows first
    group.entries.forEach(({ e }, rowIdx2) => {
      const ht = e.height ? e.height + "'" : "";
      const rowIdx = rows.length + 1; // +1 for header
      // Cols: 0=Line, 1=Facility, 2=Floor, 3=Room, 4=Type, 5=Dims, 6=Ceiling,
      //       7=Pin#, 8=Int/Ctrl, 9=Height, 10=FixtureID(+attrs), 11=Qty,
      //       12=SwitchType, 13=ExistingCtrls, 14=Notes, 15=Mounting, 16=CtrlRec(hidden)
      const intCtrl = e.fixtureType === "Exterior" ? "Exterior" : "Interior";
      const fcCells =
        rowIdx2 === 0 ? _fcCellsForRoom(e.space) : ["", "", "", ""];
      rows.push([
        e.line,
        e.facility,
        e.floor,
        e.space,
        e.roomType,
        e.dimensions || "",
        e.ceilingType || "",
        rowIdx2 === 0 ? pinNum : "",
        intCtrl,
        ht,
        composeFixtureLabel(e),
        e.qty,
        e.numSwitches || "",
        e.existingCtrls || "",
        e.notes || "",
        e.mountType || "",
        "",
        _exportProjectId,
        _exportBuildingId,
        _exportProjectName,
        EXPORT_SCHEMA_VERSION,
        ...fcCells,
      ]);
      if (e.hasCustomSwitch) cellStyles[`${rowIdx},12`] = true;
      if (e.hasCustomExisting) cellStyles[`${rowIdx},13`] = true;
    });
    // All ctrl rows after
    group.entries.forEach(({ e }) => {
      if (!(e.ctrlRecs && e.ctrlRecs.length)) return;
      const ht = e.height ? e.height + "'" : "";
      e.ctrlRecs.forEach((ctrl) => {
        const ctrlName = typeof ctrl === "object" ? ctrl.name : ctrl;
        const ctrlQ =
          typeof ctrl === "object" ? ctrl.qty : ctrlQty(ctrl, e.numSwitches);
        const rowIdx = rows.length + 1;
        rows.push([
          "",
          e.facility,
          e.floor,
          e.space,
          e.roomType,
          "",
          "",
          "",
          "Controls",
          ht,
          ctrlFixtureId(ctrlName),
          ctrlQ,
          "",
          "",
          "",
          "",
          ctrlName,
          _exportProjectId,
          _exportBuildingId,
          _exportProjectName,
          EXPORT_SCHEMA_VERSION,
          "",
          "",
          "",
          "",
        ]);
        if (e.hasCustomCtrl) cellStyles[`${rowIdx},16`] = true;
      });
    });
  });

  const wsData = [headers, ...rows];
  const wb = XLSX.utils.book_new();
  const ws = XLSX.utils.aoa_to_sheet(wsData);

  ws["!cols"] = [
    { wch: 7 },
    { wch: 12 },
    { wch: 10 },
    { wch: 16 },
    { wch: 14 },
    { wch: 10 },
    { wch: 8 },
    { wch: 14 },
    { wch: 6 },
    { wch: 16 },
    { wch: 28 },
    { wch: 6 },
    { wch: 18 },
    { wch: 8 },
    { wch: 22 },
    { wch: 14 },
    // hidden CtrlRec (col 16) + new provenance columns + schema version + FC
    { wch: 0, hidden: true },
    { wch: 18 },
    { wch: 18 },
    { wch: 20 },
    { wch: 8 },
    { wch: 10 },
    { wch: 10 },
    { wch: 10 },
    { wch: 8 },
  ];

  // Apply light green fill to custom item cells
  Object.keys(cellStyles).forEach((key) => {
    const [r, c] = key.split(",").map(Number);
    const addr = XLSX.utils.encode_cell({ r, c });
    if (ws[addr]) {
      ws[addr].s = ws[addr].s || {};
      ws[addr].s.fill = { fgColor: { rgb: "C6EFCE" } };
    }
  });

  // Header row styling
  const range = XLSX.utils.decode_range(ws["!ref"]);
  for (let C = range.s.c; C <= range.e.c; C++) {
    const addr = XLSX.utils.encode_cell({ r: 0, c: C });
    if (!ws[addr]) continue;
    ws[addr].s = {
      fill: { fgColor: { rgb: "1A3A5C" } },
      font: { color: { rgb: "FFFFFF" }, bold: true },
      alignment: { horizontal: "center", wrapText: true },
    };
  }

  XLSX.utils.book_append_sheet(wb, ws, "Fixture Survey");

  // ── Light Levels sheet — one row per individual FC measurement.
  // Main sheet keeps per-room averages for at-a-glance review; this sheet
  // preserves raw data so the proposal engine can analyze distribution and
  // compare individual pre/post points (not just means).
  const llHeaders = [
    "Project ID",
    "Building ID",
    "Project Name",
    "Floor / Area",
    "Room Name",
    "Pin #",
    "FC",
    "Measured At",
    "X %",
    "Y %",
    "Plan Name",
    "Recorded At (ISO)",
    "Schema Version",
  ];
  const llRows = [];
  Object.values(floorPlans || {}).forEach((plan) => {
    if (!plan) return;
    const fl = plan.floorId
      ? (floors || []).find((f) => f.id === plan.floorId)
      : null;
    const floorLabel = fl?.name || "";
    (plan.markup?.lightLevels || []).forEach((rec) => {
      const pin = rec.pinId
        ? (plan.pins || []).find((p) => p.id === rec.pinId)
        : null;
      llRows.push([
        _exportProjectId,
        _exportBuildingId,
        _exportProjectName,
        floorLabel,
        rec.roomName || "",
        pin?.pinNum ?? "",
        rec.fc ?? "",
        rec.measuredAt || "",
        rec.xPct != null ? Math.round(rec.xPct * 10000) / 100 : "", // percent, 2dp
        rec.yPct != null ? Math.round(rec.yPct * 10000) / 100 : "",
        plan.name || "",
        rec.createdAt || "",
        EXPORT_SCHEMA_VERSION,
      ]);
    });
  });
  if (llRows.length) {
    const llSheet = XLSX.utils.aoa_to_sheet([llHeaders, ...llRows]);
    llSheet["!cols"] = [
      { wch: 18 },
      { wch: 18 },
      { wch: 20 },
      { wch: 14 },
      { wch: 18 },
      { wch: 8 },
      { wch: 6 },
      { wch: 12 },
      { wch: 8 },
      { wch: 8 },
      { wch: 18 },
      { wch: 22 },
      { wch: 8 },
    ];
    // Header row styling — match main sheet
    const llRange = XLSX.utils.decode_range(llSheet["!ref"]);
    for (let C = llRange.s.c; C <= llRange.e.c; C++) {
      const addr = XLSX.utils.encode_cell({ r: 0, c: C });
      if (!llSheet[addr]) continue;
      llSheet[addr].s = {
        fill: { fgColor: { rgb: "C4A020" } },
        font: { color: { rgb: "FFFFFF" }, bold: true },
        alignment: { horizontal: "center", wrapText: true },
      };
    }
    XLSX.utils.book_append_sheet(wb, llSheet, "Light Levels");
  }

  const facility = ((bldgData && bldgData.name) || "Building").replace(
    /[^\w\-]+/g,
    "_",
  );
  const floor = ((activeFloor && activeFloor.name) || "Floor").replace(
    /[^\w\-]+/g,
    "_",
  );
  const date = new Date().toISOString().slice(0, 10);
  XLSX.writeFile(wb, `${facility}_${floor}_LightingSurvey_${date}.xlsx`);
  toast("Downloaded!");
}

// ── TOAST ─────────────────────────────────────────────────────────────────
function toast(msg) {
  const el = document.getElementById("toast");
  el.textContent = msg;
  el.classList.add("show");
  setTimeout(() => el.classList.remove("show"), 2200);
}

const MATCH_DATA = [
  {
    id: "A1",
    desc: "2x4",
    lampQty: 2,
    existing: "F32T8-2L-2x4-Prismatic-Recessed-4100K",
  },
  {
    id: "A2",
    desc: "2x4",
    lampQty: 3,
    existing: "F32T8-3L-2x4-Prismatic-Recessed-4100K",
  },
  {
    id: "A3",
    desc: "2x4",
    lampQty: 4,
    existing: "F32T8-4L-2x4-Prismatic-Recessed-4100K",
  },
  {
    id: "A4",
    desc: "2x4",
    lampQty: 2,
    existing: "F32T8-2L-2x4-Parabolic-Recessed-4100K",
  },
  {
    id: "A5",
    desc: "2x4",
    lampQty: 3,
    existing: "F32T8-3L-2x4-Parabolic-Recessed-4100K",
  },
  {
    id: "A6",
    desc: "2x4",
    lampQty: 4,
    existing: "F32T8-4L-2x4-Parabolic-Recessed-3500K",
  },
  {
    id: "A7",
    desc: "2x4",
    lampQty: 2,
    existing: "F32T8-2L-2x4-Prismatic-Recessed-3500K",
  },
  {
    id: "A8",
    desc: "2x4",
    lampQty: 3,
    existing: "F32T8-3L-2x4-Prismatic-Recessed-3500K",
  },
  {
    id: "A9",
    desc: "2x4",
    lampQty: 4,
    existing: "F32T8-4L-2x4-Prismatic-Recessed-3500K",
  },
  {
    id: "A10",
    desc: "2x4",
    lampQty: 2,
    existing: "F32T8-2L-2x4-Parabolic-Recessed-3500K",
  },
  {
    id: "A11",
    desc: "2x4",
    lampQty: 3,
    existing: "F32T8-3L-2x4-Parabolic-Recessed-3500K",
  },
  {
    id: "A12",
    desc: "2x4",
    lampQty: 4,
    existing: "F32T8-4L-2x4-Parabolic-Recessed-3500K",
  },
  {
    id: "A13",
    desc: "2x4",
    lampQty: 2,
    existing: "F32T8-2L-2x4-No Lens-Pendant-4100K",
  },
  {
    id: "A14",
    desc: "2x4",
    lampQty: 2,
    existing: "F32T8-2L-2x4-No Lens-Surface-4100K",
  },
  {
    id: "A15",
    desc: "2x4",
    lampQty: 4,
    existing: "F32T8-4L-2x4-No Lens-Pendant-4100K",
  },
  {
    id: "A16",
    desc: "2x4",
    lampQty: 4,
    existing: "F32T8-4L-2x4-No Lens-Surface-4100K",
  },
  {
    id: "A17",
    desc: "2x4",
    lampQty: 2,
    existing: "F32T8-2L-2x4-Prismatic-Surface-4100K",
  },
  {
    id: "A18",
    desc: "2x4",
    lampQty: 4,
    existing: "F32T8-4L-2x4-Prismatic-Surface-4100K",
  },
  {
    id: "A19",
    desc: "2x4",
    lampQty: 2,
    existing: "F28T5-2L-Prismatic-Recessed-4100K",
  },
  {
    id: "A20",
    desc: "2x4",
    lampQty: 3,
    existing: "F28T5-3L-Prismatic-Recessed-4100K",
  },
  {
    id: "A21",
    desc: "2x4",
    lampQty: 4,
    existing: "F28T5-4L-Prismatic-Recessed-4100K",
  },
  {
    id: "B1",
    desc: "2X2 U-LAMP",
    lampQty: 2,
    existing: "F32T8U-2L-2x2-Prismatic-Recessed-4100K",
  },
  {
    id: "B2",
    desc: "2X2 U-LAMP",
    lampQty: 2,
    existing: "F32T8U-2L-2x2-Parabolic-Recessed-4100K",
  },
  {
    id: "B3",
    desc: "2X2 U-LAMP",
    lampQty: 2,
    existing: "F32T8U-2L-2x2-Prismatic-Recessed-3500K",
  },
  {
    id: "B4",
    desc: "2X2 U-LAMP",
    lampQty: 2,
    existing: "F32T8U-2L-2x2-Parabolic-Recessed-3500K",
  },
  {
    id: "B5",
    desc: "2X2 U-LAMP",
    lampQty: 2,
    existing: "F40T12U-2L-2x2-Prismatic-Recessed-4100K",
  },
  {
    id: "B6",
    desc: "2X2 U-LAMP",
    lampQty: 2,
    existing: "F40T12U-2L-2x2-Parabolic-Recessed-4100K",
  },
  {
    id: "B7",
    desc: "2X2 U-LAMP",
    lampQty: 2,
    existing: "F40T12U-2L-2x2-Prismatic-Recessed-3500K",
  },
  {
    id: "B8",
    desc: "2X2 U-LAMP",
    lampQty: 2,
    existing: "F40T12U-2L-2x2-Parabolic-Recessed-3500K",
  },
  {
    id: "B9",
    desc: "2X2 U-LAMP",
    lampQty: 2,
    existing: "F32T8U-2L-2x2-Prismatic-Surface-4100K",
  },
  {
    id: "B10",
    desc: "2X2 U-LAMP",
    lampQty: 2,
    existing: "F32T8U-2L-2x2-Parabolic-Surface-4100K",
  },
  {
    id: "B11",
    desc: "2X2 U-LAMP",
    lampQty: 3,
    existing: "F32T8U-3L-2x2-Prismatic-Recessed-4100K",
  },
  {
    id: "B12",
    desc: "2X2 U-LAMP",
    lampQty: 3,
    existing: "F32T8U-3L-2x2-Parabolic-Recessed-4100K",
  },
  {
    id: "BX1",
    desc: "1X2 Biax",
    lampQty: 1,
    existing: "F41Bx-1L-1x2-Prismatic-Surface",
  },
  {
    id: "BX2",
    desc: "2X2 Biax",
    lampQty: 2,
    existing: "F41Bx-2L-2x2-Prismatic-Recessed",
  },
  {
    id: "BX3",
    desc: "2X2 Biax",
    lampQty: 3,
    existing: "F41Bx-3L-2x2-Prismatic-Recessed",
  },
  {
    id: "BX4",
    desc: "2X2 Biax",
    lampQty: 4,
    existing: "F41Bx-4L-2x2-Prismatic-Recessed",
  },
  {
    id: "BX5",
    desc: "1X4 Biax",
    lampQty: 2,
    existing: "F41Bx-2L-1x4-Recessed",
  },
  { id: "BX6", desc: "1X4 Biax", lampQty: 2, existing: "F41Bx-2L-1x4-Surface" },
  {
    id: "BX7",
    desc: "2X4 Biax",
    lampQty: 4,
    existing: "F41Bx-4L-2x4-Recessed",
  },
  { id: "BX8", desc: "2X4 Biax", lampQty: 4, existing: "F41Bx-4L-2x4-Surface" },
  {
    id: "BX9",
    desc: "Circular half globe",
    lampQty: 3,
    existing: "F41Bx-3L-half globe lens-Pendant",
  },
  {
    id: "BF8",
    desc: "4X4 Recessed Dome",
    lampQty: 8,
    existing: "F32T8-8L-Dome-Recessed & Convex",
  },
  {
    id: "C1",
    desc: "1x4",
    lampQty: 1,
    existing: "F32T8-1L-1x4-Prismatic-Recessed-4100K",
  },
  {
    id: "C2",
    desc: "1x4",
    lampQty: 2,
    existing: "F32T8-2L-1x4-Prismatic-Recessed-4100K",
  },
  {
    id: "C3",
    desc: "1x4",
    lampQty: 1,
    existing: "F32T8-1L-1x4-Prismatic-Recessed-3500K",
  },
  {
    id: "C4",
    desc: "1x4",
    lampQty: 2,
    existing: "F32T8-2L-1x4-Prismatic-Recessed-3500K",
  },
  {
    id: "C5",
    desc: "1x4",
    lampQty: 1,
    existing: "F32T8-1L-1x4-Prismatic-Recessed-3000K",
  },
  {
    id: "C6",
    desc: "1x4",
    lampQty: 2,
    existing: "F32T8-2L-1x4-Prismatic-Recessed-3000K",
  },
  {
    id: "C7",
    desc: "1x4",
    lampQty: 1,
    existing: "F32T8-1L-1x4-No Lens-Pendant-4100K",
  },
  {
    id: "C8",
    desc: "1x4",
    lampQty: 1,
    existing: "F32T8-1L-1x4-No Lens-Surface-4100K",
  },
  {
    id: "C9",
    desc: "1x4",
    lampQty: 2,
    existing: "F32T8-2L-1x4-No Lens-Pendant-4100K",
  },
  {
    id: "C10",
    desc: "1x4",
    lampQty: 2,
    existing: "F32T8-2L-1x4-No Lens-Surface-4100K",
  },
  {
    id: "C11",
    desc: "1x4",
    lampQty: 1,
    existing: "F32T8-1L-1x4-Parabolic-Recessed-4100K",
  },
  {
    id: "C12",
    desc: "1x4",
    lampQty: 2,
    existing: "F32T8-2L-1x4-Parabolic-Recessed-4100K",
  },
  {
    id: "C13",
    desc: "1x4",
    lampQty: 1,
    existing: "F28T5-1L-1x4-No Lens-Recessed-4100K",
  },
  {
    id: "C14",
    desc: "1x4",
    lampQty: 2,
    existing: "F28T5-2L-1x4-No Lens-Recessed-4100K",
  },
  {
    id: "C15",
    desc: "1x4",
    lampQty: 1,
    existing: "F32T8-1L-1x4-Prismatic-Surface-4100K",
  },
  {
    id: "C16",
    desc: "1x4",
    lampQty: 2,
    existing: "F32T8-2L-1x4-Prismatic-Surface-4100K",
  },
  {
    id: "C17",
    desc: "1x4",
    lampQty: 1,
    existing: "F28T5-1L-1x4-No Lens-Surface-4100K",
  },
  {
    id: "C18",
    desc: "1x4",
    lampQty: 2,
    existing: "F28T5-2L-1x4-No Lens-Surface-4100K",
  },
  {
    id: "C19",
    desc: "1x4",
    lampQty: 1,
    existing: "F28T5-1L-1x4-Prismatic-Recessed-4100K",
  },
  {
    id: "C20",
    desc: "1x4",
    lampQty: 2,
    existing: "F28T5-2L-1x4-Prismatic-Recessed-4100K",
  },
  {
    id: "C21",
    desc: "1x4",
    lampQty: 2,
    existing: "F32T8-2L-1x4-Parabolic-Pendant-4100K",
  },
  {
    id: "C22",
    desc: "1x4",
    lampQty: 2,
    existing: "F32T8-2L-1x4-Parabolic-Surface-4100K",
  },
  {
    id: "C23",
    desc: "1x4",
    lampQty: 2,
    existing: "F28T5-2L-1x4-Prismatic-Pendant-4100K",
  },
  {
    id: "D1",
    desc: "2X2",
    lampQty: 2,
    existing: "F17T8-2L-2x2-Prismatic -Surface-4100K",
  },
  {
    id: "D2",
    desc: "2X2",
    lampQty: 2,
    existing: "F17T8-2L-2x2-Prismatic -Recessed-4100K",
  },
  {
    id: "D3",
    desc: "2X2",
    lampQty: 2,
    existing: "F17T8-2L-2x2-Prismatic -Surface-3500K",
  },
  {
    id: "D4",
    desc: "2X2",
    lampQty: 2,
    existing: "F17T8-2L-2x2-Prismatic -Recessed-3500K",
  },
  {
    id: "D5",
    desc: "2X2",
    lampQty: 2,
    existing: "F17T8-2L-2x2-Prismatic -Surface-3500K",
  },
  {
    id: "D6",
    desc: "2X2",
    lampQty: 2,
    existing: "F17T8-2L-2x2-Prismatic -Recessed-3000K",
  },
  {
    id: "D7",
    desc: "2X2",
    lampQty: 4,
    existing: "F17T8-4L-2x2-Prismatic -Surface-4100K",
  },
  {
    id: "D8",
    desc: "2X2",
    lampQty: 4,
    existing: "F17T8-4L-2x2-Prismatic -Recessed-4100K",
  },
  {
    id: "D9",
    desc: "2X2",
    lampQty: 2,
    existing: "F17T8-2L-2x2-No Lens-Surface-4100K",
  },
  {
    id: "D10",
    desc: "2X2",
    lampQty: 2,
    existing: "F17T8-2L-2x2-No Lens-Recessed-4100K",
  },
  {
    id: "D11",
    desc: "2X2",
    lampQty: 2,
    existing: "F17T8-2L-2x2-Parabolic -Surface-4100K",
  },
  {
    id: "D12",
    desc: "2X2",
    lampQty: 2,
    existing: "F17T8-2L-2x2-Parabolic -Recessed-4100K",
  },
  {
    id: "D13",
    desc: "2X2",
    lampQty: 3,
    existing: "F17T8-3L-2x2-Parabolic -Surface-4100K",
  },
  {
    id: "D14",
    desc: "2X2",
    lampQty: 3,
    existing: "F17T8-3L-2x2-Parabolic -Recessed-4100K",
  },
  {
    id: "D15",
    desc: "2X2",
    lampQty: 2,
    existing: "F17T8-2L-2x2-No Lens-Pendant-4100K",
  },
  {
    id: "D16",
    desc: "2X2",
    lampQty: 2,
    existing: "F17T8-2L-2x2-Prismatic-Pendant-4100K",
  },
  {
    id: "D17",
    desc: "2X2",
    lampQty: 3,
    existing: "F17T8-3L-2x2-Prismatic -Recessed-4100K",
  },
  {
    id: "D18",
    desc: "2X2",
    lampQty: 2,
    existing: "F28T5-2L-2X2-Prismatic-Recessed-4100K",
  },
  {
    id: "DL1",
    desc: "L2X2",
    lampQty: 3,
    existing: "F17T8-3L-Larger than 2x2-Prismatic -Surface-4100K",
  },
  {
    id: "E1",
    desc: "1X8",
    lampQty: 2,
    existing: "F32T8-2L-1x8-No Lens-Pendant-4100K",
  },
  {
    id: "E2",
    desc: "1X8",
    lampQty: 4,
    existing: "F32T8-4L-1x8-No Lens-Pendant-4100K",
  },
  {
    id: "E3",
    desc: "1X8",
    lampQty: 2,
    existing: "F32T8-2L-1x8-No Lens-Surface-4100K",
  },
  {
    id: "E4",
    desc: "1X8",
    lampQty: 4,
    existing: "F32T8-4L-1x8-No Lens-Surface-4100K",
  },
  {
    id: "E5",
    desc: "1X8",
    lampQty: 4,
    existing: "F32T8-4L-1x8-Parabolic-Pendant-4100K",
  },
  {
    id: "E6",
    desc: "1X8",
    lampQty: 4,
    existing: "F32T8-4L-1x8-Parabolic-Surface-4100K",
  },
  {
    id: "E7",
    desc: "1X8",
    lampQty: 2,
    existing: "F28T5-2L-1x8-Prismatic-Pendant-4100K",
  },
  {
    id: "E8",
    desc: "1X8",
    lampQty: 4,
    existing: "F32T8-4L-1x8-Prismatic-Recessed-4100K",
  },
  {
    id: "E9",
    desc: "1X8",
    lampQty: 2,
    existing: "F32T8-2L-1x8-Prismatic-Recessed-4100K",
  },
  {
    id: "F1",
    desc: "Canopy",
    lampQty: 2,
    existing: "CFL18W-2L-Canopy-Prismatic-Surface-4100K",
  },
  {
    id: "F2",
    desc: "Canopy",
    lampQty: 2,
    existing: "CFL26W-2L-Canopy-Prismatic-Surface-4100K",
  },
  {
    id: "F3",
    desc: "Canopy",
    lampQty: 1,
    existing: "MH75W-1L-Canopy-Prismatic-Surface-4100K",
  },
  {
    id: "F4",
    desc: "Canopy",
    lampQty: 1,
    existing: "MH100W-1L-Canopy-Prismatic-Surface-4100K",
  },
  {
    id: "F5",
    desc: "Canopy",
    lampQty: 1,
    existing: "MH175W-1L-Canopy-Prismatic-Surface-4100K",
  },
  {
    id: "F6",
    desc: "Canopy",
    lampQty: 1,
    existing: "MH250W-1L-Canopy-Prismatic-Surface-4100K",
  },
  {
    id: "F7",
    desc: "Canopy",
    lampQty: 1,
    existing: "MH250W-1L-Canopy-Prismatic-Pendant-4100K",
  },
  {
    id: "F8",
    desc: "Canopy",
    lampQty: 2,
    existing: "CFL18W-2L-Canopy-Prismatic-Recessed-4100K",
  },
  {
    id: "F9",
    desc: "Canopy",
    lampQty: 1,
    existing: "CFL18W-1L-Canopy-Prismatic-Pendant-4100K",
  },
  {
    id: "F10",
    desc: "Canopy",
    lampQty: 2,
    existing: "CFL13W-2L-Canopy-Prismatic-Pendant-4100K",
  },
  {
    id: "F11",
    desc: "Canopy",
    lampQty: 2,
    existing: "CFL13W-2L-Canopy-Prismatic-Surface-4100K",
  },
  { id: "G1", desc: "LED", lampQty: null, existing: "LED-2x2" },
  { id: "G2", desc: "LED", lampQty: null, existing: "LED-2x4" },
  { id: "G3", desc: "LED", lampQty: null, existing: "LED-Vanity" },
  { id: "G4", desc: "LED", lampQty: null, existing: "LED-Exterior" },
  { id: "G5", desc: "LED", lampQty: null, existing: "LED-INC/CFL" },
  { id: "G6", desc: "LED", lampQty: null, existing: "LED-4'Industrial" },
  { id: "G7", desc: "LED", lampQty: null, existing: "LED-8'Industrial" },
  { id: "G8", desc: "LED", lampQty: null, existing: "LED-1x4" },
  { id: "G9", desc: "LED", lampQty: null, existing: "LED-1x8" },
  { id: "G10", desc: "LED", lampQty: 1, existing: "" },
  { id: "G11", desc: "LED", lampQty: 2, existing: "" },
  { id: "G12", desc: "LED", lampQty: 1, existing: "" },
  { id: "G13", desc: "LED", lampQty: 2, existing: "" },
  { id: "G14", desc: "LED", lampQty: 2, existing: "25T5/LED/46-835/DR-2L" },
  {
    id: "G15",
    desc: "LED",
    lampQty: 1,
    existing: "LED-10W-PAR-1L-Lens-Recessed",
  },
  {
    id: "G16",
    desc: "LED",
    lampQty: 1,
    existing: "LED-8W-ShortStripLamp-1L-Lens-Square Recessed",
  },
  {
    id: "G17",
    desc: "LED",
    lampQty: 1,
    existing: "Led-17W-PAR38-1L-No Lens-Recessed-4100K",
  },
  {
    id: "G18",
    desc: "LED",
    lampQty: 1,
    existing: "Led-17W-Round-1L-Lens-Surface-4100K",
  },
  { id: "G19", desc: "LED", lampQty: 1, existing: "LED-50W-Various" },
  {
    id: "G20",
    desc: "LED",
    lampQty: 1,
    existing: "LED-150W-Round-Surface-4100K",
  },
  {
    id: "G21",
    desc: "LED",
    lampQty: 2,
    existing: "LED-50W-Strip-DirectIndirect-Lens-Pendant-4100K",
  },
  {
    id: "H1",
    desc: "Wallpack",
    lampQty: 1,
    existing: "MH75W-1L-WP-Prismatic-Surface-4100K",
  },
  {
    id: "H2",
    desc: "Wallpack",
    lampQty: 1,
    existing: "MH100W-1L-WP-Prismatic-Surface-4100K",
  },
  {
    id: "H3",
    desc: "Wallpack",
    lampQty: 1,
    existing: "MH125W-1L-WP-Prismatic-Surface-4100K",
  },
  {
    id: "H4",
    desc: "Wallpack",
    lampQty: 1,
    existing: "MH150W-1L-WP-Prismatic-Surface-4100K",
  },
  {
    id: "H5",
    desc: "Wallpack",
    lampQty: 1,
    existing: "MH175W-1L-WP-Prismatic-Surface-4100K",
  },
  {
    id: "H6",
    desc: "Wallpack",
    lampQty: 1,
    existing: "MH250W-1L-WP-Prismatic-Surface-4100K",
  },
  {
    id: "H7",
    desc: "Wallpack",
    lampQty: 1,
    existing: "MH400W-1L-WP-Prismatic-Surface-4100K",
  },
  {
    id: "H8",
    desc: "Wallpack",
    lampQty: 1,
    existing: "MH750W-1L-WP-Prismatic-Surface-4100K",
  },
  {
    id: "H9",
    desc: "Wallpack",
    lampQty: 1,
    existing: "MH1000W-1L-WP-Prismatic-Surface-4100K",
  },
  { id: "H10", desc: "", lampQty: null, existing: "" },
  {
    id: "HPS1",
    desc: "Spotlight",
    lampQty: 1,
    existing: "400-1L-SL-Prismatic-Bracket-4100K",
  },
  {
    id: "HB1",
    desc: "2x4 Highbay",
    lampQty: 4,
    existing: "MB32T8-4L-No Lens-Pendant-4100K",
  },
  {
    id: "HB2",
    desc: "2x4 Highbay",
    lampQty: 6,
    existing: "MB32T8-6L-No Lens-Pendant-4100K",
  },
  {
    id: "HB3",
    desc: "2x4 Highbay",
    lampQty: 4,
    existing: "MB54T5-4L-No Lens-Pendant-4100K",
  },
  {
    id: "HB4",
    desc: "2x4 Highbay",
    lampQty: 6,
    existing: "MB54T5-6L-No Lens-Pendant-4100K",
  },
  {
    id: "HB5",
    desc: "2x4 Highbay",
    lampQty: 4,
    existing: "MB32T8-4L-Vapor-Pendant-4100K",
  },
  {
    id: "HB6",
    desc: "2x4 Highbay",
    lampQty: 6,
    existing: "MB32T8-6L-Vapor-Pendant-4100K",
  },
  {
    id: "SEE LINES 866 - 900 FOR MORE PRECISE CAN SIZES/FEATURES",
    desc: "",
    lampQty: null,
    existing: "",
  },
  {
    id: "I1",
    desc: "Recessed Can",
    lampQty: 2,
    existing: "CFL13W-2L-No Lens-Recessed Can-4100K",
  },
  {
    id: "IL1",
    desc: "Recessed Can",
    lampQty: 2,
    existing: "CFL13W-2L-Lens-Recessed Can-4100K",
  },
  {
    id: "I2",
    desc: "Recessed Can",
    lampQty: 2,
    existing: "CFL18W-2L-No Lens-Recessed Can-4100K",
  },
  {
    id: "I3",
    desc: "Recessed Can",
    lampQty: 2,
    existing: "CFL26W-2L-No Lens-Recessed Can-4100K",
  },
  {
    id: "I4",
    desc: "Recessed Can",
    lampQty: 2,
    existing: "CFL13W-2L-No Lens-Recessed Can-3500K",
  },
  {
    id: "I5",
    desc: "Recessed Can",
    lampQty: 2,
    existing: "CFL18W-2L-No Lens-Recessed Can-3500K",
  },
  {
    id: "I6",
    desc: "Recessed Can",
    lampQty: 2,
    existing: "CFL26W-2L-No Lens-Recessed Can-3500K",
  },
  {
    id: "I7",
    desc: "Recessed Can",
    lampQty: 2,
    existing: "CFL13W-2L-No Lens-Recessed Can-3000K",
  },
  {
    id: "I8",
    desc: "Recessed Can",
    lampQty: 2,
    existing: "CFL18W-2L-No Lens-Recessed Can-3000K",
  },
  {
    id: "I9",
    desc: "Recessed Can",
    lampQty: 2,
    existing: "CFL26W-2L-No Lens-Recessed Can-3000K",
  },
  {
    id: "I10",
    desc: "Recessed Can",
    lampQty: 1,
    existing: "PAR90W-1L-No Lens-Recessed Can-3500K",
  },
  {
    id: "I11",
    desc: "Recessed Can",
    lampQty: 1,
    existing: "PAR60W-1L-No Lens-Recessed Can-4100K",
  },
  {
    id: "I12",
    desc: "Recessed Can",
    lampQty: 1,
    existing: "CFL18W-1L-No Lens-Recessed Can-4100K",
  },
  {
    id: "I13",
    desc: "Recessed Can",
    lampQty: 1,
    existing: "CFL26W-1L-No Lens-Recessed Can-4100K",
  },
  {
    id: "I14",
    desc: "Recessed Can",
    lampQty: 1,
    existing: "CFL26W-1L-Parabolic-Recessed Can-4100K",
  },
  {
    id: "I15",
    desc: "Recessed Can",
    lampQty: 1,
    existing: "INC60W-1L-No Lens-Recessed Can-3500K",
  },
  {
    id: "I16",
    desc: "Recessed Can",
    lampQty: 1,
    existing: "CFL34W-1L-No Lens-Recessed Can-4100K",
  },
  {
    id: "I17",
    desc: "Recessed Can",
    lampQty: 1,
    existing: "MV100W-1L-Lens-Recessed Can-4100K",
  },
  {
    id: "I18",
    desc: "Recessed Can",
    lampQty: 1,
    existing: "MV100W-1L-No Lens-Recessed Can-4100K",
  },
  {
    id: "I19",
    desc: "Recessed Can",
    lampQty: 1,
    existing: "MV175W-1L-No Lens-Recessed Can-4100K",
  },
  {
    id: "IJ1",
    desc: "Jelly Jar",
    lampQty: 1,
    existing: "INC100W-1L-Glass-Sconce-4100K",
  },
  {
    id: "IJ2",
    desc: "Jelly Jar",
    lampQty: 1,
    existing: "CFL26W-1L-Glass-Sconce-4100K",
  },
  {
    id: "IJ3",
    desc: "Jelly Jar",
    lampQty: 2,
    existing: "CFL32W-2L-Glass-Pendant-4100K",
  },
  {
    id: "IJSP",
    desc: "Jelly Jar",
    lampQty: 1,
    existing: "INC100W-1L-Shatterproof-Sconce-2700K",
  },
  {
    id: "IMH1",
    desc: "Socket",
    lampQty: 1,
    existing: "MH300W-Socket-No Lens-Surface-4100K",
  },
  {
    id: "IS1",
    desc: "Sconce",
    lampQty: 1,
    existing: "CFL26W-Sconce-1L-Lens-Surface",
  },
  {
    id: "IS2",
    desc: "Sconce",
    lampQty: 2,
    existing: "CFL26W-Sconce-2L-Lens-Surface",
  },
  {
    id: "IS3",
    desc: "Circle",
    lampQty: 2,
    existing: "Circle13W-2L-Lens-Surface-4100K",
  },
  {
    id: "IS4",
    desc: "Bulkhead",
    lampQty: 1,
    existing: "CFL13W-Bulkhead-1L-Lens-Wall",
  },
  {
    id: "J1",
    desc: "4' Industrial Strip",
    lampQty: 1,
    existing: "F32T8-1L-4IND-No Lens-Surface-4100K",
  },
  {
    id: "J2",
    desc: "4' Industrial Strip",
    lampQty: 1,
    existing: "F32T8-1L-4IND-No Lens-Pendant-4100K",
  },
  {
    id: "J3",
    desc: "4' Industrial Strip",
    lampQty: 2,
    existing: "F32T8-2L-4IND-No Lens-Surface-4100K",
  },
  {
    id: "J4",
    desc: "4' Industrial Strip",
    lampQty: 2,
    existing: "F32T8-2L-4IND-No Lens-Pendant-4100K",
  },
  {
    id: "J5",
    desc: "4' Industrial Strip",
    lampQty: 1,
    existing: "F34T12-1L-4IND-No Lens-Surface-4100K",
  },
  {
    id: "J6",
    desc: "4' Industrial Strip",
    lampQty: 1,
    existing: "F34T12-1L-4IND-No Lens-Pendant-4100K",
  },
  {
    id: "J7",
    desc: "4' Industrial Strip",
    lampQty: 2,
    existing: "F34T12-2L-4IND-No Lens-Surface-4100K",
  },
  {
    id: "J8",
    desc: "4' Industrial Strip",
    lampQty: 2,
    existing: "F34T12-2L-4IND-No Lens-Pendant-4100K",
  },
  {
    id: "J9",
    desc: "4' Industrial Strip",
    lampQty: 2,
    existing: "F28T5-2L-4IND-No Lens-Surface-4100K",
  },
  {
    id: "J10",
    desc: "4' Industrial Strip",
    lampQty: 2,
    existing: "F28T5-2L-4IND-No Lens-Pendant-4100K",
  },
  {
    id: "J11",
    desc: "4' Industrial Strip",
    lampQty: 3,
    existing: "F32T8-3L-4IND-No Lens-Pendant-4100K",
  },
  {
    id: "K1",
    desc: "8' Industrial Strip",
    lampQty: 2,
    existing: "F32T8-2L-8IND-No Lens-Surface-4100K",
  },
  {
    id: "K2",
    desc: "8' Industrial Strip",
    lampQty: 2,
    existing: "F32T8-2L-8IND-No Lens-Pendant-4100K",
  },
  {
    id: "K3",
    desc: "8' Industrial Strip",
    lampQty: 4,
    existing: "F32T8-4L-8IND-No Lens-Surface-4100K",
  },
  {
    id: "K4",
    desc: "8' Industrial Strip",
    lampQty: 4,
    existing: "F32T8-4L-8IND-No Lens-Pendant-4100K",
  },
  {
    id: "K5",
    desc: "8' Industrial Strip",
    lampQty: 2,
    existing: "F34T12-2L-8IND-No Lens-Surface-4100K",
  },
  {
    id: "K6",
    desc: "8' Industrial Strip",
    lampQty: 2,
    existing: "F34T12-2L-8IND-No Lens-Pendant-4100K",
  },
  {
    id: "K7",
    desc: "8' Industrial Strip",
    lampQty: 4,
    existing: "F34T12-4L-8IND-No Lens-Surface-4100K",
  },
  {
    id: "K8",
    desc: "8' Industrial Strip",
    lampQty: 4,
    existing: "F34T12-4L-8IND-No Lens-Pendant-4100K",
  },
  {
    id: "K9",
    desc: "8' Industrial Strip",
    lampQty: 2,
    existing: "F96T12-2L-8IND-No Lens-Surface-5000K",
  },
  {
    id: "K10",
    desc: "8' Industrial Strip",
    lampQty: 2,
    existing: "F96T12-2L-8IND-No Lens-Pendant-5000K",
  },
  {
    id: "K11",
    desc: "8' Industrial Strip",
    lampQty: 4,
    existing: "F96T12-4L-8IND-No Lens-Surface-4100K",
  },
  {
    id: "K12",
    desc: "8' Industrial Strip",
    lampQty: 4,
    existing: "F96T12-4L-8IND-No Lens-Pendant-4100K",
  },
  {
    id: "K13",
    desc: "8' Industrial Strip",
    lampQty: 1,
    existing: "F96T12-1L-8IND-No Lens-Pendant-4100K",
  },
  {
    id: "K14",
    desc: "8' Industrial Strip",
    lampQty: 2,
    existing: "F28T8-2L-8IND-No Lens-Surface-4100K",
  },
  {
    id: "K15",
    desc: "8' Industrial Strip",
    lampQty: 2,
    existing: "F28T8-2L-8IND-No Lens-Pendant-4100K",
  },
  {
    id: "K16",
    desc: "8' Industrial Strip",
    lampQty: 4,
    existing: "F28T8-4L-8IND-No Lens-Surface-4100K",
  },
  {
    id: "K17",
    desc: "8' Industrial Strip",
    lampQty: 4,
    existing: "F28T5-4L-8IND-No Lens-Pendant-4100K",
  },
  {
    id: "L1",
    desc: "2' Wrap",
    lampQty: 1,
    existing: "F17T8-1L-2Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "L2",
    desc: "2' Wrap",
    lampQty: 2,
    existing: "F17T8-2L-2Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "L3",
    desc: "2' Wrap",
    lampQty: 1,
    existing: "F20T12-1L-2Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "L4",
    desc: "2' Wrap",
    lampQty: 2,
    existing: "F20T12-2L-Wrap-Prismatic-Surface-4100K",
  },
  { id: "L5", desc: "2' Wrap", lampQty: null, existing: "" },
  { id: "L6", desc: "2' Wrap", lampQty: null, existing: "" },
  {
    id: "M1",
    desc: "4' Wrap",
    lampQty: 1,
    existing: "F32T8-1L-4Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "M2",
    desc: "4' Wrap",
    lampQty: 2,
    existing: "F32T8-2L-4Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "M3",
    desc: "4' Wrap",
    lampQty: 1,
    existing: "F32T8-1L-4Wrap-Prismatic-Pendant-4100K",
  },
  {
    id: "M4",
    desc: "4' Wrap",
    lampQty: 2,
    existing: "F32T8-2L-4Wrap-Prismatic-Pendant-4100K",
  },
  {
    id: "MI5",
    desc: "4' Wrap",
    lampQty: 1,
    existing: "F32T8-1L-4Wrap-Indirect-Pendant-4100K",
  },
  {
    id: "MI6",
    desc: "4' Wrap",
    lampQty: 2,
    existing: "F32T8-2L-4Wrap-Indirect-Pendant-4100K",
  },
  {
    id: "M5",
    desc: "4' Wrap",
    lampQty: 4,
    existing: "F32T8-4L-4Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "M6",
    desc: "4' Wrap",
    lampQty: 4,
    existing: "F32T8-4L-4Wrap-Prismatic-Pendant-4100K",
  },
  {
    id: "M7",
    desc: "4' Wrap",
    lampQty: 1,
    existing: "F28T5-1L-4Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "M8",
    desc: "4' Wrap",
    lampQty: 2,
    existing: "F28T5-2L-4Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "M9",
    desc: "4' Wrap",
    lampQty: 1,
    existing: "F28T5-1L-4Wrap-Prismatic-Pendant-4100K",
  },
  {
    id: "M10",
    desc: "4' Wrap",
    lampQty: 2,
    existing: "F28T5-2L-4Wrap-Prismatic-Pendant-4100K",
  },
  {
    id: "M11",
    desc: "4' Wrap",
    lampQty: 1,
    existing: "F28T5-1L-4Wrap-Indirect-Surface-4100K",
  },
  {
    id: "M12",
    desc: "4' Wrap",
    lampQty: 2,
    existing: "F28T5-2L-4Wrap-Indirect-Surface-4100K",
  },
  {
    id: "M13",
    desc: "4' Wrap",
    lampQty: 1,
    existing: "F28T5-1L-4Wrap-Indirect-Pendant-4100K",
  },
  {
    id: "M14",
    desc: "4' Wrap",
    lampQty: 2,
    existing: "F28T5-2L-4Wrap-Indirect-Pendant-4100K",
  },
  {
    id: "M15",
    desc: "4' Wrap",
    lampQty: 3,
    existing: "F32T8-3L-4Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "M16",
    desc: "4' Wrap",
    lampQty: 3,
    existing: "F32T8-3L-4Wrap-Prismatic-Pendant-4100K",
  },
  {
    id: "M17",
    desc: "4' Wrap",
    lampQty: 3,
    existing: "F32T8-3L-4Wrap-Indirect-Surface-4100K",
  },
  {
    id: "M18",
    desc: "4' Wrap",
    lampQty: 3,
    existing: "F32T8-3L-4Wrap-Indirect-Pendant-4100K",
  },
  {
    id: "MH",
    desc: "Metal Halide",
    lampQty: 1,
    existing: "Metal Halide175-1L-Lens-Rod",
  },
  {
    id: "MR",
    desc: "Halogen",
    lampQty: 1,
    existing: "H50MR16-1L-No Lens-3500K",
  },
  {
    id: "MH1",
    desc: "Metal Halide",
    lampQty: 1,
    existing: "Metal Halide250-1L-Lens-Surface",
  },
  {
    id: "MH3",
    desc: "Metal Halide",
    lampQty: 1,
    existing: "Metal Halide300-1L-Lens-Pendant",
  },
  {
    id: "MH4",
    desc: "Metal Halide",
    lampQty: 1,
    existing: "Metal Halide400-1L-Lens-Rod",
  },
  {
    id: "N1",
    desc: "8' Wrap",
    lampQty: 2,
    existing: "F32T8-2L-8Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "N2",
    desc: "8' Wrap",
    lampQty: 4,
    existing: "F32T8-4L-8Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "N3",
    desc: "8' Wrap",
    lampQty: 2,
    existing: "F32T8-2L-8Wrap-Prismatic-Pendant-4100K",
  },
  {
    id: "N4",
    desc: "8' Wrap",
    lampQty: 4,
    existing: "F32T8-4L-8Wrap-Prismatic-Pendant-4100K",
  },
  {
    id: "N5",
    desc: "8' Wrap",
    lampQty: 2,
    existing: "F32T8-2L-8Wrap-Indirect-Pendant-4100K",
  },
  {
    id: "N6",
    desc: "8' Wrap",
    lampQty: 4,
    existing: "F32T8-4L-8Wrap-Indirect-Pendant-4100K",
  },
  {
    id: "N7",
    desc: "8' Wrap",
    lampQty: 2,
    existing: "F28T8-2L-8Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "N8",
    desc: "8' Wrap",
    lampQty: 4,
    existing: "F28T5-4L-8Wrap-Prismatic-Surface-4100K",
  },
  {
    id: "N9",
    desc: "8' Wrap",
    lampQty: 2,
    existing: "F28T8-2L-8Wrap-Prismatic-Pendant-4100K",
  },
  {
    id: "N10",
    desc: "8' Wrap",
    lampQty: 4,
    existing: "F28T5-4L-8Wrap-Prismatic-Pendant-4100K",
  },
  {
    id: "N11",
    desc: "8' Wrap",
    lampQty: 2,
    existing: "F28T5-2L-8Wrap-Indirect-Surface-4100K",
  },
  {
    id: "N12",
    desc: "8' Wrap",
    lampQty: 4,
    existing: "F28T5-4L-8Wrap-Indirect-Surface-4100K",
  },
  {
    id: "N13",
    desc: "8' Wrap",
    lampQty: 2,
    existing: "F28T5-2L-8Wrap-Indirect-Pendant-4100K",
  },
  {
    id: "N14",
    desc: "8' Wrap",
    lampQty: 4,
    existing: "F28T5-4L-8Wrap-Indirect-Pendant-4100K",
  },
  {
    id: "N15",
    desc: "8' Wrap",
    lampQty: 3,
    existing: "F32T8-3L-8Wrap-Prismatic-Pendant-4100K",
  },
  {
    id: "O1",
    desc: "1X3",
    lampQty: 1,
    existing: "F25T8-1L-1x3-Prismatic-Recessed-4100K",
  },
  {
    id: "O2",
    desc: "1X3",
    lampQty: 1,
    existing: "F25T8-1L-1x3-Prismatic-Surface-4100K",
  },
  {
    id: "O3",
    desc: "1X3",
    lampQty: 2,
    existing: "F25T8-2L-1x3-Prismatic-Recessed-4100K",
  },
  {
    id: "O4",
    desc: "1X3",
    lampQty: 2,
    existing: "F25T8-2L-1x3-Prismatic-Surface-4100K",
  },
  {
    id: "O5",
    desc: "1X3",
    lampQty: 1,
    existing: "F30T12-1L-1x3-No Lens-Recessed-4100K",
  },
  {
    id: "O6",
    desc: "1X3",
    lampQty: 1,
    existing: "F30T12-1L-1x3-No Lens-Surface-4100K",
  },
  {
    id: "O7",
    desc: "1X3",
    lampQty: 1,
    existing: "F21T5-1L-1x3-Prismatic-Recessed",
  },
  {
    id: "O8",
    desc: "1X3",
    lampQty: 1,
    existing: "F21T5-1L-1x3-Prismatic-Surface",
  },
  {
    id: "O9",
    desc: "1X3",
    lampQty: 2,
    existing: "F21T5-2L-1x3-Prismatic-Recessed",
  },
  {
    id: "O10",
    desc: "1X3",
    lampQty: 2,
    existing: "F21T5-2L-1x3-Prismatic-Surface",
  },
  {
    id: "P1",
    desc: "A19",
    lampQty: 1,
    existing: "A1960W-1L-Incandescent-Aline-Surface-4100K",
  },
  {
    id: "P2",
    desc: "A19",
    lampQty: 1,
    existing: "A1975W-1L-Incandescent-Aline-Surface-4100K",
  },
  {
    id: "P3",
    desc: "A19",
    lampQty: 1,
    existing: "A19100W-1L-Incandescent-Aline-Surface-4100K",
  },
  {
    id: "P4",
    desc: "A19",
    lampQty: 1,
    existing: "A1940W-1L-Incandescent-Aline-Surface-2700K",
  },
  {
    id: "P5",
    desc: "4PIN",
    lampQty: 1,
    existing: "4PIN23W-1L-CFL-Aline-Surface-4100K",
  },
  {
    id: "P5-2",
    desc: "4PIN",
    lampQty: 2,
    existing: "4PIN23W-2L-CFL-Aline-Surface-4100K",
  },
  {
    id: "P6",
    desc: "A19",
    lampQty: 1,
    existing: "A1975W-1L-Incandescent-BR 30-Surface-4100K",
  },
  {
    id: "P7",
    desc: "Spiral",
    lampQty: 1,
    existing: "CFL42W-1L-CFL-Aline-surface-4100K",
  },
  {
    id: "P8",
    desc: "Spiral",
    lampQty: 1,
    existing: "CFL102-1L-CFL-Aline-surface-4100K (Mogul Base)",
  },
  {
    id: "P9",
    desc: "Spiral",
    lampQty: 1,
    existing: "CFL65-1L-CFL-Aline-surface-4100K (Mogul Base)",
  },
  {
    id: "Q1",
    desc: "1X2",
    lampQty: 1,
    existing: "F17T8-1L-1x2-Prismatic-Recessed-4100K",
  },
  {
    id: "Q2",
    desc: "1X2",
    lampQty: 2,
    existing: "F17T8-2L-1x2-Prismatic-Recessed-4100K",
  },
  {
    id: "Q3",
    desc: "1X2",
    lampQty: 1,
    existing: "F17T8-1L-1x2-Parabolic-Recessed-4100K",
  },
  {
    id: "Q4",
    desc: "1X2",
    lampQty: 2,
    existing: "F17T8-2L-1x2-Parabolic-Recessed-4100K",
  },
  { id: "R1", desc: "Round", lampQty: 2, existing: "" },
  { id: "R2", desc: "Round", lampQty: 2, existing: "CFL23W-2L-Lens-Surface" },
  { id: "R3", desc: "Round", lampQty: 3, existing: "CFL13W-3L-Lens-Surface" },
  { id: "R4", desc: "Round", lampQty: 3, existing: "" },
  { id: "R5", desc: "Round", lampQty: 3, existing: "" },
  { id: "R6", desc: "Round", lampQty: 3, existing: "" },
  { id: "R7", desc: "Round", lampQty: 3, existing: "" },
  { id: "R8", desc: "Round", lampQty: 3, existing: "" },
  { id: "R9", desc: "Round", lampQty: null, existing: "" },
  { id: "R10", desc: "Round", lampQty: null, existing: "" },
  {
    id: "R11",
    desc: "Round",
    lampQty: 1,
    existing: "CFL32W-1L-No Lens-Pendant",
  },
  { id: "R12", desc: "Round", lampQty: 4, existing: "CFL26W-4L-Lens-Pendant" },
  { id: "S", desc: "", lampQty: null, existing: "" },
  {
    id: "T1",
    desc: "1x2",
    lampQty: 1,
    existing: "F17T8-1L-Tamper Proof-Recessed-4100K",
  },
  {
    id: "T2",
    desc: "1x2",
    lampQty: 2,
    existing: "F17T8-2L-Tamper Proof-Recessed-4100K",
  },
  {
    id: "T3",
    desc: "1x2",
    lampQty: 1,
    existing: "F17T8-1L-Tamper Proof-Surface-4100K",
  },
  {
    id: "T4",
    desc: "1x2",
    lampQty: 2,
    existing: "F17T8-2L-Tamper Proof-Surface-4100K",
  },
  {
    id: "T5",
    desc: "1x4",
    lampQty: 1,
    existing: "F32T8-1L-Tamper Proof-Recessed-4100K",
  },
  {
    id: "T6",
    desc: "1x4",
    lampQty: 2,
    existing: "F32T8-2L-Tamper Proof-Recessed-4100K",
  },
  {
    id: "T7",
    desc: "1x4",
    lampQty: 1,
    existing: "F32T8-1L-Tamper Proof-Surface-4100K",
  },
  {
    id: "T8",
    desc: "1x4",
    lampQty: 2,
    existing: "F32T8-2L-Tamper Proof-Surface-4100K",
  },
  {
    id: "T9",
    desc: "1x4",
    lampQty: 1,
    existing: "F32T8-1L-Tamper Proof-Pendant-4100K",
  },
  {
    id: "T10",
    desc: "1x4",
    lampQty: 2,
    existing: "F32T8-2L-Tamper Proof-Pendant-4100K",
  },
  { id: "U1", desc: "", lampQty: null, existing: "" },
  { id: "U2", desc: "", lampQty: null, existing: "" },
  { id: "Uk", desc: "", lampQty: null, existing: "Unknown-Fixture" },
  {
    id: "V1",
    desc: "2' Wrap",
    lampQty: 1,
    existing: "F17T8-1L-VaporProof-Surface-4100K",
  },
  {
    id: "V2",
    desc: "2' Wrap",
    lampQty: 2,
    existing: "F17T8-2L-VaporProof-Surface-4100K",
  },
  {
    id: "V3",
    desc: "4' Wrap",
    lampQty: 1,
    existing: "F32T8-1L-VaporProof-Surface-4100K",
  },
  {
    id: "V4",
    desc: "4' Wrap",
    lampQty: 2,
    existing: "F32T8-2L-VaporProof-Surface-4100K",
  },
  {
    id: "V5",
    desc: "4' Wrap",
    lampQty: 1,
    existing: "F25T5-1L-VaporProof-Surface-4100K",
  },
  {
    id: "V6",
    desc: "4' Wrap",
    lampQty: 2,
    existing: "F25T5-2L-VaporProof-Surface-4100K",
  },
  {
    id: "V7",
    desc: "4' Wrap",
    lampQty: 1,
    existing: "F25T5-1L-VaporProof-Pendant-4100K",
  },
  {
    id: "V8",
    desc: "4' Wrap",
    lampQty: 2,
    existing: "F25T5-2L-VaporProof-Pendant-4100K",
  },
  {
    id: "V9",
    desc: "4' Wrap",
    lampQty: 4,
    existing: "F32T8-4L-VaporProof-Pendant-4100K",
  },
  {
    id: "V10",
    desc: "4' Wrap",
    lampQty: 4,
    existing: "F32T8-4L-VaporProof-Surface-4100K",
  },
  {
    id: "V11",
    desc: "4' Wrap",
    lampQty: 2,
    existing: "F32T8-2L-VaporProof-Pendant-4100K",
  },
  {
    id: "V12",
    desc: "4' Wrap",
    lampQty: 2,
    existing: "F32T8-2L-VaporProof-Surface-4100K",
  },
  {
    id: "V13",
    desc: "4' Wrap",
    lampQty: 4,
    existing: "F32T8-4L-VaporProof-Surface-4100K",
  },
  {
    id: "V14",
    desc: "8' Wrap",
    lampQty: 4,
    existing: "F32T8-4L-VaporProof-Surface-4100K - 8FT",
  },
  {
    id: "CC1",
    desc: "",
    lampQty: null,
    existing: "Fixture with Integral Controls Philips SNS 210",
  },
  {
    id: "CC2",
    desc: "",
    lampQty: null,
    existing: "Wall Switch Occupancy Sensor - SensorWorx SWX-123-WH",
  },
  {
    id: "CC3",
    desc: "",
    lampQty: null,
    existing: "Wireless Switch Illumara ZBT-S1AWH",
  },
  {
    id: "CC4",
    desc: "",
    lampQty: null,
    existing: "Ceiling Mounted Sensor -SensorWorx-222-1",
  },
  {
    id: "CC4-HB",
    desc: "",
    lampQty: null,
    existing: "Ceiling Mounted Sensor Highbay -SensorWorx SWX-501",
  },
  {
    id: "CC5",
    desc: "",
    lampQty: null,
    existing: "Power Pack - SensorWorx SWX-900-AX",
  },
  {
    id: "CC6",
    desc: "",
    lampQty: null,
    existing: "Fixture with built-in motion sensor",
  },
  {
    id: "CC7",
    desc: "",
    lampQty: null,
    existing: "Low Voltage Switch SWX-801",
  },
  {
    id: "CC1-DOE",
    desc: "",
    lampQty: null,
    existing: "Vacancy, Photocell, Daylight",
  },
  {
    id: "CC2-DOE",
    desc: "X",
    lampQty: null,
    existing: "Acuity Wall Switch Occupancy Sensor WSX PDT SA WH",
  },
  {
    id: "CC3-DOE-BP",
    desc: "X",
    lampQty: null,
    existing: "Acuity RPODBA-DX-WH-G2",
  },
  {
    id: "CC3-DOE",
    desc: "X",
    lampQty: null,
    existing: "Wireless Switch RPODLA DX MVOLT WH G2",
  },
  {
    id: "CC4-DOE",
    desc: "X",
    lampQty: null,
    existing: "Ceiling Mounted Sensor RCMS-PDT-10-G2",
  },
  {
    id: "CC5-DOE",
    desc: "X",
    lampQty: null,
    existing: "Power Pack RPP20 D 24V EFP G2",
  },
  {
    id: "CC6-DOE",
    desc: "",
    lampQty: null,
    existing: "Fixture with built-in motion sensor",
  },
  {
    id: "CC8-DOE",
    desc: "X",
    lampQty: null,
    existing: "LBI IR sensor RP-LBI-OC2-A",
  },
  {
    id: "CC9-DOE",
    desc: "X",
    lampQty: null,
    existing: "Acuity rLSXR 10 WH G2",
  },
  { id: "CC10-DOE", desc: "X", lampQty: null, existing: "Acuity time clock" },
  { id: "CC11-DOE", desc: "X", lampQty: null, existing: "LCLSENSE/SH" },
  {
    id: "CC-RAB1",
    desc: "",
    lampQty: null,
    existing: "Wall Switch (Toggle or Rocker)",
  },
  {
    id: "CC-RAB2",
    desc: "",
    lampQty: null,
    existing: "Fixture-Mounted Sensor - PIR12W/ARM/LCB",
  },
  {
    id: "CC-RAB3",
    desc: "",
    lampQty: null,
    existing: "Power Pack - LCBLUECONTROL/W",
  },
  {
    id: "CC-RAB4",
    desc: "",
    lampQty: null,
    existing: "Wireless Dimming Switch W/ Sensor - RAB ZDIM/LCBS",
  },
  { id: "CC-RAB5", desc: "", lampQty: null, existing: "External PIR20 sensor" },
  { id: "CC-RAB6", desc: "", lampQty: null, existing: "External PIR40 Sensor" },
  {
    id: "CC-RAB7",
    desc: "",
    lampQty: null,
    existing: "Wall Switch Vacancy Sensor",
  },
  {
    id: "CC-RAB8",
    desc: "",
    lampQty: null,
    existing: "Wall Switch (Toggle or Rocker)",
  },
  { id: "CC1-DS", desc: "", lampQty: null, existing: "Toggle Switch" },
  { id: "CC2-DS", desc: "", lampQty: null, existing: "Toggle Switch" },
  { id: "CC3-DS", desc: "", lampQty: null, existing: "" },
  { id: "CC4-DS", desc: "", lampQty: null, existing: "" },
  { id: "CC5-DS", desc: "", lampQty: null, existing: "" },
  { id: "LN1", desc: "", lampQty: null, existing: "" },
  { id: "LN2", desc: "", lampQty: null, existing: "" },
  { id: "LN3", desc: "", lampQty: null, existing: "" },
  { id: "LN4", desc: "", lampQty: null, existing: "" },
  { id: "LN5", desc: "", lampQty: null, existing: "" },
  { id: "LN6", desc: "", lampQty: null, existing: "" },
  { id: "LN7", desc: "", lampQty: null, existing: "" },
  { id: "LN8", desc: "", lampQty: null, existing: "" },
  { id: "LN9", desc: "", lampQty: null, existing: "" },
  { id: "LN10", desc: "", lampQty: null, existing: "" },
  { id: "LNTG", desc: "", lampQty: null, existing: "" },
  {
    id: "X0",
    desc: "Exit Sign",
    lampQty: null,
    existing: "Existing LED Exit Sign",
  },
  { id: "X1", desc: "Exit Sign", lampQty: 2, existing: "2L 13W CFL" },
  {
    id: "CL",
    desc: "Cloud Circle",
    lampQty: 6,
    existing: "F21T5-6L-Lens-Surface-2700K",
  },
  { id: "NA", desc: "", lampQty: null, existing: "No access" },
  { id: "I31", desc: '3" Recessed Can', lampQty: 1, existing: "" },
  { id: "I32", desc: '3" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I33",
    desc: '3" Recessed Can',
    lampQty: 1,
    existing: '3"-CFL13W-1L-No Lens-Recessed Can-3500K',
  },
  {
    id: "I34",
    desc: '3" Recessed Can',
    lampQty: 1,
    existing: '3"-CFL13W-1L-Lens-Recessed Can-3500K',
  },
  {
    id: "I35",
    desc: '3" Recessed Can',
    lampQty: 1,
    existing: '3"-CFL18W-1L-No Lens-Recessed Can-3500K',
  },
  {
    id: "I35-EB",
    desc: '3" Recessed Can',
    lampQty: 1,
    existing: '3"-CFL18W-1L-No Lens-Recessed Can-3500K-Emergency Ballast',
  },
  { id: "I36", desc: '3" Recessed Can', lampQty: 1, existing: "" },
  { id: "I37", desc: '3" Recessed Can', lampQty: 1, existing: "" },
  { id: "I38", desc: '3" Recessed Can', lampQty: 1, existing: "" },
  { id: "I39", desc: '3" Recessed Can', lampQty: 1, existing: "" },
  { id: "I310", desc: '3" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I51",
    desc: '5" Recessed Can',
    lampQty: 1,
    existing: '5"-MH75W-1-No Lens-Recessed Can-3500K',
  },
  { id: "I61", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I62",
    desc: '6" Recessed Can',
    lampQty: 1,
    existing: '6"-CFL-13W-1L-Lens-Recessed can',
  },
  {
    id: "I63",
    desc: '6" Recessed Can',
    lampQty: 2,
    existing: '6"-CFL-13W-2L-No Lens-Recessed can',
  },
  { id: "I64", desc: '6" Recessed Can', lampQty: 2, existing: "" },
  { id: "I65", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I66", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I67",
    desc: '6" Recessed Can',
    lampQty: 2,
    existing: '6"-CFL18W-2L-No Lens-Recessed Can',
  },
  {
    id: "I68",
    desc: '6" Recessed Can',
    lampQty: 2,
    existing: '6"-CFL18W-2L-Lens-Recessed Can',
  },
  { id: "I69", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I610", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I611", desc: '6" Recessed Can', lampQty: 2, existing: "" },
  {
    id: "I612",
    desc: '6" Recessed Can',
    lampQty: 2,
    existing: '6"-CFL26W-2L-Lens-Recessed Can-3500K',
  },
  {
    id: "I613",
    desc: '6" Recessed Can',
    lampQty: 1,
    existing: '6"-CFL32W-1L-No Lens-Recessed Can-3500K',
  },
  {
    id: "I613-EB",
    desc: '6" Recessed Can',
    lampQty: 1,
    existing: '6"-CFL32W-1L-No Lens-Recessed Can-3500K-Emergency Ballast',
  },
  {
    id: "I614",
    desc: '6" Recessed Can',
    lampQty: 1,
    existing: '6"-CFL32W-1L-Lens-Recessed Can-3500K',
  },
  { id: "I615", desc: '6" Recessed Can', lampQty: 2, existing: "" },
  { id: "I616", desc: '6" Recessed Can', lampQty: 2, existing: "" },
  { id: "I617", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I618", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I619", desc: '6" Recessed Can', lampQty: 2, existing: "" },
  { id: "I620", desc: '6" Recessed Can', lampQty: 2, existing: "" },
  { id: "I621", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I622", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I623", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I624", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I625", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I626", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I627", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I628", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I629",
    desc: '6" Recessed Can',
    lampQty: 1,
    existing: '6"-75WPAR40-1L-No Lens-Recessed',
  },
  { id: "I630", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I631", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I632", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I633",
    desc: '6" Recessed Can',
    lampQty: 1,
    existing: '6"-MH75W-1L-No Lens-Recessed',
  },
  {
    id: "I634",
    desc: '6" Recessed Can',
    lampQty: 1,
    existing: '6"-MH75W-1L-Lens-Recessed',
  },
  { id: "I635", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I636", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I637", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  { id: "I638", desc: '6" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I639",
    desc: '6" Recessed Can',
    lampQty: 1,
    existing: '6"-MH175W-1L-No Lens-Recessed-3500K',
  },
  {
    id: "I640",
    desc: '6" Recessed Can',
    lampQty: 1,
    existing: '6"-MH175W-1L-Lens-Recessed-3500K',
  },
  {
    id: "I81",
    desc: '8" Recessed Can',
    lampQty: 1,
    existing: '8"-CFL13W-1L-No Lens-Recessed Can-3500K',
  },
  {
    id: "I82",
    desc: '8" Recessed Can',
    lampQty: 1,
    existing: '8"-CFL13W-1L-Lens-Recessed Can-3500K',
  },
  {
    id: "I83",
    desc: '8" Recessed Can',
    lampQty: 2,
    existing: '8"-CFL13W-2L-No Lens-Recessed Can-3500K',
  },
  {
    id: "I84",
    desc: '8" Recessed Can',
    lampQty: 2,
    existing: '8"-CFL13W-2L-Lens-Recessed Can-3500K',
  },
  { id: "I85", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I86", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I87",
    desc: '8" Recessed Can',
    lampQty: 2,
    existing: '8"-CFL18W-2L-No Lens-Recessed-3500K',
  },
  { id: "I88", desc: '8" Recessed Can', lampQty: 2, existing: "" },
  { id: "I89", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I810",
    desc: '8" Recessed Can',
    lampQty: 1,
    existing: '8"-CFL26W-1L-Lens-Recessed-3500K',
  },
  { id: "I811", desc: '8" Recessed Can', lampQty: 2, existing: "" },
  { id: "I812", desc: '8" Recessed Can', lampQty: 2, existing: "" },
  { id: "I813", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I814", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I815",
    desc: '8" Recessed Can',
    lampQty: 2,
    existing: '8"-CFL32W-2L-No Lens-Recessed-3500K',
  },
  { id: "I816", desc: '8" Recessed Can', lampQty: 2, existing: "" },
  { id: "I817", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I818", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I819", desc: '8" Recessed Can', lampQty: 2, existing: "" },
  {
    id: "I820",
    desc: '8" Recessed Can',
    lampQty: 2,
    existing: '8"-CFL34W-2L-No Lens-Recessed-3500K',
  },
  { id: "I821", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I822", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I823", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I824", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I825",
    desc: '8" Recessed Can',
    lampQty: 1,
    existing: '8"-75WPAR38-1L-No Lens-Recessed',
  },
  { id: "I826", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I827", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I828", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I829", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I830", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I831", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I832", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I833", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I834", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I835", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I836", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I837", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I838", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I839", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  { id: "I840", desc: '8" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I121",
    desc: '12" Recessed Can',
    lampQty: 1,
    existing: '12"-CFL26W-1L-No Lens-Recessed Can-3500K',
  },
  {
    id: "I122",
    desc: '12" Recessed Can',
    lampQty: 1,
    existing: '12"-CFL26W-1L-Lens-Recessed Can-3500K',
  },
  { id: "I123", desc: '12" Recessed Can', lampQty: 2, existing: "" },
  {
    id: "I124",
    desc: '12" Recessed Can',
    lampQty: 2,
    existing: '12"-CFL26W-2L-Lens-Recessed Can',
  },
  { id: "I125", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I126", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I127",
    desc: '12" Recessed Can',
    lampQty: 2,
    existing: '12"-CFL32W-2L-No Lens-Recessed Can-3500K',
  },
  { id: "I128", desc: '12" Recessed Can', lampQty: 2, existing: "" },
  { id: "I129", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1210", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1211", desc: '12" Recessed Can', lampQty: 2, existing: "" },
  { id: "I1212", desc: '12" Recessed Can', lampQty: 2, existing: "" },
  { id: "I1213", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1214", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1215", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1216", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1217", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1218", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1219", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1220", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  {
    id: "I1221",
    desc: '12" Recessed Can',
    lampQty: 1,
    existing: '12"-125W-PAR40-1L-No Lens-Recessed Can-3500K',
  },
  {
    id: "I1222",
    desc: '12" Recessed Can',
    lampQty: 1,
    existing: '12"-125W-PAR40-1L-Lens-Recessed Can-3500K',
  },
  { id: "I1223", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1224", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1225", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1226", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1227", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1228", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1229", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1230", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1231", desc: '12" Recessed Can', lampQty: 1, existing: "" },
  { id: "I1232", desc: '12" Recessed Can', lampQty: 1, existing: "" },
];

// ── FIXTURE BUILDER ─────────────────────────────────────────────────────
// State for the cascading selector
let fbSelType = null;
let fbSelSize = null;
let fbSelTech = null;
const fbRecentDescs = []; // [{desc, watts}]

function openFixtureBuilder() {
  fbSelType = null;
  fbSelSize = null;
  fbSelTech = null;
  document.getElementById("fbSearch").value = "";
  document.getElementById("fbSearchResults").style.display = "none";
  document.getElementById("fbCustomForm").style.display = "none";
  fbRenderTypes();
  fbUpdateBreadcrumb();
  fbRenderRecent();
  document.getElementById("fixtureBuilderModal").style.display = "block";
}

function closeFixtureBuilder() {
  document.getElementById("fixtureBuilderModal").style.display = "none";
}

// Type lookup that handles the Exterior nested subgroup transparently
function fbGetTypeNode(type) {
  if (FIXTURE_CATALOGUE[type] && type !== "Exterior")
    return FIXTURE_CATALOGUE[type];
  return (FIXTURE_CATALOGUE["Exterior"] || {})[type] || {};
}

function fbUpdateBreadcrumb() {
  const parts = ["Choose fixture type"];
  if (fbSelType) {
    if (fbIsExteriorType(fbSelType)) parts.push("Exterior", fbSelType);
    else parts.push(fbSelType);
  }
  if (fbSelSize) {
    parts.push(fbSelSize);
  }
  if (fbSelTech) {
    parts.push(fbSelTech);
  }
  document.getElementById("fbBreadcrumb").textContent = parts.join(" → ");
}

function fbIsExteriorType(type) {
  return !!(
    FIXTURE_CATALOGUE["Exterior"] && FIXTURE_CATALOGUE["Exterior"][type]
  );
}

// Most-used interior types pinned at the top, in this order. Anything else lives under "More Fixtures".
const FB_PRIORITY_TYPES = [
  "Wrap",
  "Strip",
  "Troffer",
  "Industrial",
  "High Bay",
  "Downlight",
  "Vapor Tight",
  "Vapor Jar",
];

function fbRenderTypes() {
  // Top level: 8 pinned interior types + "More Fixtures ›" + "Exterior ›"
  const wrap = document.getElementById("fbTypeButtons");
  wrap.innerHTML = "";
  FB_PRIORITY_TYPES.forEach((t) => {
    if (!FIXTURE_CATALOGUE[t]) return;
    const b = document.createElement("button");
    b.className = "fb-type-btn" + (fbSelType === t ? " active" : "");
    b.textContent = t;
    b.addEventListener("click", () => fbSelectType(t));
    wrap.appendChild(b);
  });
  // Are there any interior types not in the priority list?
  const overflow = Object.keys(FIXTURE_CATALOGUE).filter(
    (t) => t !== "Exterior" && !FB_PRIORITY_TYPES.includes(t),
  );
  if (overflow.length) {
    const more = document.createElement("button");
    more.className = "fb-type-btn";
    more.style.background = "#f0f5ff";
    more.style.borderColor = "#a8c8e8";
    more.style.color = "#1a4a80";
    more.textContent = "More Fixtures ›";
    more.addEventListener("click", fbRenderMoreFixtures);
    wrap.appendChild(more);
  }
  if (FIXTURE_CATALOGUE["Exterior"]) {
    const ext = document.createElement("button");
    ext.className = "fb-type-btn";
    ext.style.background = "#fef3e2";
    ext.style.borderColor = "#e8a050";
    ext.style.color = "#8a4810";
    ext.textContent = "Exterior ›";
    ext.addEventListener("click", fbRenderExteriorTypes);
    wrap.appendChild(ext);
  }
  document.getElementById("fbStep1").style.display = "block";
  document.getElementById("fbStep2").style.display = "none";
  document.getElementById("fbStep3").style.display = "none";
  document.getElementById("fbStep4").style.display = "none";
}

function fbRenderMoreFixtures() {
  const wrap = document.getElementById("fbTypeButtons");
  wrap.innerHTML = "";
  const back = document.createElement("button");
  back.className = "fb-type-btn";
  back.style.background = "#f0f4f8";
  back.style.borderColor = "#c8d4e3";
  back.style.color = "#5a7a9a";
  back.textContent = "‹ Back";
  back.addEventListener("click", fbRenderTypes);
  wrap.appendChild(back);
  const overflow = Object.keys(FIXTURE_CATALOGUE)
    .filter((t) => t !== "Exterior" && !FB_PRIORITY_TYPES.includes(t))
    .sort();
  overflow.forEach((t) => {
    const b = document.createElement("button");
    b.className = "fb-type-btn" + (fbSelType === t ? " active" : "");
    b.style.background = "#eaf3ff";
    b.style.borderColor = "#a8c8e8";
    b.style.color = "#1a4a80";
    b.textContent = t;
    b.addEventListener("click", () => fbSelectType(t));
    wrap.appendChild(b);
  });
}

function fbRenderExteriorTypes() {
  const wrap = document.getElementById("fbTypeButtons");
  wrap.innerHTML = "";
  const back = document.createElement("button");
  back.className = "fb-type-btn";
  back.style.background = "#f0f4f8";
  back.style.borderColor = "#c8d4e3";
  back.style.color = "#5a7a9a";
  back.textContent = "‹ Back";
  back.addEventListener("click", fbRenderTypes);
  wrap.appendChild(back);
  const exteriorTypes = Object.keys(FIXTURE_CATALOGUE["Exterior"] || {}).sort();
  exteriorTypes.forEach((t) => {
    const b = document.createElement("button");
    b.className = "fb-type-btn" + (fbSelType === t ? " active" : "");
    b.style.background = "#fff5e8";
    b.style.borderColor = "#f0c098";
    b.style.color = "#8a4810";
    b.textContent = t;
    b.addEventListener("click", () => fbSelectType(t));
    wrap.appendChild(b);
  });
}

function fbSelectType(type) {
  fbSelType = type;
  fbSelSize = null;
  fbSelTech = null;
  fbUpdateBreadcrumb();
  document
    .querySelectorAll(".fb-type-btn")
    .forEach((b) => b.classList.toggle("active", b.textContent === type));
  const sizes = Object.keys(fbGetTypeNode(type)).sort();
  const wrap = document.getElementById("fbSizeButtons");
  wrap.innerHTML = "";
  sizes.forEach((s) => {
    const b = document.createElement("button");
    b.className = "fb-size-btn";
    b.textContent = s;
    b.addEventListener("click", () => fbSelectSize(s, type));
    wrap.appendChild(b);
  });
  document.getElementById("fbStep2").style.display = "block";
  document.getElementById("fbStep3").style.display = "none";
  document.getElementById("fbStep4").style.display = "none";
}

function fbSelectSize(size, type) {
  fbSelType = type || fbSelType;
  fbSelSize = size;
  fbSelTech = null;
  fbUpdateBreadcrumb();
  document
    .querySelectorAll(".fb-size-btn")
    .forEach((b) => b.classList.toggle("active", b.textContent === size));
  const techs = Object.keys(fbGetTypeNode(fbSelType)[size] || {}).sort();
  const wrap = document.getElementById("fbTechButtons");
  wrap.innerHTML = "";
  techs.forEach((t) => {
    const b = document.createElement("button");
    b.className = "fb-tech-btn";
    b.textContent = t;
    b.addEventListener("click", () => fbSelectTech(t, size, fbSelType));
    wrap.appendChild(b);
  });
  document.getElementById("fbStep3").style.display = "block";
  document.getElementById("fbStep4").style.display = "none";
}

function fbSelectTech(tech, size, type) {
  fbSelType = type || fbSelType;
  fbSelSize = size || fbSelSize;
  fbSelTech = tech;
  fbUpdateBreadcrumb();
  document
    .querySelectorAll(".fb-tech-btn")
    .forEach((b) => b.classList.toggle("active", b.textContent === tech));
  const descs = (fbGetTypeNode(fbSelType)[fbSelSize] || {})[tech] || [];
  const list = document.getElementById("fbDescList");
  list.innerHTML = "";
  descs.forEach((d) => {
    const row = document.createElement("div");
    row.className = "fb-desc-row";
    row.innerHTML = `<span class="fb-desc-text">${d.desc}</span>${
      d.watts ? `<span class="fb-desc-watts">${d.watts}W</span>` : ""
    }<span class="fb-desc-select">›</span>`;
    row.addEventListener("click", () => fbDoSelect(d.desc, d.watts));
    list.appendChild(row);
  });
  document.getElementById("fbStep4").style.display = descs.length
    ? "block"
    : "none";
}

function fbDoSelect(desc, watts, opts) {
  const isNew = !!(opts && opts.isNewAddition);
  const displayDesc = isNew ? `[NEW] ${desc}` : desc;
  const isExt = fbIsExteriorType(fbSelType);
  const code =
    "FB:" +
    (isNew ? "NEW" : "") +
    desc
      .substring(0, 30)
      .replace(/[^a-zA-Z0-9]/g, "")
      .toUpperCase();
  FIXTURES[code] = {
    desc: displayDesc,
    watts: isNew ? 0 : watts || null,
    type: isExt ? "Exterior" : "Interior",
    _newAddition: isNew,
  };
  state.activeFixture = code;

  // Track in recent
  const already = fbRecentDescs.findIndex((r) => r.desc === displayDesc);
  if (already !== -1) fbRecentDescs.splice(already, 1);
  fbRecentDescs.unshift({ desc: displayDesc, watts: isNew ? 0 : watts });
  if (fbRecentDescs.length > 6) fbRecentDescs.pop();

  closeFixtureBuilder();
  syncFixturePills();
  toast(
    isNew
      ? "New fixture selected — set qty and tap Add ›"
      : "Fixture selected — set qty and tap Add ›",
  );
}

function fbOnSearch(q) {
  const term = q.toLowerCase().trim();
  const searchWrap = document.getElementById("fbSearchResults");
  const steps = [
    document.getElementById("fbStep1"),
    document.getElementById("fbStep2"),
    document.getElementById("fbStep3"),
    document.getElementById("fbStep4"),
  ];
  if (!term) {
    searchWrap.style.display = "none";
    steps.forEach((s) => {
      if (s.id === "fbStep1") s.style.display = "block";
    });
    fbRenderTypes();
    return;
  }
  // Search across all types/sizes/techs (interior + exterior, where exterior is nested)
  const results = [];
  const visit = (type, sizes, isExt) => {
    Object.entries(sizes).forEach(([size, techs]) => {
      Object.entries(techs).forEach(([tech, descs]) => {
        descs.forEach((d) => {
          if (d.desc.toLowerCase().includes(term)) {
            results.push({
              type,
              size,
              tech,
              desc: d.desc,
              watts: d.watts,
              ext: isExt,
            });
          }
        });
      });
    });
  };
  Object.entries(FIXTURE_CATALOGUE).forEach(([type, val]) => {
    if (type === "Exterior") {
      Object.entries(val).forEach(([extType, sizes]) =>
        visit(extType, sizes, true),
      );
    } else {
      visit(type, val, false);
    }
  });
  steps.forEach((s) => (s.style.display = "none"));
  searchWrap.style.display = "block";
  const list = document.getElementById("fbSearchList");
  if (!results.length) {
    list.innerHTML =
      '<p style="color:#8a9ab0;font-size:12px;text-align:center;padding:16px">No matches found — try different terms or add custom</p>';
    return;
  }
  list.innerHTML = "";
  results.slice(0, 60).forEach((r) => {
    const row = document.createElement("div");
    row.className = "fb-desc-row";
    const trail =
      (r.ext ? "Exterior · " : "") + `${r.type} · ${r.size} · ${r.tech}`;
    row.innerHTML = `<div style="flex:1;min-width:0">
        <div class="fb-desc-text">${r.desc}</div>
        <div style="font-size:10px;color:#8a9ab0;margin-top:2px">${trail}</div>
      </div>
      ${r.watts ? `<span class="fb-desc-watts">${r.watts}W</span>` : ""}
      <span class="fb-desc-select">›</span>`;
    row.addEventListener("click", () => fbDoSelect(r.desc, r.watts));
    list.appendChild(row);
  });
  if (results.length > 60) {
    const note = document.createElement("p");
    note.style.cssText =
      "color:#b06010;font-size:10px;text-align:center;padding:6px";
    note.textContent = `Showing 60 of ${results.length} — refine search`;
    list.appendChild(note);
  }
}

function fbToggleCustom() {
  const form = document.getElementById("fbCustomForm");
  const newFixForm = document.getElementById("fbNewFixForm");
  if (newFixForm && newFixForm.style.display !== "none")
    newFixForm.style.display = "none";
  form.style.display = form.style.display === "none" ? "block" : "none";
  if (form.style.display === "block") {
    document.getElementById("fbCustomDesc").value = "";
    document.getElementById("fbCustomWatts").value = "";
    setTimeout(() => document.getElementById("fbCustomDesc").focus(), 80);
  }
}

function fbSaveCustom() {
  const desc = document.getElementById("fbCustomDesc").value.trim();
  if (!desc) {
    toast("Enter a fixture description");
    return;
  }
  const watts =
    parseInt(document.getElementById("fbCustomWatts").value) || null;
  fbDoSelect(desc, watts);
}

function fbToggleNewFix() {
  const form = document.getElementById("fbNewFixForm");
  const customForm = document.getElementById("fbCustomForm");
  if (customForm && customForm.style.display !== "none")
    customForm.style.display = "none";
  form.style.display = form.style.display === "none" ? "block" : "none";
  if (form.style.display === "block") {
    document.getElementById("fbNewFixDesc").value = "";
    setTimeout(() => document.getElementById("fbNewFixDesc").focus(), 80);
  }
}

function fbSaveNewFixture() {
  const desc = document.getElementById("fbNewFixDesc").value.trim();
  if (!desc) {
    toast("Enter a new fixture description");
    return;
  }
  // No existing watts (this is a NEW addition, not a replacement)
  fbDoSelect(desc, 0, { isNewAddition: true });
}

function fbRenderRecent() {
  const wrap = document.getElementById("fbRecentWrap");
  const list = document.getElementById("fbRecentList");
  if (!fbRecentDescs.length) {
    wrap.style.display = "none";
    return;
  }
  wrap.style.display = "block";
  list.innerHTML = "";
  fbRecentDescs.forEach((r) => {
    const short = r.desc.length > 40 ? r.desc.substring(0, 40) + "…" : r.desc;
    const btn = document.createElement("button");
    btn.style.cssText =
      "padding:5px 10px;border-radius:8px;border:1.5px solid #b2deda;background:#e6f7f6;color:#0a6b65;font-size:11px;font-weight:600;cursor:pointer;font-family:inherit;text-align:left";
    btn.textContent = short;
    btn.title = r.desc;
    btn.addEventListener("click", () => fbDoSelect(r.desc, r.watts));
    list.appendChild(btn);
  });
}

function openFixtureBuilderWithType(type) {
  openFixtureBuilder();
  setTimeout(() => fbSelectType(type), 50);
}

// ── MATCH IDs LOOKUP ──────────────────────────────────────────────────────
// ── MATCH IDs LOOKUP ──────────────────────────────────────────────────────
// Build category list from unique descriptions (ordered by count desc)
const MATCH_CATEGORIES = (() => {
  const counts = {};
  MATCH_DATA.forEach((r) => {
    if (r.desc && r.desc.trim()) counts[r.desc] = (counts[r.desc] || 0) + 1;
  });
  const sorted = Object.entries(counts)
    .sort((a, b) => b[1] - a[1])
    .map(([d]) => d);
  if (MATCH_DATA.some((r) => !r.desc || !r.desc.trim()))
    sorted.push("Other / Controls");
  return sorted;
})();

const categoryFreq = {}; // tracks how many times each category has been tapped
let activeCategory = null;
let customFixtures = []; // user-created fixtures saved here

function openMatchIDs() {
  document.getElementById("matchSearch").value = "";
  activeCategory = null;
  buildCategoryPills();
  renderMatchTable("", null);
  document.getElementById("matchModal").style.display = "block";
}

function closeMatchIDs() {
  document.getElementById("matchModal").style.display = "none";
  document.getElementById("customFixtureForm").style.display = "none";
}

function saveCustomMatchFixture() {
  const idInput = document.getElementById("customFixIdInput");
  const descInput = document.getElementById("customFixDescInput");
  const id = idInput.value.trim();
  const desc = descInput.value.trim();
  if (!id) {
    toast("Enter a fixture ID");
    idInput.focus();
    return;
  }
  // Register in FIXTURES and customFixtures
  if (!FIXTURES[id]) {
    FIXTURES[id] = { desc: desc || id, watts: null, type: "Interior" };
  }
  if (!customFixtures.find((r) => r.id === id)) {
    customFixtures.push({ id, desc: desc, existing: desc, lampQty: null });
  }
  document.getElementById("customFixtureForm").style.display = "none";
  toast(`✔ Custom fixture "${id}" saved`);
  // Auto-select it
  selectFromLookup(id);
  // Rebuild pills so Custom Fixtures bubble appears
  buildCategoryPills();
}

function buildCategoryPills() {
  const wrap = document.getElementById("matchCategories");
  wrap.innerHTML = "";

  // Pinned top pills — fixed order, always visible, slightly larger/bolder
  // Includes 2x2 (D-series, desc='2X2'), LED, plus the 9 original pinned
  const PINNED = [
    { label: "8' Wrap", cat: "8' Wrap" },
    { label: "4' Wrap", cat: "4' Wrap" },
    { label: "2' Wrap", cat: "2' Wrap" },
    { label: "2x4", cat: "2x4" },
    { label: "2x2", cat: "2X2" },
    { label: "1x4", cat: "1x4" },
    { label: "LED", cat: "LED" },
    { label: "4' Industrial Strip", cat: "4' Industrial Strip" },
    { label: "8' Industrial Strip", cat: "8' Industrial Strip" },
    { label: '6" Recessed Can', cat: '6" Recessed Can' },
    { label: '8" Recessed Can', cat: '8" Recessed Can' },
    { label: "Tamper Proof", cat: "__TAMPER__" },
    { label: "Biax", cat: "__BIAX__" },
    { label: "Lamps", cat: "__LAMPS__" },
    { label: "Vapor Proof", cat: "__VAPOR__" },
    { label: "High Bay", cat: "__HIGHBAY__" },
    { label: "Direct/Indirect", cat: "__DI__" },
    { label: "Vapor Tight", cat: "__VAPORTIGHT__" },
    { label: "Exit Sign", cat: "Exit Sign" },
    { label: "Combo Exit", cat: "Combo Exit" },
    { label: "Exterior Exit", cat: "Exterior Exit" },
    { label: "EM Unit", cat: "EM Unit" },
  ];

  // All pill
  wrap.appendChild(
    makePill("All", activeCategory === null, 0, 1, () => {
      activeCategory = null;
      buildCategoryPills();
      renderMatchTable(document.getElementById("matchSearch").value, null);
    }),
  );

  // Pinned pills
  PINNED.forEach(({ label, cat }) => {
    const active = activeCategory === cat;
    const b = document.createElement("button");
    b.onclick = () => {
      activeCategory = cat;
      document.getElementById("matchSearch").value = "";
      buildCategoryPills();
      renderMatchTable("", cat);
    };
    b.style.cssText = `padding:7px 14px;border-radius:16px;font-size:13px;font-weight:700;cursor:pointer;transition:all .2s;border:2px solid ${
      active ? "#2563a8" : "#90b8e8"
    };background:${active ? "#2563a8" : "#ddeeff"};color:${
      active ? "#fff" : "#1a4a80"
    };white-space:nowrap`;
    b.textContent = label;
    wrap.appendChild(b);
  });

  // Divider
  const div = document.createElement("div");
  div.style.cssText = "width:100%;height:1px;background:#e4eaf2;margin:4px 0";
  wrap.appendChild(div);

  // Remaining standard categories — exclude pinned desc values and empty ones
  const PINNED_DESCS = new Set([
    "8' Wrap",
    "4' Wrap",
    "2' Wrap",
    "2x4",
    "2X2",
    "1x4",
    "LED",
    "4' Industrial Strip",
    "8' Industrial Strip",
    "2X2 Biax",
    "1X2 Biax",
    "1X4 Biax",
    "2X4 Biax",
    "Circular half globe",
    "A19",
    "4PIN",
    "Spiral",
    "1x2",
    "1x4",
    '6" Recessed Can',
    '8" Recessed Can',
  ]);
  const sorted = [...MATCH_CATEGORIES]
    .filter((c) => !PINNED_DESCS.has(c))
    .sort((a, b) => {
      const fa = categoryFreq[a] || 0,
        fb = categoryFreq[b] || 0;
      return fb - fa || a.localeCompare(b);
    });
  const maxFreq = Math.max(1, ...sorted.map((c) => categoryFreq[c] || 0));

  sorted.forEach((cat) => {
    wrap.appendChild(
      makePill(
        cat,
        activeCategory === cat,
        categoryFreq[cat] || 0,
        maxFreq,
        () => {
          categoryFreq[cat] = (categoryFreq[cat] || 0) + 1;
          activeCategory = cat;
          document.getElementById("matchSearch").value = "";
          buildCategoryPills();
          renderMatchTable("", cat);
        },
      ),
    );
  });

  // Custom Fixtures pill (only if any exist)
  if (customFixtures.length > 0) {
    const cfActive = activeCategory === "__CUSTOM__";
    const cfPill = makePill(
      `Custom Fixtures (${customFixtures.length})`,
      cfActive,
      0,
      maxFreq,
      () => {
        activeCategory = "__CUSTOM__";
        document.getElementById("matchSearch").value = "";
        buildCategoryPills();
        renderMatchTable("", "__CUSTOM__");
      },
    );
    cfPill.style.borderColor = cfActive ? "#7a3abd" : "#c8b8e8";
    cfPill.style.background = cfActive ? "#7a3abd" : "#f4f0fb";
    cfPill.style.color = cfActive ? "#fff" : "#7a3abd";
    wrap.appendChild(cfPill);
  }

  // + Custom pill
  const addPill = makePill("+ Custom", false, 0, maxFreq, () => {
    document.getElementById("customFixtureForm").style.display = "block";
    document.getElementById("customFixIdInput").value = "";
    document.getElementById("customFixDescInput").value = "";
    setTimeout(() => document.getElementById("customFixIdInput").focus(), 50);
  });
  addPill.style.borderStyle = "dashed";
  addPill.style.borderColor = "#c8d4e3";
  addPill.style.background = "#f4f7fb";
  addPill.style.color = "#7a9ab0";
  wrap.appendChild(addPill);
}

function makePill(label, active, freq, maxFreq, onClick) {
  const b = document.createElement("button");
  b.onclick = onClick;

  // Scale font and padding based on relative frequency (1.0 → 1.4x)
  const scale = maxFreq > 0 ? 1 + (freq / maxFreq) * 0.4 : 1;
  const fs = Math.round(11 * scale);
  const py = Math.round(5 * scale);
  const px = Math.round(11 * scale);

  b.style.cssText = `padding:${py}px ${px}px;border-radius:16px;font-size:${fs}px;font-weight:600;cursor:pointer;transition:all .2s;border:1.5px solid ${
    active ? "#2563a8" : "#c0d4f0"
  };background:${active ? "#2563a8" : "#e8f0fb"};color:${
    active ? "#fff" : "#2563a8"
  };white-space:nowrap`;
  b.textContent = label;
  return b;
}

function onMatchSearch(q) {
  activeCategory = null;
  buildCategoryPills();
  renderMatchTable(q, null);
}

function renderMatchTable(q, category) {
  const term = q.toLowerCase();
  let entries = MATCH_DATA;

  if (category === "__VAPOR__") {
    entries = MATCH_DATA.filter((r) => r.id.startsWith("V"));
  } else if (category === "__TAMPER__") {
    entries = MATCH_DATA.filter((r) => r.id.startsWith("T"));
  } else if (category === "__BIAX__") {
    entries = MATCH_DATA.filter((r) => r.id.startsWith("BX"));
  } else if (category === "__LAMPS__") {
    entries = MATCH_DATA.filter((r) => r.id.startsWith("P"));
  } else if (category === "__HIGHBAY__") {
    entries = MATCH_DATA.filter(
      (r) => r.id.startsWith("HB") || r.desc === "2x4 Highbay",
    );
  } else if (category === "__DI__") {
    entries = MATCH_DATA.filter(
      (r) =>
        (r.existing || "").toLowerCase().includes("direct/indirect") ||
        (r.desc || "").toLowerCase().includes("direct"),
    );
  } else if (category === "__VAPORTIGHT__") {
    entries = MATCH_DATA.filter((r) =>
      (r.existing || "").toLowerCase().includes("vapor tight"),
    );
  } else if (category === "__CUSTOM__") {
    entries = customFixtures;
  } else if (category === "2X2") {
    entries = MATCH_DATA.filter(
      (r) => r.desc === "2X2" && !r.id.startsWith("V"),
    );
  } else if (category && category !== "Other / Controls") {
    entries = entries.filter(
      (r) => r.desc === category && !r.id.startsWith("V"),
    );
  } else if (category === "Other / Controls") {
    entries = entries.filter(
      (r) => (!r.desc || !r.desc.trim()) && !r.id.startsWith("V"),
    );
  }

  if (term) {
    entries = entries.filter(
      (r) =>
        r.id.toLowerCase().includes(term) ||
        (r.desc || "").toLowerCase().includes(term) ||
        (r.existing || "").toLowerCase().includes(term),
    );
    // Also search custom fixtures when not in a special category
    if (!category) {
      const customMatches = customFixtures.filter(
        (r) =>
          r.id.toLowerCase().includes(term) ||
          (r.desc || "").toLowerCase().includes(term),
      );
      customMatches.forEach((r) => {
        if (!entries.find((e) => e.id === r.id)) entries.push(r);
      });
    }
  }

  if (entries.length === 0) {
    document.getElementById("matchTableWrap").innerHTML =
      '<p style="color:#8a9ab0;font-size:12px;text-align:center;padding:16px">No matches found.</p>';
    return;
  }

  const rows = entries
    .slice(0, 300)
    .map((r) => {
      const eid = r.id.replace(/'/g, "\\'");
      return `<tr style="border-bottom:1px solid #e4eaf2;cursor:pointer" onclick="selectFromLookup('${eid}')">
      <td style="padding:8px 6px;font-weight:700;color:#2563a8;white-space:nowrap;font-size:12px">${
        r.id
      }</td>
      <td style="padding:8px 6px;color:#2a3a50;font-size:11px;word-break:break-word">${
        r.desc || "—"
      }</td>
      <td style="padding:8px 6px;color:#b06010;white-space:nowrap;font-size:11px;text-align:center">${
        r.lampQty != null ? r.lampQty : "—"
      }</td>
      <td style="padding:8px 6px;color:#2a6a30;font-size:10px;word-break:break-word">${
        r.existing || "—"
      }</td>
    </tr>`;
    })
    .join("");

  const note =
    entries.length > 300
      ? `<p style="color:#b06010;font-size:10px;text-align:center;margin-top:6px">Showing first 300 of ${entries.length} — refine search or pick a category</p>`
      : `<p style="color:#7a8fa8;font-size:10px;text-align:center;margin-top:6px">${
          entries.length
        } result${entries.length !== 1 ? "s" : ""} — tap a row to select</p>`;

  document.getElementById("matchTableWrap").innerHTML =
    `
    <table style="width:100%;border-collapse:collapse">
      <thead>
        <tr>
          <th style="padding:6px;color:#5a7a9a;font-size:10px;text-align:left;font-weight:700;background:#f4f7fb;white-space:nowrap;position:sticky;top:0">MATCH ID</th>
          <th style="padding:6px;color:#5a7a9a;font-size:10px;text-align:left;font-weight:700;background:#f4f7fb;position:sticky;top:0">DESCRIPTION</th>
          <th style="padding:6px;color:#5a7a9a;font-size:10px;text-align:center;font-weight:700;background:#f4f7fb;white-space:nowrap;position:sticky;top:0">LAMPS</th>
          <th style="padding:6px;color:#5a7a9a;font-size:10px;text-align:left;font-weight:700;background:#f4f7fb;position:sticky;top:0">EXISTING FIXTURE</th>
        </tr>
      </thead>
      <tbody>${rows}</tbody>
    </table>` + note;
}

function selectFromLookup(code) {
  if (!FIXTURES[code]) {
    const match = MATCH_DATA.find((r) => r.id === code);
    if (match) {
      FIXTURES[code] = {
        desc: match.existing || match.desc || "",
        watts: null,
        type: "Interior",
      };
    }
  }
  state.activeFixture = code;
  closeMatchIDs();

  // Show pending fixture button + inline suffix buttons
  const grid = document.getElementById("fixtureBtns");
  grid.innerHTML = "";
  grid.style.alignItems = "center";

  // Fixture button
  const btn = document.createElement("button");
  btn.id = "pendingFixtureBtn";
  btn.className = "btn btn-fix active";
  btn.dataset.v = code;
  const match = MATCH_DATA.find((r) => r.id === code);
  const desc = match?.existing || match?.desc || "";
  btn.innerHTML = `<span style="font-size:13px;font-weight:700">${code}</span>${
    desc
      ? `<span style="display:block;font-size:9px;font-weight:400;color:#5a8ab0;margin-top:2px;max-width:80px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">${desc}</span>`
      : ""
  }`;
  btn.title = desc;
  btn.addEventListener("click", () => {
    state.activeFixture = "";
    rebuildFreqGrid();
  });
  grid.appendChild(btn);

  // Vertical divider between fixture and suffix buttons
  const divider = document.createElement("div");
  divider.style.cssText =
    "width:1.5px;background:#c8d4e3;align-self:stretch;margin:0 2px;flex-shrink:0";
  grid.appendChild(divider);

  // Inline suffix buttons — default 'none' active
  [
    { sfx: "", label: "none" },
    { sfx: "-hall", label: "-hall" },
    { sfx: "-dim", label: "-dim" },
    { sfx: "-stair", label: "-stair" },
    { sfx: "-H", label: "-H" },
  ].forEach(({ sfx, label }) => {
    const sb = document.createElement("button");
    sb.className = "sfx-btn" + (sfx === "" ? " active" : "");
    sb.dataset.sfx = sfx;
    sb.textContent = label;
    sb.onclick = () => setSuffix(sb);
    grid.appendChild(sb);
  });

  toast(`"${code}" selected — set qty and tap Add`);
}

// ── VOICE MEMO ────────────────────────────────────────────────────────────
let recognition = null;
let isRecording = false;
let voiceInterim = "";

function initSpeechRecognition() {
  const SR = window.SpeechRecognition || window.webkitSpeechRecognition;
  if (!SR) return null;
  const r = new SR();
  r.continuous = true;
  r.interimResults = true;
  r.lang = "en-US";

  r.onresult = (e) => {
    let interim = "";
    let finalText = "";
    for (let i = 0; i < e.results.length; i++) {
      const t = e.results[i][0].transcript;
      if (e.results[i].isFinal) finalText += t;
      else interim += t;
    }
    const ta = document.getElementById("notesInput");
    const base =
      ta.dataset.voiceBase !== undefined ? ta.dataset.voiceBase : ta.value;
    const sep = base && !base.endsWith(" ") ? " " : "";
    ta.value = base + sep + (finalText || interim);
    if (finalText) {
      ta.dataset.voiceBase = ta.value.trimEnd();
    }
    setVoiceStatus("Listening… " + (interim ? '"' + interim + '"' : ""));
  };

  r.onerror = (e) => {
    if (e.error === "not-allowed") {
      setVoiceStatus("Microphone access denied — check browser settings");
    } else if (e.error !== "aborted") {
      setVoiceStatus("Error: " + e.error);
    }
    stopVoice();
  };

  r.onend = () => {
    if (isRecording) r.start(); // auto-restart while button held
  };

  return r;
}

function toggleVoice() {
  if (isRecording) {
    stopVoice();
    return;
  }

  const SR = window.SpeechRecognition || window.webkitSpeechRecognition;
  if (!SR) {
    setVoiceStatus(
      "Speech recognition not supported in this browser. Try Safari on iPad.",
    );
    document.getElementById("voiceStatus").style.display = "block";
    return;
  }

  recognition = initSpeechRecognition();
  const ta = document.getElementById("notesInput");
  ta.dataset.voiceBase = ta.value;

  recognition.start();
  isRecording = true;

  const btn = document.getElementById("voiceBtn");
  btn.style.background = "#fee2e2";
  btn.style.color = "#c43030";
  btn.style.borderColor = "#f0b0b0";
  document.getElementById("voiceIcon").textContent = "⏹";
  document.getElementById("voiceLabel").textContent = "Tap to stop";
  setVoiceStatus("Listening…");
  document.getElementById("voiceStatus").style.display = "block";
}

function stopVoice() {
  isRecording = false;
  if (recognition) {
    try {
      recognition.stop();
    } catch (e) {}
    recognition = null;
  }

  const btn = document.getElementById("voiceBtn");
  btn.style.background = "#e8f0fb";
  btn.style.color = "#2563a8";
  btn.style.borderColor = "#c0d4f0";
  document.getElementById("voiceIcon").textContent = "🎙";
  document.getElementById("voiceLabel").textContent = "Hold to record";

  const ta = document.getElementById("notesInput");
  delete ta.dataset.voiceBase;
  const final = ta.value.trim();
  if (final) {
    setVoiceStatus("Transcribed ✔");
    setTimeout(() => {
      document.getElementById("voiceStatus").style.display = "none";
    }, 2500);
  } else {
    document.getElementById("voiceStatus").style.display = "none";
  }
}

function setVoiceStatus(msg) {
  const el = document.getElementById("voiceStatus");
  el.textContent = msg;
  el.style.display = "block";
}

// ── FLOOR PLAN ────────────────────────────────────────────────────────────
const floorPlans = {}; // { id: { name, imageData, pins:[{id,xPct,yPct,roomName,pinNum}] } }
let activePlanId = null;
let fpMode = "pin"; // 'pin' | 'pan'
let fpScale = 1;
let fpOffset = { x: 0, y: 0 };
let fpDragging = false;
let fpDragStart = { x: 0, y: 0 };
let fpSelectedPin = null;
let fpPinCounter = 1; // global incrementing pin number across all plans
let fpImg = null; // loaded Image object for current plan

function openFloorPlan() {
  const ov = document.getElementById("floorPlanOverlay");
  ov.style.display = "flex";
  // Strict floor binding: the plan shown must be the one linked to the active
  // floor. If no plan is linked yet, show the empty state — don't fall back to
  // some other floor's plan (caused floor 1's map to appear on floor 2).
  if (activeFloor) {
    const matchId = Object.keys(floorPlans).find(
      (id) => floorPlans[id]?.floorId === activeFloor.id,
    );
    activePlanId = matchId || null;
  } else if (!activePlanId) {
    // No active floor at all (e.g. floor-less project) — pick first plan as a courtesy
    activePlanId = Object.keys(floorPlans)[0] || null;
  }
  renderFloorPlanSelect();
  if (activePlanId) {
    renderPlan();
    document.getElementById("fpEmpty").style.display = "none";
  } else {
    clearFloorPlanVisuals();
    document.getElementById("fpEmpty").style.display = "flex";
  }
}

// Wipe the canvas + overlay SVGs so an unlinked-floor state actually shows the
// empty placeholder instead of the previous plan's pixels bleeding through.
function clearFloorPlanVisuals() {
  const canvas = document.getElementById("fpCanvas");
  if (canvas) {
    const ctx = canvas.getContext("2d");
    if (ctx) ctx.clearRect(0, 0, canvas.width, canvas.height);
    canvas.width = 0;
    canvas.height = 0;
    canvas.style.display = "none";
  }
  const markup = document.getElementById("fpMarkup");
  if (markup) {
    const mctx = markup.getContext("2d");
    if (mctx) mctx.clearRect(0, 0, markup.width, markup.height);
    markup.width = 0;
    markup.height = 0;
  }
  [
    "fpPinsLayer",
    "fpNotesLayer",
    "fpPolygonsLayer",
    "fpCropLayer",
    "fpLightLevelsLayer",
  ].forEach((id) => {
    const layer = document.getElementById(id);
    if (layer) layer.innerHTML = "";
  });
  const list = document.getElementById("fpPinList");
  if (list) list.innerHTML = "";
}

function closeFloorPlan() {
  // Sweep any orphan unnamed pins across all loaded plans before hiding
  Object.values(floorPlans || {}).forEach((plan) => {
    if (!plan || !plan.pins) return;
    const before = plan.pins.length;
    plan.pins = plan.pins.filter((p) => p.roomName);
    if (plan.pins.length !== before) renderPinList();
  });
  document.getElementById("floorPlanOverlay").style.display = "none";
  closeCallout();
  if (typeof renderAuditMinimap === "function") renderAuditMinimap();
}

function renderFloorPlanSelect() {
  const sel = document.getElementById("floorPlanSelect");
  const ids = Object.keys(floorPlans);
  if (ids.length === 0) {
    sel.innerHTML = '<option value="">No plans loaded</option>';
    return;
  }
  sel.innerHTML = ids
    .map((id) => {
      const p = floorPlans[id];
      const linked = p.floorId ? floors.find((f) => f.id === p.floorId) : null;
      const suffix = linked ? ` — ${linked.name}` : "";
      return `<option value="${id}" ${id === activePlanId ? "selected" : ""}>${
        p.name
      }${suffix}</option>`;
    })
    .join("");
  renderFpFloorBindSelect();
}

// Populates the "Floor:" select in the floor plan header — shows the floor the
// active plan is currently bound to and lets the user re-link without re-uploading.
function renderFpFloorBindSelect() {
  const bind = document.getElementById("fpFloorBindSelect");
  if (!bind) return;
  if (!activePlanId || !floorPlans[activePlanId]) {
    bind.innerHTML = '<option value="">(no plan loaded)</option>';
    bind.disabled = true;
    return;
  }
  bind.disabled = false;
  const currentFloorId = floorPlans[activePlanId].floorId || "";
  const floorOpts = (floors || [])
    .map(
      (f) =>
        `<option value="${f.id}" ${f.id === currentFloorId ? "selected" : ""}>${
          f.name
        }</option>`,
    )
    .join("");
  bind.innerHTML = `<option value="" ${
    !currentFloorId ? "selected" : ""
  }>— unlinked —</option>${floorOpts}`;
}

function setActivePlanFloor(floorId) {
  if (!activePlanId || !floorPlans[activePlanId]) return;
  floorPlans[activePlanId].floorId = floorId || null;
  if (typeof saveToStorage === "function") saveToStorage();
  renderFloorPlanSelect(); // refresh the suffix on the plan-select dropdown
  const fl = floorId ? (floors || []).find((f) => f.id === floorId) : null;
  toast(fl ? `📐 Plan linked to ${fl.name}` : "📐 Plan unlinked from floor");
}

function switchFloorPlan(id) {
  if (!id || !floorPlans[id]) return;
  activePlanId = id;
  fpScale = 1;
  fpOffset = { x: 0, y: 0 };
  closeCallout();
  renderPlan();
  renderFpFloorBindSelect();
}

function deleteCurrFloorPlan() {
  if (!activePlanId) return;
  if (!confirm(`Remove "${floorPlans[activePlanId].name}"?`)) return;
  delete floorPlans[activePlanId];
  activePlanId = Object.keys(floorPlans)[0] || null;
  renderFloorPlanSelect();
  if (activePlanId) renderPlan();
  else {
    document.getElementById("fpEmpty").style.display = "flex";
    document.getElementById("fpCanvas").style.display = "none";
    document.getElementById("fpPinsLayer").innerHTML = "";
    renderPinList();
  }
}

async function uploadFloorPlan(input) {
  const file = input.files[0];
  if (!file) return;
  input.value = "";
  const id = "plan_" + Date.now();
  const name = file.name.replace(/\.[^.]+$/, "");
  toast("Loading floor plan…");

  try {
    if (file.type === "application/pdf") {
      // Render first page via PDF.js
      const arrayBuffer = await file.arrayBuffer();
      if (typeof pdfjsLib === "undefined")
        throw new Error("PDF.js not loaded yet — try again in a moment");
      pdfjsLib.GlobalWorkerOptions.workerSrc =
        "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js";
      // Suppress benign font/TT warnings
      if (pdfjsLib.OPS) pdfjsLib.verbosity = 0;
      const pdf = await pdfjsLib.getDocument({
        data: arrayBuffer,
        verbosity: 0,
      }).promise;
      const page = await pdf.getPage(1);
      const viewport = page.getViewport({ scale: 2 });
      const offCanvas = document.createElement("canvas");
      offCanvas.width = viewport.width;
      offCanvas.height = viewport.height;
      await page.render({ canvasContext: offCanvas.getContext("2d"), viewport })
        .promise;
      floorPlans[id] = {
        name,
        imageData: offCanvas.toDataURL(),
        pins: [],
        pdfPageCount: pdf.numPages,
        floorId: activeFloor?.id || null,
      };
    } else {
      // Image file
      const dataUrl = await new Promise((res, rej) => {
        const reader = new FileReader();
        reader.onload = (e) => res(e.target.result);
        reader.onerror = rej;
        reader.readAsDataURL(file);
      });
      floorPlans[id] = {
        name,
        imageData: dataUrl,
        pins: [],
        floorId: activeFloor?.id || null,
      };
    }

    activePlanId = id;
    renderFloorPlanSelect();
    fpScale = 1;
    fpOffset = { x: 0, y: 0 };
    renderPlan();
    toast(`✔ "${name}" loaded`);
  } catch (err) {
    toast("Failed to load: " + err.message);
  }
}

function renderPlan() {
  const plan = floorPlans[activePlanId];
  if (!plan) return;
  document.getElementById("fpEmpty").style.display = "none";
  document.getElementById("fpCanvas").style.display = "block";

  fpImg = new Image();
  fpImg.onload = () => {
    const canvas = document.getElementById("fpCanvas");
    canvas.width = fpImg.width;
    canvas.height = fpImg.height;
    canvas.getContext("2d").drawImage(fpImg, 0, 0);
    // Sync markup canvas to same dimensions
    const mc = document.getElementById("fpMarkup");
    if (mc) {
      mc.width = fpImg.width;
      mc.height = fpImg.height;
    }
    redrawMarkup();
    fpFit();
    renderPins();
    renderNoteMarkers();
    renderPolygons();
    if (typeof renderLightLevels === "function") renderLightLevels();
    renderPinList();
  };
  fpImg.src = plan.imageData;
}

function applyTransform() {
  document.getElementById(
    "fpCanvasInner",
  ).style.transform = `translate(${fpOffset.x}px,${fpOffset.y}px) scale(${fpScale})`;
  // Crop handles + stroke widths are stored in plan coords scaled by 1/fpScale
  // to look constant on screen — re-render when zoom changes so they stay sized
  // correctly during pinch.
  if (_cropEdit && typeof renderCropOverlay === "function") renderCropOverlay();
}

function fpFit() {
  const wrap = document.getElementById("fpCanvasWrap");
  const canvas = document.getElementById("fpCanvas");
  if (!canvas.width) return;
  const scaleX = wrap.clientWidth / canvas.width;
  const scaleY = wrap.clientHeight / canvas.height;
  fpScale = Math.min(scaleX, scaleY) * 0.95;
  fpOffset.x = (wrap.clientWidth - canvas.width * fpScale) / 2;
  fpOffset.y = (wrap.clientHeight - canvas.height * fpScale) / 2;
  applyTransform();
}

function fpZoom(delta) {
  const wrap = document.getElementById("fpCanvasWrap");
  const cx = wrap.clientWidth / 2,
    cy = wrap.clientHeight / 2;
  const newScale = Math.max(0.2, Math.min(8, fpScale + delta));
  fpOffset.x = cx - (cx - fpOffset.x) * (newScale / fpScale);
  fpOffset.y = cy - (cy - fpOffset.y) * (newScale / fpScale);
  fpScale = newScale;
  applyTransform();
}

function setMode(m) {
  fpMode = m;
  const wrap = document.getElementById("fpCanvasWrap");
  const mkCanvas = document.getElementById("fpMarkup");
  const toolbar = document.getElementById("markupToolbar");

  // Cursor
  wrap.style.cursor =
    m === "markup"
      ? "crosshair"
      : m === "polygon"
      ? "crosshair"
      : m === "note"
      ? "cell"
      : m === "pin"
      ? "crosshair"
      : m === "lightlevel"
      ? "crosshair"
      : "grab";

  // Mode buttons — pin / note / polygon / markup / crop / lightlevel
  [
    ["pin", "modePinBtn"],
    ["note", "modeNoteBtn"],
    ["polygon", "modePolyBtn"],
    ["markup", "modeMarkupBtn"],
    ["crop", "modeCropBtn"],
    ["lightlevel", "modeLightBtn"],
  ].forEach(([mode, id]) => {
    const btn = document.getElementById(id);
    if (btn) {
      btn.style.background = m === mode ? "#2563a8" : "transparent";
      btn.style.color = m === mode ? "#fff" : "#7aabcf";
    }
  });

  // Hints
  const hints = {
    pin: "Hold to drop a pin · Pinch to zoom · Drag to pan",
    note: "Tap to drop a note · Tap a marker to edit · Pinch/drag to navigate",
    polygon:
      "Tap to add vertex · Tap the first vertex (blue dot) to close · Tap a polygon to edit",
    markup: "Draw with Pencil · Switch tool to Pan to navigate",
    crop: "Drag the rectangle or its corners to bound the building · ✓ Apply to save",
    lightlevel:
      "Tap to drop a footcandle reading · Tap an existing marker to edit",
  };
  document.getElementById("fpModeHint").textContent = hints[m] || "";

  // Close the light-level callout when leaving Light Level mode, and re-render
  // markers so their pointer-events follow the new mode.
  if (m !== "lightlevel" && typeof closeLightCallout === "function")
    closeLightCallout();
  if (typeof renderLightLevels === "function") renderLightLevels();

  // Crop mode: enter/exit edit UI
  if (m === "crop") enterCropMode();
  else if (_cropEdit) exitCropMode(false); // switching away cancels unsaved crop changes

  // Cancel any in-progress polygon draft / shape edit when leaving polygon mode
  if (m !== "polygon") {
    if (typeof cancelPolyDraft === "function") cancelPolyDraft();
    if (_polyEditingShape) {
      _polyEditingShape = null;
      const doneBtn = document.getElementById("polyShapeEditDoneBtn");
      if (doneBtn) doneBtn.style.display = "none";
      if (typeof renderPolygons === "function") renderPolygons();
    }
  }

  // Markup toolbar visibility — only in markup mode
  toolbar.style.display = m === "markup" ? "flex" : "none";
  if (mkCanvas) mkCanvas.style.pointerEvents = m === "markup" ? "all" : "none";

  // Touch-action: drawing tools in markup mode lock down gestures for clean
  // pencil input; everything else (pin, note, markup-pan) allows native
  // pan/pinch.
  if (m === "markup" && mkTool !== "pan") wrap.style.touchAction = "none";
  else wrap.style.touchAction = "auto";

  closeCallout();
}

// ── MARKUP SYSTEM ────────────────────────────────────────────────────────────
let mkTool = "pen"; // 'pen' | 'highlight' | 'eraser'
let mkColor = "#e74c3c";
let mkDrawing = false;
let mkCurrentStroke = null; // {color, width, opacity, points:[{x,y}]} — in canvas px coords

const MK_TOOLS = {
  pen: { width: 2.5, opacity: 1.0 },
  highlight: { width: 14, opacity: 0.35 },
  eraser: { width: 20, opacity: 1.0 },
};

function setMarkupTool(tool) {
  mkTool = tool;
  ["mkToolPen", "mkToolHL", "mkToolErase", "mkToolPan"].forEach((id) => {
    const b = document.getElementById(id);
    if (b) {
      b.style.borderColor = "#3a5a7a";
      b.style.background = "#0d2035";
    }
  });
  const idMap = {
    pen: "mkToolPen",
    highlight: "mkToolHL",
    eraser: "mkToolErase",
    pan: "mkToolPan",
  };
  const active = document.getElementById(idMap[tool]);
  if (active) {
    active.style.borderColor = "#fff";
    active.style.background = "#1e3d5c";
  }
  // Pan tool: temporarily allow native pan + pinch-zoom, drawing pauses.
  // Drawing tools: lock down gestures so pencil draws cleanly with no delay.
  const wrap = document.getElementById("fpCanvasWrap");
  if (wrap && fpMode === "markup") {
    wrap.style.touchAction = tool === "pan" ? "auto" : "none";
    wrap.style.cursor = tool === "pan" ? "grab" : "crosshair";
  }
}

// ── MARKER SIZE (pins + notes, per-plan setting) ──────────────────────────
// Default 1.25 (25% larger than the original baseline). Tunable via the
// Pin Size +/- buttons in the floor plan toolbar; saved with the plan.
function fpGetMarkerScale() {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  const s = plan?.markerScale;
  return typeof s === "number" && s > 0 ? s : 1.25;
}
function fpMarkerScale(delta) {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan) return;
  const cur = fpGetMarkerScale();
  const next = Math.max(0.5, Math.min(3.0, +(cur + delta).toFixed(2)));
  plan.markerScale = next;
  if (typeof renderPins === "function") renderPins();
  if (typeof renderNoteMarkers === "function") renderNoteMarkers();
  scheduleSave();
  toast(`Marker size: ${Math.round(next * 100)}%`);
}

// ── NOTES ON FLOOR PLAN ─────────────────────────────────────────────────────
const NOTE_SYMBOLS = [
  { key: "warn", glyph: "⚠", label: "Warning", color: "#d97706" },
  { key: "urgent", glyph: "!", label: "Urgent", color: "#dc2626" },
  { key: "note", glyph: "📝", label: "Note", color: "#2563a8" },
  { key: "photo", glyph: "📷", label: "Photo", color: "#7a3abd" },
  { key: "good", glyph: "✓", label: "OK", color: "#16a34a" },
  { key: "q", glyph: "?", label: "Question", color: "#64748b" },
];

let _noteEditId = null; // id of note currently in the editor (null = brand new)
let _notePendingPhoto = null; // base64 data url for photo selected but not yet saved
let _noteEditSymbol = "warn";

function noteSymbolFor(key) {
  return NOTE_SYMBOLS.find((s) => s.key === key) || NOTE_SYMBOLS[0];
}

function dropNote(xPct, yPct) {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan) return;
  if (!plan.markup) plan.markup = { strokes: [] };
  if (!plan.markup.notes) plan.markup.notes = [];
  const note = {
    id: "note_" + Date.now() + "_" + Math.random().toString(36).slice(2, 7),
    xPct,
    yPct,
    symbol: "warn",
    text: "",
    photo: null,
    createdAt: new Date().toISOString(),
  };
  plan.markup.notes.push(note);
  renderNoteMarkers();
  openNoteEdit(note.id);
}

function openNoteEdit(noteId) {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan?.markup?.notes) return;
  const note = plan.markup.notes.find((n) => n.id === noteId);
  if (!note) return;
  _noteEditId = noteId;
  _noteEditSymbol = note.symbol || "warn";
  _notePendingPhoto = note.photo || null;
  renderNoteSymbolPicker();
  // Text + photo + delete-button visibility
  document.getElementById("noteEditText").value = note.text || "";
  const preview = document.getElementById("notePhotoPreview");
  const img = document.getElementById("notePhotoImg");
  if (note.photo) {
    img.src = note.photo;
    preview.style.display = "block";
  } else {
    img.src = "";
    preview.style.display = "none";
  }
  document.getElementById("noteDeleteBtn").style.display =
    note.text || note.photo ? "inline-block" : "none";
  document.getElementById("fpNoteEditModal").style.display = "flex";
  setTimeout(() => document.getElementById("noteEditText")?.focus(), 80);
}

function renderNoteSymbolPicker() {
  const picker = document.getElementById("noteSymbolPicker");
  if (!picker) return;
  picker.innerHTML = NOTE_SYMBOLS.map((s) => {
    const sel = s.key === _noteEditSymbol;
    return `<button onclick="pickNoteSymbol('${
      s.key
    }')" style="display:flex;flex-direction:column;align-items:center;gap:2px;padding:8px 10px;border-radius:8px;border:2px solid ${
      sel ? s.color : "#c8d4e3"
    };background:${sel ? s.color : "#fff"};color:${
      sel ? "#fff" : s.color
    };cursor:pointer;font-family:inherit;min-width:54px">
      <span style="font-size:18px;line-height:1">${s.glyph}</span>
      <span style="font-size:9px;font-weight:700;letter-spacing:.04em">${
        s.label
      }</span>
    </button>`;
  }).join("");
}

function closeNoteEdit() {
  document.getElementById("fpNoteEditModal").style.display = "none";
  // If user closed without saving and the note is brand-new + empty, sweep it
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (plan?.markup?.notes && _noteEditId) {
    const n = plan.markup.notes.find((x) => x.id === _noteEditId);
    if (n && !n.text && !n.photo) {
      plan.markup.notes = plan.markup.notes.filter((x) => x.id !== _noteEditId);
      renderNoteMarkers();
    }
  }
  _noteEditId = null;
  _notePendingPhoto = null;
}

function pickNoteSymbol(key) {
  _noteEditSymbol = key;
  renderNoteSymbolPicker();
}

async function handleNotePhoto(input) {
  const file = input.files?.[0];
  if (!file) return;
  input.value = "";
  if (file.type && file.type.startsWith("video/")) {
    toast("Video capture coming soon — pick a photo for now");
    return;
  }
  if (!file.type || !file.type.startsWith("image/")) {
    toast("Unsupported file type");
    return;
  }
  try {
    const compressed = await compressImageDataUrl(file);
    _notePendingPhoto = compressed;
    document.getElementById("notePhotoImg").src = compressed;
    document.getElementById("notePhotoPreview").style.display = "block";
  } catch (_) {
    toast("Could not load photo");
  }
}

function clearNotePhoto() {
  _notePendingPhoto = null;
  document.getElementById("notePhotoImg").src = "";
  document.getElementById("notePhotoPreview").style.display = "none";
}

function saveNoteEdit() {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan?.markup?.notes || !_noteEditId) {
    closeNoteEdit();
    return;
  }
  const note = plan.markup.notes.find((n) => n.id === _noteEditId);
  if (!note) {
    closeNoteEdit();
    return;
  }
  note.symbol = _noteEditSymbol;
  note.text = document.getElementById("noteEditText").value.trim();
  note.photo = _notePendingPhoto;
  if (!note.text && !note.photo) {
    // Empty save = treat as cancel
    closeNoteEdit();
    return;
  }
  document.getElementById("fpNoteEditModal").style.display = "none";
  _noteEditId = null;
  _notePendingPhoto = null;
  renderNoteMarkers();
  scheduleSave();
  toast("✔ Note saved");
}

function deleteCurrentNote() {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan?.markup?.notes || !_noteEditId) {
    closeNoteEdit();
    return;
  }
  if (!confirm("Delete this note?")) return;
  plan.markup.notes = plan.markup.notes.filter((n) => n.id !== _noteEditId);
  document.getElementById("fpNoteEditModal").style.display = "none";
  _noteEditId = null;
  _notePendingPhoto = null;
  renderNoteMarkers();
  scheduleSave();
  toast("🗑 Note removed");
}

// ── POLYGON OUTLINES (room boundaries linked to pins) ─────────────────────
const SVG_NS = "http://www.w3.org/2000/svg";
let _polyDraft = null; // { id, points: [{xPct,yPct}, ...] } during drawing
let _polyEditId = null; // polygon currently in the edit modal
let _polyEditingShape = null; // polygon id whose vertices are being dragged
let _polySnapEnabled = true; // snap-to-PDF-line toggle (default ON)
const POLY_SNAP_RADIUS = 18; // canvas-pixel radius for dark-pixel search

// Find the nearest dark pixel in the rendered PDF canvas around (cx, cy).
// Returns { x, y } in canvas-pixel coords if a dark pixel is within radius,
// or null. Used to snap polygon vertices onto wall lines.
function findSnapPoint(cx, cy) {
  if (!_polySnapEnabled) return null;
  const canvas = document.getElementById("fpCanvas");
  if (!canvas?.width) return null;
  const radius = POLY_SNAP_RADIUS;
  const x0 = Math.max(0, Math.floor(cx - radius));
  const y0 = Math.max(0, Math.floor(cy - radius));
  const x1 = Math.min(canvas.width, Math.floor(cx + radius));
  const y1 = Math.min(canvas.height, Math.floor(cy + radius));
  const w = x1 - x0,
    h = y1 - y0;
  if (w <= 0 || h <= 0) return null;
  let imageData;
  try {
    imageData = canvas.getContext("2d").getImageData(x0, y0, w, h);
  } catch (_) {
    return null;
  }
  const data = imageData.data;
  const cxLocal = cx - x0,
    cyLocal = cy - y0;
  const r2 = radius * radius;
  let bestDist = r2;
  let bestX = -1,
    bestY = -1;
  // Walk every pixel in the box; pick the darkest one within the circle.
  for (let py = 0; py < h; py++) {
    for (let px = 0; px < w; px++) {
      const dx = px - cxLocal,
        dy = py - cyLocal;
      const d = dx * dx + dy * dy;
      if (d >= bestDist) continue;
      const i = (py * w + px) * 4;
      const r = data[i],
        g = data[i + 1],
        b = data[i + 2];
      // brightness: simple average. Threshold 90 catches typical wall lines.
      if ((r + g + b) / 3 < 90) {
        bestDist = d;
        bestX = x0 + px;
        bestY = y0 + py;
      }
    }
  }
  return bestX === -1 ? null : { x: bestX, y: bestY };
}

function togglePolySnap() {
  _polySnapEnabled = !_polySnapEnabled;
  const btn = document.getElementById("polySnapBtn");
  if (btn) {
    btn.style.background = _polySnapEnabled ? "#2563a8" : "transparent";
    btn.style.color = _polySnapEnabled ? "#fff" : "#7aabcf";
    btn.title = _polySnapEnabled
      ? "Snap to PDF lines: ON (tap to disable)"
      : "Snap to PDF lines: OFF (tap to enable)";
  }
  toast(_polySnapEnabled ? "🧲 Snap to PDF lines ON" : "Snap OFF");
}

function _activePlanPolygons() {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan) return null;
  if (!plan.markup) plan.markup = { strokes: [] };
  if (!plan.markup.polygons) plan.markup.polygons = [];
  return plan.markup.polygons;
}

// Standard ray-casting point-in-polygon test (works in normalized 0-1 coords).
function _pointInPolygon(x, y, points) {
  if (!points || points.length < 3) return false;
  let inside = false;
  for (let i = 0, j = points.length - 1; i < points.length; j = i++) {
    const xi = points[i].xPct,
      yi = points[i].yPct;
    const xj = points[j].xPct,
      yj = points[j].yPct;
    const intersect =
      yi > y !== yj > y && x < ((xj - xi) * (y - yi)) / (yj - yi) + xi;
    if (intersect) inside = !inside;
  }
  return inside;
}

// Returns the topmost polygon under the given canvas-pixel point, or null.
function _polyHitTest(canvasX, canvasY) {
  const canvas = document.getElementById("fpCanvas");
  const polygons =
    (activePlanId && floorPlans[activePlanId]?.markup?.polygons) || [];
  if (!polygons.length || !canvas?.width) return null;
  const xPct = canvasX / canvas.width;
  const yPct = canvasY / canvas.height;
  // Iterate in reverse so the most recently added (drawn last → on top) wins
  for (let i = polygons.length - 1; i >= 0; i--) {
    if (_pointInPolygon(xPct, yPct, polygons[i].points)) return polygons[i];
  }
  return null;
}

// Maps a polygon to a completeness state class. Mirrors the spot-check
// criteria so the polygon fill is a live progress indicator: grey → red →
// yellow → green as the auditor fills in pin → fixtures → switches+controls.
// Note: numSwitches and existingCtrls on a logEntry are comma-joined strings
// (e.g. "Toggle X2, Wireless Switch"), not numbers — check for non-empty.
function _polyColorClass(p) {
  if (!p.pinId) return "unlinked";
  const plan = floorPlans[activePlanId];
  const pin = plan?.pins?.find((x) => x.id === p.pinId);
  if (!pin || !pin.roomName) return "unlinked";
  const entries = logEntries.filter((e) => e.space === pin.roomName);
  if (!entries.length) return "empty";
  const first = entries[0];
  const hasSwitches = !!(first.numSwitches && String(first.numSwitches).trim());
  const hasCtrls = !!(
    first.existingCtrls && String(first.existingCtrls).trim()
  );
  return hasSwitches && hasCtrls ? "complete" : "partial";
}

// Shoelace area in normalised (0..1) plan coords. Returns a fraction of the
// canvas area (so 1.0 means the polygon covers the whole plan).
function _polygonAreaPct(poly) {
  const pts = poly?.points;
  if (!pts || pts.length < 3) return 0;
  let a = 0;
  for (let i = 0; i < pts.length; i++) {
    const j = (i + 1) % pts.length;
    a += pts[i].xPct * pts[j].yPct - pts[j].xPct * pts[i].yPct;
  }
  return Math.abs(a) * 0.5;
}

// Map a pin's containing-polygon area to a size multiplier. Small rooms get
// proportionally smaller pins so adjacent ones don't crowd each other.
// >= 2.5% of canvas → full size, <= 0.25% → 60%, linear in between.
function _pinScaleForArea(areaPct) {
  if (!areaPct) return 1.0;
  const big = 0.025,
    small = 0.0025;
  if (areaPct >= big) return 1.0;
  if (areaPct <= small) return 0.6;
  const t = (areaPct - small) / (big - small);
  return 0.6 + 0.4 * t;
}

// Find the polygon linked to a pin (reverse of polygon.pinId), returns null
// if no link exists.
function _polygonForPin(plan, pin) {
  if (!pin || !plan?.markup?.polygons) return null;
  return plan.markup.polygons.find((p) => p.pinId === pin.id) || null;
}

// Per-pin scale factor combining global marker scale + polygon-area scale.
function fpPinSizeMultiplier(pin) {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  const poly = _polygonForPin(plan, pin);
  if (!poly) return 1.0;
  return _pinScaleForArea(_polygonAreaPct(poly));
}

// True iff a polygon has a pinId but the linked pin's centre is outside the
// polygon's bounds. Drives the red-dashed "mismatch" warning class.
function _polyHasMismatchedPin(poly) {
  if (!poly?.pinId) return false;
  const plan = floorPlans[activePlanId];
  const pin = plan?.pins?.find((x) => x.id === poly.pinId);
  if (!pin) return false;
  if (typeof _pointInPolygon !== "function") return false;
  return !_pointInPolygon(pin.xPct, pin.yPct, poly.points);
}

function renderPolygons() {
  const svg = document.getElementById("fpPolygonsLayer");
  const canvas = document.getElementById("fpCanvas");
  if (!svg || !canvas?.width) return;
  const w = canvas.width,
    h = canvas.height;
  svg.setAttribute("viewBox", `0 0 ${w} ${h}`);
  svg.setAttribute("width", w);
  svg.setAttribute("height", h);
  svg.innerHTML = "";
  // In shape-edit mode the layer must accept events so vertex circles inside it
  // can be tapped/dragged. iOS Safari doesn't honor child pointer-events overrides
  // when the parent SVG is pointer-events:none, so we flip the parent here and set
  // each polygon shape to pointer-events:none individually (manual _polyHitTest in
  // the wrap's pointerup still handles tap-to-open-edit-modal).
  svg.style.pointerEvents = _polyEditingShape ? "auto" : "none";
  // Lift the polygon SVG above the pins layer during shape-edit so the pin's
  // teardrop body (z-index:5, pointer-events:all) can't intercept hover/clicks
  // meant for a vertex circle — especially at high marker scales where the
  // pin covers most of a small room.
  svg.style.zIndex = _polyEditingShape ? "10" : "";

  const polygons =
    (activePlanId && floorPlans[activePlanId]?.markup?.polygons) || [];
  polygons.forEach((p) => {
    if (!p.points || p.points.length < 3) return;
    const pts = p.points.map((pt) => `${pt.xPct * w},${pt.yPct * h}`).join(" ");
    const el = document.createElementNS(SVG_NS, "polygon");
    let cls = "fp-poly " + _polyColorClass(p);
    if (_polyHasMismatchedPin(p)) cls += " mismatch";
    el.setAttribute("class", cls);
    el.setAttribute("points", pts);
    el.setAttribute("data-poly-id", p.id);
    el.style.pointerEvents = "none";
    svg.appendChild(el);
  });

  // Editable vertex handles when a polygon is in shape-edit mode.
  // Same pattern as draft vertices: small visible dot + invisible larger hit-target.
  if (_polyEditingShape) {
    const ep = polygons.find((p) => p.id === _polyEditingShape);
    if (ep && ep.points) {
      ep.points.forEach((pt, i) => {
        const hit = document.createElementNS(SVG_NS, "circle");
        hit.setAttribute("cx", pt.xPct * w);
        hit.setAttribute("cy", pt.yPct * h);
        hit.setAttribute("r", 14);
        hit.setAttribute("fill", "transparent");
        hit.style.pointerEvents = "all";
        hit.style.cursor = "grab";
        attachPolyVertexDrag(hit, ep, i);
        svg.appendChild(hit);

        const v = document.createElementNS(SVG_NS, "circle");
        v.setAttribute("class", "fp-poly-vertex");
        v.setAttribute("cx", pt.xPct * w);
        v.setAttribute("cy", pt.yPct * h);
        v.setAttribute("r", 3);
        v.setAttribute("opacity", "0.5");
        v.setAttribute("data-poly-vertex", String(i));
        v.style.pointerEvents = "none";
        svg.appendChild(v);
      });
    }
  }

  // Draft polygon (in-progress drawing)
  if (_polyDraft && _polyDraft.points.length) {
    const dpts = _polyDraft.points.map((pt) => `${pt.xPct * w},${pt.yPct * h}`);
    if (dpts.length >= 3) {
      const fill = document.createElementNS(SVG_NS, "polygon");
      fill.setAttribute("class", "fp-poly-area-draft");
      fill.setAttribute("points", dpts.join(" "));
      svg.appendChild(fill);
    }
    if (dpts.length >= 2) {
      const line = document.createElementNS(SVG_NS, "polyline");
      line.setAttribute("class", "fp-poly-edge-draft");
      line.setAttribute("points", dpts.join(" "));
      svg.appendChild(line);
    }
    _polyDraft.points.forEach((pt, i) => {
      // Non-first vertices: invisible larger hit-target circle BEHIND the visual,
      // so the visible dot can be small while the touch area stays finger-friendly.
      // The first vertex stays tap-only — tapping it is what closes the polygon
      // (drag handlers stopPropagation, which would break the close gesture).
      if (i !== 0) {
        const hit = document.createElementNS(SVG_NS, "circle");
        hit.setAttribute("cx", pt.xPct * w);
        hit.setAttribute("cy", pt.yPct * h);
        hit.setAttribute("r", 14);
        hit.setAttribute("fill", "transparent");
        hit.style.pointerEvents = "all";
        hit.style.cursor = "grab";
        attachPolyVertexDrag(hit, _polyDraft, i);
        svg.appendChild(hit);
      }
      const v = document.createElementNS(SVG_NS, "circle");
      v.setAttribute("class", "fp-poly-vertex" + (i === 0 ? " first" : ""));
      v.setAttribute("cx", pt.xPct * w);
      v.setAttribute("cy", pt.yPct * h);
      v.setAttribute("r", i === 0 ? 4.5 : 3);
      v.setAttribute("opacity", "0.5");
      v.style.pointerEvents = "none"; // hit target above handles input
      svg.appendChild(v);
    });
  }
}

function _polyAddVertex(xPct, yPct) {
  if (!_polyDraft) {
    _polyDraft = {
      id: "poly_" + Date.now() + "_" + Math.random().toString(36).slice(2, 6),
      points: [],
    };
  }
  // Snap to nearest dark pixel in the rendered PDF (wall lines, etc.)
  const canvas = document.getElementById("fpCanvas");
  if (canvas?.width) {
    const cx = xPct * canvas.width,
      cy = yPct * canvas.height;
    const snap = findSnapPoint(cx, cy);
    if (snap) {
      xPct = snap.x / canvas.width;
      yPct = snap.y / canvas.height;
    }
  }
  _polyDraft.points.push({ xPct, yPct });
  renderPolygons();
}

function _polyTryClose(clientX, clientY) {
  if (!_polyDraft || _polyDraft.points.length < 3) return false;
  const wrap = document.getElementById("fpCanvasWrap");
  const canvas = document.getElementById("fpCanvas");
  if (!wrap || !canvas?.width) return false;
  const rect = wrap.getBoundingClientRect();
  const first = _polyDraft.points[0];
  const fx = rect.left + first.xPct * canvas.width * fpScale + fpOffset.x;
  const fy = rect.top + first.yPct * canvas.height * fpScale + fpOffset.y;
  const dx = clientX - fx;
  const dy = clientY - fy;
  // 22px screen-distance threshold to "snap" to first vertex
  if (dx * dx + dy * dy <= 484) {
    finalizePolyDraft();
    return true;
  }
  return false;
}

function finalizePolyDraft() {
  if (!_polyDraft || _polyDraft.points.length < 3) {
    _polyDraft = null;
    renderPolygons();
    return;
  }
  const polygons = _activePlanPolygons();
  if (!polygons) {
    _polyDraft = null;
    return;
  }
  const newPoly = {
    id: _polyDraft.id,
    points: _polyDraft.points.slice(),
    pinId: null,
    createdAt: new Date().toISOString(),
  };
  polygons.push(newPoly);
  _polyDraft = null;
  renderPolygons();
  scheduleSave();
  // Auto-link to a contained pin (or auto-create one) instead of forcing the
  // user through the link-to-pin modal. Only opens the modal when ambiguous.
  _autoLinkOrCreatePin(newPoly);
}

// ── POLYGON ↔ PIN AUTO-LINKING ──────────────────────────────────────────────
// Run when a draft polygon closes. Three rules:
//   1 pin inside    → auto-link to that pin (silent, just toast)
//   0 pins inside   → drop a pin at the polygon's centroid + open the room-name
//                     callout (so polygon and its pin are born together)
//   2+ pins inside  → fall back to the existing edit modal (has pin picker)
function _autoLinkOrCreatePin(newPoly) {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan) return;
  const inside = (plan.pins || []).filter((p) =>
    _pointInPolygon(p.xPct, p.yPct, newPoly.points),
  );

  if (inside.length === 1) {
    newPoly.pinId = inside[0].id;
    renderPolygons();
    scheduleSave();
    const name = inside[0].roomName || `Pin ${inside[0].pinNum}`;
    toast(`🔗 Outline linked to "${name}"`);
    return;
  }

  if (inside.length === 0) {
    // No pin inside — create one at the centroid and immediately open the
    // room-name callout (same UI as a normal pin drop). Polygon links to it.
    const c = _polygonCentroid(newPoly.points);
    const pinNum = fpPinCounter++;
    const newPin = {
      id: "pin_" + pinNum,
      xPct: c.xPct,
      yPct: c.yPct,
      roomName: "",
      roomType: "",
      pinNum,
    };
    plan.pins.push(newPin);
    newPoly.pinId = newPin.id;
    renderPins();
    renderPinList();
    renderPolygons();
    scheduleSave();
    // Compute screen position for the callout (matches dropPin's behavior)
    const wrap = document.getElementById("fpCanvasWrap");
    const canvas = document.getElementById("fpCanvas");
    if (wrap && canvas?.width) {
      const rect = wrap.getBoundingClientRect();
      const sx = rect.left + c.xPct * canvas.width * fpScale + fpOffset.x;
      const sy = rect.top + c.yPct * canvas.height * fpScale + fpOffset.y;
      showPinSetupModal(newPin, sx, sy);
    } else {
      showPinSetupModal(newPin, 200, 200);
    }
    return;
  }

  // Ambiguous — multiple pins inside. Open the existing modal so user picks.
  openPolyEdit(newPoly.id);
}

// Ray-casting point-in-polygon. Coords are normalized (xPct, yPct), but the
// algorithm is unit-agnostic.
function _pointInPolygon(x, y, points) {
  let inside = false;
  for (let i = 0, j = points.length - 1; i < points.length; j = i++) {
    const xi = points[i].xPct,
      yi = points[i].yPct;
    const xj = points[j].xPct,
      yj = points[j].yPct;
    const intersect =
      yi > y !== yj > y && x < ((xj - xi) * (y - yi)) / (yj - yi) + xi;
    if (intersect) inside = !inside;
  }
  return inside;
}

// Average of vertex positions. Not the geometric centroid, but adequate for
// "drop a pin somewhere central" — true centroid math is overkill here.
function _polygonCentroid(points) {
  let sx = 0,
    sy = 0;
  points.forEach((p) => {
    sx += p.xPct;
    sy += p.yPct;
  });
  return { xPct: sx / points.length, yPct: sy / points.length };
}

function cancelPolyDraft() {
  if (!_polyDraft) return;
  _polyDraft = null;
  renderPolygons();
}

function openPolyEdit(polyId) {
  const polygons = _activePlanPolygons();
  if (!polygons) return;
  const p = polygons.find((x) => x.id === polyId);
  if (!p) return;
  _polyEditId = polyId;
  // Build pin dropdown from current plan's pins (only those with names are useful)
  const plan = floorPlans[activePlanId];
  const sel = document.getElementById("polyEditLink");
  const pins = plan?.pins || [];
  let html = '<option value="">— Unlinked —</option>';
  pins.forEach((pin) => {
    const label = pin.roomName
      ? `Pin ${pin.pinNum} · ${pin.roomName}`
      : `Pin ${pin.pinNum} (unnamed)`;
    const sel = pin.id === p.pinId ? " selected" : "";
    html += `<option value="${pin.id}"${sel}>${label}</option>`;
  });
  sel.innerHTML = html;
  if (p.pinId) sel.value = p.pinId;
  document.getElementById("fpPolyEditModal").style.display = "flex";
}

function closePolyEdit() {
  document.getElementById("fpPolyEditModal").style.display = "none";
  _polyEditId = null;
}

function savePolyEdit() {
  const polygons = _activePlanPolygons();
  if (!polygons || !_polyEditId) {
    closePolyEdit();
    return;
  }
  const p = polygons.find((x) => x.id === _polyEditId);
  if (!p) {
    closePolyEdit();
    return;
  }
  const newPinId = document.getElementById("polyEditLink").value || null;
  p.pinId = newPinId;
  closePolyEdit();
  renderPolygons();
  scheduleSave();
  toast(newPinId ? "✔ Outline linked" : "✔ Outline saved (unlinked)");
}

// ═════════════════════════════════════════════════════════════════════════
// LIGHT LEVEL (footcandle) MEASUREMENTS
// ═════════════════════════════════════════════════════════════════════════
// Stored on the plan as plan.markup.lightLevels[] — one record per reading.
// Linked at drop time to whichever polygon they fall inside (if any), so each
// reading carries the roomName the proposal engine should attribute it to.
// Pre-install FC vs. post-install FC comparison is the downstream goal.
let _fpLightSelected = null; // the light-level being edited

function _planLightLevels(plan) {
  if (!plan) return null;
  plan.markup = plan.markup || {};
  plan.markup.lightLevels = plan.markup.lightLevels || [];
  return plan.markup.lightLevels;
}

// Find the polygon (and its linked pin's room name) that contains a normalised point.
function _resolveLightLevelRoom(plan, xPct, yPct) {
  if (!plan || typeof _pointInPolygon !== "function")
    return { roomName: "", pinId: null, polyId: null };
  const polys = plan.markup?.polygons || [];
  const containing = polys.find(
    (p) =>
      p.points && p.points.length >= 3 && _pointInPolygon(xPct, yPct, p.points),
  );
  if (!containing) return { roomName: "", pinId: null, polyId: null };
  const pin = containing.pinId
    ? (plan.pins || []).find((x) => x.id === containing.pinId)
    : null;
  return {
    roomName: pin?.roomName || "",
    pinId: pin?.id || null,
    polyId: containing.id,
  };
}

function dropLightLevel(xPct, yPct, screenX, screenY) {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan) return;
  const lvls = _planLightLevels(plan);
  const link = _resolveLightLevelRoom(plan, xPct, yPct);
  const id =
    "ll_" +
    Date.now().toString(36) +
    "_" +
    Math.random().toString(36).slice(2, 6);
  const rec = {
    id,
    xPct,
    yPct,
    fc: 0,
    measuredAt: "Floor", // sensible default; user can change in callout
    roomName: link.roomName,
    pinId: link.pinId,
    polyId: link.polyId,
    createdAt: new Date().toISOString(),
  };
  lvls.push(rec);
  renderLightLevels();
  if (typeof scheduleSave === "function") scheduleSave();
  showLightCallout(rec, screenX, screenY);
}

function renderLightLevels() {
  const svg = document.getElementById("fpLightLevelsLayer");
  const canvas = document.getElementById("fpCanvas");
  if (!svg) return;
  svg.innerHTML = "";
  if (!canvas?.width) return;
  const w = canvas.width,
    h = canvas.height;
  svg.setAttribute("viewBox", `0 0 ${w} ${h}`);
  svg.setAttribute("width", w);
  svg.setAttribute("height", h);
  // Yellow markers — kept SMALL so they don't compete with room pins. Layer
  // accepts events when in lightlevel mode; click on a marker re-opens its
  // callout for editing.
  const isLightMode = fpMode === "lightlevel";
  svg.style.pointerEvents = isLightMode ? "auto" : "none";

  const plan = activePlanId ? floorPlans[activePlanId] : null;
  const lvls = plan?.markup?.lightLevels || [];
  const NS = "http://www.w3.org/2000/svg";
  const ms = typeof fpGetMarkerScale === "function" ? fpGetMarkerScale() : 1;
  const r0 = 9 * ms;
  const fs = 9 * ms;

  lvls.forEach((rec) => {
    const cx = rec.xPct * w,
      cy = rec.yPct * h;
    const g = document.createElementNS(NS, "g");
    g.setAttribute("data-ll-id", rec.id);
    g.style.cursor = isLightMode ? "pointer" : "default";
    g.style.pointerEvents = isLightMode ? "all" : "none";

    const c = document.createElementNS(NS, "circle");
    c.setAttribute("cx", cx);
    c.setAttribute("cy", cy);
    c.setAttribute("r", r0);
    c.setAttribute("fill", "#fbbf24");
    c.setAttribute("stroke", "#7a5a10");
    c.setAttribute("stroke-width", r0 * 0.18);
    g.appendChild(c);

    const txt = document.createElementNS(NS, "text");
    txt.setAttribute("x", cx);
    txt.setAttribute("y", cy);
    txt.setAttribute("text-anchor", "middle");
    txt.setAttribute("dominant-baseline", "central");
    txt.setAttribute("fill", "#1a1a1a");
    txt.setAttribute("font-size", fs);
    txt.setAttribute("font-weight", "800");
    txt.setAttribute(
      "font-family",
      "-apple-system,BlinkMacSystemFont,sans-serif",
    );
    txt.textContent = rec.fc || "?";
    g.appendChild(txt);

    g.addEventListener("click", (e) => {
      e.stopPropagation();
      showLightCallout(rec, e.clientX, e.clientY);
    });
    svg.appendChild(g);
  });
}

function showLightCallout(rec, screenX, screenY) {
  _fpLightSelected = rec;
  const callout = document.getElementById("fpLightCallout");
  if (!callout) return;
  // Populate fields
  const fcInput = document.getElementById("fpLightFcInput");
  if (fcInput) fcInput.value = rec.fc || "";
  const roomLine = document.getElementById("fpLightCalloutRoom");
  if (roomLine) {
    roomLine.textContent = rec.roomName
      ? `🔗 ${rec.roomName}`
      : "⚠ not inside a room outline — drop inside a polygon to link";
  }
  // Sync the measured-at button row
  document.querySelectorAll("#fpLightMeasuredAt button").forEach((b) => {
    const on = b.dataset.mat === rec.measuredAt;
    b.style.background = on ? "#c4a020" : "#fff";
    b.style.color = on ? "#fff" : "#3a5a7a";
    b.style.borderColor = on ? "#8a6a10" : "#c8d4e3";
  });

  // Position the callout near the marker (same clamping logic as fpCallout)
  const ov = document
    .getElementById("floorPlanOverlay")
    .getBoundingClientRect();
  callout.style.visibility = "hidden";
  callout.style.display = "block";
  const cw = callout.offsetWidth || 220;
  const ch = callout.offsetHeight || 180;
  let cx = (screenX || 0) - ov.left + 14;
  let cy = (screenY || 0) - ov.top - ch - 14;
  if (cx + cw > ov.width - 8) cx = (screenX || 0) - ov.left - cw - 14;
  if (cy < 8) cy = (screenY || 0) - ov.top + 18;
  cx = Math.max(8, Math.min(cx, ov.width - cw - 8));
  cy = Math.max(8, Math.min(cy, ov.height - ch - 8));
  callout.style.left = cx + "px";
  callout.style.top = cy + "px";
  callout.style.visibility = "";
  setTimeout(() => {
    if (fcInput) fcInput.focus();
  }, 60);
}

function closeLightCallout() {
  const callout = document.getElementById("fpLightCallout");
  if (callout) callout.style.display = "none";
  _fpLightSelected = null;
}

function setLightMeasuredAt(val) {
  if (!_fpLightSelected) return;
  _fpLightSelected.measuredAt = val;
  // Re-sync the row visually
  document.querySelectorAll("#fpLightMeasuredAt button").forEach((b) => {
    const on = b.dataset.mat === val;
    b.style.background = on ? "#c4a020" : "#fff";
    b.style.color = on ? "#fff" : "#3a5a7a";
    b.style.borderColor = on ? "#8a6a10" : "#c8d4e3";
  });
}

function saveLightLevel() {
  if (!_fpLightSelected) return;
  const fcInput = document.getElementById("fpLightFcInput");
  const fc = parseFloat(fcInput?.value);
  if (!fc && fc !== 0) {
    toast("Enter a footcandle value first");
    return;
  }
  _fpLightSelected.fc = fc;
  if (typeof scheduleSave === "function") scheduleSave();
  renderLightLevels();
  const room = _fpLightSelected.roomName
    ? ` in ${_fpLightSelected.roomName}`
    : "";
  toast(`💡 ${fc} FC saved (${_fpLightSelected.measuredAt})${room}`);
  closeLightCallout();
}

function deleteLightLevel() {
  if (!_fpLightSelected) return;
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan) {
    closeLightCallout();
    return;
  }
  const lvls = _planLightLevels(plan);
  const idx = lvls.findIndex((x) => x.id === _fpLightSelected.id);
  if (idx >= 0) lvls.splice(idx, 1);
  if (typeof scheduleSave === "function") scheduleSave();
  closeLightCallout();
  renderLightLevels();
  toast("Light level reading deleted");
}

function enterPolyShapeEdit() {
  if (!_polyEditId) return;
  _polyEditingShape = _polyEditId;
  closePolyEdit();
  _populatePolyShapeToolbar();
  document.getElementById("polyShapeEditToolbar").style.display = "flex";
  // Show a hint in the mode bar
  const hint = document.getElementById("fpModeHint");
  if (hint)
    hint.textContent =
      "Drag any vertex to reshape · tap outside or Done to finish";
  renderPolygons();
}

function exitPolyShapeEdit() {
  _polyEditingShape = null;
  document.getElementById("polyShapeEditToolbar").style.display = "none";
  // Restore the mode hint
  if (typeof setMode === "function") setMode(fpMode);
  renderPolygons();
  scheduleSave();
  toast("✔ Shape saved");
}

// Populate the toolbar's pin-link select with the current plan's pins, and set
// it to whatever the polygon under edit is currently linked to.
function _populatePolyShapeToolbar() {
  const sel = document.getElementById("polyShapeLinkSelect");
  if (!sel) return;
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  const polys = plan?.markup?.polygons || [];
  const p = polys.find((x) => x.id === _polyEditingShape);
  const pins = plan?.pins || [];
  let html = '<option value="">— Unlinked —</option>';
  pins.forEach((pin) => {
    const label = pin.roomName
      ? `Pin ${pin.pinNum} · ${pin.roomName}`
      : `Pin ${pin.pinNum} (unnamed)`;
    const isSel = pin.id === p?.pinId ? " selected" : "";
    html += `<option value="${pin.id}"${isSel}>${label}</option>`;
  });
  sel.innerHTML = html;
  if (p?.pinId) sel.value = p.pinId;
}

// Toolbar onchange — apply pin link immediately so the polygon's color/mismatch
// state re-evaluates without waiting for Done.
function setLinkFromToolbar(pinId) {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan || !_polyEditingShape) return;
  const polys = plan.markup?.polygons || [];
  const p = polys.find((x) => x.id === _polyEditingShape);
  if (!p) return;
  p.pinId = pinId || null;
  renderPolygons();
  if (typeof scheduleSave === "function") scheduleSave();
  if (typeof renderAuditMinimap === "function") renderAuditMinimap();
  toast(pinId ? "🔗 Outline linked" : "🔗 Outline unlinked");
}

function deletePolygonInEdit() {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan || !_polyEditingShape) return;
  if (!confirm("Delete this room outline?")) return;
  const polys = plan.markup?.polygons || [];
  const idx = polys.findIndex((x) => x.id === _polyEditingShape);
  if (idx >= 0) polys.splice(idx, 1);
  _polyEditingShape = null;
  document.getElementById("polyShapeEditToolbar").style.display = "none";
  if (typeof setMode === "function") setMode(fpMode);
  renderPolygons();
  if (typeof scheduleSave === "function") scheduleSave();
  toast("🗑 Outline removed");
}

// ── CROP MODE ────────────────────────────────────────────────────────────────
// User defines a rectangle bounding the relevant area of the floor plan.
// Stored on the plan as { xPct, yPct, wPct, hPct } (relative to natural image
// dims) and applied to the audit-page minimap rendering. Main floor plan view
// is unaffected — this is purely for keeping the minimap focused on the building
// instead of margins / title blocks.
let _cropEdit = null; // { x, y, w, h } in plan-pixel coords during editing

function enterCropMode() {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan?.imageData) {
    toast("Upload a floor plan first");
    return;
  }
  const canvas = document.getElementById("fpCanvas");
  if (!canvas?.width) {
    toast("Wait for plan to render");
    return;
  }
  const PW = canvas.width,
    PH = canvas.height;
  if (plan.crop) {
    _cropEdit = {
      x: plan.crop.xPct * PW,
      y: plan.crop.yPct * PH,
      w: plan.crop.wPct * PW,
      h: plan.crop.hPct * PH,
    };
  } else {
    // Default: 80% centered — gives the user a starting rect to refine
    _cropEdit = { x: PW * 0.1, y: PH * 0.1, w: PW * 0.8, h: PH * 0.8 };
  }
  const controls = document.getElementById("cropControls");
  if (controls) controls.style.display = "flex";
  const resetBtn = document.getElementById("cropResetBtn");
  if (resetBtn) resetBtn.style.display = plan.crop ? "" : "none";
  renderCropOverlay();
}

function exitCropMode(save) {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (save && _cropEdit && plan) {
    const canvas = document.getElementById("fpCanvas");
    const PW = canvas?.width || 1,
      PH = canvas?.height || 1;
    plan.crop = {
      xPct: _cropEdit.x / PW,
      yPct: _cropEdit.y / PH,
      wPct: _cropEdit.w / PW,
      hPct: _cropEdit.h / PH,
    };
    if (typeof scheduleSave === "function") scheduleSave();
    toast("✓ Crop saved — minimap will use this area");
  }
  _cropEdit = null;
  const controls = document.getElementById("cropControls");
  if (controls) controls.style.display = "none";
  const layer = document.getElementById("fpCropLayer");
  if (layer) layer.innerHTML = "";
  if (typeof renderAuditMinimap === "function") renderAuditMinimap();
}

function applyCrop() {
  exitCropMode(true);
  // Stay in crop mode visually (re-enter so the user can adjust again easily)
  // ... actually return to pin mode so the user is ready to keep working.
  setMode("pin");
}

function removeCrop() {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (plan?.crop) {
    delete plan.crop;
    if (typeof scheduleSave === "function") scheduleSave();
    toast("✓ Crop removed");
  }
  _cropEdit = null;
  setMode("pin");
}

function renderCropOverlay() {
  const layer = document.getElementById("fpCropLayer");
  const canvas = document.getElementById("fpCanvas");
  if (!layer || !canvas?.width) return;
  layer.setAttribute("viewBox", `0 0 ${canvas.width} ${canvas.height}`);
  layer.style.pointerEvents = _cropEdit ? "auto" : "none";
  layer.innerHTML = "";
  if (!_cropEdit) return;
  const { x, y, w, h } = _cropEdit;
  const NS = "http://www.w3.org/2000/svg";

  // Dim mask outside the crop rect — 4 dark rectangles surrounding the crop
  const PW = canvas.width,
    PH = canvas.height;
  const dimColor = "rgba(10,20,30,0.55)";
  [
    { x: 0, y: 0, w: PW, h: y }, // top
    { x: 0, y: y + h, w: PW, h: PH - (y + h) }, // bottom
    { x: 0, y: y, w: x, h: h }, // left
    { x: x + w, y: y, w: PW - (x + w), h: h }, // right
  ].forEach((r) => {
    if (r.w <= 0 || r.h <= 0) return;
    const m = document.createElementNS(NS, "rect");
    m.setAttribute("x", r.x);
    m.setAttribute("y", r.y);
    m.setAttribute("width", r.w);
    m.setAttribute("height", r.h);
    m.setAttribute("fill", dimColor);
    m.style.pointerEvents = "none";
    layer.appendChild(m);
  });

  // Crop rectangle (draggable to move)
  const rect = document.createElementNS(NS, "rect");
  rect.setAttribute("x", x);
  rect.setAttribute("y", y);
  rect.setAttribute("width", w);
  rect.setAttribute("height", h);
  rect.setAttribute("fill", "rgba(251,191,36,0.08)");
  rect.setAttribute("stroke", "#fbbf24");
  rect.setAttribute("stroke-width", 6 / fpScale);
  rect.setAttribute("stroke-dasharray", `${14 / fpScale} ${10 / fpScale}`);
  rect.style.cursor = "move";
  attachCropMove(rect);
  layer.appendChild(rect);

  // 8 handles — 4 corners + 4 edge mids
  const HR = 18 / fpScale; // visual radius in plan coords
  const handles = [
    ["nw", x, y],
    ["n", x + w / 2, y],
    ["ne", x + w, y],
    ["w", x, y + h / 2],
    ["e", x + w, y + h / 2],
    ["sw", x, y + h],
    ["s", x + w / 2, y + h],
    ["se", x + w, y + h],
  ];
  handles.forEach(([id, hx, hy]) => {
    const c = document.createElementNS(NS, "circle");
    c.setAttribute("cx", hx);
    c.setAttribute("cy", hy);
    c.setAttribute("r", HR);
    c.setAttribute("fill", "#fff");
    c.setAttribute("stroke", "#fbbf24");
    c.setAttribute("stroke-width", 3 / fpScale);
    c.style.cursor = id + "-resize";
    attachCropResize(c, id);
    layer.appendChild(c);
  });
}

// Move/resize listeners attach to `document` rather than the handle itself, because
// renderCropOverlay() rebuilds the SVG on every move — re-creating the handle would
// drop pointer capture mid-drag, which is why the original implementation only
// moved a few pixels per press.
function attachCropMove(el) {
  el.addEventListener("pointerdown", (e) => {
    e.stopPropagation();
    e.preventDefault();
    const start = { x: e.clientX, y: e.clientY };
    const orig = { ..._cropEdit };
    const onMove = (ev) => {
      const canvas = document.getElementById("fpCanvas");
      const dx = (ev.clientX - start.x) / fpScale;
      const dy = (ev.clientY - start.y) / fpScale;
      _cropEdit.x = Math.max(0, Math.min(canvas.width - orig.w, orig.x + dx));
      _cropEdit.y = Math.max(0, Math.min(canvas.height - orig.h, orig.y + dy));
      renderCropOverlay();
    };
    const onUp = () => {
      document.removeEventListener("pointermove", onMove);
      document.removeEventListener("pointerup", onUp);
      document.removeEventListener("pointercancel", onUp);
    };
    document.addEventListener("pointermove", onMove);
    document.addEventListener("pointerup", onUp);
    document.addEventListener("pointercancel", onUp);
  });
}

function attachCropResize(el, handle) {
  el.addEventListener("pointerdown", (e) => {
    e.stopPropagation();
    e.preventDefault();
    const start = { x: e.clientX, y: e.clientY };
    const orig = { ..._cropEdit };
    const onMove = (ev) => {
      const canvas = document.getElementById("fpCanvas");
      const dx = (ev.clientX - start.x) / fpScale;
      const dy = (ev.clientY - start.y) / fpScale;
      let { x, y, w, h } = orig;
      const MIN = 50;
      if (handle.includes("w")) {
        const nx = Math.max(0, Math.min(x + w - MIN, x + dx));
        w = w + (x - nx);
        x = nx;
      }
      if (handle.includes("e")) {
        w = Math.max(MIN, Math.min(canvas.width - x, w + dx));
      }
      if (handle.includes("n")) {
        const ny = Math.max(0, Math.min(y + h - MIN, y + dy));
        h = h + (y - ny);
        y = ny;
      }
      if (handle.includes("s")) {
        h = Math.max(MIN, Math.min(canvas.height - y, h + dy));
      }
      _cropEdit = { x, y, w, h };
      renderCropOverlay();
    };
    const onUp = () => {
      document.removeEventListener("pointermove", onMove);
      document.removeEventListener("pointerup", onUp);
      document.removeEventListener("pointercancel", onUp);
    };
    document.addEventListener("pointermove", onMove);
    document.addEventListener("pointerup", onUp);
    document.addEventListener("pointercancel", onUp);
  });
}

// Drag handler for an SVG vertex circle in shape-edit mode.
// Snap-to-PDF applies if enabled.
// Vertex drag listeners attach to `document` rather than the circle itself —
// renderPolygons() rebuilds the SVG on every move, so binding on the circle
// would drop pointer capture mid-drag (same fix as attachCropMove/Resize).
function attachPolyVertexDrag(circleEl, polygon, vertexIdx) {
  circleEl.addEventListener("pointerdown", (e) => {
    e.stopPropagation();
    e.preventDefault();
    circleEl.style.cursor = "grabbing";
    // Snap helps while DRAWING a polygon (tracing walls), but fights you when
    // fine-tuning a committed shape — every move yanks the vertex to the
    // nearest dark pixel in a wide radius. Skip snap for shape-edit on
    // existing polygons; keep it for the in-progress draft.
    const isDraft = typeof _polyDraft !== "undefined" && polygon === _polyDraft;
    const onMove = (ev) => {
      ev.preventDefault();
      const wrap = document.getElementById("fpCanvasWrap");
      const canvas = document.getElementById("fpCanvas");
      if (!canvas?.width) return;
      const rect = wrap.getBoundingClientRect();
      let cx = (ev.clientX - rect.left - fpOffset.x) / fpScale;
      let cy = (ev.clientY - rect.top - fpOffset.y) / fpScale;
      cx = Math.max(0, Math.min(canvas.width, cx));
      cy = Math.max(0, Math.min(canvas.height, cy));
      if (isDraft) {
        const snap = findSnapPoint(cx, cy);
        if (snap) {
          cx = snap.x;
          cy = snap.y;
        }
      }
      polygon.points[vertexIdx] = {
        xPct: cx / canvas.width,
        yPct: cy / canvas.height,
      };
      // Re-render the polygon (this rebuilds the SVG including the circle
      // element this handler was originally attached to — that's why move/up
      // live on document, not the circle).
      renderPolygons();
    };
    const onUp = () => {
      document.removeEventListener("pointermove", onMove);
      document.removeEventListener("pointerup", onUp);
      document.removeEventListener("pointercancel", onUp);
    };
    document.addEventListener("pointermove", onMove);
    document.addEventListener("pointerup", onUp);
    document.addEventListener("pointercancel", onUp);
  });
}

function deleteCurrentPolygon() {
  const polygons = _activePlanPolygons();
  if (!polygons || !_polyEditId) {
    closePolyEdit();
    return;
  }
  if (!confirm("Delete this room outline?")) return;
  const idx = polygons.findIndex((x) => x.id === _polyEditId);
  if (idx >= 0) polygons.splice(idx, 1);
  closePolyEdit();
  renderPolygons();
  scheduleSave();
  toast("🗑 Outline removed");
}

function renderNoteMarkers() {
  const layer = document.getElementById("fpNotesLayer");
  if (!layer) return;
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  const canvas = document.getElementById("fpCanvas");
  const notes = plan?.markup?.notes || [];
  layer.innerHTML = "";
  if (!notes.length || !canvas?.width) return;

  const ms = fpGetMarkerScale();
  const sz = Math.round(30 * ms);
  const fs = Math.round(14 * ms);

  notes.forEach((n) => {
    const sym = noteSymbolFor(n.symbol);
    const btn = document.createElement("button");
    btn.dataset.noteId = n.id;
    btn.textContent = sym.glyph;
    const left = n.xPct * canvas.width;
    const top = n.yPct * canvas.height;
    btn.style.cssText = `position:absolute;left:${left}px;top:${top}px;transform:translate(-50%,-100%);width:${sz}px;height:${sz}px;border-radius:50%;background:${sym.color};color:#fff;border:2px solid #fff;box-shadow:0 2px 6px rgba(0,0,0,.4);font-size:${fs}px;font-weight:700;cursor:pointer;pointer-events:auto;display:flex;align-items:center;justify-content:center;font-family:inherit;padding:0;touch-action:none;user-select:none;-webkit-user-select:none`;
    attachNoteDragHandlers(btn, n);
    layer.appendChild(btn);
  });
}

// 300ms hold → drag mode. Quick tap → open editor.
function attachNoteDragHandlers(btn, note) {
  let holdTimer = null;
  let dragging = false;
  let startX = 0,
    startY = 0;

  btn.addEventListener("pointerdown", (e) => {
    e.stopPropagation();
    startX = e.clientX;
    startY = e.clientY;
    dragging = false;
    if (holdTimer) clearTimeout(holdTimer);
    holdTimer = setTimeout(() => {
      holdTimer = null;
      dragging = true;
      try {
        btn.setPointerCapture(e.pointerId);
      } catch (_) {}
      btn.style.boxShadow = "0 6px 20px rgba(0,0,0,.55)";
      btn.style.transform = "translate(-50%,-100%) scale(1.15)";
      try {
        navigator.vibrate?.(15);
      } catch (_) {}
    }, 300);
  });

  btn.addEventListener("pointermove", (e) => {
    if (holdTimer) {
      // Pre-drag: cancel hold if user moves too much (treat as scroll/pan)
      const dx = Math.abs(e.clientX - startX);
      const dy = Math.abs(e.clientY - startY);
      if (dx > 10 || dy > 10) {
        clearTimeout(holdTimer);
        holdTimer = null;
      }
      return;
    }
    if (!dragging) return;
    e.preventDefault();
    const wrap = document.getElementById("fpCanvasWrap");
    const canvas = document.getElementById("fpCanvas");
    const rect = wrap.getBoundingClientRect();
    const xPct =
      (e.clientX - rect.left - fpOffset.x) / (canvas.width * fpScale);
    const yPct =
      (e.clientY - rect.top - fpOffset.y) / (canvas.height * fpScale);
    note.xPct = Math.max(0, Math.min(1, xPct));
    note.yPct = Math.max(0, Math.min(1, yPct));
    btn.style.left = `${note.xPct * canvas.width}px`;
    btn.style.top = `${note.yPct * canvas.height}px`;
  });

  const finish = (e) => {
    if (holdTimer) {
      clearTimeout(holdTimer);
      holdTimer = null;
    }
    if (dragging) {
      dragging = false;
      try {
        btn.releasePointerCapture(e.pointerId);
      } catch (_) {}
      btn.style.boxShadow = "0 2px 6px rgba(0,0,0,.4)";
      btn.style.transform = "translate(-50%,-100%)";
      scheduleSave();
      return;
    }
    // Quick tap → open editor (only if user didn't drag)
    const dx = Math.abs(e.clientX - startX);
    const dy = Math.abs(e.clientY - startY);
    if (dx < 10 && dy < 10) openNoteEdit(note.id);
  };
  btn.addEventListener("pointerup", finish);
  btn.addEventListener("pointercancel", finish);
}

function setMarkupColor(c) {
  mkColor = c;
  const swatch = document.getElementById("mkColorSwatch");
  if (swatch) swatch.style.background = c;
  if (mkTool !== "eraser") setMarkupTool("pen");
}

function getMarkupCanvas() {
  return document.getElementById("fpMarkup");
}

function mkCanvasCoords(clientX, clientY) {
  const wrap = document.getElementById("fpCanvasWrap");
  const rect = wrap.getBoundingClientRect();
  return {
    x: (clientX - rect.left - fpOffset.x) / fpScale,
    y: (clientY - rect.top - fpOffset.y) / fpScale,
  };
}

function markupStartStroke(cx, cy) {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan) return;
  const t = MK_TOOLS[mkTool] || MK_TOOLS.pen;
  mkCurrentStroke = {
    tool: mkTool,
    color: mkTool === "eraser" ? "#ffffff" : mkColor,
    width: t.width,
    opacity: t.opacity,
    points: [{ x: cx, y: cy }],
  };
  mkDrawing = true;
}

function markupAddPoint(cx, cy) {
  if (!mkDrawing || !mkCurrentStroke) return;
  mkCurrentStroke.points.push({ x: cx, y: cy });
  redrawMarkup();
}

function markupEndStroke() {
  if (!mkDrawing || !mkCurrentStroke) return;
  mkDrawing = false;
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (plan && mkCurrentStroke.points.length >= 1) {
    if (!plan.markup) plan.markup = { strokes: [] };
    const c = document.getElementById("fpCanvas");
    const w = c.width,
      h = c.height;
    const stored = {
      ...mkCurrentStroke,
      points: mkCurrentStroke.points.map((p) => ({ x: p.x / w, y: p.y / h })),
    };
    plan.markup.strokes.push(stored);
    scheduleSave();
  }
  mkCurrentStroke = null;
}

function redrawMarkup() {
  const mc = getMarkupCanvas();
  if (!mc || !mc.width) return;
  const ctx = mc.getContext("2d");
  ctx.clearRect(0, 0, mc.width, mc.height);
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  const w = mc.width,
    h = mc.height;
  if (plan?.markup?.strokes) {
    plan.markup.strokes.forEach((s) => drawStroke(ctx, s, w, h, true));
  }
  if (mkCurrentStroke) {
    drawStroke(ctx, mkCurrentStroke, w, h, false);
  }
}

function drawStroke(ctx, s, w, h, isFraction) {
  if (!s.points || s.points.length < 1) return;
  ctx.save();
  ctx.globalAlpha = s.opacity;
  ctx.strokeStyle = s.color;
  ctx.fillStyle = s.color;
  ctx.lineWidth = s.width;
  ctx.lineCap = "round";
  ctx.lineJoin = "round";
  if (s.tool === "eraser") ctx.globalCompositeOperation = "destination-out";

  const px = (p) => (isFraction ? p.x * w : p.x);
  const py = (p) => (isFraction ? p.y * h : p.y);

  if (s.points.length === 1) {
    // Single tap — draw a dot
    ctx.beginPath();
    ctx.arc(px(s.points[0]), py(s.points[0]), s.width / 2, 0, Math.PI * 2);
    ctx.fill();
  } else {
    ctx.beginPath();
    ctx.moveTo(px(s.points[0]), py(s.points[0]));
    for (let i = 1; i < s.points.length; i++) {
      ctx.lineTo(px(s.points[i]), py(s.points[i]));
    }
    ctx.stroke();
  }
  ctx.restore();
}

function markupUndo() {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan?.markup?.strokes?.length) {
    toast("Nothing to undo");
    return;
  }
  plan.markup.strokes.pop();
  redrawMarkup();
  scheduleSave();
}

function markupClear() {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan?.markup?.strokes?.length) return;
  if (!confirm("Clear all markup on this plan?")) return;
  plan.markup.strokes = [];
  redrawMarkup();
  scheduleSave();
}

// ── MARKUP POINTER EVENTS ────────────────────────────────────────────────────
// Pointer-event-based markup: pen + mouse only. Touch is reserved for pinch-zoom
// (handled natively via touch-action:pinch-zoom on the wrap in markup mode).
// - setPointerCapture so the stroke survives bounds-leaves (e.g. pencil drifts off canvas)
// - getCoalescedEvents() captures sub-frame pencil samples for smooth high-frequency strokes
let mkActivePointerId = null; // pointerId capturing the current stroke

function initMarkupEvents() {
  const wrap = document.getElementById("fpCanvasWrap");

  wrap.addEventListener(
    "pointerdown",
    (e) => {
      if (fpMode !== "markup") return;
      // Pan tool: stand down and let native gestures (pan/pinch) handle the input.
      if (mkTool === "pan") return;
      // Touch pointers are reserved for pinch-zoom in markup mode — only pen
      // and mouse draw. (User confirmed markup is stylus-only in practice.)
      if (e.pointerType === "touch") return;
      // Force-clean any stuck previous stroke state. iPadOS occasionally drops
      // pointerup events after Apple Pencil lift, leaving mkActivePointerId set
      // and blocking the next stroke. New pointerdown wins — close the prior
      // stroke and start fresh immediately.
      if (mkActivePointerId !== null) {
        try {
          wrap.releasePointerCapture(mkActivePointerId);
        } catch (_) {}
        if (mkDrawing) markupEndStroke();
        mkActivePointerId = null;
      }
      e.preventDefault();
      try {
        wrap.setPointerCapture(e.pointerId);
      } catch (_) {}
      mkActivePointerId = e.pointerId;
      const { x, y } = mkCanvasCoords(e.clientX, e.clientY);
      markupStartStroke(x, y);
      markupAddPoint(x + 0.5, y + 0.5); // visible dot on tap-without-drag
    },
    { passive: false },
  );

  wrap.addEventListener(
    "pointermove",
    (e) => {
      if (fpMode !== "markup" || !mkDrawing) return;
      if (e.pointerId !== mkActivePointerId) return;
      e.preventDefault();
      const samples =
        e.getCoalescedEvents && e.getCoalescedEvents().length
          ? e.getCoalescedEvents()
          : [e];
      samples.forEach((ev) => {
        const { x, y } = mkCanvasCoords(ev.clientX, ev.clientY);
        markupAddPoint(x, y);
      });
    },
    { passive: false },
  );

  const finish = (e) => {
    if (e.pointerId !== mkActivePointerId) return;
    try {
      wrap.releasePointerCapture(e.pointerId);
    } catch (_) {}
    mkActivePointerId = null;
    if (mkDrawing) markupEndStroke();
  };
  wrap.addEventListener("pointerup", finish);
  wrap.addEventListener("pointercancel", finish);
  // Note: pointerleave intentionally NOT used — Apple Pencil hover-near-edge
  // can fire it mid-stroke, which would prematurely end the line.

  // ── Light Level mode tap-to-drop (footcandle readings) ───────────────
  // Tap empty space = drop a yellow marker + open callout. Tap an existing
  // marker = open its callout for edit.
  let _llPointerStart = null;
  wrap.addEventListener(
    "pointerdown",
    (e) => {
      if (fpMode !== "lightlevel") return;
      if (e.target && e.target.closest && e.target.closest("[data-ll-id]"))
        return; // existing marker handler
      if (e.target && e.target.closest && e.target.closest("#fpLightCallout"))
        return; // callout interaction
      _llPointerStart = { x: e.clientX, y: e.clientY, t: Date.now() };
    },
    { passive: true },
  );
  wrap.addEventListener(
    "pointerup",
    (e) => {
      if (fpMode !== "lightlevel" || !_llPointerStart) return;
      const dx = Math.abs(e.clientX - _llPointerStart.x);
      const dy = Math.abs(e.clientY - _llPointerStart.y);
      const dt = Date.now() - _llPointerStart.t;
      _llPointerStart = null;
      if (dx > 12 || dy > 12 || dt > 600) return; // pan/long-hold, not a tap
      if (e.target && e.target.closest && e.target.closest("#fpLightCallout"))
        return;
      const canvas = document.getElementById("fpCanvas");
      if (!canvas?.width) return;
      const rect = wrap.getBoundingClientRect();
      const canvasX = (e.clientX - rect.left - fpOffset.x) / fpScale;
      const canvasY = (e.clientY - rect.top - fpOffset.y) / fpScale;
      if (
        canvasX < 0 ||
        canvasY < 0 ||
        canvasX > canvas.width ||
        canvasY > canvas.height
      )
        return;
      dropLightLevel(
        canvasX / canvas.width,
        canvasY / canvas.height,
        e.clientX,
        e.clientY,
      );
    },
    { passive: true },
  );
  wrap.addEventListener(
    "pointercancel",
    () => {
      _llPointerStart = null;
    },
    { passive: true },
  );

  // ── Note mode tap-to-drop (Note is its own top-level fpMode) ──────────
  // Tap = drop note. Drag = pan/zoom (touch-action: auto). Distinguish via
  // start position + movement check on pointerup.
  let _notePointerStart = null;
  wrap.addEventListener(
    "pointerdown",
    (e) => {
      if (fpMode !== "note") return;
      if (e.target && e.target.closest && e.target.closest("[data-note-id]"))
        return;
      _notePointerStart = { x: e.clientX, y: e.clientY, t: Date.now() };
    },
    { passive: true },
  );
  wrap.addEventListener(
    "pointerup",
    (e) => {
      if (fpMode !== "note" || !_notePointerStart) return;
      const dx = Math.abs(e.clientX - _notePointerStart.x);
      const dy = Math.abs(e.clientY - _notePointerStart.y);
      const dt = Date.now() - _notePointerStart.t;
      _notePointerStart = null;
      if (dx > 12 || dy > 12 || dt > 600) return; // pan/long-hold, not a tap
      const canvas = document.getElementById("fpCanvas");
      if (!canvas?.width) return;
      const rect = wrap.getBoundingClientRect();
      const canvasX = (e.clientX - rect.left - fpOffset.x) / fpScale;
      const canvasY = (e.clientY - rect.top - fpOffset.y) / fpScale;
      if (
        canvasX < 0 ||
        canvasY < 0 ||
        canvasX > canvas.width ||
        canvasY > canvas.height
      )
        return;
      dropNote(canvasX / canvas.width, canvasY / canvas.height);
    },
    { passive: true },
  );
  wrap.addEventListener(
    "pointercancel",
    () => {
      _notePointerStart = null;
    },
    { passive: true },
  );

  // ── Polygon mode tap handling ─────────────────────────────────────────
  // Three distinct tap behaviors in polygon mode (priority order):
  //   1. Tap near the first vertex of the in-progress draft → close the polygon
  //   2. Tap inside an existing polygon (when NOT drafting) → open its edit modal
  //   3. Tap anywhere else → add a vertex (start or continue a draft)
  // Drag/pan motions are filtered out via start-position movement check.
  let _polyPointerStart = null;
  wrap.addEventListener(
    "pointerdown",
    (e) => {
      if (fpMode !== "polygon") return;
      _polyPointerStart = { x: e.clientX, y: e.clientY, t: Date.now() };
    },
    { passive: true },
  );
  wrap.addEventListener(
    "pointerup",
    (e) => {
      if (fpMode !== "polygon" || !_polyPointerStart) return;
      const dx = Math.abs(e.clientX - _polyPointerStart.x);
      const dy = Math.abs(e.clientY - _polyPointerStart.y);
      const dt = Date.now() - _polyPointerStart.t;
      _polyPointerStart = null;
      if (dx > 12 || dy > 12 || dt > 600) return; // pan / long-hold, not a tap

      // Taps on the floating shape-edit toolbar should not reach the polygon
      // pointer logic — otherwise tapping 🗑 / Done / the Pin select would
      // immediately exit-on-blank-area before the button's own click runs.
      if (
        e.target &&
        e.target.closest &&
        e.target.closest("#polyShapeEditToolbar")
      )
        return;

      const canvas = document.getElementById("fpCanvas");
      if (!canvas?.width) return;
      const rect = wrap.getBoundingClientRect();
      const canvasX = (e.clientX - rect.left - fpOffset.x) / fpScale;
      const canvasY = (e.clientY - rect.top - fpOffset.y) / fpScale;
      if (
        canvasX < 0 ||
        canvasY < 0 ||
        canvasX > canvas.width ||
        canvasY > canvas.height
      )
        return;

      // While in shape-edit, taps on a vertex circle never reach here (the circle
      // calls e.stopPropagation in attachPolyVertexDrag). Any other tap means
      // either switch edit to a different polygon, or exit edit if outside all.
      if (_polyEditingShape) {
        const hit = _polyHitTest(canvasX, canvasY);
        if (hit && hit.id === _polyEditingShape) return; // tap on same polygon body = no-op
        if (hit) {
          // Switch edit to the tapped polygon
          _polyEditId = hit.id;
          _polyEditingShape = hit.id;
          _populatePolyShapeToolbar();
          renderPolygons();
          return;
        }
        // Tap outside any polygon while in edit → exit edit (auto-save)
        exitPolyShapeEdit();
        return;
      }

      // 1) Closing an in-progress draft by tapping near its first vertex
      if (_polyTryClose(e.clientX, e.clientY)) return;

      // 2) Editing an existing polygon — short-tap enters shape-edit directly.
      //    No modal: link/delete are now on the floating toolbar.
      if (!_polyDraft) {
        const hit = _polyHitTest(canvasX, canvasY);
        if (hit) {
          _polyEditId = hit.id;
          enterPolyShapeEdit();
          return;
        }
      }

      // 3) Add a vertex (start or extend a draft)
      _polyAddVertex(canvasX / canvas.width, canvasY / canvas.height);
    },
    { passive: true },
  );
  wrap.addEventListener(
    "pointercancel",
    () => {
      _polyPointerStart = null;
    },
    { passive: true },
  );
}

// ── EXPORT PDF ────────────────────────────────────────────────────────────────
async function fpExportPDF(withMarkup = true) {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  if (!plan) {
    toast("No floor plan loaded");
    return;
  }

  const fpCanvas = document.getElementById("fpCanvas");
  if (!fpCanvas.width) {
    toast("Render the floor plan first");
    return;
  }

  // Load jsPDF from CDN if not already loaded
  if (typeof window.jspdf === "undefined") {
    toast("Loading PDF library…");
    await new Promise((res, rej) => {
      const s = document.createElement("script");
      s.src =
        "https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js";
      s.onload = res;
      s.onerror = rej;
      document.head.appendChild(s);
    });
  }

  const w = fpCanvas.width,
    h = fpCanvas.height;

  // Composite onto an off-screen canvas
  const out = document.createElement("canvas");
  out.width = w;
  out.height = h;
  const ctx = out.getContext("2d");

  // Draw PDF layer
  ctx.drawImage(fpCanvas, 0, 0);

  // Draw polygon outlines (saved data — render under markup strokes)
  if (plan?.markup?.polygons?.length) {
    const w = fpCanvas.width,
      h = fpCanvas.height;
    plan.markup.polygons.forEach((p) => {
      if (!p.points || p.points.length < 3) return;
      const cls =
        typeof _polyColorClass === "function" ? _polyColorClass(p) : "unlinked";
      const fill =
        cls === "logged" ? "#16a34a" : cls === "linked" ? "#d97c18" : "#94a3b8";
      const stroke =
        cls === "logged" ? "#0e7a35" : cls === "linked" ? "#b86010" : "#64748b";
      ctx.beginPath();
      p.points.forEach((pt, i) => {
        const x = pt.xPct * w,
          y = pt.yPct * h;
        if (i === 0) ctx.moveTo(x, y);
        else ctx.lineTo(x, y);
      });
      ctx.closePath();
      ctx.fillStyle = fill;
      ctx.globalAlpha = 0.18;
      ctx.fill();
      ctx.globalAlpha = 1;
      ctx.lineWidth = Math.max(1.5, Math.min(w, h) * 0.001125);
      ctx.strokeStyle = stroke;
      ctx.stroke();
    });
  }

  // Draw markup layer if requested
  if (withMarkup && plan?.markup?.strokes?.length) {
    const mc = getMarkupCanvas();
    if (mc) ctx.drawImage(mc, 0, 0);
  }

  // Draw note markers (always — they're saved data, not free-form markup)
  if (plan?.markup?.notes?.length) {
    const r = Math.max(18, Math.round(Math.min(w, h) * 0.012)); // marker radius scales with plan size
    plan.markup.notes.forEach((n, i) => {
      const sym =
        typeof noteSymbolFor === "function"
          ? noteSymbolFor(n.symbol)
          : { glyph: "!", color: "#dc2626" };
      const cx = n.xPct * w;
      const cy = n.yPct * h;
      // Filled circle
      ctx.beginPath();
      ctx.arc(cx, cy, r, 0, Math.PI * 2);
      ctx.fillStyle = sym.color;
      ctx.fill();
      ctx.lineWidth = Math.max(2, r * 0.16);
      ctx.strokeStyle = "#fff";
      ctx.stroke();
      // Glyph
      ctx.fillStyle = "#fff";
      ctx.font = `bold ${Math.round(
        r * 1.1,
      )}px -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`;
      ctx.textAlign = "center";
      ctx.textBaseline = "middle";
      ctx.fillText(sym.glyph, cx, cy + r * 0.06);
      // Sequence number above
      ctx.fillStyle = "#1a3a5c";
      ctx.font = `bold ${Math.round(
        r * 0.7,
      )}px -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`;
      ctx.fillText(String(i + 1), cx, cy - r - r * 0.5);
    });
  }

  const imgData = out.toDataURL("image/jpeg", 0.92);
  const { jsPDF } = window.jspdf;
  const orientation = w > h ? "landscape" : "portrait";
  const pdfW = w * 0.264583; // px → mm at 96dpi
  const pdfH = h * 0.264583;
  const doc = new jsPDF({ orientation, unit: "mm", format: [pdfW, pdfH] });
  doc.addImage(imgData, "JPEG", 0, 0, pdfW, pdfH);

  const label = withMarkup ? "" : "-clean";
  const name = (plan.name || "floor-plan").replace(/\s+/g, "_");
  doc.save(`${name}${label}.pdf`);
  toast(`✔ Exported ${withMarkup ? "with" : "without"} markup`);
}

// Touch/mouse events on canvas wrap
let fpPinClickConsumed = false; // set by pin mousedown to block canvas click

function initFpEvents() {
  const wrap = document.getElementById("fpCanvasWrap");

  // ── Long-press (1s) to drop a pin — prevents accidental taps ──────────
  let pinHoldTimer = null;
  let pinHoldPos = null;
  let pinHoldRing = null;
  function _cancelPinHold() {
    if (pinHoldTimer) {
      clearTimeout(pinHoldTimer);
      pinHoldTimer = null;
    }
    if (pinHoldRing) {
      pinHoldRing.remove();
      pinHoldRing = null;
    }
    pinHoldPos = null;
  }
  function _dropPinAtScreen(clientX, clientY) {
    if (!activePlanId) return;
    const canvas = document.getElementById("fpCanvas");
    if (!canvas.width || !canvas.height) return;
    const rect = wrap.getBoundingClientRect();
    const canvasX = (clientX - rect.left - fpOffset.x) / fpScale;
    const canvasY = (clientY - rect.top - fpOffset.y) / fpScale;
    if (
      canvasX < 0 ||
      canvasY < 0 ||
      canvasX > canvas.width ||
      canvasY > canvas.height
    )
      return;
    closeCallout();
    dropPin(canvasX / canvas.width, canvasY / canvas.height, clientX, clientY);
  }

  wrap.addEventListener(
    "pointerdown",
    (e) => {
      if (fpMode !== "pin") return;
      if (!activePlanId) return;
      if (e.button && e.button !== 0) return; // ignore middle/right click — middle pans
      // Skip if target is an existing pin — let pin handler take over
      if (e.target && e.target.closest && e.target.closest("[data-pin-id]"))
        return;
      // Skip if the press landed inside the callout, pin-list panel, or any other
      // floating UI. Otherwise a slow tap on Save Room (>750ms) would also kick off
      // a long-press pin drop here, eat closeCallout, and drop a new pin under the
      // user's finger — exactly the "Save does nothing + stuck Pin 2" symptom.
      if (
        e.target &&
        e.target.closest &&
        e.target.closest("#fpCallout, #fpPinList, .pin-hold-ring")
      )
        return;
      _cancelPinHold();
      pinHoldPos = { x: e.clientX, y: e.clientY };
      const cx = e.clientX,
        cy = e.clientY;
      // Visual feedback ring (grows over 1s)
      pinHoldRing = document.createElement("div");
      pinHoldRing.className = "pin-hold-ring";
      pinHoldRing.style.left = cx + "px";
      pinHoldRing.style.top = cy + "px";
      document.body.appendChild(pinHoldRing);
      pinHoldTimer = setTimeout(() => {
        pinHoldTimer = null;
        if (pinHoldRing) {
          pinHoldRing.remove();
          pinHoldRing = null;
        }
        _dropPinAtScreen(cx, cy);
      }, 750);
    },
    { passive: true },
  );

  wrap.addEventListener(
    "pointermove",
    (e) => {
      if (!pinHoldTimer || !pinHoldPos) return;
      const dx = Math.abs(e.clientX - pinHoldPos.x);
      const dy = Math.abs(e.clientY - pinHoldPos.y);
      if (dx > 12 || dy > 12) _cancelPinHold();
    },
    { passive: true },
  );

  ["pointerup", "pointercancel", "pointerleave"].forEach((ev) => {
    wrap.addEventListener(ev, _cancelPinHold);
  });

  // Pan drag
  wrap.addEventListener("mousedown", (e) => {
    if (fpMode !== "pan") return;
    if (e.button !== 0) return;
    fpDragging = false;
    fpDragStart = { x: e.clientX - fpOffset.x, y: e.clientY - fpOffset.y };
    wrap.style.cursor = "grabbing";
    const onMove = (ev) => {
      fpDragging = true;
      fpOffset.x = ev.clientX - fpDragStart.x;
      fpOffset.y = ev.clientY - fpDragStart.y;
      applyTransform();
    };
    const onUp = () => {
      wrap.style.cursor = "grab";
      document.removeEventListener("mousemove", onMove);
      document.removeEventListener("mouseup", onUp);
      setTimeout(() => {
        fpDragging = false;
      }, 50);
    };
    document.addEventListener("mousemove", onMove);
    document.addEventListener("mouseup", onUp);
  });

  // Middle-mouse-button pan — works in any mode (pin, draw, etc.) so the user
  // doesn't have to switch tools just to reposition the view on desktop.
  wrap.addEventListener("mousedown", (e) => {
    if (e.button !== 1) return;
    e.preventDefault(); // suppress browser's middle-click auto-scroll
    const startX = e.clientX - fpOffset.x;
    const startY = e.clientY - fpOffset.y;
    const prevCursor = wrap.style.cursor;
    wrap.style.cursor = "grabbing";
    const onMove = (ev) => {
      fpOffset.x = ev.clientX - startX;
      fpOffset.y = ev.clientY - startY;
      applyTransform();
    };
    const onUp = (ev) => {
      if (ev.button !== undefined && ev.button !== 1) return;
      wrap.style.cursor = prevCursor || "";
      document.removeEventListener("mousemove", onMove);
      document.removeEventListener("mouseup", onUp);
    };
    document.addEventListener("mousemove", onMove);
    document.addEventListener("mouseup", onUp);
  });
  // Prevent the browser's middle-click click default (would otherwise paste/etc.)
  wrap.addEventListener("auxclick", (e) => {
    if (e.button === 1) e.preventDefault();
  });

  // Touch pan + 2-finger pinch (routes browser pinch into in-app fpZoom so the
  // mode bar / toolbar stay anchored at all zoom levels).
  let lastTouch = null;
  let fpTouchCount = 0; // track max fingers during a gesture
  let fpTouchStartPos = null;
  let _fpPinch = null; // { d0, mx0, my0, scale0, ox0, oy0 } — pinch baseline

  wrap.addEventListener(
    "touchstart",
    (e) => {
      fpTouchCount = Math.max(fpTouchCount, e.touches.length);
      window._fpActiveTouches = e.touches.length;
      if (e.touches.length === 1) {
        lastTouch = { x: e.touches[0].clientX, y: e.touches[0].clientY };
        fpTouchStartPos = { x: e.touches[0].clientX, y: e.touches[0].clientY };
        _fpPinch = null;
      } else if (e.touches.length >= 2) {
        const a = e.touches[0],
          b = e.touches[1];
        const dx = b.clientX - a.clientX,
          dy = b.clientY - a.clientY;
        _fpPinch = {
          d0: Math.hypot(dx, dy) || 1,
          mx0: (a.clientX + b.clientX) / 2,
          my0: (a.clientY + b.clientY) / 2,
          scale0: fpScale,
          ox0: fpOffset.x,
          oy0: fpOffset.y,
        };
        e.preventDefault();
      }
    },
    { passive: false },
  );

  wrap.addEventListener(
    "touchmove",
    (e) => {
      fpTouchCount = Math.max(fpTouchCount, e.touches.length);
      // 2-finger pinch + pan — anchor zoom around initial midpoint, translate with midpoint drift
      if (e.touches.length >= 2 && _fpPinch) {
        const a = e.touches[0],
          b = e.touches[1];
        const dx = b.clientX - a.clientX,
          dy = b.clientY - a.clientY;
        const d = Math.hypot(dx, dy) || 1;
        const mx = (a.clientX + b.clientX) / 2;
        const my = (a.clientY + b.clientY) / 2;
        const newScale = Math.max(
          0.2,
          Math.min(8, _fpPinch.scale0 * (d / _fpPinch.d0)),
        );
        const wrapRect = wrap.getBoundingClientRect();
        const fx = _fpPinch.mx0 - wrapRect.left;
        const fy = _fpPinch.my0 - wrapRect.top;
        const sf = newScale / _fpPinch.scale0;
        // Keep the canvas point under the initial pinch midpoint fixed:
        //   offset_new = focal*(1 - sf) + sf*offset_old   (matches fpZoom button math)
        // Plus midpoint drift to allow 2-finger pan during the pinch.
        fpScale = newScale;
        fpOffset.x = fx * (1 - sf) + sf * _fpPinch.ox0 + (mx - _fpPinch.mx0);
        fpOffset.y = fy * (1 - sf) + sf * _fpPinch.oy0 + (my - _fpPinch.my0);
        applyTransform();
        e.preventDefault();
        return;
      }
      if (e.touches.length !== 1) return;
      // 1-finger pan: explicit pan mode, OR markup mode with pan tool selected
      // (the markup pan tool used to rely on browser-native pan via touch-action:auto;
      //  we now drive pan in JS so that 2-finger pinch can be intercepted everywhere)
      const panOK =
        fpMode === "pan" ||
        (fpMode === "markup" &&
          typeof mkTool !== "undefined" &&
          mkTool === "pan");
      if (!panOK) return;
      const t = e.touches[0];
      fpOffset.x += t.clientX - lastTouch.x;
      fpOffset.y += t.clientY - lastTouch.y;
      lastTouch = { x: t.clientX, y: t.clientY };
      applyTransform();
      e.preventDefault();
    },
    { passive: false },
  );

  wrap.addEventListener("touchend", (e) => {
    if (e.touches.length < 2) _fpPinch = null;
    // Pin drops moved to pointerdown long-press (1s hold). Just keep the
    // touch counter in sync for other code that reads window._fpActiveTouches.
    if (e.touches.length === 0) {
      fpTouchCount = 0;
      window._fpActiveTouches = 0;
    } else window._fpActiveTouches = e.touches.length;
  });
}

function dropPin(xPct, yPct, screenX, screenY) {
  const plan = floorPlans[activePlanId];
  const pinNum = fpPinCounter++;
  const roomName = ""; // always blank — user fills in callout
  const roomType = "";
  const pin = { id: "pin_" + pinNum, xPct, yPct, roomName, roomType, pinNum };
  plan.pins.push(pin);
  renderPins();
  renderPinList();

  if (fpPendingCopy) {
    fpApplyCopyToPin(pin);
  } else if (fpPendingSetPin) {
    // Auto-assign room name from log
    pin.roomName = fpPendingSetPin.roomName;
    const e = logEntries[fpPendingSetPin.entryIdx];
    if (e) {
      if (e.roomType) pin.roomType = e.roomType;
      if (e.height) pin.height = e.height;
      if (e.ceilingType) pin.ceilingType = e.ceilingType;
    }
    fpPendingSetPin = null;
    renderPins();
    renderPinList();
    document.getElementById("fpModeHint").textContent =
      "Tap plan to drop a pin";
    document.getElementById("fpModeHint").style.color = "#5a8ab0";
    toast('📍 Pin placed for "' + pin.roomName + '"');
    showCallout(pin, screenX, screenY);
  } else {
    showPinSetupModal(pin, screenX, screenY);
  }
}

function renderPins() {
  // Polygons share their color with linked pins, so re-render polygons whenever
  // pin state changes (room assignment, logged data, etc.)
  if (typeof renderPolygons === "function") renderPolygons();
  const plan = floorPlans[activePlanId];
  const layer = document.getElementById("fpPinsLayer");
  const canvas = document.getElementById("fpCanvas");
  if (!layer) return;
  layer.innerHTML = "";
  // Layer is transparent — let clicks pass through to layers below (polygon
  // vertex circles especially). Individual pin elements set their own
  // pointer-events:all in their inline cssText, so they still receive taps.
  layer.style.pointerEvents = "none";
  (plan?.pins || []).forEach((pin) => {
    const el = document.createElement("div");
    const hasLogged = !!(
      pin.roomName && logEntries.some((e) => e.space === pin.roomName)
    );
    const color = hasLogged ? "#ff6eb4" : "#00e64d";
    const shadow = hasLogged
      ? "0 0 14px rgba(255,110,180,.8), 0 4px 16px rgba(0,0,0,.5)"
      : "0 0 14px rgba(0,230,77,.8), 0 4px 16px rgba(0,0,0,.5)";
    el.style.cssText = `position:absolute;left:${pin.xPct * 100}%;top:${
      pin.yPct * 100
    }%;transform:translate(-50%,-100%);cursor:pointer;pointer-events:all;z-index:5;user-select:none;-webkit-user-select:none`;
    el.dataset.pinId = pin.id;
    const _ms = fpGetMarkerScale() * fpPinSizeMultiplier(pin);
    const _pinSz = Math.round(27 * _ms);
    const _pinFs = Math.max(9, Math.round(10 * _ms));
    // No opacity on the wrapper — that previously made the number translucent
    // too. Solid pin (matches the minimap) keeps the number crisp at any zoom.
    el.innerHTML = `<div style="background:${color};color:#003010;border-radius:50% 50% 50% 0;width:${_pinSz}px;height:${_pinSz}px;display:flex;align-items:center;justify-content:center;font-size:${_pinFs}px;font-weight:800;transform:rotate(-45deg);box-shadow:${shadow}"><span style="transform:rotate(45deg)">${pin.pinNum}</span></div>`;

    let holdTimer = null;
    let dragging = false;

    // ── Mouse ──────────────────────────────────────────────────────────────
    el.addEventListener("mousedown", (e) => {
      e.stopPropagation();
      fpPinClickConsumed = true; // prevent canvas from dropping a new pin
      dragging = false;

      holdTimer = setTimeout(() => {
        // Hold detected — start drag mode
        dragging = true;
        closeCallout();
        el.style.cursor = "grabbing";
        el.style.zIndex = "20";

        // rAF-throttled polygon re-render — gives live red-dashed feedback as the
        // pin crosses its linked polygon's boundary without rebuilding the SVG
        // more than once per frame.
        let _polyRafPending = false;
        const _schedulePolyRender = () => {
          if (_polyRafPending) return;
          _polyRafPending = true;
          requestAnimationFrame(() => {
            _polyRafPending = false;
            if (typeof renderPolygons === "function") renderPolygons();
          });
        };
        const onMove = (mv) => {
          const wrap = document.getElementById("fpCanvasWrap");
          const rect = wrap.getBoundingClientRect();
          pin.xPct = Math.max(
            0,
            Math.min(
              1,
              (mv.clientX - rect.left - fpOffset.x) / (canvas.width * fpScale),
            ),
          );
          pin.yPct = Math.max(
            0,
            Math.min(
              1,
              (mv.clientY - rect.top - fpOffset.y) / (canvas.height * fpScale),
            ),
          );
          el.style.left = `${pin.xPct * 100}%`;
          el.style.top = `${pin.yPct * 100}%`;
          _schedulePolyRender();
        };
        const onUp = () => {
          el.style.cursor = "pointer";
          el.style.zIndex = "5";
          document.removeEventListener("mousemove", onMove);
          document.removeEventListener("mouseup", onUp);
          if (typeof renderPolygons === "function") renderPolygons();
          if (typeof renderAuditMinimap === "function") renderAuditMinimap();
          if (typeof scheduleSave === "function") scheduleSave();
        };
        document.addEventListener("mousemove", onMove);
        document.addEventListener("mouseup", onUp);
      }, 250);

      const onUp = (mu) => {
        document.removeEventListener("mouseup", onUp);
        if (!dragging) {
          clearTimeout(holdTimer);
          // Quick tap — sync room name to form then show callout
          if (pin.roomName) {
            state.room = pin.roomName;
            document.getElementById("roomInput").value = pin.roomName;
            updateSummary();
          }
          showCallout(pin, mu.clientX, mu.clientY);
        }
      };
      document.addEventListener("mouseup", onUp);
    });

    // ── Touch ──────────────────────────────────────────────────────────────
    el.addEventListener(
      "touchstart",
      (e) => {
        e.stopPropagation();
        e.preventDefault();
        fpPinClickConsumed = true;
        dragging = false;
        const t0 = e.touches[0];

        holdTimer = setTimeout(() => {
          // Abort drag if a second finger is now on screen (pinch/zoom gesture)
          if (window._fpActiveTouches > 1) return;
          dragging = true;
          closeCallout();
          el.style.zIndex = "20";

          let _polyRafPending = false;
          const _schedulePolyRender = () => {
            if (_polyRafPending) return;
            _polyRafPending = true;
            requestAnimationFrame(() => {
              _polyRafPending = false;
              if (typeof renderPolygons === "function") renderPolygons();
            });
          };
          const onMove = (tv) => {
            // Cancel drag mid-gesture if second finger joins
            if (tv.touches.length > 1) {
              el.style.zIndex = "5";
              el.removeEventListener("touchmove", onMove);
              el.removeEventListener("touchend", onEnd);
              dragging = false;
              return;
            }
            const t = tv.touches[0];
            const wrap = document.getElementById("fpCanvasWrap");
            const rect = wrap.getBoundingClientRect();
            pin.xPct = Math.max(
              0,
              Math.min(
                1,
                (t.clientX - rect.left - fpOffset.x) / (canvas.width * fpScale),
              ),
            );
            pin.yPct = Math.max(
              0,
              Math.min(
                1,
                (t.clientY - rect.top - fpOffset.y) / (canvas.height * fpScale),
              ),
            );
            el.style.left = `${pin.xPct * 100}%`;
            el.style.top = `${pin.yPct * 100}%`;
            _schedulePolyRender();
          };
          const onEnd = () => {
            el.style.zIndex = "5";
            el.removeEventListener("touchmove", onMove);
            el.removeEventListener("touchend", onEnd);
            if (typeof renderPolygons === "function") renderPolygons();
            if (typeof renderAuditMinimap === "function") renderAuditMinimap();
            if (typeof scheduleSave === "function") scheduleSave();
          };
          el.addEventListener("touchmove", onMove, { passive: false });
          el.addEventListener("touchend", onEnd);
        }, 250);

        const onEnd = (te) => {
          el.removeEventListener("touchend", onEnd);
          if (!dragging) {
            clearTimeout(holdTimer);
            const t = te.changedTouches[0];
            if (pin.roomName) {
              state.room = pin.roomName;
              document.getElementById("roomInput").value = pin.roomName;
              updateSummary();
            }
            showCallout(pin, t.clientX, t.clientY);
          }
        };
        el.addEventListener("touchend", onEnd);
      },
      { passive: false },
    );

    layer.appendChild(el);
  });
  if (typeof renderAuditMinimap === "function") renderAuditMinimap();
}

function showPinSetupModal(pin, screenX, screenY) {
  if (!pin || pin.pinNum === undefined) return;
  // Floor-plan-tab pin drops always start blank — the callout is where the
  // user enters the room name, and pre-filling from state.room caused stale
  // names from a previously-audited room to bleed into fresh pins. Minimap
  // pin drops have their own auto-bind path (dropPinFromMinimap) since there's
  // no callout there.
  showCallout(pin, screenX || 200, screenY || 200);
  const input = document.getElementById("fpRoomAssignInput");
  const rtSelect = document.getElementById("fpRoomTypeSelect");
  if (input) input.value = pin.roomName || "";
  if (rtSelect) rtSelect.value = pin.roomType || "";
  setTimeout(() => {
    if (input) input.focus();
  }, 80);
}

function showCallout(pin, screenX, screenY) {
  if (!pin) return;
  fpSelectedPin = pin;
  const callout = document.getElementById("fpCallout");
  const calloutBody = document.getElementById("fpCalloutBody");
  const calloutTitle = document.getElementById("fpCalloutTitle");
  if (!callout || !calloutBody || !calloutTitle) return; // DOM not ready / wrong context
  const logged = logEntries.filter((e) => e.space === (pin.roomName || ""));
  // If pin has no roomType stored, read it from the log
  if (!pin.roomType && logged.length && logged[0].roomType) {
    pin.roomType = logged[0].roomType;
  }

  // Build fixture lines: existing description (from MATCH_DATA) + qty
  const fixtureLines = logged
    .map((e) => {
      const baseCode = e.fixtureId.replace(/-(hall|dim|stair|H)$/, "");
      const match = MATCH_DATA.find((r) => r.id === baseCode);
      const existingDesc = (
        match?.existing ||
        match?.desc ||
        e.fixtureId ||
        ""
      ).trim();
      return existingDesc
        ? `${existingDesc} ×${e.qty}`
        : `${e.fixtureId} ×${e.qty}`;
    })
    .join("<br>");

  // Dimensions from first logged entry for this room
  const dims = logged[0]?.dimensions || "";

  document.getElementById(
    "fpCalloutTitle",
  ).textContent = `📍 Pin ${pin.pinNum}`;
  document.getElementById("fpCalloutBody").innerHTML = `
    <div style="margin-bottom:5px">
      <label style="font-size:9px;font-weight:700;text-transform:uppercase;letter-spacing:.06em;color:#8a9ab0">Room</label>
      <input id="fpRoomAssignInput" value="${
        pin.roomName || ""
      }" placeholder="Type room name…"
        style="width:100%;padding:4px 7px;border:1.5px solid #c8d4e3;border-radius:6px;font-size:12px;font-family:inherit;outline:none;margin-top:2px;box-sizing:border-box"
        autocomplete="new-password" enterkeyhint="done" oninput="if(fpSelectedPin)fpSelectedPin.roomName=this.value.trim()">
    </div>
    <div style="margin-bottom:5px">
      <label style="font-size:9px;font-weight:700;text-transform:uppercase;letter-spacing:.06em;color:#8a9ab0">Room Type</label>
      <select id="fpRoomTypeSelect" onchange="if(fpSelectedPin)fpSelectedPin.roomType=this.value"
        style="width:100%;padding:4px 7px;border:1.5px solid #c8d4e3;border-radius:6px;font-size:12px;font-family:inherit;outline:none;margin-top:2px;background:#fff;box-sizing:border-box">
        <option value="">— Select type —</option>
        ${[...new Set(ROOM_TYPE_DB.map((r) => r.group))]
          .map(
            (g) =>
              `<option value="${g}" ${
                (pin.roomType || "") === g ? "selected" : ""
              }>${g}</option>`,
          )
          .join("")}
      </select>
    </div>
    ${
      dims
        ? `<div style="font-size:10px;color:#2a7a50;font-weight:600;margin-bottom:3px">📐 ${dims}</div>`
        : ""
    }
    ${
      logged.length
        ? `<div style="font-size:10px;color:#5a7a9a;line-height:1.5">${fixtureLines}</div>`
        : ""
    }
  `;

  // Position callout — measure first, then clamp to viewport so it always fits on screen
  // (critical at high zoom where a pin near the edge would otherwise push the callout off-screen)
  const ov = document
    .getElementById("floorPlanOverlay")
    .getBoundingClientRect();
  callout.style.visibility = "hidden";
  callout.style.display = "block";
  const cw = callout.offsetWidth || 220;
  const ch = callout.offsetHeight || 240;
  let cx = (screenX || 0) - ov.left + 12;
  let cy = (screenY || 0) - ov.top - ch - 12;
  // Flip horizontally if off right edge
  if (cx + cw > ov.width - 8) cx = (screenX || 0) - ov.left - cw - 12;
  // Flip vertically if off top
  if (cy < 8) cy = (screenY || 0) - ov.top + 24;
  // Final clamp — handles cases where a flip still overflows (extreme zoom / corner pins)
  cx = Math.max(8, Math.min(cx, ov.width - cw - 8));
  cy = Math.max(8, Math.min(cy, ov.height - ch - 8));
  callout.style.left = cx + "px";
  callout.style.top = cy + "px";
  callout.style.visibility = "";

  // Always show assign button; show Go To Room only if logged
  document.getElementById("fpCalloutAssign").style.display = "";
  document.getElementById("fpCalloutAssign").textContent = "Save Room";
  document.getElementById("fpCalloutGoTo").style.display = pin.roomName
    ? ""
    : "none";
  document.getElementById("fpCalloutCopy").style.display =
    pin.roomName && logged.length ? "" : "none"; // copy needs logged data

  // Focus the input
  setTimeout(() => document.getElementById("fpRoomAssignInput")?.focus(), 50);
}

function closeCallout() {
  // Sweep an unsaved pin — if the callout closes for a pin that never got a
  // room name, delete it instead of leaving a phantom marker on the plan.
  if (fpSelectedPin && !fpSelectedPin.roomName) {
    const plan = activePlanId ? floorPlans[activePlanId] : null;
    if (plan) {
      plan.pins = plan.pins.filter((p) => p.id !== fpSelectedPin.id);
      renderPins();
      renderPinList();
    }
  }
  document.getElementById("fpCallout").style.display = "none";
  fpSelectedPin = null;
  if (fpConfirmCopy._dupWarned !== undefined) fpConfirmCopy._dupWarned = false;
}

function fpAssignCurrent() {
  if (!fpSelectedPin) {
    toast("No pin selected — re-tap the pin");
    return;
  }
  try {
    const input = document.getElementById("fpRoomAssignInput");
    const rtSelect = document.getElementById("fpRoomTypeSelect");
    const name = input ? input.value.trim() : "";
    if (!name) {
      toast("Enter a room name first");
      return;
    }
    const pin = fpSelectedPin;
    pin.roomName = name;
    const newRoomType = rtSelect?.value || "";
    if (newRoomType) pin.roomType = newRoomType;
    // Also store height + ceiling type from current state
    if (state.height) pin.height = state.height;
    if (state.ceilingType) pin.ceilingType = state.ceilingType;
    // Update logEntries for this room with new name and room type
    let changed = false;
    logEntries.forEach((e) => {
      if (e.space === name || e.space === pin.roomName) {
        if (newRoomType) {
          e.roomType = newRoomType;
          changed = true;
        }
      }
    });
    if (!state.room) {
      state.room = name;
      document.getElementById("roomInput").value = name;
      updateRoomDisplay();
      updateSummary();
    }
    const savedPinNum = pin.pinNum;
    renderPins();
    renderPinList();
    if (typeof scheduleSave === "function") scheduleSave();
    // Refresh the callout in-place — re-renders with the saved roomName, which
    // makes "Go to Room" appear. This is the visual feedback the user expects
    // (button transitions from input-mode to saved-mode without dismissing).
    // The callout's screen position is preserved by passing through the same
    // button as the anchor (use the pin's current screen position).
    const wrap = document.getElementById("fpCanvasWrap");
    const canvas = document.getElementById("fpCanvas");
    let sx = 200,
      sy = 200;
    if (wrap && canvas?.width) {
      const rect = wrap.getBoundingClientRect();
      sx = rect.left + pin.xPct * canvas.width * fpScale + fpOffset.x;
      sy = rect.top + pin.yPct * canvas.height * fpScale + fpOffset.y;
    }
    showCallout(pin, sx, sy);
    toast(
      `✓ Pin ${savedPinNum} → "${name}"` +
        (changed && newRoomType ? ` · type updated in log` : ""),
    );
  } catch (err) {
    // Log full details to console so we can copy the stack trace
    try {
      console.error("[fpAssignCurrent]", err);
    } catch (_) {}
    const where = (err?.stack || "").split("\n").slice(0, 3).join(" | ");
    toast(
      "Save failed: " + (err?.message || err) + (where ? " @ " + where : ""),
    );
  }
}

function fpGoToRoom() {
  if (!fpSelectedPin?.roomName) return;
  const pin = fpSelectedPin;
  closeFloorPlan();
  loadRoomFromPin(pin);
}

// ── ROOM DRAFTS ──────────────────────────────────────────────────────────────
// Snapshot the current room's in-progress work into roomDrafts so it survives
// a navigation away. Skipped if there's nothing to save (avoids polluting the
// drafts map with empty entries when the user clicks around).
function snapshotCurrentRoomDraft() {
  const name = (state.room || "").trim();
  if (!name) return;
  const notes = (document.getElementById("notesInput")?.value || "").trim();
  const hasWork =
    state.fixtures.length > 0 ||
    state.switchType.length > 0 ||
    state.existingCtrls.length > 0 ||
    state.ctrls.length > 0 ||
    notes.length > 0;
  if (!hasWork) {
    delete roomDrafts[name];
    return;
  }
  roomDrafts[name] = {
    height: state.height,
    roomtype: state.roomtype,
    ceilingType: state.ceilingType,
    dimensions: state.dimensions,
    fixtures: state.fixtures.map((f) => ({ ...f })),
    switchType: state.switchType.map((s) => ({ ...s })),
    existingCtrls: state.existingCtrls.map((x) => ({ ...x })),
    ctrls: state.ctrls.map((x) => ({ ...x })),
    swRec: (state.swRec || []).map((x) => ({ ...x })),
    fixRec: (state.fixRec || []).map((x) => ({ ...x })),
    notes,
  };
}

// Apply a saved draft to the current state + UI. Returns true if a draft was
// found and restored, false otherwise. Caller is expected to have already set
// state.room and called clearFixtures/updateRoomDisplay.
function restoreRoomDraft(roomName) {
  const d = roomDrafts[roomName];
  if (!d) return false;
  state.height = d.height || "";
  state.roomtype = d.roomtype || "";
  state.ceilingType = d.ceilingType || "";
  state.dimensions = d.dimensions || null;
  state.fixtures = (d.fixtures || []).map((f) => ({ ...f }));
  state.switchType = (d.switchType || []).map((s) => ({ ...s }));
  state.existingCtrls = (d.existingCtrls || []).map((x) => ({ ...x }));
  state.ctrls = (d.ctrls || []).map((x) => ({ ...x }));
  state.swRec = (d.swRec || []).map((x) => ({ ...x }));
  state.fixRec = (d.fixRec || []).map((x) => ({ ...x }));

  // UI sync — mirror the patterns used by importRoomConditions / fpGoToRoom
  if (d.height) updateHeightBtn(String(d.height));
  if (d.roomtype) {
    const rtOpts = {
      "Class rooms": "Class",
      Office: "Office",
      Restroom: "Restroom",
      Closet: "Closet",
      Storage: "Storage",
      Hallway: "Hall",
      Stairwell: "Stair",
      "Slop Sink": "Slop Sink",
      Gym: "Gym",
      Mechanical: "Mech",
      Cafeteria: "Cafe",
      Library: "Library",
    };
    const lbl = document.getElementById("roomTypeBtnLabel");
    if (lbl) {
      lbl.textContent = rtOpts[d.roomtype] || d.roomtype;
      lbl.style.color = "#1a2a3a";
    }
    const btn = document.getElementById("roomTypeBtn");
    if (btn) {
      btn.style.borderColor = "#7a3abd";
      btn.style.background = "#f3eeff";
    }
    document
      .querySelectorAll(".rt-opt")
      .forEach((b) => b.classList.toggle("active", b.dataset.v === d.roomtype));
  }
  if (d.ceilingType) updateCeilingTypeBtn(d.ceilingType);
  if (d.dimensions?.len && d.dimensions?.wid) {
    const label = `${d.dimensions.len}' × ${d.dimensions.wid}'`;
    const dl = document.getElementById("dimensionsBtnLabel");
    if (dl) {
      dl.textContent = label;
      dl.style.color = "#1a3a5c";
    }
    const db = document.getElementById("dimensionsBtn");
    if (db) {
      db.style.borderColor = "#2a7a50";
      db.style.background = "#e8f4f0";
    }
  }
  const ta = document.getElementById("notesInput");
  if (ta) {
    ta.value = d.notes || "";
    if (typeof syncNotesDisplay === "function") syncNotesDisplay();
  }
  _syncSelectionUIToState();
  return true;
}

function clearRoomDraft(roomName) {
  if (!roomName) return;
  delete roomDrafts[roomName];
}

// ── COPY CONDITIONS ─────────────────────────────────────────────────────────
// One-shot clipboard for fixtures + switches + controls. Distinct from drafts
// (which preserve work per-room) and templates (named, persistent). The flow:
// snapshot current room's conditions → tap any minimap pin → that room loads
// with its own identity (name, type, height, dimensions) but the clipboard's
// fixtures/switches/controls overlay whatever it had. Clipboard is session-only
// and clears on first paste.
let _conditionsClipboard = null;

// Single click handler — dispatches based on current state. Three modes:
//   1. No clipboard → copy current room's conditions
//   2. Clipboard set, on source room → cancel the copy (gives user a way out
//      without having to navigate away first)
//   3. Clipboard set, on a different room → paste onto current room
function toggleCopyConditions() {
  // Mode 3: paste
  if (
    _conditionsClipboard &&
    (state.room || "") !== (_conditionsClipboard.sourceRoom || "")
  ) {
    applyConditionsClipboardIfPending();
    return;
  }
  // Mode 2: cancel
  if (_conditionsClipboard) {
    cancelCopyConditions();
    return;
  }
  // Mode 1: copy
  if (
    !state.fixtures.length &&
    !state.switchType.length &&
    !state.existingCtrls.length &&
    !state.ctrls.length
  ) {
    toast("Add fixtures, switches, or controls first");
    return;
  }
  _conditionsClipboard = {
    fixtures: state.fixtures.map((f) => ({ ...f })),
    switchType: state.switchType.map((s) => ({ ...s })),
    existingCtrls: state.existingCtrls.map((x) => ({ ...x })),
    ctrls: state.ctrls.map((x) => ({ ...x })),
    swRec: (state.swRec || []).map((x) => ({ ...x })),
    fixRec: (state.fixRec || []).map((x) => ({ ...x })),
    sourceRoom: state.room || "",
  };
  updateCopyConditionsBtn();
  toast("📋 Conditions copied — navigate to a room and tap Paste Conditions");
}

function cancelCopyConditions() {
  _conditionsClipboard = null;
  updateCopyConditionsBtn();
}

function updateCopyConditionsBtn() {
  const btn = document.getElementById("copyConditionsBtn");
  if (!btn) return;
  if (!_conditionsClipboard) {
    btn.textContent = "📋 Copy Conditions";
    btn.style.background = "transparent";
    btn.style.color = "#5a8ab0";
    btn.style.borderColor = "#c8d4e3";
  } else if ((state.room || "") === (_conditionsClipboard.sourceRoom || "")) {
    btn.textContent = "✕ Cancel copy";
    btn.style.background = "#7a3abd";
    btn.style.color = "#fff";
    btn.style.borderColor = "#7a3abd";
  } else {
    btn.textContent = "📋 Paste Conditions";
    btn.style.background = "#1a6a35";
    btn.style.color = "#fff";
    btn.style.borderColor = "#1a6a35";
  }
}

// Apply pending clipboard onto current state, replacing fixtures/switches/ctrls.
// Called at the end of loadRoomFromPin (after draft restore or log import) so
// the paste always wins. One-shot — clears clipboard after applying.
function applyConditionsClipboardIfPending() {
  if (!_conditionsClipboard) return false;
  const c = _conditionsClipboard;
  state.fixtures = c.fixtures.map((f) => ({ ...f }));
  state.switchType = c.switchType.map((s) => ({ ...s }));
  state.existingCtrls = c.existingCtrls.map((x) => ({ ...x }));
  state.ctrls = c.ctrls.map((x) => ({ ...x }));
  state.swRec = (c.swRec || []).map((x) => ({ ...x }));
  state.fixRec = (c.fixRec || []).map((x) => ({ ...x }));
  _syncSelectionUIToState();
  const sourceRoom = c.sourceRoom;
  _conditionsClipboard = null;
  updateCopyConditionsBtn();
  toast(`📋 Pasted conditions from "${sourceRoom || "previous room"}"`);
  return true;
}

// Re-render all panels + sync .active button states after a bulk state replace
// (paste, draft restore, etc.). Mirrors the post-state-load work in applyRoomTemplate.
function _syncSelectionUIToState() {
  if (typeof renderChips === "function") renderChips();
  if (typeof renderSwitchQtyPanel === "function") renderSwitchQtyPanel();
  if (typeof renderExistingCtrlQtyPanel === "function")
    renderExistingCtrlQtyPanel();
  if (typeof renderCtrlQtyPanel === "function") renderCtrlQtyPanel();
  if (typeof renderSwRecQtyPanel === "function") renderSwRecQtyPanel();
  if (typeof renderFixRecQtyPanel === "function") renderFixRecQtyPanel();
  // Toggle .active highlight on the source buttons so they reflect the new selection
  document.querySelectorAll('.btn[data-g="switch"]').forEach((b) => {
    if (b.dataset.v)
      b.classList.toggle(
        "active",
        state.switchType.some((sw) => sw.name === b.dataset.v),
      );
  });
  document.querySelectorAll("#existingCtrlBtns .btn").forEach((b) => {
    if (b.dataset.v)
      b.classList.toggle(
        "active",
        state.existingCtrls.some((ec) => (ec.name || ec) === b.dataset.v),
      );
  });
  document.querySelectorAll("#ctrlBtns .btn").forEach((b) => {
    if (b.dataset.v)
      b.classList.toggle(
        "active",
        state.ctrls.some((c) => c.name === b.dataset.v),
      );
  });
  document.querySelectorAll('#swRecBtns .btn[data-g="swrec"]').forEach((b) => {
    if (b.dataset.v)
      b.classList.toggle(
        "active",
        (state.swRec || []).some((s) => s.name === b.dataset.v),
      );
  });
  document
    .querySelectorAll('#fixRecBtns .btn[data-g="fixrec"]')
    .forEach((b) => {
      if (b.dataset.v)
        b.classList.toggle(
          "active",
          (state.fixRec || []).some((s) => s.name === b.dataset.v),
        );
    });
  if (typeof updateSummary === "function") updateSummary();
}

// Loads a pin's room into the audit page (state, UI, prior log data if any).
// Extracted from fpGoToRoom so both the floor-plan callout and the audit-page
// minimap can call it.
function loadRoomFromPin(pin) {
  if (!pin?.roomName) return;

  // Save current room's in-progress work BEFORE wiping it — so navigating back
  // to this room later restores everything the user had typed.
  snapshotCurrentRoomDraft();

  // Look for previously logged entries for this room
  const prevEntries = logEntries.filter((e) => e.space === pin.roomName);
  const first = prevEntries[0];

  // Start fresh
  clearFixtures();

  // Pre-fill room name
  state.room = pin.roomName;
  document.getElementById("roomInput").value = pin.roomName;
  updateRoomDisplay();

  // If we have a saved in-progress draft for this room, restore it and stop —
  // draft beats both pin metadata and prior log entries (it's the user's most
  // recent uncommitted work).
  if (restoreRoomDraft(pin.roomName)) {
    showForm();
    toast('📋 Restored "' + pin.roomName + '" — pending changes');
    return;
  }

  // Room metadata — pin wins over log entry. The pin is the live descriptor
  // (user updates it via the floor-plan callout); log entries are frozen at
  // commit time. If the user edits a pin's room type after logging, we want
  // that change to show on next load — log values are only a fallback for
  // legacy pins missing this metadata.
  const roomType = pin.roomType || first?.roomType;
  if (roomType) {
    state.roomtype = roomType;
    const rtOpts = {
      "Class rooms": "Class",
      Office: "Office",
      Restroom: "Restroom",
      Closet: "Closet",
      Storage: "Storage",
      Hallway: "Hall",
      Stairwell: "Stair",
      "Slop Sink": "Slop Sink",
      Gym: "Gym",
      Mechanical: "Mech",
      Cafeteria: "Cafe",
      Library: "Library",
    };
    const lbl = document.getElementById("roomTypeBtnLabel");
    if (lbl) {
      lbl.textContent = rtOpts[roomType] || roomType;
      lbl.style.color = "#1a2a3a";
    }
    const btn = document.getElementById("roomTypeBtn");
    if (btn) {
      btn.style.borderColor = "#7a3abd";
      btn.style.background = "#f3eeff";
    }
    document
      .querySelectorAll(".rt-opt")
      .forEach((b) => b.classList.toggle("active", b.dataset.v === roomType));
  }

  const height = pin.height || first?.height;
  if (height) {
    state.height = height;
    updateHeightBtn(String(height));
  }

  const ceilingType = pin.ceilingType || first?.ceilingType;
  if (ceilingType) {
    state.ceilingType = ceilingType;
    updateCeilingTypeBtn(ceilingType);
  }

  // Dimensions — from log
  if (first?.dimensions) {
    const parts = first.dimensions.match(/([\d.]+)'\s*[x×]\s*([\d.]+)'/i);
    if (parts) {
      state.dimensions = { len: parts[1], wid: parts[2] };
      const label = first.dimensions;
      const dl = document.getElementById("dimensionsBtnLabel");
      if (dl) {
        dl.textContent = label;
        dl.style.color = "#1a3a5c";
      }
      const db = document.getElementById("dimensionsBtn");
      if (db) {
        db.style.borderColor = "#2a7a50";
        db.style.background = "#e8f4f0";
      }
    }
  }

  // Restore all conditions using importRoomConditions (handles all parsing correctly)
  if (prevEntries.length) {
    const firstIdx = logEntries.findIndex((e) => e.space === pin.roomName);
    if (firstIdx >= 0) {
      importRoomConditions(firstIdx, pin.roomName);
      showForm(); // navigate to data entry
      return;
    }
  }

  updateSummary();
  showForm(); // navigate to data entry
  toast(
    '📋 Loaded "' +
      pin.roomName +
      '"' +
      (prevEntries.length
        ? " — tap Log Entry to update"
        : " — enter fixtures and log"),
  );
}

// ── AUDIT-PAGE MINIMAP ───────────────────────────────────────────────────────
// Static thumbnail of the current floor's plan with pins/polygons overlaid.
// Tap any pin (or a polygon linked to a pin) to load that room's data.
// Long-press an empty area to drop a pin auto-bound to the current room context.
// Pan with 1-finger drag · pinch with 2 fingers · scroll wheel zooms on PC.

// ── Minimap pan/zoom state (independent from the floor plan tab's fpScale/fpOffset) ──
let mmScale = 1;
let mmOffsetX = 0;
let mmOffsetY = 0;
const MM_MIN_ZOOM = 1; // never below "fit" (no negative space)
const MM_MAX_ZOOM = 6; // 6× is plenty for finger placement on iPad
let _mmGestureLocked = false; // suppress click events emitted after a drag/pinch/long-press

function applyMinimapTransform() {
  const inner = document.getElementById("auditMinimapInner");
  if (!inner) return;
  inner.style.transform = `translate(${mmOffsetX}px,${mmOffsetY}px) scale(${mmScale})`;
}

function mmFit() {
  mmScale = 1;
  mmOffsetX = 0;
  mmOffsetY = 0;
  applyMinimapTransform();
}

// Zoom anchored at the container's center.
function mmZoomBy(factor) {
  const wrap = document.getElementById("auditMinimap");
  if (!wrap) return;
  const r = wrap.getBoundingClientRect();
  mmZoomAt(factor, r.width / 2, r.height / 2);
}

// Zoom anchored at a screen point (relative to the container) — keeps the pixel
// under the cursor/finger fixed during zoom (standard map-zoom behaviour).
function mmZoomAt(factor, sx, sy) {
  const newScale = Math.max(
    MM_MIN_ZOOM,
    Math.min(MM_MAX_ZOOM, mmScale * factor),
  );
  if (newScale === mmScale) return;
  // Solve for new offset so the point (sx, sy) stays at the same screen location.
  // sx = oldOffsetX + planX * oldScale  →  planX = (sx - oldOffsetX) / oldScale
  // sx = newOffsetX + planX * newScale  →  newOffsetX = sx - planX * newScale
  const planX = (sx - mmOffsetX) / mmScale;
  const planY = (sy - mmOffsetY) / mmScale;
  mmScale = newScale;
  mmOffsetX = sx - planX * newScale;
  mmOffsetY = sy - planY * newScale;
  clampMinimapOffsets();
  applyMinimapTransform();
}

// Keep the inner wrapper from being panned outside the visible container.
function clampMinimapOffsets() {
  const wrap = document.getElementById("auditMinimap");
  if (!wrap) return;
  const r = wrap.getBoundingClientRect();
  const minX = r.width - r.width * mmScale;
  const minY = r.height - r.height * mmScale;
  if (mmOffsetX > 0) mmOffsetX = 0;
  if (mmOffsetY > 0) mmOffsetY = 0;
  if (mmOffsetX < minX) mmOffsetX = minX;
  if (mmOffsetY < minY) mmOffsetY = minY;
}

// Screen coords → plan-pixel coords. Accounts for the wrapper transform AND
// the existing fit-to-crop math used inside renderAuditMinimap (img is sized
// to (W*fit × H*fit) and offset so the crop region fills the container).
function mmScreenToPlanPixel(clientX, clientY) {
  const wrap = document.getElementById("auditMinimap");
  if (!wrap) return null;
  const r = wrap.getBoundingClientRect();
  // Step 1: undo the wrapper transform
  const innerX = (clientX - r.left - mmOffsetX) / mmScale;
  const innerY = (clientY - r.top - mmOffsetY) / mmScale;
  // Step 2: map inner coords back to plan pixels using the active plan's fit + crop
  // (mirrors the math in renderAuditMinimap)
  let plan = null;
  if (activeFloor) {
    for (const id in floorPlans) {
      if (floorPlans[id]?.floorId === activeFloor.id) {
        plan = floorPlans[id];
        break;
      }
    }
  }
  if (!plan || !plan._mmDims) return null;
  const W = plan._mmDims.w,
    H = plan._mmDims.h;
  const c = plan.crop;
  const cx = c ? c.xPct * W : 0;
  const cy = c ? c.yPct * H : 0;
  const cw = c ? c.wPct * W : W;
  const ch = c ? c.hPct * H : H;
  const CW = r.width,
    CH = r.height;
  const fit = Math.min(CW / cw, CH / ch);
  const imgLeft = (CW - cw * fit) / 2 - cx * fit;
  const imgTop = (CH - ch * fit) / 2 - cy * fit;
  const planX = (innerX - imgLeft) / fit;
  const planY = (innerY - imgTop) / fit;
  if (planX < 0 || planY < 0 || planX > W || planY > H) return null;
  return { planX, planY, planW: W, planH: H, plan };
}

// Drop a pin from the minimap. Auto-binds metadata from the current room context
// so the user doesn't have to re-enter room name/type/height/ceiling — they're
// already typing those in the data-entry header.
function dropPinFromMinimap(planX, planY) {
  // Require a room name. Pins without a name are unreachable in loadRoomFromPin
  // (it early-returns on !pin.roomName), so dropping one would be a dead pin.
  if (!state.room) {
    toast("Enter the room name before dropping a pin");
    return;
  }
  const wrap = document.getElementById("auditMinimap");
  const r = wrap.getBoundingClientRect();
  let plan = null;
  if (activeFloor) {
    for (const id in floorPlans) {
      if (floorPlans[id]?.floorId === activeFloor.id) {
        plan = floorPlans[id];
        break;
      }
    }
  }
  if (!plan) {
    toast("No floor plan linked to this floor");
    return;
  }
  const W = plan._mmDims?.w || planX,
    H = plan._mmDims?.h || planY;
  const xPct = planX / W,
    yPct = planY / H;

  // De-dupe: if a pin already exists very near this point AND already names the
  // current room, treat the long-press as a no-op (probably a mis-fire).
  if (state.room) {
    const existing = (plan.pins || []).find(
      (p) =>
        p.roomName === state.room &&
        Math.hypot((p.xPct - xPct) * W, (p.yPct - yPct) * H) < 30,
    );
    if (existing) {
      toast("Pin for this room already here");
      return;
    }
  }

  const pinNum = fpPinCounter++;
  const pin = {
    id: "pin_" + pinNum,
    xPct,
    yPct,
    pinNum,
    roomName: state.room || "",
    roomType: state.roomtype || "",
    height: state.height || "",
    ceilingType: state.ceilingType || "",
  };
  plan.pins = plan.pins || [];
  plan.pins.push(pin);

  // Auto-link to any polygon this point falls inside (same logic the floor plan
  // tab uses when an outline is drawn around an existing pin).
  if (typeof _pointInPolygon === "function") {
    const polys = plan.markup?.polygons || [];
    const enclosing = polys.find(
      (p) =>
        !p.pinId &&
        p.points &&
        p.points.length >= 3 &&
        _pointInPolygon(xPct, yPct, p.points),
    );
    if (enclosing) {
      enclosing.pinId = pin.id;
      toast(
        `📍 Pin ${pinNum} dropped${
          pin.roomName ? ' for "' + pin.roomName + '"' : ""
        } — outline linked`,
      );
    } else {
      toast(
        `📍 Pin ${pinNum} dropped${
          pin.roomName ? ' for "' + pin.roomName + '"' : ""
        }`,
      );
    }
  }

  if (typeof scheduleSave === "function") scheduleSave();
  renderAuditMinimap();
}

// One-time wiring of the minimap gesture handlers. Idempotent — calling again
// won't double-bind because we tag the container with a data attribute.
function initMinimapEvents() {
  const wrap = document.getElementById("auditMinimap");
  if (!wrap || wrap.dataset.gesturesBound === "1") return;
  wrap.dataset.gesturesBound = "1";

  let pressStart = null; // { x, y, t }
  let dragMode = null; // null | 'pan' | 'pinch'
  let holdTimer = null;
  let holdRing = null;
  let pinch = null; // { d0, mx0, my0, scale0, ox0, oy0 }
  const HOLD_MS = 650;
  const MOVE_CANCEL_PX = 8;

  const cancelHold = () => {
    if (holdTimer) {
      clearTimeout(holdTimer);
      holdTimer = null;
    }
    if (holdRing) {
      holdRing.remove();
      holdRing = null;
    }
  };

  const startHoldFeedback = (clientX, clientY) => {
    cancelHold();
    holdRing = document.createElement("div");
    holdRing.className = "pin-hold-ring";
    holdRing.style.left = clientX + "px";
    holdRing.style.top = clientY + "px";
    document.body.appendChild(holdRing);
  };

  // ── Touch ──────────────────────────────────────────────────────────────
  wrap.addEventListener(
    "touchstart",
    (e) => {
      if (e.touches.length === 2) {
        // Pinch — anchored to midpoint, scale relative to baseline
        cancelHold();
        const a = e.touches[0],
          b = e.touches[1];
        const r = wrap.getBoundingClientRect();
        const dx = b.clientX - a.clientX,
          dy = b.clientY - a.clientY;
        pinch = {
          d0: Math.hypot(dx, dy) || 1,
          mx0: (a.clientX + b.clientX) / 2 - r.left,
          my0: (a.clientY + b.clientY) / 2 - r.top,
          scale0: mmScale,
          ox0: mmOffsetX,
          oy0: mmOffsetY,
        };
        dragMode = "pinch";
        _mmGestureLocked = true;
        e.preventDefault();
        return;
      }
      if (e.touches.length === 1) {
        const t = e.touches[0];
        pressStart = { x: t.clientX, y: t.clientY, t: Date.now() };
        dragMode = null;
        _mmGestureLocked = false;
        // Skip long-press when the touch landed on an existing pin/polygon (those
        // should still tap-to-navigate without a delay).
        const onInteractive =
          e.target && e.target.closest && e.target.closest("circle,polygon");
        if (!onInteractive) {
          startHoldFeedback(t.clientX, t.clientY);
          holdTimer = setTimeout(() => {
            holdTimer = null;
            if (holdRing) {
              holdRing.remove();
              holdRing = null;
            }
            const hit = mmScreenToPlanPixel(t.clientX, t.clientY);
            if (hit) {
              _mmGestureLocked = true;
              dropPinFromMinimap(hit.planX, hit.planY);
            }
            pressStart = null;
          }, HOLD_MS);
        }
      }
    },
    { passive: false },
  );

  wrap.addEventListener(
    "touchmove",
    (e) => {
      if (dragMode === "pinch" && e.touches.length >= 2 && pinch) {
        const a = e.touches[0],
          b = e.touches[1];
        const r = wrap.getBoundingClientRect();
        const dx = b.clientX - a.clientX,
          dy = b.clientY - a.clientY;
        const d = Math.hypot(dx, dy) || 1;
        const mx = (a.clientX + b.clientX) / 2 - r.left;
        const my = (a.clientY + b.clientY) / 2 - r.top;
        const newScale = Math.max(
          MM_MIN_ZOOM,
          Math.min(MM_MAX_ZOOM, pinch.scale0 * (d / pinch.d0)),
        );
        // Anchor on initial midpoint, then translate by midpoint drift
        const planX = (pinch.mx0 - pinch.ox0) / pinch.scale0;
        const planY = (pinch.my0 - pinch.oy0) / pinch.scale0;
        mmScale = newScale;
        mmOffsetX = mx - planX * newScale;
        mmOffsetY = my - planY * newScale;
        clampMinimapOffsets();
        applyMinimapTransform();
        e.preventDefault();
        return;
      }
      if (pressStart && e.touches.length === 1) {
        const t = e.touches[0];
        const dx = t.clientX - pressStart.x,
          dy = t.clientY - pressStart.y;
        if (
          !dragMode &&
          (Math.abs(dx) > MOVE_CANCEL_PX || Math.abs(dy) > MOVE_CANCEL_PX)
        ) {
          cancelHold();
          dragMode = "pan";
          _mmGestureLocked = true;
          pressStart = {
            x: t.clientX - mmOffsetX,
            y: t.clientY - mmOffsetY,
            t: pressStart.t,
          };
        }
        if (dragMode === "pan") {
          mmOffsetX = t.clientX - pressStart.x;
          mmOffsetY = t.clientY - pressStart.y;
          clampMinimapOffsets();
          applyMinimapTransform();
          e.preventDefault();
        }
      }
    },
    { passive: false },
  );

  const endTouch = () => {
    cancelHold();
    pressStart = null;
    pinch = null;
    dragMode = null;
    // Leave _mmGestureLocked set — the capture-phase click handler will read
    // and reset it on the next click.
  };
  wrap.addEventListener("touchend", endTouch);
  wrap.addEventListener("touchcancel", endTouch);

  // ── Mouse (desktop) ────────────────────────────────────────────────────
  wrap.addEventListener("mousedown", (e) => {
    if (e.button !== 0) return;
    pressStart = { x: e.clientX, y: e.clientY, t: Date.now() };
    dragMode = null;
    _mmGestureLocked = false;
    const onInteractive =
      e.target && e.target.closest && e.target.closest("circle,polygon");
    if (!onInteractive) {
      startHoldFeedback(e.clientX, e.clientY);
      holdTimer = setTimeout(() => {
        holdTimer = null;
        if (holdRing) {
          holdRing.remove();
          holdRing = null;
        }
        const hit = mmScreenToPlanPixel(e.clientX, e.clientY);
        if (hit) {
          _mmGestureLocked = true;
          dropPinFromMinimap(hit.planX, hit.planY);
        }
        pressStart = null;
      }, HOLD_MS);
    }
  });

  window.addEventListener("mousemove", (e) => {
    if (!pressStart || dragMode === "pinch") return;
    const dx = e.clientX - pressStart.x,
      dy = e.clientY - pressStart.y;
    if (
      !dragMode &&
      (Math.abs(dx) > MOVE_CANCEL_PX || Math.abs(dy) > MOVE_CANCEL_PX)
    ) {
      cancelHold();
      dragMode = "pan";
      _mmGestureLocked = true;
      pressStart = {
        x: e.clientX - mmOffsetX,
        y: e.clientY - mmOffsetY,
        t: pressStart.t,
      };
    }
    if (dragMode === "pan") {
      mmOffsetX = e.clientX - pressStart.x;
      mmOffsetY = e.clientY - pressStart.y;
      clampMinimapOffsets();
      applyMinimapTransform();
    }
  });

  window.addEventListener("mouseup", () => {
    cancelHold();
    pressStart = null;
    dragMode = null;
  });

  // Wheel zoom — anchored at cursor position
  wrap.addEventListener(
    "wheel",
    (e) => {
      e.preventDefault();
      const r = wrap.getBoundingClientRect();
      const sx = e.clientX - r.left;
      const sy = e.clientY - r.top;
      const factor = e.deltaY < 0 ? 1.15 : 1 / 1.15;
      mmZoomAt(factor, sx, sy);
    },
    { passive: false },
  );

  // Click suppression — if a gesture happened, swallow the click before it
  // reaches SVG pin/polygon handlers (otherwise a pan-drag would also navigate).
  wrap.addEventListener(
    "click",
    (e) => {
      if (_mmGestureLocked) {
        e.stopPropagation();
        e.stopImmediatePropagation();
        e.preventDefault();
        _mmGestureLocked = false;
      }
    },
    true,
  );
}

function renderAuditMinimap() {
  const section = document.getElementById("auditMinimapSection");
  const svg = document.getElementById("auditMinimapSvg");
  const img = document.getElementById("auditMinimapBg");
  if (!section || !svg || !img) return;

  // Find a floor plan for the current audit floor
  let plan = null;
  if (activeFloor) {
    for (const id in floorPlans) {
      if (floorPlans[id]?.floorId === activeFloor.id) {
        plan = floorPlans[id];
        break;
      }
    }
  }
  if (!plan || !plan.imageData) {
    section.style.display = "none";
    return;
  }
  section.style.display = "";

  if (img.getAttribute("src") !== plan.imageData)
    img.setAttribute("src", plan.imageData);

  // Need the rendered plan's natural pixel dimensions for the SVG viewBox.
  // Cache on the plan so we don't decode the dataURL on every render.
  if (!plan._mmDims) {
    const tmp = new Image();
    tmp.onload = () => {
      plan._mmDims = { w: tmp.naturalWidth, h: tmp.naturalHeight };
      renderAuditMinimap();
    };
    tmp.src = plan.imageData;
    return;
  }
  const W = plan._mmDims.w,
    H = plan._mmDims.h;

  // Crop rect in plan-pixel coords (full plan if no crop set)
  const c = plan.crop;
  const cx = c ? c.xPct * W : 0;
  const cy = c ? c.yPct * H : 0;
  const cw = c ? c.wPct * W : W;
  const ch = c ? c.hPct * H : H;

  svg.setAttribute("viewBox", `${cx} ${cy} ${cw} ${ch}`);

  // Position the background image to show only the crop area, fitted to the
  // container via the same aspect-fit math the SVG uses (preserveAspectRatio).
  const containerRect = document
    .getElementById("auditMinimap")
    .getBoundingClientRect();
  const CW = containerRect.width || 1,
    CH = containerRect.height || 1;
  const fit = Math.min(CW / cw, CH / ch);
  img.style.position = "absolute";
  img.style.width = W * fit + "px";
  img.style.height = H * fit + "px";
  img.style.left = (CW - cw * fit) / 2 - cx * fit + "px";
  img.style.top = (CH - ch * fit) / 2 - cy * fit + "px";
  img.style.objectFit = "fill";

  // Pins/text scale based on crop dimensions (so they stay screen-pixel sized
  // whether you're viewing the full plan or a tight crop)
  const scale = fit; // SVG uses the same fit since viewBox = crop rect
  const PIN_R = 9.35 / scale; // 11 * 0.85 — pins shrunk 15% per user request
  const HIT_R = 28 / scale;
  const FONT_SZ = 12 / scale;
  const STROKE = 2 / scale;

  const NS = "http://www.w3.org/2000/svg";
  svg.innerHTML = "";

  // Crop-area test (xPct/yPct against crop bounds)
  const inCrop = (xPct, yPct) => {
    if (!c) return true;
    return (
      xPct >= c.xPct &&
      xPct <= c.xPct + c.wPct &&
      yPct >= c.yPct &&
      yPct <= c.yPct + c.hPct
    );
  };

  // 1) Polygons first (under pins). Color by completeness state (mirrors floor
  // plan tab: grey/red/yellow/green) so the minimap doubles as a live progress
  // overview. `isCurrent` adds a thicker outline so the room being audited
  // stays visually anchored.
  // Map state → fill + stroke. Fill is more transparent than the floor plan
  // tab's 22% because minimap is smaller and stacks; keep around 25%.
  const POLY_COLORS = {
    unlinked: { fill: "rgba(148,163,184,0.18)", stroke: "#64748b" },
    empty: { fill: "rgba(220, 38, 38,0.22)", stroke: "#991b1b" },
    partial: { fill: "rgba(245,158, 11,0.24)", stroke: "#b45309" },
    complete: { fill: "rgba( 22,163, 74,0.22)", stroke: "#0e7a35" },
  };
  const polygons = plan.markup?.polygons || [];
  polygons.forEach((p) => {
    if (!p.points || p.points.length < 3) return;
    if (c && !p.points.some((pt) => inCrop(pt.xPct, pt.yPct))) return;
    const linkedPin = (plan.pins || []).find((pin) => pin.id === p.pinId);
    const isCurrent =
      linkedPin && linkedPin.roomName && linkedPin.roomName === state.room;
    const mismatched = _polyHasMismatchedPin(p);
    const stateClass = _polyColorClass(p);
    const palette = POLY_COLORS[stateClass] || POLY_COLORS.unlinked;
    const pts = p.points.map((pt) => `${pt.xPct * W},${pt.yPct * H}`).join(" ");
    const el = document.createElementNS(NS, "polygon");
    el.setAttribute("points", pts);
    el.setAttribute("fill", palette.fill);
    el.setAttribute("stroke", mismatched ? "#ea580c" : palette.stroke);
    el.setAttribute(
      "stroke-width",
      mismatched ? STROKE * 1.7 : isCurrent ? STROKE * 1.5 : STROKE * 0.8,
    );
    if (mismatched)
      el.setAttribute("stroke-dasharray", `${STROKE * 6} ${STROKE * 3}`);
    el.style.cursor = linkedPin?.roomName ? "pointer" : "default";
    if (linkedPin?.roomName)
      el.addEventListener("click", () => loadRoomFromPin(linkedPin));
    svg.appendChild(el);
  });

  // 2) Pins. Orphan unnamed pins skipped (mirrors closeFloorPlan sweep behavior).
  // Out-of-crop pins also skipped (still reachable from main floor plan view).
  (plan.pins || []).forEach((pin) => {
    if (!pin.roomName) return;
    if (!inCrop(pin.xPct, pin.yPct)) return;
    const isCurrent = pin.roomName === state.room;
    const hasLogged = logEntries.some((e) => e.space === pin.roomName);
    const color = hasLogged ? "#ff6eb4" : "#00e64d";

    // Scale this pin down if its linked polygon is small — keeps adjacent
    // pins in tight rooms from visually overlapping each other.
    const pinMul = fpPinSizeMultiplier(pin);
    const r0 = PIN_R * pinMul;
    const rh = HIT_R * pinMul;
    const fs = FONT_SZ * pinMul;

    // Larger transparent hit target for finger taps at small minimap scale
    const hit = document.createElementNS(NS, "circle");
    hit.setAttribute("cx", pin.xPct * W);
    hit.setAttribute("cy", pin.yPct * H);
    hit.setAttribute("r", rh);
    hit.setAttribute("fill", "transparent");
    hit.style.cursor = "pointer";
    hit.addEventListener("click", () => loadRoomFromPin(pin));
    svg.appendChild(hit);

    const c = document.createElementNS(NS, "circle");
    c.setAttribute("cx", pin.xPct * W);
    c.setAttribute("cy", pin.yPct * H);
    c.setAttribute("r", r0);
    c.setAttribute("fill", color);
    c.setAttribute("stroke", "#fff");
    c.setAttribute("stroke-width", r0 * 0.2);
    c.style.pointerEvents = "none";
    svg.appendChild(c);

    const lbl = document.createElementNS(NS, "text");
    lbl.setAttribute("x", pin.xPct * W);
    lbl.setAttribute("y", pin.yPct * H);
    lbl.setAttribute("text-anchor", "middle");
    lbl.setAttribute("dominant-baseline", "central");
    lbl.setAttribute("fill", "#003010");
    lbl.setAttribute("font-size", fs);
    lbl.setAttribute("font-weight", "800");
    lbl.setAttribute(
      "font-family",
      "-apple-system,BlinkMacSystemFont,sans-serif",
    );
    lbl.style.pointerEvents = "none";
    lbl.textContent = pin.pinNum;
    svg.appendChild(lbl);
  });
}

// Stores conditions waiting to be applied to a new pin placement
let fpPendingCopy = null;
let fpPendingSetPin = null;

function setLogRoomPin(roomName) {
  // Find the log entry for this room to get its conditions
  const entryIdx = logEntries.findIndex((e) => e.space === roomName);
  if (entryIdx === -1) return;

  // Open floor plan in pin-drop mode, pre-load room conditions
  openFloorPlan();

  // After a short delay (let plan render), switch to pin mode with a pending room
  setTimeout(() => {
    setMode("pin");
    document.getElementById("fpModeHint").textContent =
      '📍 Tap to place pin for "' + roomName + '"';
    document.getElementById("fpModeHint").style.color = "#e07020";
    // Store the room info so when pin is dropped it gets assigned
    fpPendingSetPin = { roomName, entryIdx };
    toast('Tap the floor plan to place a pin for "' + roomName + '"');
  }, 200);
}

function fpCopyRoom() {
  if (!fpSelectedPin?.roomName) return;
  const entryIdx = logEntries.findIndex(
    (e) => e.space === fpSelectedPin.roomName,
  );
  if (entryIdx === -1) {
    toast("No logged data found for this room");
    return;
  }

  fpPendingCopy = { sourceRoom: fpSelectedPin.roomName, entryIdx };
  closeCallout();

  // Switch to pin-drop mode and show guidance
  setMode("pin");
  document.getElementById(
    "fpModeHint",
  ).textContent = `📋 Tap to place copied pin for "${fpSelectedPin.roomName}"`;
  document.getElementById("fpModeHint").style.color = "#a060e0";
  toast(`📋 Tap anywhere to place the new pin`);
}

function fpApplyCopyToPin(pin) {
  if (!fpPendingCopy) return;

  // Show a name-entry prompt in the callout immediately after pin drop
  fpSelectedPin = pin;
  const callout = document.getElementById("fpCallout");
  document.getElementById(
    "fpCalloutTitle",
  ).textContent = `📋 Copy of "${fpPendingCopy.sourceRoom}"`;
  document.getElementById("fpCalloutBody").innerHTML = `
    <div style="font-size:11px;color:#7a3abd;margin-bottom:6px;font-weight:600">Enter a new room name to log this pin:</div>
    <input id="fpCopyRoomInput" placeholder="New room name…" autofocus
      style="width:100%;padding:6px 8px;border:1.5px solid #7a3abd;border-radius:6px;font-size:13px;font-family:inherit;outline:none"
      onkeydown="if(event.key==='Enter')fpConfirmCopy()">
  `;

  document.getElementById("fpCalloutAssign").style.display = "none";
  document.getElementById("fpCalloutGoTo").style.display = "none";
  document.getElementById("fpCalloutCopy").style.display = "none";

  // Replace Delete button temporarily with Confirm
  const deleteBtn = callout.querySelector('button[onclick="fpDeletePin()"]');
  if (deleteBtn) deleteBtn.style.display = "none";

  // Add confirm button dynamically
  let confirmBtn = document.getElementById("fpCopyConfirmBtn");
  if (!confirmBtn) {
    confirmBtn = document.createElement("button");
    confirmBtn.id = "fpCopyConfirmBtn";
    confirmBtn.style.cssText =
      "flex:1;padding:6px 12px;background:#7a3abd;color:#fff;border:none;border-radius:6px;font-size:11px;font-weight:600;cursor:pointer;font-family:inherit";
    confirmBtn.textContent = "Log New Room ›";
    confirmBtn.onclick = fpConfirmCopy;
    callout
      .querySelector('div[style*="margin-top:10px"]')
      .appendChild(confirmBtn);
  } else {
    confirmBtn.style.display = "";
  }

  // Position callout — measure first, clamp to viewport (same pattern as showCallout)
  const ov = document
    .getElementById("floorPlanOverlay")
    .getBoundingClientRect();
  callout.style.visibility = "hidden";
  callout.style.display = "block";
  const cw = callout.offsetWidth || 220;
  const ch = callout.offsetHeight || 200;
  let cx = pin.xPct * ov.width + 12;
  let cy = pin.yPct * ov.height - ch - 12;
  if (cx + cw > ov.width - 8) cx = pin.xPct * ov.width - cw - 12;
  if (cy < 8) cy = pin.yPct * ov.height + 24;
  cx = Math.max(8, Math.min(cx, ov.width - cw - 8));
  cy = Math.max(8, Math.min(cy, ov.height - ch - 8));
  callout.style.left = cx + "px";
  callout.style.top = cy + "px";
  callout.style.visibility = "";

  setTimeout(() => document.getElementById("fpCopyRoomInput")?.focus(), 80);
}

function fpConfirmCopy() {
  const input = document.getElementById("fpCopyRoomInput");
  let newName = input?.value.trim();
  if (!newName) {
    toast("Enter a room name");
    return;
  }
  if (newName === fpPendingCopy.sourceRoom) {
    toast("Must be a different room name");
    return;
  }
  if (!fpSelectedPin) return;

  // Check if this room name already exists in the log
  const alreadyLogged = logEntries.some((e) => e.space === newName);
  if (alreadyLogged && !fpConfirmCopy._dupWarned) {
    // Flash warning on the input, update button text, set flag — allow on next click
    fpConfirmCopy._dupWarned = true;
    if (input) {
      input.style.borderColor = "#dc2626";
      input.style.background = "#fee2e2";
    }
    const confirmBtn = document.getElementById("fpCopyConfirmBtn");
    if (confirmBtn) confirmBtn.textContent = "Duplicate — Log Anyway ›";
    toast(
      '⚠ Duplicate Room — click Log Again to save as "' + newName + ' (2)"',
    );
    return;
  }
  // If dup warning was shown, rename with suffix
  if (fpConfirmCopy._dupWarned && alreadyLogged) {
    // Find next available suffix
    let suffix = 2;
    while (logEntries.some((e) => e.space === newName + " (" + suffix + ")"))
      suffix++;
    newName = newName + " (" + suffix + ")";
    if (input) input.value = newName;
  }
  fpConfirmCopy._dupWarned = false;

  // Assign room name to pin
  fpSelectedPin.roomName = newName;
  renderPins();
  renderPinList();

  // Save refs before closeCallout nulls fpSelectedPin
  const entryIdx = fpPendingCopy.entryIdx;
  fpPendingCopy = null;

  // Reset confirm button
  const confirmBtn = document.getElementById("fpCopyConfirmBtn");
  if (confirmBtn) confirmBtn.style.display = "none";
  const deleteBtn = document
    .getElementById("fpCallout")
    ?.querySelector('button[onclick="fpDeletePin()"]');
  if (deleteBtn) deleteBtn.style.display = "";

  closeCallout(); // sets fpSelectedPin = null — must be after we saved refs
  closeFloorPlan();

  // Load conditions with new name override
  importRoomConditions(entryIdx, newName);

  const grid = document.getElementById("fixtureBtns");
  if (grid) {
    grid.innerHTML =
      '<span style="color:#8a9ab0;font-size:11px;font-style:italic;padding:4px 0">Fixtures loaded — tap Select Fixture to add more</span>';
    grid.style.alignItems = "";
  }
  updateSummary();
  toast(`📋 "${newName}" ready — review and tap Log Entry`);

  // Reset hint text
  document.getElementById("fpModeHint").textContent = "Tap plan to drop a pin";
  document.getElementById("fpModeHint").style.color = "#5a8ab0";
}

function fpDeletePin() {
  if (!fpSelectedPin || !activePlanId) return;
  const plan = floorPlans[activePlanId];
  plan.pins = plan.pins.filter((p) => p.id !== fpSelectedPin.id);
  // Recalculate counter so next pin reuses the lowest available number
  const usedNums = new Set(plan.pins.map((p) => p.pinNum));
  let next = 1;
  while (usedNums.has(next)) next++;
  fpPinCounter = next;
  closeCallout();
  renderPins();
  renderPinList();
  toast("Pin removed");
}

function renderPinList() {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  const container = document.getElementById("fpPinListItems");
  if (!container) return;
  if (!plan || plan.pins.length === 0) {
    container.innerHTML =
      '<span style="color:#3a5a7a;font-size:11px;font-style:italic">No pins dropped yet</span>';
    return;
  }
  container.innerHTML = plan.pins
    .map((pin) => {
      const logged =
        pin.roomName && logEntries.some((e) => e.space === pin.roomName);
      const color = logged ? "#16a34a" : pin.roomName ? "#d97c18" : "#2563a8";
      const goBtn = pin.roomName
        ? `<button onclick="event.stopPropagation();fpGoFromList('${pin.id}')" title="Open room data entry" style="background:transparent;border:none;color:#7aabcf;font-size:14px;font-weight:700;cursor:pointer;padding:0 4px;line-height:1">→</button>`
        : "";
      return `<div style="background:#0d2035;border:1px solid ${color};border-radius:6px;padding:3px 4px 3px 8px;display:flex;align-items:center;gap:5px;cursor:pointer" onclick="fpJumpToPin('${
        pin.id
      }')">
      <span style="width:16px;height:16px;border-radius:50%;background:${color};color:#fff;font-size:9px;font-weight:700;display:flex;align-items:center;justify-content:center;flex-shrink:0">${
        pin.pinNum
      }</span>
      <span style="color:#c8e4ff;font-size:11px;max-width:120px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">${
        pin.roomName || "unassigned"
      }</span>
      ${goBtn}
    </div>`;
    })
    .join("");
}

function fpGoFromList(pinId) {
  const plan = activePlanId ? floorPlans[activePlanId] : null;
  const pin = plan?.pins.find((p) => p.id === pinId);
  if (!pin || !pin.roomName) return;
  fpSelectedPin = pin;
  fpGoToRoom();
}

function fpJumpToPin(pinId) {
  const plan = floorPlans[activePlanId];
  const pin = plan?.pins.find((p) => p.id === pinId);
  if (!pin) return;
  const wrap = document.getElementById("fpCanvasWrap");
  const canvas = document.getElementById("fpCanvas");
  fpOffset.x = wrap.clientWidth / 2 - pin.xPct * canvas.width * fpScale;
  fpOffset.y = wrap.clientHeight / 2 - pin.yPct * canvas.height * fpScale;
  applyTransform();
  const screenX =
    wrap.getBoundingClientRect().left +
    pin.xPct * canvas.width * fpScale +
    fpOffset.x;
  const screenY =
    wrap.getBoundingClientRect().top +
    pin.yPct * canvas.height * fpScale +
    fpOffset.y;
  showCallout(pin, screenX, screenY);
}

// ── COMMON NOTES ──────────────────────────────────────────────────────────
let cnParts = []; // accumulated note parts before inserting

function openCommonNotes() {
  cnParts = [];
  document
    .querySelectorAll(".cn-opt")
    .forEach((b) => b.classList.remove("active"));
  document.getElementById("cnPreview").textContent = "";
  document.getElementById("commonNotesModal").style.display = "block";
}

function closeCommonNotes() {
  document.getElementById("commonNotesModal").style.display = "none";
  cnParts = [];
}

function cnTap(btn) {
  const label = btn.dataset.label;
  const isCount = btn.classList.contains("cn-count");
  const isNote = btn.classList.contains("cn-note");

  if (isNote) {
    // Condition notes — independent multi-select toggle
    if (cnParts.includes(label)) {
      cnParts = cnParts.filter((p) => p !== label);
      btn.classList.remove("active");
    } else {
      cnParts.push(label);
      btn.classList.add("active");
    }
  } else if (isCount) {
    // Count buttons — mutually exclusive
    document
      .querySelectorAll(".cn-count")
      .forEach((b) => b.classList.remove("active"));
    if (cnParts.includes(label)) {
      cnParts = cnParts.filter(
        (p) =>
          ![
            "X1",
            "X2",
            "X3",
            "X4",
            "X5",
            "X6",
            "X7",
            "X8",
            "X9",
            "X10",
          ].includes(p),
      );
    } else {
      cnParts = cnParts.filter(
        (p) =>
          ![
            "X1",
            "X2",
            "X3",
            "X4",
            "X5",
            "X6",
            "X7",
            "X8",
            "X9",
            "X10",
          ].includes(p),
      );
      cnParts.push(label);
      btn.classList.add("active");
    }
  } else {
    // Row / Solo — mutually exclusive in this group
    document
      .querySelectorAll(".cn-opt:not(.cn-count):not(.cn-note)")
      .forEach((b) => b.classList.remove("active"));
    const rowLabels = [
      "Solo Fixtures",
      "Row 12'",
      "Row 16'",
      "Row 20'",
      "Row 24'",
      "Row 28'",
      "Row 32'",
      "Row 36'",
      "Row 40'",
      "Row 44'",
      "Row 48'",
      "Row 52'",
      "Row 56'",
      "Row 60'",
      "Row 64'",
    ];
    cnParts = cnParts.filter((p) => !rowLabels.includes(p));
    cnParts.push(label);
    btn.classList.add("active");
  }
  const preview = cnParts.join(", ");
  document.getElementById("cnPreview").textContent = preview || "";
}

function cnInsert() {
  if (cnParts.length === 0) {
    toast("Select a note first");
    return;
  }
  const text = cnParts.join(" ");
  const ta = document.getElementById("notesInput");
  const existing = ta.value.trim();
  // Comma goes AFTER the inserted block, not between individual items
  ta.value = existing ? existing + ", " + text + "," : text + ",";
  syncNotesDisplay();
  // Clear selections but STAY in modal for another pick
  cnParts = [];
  document
    .querySelectorAll(".cn-opt")
    .forEach((b) => b.classList.remove("active"));
  document.getElementById("cnPreview").textContent = "";
  toast("✔ Added — select more or tap Done");
}

// ── FLOORS / AREAS PAGE ───────────────────────────────────────────────────
// floors: [{id, name, type, createdAt}]
// activeFloor: {id, name, type}
let floors = [];
let activeFloor = null;
const FLOORS_KEY = "auditTool_floors_v1";

function floorsLoad() {
  try {
    const raw = localStorage.getItem(FLOORS_KEY);
    if (raw) floors = JSON.parse(raw);
  } catch (e) {
    floors = [];
  }
}

function floorsSave() {
  try {
    localStorage.setItem(FLOORS_KEY, JSON.stringify(floors));
  } catch (e) {}
}

function openFloorsPage() {
  floorsLoad();
  document.getElementById("floorsBldgLabel").textContent =
    bldgData.name || "Building";
  floorHideAddForm();
  renderFloorCards();
  document.getElementById("floorsPage").classList.add("visible");
}

function closeFloorsPage() {
  document.getElementById("floorsPage").classList.remove("visible");
}

function floorsGoBuilding() {
  closeFloorsPage();
  openBuildingPage();
}

function floorShowAddForm() {
  document.getElementById("floorAddTrigger").style.display = "none";
  document.getElementById("floorAddForm").style.display = "block";
  setTimeout(() => document.getElementById("floorNameInput").focus(), 60);
}

function floorHideAddForm() {
  document.getElementById("floorAddForm").style.display = "none";
  document.getElementById("floorAddTrigger").style.display = "flex";
  const inp = document.getElementById("floorNameInput");
  if (inp) inp.value = "";
}

function floorSaveNew() {
  const name = document.getElementById("floorNameInput").value.trim();
  if (!name) {
    toast("Enter a name for this floor / area");
    return;
  }
  const type = document.getElementById("floorTypeInput").value || "Floor";
  const id = "floor_" + Date.now();
  floors.push({ id, name, type, createdAt: new Date().toISOString() });
  floorsSave();
  floorHideAddForm();
  renderFloorCards();
  renderFloorStrip();
  toast('✔ "' + name + '" added');
}

let _floorRenameId = null;

// prompt() is silently blocked in iOS standalone PWA mode, so rename now uses
// an in-app modal with a native input (native inputs work fine on the floors
// page — the add-floor form proves it).
function floorRename(id) {
  const fl = floors.find((f) => f.id === id);
  if (!fl) return;
  _floorRenameId = id;
  const input = document.getElementById("floorRenameInput");
  if (input) input.value = fl.name;
  document.getElementById("floorRenameModal").style.display = "flex";
  setTimeout(() => {
    if (input) {
      input.focus();
      input.select();
    }
  }, 60);
}

function closeFloorRename() {
  document.getElementById("floorRenameModal").style.display = "none";
  _floorRenameId = null;
}

function saveFloorRename() {
  const id = _floorRenameId;
  const fl = floors.find((f) => f.id === id);
  if (!fl) {
    closeFloorRename();
    return;
  }
  const trimmed = (
    document.getElementById("floorRenameInput").value || ""
  ).trim();
  if (!trimmed || trimmed === fl.name) {
    closeFloorRename();
    return;
  }
  if (floors.some((f) => f.id !== id && f.name === trimmed)) {
    toast(`A floor named "${trimmed}" already exists`);
    return;
  }
  const oldName = fl.name;
  fl.name = trimmed;
  // Migrate logEntries that reference the old floor name
  let migrated = 0;
  logEntries.forEach((e) => {
    if (e.floor === oldName) {
      e.floor = trimmed;
      migrated++;
    }
  });
  // If the renamed floor is the active one, sync activeFloor + top bar
  if (activeFloor && activeFloor.id === id) {
    activeFloor.name = trimmed;
    if (typeof updateTopCrumb === "function") updateTopCrumb();
  }
  floorsSave();
  scheduleSave();
  closeFloorRename();
  renderFloorCards();
  renderFloorStrip();
  toast(
    `Renamed → "${trimmed}"${
      migrated
        ? ` (${migrated} log row${migrated === 1 ? "" : "s"} updated)`
        : ""
    }`,
  );
}

function floorDelete(id) {
  const fl = floors.find((f) => f.id === id);
  if (!fl) return;
  if (!confirm('Delete "' + fl.name + '"? This cannot be undone.')) return;
  floors = floors.filter((f) => f.id !== id);
  if (activeFloor && activeFloor.id === id) activeFloor = null;
  floorsSave();
  renderFloorCards();
  renderFloorStrip();
  toast('Deleted "' + fl.name + '"');
}

function renderFloorCards() {
  const list = document.getElementById("floorCardList");
  const FLOOR_ICONS = {
    Floor: "🏢",
    Area: "📋",
    Wing: "🔀",
    Basement: "⬇",
    Roof: "⬆",
    Exterior: "🌿",
    Penthouse: "⭐",
  };

  if (!floors.length) {
    list.innerHTML =
      '<div style="text-align:center;padding:30px 0;color:#b0b8c8;font-size:13px">No floors yet — add one above</div>';
    return;
  }

  list.innerHTML = floors
    .map((fl) => {
      // Count rooms logged for this floor
      const roomCount = [
        ...new Set(
          logEntries.filter((e) => e.floor === fl.name).map((e) => e.space),
        ),
      ].length;
      const fixCount = logEntries
        .filter((e) => e.floor === fl.name)
        .reduce((s, e) => s + (e.qty || 0), 0);
      const isActive = activeFloor && activeFloor.id === fl.id;
      const hasDrafts = roomCount > 0;
      return `<div class="floor-card" data-floor-id="${fl.id}" style="${
        isActive
          ? "border-color:#0d9488;box-shadow:0 0 0 2px rgba(13,148,136,.15)"
          : ""
      }">
      <div class="floor-card-header">
        <div class="floor-drag-handle" title="Drag to reorder" style="cursor:grab;color:#b0b8c8;font-size:18px;padding:0 8px 0 2px;touch-action:none;user-select:none;-webkit-user-select:none;align-self:stretch;display:flex;align-items:center">⠿</div>
        <div class="floor-icon">${FLOOR_ICONS[fl.type] || "🏢"}</div>
        <div class="floor-info">
          <div class="floor-name">${
            fl.name
          } <span style="font-size:11px;font-weight:500;color:#94a3b8">${
        fl.type
      }</span></div>
          <div class="floor-meta">${
            hasDrafts
              ? roomCount + " rooms · " + fixCount + " fixtures logged"
              : "No rooms logged yet"
          }</div>
        </div>
        <div style="display:flex;flex-direction:column;gap:6px;align-items:flex-end">
          <button onclick="floorEnterAudit('${fl.id}')"
            class="floor-start-btn ${hasDrafts ? "resume" : "fresh"}">
            ${hasDrafts ? "▶ Resume" : "Start →"}
          </button>
          <div style="display:flex;gap:10px">
            <button onclick="floorRename('${fl.id}')"
              style="font-size:10px;color:#5a7a9a;background:transparent;border:none;cursor:pointer;font-family:inherit;padding:2px 0">
              ✏ Rename
            </button>
            <button onclick="floorDelete('${fl.id}')"
              style="font-size:10px;color:#c04040;background:transparent;border:none;cursor:pointer;font-family:inherit;padding:2px 0">
              🗑 Remove
            </button>
          </div>
        </div>
      </div>
    </div>`;
    })
    .join("");
  initFloorCardDrag();
}

// Pointer-based drag-to-reorder for floor cards. HTML5 drag-and-drop is
// unreliable on touch, so this uses pointer events on a grip handle. Bound
// once via delegation on the (persistent) list container; the inner HTML is
// replaced on each render but the listener survives.
function initFloorCardDrag() {
  const list = document.getElementById("floorCardList");
  if (!list || list.dataset.dragBound === "1") return;
  list.dataset.dragBound = "1";

  list.addEventListener("pointerdown", (e) => {
    const handle = e.target.closest && e.target.closest(".floor-drag-handle");
    if (!handle) return;
    const card = handle.closest(".floor-card");
    if (!card) return;
    const dragId = card.dataset.floorId;
    if (!dragId) return;
    e.preventDefault();
    handle.style.cursor = "grabbing";

    const markDragged = () => {
      const el = list.querySelector(`.floor-card[data-floor-id="${dragId}"]`);
      if (el) {
        el.style.opacity = "0.55";
        el.style.boxShadow = "0 6px 18px rgba(0,0,0,.15)";
      }
    };
    markDragged();

    const onMove = (ev) => {
      const cards = [...list.querySelectorAll(".floor-card")];
      // Insert before the first OTHER card whose vertical midpoint is below
      // the pointer; if none, the dragged card goes to the end.
      let beforeId = null;
      for (const c of cards) {
        if (c.dataset.floorId === dragId) continue;
        const r = c.getBoundingClientRect();
        if (ev.clientY < r.top + r.height / 2) {
          beforeId = c.dataset.floorId;
          break;
        }
      }
      const fromIdx = floors.findIndex((f) => f.id === dragId);
      if (fromIdx < 0) return;
      const order = floors.map((f) => f.id);
      const [movedId] = order.splice(fromIdx, 1);
      if (beforeId == null) order.push(movedId);
      else order.splice(order.indexOf(beforeId), 0, movedId);
      // Only re-render if the order actually changed (avoids thrash)
      if (order.join("|") !== floors.map((f) => f.id).join("|")) {
        floors = order.map((id) => floors.find((f) => f.id === id));
        renderFloorCards();
        markDragged();
      }
    };
    const onUp = () => {
      document.removeEventListener("pointermove", onMove);
      document.removeEventListener("pointerup", onUp);
      document.removeEventListener("pointercancel", onUp);
      floorsSave();
      renderFloorCards();
      renderFloorStrip();
      toast("Floor order saved");
    };
    document.addEventListener("pointermove", onMove);
    document.addEventListener("pointerup", onUp);
    document.addEventListener("pointercancel", onUp);
  });
}

function floorEnterAudit(id) {
  const fl = floors.find((f) => f.id === id);
  if (!fl) return;
  activeFloor = fl;
  const linkedId = Object.keys(floorPlans).find(
    (id) => floorPlans[id]?.floorId === fl.id,
  );
  activePlanId = linkedId || null;
  floorsSave();
  updateTopCrumb();
  closeFloorsPage();
  document.getElementById("mainApp").style.display = "flex";
  if (typeof mmFit === "function") mmFit();
  if (typeof renderAuditMinimap === "function") renderAuditMinimap();
  toast("📋 Auditing: " + (bldgData.name || "Building") + " · " + fl.name);
}

function floorPillClick(id) {
  if (activeFloor && activeFloor.id === id) return;
  const fl = floors.find((f) => f.id === id);
  if (!fl) return;
  // Save current room's in-progress work before switching floors
  if (typeof snapshotCurrentRoomDraft === "function")
    snapshotCurrentRoomDraft();
  activeFloor = fl;
  // Re-bind the floor plan selection to whichever plan is linked to this new
  // floor (or null if none). Prevents the prior floor's plan from showing up
  // when the user reopens the floor plan overlay.
  const linkedId = Object.keys(floorPlans).find(
    (id) => floorPlans[id]?.floorId === fl.id,
  );
  activePlanId = linkedId || null;
  floorsSave();
  clearFixtures();
  updateTopCrumb();
  if (typeof mmFit === "function") mmFit();
  if (typeof renderAuditMinimap === "function") renderAuditMinimap();
  toast("📋 Switched to: " + fl.name);
}

function renderFloorStrip() {
  const strip = document.getElementById("floorStrip");
  if (!strip) return;
  strip.innerHTML = "";
  if (!floors || !floors.length) {
    const span = document.createElement("span");
    span.className = "floor-strip-empty";
    span.textContent = "No floors yet — tap building name to add";
    strip.appendChild(span);
    return;
  }
  floors.forEach((f) => {
    const btn = document.createElement("button");
    btn.className =
      "floor-pill" + (activeFloor && activeFloor.id === f.id ? " active" : "");
    btn.textContent = f.name || "Floor";
    btn.onclick = () => floorPillClick(f.id);
    strip.appendChild(btn);
  });
}

function updateTopCrumb() {
  const topName = document.getElementById("topBldgName");
  if (topName) topName.textContent = bldgData.name || "Building";
  if (typeof updateProjectsCrumb === "function") updateProjectsCrumb();
  renderFloorStrip();
}

// ── SPECIFY POPOVERS (replacement specs) ─────────────────────────────────
function openSpecifyFixtures() {
  document.getElementById("specifyFixturesModal").style.display = "block";
}
function closeSpecifyFixtures() {
  document.getElementById("specifyFixturesModal").style.display = "none";
}
function openSpecifySwitch() {
  document.getElementById("specifySwitchModal").style.display = "block";
}
function closeSpecifySwitch() {
  document.getElementById("specifySwitchModal").style.display = "none";
}
function openSpecifyControls() {
  document.getElementById("specifyControlsModal").style.display = "block";
}
function closeSpecifyControls() {
  document.getElementById("specifyControlsModal").style.display = "none";
}

function updateSpecifyCounts() {
  const sw = (state.swRec || []).length;
  const ct = (state.ctrls || []).length;
  const fx = (state.fixRec || []).length;
  const swBadge = document.getElementById("specifySwitchCount");
  const ctBadge = document.getElementById("specifyControlsCount");
  const fxBadge = document.getElementById("specifyFixturesCount");
  if (swBadge) {
    swBadge.textContent = sw;
    swBadge.style.display = sw ? "inline-block" : "none";
  }
  if (ctBadge) {
    ctBadge.textContent = ct;
    ctBadge.style.display = ct ? "inline-block" : "none";
  }
  if (fxBadge) {
    fxBadge.textContent = fx;
    fxBadge.style.display = fx ? "inline-block" : "none";
  }
}

// ── SWIPE NAVIGATION ──────────────────────────────────────────────────────
// Pages in order: 0=Data Entry, 1=Room View, 2=Data Table
// Swipe left → next page, swipe right → prev page
// Only fires when swipe starts/ends in top-level scroll containers
// (blocks swipe if touch scrolling horizontally inside room-card-body)

const SWIPE_MIN_X = 60; // min horizontal px to count as swipe
const SWIPE_MAX_Y = 80; // max vertical drift allowed
const SWIPE_EDGE = 40; // ignore swipes starting from within scrollable content

let _swX = null,
  _swY = null,
  _swEl = null;

function swipeCurrentPage() {
  const fpVisible =
    document.getElementById("floorPlanOverlay").style.display !== "none";
  if (fpVisible) return 3;
  const logVisible = document
    .getElementById("logPanel")
    .classList.contains("visible");
  if (!logVisible) return 0;
  const tableVisible =
    document.getElementById("logTableWrap").style.display !== "none";
  return tableVisible ? 2 : 1;
}

function swipeGoTo(page) {
  const cur = swipeCurrentPage();
  if (page === cur) return;
  if (page === 0) {
    closeFloorPlan();
    showForm();
    showSwipeIndicator("← Data Entry");
  } else if (page === 1) {
    closeFloorPlan();
    if (cur === 0) showLog();
    setLogView("visual");
    showSwipeIndicator("📋 Room View");
  } else if (page === 2) {
    closeFloorPlan();
    if (cur === 0) showLog();
    setLogView("table");
    showSwipeIndicator("📊 Data Table");
  } else if (page === 3) {
    if (cur === 0) showLog();
    openFloorPlan();
    showSwipeIndicator("📐 Floor Plan");
  }
  renderSwipeDots();
}

function showSwipeIndicator(label) {
  let ind = document.getElementById("swipeIndicator");
  if (!ind) {
    ind = document.createElement("div");
    ind.id = "swipeIndicator";
    ind.style.cssText =
      "position:fixed;top:50%;left:50%;transform:translate(-50%,-50%);background:rgba(20,40,80,.75);color:#fff;font-size:16px;font-weight:700;padding:12px 28px;border-radius:30px;z-index:9999;pointer-events:none;transition:opacity .4s;white-space:nowrap";
    document.body.appendChild(ind);
  }
  ind.textContent = label;
  ind.style.opacity = "1";
  clearTimeout(ind._t);
  ind._t = setTimeout(() => {
    ind.style.opacity = "0";
  }, 800);
}

document.addEventListener(
  "touchstart",
  (e) => {
    const t = e.touches[0];
    _swX = t.clientX;
    _swY = t.clientY;
    _swEl = e.target;
  },
  { passive: true },
);

document.addEventListener(
  "touchend",
  (e) => {
    if (_swX === null) return;
    const t = e.changedTouches[0];
    const dx = t.clientX - _swX;
    const dy = t.clientY - _swY;
    _swX = _swY = null;

    if (Math.abs(dx) < SWIPE_MIN_X || Math.abs(dy) > SWIPE_MAX_Y) return;

    // Don't fire if swipe originated inside a horizontally-scrolling container
    if (_swEl && _swEl.closest(".room-card-body, .xl-table")) return;

    // Don't fire if inside a popover/modal
    if (
      _swEl &&
      _swEl.closest('[id$="Modal"],[id$="Popover"],#qtyKbPanel,#roomKb')
    )
      return;

    // Don't fire if inside the floor plan canvas (has its own pan)
    if (_swEl && _swEl.closest("#fpCanvasWrap")) return;

    const cur = swipeCurrentPage();
    if (dx < 0) {
      // Swipe left → next
      if (cur < 3) swipeGoTo(cur + 1);
    } else {
      // Swipe right → prev
      if (cur > 0) swipeGoTo(cur - 1);
    }
  },
  { passive: true },
);

// Show subtle swipe dots indicator on load
function renderSwipeDots() {
  let dots = document.getElementById("swipeDots");
  if (!dots) {
    dots = document.createElement("div");
    dots.id = "swipeDots";
    dots.style.cssText =
      "position:fixed;bottom:10px;left:50%;transform:translateX(-50%);display:flex;gap:7px;z-index:300;pointer-events:none;opacity:.5";
    document.body.appendChild(dots);
  }
  const cur = swipeCurrentPage();
  const labels = ["Data Entry", "Room View", "Data Table", "Floor Plan"];
  dots.innerHTML = labels
    .map(
      (l, i) =>
        `<div title="${l}" style="width:${
          i === cur ? 18 : 7
        }px;height:7px;border-radius:4px;background:${
          i === cur ? "#2563a8" : "#94a3b8"
        };transition:all .25s"></div>`,
    )
    .join("");
}

// Update dots whenever a view change occurs
const _origShowLog = showLog;
const _origShowForm = showForm;
const _origSetLogView = setLogView;
showLog = function () {
  _origShowLog.apply(this, arguments);
  setTimeout(renderSwipeDots, 50);
};
showForm = function () {
  _origShowForm.apply(this, arguments);
  setTimeout(renderSwipeDots, 50);
};
setLogView = function () {
  _origSetLogView.apply(this, arguments);
  setTimeout(renderSwipeDots, 50);
};
const _origOpenFP = openFloorPlan;
const _origCloseFP = closeFloorPlan;
openFloorPlan = function () {
  _origOpenFP.apply(this, arguments);
  setTimeout(renderSwipeDots, 50);
};
closeFloorPlan = function () {
  _origCloseFP.apply(this, arguments);
  setTimeout(renderSwipeDots, 50);
};

setTimeout(renderSwipeDots, 200);

function syncHoursPresets(hrs) {
  document.querySelectorAll(".hours-preset-btn").forEach((b) => {
    const v = b.dataset.hrs;
    b.classList.toggle(
      "active",
      v === String(hrs) ||
        (v === "custom" &&
          !["2000", "2500", "3000", "4000", "8760"].includes(String(hrs))),
    );
  });
}

function selectHoursPreset(btn, val) {
  document
    .querySelectorAll(".hours-preset-btn")
    .forEach((b) => b.classList.remove("active"));
  btn.classList.add("active");
  if (val !== "custom") {
    document.getElementById("bldgHoursInput").value = val;
    document.getElementById("bldgScheduleResult").style.display = "none";
    bldgSaveDraft();
  } else {
    document.getElementById("bldgHoursInput").focus();
  }
}

function bldgHoursFromAnnual(val) {
  const hrs = parseInt(val) || 0;
  syncHoursPresets(hrs);
  document.getElementById("bldgScheduleResult").style.display = "none";
  bldgSaveDraft();
}

function bldgHoursFromSchedule() {
  const hd = parseFloat(document.getElementById("bldgHrsDay").value) || 0;
  const dw = parseFloat(document.getElementById("bldgDaysWeek").value) || 0;
  const wy = parseFloat(document.getElementById("bldgWeeksYr").value) || 50;
  if (!hd || !dw) return;
  const total = Math.round(hd * dw * wy);
  document.getElementById("bldgHoursInput").value = total;
  syncHoursPresets(total);
  const res = document.getElementById("bldgScheduleResult");
  res.style.display = "block";
  res.textContent =
    hd +
    " hr/day × " +
    dw +
    " days/wk × " +
    wy +
    " wks = " +
    total.toLocaleString() +
    " hrs/yr";
  bldgSaveDraft();
}

// ── ROOM-TYPE HOURS MODAL ─────────────────────────────────────────────────

// Default hours per room group (used when no user override is set)
const RTH_DEFAULTS = {
  Atrium: 2500,
  Office: 2250,
  Restroom: 2500,
  "Locker Room": 2000,
  Hallway: 3000,
  Storage: 600,
  Café: 1800,
  Warehouse: 3500,
  "Loading Dock": 2500,
  Mechanical: 1000,
  "Laundry Room": 1500,
  Class: 1800,
  Auditorium: 1200,
  Gymnasium: 1800,
  "Weight Room": 1500,
  Pool: 1200,
  Stairwell: 3500,
  Exterior: 4000,
};

// Dynamically build the room type hours list from ROOM_TYPE_DB, filtered by building type
function getRthGroups() {
  const bCode = getBldgTypeCode();
  const seen = new Set();
  const groups = [];
  ROOM_TYPE_DB.forEach((r) => {
    if (seen.has(r.group)) return;
    if (!r.bldg.includes("universal") && !r.bldg.includes(bCode)) return;
    seen.add(r.group);
    groups.push({
      key: r.group,
      label: r.group,
      defaultHrs: RTH_DEFAULTS[r.group] || 2000,
    });
  });
  return groups;
}

let rthMode = "annual"; // 'annual' or 'schedule'

// ── ROOM TYPE DATABASE ────────────────────────────────────────────────────────
// Source: Building_Hours___Code_Data.xlsx — NYC ECC2025 Space-By-Space + ASHRAE 90.1-2022 Model
// buildingTypes: 'universal'=all, 'k12'=K-12/Higher Ed, 'office'=Office/Municipal
// lpd_nycecc: IECC 2024 / NYC ECC 2025 LPD (W/ft²)
// lpd_ashrae: ASHRAE 90.1-2022 Addendum s LPD (W/ft²)
// lpdDesignator + subDesignation: hidden — for future code compliance report
const ROOM_TYPE_DB = [
  // ── ATRIUM ────────────────────────────────────────────────────────────────
  {
    group: "Atrium",
    sub: "< 40 ft",
    bldg: ["office"],
    fc: "—",
    fcT: "—",
    lpd_nyc: 0.41,
    lpd_ash: 0.29,
    des: "Atrium",
    subDes: "<20 FT in height",
  },
  {
    group: "Atrium",
    sub: "> 40 ft",
    bldg: ["office"],
    fc: "—",
    fcT: "—",
    lpd_nyc: 0.51,
    lpd_ash: 0.49,
    des: "Atrium",
    subDes: ">40 ft in height",
  },
  // ── OFFICE ────────────────────────────────────────────────────────────────
  {
    group: "Office",
    sub: "Private Office",
    bldg: ["universal"],
    fc: "40–60 FC",
    fcT: "50 FC",
    lpd_nyc: 0.73,
    lpd_ash: 0.69,
    des: "Office",
    subDes: "",
  },
  {
    group: "Office",
    sub: "Open (>300 ft²)",
    bldg: ["universal"],
    fc: "40–60 FC",
    fcT: "50 FC",
    lpd_nyc: 0.56,
    lpd_ash: 0.52,
    des: "Office",
    subDes: "",
  },
  {
    group: "Office",
    sub: "Conference Room",
    bldg: ["universal"],
    fc: "40–60 FC",
    fcT: "50 FC",
    lpd_nyc: 0.88,
    lpd_ash: 0.83,
    des: "Conference/Meeting/Multipurpose Rooms",
    subDes: "",
  },
  {
    group: "Office",
    sub: "Break Room",
    bldg: ["universal"],
    fc: "10–30 FC",
    fcT: "20 FC",
    lpd_nyc: 0.44,
    lpd_ash: 0.5,
    des: "Lounge/Breakroom",
    subDes: "",
  },
  {
    group: "Office",
    sub: "Copy Room",
    bldg: ["universal"],
    fc: "20–40 FC",
    fcT: "30 FC",
    lpd_nyc: 0.5,
    lpd_ash: 0.52,
    des: "Copy/Print Room",
    subDes: "",
  },
  {
    group: "Office",
    sub: "Multi-Purpose Room",
    bldg: ["universal"],
    fc: "20–40 FC",
    fcT: "30 FC",
    lpd_nyc: 0.88,
    lpd_ash: 0.83,
    des: "Conference/Meeting/Multipurpose Rooms",
    subDes: "",
  },
  // ── RESTROOM ──────────────────────────────────────────────────────────────
  {
    group: "Restroom",
    sub: "Individual",
    bldg: ["universal"],
    fc: "7.5–30 FC",
    fcT: "15 FC",
    lpd_nyc: 0.74,
    lpd_ash: 0.73,
    des: "Restroom",
    subDes: "",
  },
  {
    group: "Restroom",
    sub: "Large",
    bldg: ["universal"],
    fc: "7.5–30 FC",
    fcT: "15 FC",
    lpd_nyc: 0.74,
    lpd_ash: 0.73,
    des: "Restroom",
    subDes: "",
  },
  {
    group: "Restroom",
    sub: "Student",
    bldg: ["universal"],
    fc: "7.5–30 FC",
    fcT: "15 FC",
    lpd_nyc: 0.74,
    lpd_ash: 0.73,
    des: "Restroom",
    subDes: "",
  },
  // ── LOCKER ROOM ───────────────────────────────────────────────────────────
  {
    group: "Locker Room",
    sub: "General",
    bldg: ["universal"],
    fc: "10–30 FC",
    fcT: "20 FC",
    lpd_nyc: 0.43,
    lpd_ash: 0.4,
    des: "Locker Room",
    subDes: "",
  },
  {
    group: "Locker Room",
    sub: "Showers",
    bldg: ["universal"],
    fc: "10 FC",
    fcT: "10 FC",
    lpd_nyc: 0.74,
    lpd_ash: 0.4,
    des: "Restroom",
    subDes: "",
  },
  // ── HALLWAY ───────────────────────────────────────────────────────────────
  {
    group: "Hallway",
    sub: "Corridor",
    bldg: ["universal"],
    fc: "10–40 FC",
    fcT: "15 FC",
    lpd_nyc: 0.44,
    lpd_ash: 0.43,
    des: "Corridor",
    subDes: "Otherwise",
  },
  {
    group: "Hallway",
    sub: "Vestibule",
    bldg: ["universal"],
    fc: "10–40 FC",
    fcT: "15 FC",
    lpd_nyc: 0.44,
    lpd_ash: 0.43,
    des: "Corridor",
    subDes: "Otherwise",
  },
  {
    group: "Hallway",
    sub: "Foyer",
    bldg: ["universal"],
    fc: "10–40 FC",
    fcT: "15 FC",
    lpd_nyc: 0.44,
    lpd_ash: 0.43,
    des: "Corridor",
    subDes: "Otherwise",
  },
  {
    group: "Hallway",
    sub: "Lobby",
    bldg: ["universal"],
    fc: "10–40 FC",
    fcT: "15 FC",
    lpd_nyc: 0.8,
    lpd_ash: 0.74,
    des: "Lobby",
    subDes: "All other lobbies",
  },
  // ── STORAGE ───────────────────────────────────────────────────────────────
  {
    group: "Storage",
    sub: "Closet",
    bldg: ["universal"],
    fc: "10 FC",
    fcT: "10 FC",
    lpd_nyc: 0.35,
    lpd_ash: 0.46,
    des: "Storage Room",
    subDes: "",
  },
  {
    group: "Storage",
    sub: "Storage Room",
    bldg: ["universal"],
    fc: "10 FC",
    fcT: "10 FC",
    lpd_nyc: 0.35,
    lpd_ash: 0.33,
    des: "Storage Room",
    subDes: "",
  },
  {
    group: "Storage",
    sub: "Slop Sink",
    bldg: ["universal"],
    fc: "10 FC",
    fcT: "10 FC",
    lpd_nyc: 0.35,
    lpd_ash: 0.46,
    des: "Storage Room",
    subDes: "",
  },
  {
    group: "Storage",
    sub: "Equipment Room",
    bldg: ["universal"],
    fc: "10 FC",
    fcT: "10 FC",
    lpd_nyc: 0.35,
    lpd_ash: 0.69,
    des: "Equipment Room",
    subDes: "",
  },
  // ── CAFÉ ──────────────────────────────────────────────────────────────────
  {
    group: "Café",
    sub: "Kitchen",
    bldg: ["universal"],
    fc: "40–60 FC",
    fcT: "50 FC",
    lpd_nyc: 0.92,
    lpd_ash: 0.93,
    des: "Food preparation area",
    subDes: "",
  },
  {
    group: "Café",
    sub: "Food Prep",
    bldg: ["universal"],
    fc: "40–60 FC",
    fcT: "60 FC",
    lpd_nyc: 0.92,
    lpd_ash: 0.93,
    des: "Food preparation area",
    subDes: "",
  },
  {
    group: "Café",
    sub: "Serving Area",
    bldg: ["universal"],
    fc: "20 FC",
    fcT: "20 FC",
    lpd_nyc: 0.92,
    lpd_ash: 0.4,
    des: "Food preparation area",
    subDes: "",
  },
  {
    group: "Café",
    sub: "Cafeteria",
    bldg: ["universal"],
    fc: "10–30 FC",
    fcT: "20 FC",
    lpd_nyc: 0.36,
    lpd_ash: 0.35,
    des: "Dining Areas",
    subDes: "In cafeteria or fast food dining",
  },
  // ── WAREHOUSE ─────────────────────────────────────────────────────────────
  {
    group: "Warehouse",
    sub: "Open Warehouse",
    bldg: ["universal"],
    fc: "10–30 FC",
    fcT: "20 FC",
    lpd_nyc: 0.27,
    lpd_ash: 0.41,
    des: "Warehouse - Storage Area",
    subDes: "Medium-to-bulk, palletized items",
  },
  {
    group: "Warehouse",
    sub: "Bulk Storage",
    bldg: ["universal"],
    fc: "10–20 FC",
    fcT: "20 FC",
    lpd_nyc: 0.27,
    lpd_ash: 0.41,
    des: "Warehouse - Storage Area",
    subDes: "Medium-to-bulk, palletized items",
  },
  {
    group: "Warehouse",
    sub: "Bulk Storage (Small Labels)",
    bldg: ["universal"],
    fc: "15–60 FC",
    fcT: "30 FC",
    lpd_nyc: 0.65,
    lpd_ash: 0.83,
    des: "Warehouse - Storage Area",
    subDes: "For smaller, hand-carried items",
  },
  {
    group: "Warehouse",
    sub: "Active Rack Aisles",
    bldg: ["universal"],
    fc: "20–40 FC",
    fcT: "30 FC",
    lpd_nyc: 0.27,
    lpd_ash: 0.41,
    des: "Warehouse - Storage Area",
    subDes: "Medium-to-bulk, palletized items",
  },
  {
    group: "Warehouse",
    sub: "Picking/Packing",
    bldg: ["universal"],
    fc: "30–75 FC",
    fcT: "50 FC",
    lpd_nyc: 0.65,
    lpd_ash: 0.83,
    des: "Warehouse - Storage Area",
    subDes: "For smaller, hand-carried items",
  },
  {
    group: "Warehouse",
    sub: "Quality Control",
    bldg: ["universal"],
    fc: "40–60 FC",
    fcT: "50 FC",
    lpd_nyc: 0.65,
    lpd_ash: 0.83,
    des: "Warehouse - Storage Area",
    subDes: "For smaller, hand-carried items",
  },
  // ── LOADING DOCK ──────────────────────────────────────────────────────────
  {
    group: "Loading Dock",
    sub: "Loading Dock",
    bldg: ["universal"],
    fc: "10–30 FC",
    fcT: "20 FC",
    lpd_nyc: 0.51,
    lpd_ash: 0.82,
    des: "Loading Dock, Interior",
    subDes: "",
  },
  // ── MECHANICAL ────────────────────────────────────────────────────────────
  {
    group: "Mechanical",
    sub: "General",
    bldg: ["universal"],
    fc: "20–50 FC",
    fcT: "30 FC",
    lpd_nyc: 0.39,
    lpd_ash: 0.67,
    des: "Electrical/Mechanical Room",
    subDes: "",
  },
  {
    group: "Mechanical",
    sub: "Boiler Room",
    bldg: ["universal"],
    fc: "20–50 FC",
    fcT: "30 FC",
    lpd_nyc: 0.39,
    lpd_ash: 0.67,
    des: "Electrical/Mechanical Room",
    subDes: "",
  },
  {
    group: "Mechanical",
    sub: "Electrical Room",
    bldg: ["universal"],
    fc: "10 FC",
    fcT: "10 FC",
    lpd_nyc: 0.39,
    lpd_ash: 0.67,
    des: "Electrical/Mechanical Room",
    subDes: "",
  },
  {
    group: "Mechanical",
    sub: "Elevator Machine Room",
    bldg: ["universal"],
    fc: "20–50 FC",
    fcT: "30 FC",
    lpd_nyc: 0.39,
    lpd_ash: 0.67,
    des: "Electrical/Mechanical Room",
    subDes: "",
  },
  {
    group: "Mechanical",
    sub: "Crawl Space",
    bldg: ["universal"],
    fc: "5 FC",
    fcT: "5 FC",
    lpd_nyc: 0.39,
    lpd_ash: 0.67,
    des: "Electrical/Mechanical Room",
    subDes: "",
  },
  {
    group: "Mechanical",
    sub: "Workshop",
    bldg: ["universal"],
    fc: "—",
    fcT: "—",
    lpd_nyc: 1.09,
    lpd_ash: 1.1,
    des: "Workshop",
    subDes: "",
  },
  // ── LAUNDRY ROOM ──────────────────────────────────────────────────────────
  {
    group: "Laundry Room",
    sub: "Laundry",
    bldg: ["universal"],
    fc: "20–50 FC",
    fcT: "30 FC",
    lpd_nyc: 0.43,
    lpd_ash: 0.48,
    des: "Laundry/Washing Area",
    subDes: "",
  },
  // ── CLASS (K-12 / Higher Ed) ──────────────────────────────────────────────
  {
    group: "Class",
    sub: "General",
    bldg: ["k12"],
    fc: "30–50 FC",
    fcT: "50 FC",
    lpd_nyc: 0.72,
    lpd_ash: 0.68,
    des: "Classroom/lecture hall/training room",
    subDes: "Otherwise",
  },
  {
    group: "Class",
    sub: "Lab",
    bldg: ["k12"],
    fc: "30–50 FC",
    fcT: "50 FC",
    lpd_nyc: 1.04,
    lpd_ash: 1.0,
    des: "Laboratory",
    subDes: "In or as a classroom",
  },
  {
    group: "Class",
    sub: "Computer Class",
    bldg: ["k12"],
    fc: "30–50 FC",
    fcT: "50 FC",
    lpd_nyc: 0.72,
    lpd_ash: 0.7,
    des: "Classroom/lecture hall/training room",
    subDes: "Otherwise",
  },
  {
    group: "Class",
    sub: "Art",
    bldg: ["k12"],
    fc: "40–60 FC",
    fcT: "60 FC",
    lpd_nyc: 0.72,
    lpd_ash: 0.68,
    des: "Classroom/lecture hall/training room",
    subDes: "Otherwise",
  },
  {
    group: "Class",
    sub: "Shop",
    bldg: ["k12"],
    fc: "40–60 FC",
    fcT: "60 FC",
    lpd_nyc: 1.09,
    lpd_ash: 1.1,
    des: "Workshop",
    subDes: "Shop Classroom",
  },
  {
    group: "Class",
    sub: "Lecture Hall",
    bldg: ["k12"],
    fc: "30–50 FC",
    fcT: "40 FC",
    lpd_nyc: 0.72,
    lpd_ash: 0.68,
    des: "Classroom/lecture hall/training room",
    subDes: "Otherwise",
  },
  // ── AUDITORIUM ────────────────────────────────────────────────────────────
  {
    group: "Auditorium",
    sub: "Seating Area",
    bldg: ["k12"],
    fc: "3–10 FC",
    fcT: "7.5 FC",
    lpd_nyc: 0.57,
    lpd_ash: 0.56,
    des: "Audience Seating Area",
    subDes: "",
  },
  {
    group: "Auditorium",
    sub: "Stage",
    bldg: ["k12"],
    fc: "20–50 FC",
    fcT: "25 FC",
    lpd_nyc: 0.57,
    lpd_ash: 0.56,
    des: "Audience Seating Area",
    subDes: "",
  },
  // ── GYMNASIUM ─────────────────────────────────────────────────────────────
  {
    group: "Gymnasium",
    sub: "Class IV (Elementary)",
    bldg: ["k12"],
    fc: "30 FC",
    fcT: "30 FC",
    lpd_nyc: 0.72,
    lpd_ash: 0.81,
    des: "Sports Arena",
    subDes: "Class IV Facility",
  },
  {
    group: "Gymnasium",
    sub: "Class III (HS)",
    bldg: ["k12"],
    fc: "50 FC",
    fcT: "50 FC",
    lpd_nyc: 1.08,
    lpd_ash: 1.21,
    des: "Sports Arena",
    subDes: "Class III Facility",
  },
  {
    group: "Gymnasium",
    sub: "Class II (Div 2/3)",
    bldg: ["k12"],
    fc: "75 FC",
    fcT: "75 FC",
    lpd_nyc: 1.45,
    lpd_ash: 1.87,
    des: "Sports Arena",
    subDes: "Class II Facility",
  },
  {
    group: "Gymnasium",
    sub: "Class I (Div. 1)",
    bldg: ["k12"],
    fc: "100 FC",
    fcT: "100 FC",
    lpd_nyc: 2.26,
    lpd_ash: 2.65,
    des: "Sports Arena",
    subDes: "Class I Facility",
  },
  {
    group: "Gymnasium",
    sub: "Play Area",
    bldg: ["k12"],
    fc: "30 FC",
    fcT: "30 FC",
    lpd_nyc: 0.75,
    lpd_ash: 0.78,
    des: "Gymnasium/Fitness Center",
    subDes: "In a playing area",
  },
  // ── WEIGHT ROOM ───────────────────────────────────────────────────────────
  {
    group: "Weight Room",
    sub: "Weight Room",
    bldg: ["k12"],
    fc: "30 FC",
    fcT: "30 FC",
    lpd_nyc: 0.5,
    lpd_ash: 0.78,
    des: "Gymnasium/Fitness Center",
    subDes: "Exercise Area",
  },
  // ── POOL ──────────────────────────────────────────────────────────────────
  {
    group: "Pool",
    sub: "Class IV (Elementary)",
    bldg: ["k12"],
    fc: "30 FC",
    fcT: "30 FC",
    lpd_nyc: 0.59,
    lpd_ash: 0.56,
    des: "Natatorium",
    subDes: "Class IV Facility",
  },
  {
    group: "Pool",
    sub: "Class III (HS)",
    bldg: ["k12"],
    fc: "30 FC",
    fcT: "30 FC",
    lpd_nyc: 0.99,
    lpd_ash: 0.93,
    des: "Natatorium",
    subDes: "Class III Facility",
  },
  {
    group: "Pool",
    sub: "Class II (Div 2/3)",
    bldg: ["k12"],
    fc: "50 FC",
    fcT: "50 FC",
    lpd_nyc: 1.47,
    lpd_ash: 1.39,
    des: "Natatorium",
    subDes: "Class II Facility",
  },
  {
    group: "Pool",
    sub: "Class I (Div. 1)",
    bldg: ["k12"],
    fc: "50 FC",
    fcT: "50 FC",
    lpd_nyc: 2.2,
    lpd_ash: 2.09,
    des: "Natatorium",
    subDes: "Class I Facility",
  },
  // ── STAIRWELL ─────────────────────────────────────────────────────────────
  {
    group: "Stairwell",
    sub: "Interior",
    bldg: ["universal"],
    fc: "2.5–10 FC",
    fcT: "7.5 FC",
    lpd_nyc: 0.47,
    lpd_ash: 0.44,
    des: "Stairwell",
    subDes: "",
  },
  {
    group: "Stairwell",
    sub: "Exterior",
    bldg: ["universal"],
    fc: "1–10 FC",
    fcT: "10 FC",
    lpd_nyc: null,
    lpd_ash: null,
    des: "—",
    subDes: "",
  },
  // ── EXTERIOR ──────────────────────────────────────────────────────────────
  {
    group: "Exterior",
    sub: "Parking Lot",
    bldg: ["universal"],
    fc: "1–3 FC",
    fcT: "1 FC",
    lpd_nyc: null,
    lpd_ash: null,
    des: "Parking Garage",
    subDes: "",
  },
  {
    group: "Exterior",
    sub: "Canopy/Overhang",
    bldg: ["universal"],
    fc: "5–15 FC",
    fcT: "10 FC",
    lpd_nyc: null,
    lpd_ash: null,
    des: "—",
    subDes: "",
  },
  {
    group: "Exterior",
    sub: "Walkway/Entrance",
    bldg: ["universal"],
    fc: "1–5 FC",
    fcT: "3 FC",
    lpd_nyc: null,
    lpd_ash: null,
    des: "—",
    subDes: "",
  },
  {
    group: "Exterior",
    sub: "Façade",
    bldg: ["universal"],
    fc: "—",
    fcT: "—",
    lpd_nyc: null,
    lpd_ash: null,
    des: "—",
    subDes: "",
  },
];

// Building type code mapping (bldgData.type → filter code)
function getBldgTypeCode() {
  const t = (
    document.getElementById("bldgType")?.value ||
    bldgData.type ||
    ""
  ).toLowerCase();
  if (t.includes("k-12") || t.includes("school")) return "k12";
  if (t.includes("higher ed") || t.includes("university")) return "k12"; // higher ed shows K-12 rooms too
  if (t.includes("office") || t.includes("municipal")) return "office";
  if (t.includes("healthcare") || t.includes("hospital")) return "healthcare";
  if (t.includes("warehouse")) return "warehouse";
  if (t.includes("retail")) return "retail";
  if (t.includes("industrial")) return "industrial";
  return "universal"; // all others: universal rooms only
}

// ── PROJECT TYPE ─────────────────────────────────────────────────────────────
function setProjectType(type) {
  bldgData.projectType = type;
  // Update button styles
  const map = { normal: "ptNormal", nycecc: "ptNycEcc", ashrae: "ptAshrae" };
  Object.entries(map).forEach(([k, id]) => {
    const b = document.getElementById(id);
    if (!b) return;
    const active = k === type;
    b.style.background = active ? "#2563a8" : "#f7f9fc";
    b.style.color = active ? "#fff" : "#8a9ab0";
    b.style.borderColor = active ? "#2563a8" : "#c8d4e3";
  });
  bldgSaveDraft();
}

// ── ROOM TYPE POPOVER ─────────────────────────────────────────────────────────
let _rtShowSubs = false; // Normal mode: whether subgroups are enabled
let _rtShowAll = false; // Whether "Show All" is expanded
let _rtGroupPending = null; // For NYC ECC/ASHRAE: the group clicked, waiting for subgroup selection

function openRoomTypePopover() {
  _rtGroupPending = null;
  _rtShowAll = false;
  renderRoomTypePopover();
  document.getElementById("roomTypeModal").style.display = "block";
}

function closeRoomTypePopover() {
  document.getElementById("roomTypeModal").style.display = "none";
  _rtGroupPending = null;
}

function renderRoomTypePopover() {
  const pt = bldgData.projectType || "normal";
  const bCode = getBldgTypeCode();
  const body = document.getElementById("rtModalBody");
  const subBar = document.getElementById("rtSubgroupBar");
  const header = document.getElementById("rtModalHeader");
  const subhead = document.getElementById("rtModalSubhead");

  // Header color and subtitle
  const hdrColors = { normal: "#2563a8", nycecc: "#1a4a2a", ashrae: "#3b1060" };
  header.style.background = hdrColors[pt] || "#2563a8";
  subhead.textContent =
    pt === "nycecc"
      ? "NYC ECC 2025 — tap group → select subtype with LPD"
      : pt === "ashrae"
      ? "ASHRAE 90.1-2022 — tap group → select subtype with LPD"
      : "Normal — select room group";

  // Subgroup toggle: only in Normal mode
  subBar.style.display = pt === "normal" ? "flex" : "none";
  const tog = document.getElementById("rtSubgroupToggle");
  if (tog) {
    tog.textContent = _rtShowSubs ? "Subgroups On ✓" : "Enable Subgroups";
    tog.style.background = _rtShowSubs ? "#2563a8" : "#fff";
    tog.style.color = _rtShowSubs ? "#fff" : "#5a7a9a";
    tog.style.borderColor = _rtShowSubs ? "#2563a8" : "#c8d4e3";
  }

  // --- Normal mode ---
  if (pt === "normal") {
    if (_rtShowSubs) {
      renderRtSubgroupMode(body, bCode, "normal");
    } else {
      renderRtGroupMode(body, bCode);
    }
    return;
  }

  // --- NYC ECC or ASHRAE ---
  if (_rtGroupPending) {
    renderRtSubgroupPanel(body, _rtGroupPending, pt);
  } else {
    renderRtGroupMode(body, bCode);
  }
}

function renderRtGroupMode(body, bCode) {
  const allGroups = [...new Set(ROOM_TYPE_DB.map((r) => r.group))];
  const visGroups = _rtShowAll
    ? allGroups
    : allGroups.filter((g) =>
        ROOM_TYPE_DB.some(
          (r) =>
            r.group === g &&
            (r.bldg.includes("universal") || r.bldg.includes(bCode)),
        ),
      );
  body.innerHTML =
    '<div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px">' +
    visGroups
      .map(
        (g) =>
          '<button class="rt-opt" onclick="rtGroupClick(\'' +
          g.replace(/'/g, "'") +
          "')\">" +
          g +
          "</button>",
      )
      .join("") +
    "</div>";
}

function renderRtSubgroupMode(body, bCode, pt) {
  // Flat list: group header + subs underneath
  const allGroups = [...new Set(ROOM_TYPE_DB.map((r) => r.group))];
  let html = "";
  allGroups.forEach((g) => {
    const subs = ROOM_TYPE_DB.filter(
      (r) =>
        r.group === g &&
        (_rtShowAll || r.bldg.includes("universal") || r.bldg.includes(bCode)),
    );
    if (!subs.length) return;
    html += `<div style="margin-bottom:12px">
      <div style="font-size:10px;font-weight:800;color:#94a3b8;text-transform:uppercase;letter-spacing:.08em;margin-bottom:6px">${g}</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:6px">
        ${subs
          .map(
            (r) => `
          <button class="rt-opt" style="font-size:11px;padding:10px 8px" onclick="rtSubClick('${r.group.replace(
            /'/g,
            "\\'",
          )}','${r.sub.replace(/'/g, "\\'")}')">
            ${r.sub}
          </button>`,
          )
          .join("")}
      </div>
    </div>`;
  });
  body.innerHTML =
    html ||
    '<p style="color:#94a3b8;text-align:center;padding:16px">No room types for this building type</p>';
}

function renderRtSubgroupPanel(body, groupName, pt) {
  const bCode = getBldgTypeCode();
  const rows = ROOM_TYPE_DB.filter(
    (r) =>
      r.group === groupName &&
      (_rtShowAll || r.bldg.includes("universal") || r.bldg.includes(bCode)),
  );
  const lpdKey = pt === "ashrae" ? "lpd_ash" : "lpd_nyc";
  const lpdLabel = pt === "ashrae" ? "ASHRAE 90.1" : "NYC ECC 2025";

  const backBtn = document.createElement("button");
  backBtn.textContent = "← Back to Groups";
  backBtn.style.cssText =
    "margin-bottom:12px;padding:6px 12px;border-radius:8px;border:1.5px solid #c8d4e3;background:#f7f9fc;color:#5a7a9a;font-size:12px;font-weight:700;cursor:pointer;font-family:inherit";
  backBtn.onclick = () => {
    _rtGroupPending = null;
    renderRoomTypePopover();
  };

  const grpLabel = document.createElement("div");
  grpLabel.style.cssText =
    "font-size:11px;font-weight:800;color:#94a3b8;text-transform:uppercase;letter-spacing:.08em;margin-bottom:10px";
  grpLabel.textContent = groupName;

  const list = document.createElement("div");
  list.style.cssText = "display:flex;flex-direction:column;gap:7px";

  rows.forEach((r) => {
    const lpd = r[lpdKey];
    const btn = document.createElement("button");
    btn.style.cssText =
      "width:100%;padding:10px 12px;border-radius:10px;border:1.5px solid #d4b8f0;background:#f3eeff;color:#3a1060;font-family:inherit;cursor:pointer;text-align:left;display:flex;align-items:center;justify-content:space-between";
    btn.onclick = () => rtSubClick(r.group, r.sub);

    const left = document.createElement("div");
    const nameDiv = document.createElement("div");
    nameDiv.style.cssText = "font-size:13px;font-weight:700";
    nameDiv.textContent = r.sub;
    left.appendChild(nameDiv);

    if (r.fc && r.fc !== "—") {
      const fcDiv = document.createElement("div");
      fcDiv.style.cssText = "font-size:10px;color:#8a6ab0;margin-top:2px";
      fcDiv.textContent = "FC: " + r.fc + " · Target: " + r.fcT;
      left.appendChild(fcDiv);
    }
    btn.appendChild(left);

    if (lpd) {
      const right = document.createElement("div");
      right.style.cssText = "flex-shrink:0;margin-left:10px;text-align:right";
      right.innerHTML =
        '<div style="font-size:12px;font-weight:800;color:#1a4a2a">' +
        lpd +
        " W/ft²</div>" +
        '<div style="font-size:9px;color:#5a8a6a">' +
        lpdLabel +
        "</div>";
      btn.appendChild(right);
    }
    list.appendChild(btn);
  });

  body.innerHTML = "";
  body.appendChild(backBtn);
  body.appendChild(grpLabel);
  body.appendChild(list);
}

function rtGroupClick(groupName) {
  const pt = bldgData.projectType || "normal";
  if (pt === "normal" && !_rtShowSubs) {
    // In normal mode without subgroups: select the group directly
    selectRoomType(groupName, groupName);
    return;
  }
  // NYC ECC / ASHRAE or normal+subgroups: drill into subgroup panel
  _rtGroupPending = groupName;
  renderRoomTypePopover();
}

function rtSubClick(group, sub) {
  selectRoomType(sub, sub);
}

function toggleRtSubgroups() {
  _rtShowSubs = !_rtShowSubs;
  renderRoomTypePopover();
}

function toggleRtShowAll() {
  _rtShowAll = !_rtShowAll;
  const btn = document.getElementById("rtShowAllBtn");
  if (btn)
    btn.textContent = _rtShowAll
      ? "− Show Fewer Room Types"
      : "+ Show All Room Types";
  renderRoomTypePopover();
}

function syncProjectTypeButtons() {
  const pt = bldgData.projectType || "normal";
  const map = { normal: "ptNormal", nycecc: "ptNycEcc", ashrae: "ptAshrae" };
  Object.entries(map).forEach(([k, id]) => {
    const b = document.getElementById(id);
    if (!b) return;
    const active = k === pt;
    b.style.background = active ? "#2563a8" : "#f7f9fc";
    b.style.color = active ? "#fff" : "#8a9ab0";
    b.style.borderColor = active ? "#2563a8" : "#c8d4e3";
  });
}
// Source: EPA eGRID 2023 Summary Tables (March 2025), CO2e output emission rates lb/MWh
const EGRID_SUBREGIONS = {
  AKGD: { name: "ASCC Alaska Grid", lbsMWh: 905.1 },
  AKMS: { name: "ASCC Miscellaneous", lbsMWh: 522.4 },
  AZNM: { name: "WECC Southwest", lbsMWh: 706.2 },
  CAMX: { name: "WECC California", lbsMWh: 430.0 },
  ERCT: { name: "ERCOT All", lbsMWh: 736.6 },
  FRCC: { name: "FRCC All", lbsMWh: 784.8 },
  HIMS: { name: "HICC Miscellaneous", lbsMWh: 1133.3 },
  HIOA: { name: "HICC Oahu", lbsMWh: 1498.9 },
  MROE: { name: "MRO East", lbsMWh: 1405.0 },
  MROW: { name: "MRO West", lbsMWh: 926.6 },
  NEWE: { name: "NPCC New England", lbsMWh: 543.2 },
  NWPP: { name: "WECC Northwest", lbsMWh: 635.3 },
  NYCW: { name: "NPCC NYC/Westchester", lbsMWh: 865.7 },
  NYLI: { name: "NPCC Long Island", lbsMWh: 1189.3 },
  NYUP: { name: "NPCC Upstate NY", lbsMWh: 242.8 },
  PRMS: { name: "Puerto Rico Miscellaneous", lbsMWh: 1548.5 },
  RFCE: { name: "RFC East", lbsMWh: 599.2 },
  RFCM: { name: "RFC Michigan", lbsMWh: 976.0 },
  RFCW: { name: "RFC West", lbsMWh: 916.1 },
  RMPA: { name: "WECC Rockies", lbsMWh: 1042.5 },
  SPNO: { name: "SPP North", lbsMWh: 867.7 },
  SPSO: { name: "SPP South", lbsMWh: 875.6 },
  SRMV: { name: "SERC Mississippi Valley", lbsMWh: 741.7 },
  SRMW: { name: "SERC Midwest", lbsMWh: 1248.6 },
  SRSO: { name: "SERC South", lbsMWh: 846.0 },
  SRTV: { name: "SERC Tennessee Valley", lbsMWh: 903.3 },
  SRVC: { name: "SERC Virginia/Carolina", lbsMWh: 596.3 },
};

// State → utilities → eGRID subregion mapping
const UTILITY_DATA = {
  "New York": [
    { name: "Con Edison (NYC & Westchester)", subregion: "NYCW" },
    { name: "PSEG Long Island / LIPA", subregion: "NYLI" },
    { name: "National Grid (Upstate NY)", subregion: "NYUP" },
    { name: "NYSEG", subregion: "NYUP" },
    { name: "Central Hudson", subregion: "NYUP" },
    { name: "Orange & Rockland", subregion: "NYCW" },
    { name: "Rochester Gas & Electric", subregion: "NYUP" },
  ],
  "New Jersey": [
    { name: "PSE&G", subregion: "RFCE" },
    { name: "Jersey Central P&L (FirstEnergy)", subregion: "RFCE" },
    { name: "Atlantic City Electric", subregion: "RFCE" },
    { name: "Rockland Electric", subregion: "RFCE" },
  ],
  Connecticut: [
    { name: "Eversource Energy (CT)", subregion: "NEWE" },
    { name: "United Illuminating", subregion: "NEWE" },
  ],
  Massachusetts: [
    { name: "Eversource Energy (MA)", subregion: "NEWE" },
    { name: "National Grid (MA)", subregion: "NEWE" },
    { name: "Unitil", subregion: "NEWE" },
  ],
  "Rhode Island": [{ name: "National Grid (RI)", subregion: "NEWE" }],
  Vermont: [
    { name: "Green Mountain Power", subregion: "NEWE" },
    { name: "Vermont Electric Coop", subregion: "NEWE" },
  ],
  Maine: [
    { name: "Central Maine Power", subregion: "NEWE" },
    { name: "Versant Power", subregion: "NEWE" },
  ],
  "New Hampshire": [
    { name: "Eversource Energy (NH)", subregion: "NEWE" },
    { name: "Unitil (NH)", subregion: "NEWE" },
  ],
  Pennsylvania: [
    { name: "PECO Energy", subregion: "RFCE" },
    { name: "PPL Electric Utilities", subregion: "RFCE" },
    { name: "Met-Ed (FirstEnergy)", subregion: "RFCE" },
    { name: "Penelec (FirstEnergy)", subregion: "RFCE" },
    { name: "West Penn Power (FirstEnergy)", subregion: "RFCW" },
    { name: "Duquesne Light", subregion: "RFCW" },
    { name: "Penn Power (FirstEnergy)", subregion: "RFCW" },
  ],
  Maryland: [
    { name: "BGE (Baltimore Gas & Electric)", subregion: "RFCE" },
    { name: "Pepco", subregion: "RFCE" },
    { name: "Delmarva Power", subregion: "RFCE" },
    { name: "Potomac Edison (FirstEnergy)", subregion: "RFCE" },
  ],
  Delaware: [{ name: "Delmarva Power (Pepco)", subregion: "RFCE" }],
  Virginia: [
    { name: "Dominion Energy Virginia", subregion: "SRVC" },
    { name: "Appalachian Power (AEP)", subregion: "SRVC" },
    { name: "Rappahannock Electric", subregion: "SRVC" },
  ],
  "North Carolina": [
    { name: "Duke Energy Carolinas", subregion: "SRVC" },
    { name: "Duke Energy Progress", subregion: "SRVC" },
    { name: "Dominion Energy NC", subregion: "SRVC" },
  ],
  "South Carolina": [
    { name: "Duke Energy Carolinas (SC)", subregion: "SRVC" },
    { name: "Dominion Energy SC", subregion: "SRVC" },
    { name: "Santee Cooper", subregion: "SRVC" },
  ],
  Georgia: [
    { name: "Georgia Power", subregion: "SRSO" },
    { name: "Georgia EMC", subregion: "SRSO" },
  ],
  Florida: [
    { name: "Florida Power & Light (FPL)", subregion: "FRCC" },
    { name: "Duke Energy Florida", subregion: "FRCC" },
    { name: "Tampa Electric (TECO)", subregion: "FRCC" },
    { name: "Gulf Power (FPL)", subregion: "FRCC" },
    { name: "JEA", subregion: "FRCC" },
  ],
  Tennessee: [
    { name: "TVA / Local Distributors", subregion: "SRTV" },
    { name: "Memphis Light Gas & Water", subregion: "SRTV" },
  ],
  Alabama: [
    { name: "Alabama Power", subregion: "SRSO" },
    { name: "PowerSouth Energy", subregion: "SRSO" },
  ],
  Mississippi: [
    { name: "Entergy Mississippi", subregion: "SRMV" },
    { name: "Mississippi Power", subregion: "SRSO" },
  ],
  Louisiana: [
    { name: "Entergy Louisiana", subregion: "SRMV" },
    { name: "Cleco Power", subregion: "SRMV" },
    { name: "SWEPCO (AEP)", subregion: "SRMV" },
  ],
  Texas: [
    { name: "Oncor Electric Delivery", subregion: "ERCT" },
    { name: "CenterPoint Energy Houston", subregion: "ERCT" },
    { name: "AEP Texas Central", subregion: "ERCT" },
    { name: "AEP Texas North", subregion: "ERCT" },
    { name: "Texas New Mexico Power", subregion: "ERCT" },
    { name: "Lubbock Power & Light", subregion: "ERCT" },
    { name: "El Paso Electric", subregion: "AZNM" },
  ],
  Illinois: [
    { name: "ComEd (Exelon)", subregion: "MROE" },
    { name: "Ameren Illinois", subregion: "SRMW" },
    { name: "MidAmerican Energy", subregion: "MROW" },
  ],
  Ohio: [
    { name: "AEP Ohio", subregion: "RFCW" },
    { name: "Duke Energy Ohio", subregion: "RFCW" },
    { name: "Ohio Edison (FirstEnergy)", subregion: "RFCW" },
    { name: "Toledo Edison (FirstEnergy)", subregion: "RFCW" },
    { name: "Ohio Power (AEP)", subregion: "RFCW" },
  ],
  Michigan: [
    { name: "DTE Energy (Detroit Edison)", subregion: "RFCM" },
    { name: "Consumers Energy", subregion: "RFCM" },
    { name: "Indiana Michigan Power (AEP)", subregion: "RFCM" },
  ],
  Indiana: [
    { name: "AEP Indiana Michigan Power", subregion: "RFCW" },
    { name: "Duke Energy Indiana", subregion: "RFCW" },
    { name: "Indianapolis Power & Light", subregion: "RFCW" },
    { name: "NIPSCO (NiSource)", subregion: "RFCW" },
  ],
  California: [
    { name: "Pacific Gas & Electric (PG&E)", subregion: "CAMX" },
    { name: "Southern California Edison", subregion: "CAMX" },
    { name: "SDG&E", subregion: "CAMX" },
    { name: "LADWP", subregion: "CAMX" },
    { name: "Sacramento Municipal Utility", subregion: "CAMX" },
  ],
  Washington: [
    { name: "Puget Sound Energy", subregion: "NWPP" },
    { name: "Seattle City Light", subregion: "NWPP" },
    { name: "Pacific Power (PacifiCorp)", subregion: "NWPP" },
    { name: "Tacoma Public Utilities", subregion: "NWPP" },
  ],
  Oregon: [
    { name: "Portland General Electric", subregion: "NWPP" },
    { name: "Pacific Power (PacifiCorp)", subregion: "NWPP" },
    { name: "Eugene Water & Electric", subregion: "NWPP" },
  ],
  Colorado: [
    { name: "Xcel Energy (Public Service CO)", subregion: "RMPA" },
    { name: "Black Hills Energy", subregion: "RMPA" },
    { name: "United Power", subregion: "RMPA" },
  ],
  Arizona: [
    { name: "Arizona Public Service (APS)", subregion: "AZNM" },
    { name: "Salt River Project (SRP)", subregion: "AZNM" },
    { name: "Tucson Electric Power", subregion: "AZNM" },
    { name: "UniSource Energy (TEP)", subregion: "AZNM" },
  ],
  "New Mexico": [
    { name: "PNM (Public Service NM)", subregion: "AZNM" },
    { name: "Xcel Energy (NM)", subregion: "AZNM" },
    { name: "El Paso Electric (NM)", subregion: "AZNM" },
  ],
  Nevada: [
    { name: "NV Energy (Nevada Power)", subregion: "AZNM" },
    { name: "NV Energy (Sierra Pacific)", subregion: "NWPP" },
  ],
  Minnesota: [
    { name: "Xcel Energy (NSP)", subregion: "MROW" },
    { name: "Minnesota Power (ALLETE)", subregion: "MROW" },
    { name: "Great River Energy", subregion: "MROW" },
  ],
  Wisconsin: [
    { name: "We Energies", subregion: "MROE" },
    { name: "WPS (Wisconsin Public Service)", subregion: "MROE" },
    { name: "Madison Gas & Electric", subregion: "MROE" },
    { name: "Alliant Energy (WPS)", subregion: "MROE" },
  ],
  Hawaii: [
    { name: "HECO (Oahu)", subregion: "HIOA" },
    { name: "MECO (Maui)", subregion: "HIMS" },
    { name: "HELCO (Big Island)", subregion: "HIMS" },
  ],
  Alaska: [
    { name: "Chugach Electric", subregion: "AKGD" },
    { name: "Golden Valley Electric", subregion: "AKGD" },
    { name: "AEA / Rural utilities", subregion: "AKMS" },
  ],
  "Puerto Rico": [{ name: "LUMA Energy / PREPA", subregion: "PRMS" }],
};

// eGRID 2023 State-level CO2e output emission rates (lb/MWh)
// Source: EPA eGRID 2023 Summary Tables Table 3, March 2025
const EGRID_STATES = {
  AK: 814.5,
  AL: 714.0,
  AR: 998.4,
  AZ: 689.1,
  CA: 394.8,
  CO: 1091.1,
  CT: 540.7,
  DC: 395.0,
  DE: 704.2,
  FL: 789.1,
  GA: 717.4,
  HI: 1389.2,
  IA: 924.8,
  ID: 237.8,
  IL: 634.1,
  IN: 916.1,
  KS: 867.7,
  KY: 1248.6,
  LA: 741.7,
  MA: 543.2,
  MD: 599.2,
  ME: 543.2,
  MI: 976.0,
  MN: 926.6,
  MO: 1248.6,
  MS: 741.7,
  MT: 635.3,
  NC: 596.3,
  ND: 926.6,
  NE: 926.6,
  NH: 543.2,
  NJ: 599.2,
  NM: 706.2,
  NV: 706.2,
  NY: 541.6,
  OH: 916.1,
  OK: 875.6,
  OR: 635.3,
  PA: 599.2,
  RI: 543.2,
  SC: 596.3,
  SD: 926.6,
  TN: 903.3,
  TX: 736.6,
  UT: 1042.5,
  VA: 596.3,
  VT: 543.2,
  WA: 635.3,
  WI: 976.0,
  WV: 916.1,
  WY: 1042.5,
  PR: 1548.5,
};

// Full state name → abbrev map for lookup
const STATE_ABBREVS = {
  Alaska: "AK",
  Alabama: "AL",
  Arkansas: "AR",
  Arizona: "AZ",
  California: "CA",
  Colorado: "CO",
  Connecticut: "CT",
  Delaware: "DE",
  Florida: "FL",
  Georgia: "GA",
  Hawaii: "HI",
  Idaho: "ID",
  Illinois: "IL",
  Indiana: "IN",
  Iowa: "IA",
  Kansas: "KS",
  Kentucky: "KY",
  Louisiana: "LA",
  Maine: "ME",
  Maryland: "MD",
  Massachusetts: "MA",
  Michigan: "MI",
  Minnesota: "MN",
  Mississippi: "MS",
  Missouri: "MO",
  Montana: "MT",
  Nebraska: "NE",
  Nevada: "NV",
  "New Hampshire": "NH",
  "New Jersey": "NJ",
  "New Mexico": "NM",
  "New York": "NY",
  "North Carolina": "NC",
  "North Dakota": "ND",
  Ohio: "OH",
  Oklahoma: "OK",
  Oregon: "OR",
  Pennsylvania: "PA",
  "Rhode Island": "RI",
  "South Carolina": "SC",
  "South Dakota": "SD",
  Tennessee: "TN",
  Texas: "TX",
  Utah: "UT",
  Vermont: "VT",
  Virginia: "VA",
  Washington: "WA",
  "Washington DC": "DC",
  "West Virginia": "WV",
  Wisconsin: "WI",
  Wyoming: "WY",
  "Puerto Rico": "PR",
};
function openStatePopover() {
  const list = document.getElementById("stateList");
  const states = Object.keys(UTILITY_DATA).sort();
  const cur = bldgData.state || "";
  list.innerHTML = states
    .map(
      (s) =>
        `<button class="hvac-opt${
          s === cur ? " active" : ""
        }" onclick="selectState('${s.replace(/'/g, "\\'")}')">
       ${s}
     </button>`,
    )
    .join("");
  document.getElementById("stateOverlay").style.display = "block";
  document.getElementById("statePopover").style.display = "block";
}
function closeStatePopover() {
  document.getElementById("stateOverlay").style.display = "none";
  document.getElementById("statePopover").style.display = "none";
}
function selectState(state) {
  bldgData.state = state;
  bldgData.utilityName = "";
  bldgData.egridSubregion = "";
  closeStatePopover();
  // Update state button
  const lbl = document.getElementById("bldgStateLabel");
  const btn = document.getElementById("bldgStateBtn");
  if (lbl) {
    lbl.textContent = state;
    lbl.style.color = "#1a2a3a";
  }
  if (btn) {
    btn.style.borderColor = "#2563a8";
    btn.style.background = "#e8f0fb";
  }
  // Show utility button, hide emissions
  const ub = document.getElementById("bldgUtilityBtn");
  const eb = document.getElementById("bldgEmissionsBtn");
  if (ub) {
    ub.style.display = "flex";
    const ul = document.getElementById("bldgUtilityLabel");
    if (ul) {
      ul.textContent = "Select Utility";
      ul.style.color = "#8a9ab0";
    }
    ub.style.borderColor = "#c8d4e3";
    ub.style.background = "#f7f9fc";
  }
  if (eb) eb.style.display = "none";
  // Auto-pick climate zone (single-zone states) or prompt city pick (multi-zone)
  if (typeof autoPickClimateForState === "function")
    autoPickClimateForState(state);
  bldgSaveDraft();
}

function openUtilityPopover() {
  const state = bldgData.state;
  if (!state) {
    toast("Select a state first");
    return;
  }
  const utils = UTILITY_DATA[state] || [];
  const cur = bldgData.utilityName || "";
  document.getElementById("utilityPopoverState").textContent = state;
  document.getElementById("utilityList").innerHTML = utils
    .map(
      (u) =>
        `<button class="hvac-opt${
          u.name === cur ? " active" : ""
        }" onclick="selectUtility('${u.name.replace(/'/g, "\\'")}','${
          u.subregion
        }')">
       <span>${u.name}</span>
       <span style="font-size:10px;color:#8a9ab0;margin-left:8px">${
         u.subregion
       }</span>
     </button>`,
    )
    .join("");
  document.getElementById("utilityOverlay").style.display = "block";
  document.getElementById("utilityPopover").style.display = "block";
}
function closeUtilityPopover() {
  document.getElementById("utilityOverlay").style.display = "none";
  document.getElementById("utilityPopover").style.display = "none";
}
function selectUtility(name, subregion) {
  bldgData.utilityName = name;
  closeUtilityPopover();
  // Update utility button
  const ub = document.getElementById("bldgUtilityBtn");
  const ul = document.getElementById("bldgUtilityLabel");
  if (ul) {
    ul.textContent = name;
    ul.style.color = "#1a2a3a";
  }
  if (ub) {
    ub.style.borderColor = "#2563a8";
    ub.style.background = "#e8f0fb";
  }
  // Auto-select subregion
  selectEgridSubregion(subregion, true);
  bldgSaveDraft();
}

let _emTab = "subregion";
function setEmissionsTab(tab) {
  _emTab = tab;
  document.getElementById("emTabSubregion").style.borderBottomColor =
    tab === "subregion" ? "#1a7a48" : "transparent";
  document.getElementById("emTabSubregion").style.color =
    tab === "subregion" ? "#1a7a48" : "#8a9ab0";
  document.getElementById("emTabState").style.borderBottomColor =
    tab === "state" ? "#1a7a48" : "transparent";
  document.getElementById("emTabState").style.color =
    tab === "state" ? "#1a7a48" : "#8a9ab0";
  renderEmissionsList();
}

function renderEmissionsList() {
  const list = document.getElementById("emissionsList");
  if (!list) return;
  if (_emTab === "subregion") {
    const cur = bldgData.egridSubregion || "";
    list.innerHTML = Object.entries(EGRID_SUBREGIONS)
      .map(
        ([code, d]) =>
          `<button class="hvac-opt${
            code === cur ? " active" : ""
          }" onclick="selectEgridSubregion('${code}',false)">
         <div style="display:flex;justify-content:space-between;align-items:center;width:100%">
           <div><span style="font-weight:800">${code}</span>
             <span style="color:#5a7a9a;margin-left:6px;font-weight:600">${
               d.name
             }</span></div>
           <span style="font-size:11px;font-weight:700;color:#1a7a48;flex-shrink:0;margin-left:8px">${d.lbsMWh.toLocaleString()} lb/MWh</span>
         </div>
       </button>`,
      )
      .join("");
  } else {
    // Statewide: show states in our UTILITY_DATA list that have a state abbrev mapping
    const curState = bldgData.state || "";
    const curAbbrev = STATE_ABBREVS[curState] || "";
    const curRate = bldgData.egridStateRate || 0;
    list.innerHTML = Object.entries(STATE_ABBREVS)
      .filter(([name]) => EGRID_STATES[STATE_ABBREVS[name]])
      .sort(([a], [b]) => a.localeCompare(b))
      .map(([name, abbrev]) => {
        const rate = EGRID_STATES[abbrev] || 0;
        const isActive =
          bldgData.egridMode === "state" &&
          abbrev === (bldgData.egridStateAbbrev || curAbbrev);
        return `<button class="hvac-opt${
          isActive ? " active" : ""
        }" onclick="selectStateEmissions('${abbrev}','${name}')">
          <div style="display:flex;justify-content:space-between;align-items:center;width:100%">
            <div><span style="font-weight:800">${abbrev}</span>
              <span style="color:#5a7a9a;margin-left:6px;font-weight:600">${name}</span></div>
            <span style="font-size:11px;font-weight:700;color:#1a7a48;flex-shrink:0;margin-left:8px">${rate.toLocaleString()} lb/MWh</span>
          </div>
        </button>`;
      })
      .join("");
  }
}

function openEmissionsPopover() {
  const note = document.getElementById("emissionsAutoNote");
  if (note) note.style.display = "none";
  // Default tab based on current mode
  setEmissionsTab(bldgData.egridMode === "state" ? "state" : "subregion");
  document.getElementById("emissionsOverlay").style.display = "block";
  document.getElementById("emissionsPopover").style.display = "block";
}
function closeEmissionsPopover() {
  document.getElementById("emissionsOverlay").style.display = "none";
  document.getElementById("emissionsPopover").style.display = "none";
}
function selectStateEmissions(abbrev, name) {
  const rate = EGRID_STATES[abbrev];
  if (!rate) return;
  bldgData.egridMode = "state";
  bldgData.egridStateAbbrev = abbrev;
  bldgData.egridSubregion = "";
  closeEmissionsPopover();
  const el = document.getElementById("bldgEmissionsLabel"),
    eb = document.getElementById("bldgEmissionsBtn");
  if (el) {
    el.textContent = `${abbrev} Statewide · ${rate.toLocaleString()} lb/MWh`;
    el.style.color = "#1a7a48";
  }
  if (eb) {
    eb.style.display = "flex";
    eb.style.borderColor = "#1a7a48";
    eb.style.background = "#dcfce7";
  }
  toast(`✔ ${name} statewide average — ${rate.toLocaleString()} lb CO₂e/MWh`);
  bldgSaveDraft();
}

function selectEgridSubregion(code, autoSelected) {
  const d = EGRID_SUBREGIONS[code];
  if (!d) return;
  bldgData.egridSubregion = code;
  bldgData.egridMode = "subregion";
  bldgData.egridStateAbbrev = "";
  closeEmissionsPopover();
  const eb = document.getElementById("bldgEmissionsBtn");
  const el = document.getElementById("bldgEmissionsLabel");
  if (el) {
    el.textContent = `${code} · ${d.lbsMWh.toLocaleString()} lb/MWh`;
    el.style.color = "#1a7a48";
  }
  if (eb) {
    eb.style.display = "flex";
    eb.style.borderColor = "#1a7a48";
    eb.style.background = "#dcfce7";
  }
  if (autoSelected) {
    const note = document.getElementById("emissionsAutoNote");
    if (note) note.style.display = "block";
    toast(`✔ ${code} auto-selected — ${d.lbsMWh.toLocaleString()} lb CO₂e/MWh`);
  }
  bldgSaveDraft();
}

// Convert kWh saved → metric tons CO2e using selected emissions factor
function kwhToMtCO2e(kwhSaved) {
  let lbsMWh = 770.9; // US average fallback
  if (bldgData.egridMode === "state" && bldgData.egridStateAbbrev) {
    lbsMWh = EGRID_STATES[bldgData.egridStateAbbrev] || lbsMWh;
  } else if (
    bldgData.egridSubregion &&
    EGRID_SUBREGIONS[bldgData.egridSubregion]
  ) {
    lbsMWh = EGRID_SUBREGIONS[bldgData.egridSubregion].lbsMWh;
  }
  return (kwhSaved / 1000) * lbsMWh * 0.000453592;
}
// WHFc = Cooling waste heat factor (positive — less lighting heat → less cooling load → adds to electric savings)
// WHFh = Heating waste heat factor (negative — less lighting heat → more heating needed → MMBtu penalty)
// Source: NY TRM V11, C/I Interior Lighting measure, Appendix D
const TRM_WHF = {
  // Gas heat systems
  "Gas Heat / Electric Cooling": { WHFc: 0.25, WHFh: -0.08 },
  "Gas Heat / No Cooling": { WHFc: 0.0, WHFh: -0.08 },
  "Gas Heat / Gas Cooling (Absorption)": { WHFc: 0.0, WHFh: -0.08 },
  // Oil heat systems
  "Oil Heat / Electric Cooling": { WHFc: 0.25, WHFh: -0.08 },
  "Oil Heat / No Cooling": { WHFc: 0.0, WHFh: -0.08 },
  // Electric systems
  "Electric Resistance / Electric Cooling": { WHFc: 0.25, WHFh: -0.74 },
  "Electric Resistance / No Cooling": { WHFc: 0.0, WHFh: -0.74 },
  "Heat Pump / Electric Cooling": { WHFc: 0.25, WHFh: -0.25 },
  // Steam / District
  "Steam or District Heat / Electric Cooling": { WHFc: 0.25, WHFh: -0.03 },
  "Steam or District Heat / No Cooling": { WHFc: 0.0, WHFh: -0.03 },
  // No heat / unconditioned
  "No Heat / Electric Cooling": { WHFc: 0.25, WHFh: 0.0 },
  "No Heat / No Cooling (Unconditioned)": { WHFc: 0.0, WHFh: 0.0 },
};

// Default (unknown / not selected) — conservative: no HVAC interaction assumed
const TRM_WHF_DEFAULT = { WHFc: 0.0, WHFh: 0.0 };

function getHvacFactors() {
  return TRM_WHF[bldgData.hvacType] || TRM_WHF_DEFAULT;
}

// ── CLIMATE ZONE ──────────────────────────────────────────────────────────
// ASHRAE climate zone → HVAC interaction factors (cooling benefit, heating penalty)
const CLIMATE_ZONES = {
  hot: {
    label: "Hot",
    ashrae: "1A–2B",
    cities: "Miami · Houston · Phoenix · Las Vegas · New Orleans",
    HVACc: 0.04,
    HVACff: -0.0002,
    emoji: "🌞",
    color: "#c04020",
    bg: "#fef3ee",
    border: "#f0b090",
  },
  mixed: {
    label: "Mixed",
    ashrae: "3A–4A",
    cities: "Atlanta · Dallas · DC · Charlotte · Memphis",
    HVACc: 0.025,
    HVACff: -0.0005,
    emoji: "🌤",
    color: "#b07020",
    bg: "#fefaee",
    border: "#e8d090",
  },
  cold_mixed: {
    label: "Cold / Mixed",
    ashrae: "4B–5B",
    cities: "NYC · Chicago · Denver · Seattle · Philadelphia",
    HVACc: 0.02,
    HVACff: -0.0008,
    emoji: "🌥",
    color: "#2060a8",
    bg: "#eef4fc",
    border: "#90b8e8",
  },
  cold: {
    label: "Cold",
    ashrae: "6A–7",
    cities: "Minneapolis · Buffalo · Anchorage · Green Bay · Duluth",
    HVACc: 0.01,
    HVACff: -0.0012,
    emoji: "❄",
    color: "#1a4a8a",
    bg: "#eaf0fc",
    border: "#7090d0",
  },
};

const STATE_CLIMATE_ZONE = {
  Alabama: "hot",
  Alaska: "cold",
  Arizona: "hot",
  Arkansas: "hot",
  California: null,
  Colorado: null,
  Connecticut: "cold_mixed",
  Delaware: "mixed",
  Florida: null,
  Georgia: "mixed",
  Hawaii: "hot",
  Idaho: "cold",
  Illinois: "cold_mixed",
  Indiana: "cold_mixed",
  Iowa: "cold",
  Kansas: "mixed",
  Kentucky: "mixed",
  Louisiana: "hot",
  Maine: "cold",
  Maryland: "mixed",
  Massachusetts: "cold_mixed",
  Michigan: "cold",
  Minnesota: "cold",
  Mississippi: "hot",
  Missouri: "mixed",
  Montana: "cold",
  Nebraska: "cold_mixed",
  Nevada: null,
  "New Hampshire": "cold",
  "New Jersey": "mixed",
  "New Mexico": "mixed",
  "New York": null,
  "North Carolina": "mixed",
  "North Dakota": "cold",
  Ohio: "cold_mixed",
  Oklahoma: "hot",
  Oregon: null,
  Pennsylvania: "cold_mixed",
  "Rhode Island": "cold_mixed",
  "South Carolina": "mixed",
  "South Dakota": "cold",
  Tennessee: "mixed",
  Texas: null,
  Utah: "cold_mixed",
  Vermont: "cold",
  Virginia: "mixed",
  Washington: null,
  "Washington DC": "mixed",
  "West Virginia": "cold_mixed",
  Wisconsin: "cold",
  Wyoming: "cold",
};

const STATE_CITY_ZONES = {
  California: [
    { city: "Los Angeles / San Diego", zone: "hot" },
    { city: "Sacramento / Fresno", zone: "mixed" },
    { city: "San Francisco / Oakland", zone: "cold_mixed" },
    { city: "Lake Tahoe / Mammoth", zone: "cold" },
  ],
  Colorado: [
    { city: "Denver / Colorado Springs", zone: "cold_mixed" },
    { city: "Aspen / Vail / Pueblo", zone: "cold" },
  ],
  Florida: [
    { city: "Miami / Fort Lauderdale / Key West", zone: "hot" },
    { city: "Orlando / Tampa / Jacksonville", zone: "hot" },
    { city: "Pensacola / Tallahassee", zone: "mixed" },
  ],
  Nevada: [
    { city: "Las Vegas", zone: "hot" },
    { city: "Reno / Carson City", zone: "cold_mixed" },
  ],
  "New York": [
    { city: "NYC / Long Island / Yonkers", zone: "cold_mixed" },
    { city: "Albany / Poughkeepsie", zone: "cold_mixed" },
    { city: "Buffalo / Rochester / Syracuse", zone: "cold" },
  ],
  Oregon: [
    { city: "Portland / Eugene", zone: "cold_mixed" },
    { city: "Bend / Medford / Eastern OR", zone: "cold" },
  ],
  Texas: [
    { city: "Houston / San Antonio / Austin", zone: "hot" },
    { city: "Dallas / Fort Worth / Waco", zone: "mixed" },
    { city: "Amarillo / Lubbock / El Paso", zone: "cold_mixed" },
  ],
  Washington: [
    { city: "Seattle / Tacoma / Olympia", zone: "cold_mixed" },
    { city: "Spokane / Yakima / Eastern WA", zone: "cold" },
  ],
};

function openClimateZonePopover() {
  renderClimateZoneList();
  const picker = document.getElementById("climateCityPicker");
  if (picker) picker.style.display = "none";
  const autoNote = document.getElementById("climateAutoNote");
  if (autoNote)
    autoNote.style.display = bldgData.climateZone ? "block" : "none";

  const stateName = bldgData.state || "";
  // If multi-zone state and no climate yet, show city picker
  if (stateName && STATE_CITY_ZONES[stateName] && !bldgData.climateZone) {
    renderClimateCityPicker(stateName);
  }
  document.getElementById("climateZoneOverlay").style.display = "block";
  document.getElementById("climateZonePopover").style.display = "block";
}

function closeClimateZonePopover() {
  document.getElementById("climateZoneOverlay").style.display = "none";
  document.getElementById("climateZonePopover").style.display = "none";
}

function renderClimateZoneList() {
  const list = document.getElementById("climateZoneList");
  if (!list) return;
  const cur = bldgData.climateZone || "";
  list.innerHTML = Object.entries(CLIMATE_ZONES)
    .map(
      ([key, z]) => `
    <button onclick="selectClimateZone('${key}', false)"
      style="padding:12px 14px;border-radius:10px;border:1.5px solid ${
        cur === key ? z.color : z.border
      };background:${
        cur === key ? z.bg : "#fafbfc"
      };cursor:pointer;font-family:inherit;text-align:left;transition:all .15s;display:flex;align-items:flex-start;gap:12px">
      <span style="font-size:22px;line-height:1;flex-shrink:0;margin-top:2px">${
        z.emoji
      }</span>
      <div style="flex:1;min-width:0">
        <div style="display:flex;align-items:baseline;gap:8px;flex-wrap:wrap">
          <span style="font-size:13px;font-weight:800;color:${z.color}">${
        z.label
      }</span>
          <span style="font-size:10px;color:#8a9ab0;font-weight:600">ASHRAE ${
            z.ashrae
          }</span>
        </div>
        <div style="font-size:11px;color:#5a7a9a;margin-top:3px">${
          z.cities
        }</div>
        <div style="display:flex;gap:16px;margin-top:6px">
          <span style="font-size:10px;font-weight:700;color:#1a7a48">HVACc: ${z.HVACc.toFixed(
            3,
          )}</span>
          <span style="font-size:10px;font-weight:700;color:#c04040">HVACff: ${z.HVACff.toFixed(
            4,
          )}</span>
        </div>
      </div>
      ${
        cur === key
          ? '<span style="color:' +
            z.color +
            ';font-size:16px;flex-shrink:0">✓</span>'
          : ""
      }
    </button>`,
    )
    .join("");
}

function renderClimateCityPicker(stateName) {
  const cities = STATE_CITY_ZONES[stateName];
  if (!cities) return;
  const picker = document.getElementById("climateCityPicker");
  const cityList = document.getElementById("climateCityList");
  if (!picker || !cityList) return;
  cityList.innerHTML = cities
    .map(({ city, zone }) => {
      const z = CLIMATE_ZONES[zone];
      return `
      <button onclick="selectClimateZone('${zone}', false)"
        style="padding:10px 12px;border-radius:9px;border:1.5px solid ${
          z.border
        };background:${
        z.bg
      };cursor:pointer;font-family:inherit;text-align:left;display:flex;align-items:center;justify-content:space-between;gap:10px">
        <div>
          <div style="font-size:12px;font-weight:700;color:#1a2a3a">${city}</div>
          <div style="font-size:10px;color:${
            z.color
          };font-weight:700;margin-top:2px">${z.emoji} ${z.label} · ASHRAE ${
        z.ashrae
      }</div>
        </div>
        <div style="text-align:right;flex-shrink:0">
          <div style="font-size:10px;color:#1a7a48;font-weight:700">HVACc ${z.HVACc.toFixed(
            3,
          )}</div>
          <div style="font-size:10px;color:#c04040;font-weight:700">HVACff ${z.HVACff.toFixed(
            4,
          )}</div>
        </div>
      </button>`;
    })
    .join("");
  picker.style.display = "block";
}

function selectClimateZone(zoneKey, autoSelected) {
  const z = CLIMATE_ZONES[zoneKey];
  if (!z) return;
  bldgData.climateZone = zoneKey;
  bldgData.HVACc = z.HVACc;
  bldgData.HVACff = z.HVACff;
  closeClimateZonePopover();
  const btn = document.getElementById("bldgClimateBtn");
  const lbl = document.getElementById("bldgClimateLabel");
  if (lbl) {
    lbl.textContent = `${z.emoji}  ${z.label}  —  ${z.cities
      .split("·")[0]
      .trim()}…`;
    lbl.style.color = z.color;
  }
  if (btn) {
    btn.style.borderColor = z.color;
    btn.style.background = z.bg;
  }
  if (autoSelected) {
    const note = document.getElementById("climateAutoNote");
    if (note) note.style.display = "block";
    if (typeof toast === "function")
      toast(
        `✔ ${z.label} climate zone auto-selected — HVACc ${z.HVACc.toFixed(
          3,
        )}, HVACff ${z.HVACff.toFixed(4)}`,
      );
  }
  bldgSaveDraft();
}

function autoPickClimateForState(stateName) {
  if (!stateName) return;
  const zoneKey = STATE_CLIMATE_ZONE[stateName];
  if (zoneKey) {
    // Single-zone state — auto-select silently
    selectClimateZone(zoneKey, true);
  } else if (STATE_CITY_ZONES[stateName]) {
    // Multi-zone — reset, prompt user to pick a city
    bldgData.climateZone = "";
    bldgData.HVACc = undefined;
    bldgData.HVACff = undefined;
    const lbl = document.getElementById("bldgClimateLabel");
    const btn = document.getElementById("bldgClimateBtn");
    if (lbl) {
      lbl.textContent = `Pick city for ${stateName}…`;
      lbl.style.color = "#a04a20";
    }
    if (btn) {
      btn.style.borderColor = "#f0a030";
      btn.style.background = "#fffaee";
    }
  }
}

function restoreClimateZoneButton() {
  if (!bldgData.climateZone) return;
  const z = CLIMATE_ZONES[bldgData.climateZone];
  if (!z) return;
  const btn = document.getElementById("bldgClimateBtn");
  const lbl = document.getElementById("bldgClimateLabel");
  if (lbl) {
    lbl.textContent = `${z.emoji}  ${z.label}  —  ${z.cities
      .split("·")[0]
      .trim()}…`;
    lbl.style.color = z.color;
  }
  if (btn) {
    btn.style.borderColor = z.color;
    btn.style.background = z.bg;
  }
}

// Returns HVACc (cooling benefit factor) and HVACff (heating penalty factor)
// for the current project. Defaults to Cold/Mixed (NYC) if unset.
function getClimateFactors() {
  if (bldgData.HVACc != null && bldgData.HVACff != null) {
    return { HVACc: bldgData.HVACc, HVACff: bldgData.HVACff };
  }
  return { HVACc: 0.02, HVACff: -0.0008 };
}

function openHvacPopover() {
  document.getElementById("hvacOverlay").style.display = "block";
  document.getElementById("hvacPopover").style.display = "block";
  // Highlight current selection
  const cur = bldgData.hvacType || "";
  document.querySelectorAll(".hvac-opt").forEach((b) => {
    b.classList.toggle("active", b.textContent.trim() === cur);
  });
}

function closeHvacPopover() {
  document.getElementById("hvacOverlay").style.display = "none";
  document.getElementById("hvacPopover").style.display = "none";
}

function selectHvac(val) {
  bldgData.hvacType = val;
  const btn = document.getElementById("bldgHvacBtn");
  const lbl = document.getElementById("bldgHvacLabel");
  if (lbl) {
    lbl.textContent = val;
    lbl.style.color = "#1a2a3a";
  }
  if (btn) {
    btn.style.borderColor = "#2563a8";
    btn.style.background = "#e8f0fb";
  }
  document.querySelectorAll(".hvac-opt").forEach((b) => {
    b.classList.toggle("active", b.textContent.trim() === val);
  });
  bldgSaveDraft();
}

// ── EMERGENCY LIGHTING (building-wide EM strategy) ────────────────────────
function openEmLightingPopover() {
  document.getElementById("emLightingOverlay").style.display = "block";
  document.getElementById("emLightingPopover").style.display = "block";
  const cur = bldgData.emLighting || "";
  document.querySelectorAll("#emLightingPopover .hvac-opt").forEach((b) => {
    // Compare by the first text node only — labels include subtitles
    const first = (
      (b.firstChild && b.firstChild.nodeValue) ||
      b.textContent
    ).trim();
    b.classList.toggle("active", first === cur);
  });
}
function closeEmLightingPopover() {
  document.getElementById("emLightingOverlay").style.display = "none";
  document.getElementById("emLightingPopover").style.display = "none";
}
function selectEmLighting(val) {
  bldgData.emLighting = val;
  const btn = document.getElementById("bldgEmBtn");
  const lbl = document.getElementById("bldgEmLabel");
  if (lbl) {
    lbl.textContent = val;
    lbl.style.color = "#1a2a3a";
  }
  if (btn) {
    btn.style.borderColor = "#2563a8";
    btn.style.background = "#e8f0fb";
  }
  document.querySelectorAll("#emLightingPopover .hvac-opt").forEach((b) => {
    const first = (
      (b.firstChild && b.firstChild.nodeValue) ||
      b.textContent
    ).trim();
    b.classList.toggle("active", first === val);
  });
  bldgSaveDraft();
}

function openRoomTypeHours() {
  rthMode = "annual";
  rthRenderRows();
  document.getElementById("roomTypeHoursModal").style.display = "block";
}

function closeRoomTypeHours() {
  if (!bldgData.roomTypeHours) bldgData.roomTypeHours = {};
  getRthGroups().forEach((rt) => {
    const el = document.getElementById("rth-" + rt.key);
    if (el) {
      const val = parseInt(el.value);
      if (val && !isNaN(val)) bldgData.roomTypeHours[rt.key] = val;
    }
  });
  document.getElementById("roomTypeHoursModal").style.display = "none";
  bldgSaveDraft();
  updateHoursIndicator();
  toast("✔ Room type hours saved");
}

function updateHoursIndicator() {
  const ind = document.getElementById("bldgHoursIndicator");
  const txt = document.getElementById("bldgHoursIndicatorText");
  if (!ind || !txt) return;
  const overrides = bldgData.roomTypeHours
    ? Object.keys(bldgData.roomTypeHours)
    : [];
  if (overrides.length === 0) {
    ind.style.display = "none";
    return;
  }
  const baseHrs = bldgData.hours || 2000;
  const parts = overrides.map((k) => {
    const hrs = bldgData.roomTypeHours[k];
    return `${k}: ${hrs.toLocaleString()} hrs`;
  });
  txt.textContent = `Room-type hours active — overriding ${baseHrs.toLocaleString()} hr baseline for: ${parts.join(
    " · ",
  )}`;
  ind.style.display = "flex";
}

function rthSetMode(mode) {
  rthMode = mode;
  document.getElementById("rthModeAnnual").style.background =
    mode === "annual" ? "#2563a8" : "#fff";
  document.getElementById("rthModeAnnual").style.color =
    mode === "annual" ? "#fff" : "#5a7a9a";
  document.getElementById("rthModeAnnual").style.borderColor =
    mode === "annual" ? "#2563a8" : "#c8d4e3";
  document.getElementById("rthModeSchedule").style.background =
    mode === "schedule" ? "#2563a8" : "#fff";
  document.getElementById("rthModeSchedule").style.color =
    mode === "schedule" ? "#fff" : "#5a7a9a";
  document.getElementById("rthModeSchedule").style.borderColor =
    mode === "schedule" ? "#2563a8" : "#c8d4e3";
  rthRenderRows();
}

function rthGetHours(key) {
  return (bldgData.roomTypeHours && bldgData.roomTypeHours[key]) || null;
}

function rthSetHours(key, hrs) {
  if (!bldgData.roomTypeHours) bldgData.roomTypeHours = {};
  if (!hrs || isNaN(hrs)) delete bldgData.roomTypeHours[key];
  else bldgData.roomTypeHours[key] = Math.round(hrs);
  try {
    localStorage.setItem(BLDG_KEY, JSON.stringify(bldgData));
  } catch (e) {}
}

function rthCalcFromSchedule(key) {
  const hd = parseFloat(document.getElementById("rth-hd-" + key)?.value) || 0;
  const dw = parseFloat(document.getElementById("rth-dw-" + key)?.value) || 0;
  const wy = parseFloat(document.getElementById("rth-wy-" + key)?.value) || 50;
  if (!hd || !dw) return;
  const total = Math.round(hd * dw * wy);
  const res = document.getElementById("rth-res-" + key);
  if (res) res.textContent = total.toLocaleString() + " hr/yr";
  rthSetHours(key, total);
}

function rthRenderRows() {
  const bldgHrs =
    parseInt(document.getElementById("bldgHoursInput")?.value) || 2000;
  const wrap = document.getElementById("rthRows");
  const groups = getRthGroups();
  wrap.innerHTML = groups
    .map((rt) => {
      const saved = rthGetHours(rt.key);
      const displayHrs = saved || rt.defaultHrs;
      if (rthMode === "annual") {
        return `<div class="rth-row">
        <div style="flex:1;min-width:0">
          <div class="rth-label">${rt.label}</div>
        </div>
        <input class="rth-input" id="rth-${
          rt.key
        }" type="number" min="0" max="8760"
          value="${displayHrs}" placeholder="${bldgHrs}"
          oninput="rthSetHours('${rt.key}',parseInt(this.value))"
          style="${saved ? "border-color:#2563a8;background:#f0f5ff" : ""}">
        <span style="font-size:11px;color:#94a3b8;margin-left:4px;width:30px">hr/yr</span>
      </div>`;
      } else {
        const hd = saved
          ? (saved / (5 * 50)).toFixed(1)
          : (rt.defaultHrs / (5 * 50)).toFixed(1);
        return `<div class="rth-row" style="flex-wrap:wrap;gap:6px">
        <div style="flex:1;min-width:80px">
          <div class="rth-label">${rt.label}</div>
        </div>
        <div class="rth-sched">
          <input id="rth-hd-${
            rt.key
          }" type="number" min="0.5" max="24" step="0.5" value="${hd}" placeholder="hr"
            oninput="rthCalcFromSchedule('${rt.key}')">
          <span>hr/day ×</span>
          <input id="rth-dw-${
            rt.key
          }" type="number" min="1" max="7" step="1" value="5" placeholder="days"
            oninput="rthCalcFromSchedule('${rt.key}')">
          <span>d/wk ×</span>
          <input id="rth-wy-${
            rt.key
          }" type="number" min="1" max="52" step="1" value="50" placeholder="wks"
            oninput="rthCalcFromSchedule('${rt.key}')">
          <span>wk/yr</span>
        </div>
        <div class="rth-result" id="rth-res-${
          rt.key
        }">${displayHrs.toLocaleString()} hr/yr</div>
      </div>`;
      }
    })
    .join("");
}

function rthApplyToAll() {
  const bldgHrs =
    parseInt(document.getElementById("bldgHoursInput")?.value) || 2000;
  if (!bldgData.roomTypeHours) bldgData.roomTypeHours = {};
  getRthGroups().forEach((rt) => {
    bldgData.roomTypeHours[rt.key] = bldgHrs;
    const el = document.getElementById("rth-" + rt.key);
    if (el) {
      el.value = bldgHrs;
      el.style.borderColor = "";
      el.style.background = "";
    }
  });
  try {
    localStorage.setItem(BLDG_KEY, JSON.stringify(bldgData));
  } catch (e) {}
  toast(
    "✔ Building baseline (" +
      bldgHrs.toLocaleString() +
      " hr/yr) applied to all room types",
  );
}

function rthResetAll() {
  bldgData.roomTypeHours = {};
  try {
    localStorage.setItem(BLDG_KEY, JSON.stringify(bldgData));
  } catch (e) {}
  rthRenderRows();
  toast("Room type hours reset to defaults");
}

// Helper: get hours for a given room type label (used during audit for energy calc)
// roomTypeLabel can be a group name ("Office") or subtype ("Private Office") or legacy key
function getHoursForRoomType(roomTypeLabel) {
  if (!roomTypeLabel) return bldgData.hours || 2000;
  // Check explicit user override — key is the group name
  if (bldgData.roomTypeHours) {
    // Direct key match (group name)
    if (bldgData.roomTypeHours[roomTypeLabel] != null) {
      return bldgData.roomTypeHours[roomTypeLabel];
    }
    // If it's a subtype, find its parent group
    const row = ROOM_TYPE_DB.find((r) => r.sub === roomTypeLabel);
    if (row && bldgData.roomTypeHours[row.group] != null) {
      return bldgData.roomTypeHours[row.group];
    }
  }
  // Fall back to building baseline, then RTH_DEFAULTS for the group
  const row = ROOM_TYPE_DB.find(
    (r) => r.group === roomTypeLabel || r.sub === roomTypeLabel,
  );
  const groupKey = row ? row.group : roomTypeLabel;
  return bldgData.hours || RTH_DEFAULTS[groupKey] || 2000;
}

const BLDG_KEY = "auditTool_building_v1";

let bldgData = {
  name: "",
  address: "",
  client: "",
  auditor: "",
  type: "",
  sqft: "",
  year: "",
  utility: "",
  hours: 2000,
  notes: "",
};

function openBuildingPage() {
  bldgLoadData();
  bldgUpdateStats();
  document.getElementById("bldgPage").classList.add("visible");
}

function closeBuildingPage() {
  document.getElementById("bldgPage").classList.remove("visible");
}

function bldgLoadData() {
  try {
    const raw = localStorage.getItem(BLDG_KEY);
    if (raw) bldgData = { ...bldgData, ...JSON.parse(raw) };
  } catch (e) {}
  document.getElementById("bldgName").textContent = bldgData.name || "";
  document.getElementById("bldgAddress").textContent = bldgData.address || "";
  document.getElementById("bldgClient").value = bldgData.client || "";
  document.getElementById("bldgAuditor").value = bldgData.auditor || "";
  document.getElementById("bldgType").value = bldgData.type || "";
  document.getElementById("bldgSqft").value = bldgData.sqft || "";
  document.getElementById("bldgYear").value = bldgData.year || "";
  document.getElementById("bldgUtility").value = bldgData.utility || "";
  document.getElementById("bldgNotes").value = bldgData.notes || "";
  const hrs = bldgData.hours || 2000;
  const hi = document.getElementById("bldgHoursInput");
  if (hi) hi.value = hrs;
  // Sync preset buttons
  syncHoursPresets(hrs);
  // Wire up autosave on editable fields
  ["bldgName", "bldgAddress"].forEach((id) => {
    const el = document.getElementById(id);
    el.oninput = bldgSaveDraft;
  });
  [
    "bldgClient",
    "bldgAuditor",
    "bldgType",
    "bldgSqft",
    "bldgYear",
    "bldgUtility",
  ].forEach((id) => {
    const el = document.getElementById(id);
    if (el) el.oninput = bldgSaveDraft;
  });
}

function bldgSaveDraft() {
  bldgData = {
    name: document.getElementById("bldgName").textContent.trim(),
    address: document.getElementById("bldgAddress").textContent.trim(),
    client: document.getElementById("bldgClient").value.trim(),
    auditor: document.getElementById("bldgAuditor").value.trim(),
    type: document.getElementById("bldgType").value,
    sqft: document.getElementById("bldgSqft").value,
    year: document.getElementById("bldgYear").value,
    utility: document.getElementById("bldgUtility").value.trim(),
    hours: parseInt(document.getElementById("bldgHoursInput")?.value) || 2000,
    notes: document.getElementById("bldgNotes").value.trim(),
    roomTypeHours: bldgData.roomTypeHours || {},
    hvacType: bldgData.hvacType || "",
    emLighting: bldgData.emLighting || "",
    projectType: bldgData.projectType || "normal",
    state: bldgData.state || "",
    utilityName: bldgData.utilityName || "",
    egridSubregion: bldgData.egridSubregion || "",
    egridMode: bldgData.egridMode || "subregion",
    egridStateAbbrev: bldgData.egridStateAbbrev || "",
    climateZone: bldgData.climateZone || "",
    HVACc: bldgData.HVACc !== undefined ? bldgData.HVACc : null,
    HVACff: bldgData.HVACff !== undefined ? bldgData.HVACff : null,
  };
  try {
    localStorage.setItem(BLDG_KEY, JSON.stringify(bldgData));
  } catch (e) {}
  // Sync the active building's name in the projects registry so the projects
  // landing page reflects any rename done on the building profile.
  if (typeof _currentBuilding === "function") {
    const b = _currentBuilding();
    if (b && bldgData.name && b.name !== bldgData.name) {
      b.name = bldgData.name;
      if (typeof saveProjectsRegistry === "function") saveProjectsRegistry();
    }
  }
  // Update top bar label
  const topName = document.getElementById("topBldgName");
  if (topName) topName.textContent = bldgData.name || "Building";
  // Restore state/utility/emissions buttons
  if (bldgData.state) {
    const sl = document.getElementById("bldgStateLabel"),
      sb = document.getElementById("bldgStateBtn");
    if (sl) {
      sl.textContent = bldgData.state;
      sl.style.color = "#1a2a3a";
    }
    if (sb) {
      sb.style.borderColor = "#2563a8";
      sb.style.background = "#e8f0fb";
    }
    const ub = document.getElementById("bldgUtilityBtn");
    if (ub) ub.style.display = "flex";
  }
  if (bldgData.utilityName) {
    const ul = document.getElementById("bldgUtilityLabel"),
      ub = document.getElementById("bldgUtilityBtn");
    if (ul) {
      ul.textContent = bldgData.utilityName;
      ul.style.color = "#1a2a3a";
    }
    if (ub) {
      ub.style.borderColor = "#2563a8";
      ub.style.background = "#e8f0fb";
    }
  }
  if (
    bldgData.egridSubregion ||
    (bldgData.egridMode === "state" && bldgData.egridStateAbbrev)
  ) {
    const eb = document.getElementById("bldgEmissionsBtn");
    const el = document.getElementById("bldgEmissionsLabel");
    if (bldgData.egridMode === "state" && bldgData.egridStateAbbrev) {
      const rate = EGRID_STATES[bldgData.egridStateAbbrev];
      if (rate && el) {
        el.textContent = `${
          bldgData.egridStateAbbrev
        } Statewide · ${rate.toLocaleString()} lb/MWh`;
        el.style.color = "#1a7a48";
      }
    } else if (bldgData.egridSubregion) {
      const d = EGRID_SUBREGIONS[bldgData.egridSubregion];
      if (d && el) {
        el.textContent = `${
          bldgData.egridSubregion
        } · ${d.lbsMWh.toLocaleString()} lb/MWh`;
        el.style.color = "#1a7a48";
      }
    }
    if (eb) {
      eb.style.display = "flex";
      eb.style.borderColor = "#1a7a48";
      eb.style.background = "#dcfce7";
    }
  }
  // Restore project type buttons
  if (bldgData.projectType) syncProjectTypeButtons();
  // Restore HVAC type button
  if (bldgData.hvacType) {
    const lbl = document.getElementById("bldgHvacLabel");
    const btn = document.getElementById("bldgHvacBtn");
    if (lbl) {
      lbl.textContent = bldgData.hvacType;
      lbl.style.color = "#1a2a3a";
    }
    if (btn) {
      btn.style.borderColor = "#2563a8";
      btn.style.background = "#e8f0fb";
    }
  }
  // Restore Emergency Lighting button
  if (bldgData.emLighting) {
    const lbl = document.getElementById("bldgEmLabel");
    const btn = document.getElementById("bldgEmBtn");
    if (lbl) {
      lbl.textContent = bldgData.emLighting;
      lbl.style.color = "#1a2a3a";
    }
    if (btn) {
      btn.style.borderColor = "#2563a8";
      btn.style.background = "#e8f0fb";
    }
  }
  // Restore Climate Zone button
  if (typeof restoreClimateZoneButton === "function")
    restoreClimateZoneButton();
  updateHoursIndicator();
}

function bldgUpdateStats() {
  const groups = groupLogByRoom ? groupLogByRoom() : [];
  const rooms = groups.length;
  const fixtures = logEntries.reduce((s, e) => s + (e.qty || 0), 0);
  const withCtrl = groups.filter((g) =>
    g.entries.some(({ e }) => e.ctrlRecs && e.ctrlRecs.length),
  ).length;
  const statsEl = document.getElementById("bldgAuditStats");
  statsEl.style.display = rooms > 0 ? "block" : "none";
  document.getElementById("bldgStatRooms").textContent = rooms;
  document.getElementById("bldgStatFix").textContent = fixtures;
  document.getElementById("bldgStatCtrl").textContent = withCtrl;
  const btn = document.getElementById("bldgStartBtn");
  btn.textContent = rooms > 0 ? "Continue Audit →" : "Start Audit →";
  btn.className = "bldg-start-btn " + (rooms > 0 ? "continue" : "start");
}

function bldgStartAudit() {
  bldgSaveDraft();
  if (!bldgData.name) {
    toast("Enter a building name first");
    document.getElementById("bldgName")?.focus();
    return;
  }
  closeBuildingPage();
  openFloorsPage();
}

// ── AUTO-SAVE (localStorage) ──────────────────────────────────────────────
const SAVE_KEY = "auditTool_willdan_v1";
let autoSaveTimer = null;

function saveToStorage() {
  try {
    // Snapshot current room into roomDrafts so it persists alongside other-room
    // drafts. (draftState below is kept for backward compat with old sessions.)
    if (typeof snapshotCurrentRoomDraft === "function")
      snapshotCurrentRoomDraft();
    const payload = {
      logEntries,
      customFixtures,
      roomTemplates,
      roomDrafts,
      // Save current in-progress form state so nothing is lost on reload
      draftState:
        state.fixtures.length || state.room
          ? {
              room: state.room,
              height: state.height,
              roomtype: state.roomtype,
              ceilingType: state.ceilingType,
              dimensions: state.dimensions,
              fixtures: state.fixtures.map((f) => ({ ...f })),
              switchType: state.switchType.map((s) => ({ ...s })),
              existingCtrls: state.existingCtrls.map((x) => ({ ...x })),
              ctrls: state.ctrls.map((x) => ({ ...x })),
              notes: document.getElementById("notesInput")?.value || "",
            }
          : null,
      savedAt: new Date().toISOString(),
    };
    localStorage.setItem(SAVE_KEY, JSON.stringify(payload));
    showSaveIndicator("✔ Saved");
  } catch (e) {
    showSaveIndicator("⚠ Save failed");
  }
}

function scheduleSave() {
  clearTimeout(autoSaveTimer);
  autoSaveTimer = setTimeout(saveToStorage, 800); // debounce 800ms
}

function showSaveIndicator(msg) {
  let el = document.getElementById("saveIndicator");
  if (!el) return;
  el.textContent = msg;
  el.style.opacity = "1";
  clearTimeout(el._hideTimer);
  el._hideTimer = setTimeout(() => {
    el.style.opacity = "0";
  }, 2500);
}

function loadFromStorage() {
  try {
    const raw = localStorage.getItem(SAVE_KEY);
    if (!raw) return false;
    const payload = JSON.parse(raw);
    if (!payload.logEntries?.length) return false;

    const savedAt = new Date(payload.savedAt);
    const timeStr = savedAt.toLocaleString("en-US", {
      month: "short",
      day: "numeric",
      hour: "numeric",
      minute: "2-digit",
    });

    // Show restore prompt
    const banner = document.createElement("div");
    banner.id = "restoreBanner";
    banner.style.cssText =
      "position:fixed;bottom:0;left:0;right:0;background:#1a3a6a;color:#fff;padding:12px 16px;display:flex;align-items:center;justify-content:space-between;gap:10px;z-index:999;font-family:inherit;box-shadow:0 -4px 16px rgba(0,0,0,.3)";
    banner.innerHTML = `
      <div>
        <div style="font-size:13px;font-weight:700">📂 Unsaved session found</div>
        <div style="font-size:11px;color:#a0c4e8;margin-top:2px">${payload.logEntries.length} entries from ${timeStr}</div>
      </div>
      <div style="display:flex;gap:8px;flex-shrink:0">
        <button onclick="restoreSession()" style="padding:8px 16px;background:#2563a8;color:#fff;border:none;border-radius:7px;font-size:13px;font-weight:700;cursor:pointer;font-family:inherit">Restore</button>
        <button onclick="discardSession()" style="padding:8px 12px;background:transparent;color:#a0c4e8;border:1px solid #3a6a9a;border-radius:7px;font-size:12px;cursor:pointer;font-family:inherit">Discard</button>
      </div>`;
    document.body.appendChild(banner);

    // Stash payload for restore
    window._pendingRestore = payload;
    return true;
  } catch (e) {
    return false;
  }
}

function restoreSession() {
  const payload = window._pendingRestore;
  if (!payload) return;
  logEntries = payload.logEntries || [];
  customFixtures = payload.customFixtures || [];
  roomTemplates = payload.roomTemplates || [];
  roomDrafts = payload.roomDrafts || {};
  updateLogBadge();
  buildCategoryPills();
  renderTemplatePills();
  document.getElementById("restoreBanner")?.remove();
  // Restore in-progress form draft if present
  const draft = payload.draftState;
  if (draft) {
    state.room = draft.room || "";
    state.height = draft.height || "";
    state.roomtype = draft.roomtype || "";
    state.ceilingType = draft.ceilingType || "";
    state.dimensions = draft.dimensions || null;
    state.fixtures = (draft.fixtures || []).map((f) => ({ ...f }));
    state.switchType = (draft.switchType || []).map((s) => ({ ...s }));
    state.existingCtrls = (draft.existingCtrls || []).map((x) => ({ ...x }));
    state.ctrls = (draft.ctrls || []).map((x) => ({ ...x }));
    if (draft.room) {
      document.getElementById("roomInput").value = draft.room;
      updateRoomDisplay();
    }
    if (draft.notes) {
      document.getElementById("notesInput").value = draft.notes;
      if (typeof syncNotesDisplay === "function") syncNotesDisplay();
    }
    if (draft.height) updateHeightBtn(draft.height);
    if (draft.ceilingType) updateCeilingTypeBtn(draft.ceilingType);
    renderChips();
    renderSwitchQtyPanel();
    renderExistingCtrlQtyPanel();
    renderCtrlQtyPanel();
    if (draft.fixtures?.length)
      toast(`✔ Restored ${logEntries.length} entries + in-progress form`);
    else toast(`✔ Restored ${logEntries.length} log entries`);
  } else {
    toast(`✔ Restored ${logEntries.length} log entries`);
  }
  delete window._pendingRestore;
}

function discardSession() {
  localStorage.removeItem(SAVE_KEY);
  document.getElementById("restoreBanner")?.remove();
  delete window._pendingRestore;
}

function clearSavedSession() {
  localStorage.removeItem(SAVE_KEY);
}

function _bootApp() {
  init();
  updateSummary();
  initFpEvents();
  initMarkupEvents();
  initMinimapEvents();
  // Projects registry must load before anything touches the legacy keys, so a
  // first-time user gets their existing audit wrapped into a Default Project.
  loadProjectsRegistry();
  migrateLegacyIfNeeded();
  loadFromStorage();
  loadRoomPhotos();
  loadCatalogue();
  bldgBootstrap();
  updateProjectsCrumb();
  updateHoursIndicator();
  // Migrate any legacy base64 photos to IndexedDB blobs (runs async in background)
  if (window.photoStore && window.auditStorage) {
    window.photoStore.migratePhotosToBlobs(roomPhotos, window.auditStorage)
      .then(({ changed }) => { if (changed) saveRoomPhotos(); })
      .catch(() => {});
  }
}
if (document.readyState === "loading") {
  document.addEventListener("DOMContentLoaded", _bootApp);
} else {
  _bootApp();
}

// ═════════════════════════════════════════════════════════════════════════
// PROJECTS REGISTRY
// ═════════════════════════════════════════════════════════════════════════
// One project contains many buildings. Each building's data still lives in
// the legacy "active" localStorage keys (BLDG_KEY, FLOORS_KEY, SAVE_KEY, etc.)
// so existing load/save code keeps working unchanged. The registry tracks the
// project tree + holds snapshots of non-active buildings; switching buildings
// archives the active keys into the current building's snapshot and restores
// the target building's snapshot back into the active keys.
const PROJECTS_KEY = "auditTool_projects_v1";
let projectsRegistry = {
  projects: [], // [{id, name, createdAt, buildings: [{id, name, createdAt, snapshot: null}]}]
  activeProjectId: null,
  activeBuildingId: null,
  schemaVersion: 1,
};

function _uuid() {
  return (
    "id_" +
    Date.now().toString(36) +
    "_" +
    Math.random().toString(36).slice(2, 8)
  );
}

function loadProjectsRegistry() {
  try {
    const raw = localStorage.getItem(PROJECTS_KEY);
    if (raw) projectsRegistry = { ...projectsRegistry, ...JSON.parse(raw) };
  } catch (e) {}
}
function saveProjectsRegistry() {
  try {
    localStorage.setItem(PROJECTS_KEY, JSON.stringify(projectsRegistry));
  } catch (e) {}
}

function _currentProject() {
  return (
    projectsRegistry.projects.find(
      (p) => p.id === projectsRegistry.activeProjectId,
    ) || null
  );
}
function _currentBuilding() {
  const p = _currentProject();
  return (
    p?.buildings.find((b) => b.id === projectsRegistry.activeBuildingId) || null
  );
}

// First-boot migration: if there's no registry yet but old legacy keys exist,
// wrap that data into a "Default Project / Building 1" so the user's prior
// audits don't disappear when the projects layer ships.
function migrateLegacyIfNeeded() {
  if (projectsRegistry.projects.length > 0) return;
  const legacyBldg = localStorage.getItem(BLDG_KEY);
  const legacyFloors = localStorage.getItem(FLOORS_KEY);
  const legacySession = localStorage.getItem(SAVE_KEY);
  const hasLegacy = !!(legacyBldg || legacyFloors || legacySession);

  const projectId = _uuid();
  const buildingId = _uuid();
  let buildingName = "Building 1";
  if (hasLegacy && legacyBldg) {
    try {
      buildingName = (JSON.parse(legacyBldg).name || "").trim() || "Building 1";
    } catch (_) {}
  }
  projectsRegistry.projects.push({
    id: projectId,
    name: hasLegacy ? "Default Project" : "My Project",
    createdAt: new Date().toISOString(),
    buildings: [
      {
        id: buildingId,
        name: buildingName,
        createdAt: new Date().toISOString(),
        snapshot: null,
      },
    ],
  });
  projectsRegistry.activeProjectId = projectId;
  projectsRegistry.activeBuildingId = buildingId;
  saveProjectsRegistry();
}

// Snapshot the active keys into the current building's slot. Called before
// switching to a different building so the leaving-building's state survives.
function archiveCurrentBuilding() {
  const b = _currentBuilding();
  if (!b) return;
  // Make sure any in-flight session state is committed first
  if (typeof saveToStorage === "function") saveToStorage();
  if (typeof saveRoomPhotos === "function") saveRoomPhotos();
  if (typeof saveCatalogue === "function") saveCatalogue();
  b.snapshot = {
    bldg: localStorage.getItem(BLDG_KEY) || null,
    floors: localStorage.getItem(FLOORS_KEY) || null,
    session: localStorage.getItem(SAVE_KEY) || null,
    photos: localStorage.getItem("auditTool_photos_v1") || null,
    catalogue: localStorage.getItem("auditTool_catalogue_v1") || null,
  };
  b.name = (bldgData?.name || b.name || "Building").trim() || "Building";
  saveProjectsRegistry();
}

// Restore a building's snapshot back into the active keys (overwrites whatever
// is currently in localStorage). After this, call bldgBootstrap-style reloads
// so in-memory state matches.
function restoreBuildingToActive(buildingId) {
  const proj = _currentProject();
  const b = proj?.buildings.find((x) => x.id === buildingId);
  if (!b) return false;
  const snap = b.snapshot || {};
  const set = (k, v) => {
    if (v == null) localStorage.removeItem(k);
    else localStorage.setItem(k, v);
  };
  set(BLDG_KEY, snap.bldg);
  set(FLOORS_KEY, snap.floors);
  set(SAVE_KEY, snap.session);
  set("auditTool_photos_v1", snap.photos);
  set("auditTool_catalogue_v1", snap.catalogue);
  projectsRegistry.activeBuildingId = buildingId;
  saveProjectsRegistry();
  return true;
}

// Public switcher used by the project page when the user taps a building.
function switchToBuilding(projectId, buildingId) {
  if (!projectId || !buildingId) return;
  if (
    projectsRegistry.activeProjectId === projectId &&
    projectsRegistry.activeBuildingId === buildingId
  ) {
    closeProjectsPage();
    return; // already active
  }
  archiveCurrentBuilding();
  projectsRegistry.activeProjectId = projectId;
  restoreBuildingToActive(buildingId);
  // Hard reload in-memory state from the freshly-restored active keys
  _reloadActiveBuildingInMemory();
  closeProjectsPage();
  if (!bldgData.name) openBuildingPage();
  else if (!activeFloor) openFloorsPage();
}

function _reloadActiveBuildingInMemory() {
  // Reset in-memory state that depends on the active building
  bldgData = {
    name: "",
    address: "",
    client: "",
    auditor: "",
    type: "",
    sqft: "",
    year: "",
    utility: "",
    hours: 2000,
    notes: "",
  };
  floors = [];
  activeFloor = null;
  logEntries = [];
  customFixtures = [];
  roomTemplates = [];
  roomDrafts = {};
  roomPhotos = {};
  catalogueItems = [];
  catalogueBuilding = "";
  state.fixtures = [];
  state.switchType = [];
  state.existingCtrls = [];
  state.ctrls = [];
  state.swRec = [];
  state.fixRec = [];
  state.room = "";
  state.height = "";
  state.roomtype = "";
  state.ceilingType = "";
  state.dimensions = null;
  state.activeFixture = "";
  // Now re-load from the (just-restored) active keys
  if (typeof loadFromStorage === "function") loadFromStorage();
  if (typeof loadRoomPhotos === "function") loadRoomPhotos();
  if (typeof loadCatalogue === "function") loadCatalogue();
  // bldgData + floors get loaded in bldgBootstrap below
  try {
    const raw = localStorage.getItem(BLDG_KEY);
    if (raw) bldgData = { ...bldgData, ...JSON.parse(raw) };
  } catch (_) {}
  if (typeof floorsLoad === "function") floorsLoad();
  if (typeof renderFloorStrip === "function") renderFloorStrip();
  if (typeof updateTopCrumb === "function") updateTopCrumb();
  if (typeof renderAuditMinimap === "function") renderAuditMinimap();
  updateProjectsCrumb();
}

function createNewProject() {
  const name = (prompt("Project name?", "New Project") || "").trim();
  if (!name) return;
  const projectId = _uuid();
  const buildingId = _uuid();
  projectsRegistry.projects.push({
    id: projectId,
    name,
    createdAt: new Date().toISOString(),
    buildings: [
      {
        id: buildingId,
        name: "Building 1",
        createdAt: new Date().toISOString(),
        snapshot: null,
      },
    ],
  });
  saveProjectsRegistry();
  switchToBuilding(projectId, buildingId);
}

function createNewBuilding(projectId) {
  const proj = projectsRegistry.projects.find((p) => p.id === projectId);
  if (!proj) return;
  const name = (
    prompt("Building name?", "Building " + (proj.buildings.length + 1)) || ""
  ).trim();
  if (!name) return;
  const buildingId = _uuid();
  proj.buildings.push({
    id: buildingId,
    name,
    createdAt: new Date().toISOString(),
    snapshot: null,
  });
  saveProjectsRegistry();
  switchToBuilding(projectId, buildingId);
}

function deleteProject(projectId) {
  const proj = projectsRegistry.projects.find((p) => p.id === projectId);
  if (!proj) return;
  if (
    !confirm(
      `Delete project "${proj.name}" and all its buildings? This cannot be undone.`,
    )
  )
    return;
  projectsRegistry.projects = projectsRegistry.projects.filter(
    (p) => p.id !== projectId,
  );
  // If we deleted the active project, point to whatever's left (or null)
  if (projectsRegistry.activeProjectId === projectId) {
    const fallback = projectsRegistry.projects[0];
    projectsRegistry.activeProjectId = fallback?.id || null;
    projectsRegistry.activeBuildingId = fallback?.buildings[0]?.id || null;
    if (fallback?.buildings[0])
      restoreBuildingToActive(fallback.buildings[0].id);
    _reloadActiveBuildingInMemory();
  }
  saveProjectsRegistry();
  renderProjectsPage();
}

function deleteBuilding(projectId, buildingId) {
  const proj = projectsRegistry.projects.find((p) => p.id === projectId);
  if (!proj) return;
  const b = proj.buildings.find((x) => x.id === buildingId);
  if (!b) return;
  if (proj.buildings.length <= 1) {
    toast(
      "Can't delete the last building in a project — delete the project instead",
    );
    return;
  }
  if (!confirm(`Delete building "${b.name}"? This cannot be undone.`)) return;
  proj.buildings = proj.buildings.filter((x) => x.id !== buildingId);
  if (projectsRegistry.activeBuildingId === buildingId) {
    const fallback = proj.buildings[0];
    if (fallback) {
      restoreBuildingToActive(fallback.id);
      _reloadActiveBuildingInMemory();
    }
  }
  saveProjectsRegistry();
  renderProjectsPage();
}

function renameProject(projectId) {
  const proj = projectsRegistry.projects.find((p) => p.id === projectId);
  if (!proj) return;
  const next = (prompt("Project name", proj.name) || "").trim();
  if (!next) return;
  proj.name = next;
  saveProjectsRegistry();
  renderProjectsPage();
  updateProjectsCrumb();
}

function openProjectsPage() {
  renderProjectsPage();
  document.getElementById("projectsPage").classList.add("visible");
  document.getElementById("projectsPage").style.display = "";
}
function closeProjectsPage() {
  document.getElementById("projectsPage").classList.remove("visible");
  document.getElementById("projectsPage").style.display = "none";
}

// Helper: read floors for a building. Active building uses the in-memory list;
// non-active buildings read from the archived snapshot in the registry.
function _floorsForBuilding(building, isActive) {
  if (isActive) return typeof floors !== "undefined" && floors ? floors : [];
  try {
    if (building.snapshot?.floors) {
      const parsed = JSON.parse(building.snapshot.floors);
      if (Array.isArray(parsed)) return parsed;
    }
  } catch (_) {}
  return [];
}

// Pill-click navigation: switch building if needed, then go to a destination.
function _switchIfNeeded(projectId, buildingId) {
  if (
    projectsRegistry.activeBuildingId === buildingId &&
    projectsRegistry.activeProjectId === projectId
  )
    return false;
  archiveCurrentBuilding();
  projectsRegistry.activeProjectId = projectId;
  restoreBuildingToActive(buildingId);
  _reloadActiveBuildingInMemory();
  return true;
}

function projectPillToProfile(projectId, buildingId) {
  _switchIfNeeded(projectId, buildingId);
  closeProjectsPage();
  if (typeof openBuildingPage === "function") openBuildingPage();
}

function projectPillToFloor(projectId, buildingId, floorId) {
  _switchIfNeeded(projectId, buildingId);
  closeProjectsPage();
  const fl = (floors || []).find((f) => f.id === floorId);
  if (fl && typeof floorEnterAudit === "function") floorEnterAudit(fl.id);
}

function renderProjectsPage() {
  const list = document.getElementById("projectsList");
  if (!list) return;
  const esc = (s) =>
    String(s).replace(
      /[&<>"']/g,
      (c) =>
        ({
          "&": "&amp;",
          "<": "&lt;",
          ">": "&gt;",
          '"': "&quot;",
          "'": "&#39;",
        }[c]),
    );
  if (!projectsRegistry.projects.length) {
    list.innerHTML = `<div style="padding:24px 16px;text-align:center;color:#7a8fa8;font-size:13px">No projects yet — tap <b>+ New Project</b> above.</div>`;
    return;
  }
  list.innerHTML = projectsRegistry.projects
    .map((proj) => {
      const isActiveProj = proj.id === projectsRegistry.activeProjectId;
      const buildingsHtml = proj.buildings
        .map((b) => {
          const isActiveB =
            isActiveProj && b.id === projectsRegistry.activeBuildingId;
          const flrs = _floorsForBuilding(b, isActiveB);
          // Pill styling: profile pill = blue, floor pills = neutral
          const profilePill = `<button onclick="projectPillToProfile('${proj.id}','${b.id}')" style="background:#e8f0fb;color:#1a4a80;border:1.5px solid #2563a8;border-radius:14px;padding:5px 11px;font-size:11px;font-weight:700;cursor:pointer;font-family:inherit;white-space:nowrap">🏢 Profile</button>`;
          const floorPills = flrs
            .map(
              (f) =>
                `<button onclick="projectPillToFloor('${proj.id}','${b.id}','${
                  f.id
                }')" style="background:#f7fbff;color:#3a6090;border:1.5px solid #c8d4e3;border-radius:14px;padding:5px 11px;font-size:11px;font-weight:600;cursor:pointer;font-family:inherit;white-space:nowrap">${esc(
                  f.name,
                )}</button>`,
            )
            .join("");
          return `<div style="display:flex;align-items:center;gap:8px;padding:9px 10px;background:${
            isActiveB ? "#e8f4f0" : "#fff"
          };border:1px solid ${
            isActiveB ? "#2a7a50" : "#c8d4e3"
          };border-radius:8px;flex-wrap:wrap">
        <div style="flex:1;min-width:140px">
          <div style="font-size:13px;font-weight:700;color:#1a2a3a;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">${esc(
            b.name,
          )}${
            isActiveB
              ? ' <span style="color:#2a7a50;font-size:10px;font-weight:600">(active)</span>'
              : ""
          }</div>
        </div>
        <div style="display:flex;gap:5px;flex-wrap:wrap;align-items:center">${profilePill}${floorPills}</div>
        <button onclick="deleteBuilding('${proj.id}','${
            b.id
          }')" title="Delete building" style="background:transparent;color:#c04040;font-size:14px;border:none;cursor:pointer;padding:4px 6px;line-height:1">🗑</button>
      </div>`;
        })
        .join("");
      return `<div style="background:#f7fbff;border:1.5px solid ${
        isActiveProj ? "#2563a8" : "#c8d4e3"
      };border-radius:10px;padding:12px">
      <div style="display:flex;align-items:center;gap:8px;margin-bottom:10px">
        <div style="flex:1;min-width:0">
          <div style="font-size:15px;font-weight:700;color:#1a2a3a;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">${esc(
            proj.name,
          )}</div>
          <div style="font-size:10px;color:#7a8fa8;margin-top:1px">${
            proj.buildings.length
          } building${proj.buildings.length === 1 ? "" : "s"}${
        isActiveProj ? " · active" : ""
      }</div>
        </div>
        <button onclick="renameProject('${
          proj.id
        }')" title="Rename" style="background:transparent;color:#5a8ab0;font-size:13px;border:1px solid #c8d4e3;border-radius:10px;padding:4px 8px;cursor:pointer;font-family:inherit">✏</button>
        <button onclick="deleteProject('${
          proj.id
        }')" title="Delete project" style="background:transparent;color:#c04040;font-size:14px;border:1px solid #fcc;border-radius:10px;padding:4px 8px;cursor:pointer;font-family:inherit">🗑</button>
      </div>
      <div style="display:flex;flex-direction:column;gap:6px;margin-bottom:8px">${buildingsHtml}</div>
      <button onclick="createNewBuilding('${
        proj.id
      }')" style="width:100%;background:#fff;border:1.5px dashed #c8d4e3;color:#5a7a9a;border-radius:8px;padding:8px;font-size:12px;font-weight:700;cursor:pointer;font-family:inherit">+ Add Building</button>
    </div>`;
    })
    .join("");
}

function updateProjectsCrumb() {
  const proj = _currentProject();
  const el = document.getElementById("topProjectName");
  if (el) el.textContent = proj?.name || "Project";
}

function bldgBootstrap() {
  // Load building data and update top bar
  try {
    const raw = localStorage.getItem(BLDG_KEY);
    if (raw) {
      const d = JSON.parse(raw);
      bldgData = { ...bldgData, ...d };
      const topName = document.getElementById("topBldgName");
      if (topName && d.name) topName.textContent = d.name;
    }
  } catch (e) {}
  // Load floors
  floorsLoad();
  // Show building page first if no building name set yet
  if (!bldgData.name) {
    openBuildingPage();
  } else if (!activeFloor) {
    // Have building but no active floor — go straight to floors page
    openFloorsPage();
  }
  if (typeof renderAuditMinimap === "function") renderAuditMinimap();
}
